# Allow .expect() and .unwrap() in test code; production must use ? +
# typed errors. See .claude/rules/rust/rust-error-handling.md and
# rust-testing.md.
allow-expect-in-tests = true
allow-unwrap-in-tests = true
