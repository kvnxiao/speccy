# speccy-core

Library crate for the `speccy` CLI. Provides deterministic parsers for Speccy's artifact files, cross-reference utilities, and a workspace-wide supersession index.

See `docs/ARCHITECTURE.md` for the canonical contract.
