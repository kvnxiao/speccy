---
spec: SPEC-0037
task: T-006
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-21T22:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Updated `resources/modules/skills/speccy-amend.md` for
  REQ-008 amend side. Rewrote step 4 of the amend-flow prose to name
  `journal/T-NNN.md` write target, use the `<blockers>` element with
  required `date` / `round` attributes, document the round-selection
  rule (`round = N+1` from highest prior round across `<implementer>`
  / `<review>` / `<blockers>`; `round = 1` if no prior journal exists;
  create journal file with frontmatter if absent), preserve the
  amendment-driven body directive semantics, and preserve the
  `completed` → `pending` state-flip semantics. Also swept the
  pre-existing stale `<retry>` / "consolidated retry note" wording in
  `resources/modules/personas/verdict_return_contract.md:5-7` (T-005
  forward observation) — replaced with `<blockers>` aggregation
  appended to `journal/T-NNN.md`. Confirmed via grep that
  `resources/agents/.claude/skills/speccy-amend/SKILL.md.tmpl` and
  `resources/agents/.agents/skills/speccy-amend/SKILL.md.tmpl`
  frontmatter contain no `<retry>` references; no edits needed there.

- Undone: T-007 sweep still pending.

- Commands run: Delete materialized speccy-amend files AND all six
  reviewer materializations (because the `verdict_return_contract.md`
  sweep cascades into all six reviewer files) + `cargo clean -p
  speccy-cli` + `speccy init --force` for both hosts. Plus four
  hygiene gates. Plus `dogfood_outputs_match_committed_tree` to confirm
  host-pack drift = 0.

- Exit codes: cargo test 0, cargo clippy 0, cargo +nightly fmt --check
  0, cargo deny check 0. After the drift-dance wipe and first `init
  --force` per host (7 created, 12 unchanged each), the second `init
  --force` run for both `claude-code` and `codex` reports `0 created,
  0 overwritten, 19 unchanged` (idempotent).

- Discovered issues: First `cargo test` failed
  `dogfood_outputs_match_committed_tree` because committed
  `.codex/agents/reviewer-*.toml` and `.claude/agents/reviewer-*.md`
  files still carried the stale `verdict_return_contract` prose. Fixed
  by deleting the full set of six reviewer materializations alongside
  the amend SKILL.md files before re-init. Lesson for T-007: when
  sweeping a shared partial (like `verdict_return_contract.md`), all
  six reviewer materializations are affected, not just the
  speccy-amend ones — wipe the full set up front.

- Procedural compliance: Touched only `resources/modules/skills/speccy-amend.md`,
  `resources/modules/personas/verdict_return_contract.md`, and the
  materialized speccy-amend SKILL.md files + the six reviewer
  materializations (regenerated, not hand-edited). Did not modify
  Rust source, speccy-work.md (T-004), speccy-review.md (T-005), or
  any other spec.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T22:30:00Z" round="1">
T-006 lands the REQ-008 amend-side update cleanly. Every
task-scenario resolves against the diff: names
`.speccy/specs/NNNN-slug/journal/T-NNN.md` as the write target for
amendment-driven blocker entries; uses the renamed `<blockers>`
element (legacy `<retry>` retired); names exactly two required
attributes (`date`, `round`) matching REQ-003's `<blockers>` attribute
schema; documents the `round = N+1` rule and the `round = 1` fallback
when no prior journal exists, plus frontmatter creation for the
first-touch case; preserves the `completed` → `pending` state-flip
semantics. The `verdict_return_contract.md:5-8` sweep replaces the
stale "consolidated retry note" wording with "the consolidated
`<blockers>` element it appends to `journal/T-NNN.md`", matching
T-005's `<blockers>`/journal model and T-003's transcription contract.
Materialized host packs at `.claude/skills/speccy-amend/SKILL.md` and
`.agents/skills/speccy-amend/SKILL.md` carry the same body verbatim.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T22:30:00Z" round="1">
All six T-006 task-scenarios verified against the diff. `journal/T-NNN.md`
named as the write target. Zero `retry` matches anywhere in
`speccy-amend.md` or `verdict_return_contract.md` (case-insensitive
grep). Required attributes `date`, `round` and the `round = N+1` rule
documented. State-flip semantics preserved verbatim. Materialized
SKILL.md files reflect source verbatim with only templated
`cmd_prefix` substitution differing. The verdict_return_contract.md
sweep flowed through to all 12 reviewer materializations (6
`.claude/agents/reviewer-*.md` + 6 `.codex/agents/reviewer-*.toml`),
each carrying "consolidated `<blockers>` element" replacing the legacy
"consolidated retry note". `dogfood_outputs_match_committed_tree`
passes locally, gating host-pack drift. The implementer's
red-then-green paper trail (initial test failure → delete all six +
`cargo clean` + re-init → green) is a real coupling lesson, not a
fabrication pattern.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T22:30:00Z" round="1">
No security-blocking findings. The diff is prose-only: no credentials,
tokens, secrets, or internal hostnames appear in any modified file.
The journal write-path (`journal/T-NNN.md`) named in `speccy-amend.md`
step 4 is a safe relative path — `T-NNN` resolves from a task ID
already validated by the parser's `^T-\d{3,}$` regex (established in
T-001), so no path traversal vector is introduced. The `round = N+1`
rule describes a natural-language instruction to a skill agent to read
existing block attributes and increment; no unsafe parsing or shell
execution is implied.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T22:30:00Z" round="1">
All style checks pass. `<retry>` rename is total across
`resources/modules/`. Grep for `<retry` returns no matches anywhere
under `resources/modules/` — the rename is complete.
`verdict_return_contract.md` lede now correctly reads "the
consolidated `<blockers>` element it appends to `journal/T-NNN.md`";
the stale `<retry>` / "consolidated retry note" wording is gone.
`speccy-amend.md` heading structure matches `speccy-work.md` and
`speccy-review.md` exactly. Prose voice is imperative,
noun-phrase-leading throughout. `<blockers>` example in step 4 carries
both required `date` and `round` attributes. The `round = N+1` rule
documentation is internally consistent with ARCHITECTURE.md's
monotonic round semantics. `verdict_return_contract.md` sweep
maintains style consistency with the T-003 sections.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T22:30:00Z" round="1">
All six T-006 instructions are explicit and unambiguous.
`speccy-amend.md` step 4 names the journal path, renames `<retry>` to
`<blockers>`, documents `date` and `round` as required attributes
(matching ARCHITECTURE.md exactly), gives the full `round = N+1` /
`round = 1` fallback rule, preserves amendment-driven body directive
semantics, and preserves the `completed` → `pending` state-flip
unchanged. `verdict_return_contract.md` post-sweep adds `model="..."`
to the `<review>` element signature, replaces stale `<retry>` /
"consolidated retry note" wording with `<blockers>` + journal path,
and is fully coherent with T-005's orchestrator model and T-003's
`model` requirement. Cross-coherence with ARCHITECTURE.md,
`speccy-review.md`, and T-004 holds throughout. One non-blocking
forward-reference in step 4 lede (example uses `round="N+1"` before
the variable is defined two sentences later) is too small to mislead
a future amender subagent.
</review>
</content>
</invoke>
