---
spec: SPEC-0037
task: T-005
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-21T21:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Updated `resources/modules/skills/speccy-review.md` for
  REQ-008 reviewer side. Rewrote the body so the orchestrator writes
  `<review>` to `journal/T-NNN.md` (not TASKS.md), preserves the
  DEC-008 serial-writer contract (reviewers return blocks, orchestrator
  writes serially), mandates verbatim `model` transcription per
  `verdict_return_contract.md` with halt-on-missing-model behavior,
  names the full `<review>` attribute set (`date`, `model`, `persona`,
  `verdict`, `round` — all five required), replaces `<retry>` with
  `<blockers>` (renamed to use `date` and `round` attrs), and updates
  both spawn-prompt example snippets (claude-code branch + codex
  branch) to include `model="..."`. Also updated
  `resources/agents/.claude/skills/speccy-review/SKILL.md.tmpl` and
  `resources/agents/.agents/skills/speccy-review/SKILL.md.tmpl`
  frontmatter `description:` strings to drop `<retry>` references —
  these `.tmpl` files are independent of the included module body
  and required separate edits.

- Undone: T-006 still pending (speccy-amend.md `<retry>` → `<blockers>`
  rename). T-007 sweep also pending.

- Commands run: Delete materialized speccy-review files + `cargo clean
  -p speccy-cli` + `speccy init --force` for both hosts. Plus four
  hygiene gates.

- Exit codes: cargo test 0, cargo clippy 0, cargo +nightly fmt --check
  0, cargo deny check 0. Second `init --force` for each host returns
  `0 created, 0 overwritten, 19 unchanged` (idempotent).

- Discovered issues: SKILL.md `.tmpl` frontmatter `description:`
  strings are independent of the included module body and required
  separate edits to drop `<retry>`. Forward signal for T-006: the
  `speccy-amend` SKILL templates likely have the same pattern and
  should be grepped + updated in lockstep.

- Procedural compliance: Touched only the named files. Did not modify
  Rust source, other skill bodies, or any other spec.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T21:30:00Z" round="1">
T-005 REQ-008 reviewer-side done-when fully satisfied. Every
reviewer-side bullet in REQ-008's done-when maps to the diff in
`resources/modules/skills/speccy-review.md`. Names `journal/T-NNN.md`
as write target. Preserves DEC-008 concurrency contract — reviewers
return blocks, orchestrator writes serially. Cites the verbatim-`model`
transcription rule with no-substitute clause per T-003's contract.
Names all five required attributes. Removes references to writing
`<review>` into TASKS.md (explicitly prohibited with TSK-006
cross-reference). Spawn-prompt example snippets include `model` in
both host branches. `<retry>` → `<blockers>` rename complete. The two
`.tmpl` frontmatter edits are in-scope-by-necessity given REQ-005's
`<retry>` retirement.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T21:30:00Z" round="1">
All six T-005 task-scenarios materially satisfied and reproducible
from the working tree. `resources/modules/skills/speccy-review.md`
names `journal/T-NNN.md` 8 times. Legacy "append `<review>` to
`<task>` body in TASKS.md" prose is gone — every TASKS.md reference
is either a state-attribute write site or an explicit "do not write"
instruction. DEC-008 serial-write preserved. Verbatim `model`
documented with halt-on-missing. Required attribute set documented
(date, model, persona, verdict, round). No `<retry>` references in
live workflow prose; both `.tmpl` frontmatter `description:` fields
updated. Idempotency: `speccy init --force` reports `0 / 0 / 19
unchanged` for both hosts. Four hygiene gates exit 0.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T21:30:00Z" round="1">
Prose-only change across `speccy-review.md` and two SKILL.md.tmpl
files. No blocking findings. (1) No credentials, tokens, API keys,
internal hostnames, or PII. (2) Verbatim-model transcription — no
injection vector. The T-001 parser validates `model` as non-empty
only; the string is never executed, shell-interpolated, or
template-evaluated. (3) DEC-008 serial-writer contract correctly
documented: the orchestrator is the sole journal writer during review;
spawn-prompt text explicitly tells each reviewer subagent "Do not
edit TASKS.md and do not edit the journal file." (4) Halt-on-missing-model
is correctly defensive — prevents silent identity laundering.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T21:30:00Z" round="1">
All style checks pass. Voice and prose register match `speccy-work.md`
throughout: imperative, noun-phrase leading, same heading structure
and depth. `<retry>` rename is total in T-005's scope. Both `.tmpl`
`description:` fields updated in lockstep. Spawn-prompt example
snippets carry `model="..."` in both the claude-code and generic
branches. The abbreviated spawn-prompt form omits `date` and `round`,
consistent with sibling skill patterns; the orchestrator-side
transcription section explicitly enumerates all five required
attributes. `<blockers>` example carries `date` and `round` per the
ARCHITECTURE.md schema. TSK-006 reference is accurate. ARCHITECTURE.md
Phase 4, journal artifact section, and lint-code descriptions all
consistent. `speccy-amend.md` forward compatibility intact.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T21:30:00Z" round="1">
All seven forward observations from T-003's docs round-1 note are
resolved in `resources/modules/skills/speccy-review.md`. (1) Journal
file path named as write target. (2) DEC-008 concurrency contract
preserved with explicit name-call. (3) Verbatim model transcription
with explicit citation to `verdict_return_contract.md` and
halt-on-missing clause. (4) Required attributes enumerated with "All
five are required". (5) No TASKS.md write refs for `<review>` —
explicitly prohibited. (6) Spawn-prompt examples include `model` in
both branches. (7) `<retry>` → `<blockers>` complete: both `.tmpl`
frontmatter descriptions updated; both materialized SKILL.md files
consistent. Cross-coherence with ARCHITECTURE.md `# Review` section,
`speccy-work.md`, and `verdict_return_contract.md` (no contradiction
introduced). One pre-existing stale reference in
`verdict_return_contract.md` lede (`<retry>` / "retry note") is a
T-003 miss that T-005 did not introduce — forward observation for
T-006 to sweep during its rename work.
</review>
</content>
</invoke>
