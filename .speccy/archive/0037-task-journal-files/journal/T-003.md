---
spec: SPEC-0037
task: T-003
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-21T15:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Updated `resources/modules/personas/verdict_return_contract.md`
  per REQ-009. Added `model` to the `<review>` example in the lede.
  Added three new sections: "## The `model` attribute is required",
  "## Orchestrator-side transcription rule", "## No-substitute clause".
  Each section names the required behavior explicitly, includes
  slash-suffix encoding examples (e.g. `claude-opus-4.7[1m]/low`,
  `claude-sonnet-4.7/medium`), and documents the orchestrator
  verbatim-transcription rule and halt-on-missing behavior.
  Regenerated host packs via `speccy init --force` for both
  claude-code and codex hosts. Required `cargo clean -p speccy-cli` to
  force `include_dir!` recompile, plus deletion of the materialized
  reviewer files prior to re-init because SPEC-0027's Skip-on-exists
  semantics prevent in-place overwrite.

- Undone: T-004, T-005, T-006 still pending; their skill bodies are
  separate work units. T-007 sweep also pending.

- Commands run: `cargo test --workspace --all-features --locked` (0),
  `cargo clippy --workspace --all-targets --all-features --locked --
  -D warnings` (0), `cargo +nightly fmt --all --check` (0), `cargo
  deny check` (0). Plus `cargo run --bin speccy -- init --force` for
  both hosts twice (second run reports 0/0/19 unchanged, confirming
  idempotency).

- Exit codes: All four hygiene gates 0.

- Discovered issues: (1) SPEC-0027 REQ-002 Skip-on-exists for
  reviewer files means materialized files won't update without prior
  deletion. (2) `include_dir!` is a compile-time macro that Cargo
  doesn't invalidate when source resources change — requires `cargo
  clean -p speccy-cli` to force a rebuild. Both are known constraints,
  documented for future tasks touching `resources/modules/personas/*`.

- Procedural compliance: Touched only `resources/modules/personas/verdict_return_contract.md`
  and the materialized host-pack files (regenerated, not hand-edited).
  T-003 scope is contract-only; no skill-body changes made.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T16:00:00Z" round="1">
REQ-009's four contract clauses (required `model`, slash-suffix
encoding, verbatim transcription rule, no-substitute clause) all land
cleanly in `resources/modules/personas/verdict_return_contract.md`.
T-003's task-scenarios grep budget verified clean. Regen flowed
through to all 12 materialized reviewer files
(`reviewer-{architecture,business,docs,security,style,tests}` ×
{.claude,.codex}) with identical hunks. No business-scope regressions,
no non-goals violated (no `effort`/`host` attribute, no new wrapper,
slash-suffix on `model` matches DEC-003). Implementer-side contract
correctly untouched per REQ-009 done-when bullet 4. One non-blocking
forward observation for T-005's speccy-review.md scope (the lede's
`<retry>` body text phrasing is pre-existing and untouched by T-003).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-21T16:00:00Z" round="1">
T-003 satisfies all five task-scenarios cleanly. Required substrings
present in `verdict_return_contract.md`: `model`-attribute-required
section, slash-suffix convention with concrete examples, verbatim
transcription rule, no-substitute clause. The 12 materialized reviewer
files carry the same contract prose with `{{ persona_name }}`
correctly rendered per persona; second `speccy init --force` runs
report `0 created, 0 overwritten, 19 unchanged` for both hosts,
confirming source/pack sync. Four hygiene gates exit 0. The
`include_dir!` compile-time + SPEC-0027 Skip-on-exists workflow quirks
documented in the implementer note.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T16:00:00Z" round="1">
Pure prose/contract change. No Rust source changes, no new
dependencies, no secrets or credentials. The no-substitute clause is
correctly framed — orchestrator halts and surfaces a contract
violation rather than fabricating a model value when `model` is
absent. This is the correct defensive posture against silent identity
laundering. The verbatim transcription rule does not open an injection
vector — the `model` string flows into a static Markdown file on
disk; no shell execution or template rendering evaluates it as code.
`model` correctly not validated against a known-identifier list — the
slash-suffix is a documented convention, not a parser-enforced schema.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T16:00:00Z" round="1">
One blocking issue: `resources/modules/personas/inline_note_format.md:3`
and the hardcoded `## Example` blocks in each of the six
`resources/modules/personas/reviewer-*.md` source files (e.g.
`reviewer-style.md:82-86`) still show `<review>` elements without a
`model` attribute. These produce the stale format in the `## Inline
note format` and `## Example` sections of every generated reviewer
persona file. The `## Verdict return contract` section in the same
generated file — added by T-003 — immediately above these sections
now says `model` is required and non-optional, and the no-substitute
clause says a missing `model` is a hard error. A reviewer subagent
following the worked example would produce a non-conforming `<review>`
element that the no-substitute clause rejects.

Fix: update `inline_note_format.md` to add `model="..."` to the
example `<review>` element, and add `model="..."` to the `## Example`
block in each of the six `reviewer-*.md` source files. Re-run the
`cargo clean -p speccy-cli` + delete + `init --force` dance to
propagate.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T16:00:00Z" round="1">
`resources/modules/personas/verdict_return_contract.md` satisfies all
four contract clauses within T-003's scope. The lede example carries
`model="..."` in the attribute position. "## The `model` attribute is
required" names model as non-optional, explains the heterogeneous-persona
rationale, and provides concrete slash-suffix examples. "##
Orchestrator-side transcription rule" names `journal/T-NNN.md` as the
destination and says verbatim copy. "## No-substitute clause" makes
missing `model` a hard contract error. All regenerated reviewer agent
files inherit the prose correctly.

Two forward observations for T-005, not blocking T-003:
(1) `resources/modules/skills/speccy-review.md:62-65` and `102-125`
still describe the TASKS.md write path and `<retry>`. (2) The
spawn-prompt templates in `speccy-review.md:77-78` and `92` omit
`model` from the `<review>` example.
</review>

<blockers date="2026-05-21T17:00:00Z" round="2">
Style review blocks landing T-003 on one issue: the worked-example
`<review>` elements shipped in `resources/modules/personas/inline_note_format.md`
and in each of the six `resources/modules/personas/reviewer-{architecture,business,docs,security,style,tests}.md`
source files lack a `model="..."` attribute. The new `## Verdict
return contract` section that T-003 added — which sits in the same
generated reviewer file just above the examples — now says `model` is
required and the no-substitute clause makes a missing `model` a hard
contract violation. Following the worked examples would produce a
non-conforming `<review>` element.

Fixes required:

1. Edit `resources/modules/personas/inline_note_format.md`: add
   `model="..."` (e.g. `claude-opus-4.7[1m]/medium`) to the example
   `<review>` element.

2. Edit each of `resources/modules/personas/reviewer-{architecture,business,docs,security,style,tests}.md`:
   add a `model="..."` attribute to the example `<review>` element in
   the `## Example` block. Use persona-appropriate effort tiers.

3. Propagate to materialized host packs via delete +
   `cargo clean -p speccy-cli` + `speccy init --force` for both hosts.

4. Re-run all four hygiene gates and confirm exit 0.

Forward observations from docs persona (NOT blocking T-003 —
explicitly T-005's scope): `resources/modules/skills/speccy-review.md`
still describes the TASKS.md write path, references `<retry>`, and
omits `model` from spawn-prompt example snippets.
</blockers>

<implementer date="2026-05-21T18:00:00Z" model="claude-opus-4.7[1m]/low" round="2">
- Completed: All four round-2 blocker fixes landed.
  (1) `inline_note_format.md` example `<review>` now carries
  `model="claude-opus-4.7[1m]/medium"`.
  (2) The six `reviewer-*.md` source files updated with persona-appropriate
  model values: `reviewer-business.md` and `reviewer-architecture.md`
  use `claude-opus-4.7[1m]/high`; `reviewer-docs.md`,
  `reviewer-security.md`, `reviewer-style.md`, `reviewer-tests.md` use
  `claude-sonnet-4-6[1m]/medium`.
  (3) Propagated via delete + `cargo clean -p speccy-cli` + `speccy
  init --force` for both hosts. First run: 6 created / 0 overwritten /
  13 unchanged. Second run: 0 / 0 / 19 unchanged (idempotent).
  (4) Hygiene gates all exit 0.

- Undone: Nothing in T-003 scope remains.

- Commands run: as above plus four hygiene gates.

- Exit codes: clippy 0, test 0, fmt 0, deny 0.

- Discovered issues: None new. The `include_dir!` + SPEC-0027
  Skip-on-exists dance carries over from round 1.

- Procedural compliance: Did not touch `resources/modules/skills/speccy-review.md`
  (T-005), `resources/modules/phases/speccy-work.md` (T-004), or
  `resources/modules/skills/speccy-amend.md` (T-006). Forward
  observations from docs reviewer deferred to T-005.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T19:00:00Z" round="2">
Round-2 narrow fix lands clean against REQ-009. The seven source files
named in the round-2 blockers all now carry `model="..."` on their
example `<review>` snippets. The worked examples no longer contradict
the `## Verdict return contract` clause that sits immediately above
them in each generated reviewer file. No business-scope regressions:
round 2 stayed within examples-conform-to-contract scope, did not
touch Rust source, did not add new CLI surface, did not modify
`verdict_return_contract.md` itself, did not modify the
implementer-side contract. Round-1 business verdict was `pass`;
round 2 introduces no business deltas.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-21T19:00:00Z" round="2">
Round 2 satisfies T-003's task-scenarios cleanly. All 7 source files
carry `model="..."` on their example `<review>` elements. All 12
materialized host-pack files reflect the source change verbatim.
Idempotency confirmed locally: second `init --force` reports `0 / 0 /
19 unchanged` for both hosts. Hygiene gate `cargo test` exits 0 with
all suites green; T-007-gated `#[ignore]`d corpus tests stay ignored.
Contradiction resolved: the contract clause (`model` is required) and
the worked examples now agree.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T19:00:00Z" round="2">
Round-2 changes are example-snippet-only edits across seven Markdown
source files and twelve regenerated host-pack files. No Rust source
changes, no new dependencies, no credentials or secrets introduced.
The new `model="..."` values are synthetic model identifier strings
used as copy-paste-safe documentation examples, not real credentials.
The T-001 parser validates `model` as non-empty only and never
executes or interprets the string value. No injection surface exists.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T19:00:00Z" round="2">
Round-1 blocking concern fully resolved. All seven source files now
carry `model="..."` on every example `<review>` element.
`inline_note_format.md` uses `claude-opus-4.7[1m]/medium`. The six
`reviewer-*.md` files carry persona-appropriate model values:
`claude-opus-4.7[1m]/high` for business and architecture
(heavier-judgment personas), `claude-sonnet-4-6[1m]/medium` for docs,
security, style, and tests. Materialized `.claude/agents/reviewer-*.md`
and `.codex/agents/reviewer-*.toml` spot-checked — both the
`inline_note_format.md` inclusion and the hardcoded `## Example` block
carry `model` attributes. The contradiction is gone from all generated
files. No new style violations.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T19:00:00Z" round="2">
Round-2 fix lands clean. The style blocker is resolved without
regressing the four contract clauses confirmed in round 1. Four
contract clauses verified intact in `verdict_return_contract.md`:
`model` named as required, slash-suffix encoding documented with
worked examples, verbatim transcription rule, no-substitute clause.
Slash-suffix conventions in the round-2 additions are internally
consistent. Cross-coherence between `verdict_return_contract.md`,
`inline_note_format.md`, and all six `reviewer-*.md` source files is
intact: every place a reviewer subagent looks for the example form now
carries `model="..."`.
</review>
</content>
</invoke>
