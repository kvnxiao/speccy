---
spec: SPEC-0037
task: T-004
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-21T20:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Updated `resources/modules/phases/speccy-work.md` for
  REQ-008 implementer side. Rewrote step 5 of the implementer phase
  prose to direct journal-file writes (`journal/T-NNN.md`) with
  frontmatter (`spec: SPEC-NNNN`, `task: T-NNN`, `generated_at:
  <ISO8601>`) for the round-1 case, the `<implementer date|model|round>`
  schema with all attributes required and no-optional, the slash-suffix
  `model` convention with worked example
  (`claude-opus-4.7[1m]/low`/`/medium`/host-omits-suffix guidance), and
  monotonic-from-1 `round` semantics with explicit "do not skip values;
  do not reset" guard. Removed inline `<implementer-note>` write
  instructions; the surviving `<implementer-note>` token is the
  retirement-notice prohibition guard ("Do NOT inline an
  `<implementer-note>` inside the `<task>` body in TASKS.md — that
  element is retired; the parser rejects it"). Propagated to
  `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`,
  `.claude/skills/speccy-work/SKILL.md`, and `.agents/skills/speccy-work/SKILL.md`
  via `speccy init --force` for both hosts.

- Undone: T-005, T-006 still pending; their skill bodies are separate
  work units. T-007 sweep also pending.

- Commands run: Delete materialized speccy-work files + `cargo clean -p
  speccy-cli` + `cargo run --bin speccy -- init --force --host claude-code`
  (1 created / 1 overwritten / 17 unchanged), `--host codex` (same).
  Second `init --force` per host: 0 / 0 / 19 unchanged for both
  (idempotent). Plus four hygiene gates.

- Exit codes: cargo test 0, cargo clippy 0, cargo +nightly fmt --check
  0, cargo deny check 0.

- Discovered issues: None new. The `include_dir!` + Skip-on-exists
  dance from T-003 round 1 carries over cleanly.

- Procedural compliance: Touched only `resources/modules/phases/speccy-work.md`
  and the four materialized speccy-work files. Did not touch
  speccy-review.md (T-005), speccy-amend.md (T-006), or any other
  spec.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T20:45:00Z" round="1">
REQ-008's implementer-side done-when bullets are all satisfied by
`resources/modules/phases/speccy-work.md`. Step 5 names the journal
path as the write target for `<implementer>` blocks and explicitly
forbids inlining `<implementer-note>` into the `<task>` body in
TASKS.md. Round-1 file-creation prose supplies a code example with
all three required frontmatter fields. Required-attribute documentation
enumerates `date` (full ISO8601 with seconds and timezone), `model`
(with the slash-suffix convention examples and the "omit suffix on
hosts without effort knob" caveat), and `round` (monotonic positive
integer from 1, increment-by-1 on post-blocker retry). Materialization
propagated cleanly to `.claude/agents/speccy-work.md`.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T20:30:00Z" round="1">
All six T-004 task-scenarios materially satisfied. (1) journal/T-NNN.md
named as write target (lines 62, 68). (2) No live-workflow
`<implementer-note` references — the one occurrence at line 64 is the
retirement notice "Do NOT inline...". (3) Required `date`, `model`,
`round` attributes documented with slash-suffix convention and
"non-empty" disclaimer. (4) Round monotonic-from-1 documented with
"do not skip values; do not reset". (5) Materialized host packs reflect
source verbatim; idempotency confirmed (0/0/19 unchanged per host).
(6) Four hygiene gates exit 0.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T20:30:00Z" round="1">
Prose-only change. No credentials, tokens, API keys, internal
hostnames, or PII in any of the modified files. The file-write target
named in the skill body is `journal/T-NNN.md` — a relative path using
the fixed prefix `journal/` and the task-ID pattern `T-NNN`; no
user-controlled path segments are concatenated. No path traversal
vector introduced. Materialized host packs reflect the source module
verbatim modulo the expected template-variable substitution and TOML
wrapping; no silent divergence detected.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T20:30:00Z" round="1">
All style checks pass. Heading structure (`#` / `## When to use` / `##
Steps`) matches every sibling phase file. Step 5 prose voice matches
the surrounding imperative, noun-phrase-leading register used across
all phase and skill bodies. The worked-example `<implementer>` carries
`model="claude-opus-4.7[1m]/low"` and `round="1"` consistent with the
ARCHITECTURE.md canonical example. Slash-suffix convention is
documented in exact alignment with `verdict_return_contract.md` and
ARCHITECTURE.md attribute value rules. The single `<implementer-note>`
mention is a correct prohibition guard, not a live workflow
instruction. No contradictions with ARCHITECTURE.md,
`verdict_return_contract.md`, or the materialized
`.claude/skills/speccy-work/SKILL.md`. Terminology is forward-compatible
with T-005/T-006.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T20:55:00Z" round="1">
All four T-004 instructions are explicit and unambiguous. File path
`journal/T-NNN.md` is named. Round-1 frontmatter creation (three
required fields: `spec`, `task`, `generated_at`) is shown via a
concrete code example. All three required `<implementer>` attributes
are enumerated with no-optional stated. The slash-suffix `model`
convention matches `verdict_return_contract.md` exactly. Round
monotonicity (positive integer from 1, increment by exactly 1 on each
post-blocker retry, no skip, no reset) is documented. No
`<implementer-note>` references remain in live-workflow prose; the
one appearance is the explicit prohibition. The ARCHITECTURE.md
journal-artifact section is consistent with the skill on every point.
No prose ambiguity that could mislead a future implementer subagent.
</review>
</content>
</invoke>
