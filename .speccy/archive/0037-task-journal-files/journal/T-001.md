---
spec: SPEC-0037
task: T-001
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-20T16:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Landed the full Rust foundation for SPEC-0037's
  REQ-001..REQ-007. Added `speccy-core/src/parse/journal_xml/` (new
  module mirroring `task_xml/` and `report_xml/`) carrying YAML
  frontmatter parsing (`spec`, `task`, `generated_at`), a chronological
  bare-element body parser for `<implementer>`, `<review>`, and
  `<blockers>` blocks (DEC-002: no wrapper), full ISO8601 / non-empty
  string / positive-integer attribute validation, and monotonic
  round-sequence enforcement. Hard-cutover updates to
  `speccy-core/src/parse/task_xml/mod.rs`: dropped `implementer-note`,
  `retry`, AND `tasks` from the allow-list; added `implementer`,
  `review`, `blockers`; TASKS.md parser now expects bare `<task>`
  children under the `# Tasks:` heading (no `<tasks>` wrapper). New
  lint family `speccy-core/src/lint/rules/jnl.rs` carrying
  `JNL-001`/`JNL-002`/`JNL-003` at `Level::Error`, registered in
  `speccy-core/src/lint/registry.rs`, with the
  in-progress/in-review lifecycle skip baked into the rule entry
  (DEC-004). Extended `speccy-core/src/lint/rules/tsk.rs` with TSK-006
  ("no journal elements in TASKS.md", non-state-gated). Wrote
  `speccy-core/tests/lint_jnl.rs` (17 integration tests) plus 11
  in-module unit tests under `journal_xml/mod.rs`. Added the regression
  scenarios for REQ-007 (net-new task retention through state
  transitions).

- Undone: Nothing within T-001 scope. T-002..T-007 remain pending as
  designed. The two integration tests
  `every_in_tree_tasks_md_parses_and_has_populated_scenarios` and
  `speccy_verify_exits_zero_on_migrated_in_tree_workspace` are marked
  `#[ignore = "..."]` with reasons pointing at SPEC-0037 T-007 as the
  unblocker — re-enabled by T-007's migration sweep.

- Commands run: `cargo check --workspace --all-targets --all-features
  --locked`, `cargo clippy --workspace --all-targets --all-features
  --locked -- -D warnings`, `cargo +nightly fmt --all --check`, `cargo
  test --workspace --all-features --locked`, `cargo deny check`.

- Exit codes: 0 across all four hygiene gates.

- Discovered issues: Multiple clippy violations in the new code and
  touched test files surfaced on the first clippy run; fixed in-place
  before exit. The host-pack drift-check meta-test
  (`dogfood_outputs_match_committed_tree`) was unaffected because T-001
  did not touch any `resources/modules/` skill bodies.

- Procedural compliance: Wrote the legacy `<implementer-note>` form
  intentionally as called out in the briefing — T-007 will migrate it
  to `journal/T-001.md` (this file). Did not touch ARCHITECTURE.md /
  AGENTS.md (T-002), resources/modules/ (T-004..T-006), or other specs
  (T-007). The four hygiene gates all reach exit 0 with the two
  `#[ignore]` attributes carrying the SPEC-0037 T-007 unblocker reason.
  Workspace-wide `speccy verify` is expected to remain red until T-007
  — that is the documented red-CI window for this SPEC.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-20T19:00:00Z" round="1">
T-001's narrowed scope (REQ-001..007 Rust code + tests + ignore-tags)
lands as the SPEC intended. The new `journal_xml/mod.rs` carries the
frontmatter+bare-element parser with full ISO8601 / positive-integer /
non-empty-model validation and monotonic round-sequence enforcement
(REQ-001, REQ-003, REQ-004). The new `lint/rules/jnl.rs` implements
JNL-001/002/003 with the lifecycle skip for `in-progress`/`in-review`
baked into the rule entry (REQ-002). All three codes are registered as
`Level::Error` so `speccy verify` exits non-zero. The TSK-006 addition
in `tsk.rs` fires unconditionally regardless of task state (REQ-006).
The `task_xml/mod.rs` cutover declares exactly five live element names
with `tasks`/`implementer-note`/`retry` retired (REQ-005). REQ-007
covered by `appending_a_new_task_does_not_trip_tsk006_or_staleness`.
Non-goals respected: no `task_hash_at_generation`, no `effort`
attribute, no `host` attribute, no `speccy migrate` subcommand. The
two `#[ignore]` attributes name SPEC-0037 T-007 as unblocker, matching
the documented red-CI window.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-20T19:00:00Z" round="1">
The 17 `lint_jnl.rs` integration tests + 11 `journal_xml/mod.rs`
in-module tests + the task_xml unit tests
(`legacy_tasks_wrapper_is_rejected`, `legacy_implementer_note_is_rejected`,
`legacy_retry_is_rejected`, `misplaced_implementer/blockers_in_tasks_md_is_recorded`,
`happy_path_two_bare_tasks_no_wrapper`) cover every Given/When/Then in
T-001's `<task-scenarios>` block. Tests build real tempdir workspaces
and run the real `speccy_core::lint::run` pipeline — no mocks around
the unit under test. Negative silence assertions are present:
`jnl_skips_in_progress_state`, `jnl_skips_in_review_state`,
`jnl_silent_on_pending_task_without_journal`,
`jnl_silent_on_completed_task_with_valid_journal`, and the lifecycle
`pending -> in-progress -> in-review -> completed` cycle in
`lifecycle_state_transitions_stay_quiet`. JNL-003 boundary coverage
hits all four cases (first-not-1, skip, non-monotonic, valid
multi-round). TSK-006 fires at all three element kinds across all
four states. The two `#[ignore]` tags carry reasons explicitly naming
SPEC-0037 T-007 as the unblocker.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-20T19:00:00Z" round="1">
No security-blocking findings. All production code paths in
`journal_xml/mod.rs`, `lint/rules/jnl.rs`, `lint/rules/tsk.rs`, and
the updated `task_xml/mod.rs` are panic-free: the two `unwrap()` calls
are in `OnceLock`-guarded compile-time literal regex initialisers,
each annotated with `#[expect(clippy::unwrap_used, reason = "...")]`
per project policy. No `[i]` slice indexing in new code. Path
primitives use `camino::Utf8Path`/`Utf8PathBuf` and `fs_err`
consistently. The ISO8601 regex is anchored with `^`/`$` and uses
fixed-width digit groups; no REDOS risk. The `state` attribute
dispatches through a closed enum so no rogue state value can bypass
the in-progress/in-review gate. Journal file paths are constructed
from the regex-validated task ID (`T-\d{3,}`) — no path traversal
vector. All `ParseError` variants propagate via `?`; no silent-swallow
patterns. Test fixtures contain only synthetic identifiers; no
credentials or PII.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-20T19:00:00Z" round="1">
Two blocking issues and one minor one in the T-001 diff.

Blocking 1 — `#![allow]` in `lint_jnl.rs` (and matching `lint_tsk.rs`):
`speccy-core/tests/lint_jnl.rs:1` uses `#![allow(clippy::expect_used,
reason = "...")]`. The project's `Cargo.toml` sets `allow_attributes =
"deny"`, which bans `#[allow]` entirely — even when a `reason` is
provided. The mandated form is `#[expect(..., reason = "...")]` per
AGENTS.md "Conventions for AI agents". Note that `clippy.toml`
already sets `allow-expect-in-tests = true`, so the suppression is
likely redundant anyway.

Blocking 2 — Dead import kept alive by a dummy test in `lint_jnl.rs`:
`speccy-core/tests/lint_jnl.rs:15` imports `use camino::Utf8Path` but
`Utf8Path` is never used in any substantive test — only in
`unused_path_helper_to_keep_imports_balanced` (lines 445–449), a
fabricated test whose only purpose is to silence the dead-import
warning. AGENTS.md "Clean up orphans your changes created" requires
removing unused imports, not wrapping them in dummy tests.

Minor — `&Utf8PathBuf` parameter type in `jnl.rs` private helpers:
`speccy-core/src/lint/rules/jnl.rs:50,70` declare `journal_path:
&Utf8PathBuf`. Every analogous private helper in `tsk.rs` uses
`&camino::Utf8Path`, which is the idiomatic borrow form. Switch to
`&Utf8Path`.
</review>

<blockers date="2026-05-20T20:00:00Z" round="2">
Style review blocks landing T-001 on three findings (two blocking,
one minor) in the test / lint code introduced by this task. Fix all
three, re-run the four hygiene gates, then re-spawn the style review.

1. Blocking — `speccy-core/tests/lint_jnl.rs:1` and
   `speccy-core/tests/lint_tsk.rs:1` use `#![allow(clippy::expect_used,
   reason = "...")]`. Replace with `#![expect(...)]` or drop the
   suppression entirely since `clippy.toml` already sets
   `allow-expect-in-tests = true`.

2. Blocking — `speccy-core/tests/lint_jnl.rs` carries a dead import
   `use camino::Utf8Path` kept alive by the dummy test
   `unused_path_helper_to_keep_imports_balanced`. Delete both.

3. Minor — `speccy-core/src/lint/rules/jnl.rs:50,70` declare
   `journal_path: &Utf8PathBuf`. Switch to `&Utf8Path` to match
   `tsk.rs` companion conventions.

Business / tests / security all returned `verdict="pass"` — only
style is blocking. No other rework needed.
</blockers>

<implementer date="2026-05-20T20:30:00Z" model="claude-opus-4.7[1m]/low" round="2">
- Completed: All three style findings addressed.
  (1) `speccy-core/tests/lint_jnl.rs:1` and
  `speccy-core/tests/lint_tsk.rs:1` — dropped the `expect_used`
  suppression entirely (the reviewer's alternative); `clippy.toml`
  already sets `allow-expect-in-tests = true`, so the attribute was
  redundant. First tried `#![allow]` → `#![expect]` but clippy then
  flagged `unfulfilled_lint_expectations`, confirming the suppression
  was a no-op.
  (2) Deleted the dead `use camino::Utf8Path;` import and the
  `unused_path_helper_to_keep_imports_balanced` dummy test in
  `lint_jnl.rs`.
  (3) `speccy-core/src/lint/rules/jnl.rs` — changed `journal_path:
  &Utf8PathBuf` → `&Utf8Path` in both `check_pending` (line 50) and
  `check_completed` (line 70); updated the import accordingly. Replaced
  four `journal_path.clone()` calls with `journal_path.to_path_buf()`
  since `&Utf8Path` is unsized.

- Undone: Nothing in T-001 scope remains.

- Commands run: `cargo clippy --workspace --all-targets --all-features
  --locked -- -D warnings`, `cargo test --workspace --all-features
  --locked`, `cargo +nightly fmt --all` then `--check`, `cargo deny
  check`.

- Exit codes: clippy 0, test 0, fmt 0, deny 0.

- Discovered issues: None. The reviewer's "Alternative: drop the
  suppression entirely" was the correct fix.

- Procedural compliance: Touched only the three files named in the
  blockers (`tests/lint_jnl.rs`, `tests/lint_tsk.rs`,
  `src/lint/rules/jnl.rs`). Did not touch ARCHITECTURE.md /
  AGENTS.md (T-002), skill bodies (T-004..T-006), other specs (T-007),
  or materialized host packs. Did not un-ignore the two `#[ignore]`d
  tests.
</implementer>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-20T21:00:00Z" round="2">
All three round-1 findings confirmed resolved and clean.

Blocking 1: Both `tests/lint_jnl.rs:1` and `tests/lint_tsk.rs:1` no
longer carry any `#![allow(clippy::expect_used)]` line — suppression
dropped entirely.

Blocking 2: `lint_jnl.rs` no longer imports `use camino::Utf8Path`,
and the `unused_path_helper_to_keep_imports_balanced` dummy test is
gone.

Minor: `lint/rules/jnl.rs` imports `camino::Utf8Path`; both
`check_pending` and `check_completed` now take `journal_path:
&Utf8Path`. Call sites pass `.to_path_buf()` where `Utf8PathBuf` is
needed.

`cargo clippy --workspace --all-targets --all-features --locked --
-D warnings` exits clean. `cargo test` passes all suites with the two
SPEC-0037 T-007-gated corpus tests still `#[ignore]`d. Style approves
landing T-001.
</review>
</content>
</invoke>
