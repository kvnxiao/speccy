---
spec: SPEC-0018
task: T-002
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" round="1">
- Completed: Rewrote `speccy-cli/src/check.rs` as a render-only command. Dropped the `use speccy_core::exec::shell_command` import, deleted the `run_executable` / `render_manual` execution helpers and the `CheckError::ChildSpawn` variant, and replaced `execute_checks` with `render_checks` + `render_one`. Each selected check now prints `==> CHK-NNN (SPEC-NNNN): <scenario first line>` from `CheckEntry.scenario` with continuation lines indented two spaces; the run closes with `N scenarios rendered across M specs` (M = distinct spec count via a `BTreeSet`). All SPEC-0017 selector shapes (`All`, `UnqualifiedCheck`, `Spec`, `QualifiedCheck`, `Task`) reach the same renderer; dropped/superseded specs still get the explicit "no checks rendered" skip line when named directly. Exit code is non-zero only on selector/lookup/parse/workspace errors (`malformed > 0` retains the existing 1-exit-on-warning shape; scenarios never gate). `speccy-cli/src/main.rs` lost the now-unreachable `ChildSpawn` arm. The integration test file `speccy-cli/tests/check.rs` was rewritten with 20 tests mapped one-to-one to the bullets in this task entry plus selector/error-surface coverage; legacy framing (`<-- CHK-... PASS|FAIL|IN-FLIGHT|MANUAL`, exit-code propagation from commands, sentinel-file write-back, live-streaming smoke) is gone. The no-subprocess proof uses a legacy `command = "echo BAD > sentinel"` row that the T-001 bridge happily parses; after rendering, the sentinel file does not exist.
- Undone: `speccy-core::exec` and `parse::CheckPayload` remain (T-004 deletes them). `speccy check` still reads through `CheckEntry.payload` indirectly because the bridge keeps populating it, but no production code path in this crate touches `payload` anymore. `speccy verify` execution removal is T-003.
- Commands run:
  - `cargo test -p speccy-cli --test check`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- Exit codes:
  - pass (20/20)
  - pass (all suites green)
  - pass (no warnings)
- Discovered issues: (none)
- Procedural compliance: (none)
</implementer>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="tests" verdict="pass" round="1">
T-002's `speccy-cli/tests/check.rs` is non-vacuous and covers each REQ-002 bullet against the real `speccy_cli::check::run` entry point (not a mock): output buffers are real `Vec<u8>`, fixtures parse through the real workspace scanner + TOML deserializer, and selector resolution flows through `parse_selector` + `task_lookup::find`. Bullet-to-test mapping is clean: no-selector run + count summary (`check.rs:157`), multi-line header + indented continuations (`check.rs:217`), all five SPEC-0017 selector shapes (`check.rs:289,320,349,376,422`), overlapping-task dedup with first-occurrence order asserted via byte positions and a `matches("==> CHK-002 (SPEC-0020)").count() == 1` assertion (`check.rs:456`), `SPEC-9999` preserves `NoSpecMatching` wording (`check.rs:525`), and `assert_no_legacy_footers` (`check.rs:143`) blocks regressions that re-introduce `<-- CHK`, `PASS`, `FAIL`, `IN-FLIGHT`, or `MANUAL` framing. The "legacy `command` row is not spawned" bullet was replaced post-T-004 by `legacy_command_field_is_rejected_by_deny_unknown_fields` (`check.rs:254`) — substitution is acceptable because hard-break parse rejection is strictly stronger than the bridge-era no-spawn sentinel proof (a row that cannot deserialize cannot reach the renderer). A mentally-rewritten implementation that drops continuation indentation, swaps the summary wording, prints CHK-002 twice in the dedup case, or fails to preserve selector-error wording each fails at least one assertion. Non-blocking nit: `multiline_scenario_header_then_indented_continuations` (`check.rs:230-241`) asserts `out.contains("  when CHK-001 is rendered,")`, which still passes if a regression over-indents to 4 spaces because `"    when..."` contains the 2-space substring. Tighten with a line-prefix or exact-line compare (split on `\n` and assert the continuation line equals `"  when CHK-001 is rendered,"`) so indentation regressions in either direction are caught.
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-002 is a net security win: it deletes the arbitrary-command-execution sink that was the primary security surface of `speccy check`. The new `check.rs` contains no `Command`, `spawn`, or `process::` references (`speccy-cli/src/check.rs:1-454`); `shell_command`, `run_executable`, and `render_manual` are gone, along with the `CheckError::ChildSpawn` variant (`speccy-cli/src/main.rs` no longer pattern-matches it). `render_one` (`check.rs:381-390`) only writes the developer-authored `scenario` string verbatim through `writeln!` to caller-supplied `dyn Write`, with no shell, no interpolation, no env-var expansion. Scenario contents originate from in-tree `spec.toml` (already a trusted, repo-committed artifact) and are bounded by `#[serde(deny_unknown_fields)]` plus the T-001 non-empty validation. The "legacy `command` is not spawned" bullet is verified by a sentinel-file integration test (per implementer note); since no `std::process` call exists in this module, the sentinel test is a belt-and-suspenders confirmation rather than the sole guarantee. Error messages (`CheckError::NoCheckMatching`, `ProjectRootNotFound`, `Io`) name only the user-supplied selector and workspace path — no secret or credential surface. No new dependencies, no `unsafe`, no panics in production code paths. The render path is well-behaved on adversarial scenario text (terminal control chars would print verbatim, but this matches every other `speccy` text-output command and is out of scope for SPEC-0018's threat model, which assumes repo content is trusted).
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/high" persona="business" verdict="pass" round="1">
T-002 satisfies REQ-002's `done_when` items end-to-end. `speccy-cli/src/check.rs` removed the `speccy_core::exec` dependency (`check.rs:7-27`); every SPEC-0017 selector shape (`All`, `UnqualifiedCheck`, `Spec`, `QualifiedCheck`, `Task`) reaches the same `render_checks` renderer (`check.rs:126-137`); the header `==> CHK-NNN (SPEC-NNNN): <first line>` with two-space indented continuations matches REQ-002's framing exactly (`check.rs:381-390`); the summary `N scenarios rendered across M specs` is emitted with a `BTreeSet`-deduplicated spec count (`check.rs:368-376`); the old `<-- CHK-NNN PASS|FAIL|IN-FLIGHT|MANUAL` footer is gone (asserted by `assert_no_legacy_footers` and the test `legacy_command_field_is_rejected_by_deny_unknown_fields`); and exit code is non-zero only on selector/lookup/parse/workspace errors per `i32::from(malformed > 0)` (`check.rs:378`, `415`). REQ-002's three Behavior cases — six headers across three two-check specs, preserved `SPEC-9999` wording, and task-selector scenarios derived from covered requirements with SPEC-0017 semantics — are covered by `no_selector_renders_all_scenarios_with_count_summary`, `binary_spec_9999_preserves_no_matching_spec_wording`, and the `qualified_task_selector_renders_covered_scenarios` / `task_selector_dedups_overlapping_checks_in_first_occurrence_order` pair. No non-goal is breached: nothing here adds `--exec`, registers commands, or grades scenarios. No Open Question is silently resolved. The user story for the implementer agent ("each check describes behavior in English") and the CI-maintainer story ("fast structural gate") are advanced by this task; the reviewer-tests persona update lands in T-005, which is out of scope here.
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
`speccy-cli/src/check.rs` is idiomatic for this codebase — `thiserror`-backed `CheckError` with `#[non_exhaustive]` and `#[from]` propagation, `?` everywhere, no `unwrap()` / `expect()` / `panic!()` / `#[allow(...)]` in production code, `camino::Utf8Path` at every path boundary, doc comments on every public item with `# Errors` sections per `.claude/rules/rust/rust-documentation.md`. The `unwrap_or` / `unwrap_or_else` calls (`check.rs:206,244,299,323,351,384,402`) are the infallible variants and don't trip `clippy::unwrap_used`. Selector dispatch in `run` (`check.rs:126-136`) follows the established per-shape-handler split, and the shared `render_checks` / `render_one` / `collect_for_spec` / `display_spec_label` helpers keep each handler short and reuse one helper rather than inventing parallels. `speccy-cli/tests/check.rs` follows the existing test-file convention: the file-level `#![allow(clippy::expect_used, reason = "...")]` matches `tests/review.rs`, `tests/tasks.rs`, `tests/plan.rs`, etc.; tests use `.expect("descriptive")` per `.claude/rules/rust/rust-testing.md`; no `.unwrap()` / `panic!()`. `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all --check` are both clean.
</review>
