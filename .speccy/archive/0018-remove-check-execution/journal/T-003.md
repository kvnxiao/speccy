---
spec: SPEC-0018
task: T-003
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" round="1">
- Completed: Rewrote `speccy-cli/src/verify.rs` and `speccy-cli/src/verify_output.rs` as a shape-only validator. The `verify::run` entry point no longer imports `speccy_core::exec`, no longer calls `run_checks_captured`, and no longer invokes `speccy check`; `collect_check_specs` is gone. `VerifyReport` now carries `specs_total` / `requirements_total` / `scenarios_total` plus the three lint buckets, and `passed()` is `lint_errors.is_empty()`. Text output writes the contract line `verified N specs, M requirements, K scenarios; E errors` as the last line. JSON output bumps `schema_version` to `2`, replaces the per-check execution array and `summary.checks` block with a `summary.shape { specs, requirements, scenarios, errors }` block, and a regression-guard recurses the whole envelope asserting no `outcome`, `exit_code`, or `duration_ms` keys appear anywhere. Added `REQ-003` lint rule (`speccy-core/src/lint/rules/req.rs`) that flags `[[checks]]` rows not referenced by any `[[requirements]].checks`, registered it at `Level::Error` in `speccy-core/src/lint/registry.rs`, and updated `speccy-core/tests/snapshots/lint_registry.snap`. Demotion was widened: errors on `in-progress`, `dropped`, and `superseded` specs are now demoted to `info`, preserving the pre-SPEC-0018 "dropped/superseded checks never ran" non-gating contract; workspace-level errors and errors on `implemented` specs still gate. Replaced `speccy-cli/tests/verify.rs` with 19 tests mapped one-to-one to T-003's bullets (empty `checks` array, unknown CHK ref, unreferenced scenario, no-spawn-on-clean, text summary shape, JSON schema_version=2 + no execution fields, dropped/superseded/in-progress demotion, implemented gating, workspace parse errors still gate, deterministic JSON, binary dispatcher). Added `req_003_*` tests in `speccy-core/tests/lint_req.rs`.
- Undone: REPORT.md parse errors are not surfaced because the workspace scanner does not parse REPORT.md today; adding it is out of scope for T-003 (separate lint surface). T-004 still owns deleting `speccy-core::exec` and migrating all in-tree `spec.toml` files; `verify` no longer uses `exec`, but the module remains until T-004.
- Commands run:
  - `cargo build --workspace`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `cargo test -p speccy-cli --test verify`
- Exit codes:
  - pass
  - pass (50 test binaries, all green)
  - pass (no warnings)
  - pass (19/19)
- Discovered issues: (none)
- Procedural compliance: (none)
</implementer>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Net reduction in attack surface. `speccy verify` no longer spawns child processes (`speccy-cli/src/verify.rs:5-10`) — the entire command-injection vector that the legacy `[[checks]].command` path created on every CI run is gone, and a grep of `verify.rs`/`verify_output.rs` confirms zero `Command`/`spawn`/`exec` usage. New `REQ-003` lint and demotion logic operate on already-parsed in-tree workspace data; no new untrusted input is read or rendered into shells. JSON envelope (`schema_version = 2`) carries only structural counts and lint diagnostics on local file paths — no secrets, no credentials, no environment passthrough.
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="tests" verdict="pass" round="1">
T-003's 19 integration tests in `speccy-cli/tests/verify.rs` plus the four `req_003_*`/REQ-* unit tests in `speccy-core/tests/lint_req.rs:24-145` are non-vacuous and map one-to-one to the bullet list. Each integration test invokes the real `speccy_cli::verify::run` against a fixture workspace through the real workspace scanner, TOML deserializer, and lint registry — no mocks. Bullet-to-test mapping: empty `checks` array asserts exit=1 and that the REQ-001 diagnostic message contains the literal `REQ-001` (`verify.rs:157-186`); unknown CHK ref asserts the diagnostic message contains both `REQ-001` and `CHK-099` (`verify.rs:192-220`); unreferenced scenario asserts the REQ-003 code is emitted and the message names `CHK-077`, corroborated by the lint-layer unit test (`lint_req.rs:59-91`) which adds the negative case `req_003_silent_when_every_scenario_is_referenced`; text summary uses exact-equals on the last line shape `verified N specs, M requirements, K scenarios; E errors` and aggregates correctly across specs and on an empty workspace (`verify.rs:303-364`); the JSON envelope test (`verify.rs:370-410`) pins `schema_version == 2`, asserts the `summary.shape` counts, and uses a recursive `assert_no_execution_keys` walker (`verify.rs:129-145`) that catches a stray `outcome`/`exit_code`/`duration_ms` at any nesting depth, including inside `lint.errors[].extra`; dropped/superseded/in-progress non-gating and implemented-still-gates form a clean contrast quartet (`verify.rs:453-592`); the binary dispatcher coverage (`verify.rs:598-659`) closes the wrapper. The no-spawn bullet (`clean_workspace_exits_zero_without_spawning_child_processes`, `verify.rs:260-297`) is technically a weaker proof than the bullet asks for — it asserts no sentinel file was created, but a regression that called `run_checks_captured` without shelling out to `touch` could still pass it. That said, the absence of `Command::new`/`spawn`/`process::`/`run_checks_captured`/`speccy_core::exec` in `verify.rs` + `verify_output.rs` (verified by grep) plus T-004's deletion of `speccy_core::exec` itself make the structural guarantee load-bearing and the sentinel a confirmatory belt-and-suspenders; acceptable. Mentally rewriting the implementation to drop REQ-003, change the summary wording, leave `schema_version` at 1, or stop demoting dropped/superseded specs each fails at least one assertion. One non-blocking nit: `text_output_summary_counts_aggregate_across_specs` (`verify.rs:326-351`) writes two fixtures that both render under SPEC-0001's template (`spec_md_template("SPEC-0001", ...)` is reused for both slugs) — the test still passes today because counts come from parsed specs, but a future "duplicate SPEC id in workspace" lint would silently flip it red; consider parameterizing the second fixture with `SPEC-0002` to future-proof.
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
T-003's production surface in `speccy-cli/src/verify.rs`, `speccy-cli/src/verify_output.rs`, and the new REQ-003 branch in `speccy-core/src/lint/rules/req.rs` is idiomatic for this codebase. `VerifyError` (`verify.rs:30-52`) is `thiserror`-backed with `#[non_exhaustive]` and `#[from]` propagation; `run` (`verify.rs:108-144`) and `partition_lint` (`verify.rs:164-191`) use `?` everywhere with no `unwrap()` / `expect()` / `panic!()` / `unreachable!()` / `#[allow(...)]` in production code. `VerifyReport::passed` carries `#[must_use = "the pass/fail signal drives the exit code"]` per `rust-code-quality.md`. `shape_totals` (`verify.rs:208-225`) uses `saturating_add` rather than `+`, matching the project's overflow-aware posture. The new REQ-003 branch in `req.rs:57-70` mirrors the REQ-001/REQ-002 emission shape (`Diagnostic::with_file`, `Level::Error`, named-arg message format) rather than inventing a parallel helper, and the `REQ_003` constant is grouped with `REQ_001`/`REQ_002` at the top of the file. `registry.rs:23` slots `REQ-003` into the existing REQ-* block in code order; the snapshot was regenerated, not allow-listed. `verify_output.rs` keeps the existing `JsonOutput`/`JsonSummary`/`JsonLintCounts` struct split and adds `JsonShapeCounts` in the same `#[derive(Debug, Clone, Copy, Serialize)]` style with full doc comments on every public field. Test files (`speccy-cli/tests/verify.rs`, `speccy-core/tests/lint_req.rs`) use the established file-level `#![allow(clippy::expect_used, reason = "...")]` / `#![expect(clippy::panic_in_result_fn, reason = "...")]` pattern shared by every other integration test in the crate. `cargo clippy --workspace --all-targets --all-features -- -D warnings` is clean and `cargo +nightly fmt --all --check` shows no diffs.
</review>

<review date="2026-05-15T14:31:27Z" model="claude-sonnet-4-6[1m]/high" persona="business" verdict="pass" round="1">
T-003 satisfies REQ-003's `done_when` end-to-end. `speccy-cli::verify::run` has no `speccy_core::exec` import and no execution helper call (`speccy-cli/src/verify.rs:12-25,108-144`); lint output includes parse errors (SPC-*), requirements-with-no-scenarios (REQ-001), unknown-scenario refs (REQ-002), unreferenced-scenario rows (new REQ-003 rule in `speccy-core/src/lint/rules/req.rs:57-70`, registered at `Level::Error` in `lint/registry.rs:23`), plus the existing TSK-* / QST-001 diagnostics; dropped/superseded specs stay non-gating via the demotion in `partition_lint` (`speccy-cli/src/verify.rs:171-189`); the text summary's terminating line `verified N specs, M requirements, K scenarios; E errors` is emitted unconditionally (`speccy-cli/src/verify_output.rs:42-49`); JSON bumps `schema_version` to `2` (`verify_output.rs:17`) and the `assert_no_execution_keys` recursion in `speccy-cli/tests/verify.rs:129-145` proves `outcome` / `exit_code` / `duration_ms` are absent at every depth. The three Behavior cases (REQ-001 names the requirement, REQ-002 names both ids, clean workspace exits 0 without spawning) are covered by named tests. Two non-blocking gaps worth surfacing: (1) REQ-003's `done_when` names `REPORT.md` parse errors among the diagnostics verify must report, but the workspace scanner does not parse REPORT.md and the implementer note explicitly defers that surface — this is pre-existing scanner behavior, not a T-003 regression, but the SPEC bullet is technically unmet and the deferral should be tracked in this spec's Open Questions or a follow-up, not silently inherited; (2) `partition_lint` (`verify.rs:177-182`) also demotes errors on in-progress specs, which widens the SPEC's literal "Dropped and superseded specs remain non-gating" — pre-SPEC-0018 already demoted in-progress, so this preserves prior behavior, but the SPEC wording could be tightened to match. No non-goal is breached, no Open Question is silently resolved.
</review>
