---
spec: SPEC-0057
task: T-002
generated_at: 2026-06-11T01:43:23Z
---

<implementer date="2026-06-11T01:43:23Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area = `speccy-core/src/lint/` + the `xml_scanner` helpers T-001 landed. Reuse-as-is: `scan_foreign_tags`, `collect_code_fence_byte_ranges`, `is_void_element_name`, `ForeignTag` (all from `parse::xml_scanner`, built by T-001); `Diagnostic::with_location` / `Level::Error` (lint types); the three whitelist constants `spec_xml::SPECCY_ELEMENT_NAMES` / `task_xml::TASKS_ELEMENT_NAMES` / `report_xml::REPORT_ELEMENT_NAMES`; `ParsedSpec::{spec_doc_ok,tasks_md_ok,report_md_ok}` accessors and their `raw` fields; the `lint_jnl.rs` tempdir-workspace + `run_lint` helper shape and the `verify.rs` `invoke`/`at`/`field` JSON helpers. Extend: appended `("XML-001", Level::Error)` to the existing `REGISTRY` slice and the existing per-spec lint loop in `lint/mod.rs` (one new `rules::xml::lint` call). Write-fresh: `lint/rules/xml.rs` (the name-scoped balance pass) — searched `lint/rules/` and found no existing balance/stack logic to extend; new top-level symbols `lint`, `balance_pass`, `orphan_diagnostic`, `const XML_001` mirror the per-rule module shape of `jnl.rs`/`rpt.rs`.
- Completed: Added `speccy-core/src/lint/rules/xml.rs` implementing the `XML-001` balance lint over SPEC.md/TASKS.md/REPORT.md using a `HashMap<String, Vec<u32>>` per-name open-line stack — non-void foreign opens push, foreign closes pop (empty stack = dangling close), leftover stack entries = dangling opens; one shared message template parameterized only by tag name; `with_location` carries path + 1-indexed line. Wired `pub mod xml;` into `rules/mod.rs`, added the `rules::xml::lint` call + module-doc family entry in `lint/mod.rs`, appended `("XML-001", Level::Error)` to `registry.rs`, and re-blessed `tests/snapshots/lint_registry.snap`. Added `speccy-core/tests/lint_xml.rs` (CHK-001..CHK-006) and one CLI test in `speccy-cli/tests/verify.rs` for CHK-008 (status: implemented, asserts exit 1 + JSON file/line location).
- Undone: CHK-007 (journal artifact coverage) and the REQ-004 journal portion are deliberately out of scope — the task body assigns the journal artifact to T-003. No other gaps.
- Hygiene checks: cargo test --workspace = 0; cargo clippy --workspace --all-targets --all-features -- -D warnings = 0; cargo +nightly fmt --all --check = 0; cargo deny check = 0.
- Evidence: no separate evidence file written; the integration tests are the paper trail. Roll call for CHKs under T-002's covered REQs:
  - CHK-001 (orphan close in TASKS.md -> one XML-001 per line, naming TASKS.md): demonstrated -> `xml_001_fires_per_orphan_close_in_tasks_md` in `speccy-core/tests/lint_xml.rs` (asserts 2 hits, distinct lines, TASKS.md path).
  - CHK-002 (dangling non-void open -> one XML-001 naming its line): demonstrated -> `xml_001_fires_for_dangling_non_void_open`.
  - CHK-003 (balanced foreign pair -> zero XML-001): demonstrated -> `xml_001_silent_on_balanced_foreign_pair`.
  - CHK-004 (void `<br>` exempt, non-void fires): demonstrated -> `xml_001_exempts_void_open_but_not_non_void`.
  - CHK-005 (fenced orphan close exempt; unfenced still fires): demonstrated -> `xml_001_exempts_fenced_orphan_close` (two-case: fenced-only silent, mixed fenced+unfenced -> exactly one).
  - CHK-006 (one orphan in each of SPEC.md/TASKS.md/REPORT.md -> exactly three, correct paths): demonstrated -> `xml_001_covers_all_three_parsed_artifacts`.
  - CHK-008 (verify exits non-zero, output names path+line): demonstrated -> `xml_001_unbalanced_foreign_tag_gates_verify_and_names_location` in `speccy-cli/tests/verify.rs` (status: implemented so the Error is not demoted; asserts exit 1 + JSON `file` ends with SPEC.md + `line` present).
  - Registry pin (REQ-005 done-when: snapshot includes XML-001 at error): hygiene -> `registry_matches_snapshot` in `tests/lint_registry.rs` over the re-blessed snapshot.
- Discovered issues: none. The `HashMap` iteration order for dangling-open emission is nondeterministic per-call, but `lint::run` sorts diagnostics by `(spec_id, code, file, line)` before returning, so final output is deterministic — no fix needed.
- Procedural compliance: followed speccy-work first-attempt recipe (clean tree, journal absent). No edits to shipped skill/resource modules were needed; no friction with the prompt. CHK-008 was placed in `speccy-cli/tests/verify.rs` rather than the core `lint_xml.rs` because the verify exit-code + in-progress-demotion gate is a CLI-layer concern, matching the precedent set by the SPEC-0035 RPT-* verify tests already in that file.
</implementer>
<review date="2026-06-11T01:45:06Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. Internal lint slice — no auth, secret, crypto, or network
surface; review scoped to injection, arithmetic safety, panic paths,
and diagnostic data exposure.

Injection / path traversal: the only attacker-influenced value reaching
the diagnostic is the foreign tag name, extracted by the anchored,
linear-time `open_tag_regex` / `close_tag_regex` (`xml_scanner/mod.rs:128,138`),
so it is constrained to `[a-z][a-z-]*` — it cannot smuggle a path,
format specifier, or shell/SQL metacharacter. It is interpolated as a
`format!` argument, not as the format string (`xml.rs:117`). Artifact
paths are all internally derived (`spec.spec_md_path`,
`spec.tasks_md_path`, `spec.dir.join("REPORT.md")` at `xml.rs:35-58`),
never from tag content — no path-traversal vector.

ReDoS: the reused regexes are fully anchored with bounded, non-nested
quantifiers, and the `regex` crate is linear-time by construction — no
catastrophic-backtracking surface.

Arithmetic / panics: balance pass uses `HashMap` entry + `Vec::pop`
via `.get_mut().and_then(Vec::pop)` (`xml.rs:83`) and `.or_default().push()`
— no slice indexing, no `unwrap`/`expect`/`panic!` in production paths,
no integer arithmetic in this file (the line counter's `saturating_add`
lives in the already-reviewed T-001 scanner helper).

Data exposure: `ForeignTag` and the emitted `Diagnostic` carry only the
element name and 1-indexed line, never raw artifact body content
(`xml.rs:110-118`), so no sensitive file contents leak into diagnostics
or logs. The change is security-positive: it makes a previously-silent
leaked-tag class loud at verify time.
</review>
<review date="2026-06-11T01:45:50Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. Every covered requirement's done-when maps to the diff and to a passing scenario test. REQ-001: shared single message template in `orphan_diagnostic` (speccy-core/src/lint/rules/xml.rs:110), parameterized only by tag name with no open/close wording branch; bare-close orphan, dangling-open orphan, and balanced cases proven by CHK-001..003. REQ-002: void guard `is_void_element_name` before push (xml.rs:87) proven by CHK-004. REQ-003: fence-aware scan proven by CHK-005 incl. mixed fenced+unfenced. REQ-004: SPEC/TASKS/REPORT all wired (xml.rs:34-60), three-artifact fan-out proven by CHK-006 — the journal portion (CHK-007) is deferred to sibling T-003 per the explicit T-002 task body ("the journal artifact is added in T-003") and T-002's `<task-scenarios>` omitting CHK-007, so this is a SPEC-sanctioned decomposition split, not dropped scope. REQ-005: `("XML-001", Level::Error)` registry append + re-blessed snapshot (registry_matches_snapshot passes); CHK-008 in speccy-cli/tests/verify.rs uses status:implemented to dodge the in-progress demotion and asserts exit 1 plus JSON file=SPEC.md + line present. Non-goals honored: scanner passthrough untouched, no autofix, no cross-name nesting, no unparsed-prose coverage. No open question silently resolved; changelog intent (initial + DEC-004 decompose) matches the diff.
</review>
<review date="2026-06-11T01:46:05Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The XML-001 balance pass in `speccy-core/src/lint/rules/xml.rs:64` is correct across the control-flow paths I checked. The close arm (`xml.rs:83`, `open_lines.get_mut(&tag.name).and_then(Vec::pop).is_some()`) handles all three states correctly: absent stack and present-but-empty stack both yield a dangling-close emission, present-with-elements pops and matches — no off-by-one. Void filtering (`xml.rs:87`) skips void opens from the push without misclassifying them, satisfying REQ-002; void closes correctly remain flaggable. Fence exemption is delegated to the scanner so a fenced open never pushes and an unfenced close still pops empty (CHK-005). The `&&` open/close branch logic is not inverted. The HashMap-ordered dangling-open emission (`xml.rs:98`) is nondeterministic per call but fully normalized by the `(spec_id, code, file, line)` sort in `lint/mod.rs:55`, and the sort key is unique per diagnostic (one tag per line), so final output is deterministic. The REPORT.md path derivation (`spec.dir.join("REPORT.md")`) and the TASKS.md `&&`-guarded path are sound. Cross-name interleaving is left unenforced per DEC-002 as intended. Integration tests in `speccy-core/tests/lint_xml.rs` and the CLI gate test in `speccy-cli/tests/verify.rs` pass.
</review>
<review date="2026-06-11T01:46:49Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style checks pass.

Naming and module structure: `pub mod xml;` in `rules/mod.rs` follows the alphabetical sibling order (`jnl`, `qst`, `req`, `rpt`, `spc`, `tsk`, `vet`, `xml`). The `XML_001: &str` constant and `balance_pass`/`orphan_diagnostic` helpers mirror the `JNL_001`/`RPT_001` constant + helper shape of `jnl.rs`/`rpt.rs`.

Imports in `xml.rs`: all eight `use` items are consumed — `Diagnostic`, `Level`, `ParsedSpec`, three whitelist constants, `collect_code_fence_byte_ranges`, `is_void_element_name`, `scan_foreign_tags`, `camino::Utf8Path`, `std::collections::HashMap`. No unused or missing imports.

Production-code hygiene: no `.unwrap()`, no `[i]` slice indexing, no `#[allow(...)]`. The `tasks_md_path` optional (`Option<Utf8PathBuf>`) is handled with a let-chain `&& let Some(tasks_path) = spec.tasks_md_path.as_deref()` — correct Rust 2024 idiom.

`lint/mod.rs` module-doc update: `XML-*` appended to the stable-codes list at the correct position; the inline call comment follows the SPEC-number + rule-motivation format used by the `JNL-*` and `VET-*` entries immediately above.

`lint_xml.rs` test file: `#![expect(clippy::panic_in_result_fn, reason = "...")]` mirrors `lint_jnl.rs` exactly. Imports match the sibling pattern (`camino::Utf8PathBuf`, `speccy_core::lint::{Diagnostic, run}`, `supersession_index`, `scan`, `tempfile::TempDir`). All six CHK assertions are non-vacuous — each gates a real behavioral property: CHK-001 checks count + file path + distinct lines; CHK-002 checks count + message content + file; CHK-003 checks zero hits; CHK-004 checks two separate fixture runs; CHK-005 checks both the silence case and the fenced+unfenced mixed case; CHK-006 checks count + all three artifact paths.

CHK-008 in `verify.rs`: the `line` assertion `field(xml_001, "line").as_u64().is_some()` is not vacuous — it confirms the diagnostic carries a numeric line (it could be absent/null if the implementation failed to attach a location) and matches the `.is_some_and(...)` assertion pattern used in adjacent tests.

`lint_registry.snap`: `XML-001	error` appended as the last entry; snapshot has a trailing newline (confirmed by byte probe: `0a`).

No dead code, no reinvented helpers, no lint suppressions, no naming drift found.
</review>
<review date="2026-06-11T01:47:12Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
XML-001 balance lint tests genuinely drive the real lint engine and CLI verify path -- no mocking of the system under test. `speccy-core/tests/lint_xml.rs` calls the real `speccy_core::lint::run`; `speccy-cli/tests/verify.rs:977` calls the real `verify::run`. Verified mutation-sensitivity by rewriting the implementation three ways: (1) dropping the void-exemption guard at `xml.rs:87` fails `xml_001_exempts_void_open_but_not_non_void` (CHK-004); (2) neutering the leftover-stack dangling-open loop at `xml.rs:98` fails CHK-002/004/006; (3) bypassing fence ranges at `xml.rs:71` fails `xml_001_exempts_fenced_orphan_close` (CHK-005). All restored green afterward. Assertions are substantive (exact counts, distinct lines, artifact paths, message names the tag, exit==1 + JSON XML-001 file/line) -- no `is_ok`/`!is_empty` vacuity. Roll call is complete for T-002's `<task-scenarios>` (CHK-001..006 demonstrated, CHK-008 demonstrated, registry pin hygiene -> `registry_matches_snapshot`); CHK-007 (journal artifact) is legitimately carved out to sibling T-003 per the task body and is absent from T-002's scenario block, so its omission is not a silent gap. Evidence is the inline roll call rather than a separate file; every demonstrated CHK cites a named test that exists in the diff and I confirmed the red-then-green transition by mutation. CHK-008 asserts the JSON rendering (a contracted verify output mode) for path+line rather than the human renderer, an adequate deterministic proxy matching the sibling RPT-* precedent.
</review>
