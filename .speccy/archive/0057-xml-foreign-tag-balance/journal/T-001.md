---
spec: SPEC-0057
task: T-001
generated_at: 2026-06-11T01:30:53Z
---

<implementer date="2026-06-11T01:30:53Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: bounded to `speccy-core/src/parse/xml_scanner/`. Reuse-as-is: `HTML5_ELEMENT_NAMES` (subset test), and the private `next_line` walk, `range_inside_any_fence` skip, `open_tag_regex()` / `close_tag_regex()` strict regexes inside `scan_foreign_tags` — the SPEC DEC-004 single-source tag grammar, not forked. Write-fresh: `VOID_ELEMENT_NAMES` + `is_void_element_name` (mirrors the existing `HTML5_ELEMENT_NAMES` + `is_html5_element_name` pair; searched html5_names.rs, no void set existed); `ForeignTag` struct + `scan_foreign_tags` + private `classify_foreign_line` (searched mod.rs for an existing foreign-tag view, only `scan_tags` keeping the whitelisted view existed — this is its declared inverse).
- Completed: added `VOID_ELEMENT_NAMES` (14 HTML5 void names) and `is_void_element_name` to `html5_names.rs`; re-exported both from `xml_scanner/mod.rs`. Added `pub struct ForeignTag { name, is_close, line }` and `pub fn scan_foreign_tags(source, code_fence_ranges, whitelist) -> Vec<ForeignTag>` — inverse of `scan_tags`, reusing the same line walk / fence skip / strict open+close regexes, emitting on the non-whitelisted branch with a 1-indexed line stamp. Fence skip lives in the helper (REQ-003 exemption). Self-closing `<foo/>` never matches the strict open regex. No balance logic, no void filtering, no diagnostics — callers decide. `scan_tags` passthrough untouched.
- Undone: nothing in scope. The `XML-001` lint, balance computation, and void filtering are explicitly later tasks (T-002+); this task is foundation-only per the task body.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no evidence paper-trail file — this foundation-only task lands no `XML-001` diagnostic surface, so the covered CHKs are not demonstrable at this altitude. Roll call for CHKs under REQ-002 / REQ-003:
  - CHK-004 (void-element fixture vs non-void fixture produce 0 / 1 `XML-001`): judgment-only at this task — the lint engine that emits `XML-001` is built in T-002; this task only lands `VOID_ELEMENT_NAMES` + `is_void_element_name`, the substrate CHK-004 will consume. Proven inert-but-correct by `void_set_is_subset_of_html5_element_set` (hygiene, `html5_names.rs`).
  - CHK-005 (fenced-only orphan close produces no `XML-001`): judgment-only at this task — no lint emits yet; the fence exemption substrate is `scan_foreign_tags`'s fence skip, proven by `scan_foreign_tags_skips_fenced_lines` (hygiene, `xml_scanner/mod.rs`) which shows a fenced `</custom>` yields no `ForeignTag`.
  - Task `<task-scenarios>` (the demonstrable surface at this altitude): all proven under `cargo test` as hygiene-label unit tests in `xml_scanner/mod.rs` — `scan_foreign_tags_returns_open_and_close_with_lines` (open@3, close@7), `scan_foreign_tags_skips_fenced_lines`, `scan_foreign_tags_excludes_whitelisted_names`, `scan_foreign_tags_ignores_self_closing` — and `void_set_is_subset_of_html5_element_set` in `html5_names.rs`.
- Discovered issues: SPEC-0057's own committed `SPEC.md` violated the in-tree blank-line-after-close convention (6 `</scenario>` lines directly followed by `</requirement>`), failing `every_in_tree_spec_md_has_blank_line_after_each_close_tag` on a stashed tree before my changes — a decompose-time authoring slip, not introduced here. Inserted a blank line at each of the 6 sites so the workspace hygiene gate passes. Body-only edit; no requirement semantics changed and the recorded spec hash did not drift (consistency reports no hash drift, only the expected in-progress-orphaned dirty-tree drift).
- Procedural compliance: followed speccy-work first-attempt branch — clean tree at entry, transition to in-progress, reuse survey, tests-first, self-review against the four reviewer north-stars + convention-drift checklist, four-gate hygiene suite, transition to in-review, this append. No edits to `state` in TASKS.md by hand. No shipped skill/module friction encountered.
</implementer>
<review date="2026-06-11T01:32:26Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. Foundation-only scanner slice with no auth, secret, crypto, or
network surface; security review scoped to injection, arithmetic
safety, and data exposure for this in-memory parser primitive.
Regexes are reused from the existing single source and are anchored
with bounded quantifiers (`open_tag_regex`/`close_tag_regex`,
`xml_scanner/mod.rs:122-139`) — no ReDoS backtracking surface
introduced. `scan_foreign_tags` (`xml_scanner/mod.rs:254-285`) operates
on an in-memory `&str` with no filesystem I/O; the
`Utf8Path::new("<foreign-tag-scan>")` placeholder feeds only
`next_line`'s signature and its `Err`/`None` arms terminate the walk —
no path-traversal vector. Byte arithmetic uses `checked_add`/
`saturating_add` and capture access uses `.get().unwrap_or_default()`
with no slice indexing, so no panic or overflow path. `ForeignTag`
records only element name + line number, never body content, so there
is no sensitive-data leak into the diagnostic substrate.
</review>
<review date="2026-06-11T01:32:41Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
Pass. T-001 is foundation-only (SPEC DEC-004); the diff lands exactly the substrate slice the task promised and sneaks in no excluded scope. `scan_foreign_tags` does no balance computation and no void filtering (`speccy-core/src/parse/xml_scanner/mod.rs`), matching the non-goal "detection lives in the lint engine, not the scanner" — `is_void_element_name` is a standalone helper, never called inside the scanner, so the void/fence exemptions stay the T-002 lint's job. All five `<task-scenarios>` map to tests: open@3/close@7, fenced-skip, whitelist-exclude, self-closing-ignore, and the void-subset invariant. The 14 `VOID_ELEMENT_NAMES` match REQ-002 verbatim. REQ-002 / REQ-003 `<done-when>` are framed around `XML-001` emission, which does not exist at this altitude — correctly deferred to sibling T-002 (which also covers REQ-002/REQ-003), so no observable requirement is dropped; the implementer's evidence roll-call marks CHK-004/CHK-005 judgment-only honestly. Changelog (2026-06-10 DEC-004) reflects current intent and the diff matches it; no Open Questions section, so none silently resolved.
</review>
<review date="2026-06-11T01:33:10Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects at or above the reporting bar. The new `scan_foreign_tags` walk in `speccy-core/src/parse/xml_scanner/mod.rs` faithfully mirrors the proven `scan_tags` loop: `while line_start <= source.len()` with `next_line` returning `None` on empty remainder terminates the walk, and the no-trailing-newline final line yields `next_start_in_body = source.len()+1`, exiting the loop on the next bound check — no infinite loop and no off-by-one. The 1-indexed `line_no` is incremented once per iteration starting at 1, matching the open@3/close@7 test expectation. Fence ranges are computed over `source` and the walk uses `body_offset=0`, so `range_inside_any_fence` offsets align — the REQ-003 fence skip is correct. `classify_foreign_line` mirrors `classify_line`'s non-whitelisted branch: same `open_tag_regex`/`close_tag_regex`, same `caps.get(1)` name extraction, same `whitelist.contains(&name.as_str())` filter, emitting on the foreign branch instead of returning early. Self-closing `<custom/>` correctly never matches the strict open regex. The `let Ok(Some(...)) = next_line(...) else { break }` collapses the `Err` arm into walk-termination rather than propagating it, but `next_line` only errors on byte-arithmetic overflow that cannot occur for an in-memory string, so the swallowed error is unreachable — acceptable for the non-fallible signature. `void_set_is_subset_of_html5_element_set` proves the structural subset invariant. All 11 xml_scanner lib unit tests pass.
</review>
<review date="2026-06-11T01:33:16Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. Foundation-only scanner slice; both covered CHKs (CHK-004 under
REQ-002, CHK-005 under REQ-003) are correctly labelled judgment-only and
deferred to sibling T-002, which is where the `XML-001` lint engine that
both scenarios assert against ("when the lint engine runs … produces N
diagnostics") is actually built — confirmed against the sibling index
(T-002 covers REQ-002/REQ-003). No `XML-001` emission path exists in this
diff, so neither CHK is demonstrable by a deterministic command here; the
label is plausible and not mine to block on.

The demonstrable substrate is proven by non-vacuous unit tests I re-ran
green (`cargo test -p speccy-core --lib xml_scanner`, 9 in-scope tests
pass):
- `scan_foreign_tags_returns_open_and_close_with_lines`
  (`xml_scanner/mod.rs`) asserts full `ForeignTag` struct equality
  including the 1-indexed line stamps (open@3, close@7), so it catches
  off-by-one line counting, dropped close detection, and swapped
  `is_close` — not a loose count.
- `scan_foreign_tags_skips_fenced_lines` drives the real fence path: the
  triple-backtick range covers the `</custom>` on line 3; removing the
  `range_inside_any_fence` guard would emit a `ForeignTag` and fail it.
  This is the REQ-003 exemption substrate.
- `scan_foreign_tags_excludes_whitelisted_names` and the positive test
  together pin both directions of the `!whitelist.contains` branch, so an
  inverted whitelist condition fails the suite.
- `scan_foreign_tags_ignores_self_closing` confirms `<custom/>` is
  rejected by the strict `^<([a-z][a-z-]*)…>$` open regex (verified the
  regex source at `mod.rs:128`).
- `void_set_is_subset_of_html5_element_set` (`html5_names.rs`) is a
  structural subset invariant, not a hand-copied literal — compliant with
  the no-vacuous-test rule.

Evidence contract: the `Evidence:` field is present and the per-CHK roll
call is complete (no covered CHK silently omitted). It names no
paper-trail path that is then missing; it states with reasoned
justification that no red-then-green surface exists at this altitude,
which is consistent because both covered CHKs are legitimately
judgment-only and the hygiene-adjacent task-scenario claims each cite a
specific test name that I re-ran green. No fabrication pattern matches.
</review>
<review date="2026-06-11T01:33:22Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style checks pass.

Conventions: `VOID_ELEMENT_NAMES` / `is_void_element_name` pair mirrors the pre-existing `HTML5_ELEMENT_NAMES` / `is_html5_element_name` pair in `html5_names.rs` exactly — naming and export pattern match. `ForeignTag` struct and `scan_foreign_tags` pub-export order in `mod.rs` follows the `RawTag` / `scan_tags` precedent.

Pattern reuse: `classify_foreign_line` replicates the `.get(1).map(|m| m.as_str().to_owned()).unwrap_or_default()` capture extraction pattern used in `classify_line` (lines 399, 419) and `build_open_tag` (lines 455-456). `trim_start().trim_end()` matches the `trimmed.trim_end()` pipeline in `classify_line`.

`#[must_use]` annotations carry descriptive messages per the `rust-code-quality.md` rule. No bare `#[allow(...)]` suppressions — the existing `#[expect(clippy::unwrap_used, reason = "...")]` pattern is not replicated here because the new code avoids `.unwrap()` in production paths.

SPEC.md edits: blank-line-after-close-tag hygiene fix (6 sites) matches the existing in-tree convention and passes the `every_in_tree_spec_md_has_blank_line_after_each_close_tag` gate.

Tests: all four task-scenario tests are present and non-vacuous; each exercises a real behavioral property (`scan_foreign_tags_returns_open_and_close_with_lines`, `_skips_fenced_lines`, `_excludes_whitelisted_names`, `_ignores_self_closing`). The `void_set_is_subset_of_html5_element_set` test derives a structural property rather than re-asserting a copied literal — compliant with the no-vacuous-tests rule.

No dead imports, unused variables, or reinvented helpers found. No lint suppressions added.
</review>
