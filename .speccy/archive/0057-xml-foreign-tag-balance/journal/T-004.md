---
spec: SPEC-0057
task: T-004
generated_at: 2026-06-11T02:05:43Z
---

<implementer date="2026-06-11T02:05:43Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area-mapped the three parser modules and the existing roundtrip tests. Reuse-as-is: `parse_spec_xml` / `parse_task_xml` / `parse_report_xml` (public re-exports in `speccy-core/src/parse/mod.rs`) and the canonical valid fixtures at `tests/fixtures/{spec,task,report}_xml/canonical.md` — the valid-fixture sources the task directs me to reuse. Write-fresh: new `speccy-core/tests/raw_retention.rs`. Searched the three `*_roundtrip.rs` tests for an existing `raw == source` assertion; none exists (they compare typed-field projections only), so the lock is genuinely new. New top-level symbols: `read_fixture` helper (no existing shared fixture-reader exported across test crates; each roundtrip test hand-rolls its own private `load_fixture`, so a fresh local helper matches convention) and the three `#[test]` fns.
- Completed: Added `speccy-core/tests/raw_retention.rs` with three tests asserting `SpecDoc.raw`, `TasksDoc.raw`, and `ReportDoc.raw` are each byte-identical (`==`) to the canonical fixture source passed to the parser. No production code changed (task is test-lock only).
- Undone: Nothing in scope. REQ-006 / CHK-009 fully covered.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok; the MPL-2.0 unmatched-allowance line is a pre-existing benign warning, not a failure).
- Evidence: roll call for CHKs under REQ-006:
  - CHK-009 (parsed SpecDoc/TasksDoc/ReportDoc.raw byte-identical to source): hygiene → `spec_doc_retains_byte_identical_raw_source`, `tasks_doc_retains_byte_identical_raw_source`, `report_doc_retains_byte_identical_raw_source` in `speccy-core/tests/raw_retention.rs`, all green under `cargo test --workspace`. Non-vacuous: each parser assigns `raw: source.to_owned()` today; a refactor storing a trimmed/normalised view would break `raw == source` against fixtures that carry trailing/structural bytes.
- Discovered issues: none.
- Procedural compliance: Followed speccy-work first-attempt recipe. No skill/module friction encountered; no edits to `resources/` needed.
</implementer>
<review date="2026-06-11T02:06:55Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security exposure in this test-only change. `speccy-core/tests/raw_retention.rs` reads three static, version-controlled local fixture paths via `fs_err::read_to_string` (no untrusted input, no path-traversal vector — paths are literals) and asserts `raw == source` on parsed docs. No production code touched, no auth/authz boundary, no input-validation or injection surface, no secret handling, no logging of sensitive data, no crypto, and no new dependency. The `#![allow(clippy::expect_used)]` is correctly scoped to test code per the project's allow-expect-in-tests convention.
</review>
<review date="2026-06-11T02:07:24Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-006 / CHK-009 fully delivered. The three `<done-when>` items (SpecDoc/TasksDoc/ReportDoc `.raw` byte-identical to source) each map to a dedicated test in `speccy-core/tests/raw_retention.rs`: `spec_doc_retains_byte_identical_raw_source`, `tasks_doc_retains_byte_identical_raw_source`, `report_doc_retains_byte_identical_raw_source`, each asserting `assert_eq!(doc.raw, source)`. Verified the property is real and non-vacuous: all three docs carry `pub raw: String` and assign `raw: source.to_owned()` (`spec_xml/mod.rs:60,348`, `task_xml/mod.rs:62,443`, `report_xml/mod.rs:52,262`), so a refactor storing a trimmed/normalised view would break the lock against fixtures carrying trailing/structural bytes. Tests reuse the canonical valid fixtures as the task directed and pass green (`cargo test -p speccy-core --test raw_retention` → 3 passed). Scope is clean: test-lock only, no production code touched, consistent with the SPEC's "test only" task body and the no-autofix/no-scanner-change non-goals. No open questions silently resolved; changelog shows raw-retention lock was named intent from the initial SPEC.
</review>
<review date="2026-06-11T02:07:47Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-009 (REQ-006) is genuinely demonstrated. `speccy-core/tests/raw_retention.rs:31,43,55` assert `doc.raw == source` against the canonical fixtures for SpecDoc/TasksDoc/ReportDoc, which is the exact byte-identical-retention property REQ-006 locks. Non-vacuous: each parser today assigns `raw: source.to_owned()` (spec_xml/mod.rs:348, task_xml/mod.rs:443, report_xml/mod.rs:262); all three fixtures end with a trailing newline after structural close-tags, so a refactor storing a trimmed/normalised/re-serialised view would drop bytes and break the `==` assertion. Ran `cargo test -p speccy-core --test raw_retention` -> 3 passed. The three test fns named in the journal roll call appear verbatim in the (untracked WIP) file; no fabrication pattern. Evidence roll call covers the sole CHK under the sole covered REQ; `hygiene` label is correct for a pure test-lock task with no production change and no red phase to demonstrate, and it cites specific test names + file path per the hygiene-cite rule.
</review>
<review date="2026-06-11T02:08:07Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Pure test-lock task with no production changes. The three tests in `speccy-core/tests/raw_retention.rs` each read a canonical fixture's bytes, parse via the correctly-signed public parser (`parse_spec_xml`/`parse_task_xml`/`parse_report_xml`, all `(source: &str, path: &Utf8Path) -> ParseResult<..Doc>`), and assert `doc.raw == source`. No control-flow, Option/Result, boundary, concurrency, or resource-leak defects: the `.expect()` calls on fixture read and parse produce hard test failures rather than false passes. The assertion is genuine — each parser assigns `raw: source.to_owned()` (spec_xml/mod.rs:348, task_xml/mod.rs:443, report_xml/mod.rs:262) and the report fixture ends with a trailing newline after `</report>`, so a future trimming/normalising refactor would break `raw == source`. `cargo test -p speccy-core --test raw_retention` is green (3 passed). Test-quality judgment is out of lane (tests persona).
</review>
<review date="2026-06-11T02:08:28Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No blocking style issues found.

Checked: AGENTS.md conventions, naming, suppression annotations, dead code, reuse survey, test vacuity.

One non-blocking aside: `speccy-core/tests/raw_retention.rs:1-4` uses `#![allow(clippy::expect_used, reason = "...")]` while AGENTS.md mandates `#[expect(..., reason = "...")]` over `#[allow(...)]`, and `lint_xml.rs` (T-002) correctly uses `#![expect(...)]`. However, `clippy.toml` sets `allow-expect-in-tests = true`, so `clippy::expect_used` does not fire in test files — the suppression is vacuous and the lint cannot be confirmed to fire here. Per the grounding rule, no blocking verdict is warranted. The sibling roundtrip tests predate the rule and share the same pattern. Recommend aligning with `lint_xml.rs`'s `#![expect]` form on a future pass.

All other conventions satisfied: imports are used, tests gate real invariants (byte-identical `raw` field is falsifiable), reuse survey names accurate symbols (`parse_spec_xml`/`parse_task_xml`/`parse_report_xml` and canonical fixtures), no reinvented helpers, `#[allow]` suppression carries a reason string. Hygiene gates reported clean by implementer.
</review>
