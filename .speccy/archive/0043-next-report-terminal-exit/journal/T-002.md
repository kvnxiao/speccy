---
spec: SPEC-0043
task: T-002
generated_at: 2026-05-23T22:00:00Z
---

<implementer date="2026-05-23T22:00:00Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Wired terminal-state exit code `2` and the stderr
  advisory into the per-spec form of `speccy next`. Changes:
  (1) Added `TerminalReason` enum (`Completed` / `Dropped` /
  `Superseded`) and `render_json_per_spec_with_reason` /
  `render_text_per_spec_with_reason` helpers in
  `speccy-cli/src/next_output.rs` so the existing JSON `reason`
  field carries the actual terminal-state label rather than always
  `"completed"`. (2) Changed `speccy_cli::next::run` signature to
  `Result<i32, NextError>` taking both `out` and `err` writers,
  mirroring `speccy_cli::check::run`. Per-spec branch was extracted
  into a `run_per_spec` helper (clippy `too_many_lines`). The
  helper short-circuits on `ParsedSpec.status_or_in_progress()` for
  `Dropped` / `Superseded` (regardless of task state) and otherwise
  consults `compute_for_spec`; `None` from `compute_for_spec`
  resolves to `TerminalReason::Completed` (REPORT.md present, per
  REQ-002 / T-001). When a terminal reason is set, the helper
  writes the stderr line `speccy next: SPEC-NNNN is {reason}; run
  \`speccy archive SPEC-NNNN\` to move it out of the active tree.`
  and returns exit code `2` (exposed as
  `speccy_cli::next::TERMINAL_EXIT_CODE`). Workspace form is
  unchanged. (3) Updated `run_next` in `speccy-cli/src/main.rs` to
  thread the new exit code through to the process exit and pass a
  stderr writer; mapped `i32 → u8` via `u8::try_from` with a
  safe fallback to `1` rather than `unwrap`. (4) Updated callers in
  `speccy-cli/tests/next_json.rs` and `next_text.rs` to pass the
  new stderr writer. (5) Updated the existing
  `per_spec_json_null_when_all_done_and_report_present` test in
  `next_derived.rs` to assert exit code `2` instead of `success()`
  — that test was the only pre-existing assertion that the
  completed-with-REPORT path exits zero, and it now reflects the
  new REQ-003 contract.

- Undone: Nothing in scope of T-002 is deferred. T-003 (template
  sweep) and T-004 (mirror to shipped packs) remain pending as
  separate tasks per the SPEC's decomposition.

- Hygiene checks:
  - `cargo test --workspace` — pass.
  - `cargo clippy --workspace --all-targets --all-features --
    -D warnings` — pass.
  - `cargo +nightly fmt --all --check` — pass.
  - `cargo deny check` — not re-run; no dependency changes.

- Evidence: paper trail in the new tests below; manual verification
  recorded inline.
  Roll call for CHKs under REQ-003:
  - CHK-006 (in-tree SPEC-0001 → exit 2, JSON envelope, stderr
    archive suggestion): demonstrated → new test
    `per_spec_terminal_completed_exits_2_with_stderr_and_envelope`
    in `speccy-cli/tests/next_json.rs` plus
    `per_spec_terminal_completed_cli_exits_2` in
    `speccy-cli/tests/next_text.rs`. Manual run against the real
    in-tree SPEC-0001 produced exit=2, the exact stderr line, and
    the JSON envelope with `"reason":"completed"`.
  - CHK-007 (fixture `status: dropped` → exit 2,
    `"reason":"dropped"`): demonstrated → new test
    `per_spec_terminal_dropped_exits_2_with_dropped_reason` in
    `next_json.rs`.
  - CHK-008 (fixture `status: superseded` → exit 2,
    `"reason":"superseded"`): demonstrated → new test
    `per_spec_terminal_superseded_exits_2_with_superseded_reason`
    in `next_json.rs`.
  - CHK-009 (fixture with one pending task → exit 0, empty
    stderr, non-null next_action): demonstrated → new test
    `per_spec_non_terminal_exits_0_with_empty_stderr` in
    `next_json.rs` and `per_spec_non_terminal_cli_exits_0` in
    `next_text.rs`.

- Discovered issues: None. The `ParsedSpec.status_or_in_progress()`
  accessor named in the SPEC's Assumptions already returned
  `SpecStatus::Dropped` / `Superseded` directly; no new parsing
  surface needed.

- Procedural compliance: Followed the speccy-work skill body
  end-to-end. No skill-file friction encountered; no edits made to
  `skills/` or shipped packs (T-004 owns the mirror step).
</implementer>

<review date="2026-05-23T22:15:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-002 implementation faithfully satisfies REQ-003. All four CHK scenarios (CHK-006 completed, CHK-007 dropped, CHK-008 superseded, CHK-009 non-terminal) are demonstrated via new tests in `speccy-cli/tests/next_json.rs` and `next_text.rs`, plus the existing `per_spec_json_null_when_all_done_and_report_present` test in `next_derived.rs` was correctly updated to assert exit code 2. REQ-003 done-when items confirmed against `speccy-cli/src/next.rs::run_per_spec` and `speccy-cli/src/next_output.rs::TerminalReason`: exit code 2 via `TERMINAL_EXIT_CODE` matches DEC-002; stderr line contains the required `completed`/`dropped`/`superseded` substring plus the archive suggestion; JSON envelope shape preserved unchanged (`schema_version: 1`, `next_action: null`, `reason` field) honouring DEC-003; non-terminal kinds continue to exit `0` (DEC-004); workspace form untouched; dropped/superseded short-circuit at the top of `run_per_spec` correctly overrides `compute_for_spec` per REQ-003 prose. Non-goals respected: no new envelope fields, no per-spec verify filter, no VET.md backfill, `speccy check` untouched. Dispatcher in `main.rs` cleanly threads the new exit code via `u8::try_from(code).unwrap_or(1)` and existing `NextError` variants keep exit `1` so exit code `2` does not collide.
</review>

<review date="2026-05-23T22:15:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
Tests under T-002 genuinely exercise REQ-003's terminal-state behaviour for all four CHKs. CHK-006 → `per_spec_terminal_completed_exits_2_with_stderr_and_envelope` in `speccy-cli/tests/next_json.rs` calls the real `run()` against a fixture spec with a completed task + REPORT.md, parses the JSON output, and asserts exit code 2, `next_action: null`, `reason: "completed"`, plus both required stderr substrings (`SPEC-0001 is completed` and `speccy archive SPEC-0001`). Companion CLI smoke test `per_spec_terminal_completed_cli_exits_2` in `next_text.rs` exercises the actual binary through `Command::cargo_bin("speccy")`. CHK-007 → `per_spec_terminal_dropped_exits_2_with_dropped_reason` is the most rigorous of the three terminal tests: it pairs `status: dropped` with a *pending* task to specifically prove the frontmatter short-circuit overrides task-state derivation. CHK-008 → `per_spec_terminal_superseded_exits_2_with_superseded_reason` covers the symmetric superseded path. CHK-009 → `per_spec_non_terminal_exits_0_with_empty_stderr` confirms non-terminal kinds still exit 0 with an empty stderr and a non-null `next_action`. Pre-existing `per_spec_json_null_when_all_done_and_report_present` in `next_derived.rs` correctly updated from `.success()` to `.code(2)`. Tests would catch a mutant implementation that (a) returned exit 0 from the terminal path, (b) emitted the wrong reason slug, (c) ignored the frontmatter status, or (d) leaked stderr into the non-terminal branch.
</review>

<review date="2026-05-23T22:15:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns in this task's scope. The implementation is a local CLI with no network I/O, no authentication, and no credentials. The stderr advisory message at `speccy-cli/src/next.rs:206-213` is constructed entirely from (1) the workspace-validated `spec_id` (not raw argv) and (2) `TerminalReason::as_str()`, which returns one of three hardcoded static strings — no injection vector. Exit code 2 is correctly reserved for the terminal-state success path (`Ok(TERMINAL_EXIT_CODE)`) and cannot collide with the existing error path (`Err(e) => 1` in `run_next`). No plaintext leaks, no authorization surfaces, no new dependencies introduced.
</review>

<review date="2026-05-23T22:15:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`speccy-cli/src/next_output.rs:29` — the `reason` field doc on `JsonPerSpec` is stale. It reads:

```
/// Present when `next_action` is `null`; `"completed"` or `"superseded"`.
```

But T-002 added `TerminalReason::Dropped` and the `"dropped"` string is a valid third value this field can carry (demonstrated by the new `per_spec_terminal_dropped_exits_2_with_dropped_reason` test). The doc should read `"completed"`, `"dropped"`, or `"superseded"` — the omission of `"dropped"` makes the public API contract doc incorrect and will mislead skill authors parsing this JSON.

Fix: update the doc comment on line 29 to enumerate all three values.
</review>

<blockers date="2026-05-23T22:15:00Z" round="1">
Update the `reason` field doc comment on `JsonPerSpec` at `speccy-cli/src/next_output.rs:29` to enumerate all three possible values: `"completed"`, `"dropped"`, or `"superseded"`.
- style: stale doc comment omits `"dropped"` introduced by `TerminalReason::Dropped`; misleads skill authors parsing the JSON envelope.
</blockers>

<implementer date="2026-05-23T22:30:00Z" model="claude-opus-4-7[1m]/low" round="2">
- Completed: Addressed the round-1 style blocker. Updated the
  `reason` field doc comment on `JsonPerSpec` in
  `speccy-cli/src/next_output.rs` to enumerate all three values
  carried by `TerminalReason` — `"completed"`, `"dropped"`, or
  `"superseded"`. Wrapped across two lines to stay within rustfmt's
  width budget.

- Undone: Nothing in scope of T-002 remains. T-003 and T-004 are
  still pending as separate tasks.

- Hygiene checks:
  - `cargo test --workspace` — pass.
  - `cargo clippy --workspace --all-targets --all-features --
    -D warnings` — pass.
  - `cargo +nightly fmt --all --check` — pass.
  - `cargo deny check` — not re-run; doc-comment-only change.

- Evidence: doc-comment change only; no new tests added (the
  blocker called for a doc fix, not a behavioural change). The
  round-1 test suite still covers the three-variant contract.
  Roll call for CHKs under REQ-003:
  - CHK-006: demonstrated (unchanged) → round-1 tests still
    pass.
  - CHK-007: demonstrated (unchanged) → round-1 tests still
    pass.
  - CHK-008: demonstrated (unchanged) → round-1 tests still
    pass.
  - CHK-009: demonstrated (unchanged) → round-1 tests still
    pass.

- Discovered issues: None.

- Procedural compliance: Followed the speccy-work skill body for a
  retry round. No skill-file friction.
</implementer>

<review date="2026-05-23T22:35:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round-2 change is a tight, surgical doc-comment fix to `speccy-cli/src/next_output.rs:29-30` enumerating the third `"dropped"` value on `JsonPerSpec.reason` — exactly what the round-1 `<blockers>` directive requested. No behavioural drift: REQ-003's `<done-when>` items (exit 2 with stderr archive suggestion for `completed`/`dropped`/`superseded`; exit 0 for non-terminal; schema preserved per DEC-003; no collision with existing `NextError` exits) remain satisfied by the round-1 implementation. Non-goals still respected — no new envelope fields, no per-spec `speccy verify` filter, no synthetic VET.md backfill, `speccy check` untouched. CHK-006/007/008/009 still demonstrated by the round-1 test suite. The user stories about "loop-stop signal via non-zero exit" and "archive hygiene reminder in stderr" are served unchanged. No open questions silently resolved.
</review>

<review date="2026-05-23T22:35:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round-2 change is doc-comment-only at `speccy-cli/src/next_output.rs:29-30` — the `JsonPerSpec.reason` field doc now enumerates all three values (`"completed"`, `"dropped"`, `"superseded"`) that `TerminalReason::as_str` can emit. No test surface area changes are warranted: the round-1 tests already covered every CHK under REQ-003 (`per_spec_terminal_completed_exits_2_with_stderr_and_envelope`, `per_spec_terminal_dropped_exits_2_with_dropped_reason`, `per_spec_terminal_superseded_exits_2_with_superseded_reason`, `per_spec_non_terminal_exits_0_with_empty_stderr`; CLI smokes in `next_text.rs`; updated `per_spec_json_null_when_all_done_and_report_present` in `next_derived.rs`). Re-ran `cargo test -p speccy-cli --test next_json --test next_text --test next_derived` — all 26 tests pass. The dropped/superseded tests still pair the terminal frontmatter with a *pending* task so they specifically prove the frontmatter short-circuit wins over task-state derivation — a real mutation that ignored `spec.status_or_in_progress()` would still be caught.
</review>

<review date="2026-05-23T22:35:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 change is a one-line doc-comment correction on `JsonPerSpec.reason` at `speccy-cli/src/next_output.rs:29` — no behavioral delta. The round-1 security analysis stands: local CLI only, no network I/O, no auth surfaces, no credentials, no new dependencies. The stderr advisory string is still constructed exclusively from the workspace-validated `spec_id` and `TerminalReason::as_str()` (three hardcoded static strings). Exit-code routing via `u8::try_from(code).unwrap_or(1)` in `main.rs` is correct and cannot be driven to an unexpected value by user input. No injection vectors, no sensitive data exposure, no secrets.
</review>

<review date="2026-05-23T22:35:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 style blocker resolved. On-disk `speccy-cli/src/next_output.rs` lines 29-30 now read `Present when `next_action` is `null`; `"completed"`, `"dropped"`, or `"superseded"`.` — all three `TerminalReason` variants enumerated. No other style issues: naming is consistent, `#[must_use]` attributes carry custom messages per project rules, no `#[allow]` suppressions, and `TerminalReason` correctly uses an enum rather than stringly-typed values.
</review>
