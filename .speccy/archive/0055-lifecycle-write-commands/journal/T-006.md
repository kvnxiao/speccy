---
spec: SPEC-0055
task: T-006
generated_at: 2026-06-10T06:19:43Z
---

<implementer date="2026-06-10T06:19:43Z" model="claude-opus-4-8[1m]/high" round="1">
Reuse survey:
- Reuse-as-is: `vet_xml::parse` (strict frozen-grammar authority), `Diagnostic::with_file` / `Level::Error` / `ParsedSpec` (`dir`, `spec_id`), `fs_err::read_to_string`, the lint dispatch loop in `lint/mod.rs::run`, and the REGISTRY append pattern in `lint/registry.rs`. The JNL family (`lint/rules/jnl.rs`) is the structural template for a journal-file-existence-gated lint family. The CLI verify tests reuse `tests/common` (`Workspace`, `write_spec`, `spec_md_template`) and the RPT-* test posture (`status: implemented` to keep errors gating, since `partition_lint` demotes errors on in-progress/dropped/superseded specs).
- Extend: `lint/mod.rs` (one `rules::vet::lint` call mirroring `rules::jnl::lint`); `lint/rules/mod.rs` (`pub mod vet;`); `lint/registry.rs` REGISTRY (append `VET-001`, `VET-002`); the registry snapshot `.snap`. To split VET-001 vs VET-002 cleanly I extended `validate_section` in `vet_xml/mod.rs` to emit a new typed error instead of the generic `MalformedMarker` for the three gate-placement violations.
- Write-fresh: `speccy-core/src/lint/rules/vet.rs` (searched `lint/rules/` — no VET family existed, only spc/req/tsk/qst/rpt/jnl). New top-level symbol `ParseError::VetGateStructure` in `error.rs` — searched the existing variants; the gate-structure violations were previously folded into `MalformedMarker` alongside unclosed/nested-tag failures, so there was no existing typed discriminator to reuse. A dedicated variant is the non-fragile alternative to reason-string matching. New integration test file `speccy-cli/tests/lint_vet.rs` (no VET CLI test existed).

Completed:
- Added the `VET-*` lint family in `speccy-core/src/lint/rules/vet.rs`, gated on `<spec-dir>/journal/VET.md` existence. It strict-parses the file via `vet_xml::parse`; a `ParseError::VetGateStructure` routes to VET-002, every other parse failure to VET-001, an Ok parse to no diagnostic. A read error routes to VET-001.
- Registered `pub mod vet;` (`lint/rules/mod.rs`), the `rules::vet::lint` dispatch (`lint/mod.rs`), and `VET-001` / `VET-002` as `Level::Error` in the REGISTRY (`lint/registry.rs`) and its pinned snapshot.
- Introduced `ParseError::VetGateStructure { path, offset, reason }` in `speccy-core/src/error.rs` and switched `vet_xml::validate_section`'s three gate-placement error sites (no terminal gate, gate-not-last, multiple gates) to emit it. Updated the five in-module `vet_xml` test assertions for those cases from `MalformedMarker` to `VetGateStructure`; the genuinely-different cases (block-before-first-heading, nested-tag-in-body) keep `MalformedMarker`. Updated the `parse` doc comment.
- Added `speccy-cli/tests/lint_vet.rs` (6 tests) and a T-006 evidence paper trail.

Undone:
- Nothing in scope for REQ-007. REQ-007 carries one scenario CHK (CHK-010) plus done-when bullets, all covered.

Hygiene checks:
- `cargo test --workspace` — exit 0 (0 failed).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0.
- `cargo +nightly fmt --all --check` — exit 0.
- `cargo deny check` — exit 0 (advisories / bans / licenses / sources ok).

Evidence: paper trail at `.speccy/specs/0055-lifecycle-write-commands/evidence/T-006.md`.
Roll call for CHKs under REQ-007:
- CHK-010 (a `verdict="maybe"` drift-review fires VET-001 naming the file and exits non-zero): demonstrated → evidence Scenario 1 covers the red/green pair (`out_of_domain_verdict_fires_vet_001_and_gates` in `speccy-cli/tests/lint_vet.rs`), with registry-error gating backed by evidence Scenario 6 (`registry_matches_snapshot`).

Discovered issues:
- The strict `vet_xml::parse` already rejected both VET-002 conditions, but via the generic `MalformedMarker` variant shared with unclosed/nested-tag failures. Reason-string matching to classify VET-001 vs VET-002 would have been brittle, so I added the typed `ParseError::VetGateStructure` variant emitted only by the gate-placement sites. This also tightens `parse`/`parse_in_flight`'s error contract for any future consumer that needs to distinguish gate-structure drift from other grammar failures.

Procedural compliance:
- Used `speccy task transition` for the state flip and `speccy journal append` for this block (the SPEC-0055 contract this SPEC ships). No hand-edits to TASKS.md `state` or journal files.
- No friction in the shipped skill/CLI required a `resources/` edit this turn.
- The five `vet_xml` in-module test edits and the `parse` doc-comment edit are not in T-006's advisory suggested-files list but are required by the typed-variant change: leaving them as `MalformedMarker` assertions would have failed the suite. This is surgical fallout of the chosen classification design, not scope creep.
</implementer>
<review date="2026-06-10T06:23:39Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-006 satisfies REQ-007. Every `<done-when>` bullet and all three scenarios map to a passing test in `speccy-cli/tests/lint_vet.rs`: CHK-010 (`out_of_domain_verdict_fires_vet_001_and_gates` — asserts VET-001 in `lint.errors`, file ends with `VET.md`, exit 1), the non-final-section-missing-gate VET-002 behavior (`non_final_section_without_gate_fires_vet_002_and_gates`), block-after-gate VET-002 (`block_after_gate_fires_vet_002`), well-formed → no VET-* (`well_formed_vet_md_fires_no_vet_diagnostic`), and absent VET.md → no VET-* (`absent_vet_md_emits_no_vet_diagnostic`). Both codes are registered as `Level::Error` (`speccy-core/src/lint/registry.rs` + snapshot), the lint is existence-gated and mirrors the JNL posture (`speccy-core/src/lint/rules/vet.rs:36`), and the JSON envelope counts them as errors. The serving goal (grammar-violating VET.md fails CI loudly) and user story (solo dev) are met; no non-goal breached — the change adds only the VET lint family plus a typed `ParseError::VetGateStructure` variant, no scope creep. All 6 integration tests and the 34 vet_xml unit tests pass.

Non-blocking note: REQ-007 scopes VET-002's missing-gate case to "a section other than the last lacks a terminal gate," but the lint (using strict `parse`) routes a single/last ungated section to VET-002 too (`speccy-core/src/parse/vet_xml/mod.rs:791`). That edge is in no done-when bullet or scenario, and both codes are gating errors, so the business outcome (verify fails non-zero with a VET-* error) holds regardless of which code lands. Worth a one-line clarification in the lint module doc if the boundary is later tightened, but it does not block merge.
</review>
<review date="2026-06-10T06:23:49Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-006 tests genuinely exercise REQ-007's behaviour. All three `<task-scenarios>` map to real CLI integration tests in `speccy-cli/tests/lint_vet.rs` that drive `speccy_cli::verify::run` end-to-end (no mocking of the system under test): CHK-010 via `out_of_domain_verdict_fires_vet_001_and_gates` (asserts the specific `VET-001` code, the `file` ending in `VET.md`, and exit 1); the VET-002 non-final-no-gate behavior via `non_final_section_without_gate_fires_vet_002_and_gates` (asserts VET-002 present AND VET-001 absent, catching the misrouting failure mode the typed `ParseError::VetGateStructure` variant exists to prevent); the clean/absent negative paths via `well_formed_vet_md_fires_no_vet_diagnostic` and `absent_vet_md_emits_no_vet_diagnostic` (explicit assertions across errors/warnings/info buckets, not mere absence-of-crash). A no-op or misrouting mutation of `rules::vet::lint` would be caught. Evidence at `.speccy/specs/0055-lifecycle-write-commands/evidence/T-006.md` carries a complete CHK roll call (REQ-007's sole scenario CHK-010 = demonstrated, naming the specific test), real Rust runner `test <name> ... ok` output, scoped per-test invocations rather than the workspace hygiene run, and cited test names that all appear in the diff. No fabrication patterns matched. Tests are fixture-isolated with no time/ordering/parallel-flake dependence.
</review>
<review date="2026-06-10T06:23:58Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
T-006 (VET-* lint family) introduces no security defects. The lint is a read-only, deterministic check over a fixed-literal path (`spec.dir/journal/VET.md`) with no attacker-controlled path segments — no traversal vector. Diagnostic messages embed only the file path and templated structural `reason` strings (invocation indices, counts, attribute/verdict domains) via the typed `ParseError::VetGateStructure` and the parser's other structural errors; no raw VET.md body content is echoed, so there is no sensitive-data-exposure leak (`speccy-core/src/lint/rules/vet.rs:54-76`). Read and parse failures degrade to a `VET-001` diagnostic rather than panicking — no `unwrap`/`expect`/`panic` in the production path. No new dependency, auth, crypto, or network surface. The 6 `lint_vet.rs` integration tests pass and confirm the error-gating exit code. Files: `speccy-core/src/lint/rules/vet.rs`, `speccy-core/src/lint/mod.rs`, `speccy-core/src/lint/registry.rs`, `speccy-core/src/lint/rules/mod.rs`, `speccy-core/src/error.rs`, `speccy-core/src/parse/vet_xml/mod.rs`, `speccy-cli/tests/lint_vet.rs`.
</review>
<review date="2026-06-10T06:24:08Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
All changes are surgical and convention-compliant. `vet.rs` mirrors `jnl.rs` faithfully in structure, constant declarations, and `Diagnostic::with_file` call shape. The `VetGateStructure` variant follows the existing `ParseError` struct-variant pattern with field-level doc comments and a `#[error(...)]` format string. The three `MalformedMarker` → `VetGateStructure` substitutions in `vet_xml/mod.rs` and the corresponding test-assertion updates are precise and consistent. `lint_vet.rs` matches the `#[allow(clippy::expect_used, reason = "...")]` + `#[expect(clippy::panic_in_result_fn, ...)]` header pattern used by every sibling test file, and uses `.get(i)` / `.find(...)` rather than slice indexing throughout. No dead imports, no bare suppressions, no reinvented helpers.
</review>
<review date="2026-06-10T06:24:22Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found in SPEC-0055/T-006 (the `VET-*` lint family).

Reviewed the WIP working-tree state (uncommitted, as expected for per-task review): `speccy-core/src/lint/rules/vet.rs`, the `lint/mod.rs` dispatch, `lint/registry.rs`, `lint/rules/mod.rs`, the `ParseError::VetGateStructure` addition in `error.rs`, the `vet_xml/mod.rs` error-site re-routing, and `speccy-cli/tests/lint_vet.rs`.

Control-flow and error-routing are sound:
- The existence gate (`speccy-core/src/lint/rules/vet.rs:36`) mirrors the JNL precedent; absent VET.md emits nothing.
- The `match parse_vet(...)` arm is exhaustive: `ParseError::VetGateStructure` routes to VET-002, every other parse failure to VET-001, `Ok` to no diagnostic, and the read-error path emits VET-001. No inverted condition, no swallowed error, no unwrap/panic.
- `validate_section` runs round-sequence before gate-placement and short-circuits via `?`; the four gate slice-arms (`[] if allow_open`, `[]`, `[gate_idx]` with terminal check, `_` multi-gate) cover the boundary cases with no off-by-one (`*gate_idx + 1 != inv.blocks.len()` is the correct last-position test). The `MalformedMarker`→`VetGateStructure` re-routing touches only the three gate-placement sites; block-before-heading and nested-tag cases correctly retain `MalformedMarker`→VET-001.
- All 6 integration tests pass locally (`cargo test -p speccy-cli --test lint_vet`).

One interpretation gap I deliberately do not flag as a correctness blocker: the lint uses strict `vet_xml::parse` (`LastSection::MustBeGated`), so an in-flight VET.md whose last section is not yet gate-terminated would fire VET-002, whereas the third task-scenario's "produced solely by `journal append`" intent could be read to mean a mid-vet-run file should stay clean. The code is deterministic and does exactly what it was written to do; the scenario's own fixture uses a gate-terminated file and passes. Whether strict-on-the-last-section matches REQ-007's intent is a business/scope judgment, not a logic defect, so it belongs to the business reviewer. My confidence that this is a correctness defect is below the reporting bar.
</review>
