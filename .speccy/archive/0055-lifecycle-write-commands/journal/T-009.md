---
spec: SPEC-0055
task: T-009
generated_at: 2026-06-10T19:16:47Z
---

<implementer date="2026-06-10T19:16:47Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Docs-only task — no code symbols introduced, so the per-symbol floor is vacuous. Full area-map (round-1) scoped to docs/ARCHITECTURE.md: the five sections T-009 names already existed and were edited in place (CLI Surface, TASKS.md state model, journal sections, concurrency contract, "What We Deliberately Don't Do" plus lint catalogue), reusing the existing prose structure and DEC-00x cross-reference convention already present in the file rather than introducing new sections.
- Completed: Adopted a crashed prior pass's WIP on docs/ARCHITECTURE.md and verified it fully satisfies REQ-010 and both task scenarios. All five sections reflect post-SPEC-0055 behavior: (1) CLI Surface documents task transition, journal append, and journal show with their JSON envelopes; (2) state-model "Who sets it" column rewritten to CLI-mediated writes plus the closed legal-graph paragraph; (3) journal sections describe CLI-mediated writes and the vet "skill body is the only writer" sentence is replaced by the CLI-append contract; (4) concurrency contract replaces the "sole serial writer" rule with the CLI-serialized append contract; (5) the claim-files/leases exclusions row is narrowed to task claiming with a pointer to this SPEC's append-serialization decision, and the lint catalogue gains the VET-star family (VET-001/VET-002). The journal-show JSON envelope is documented in JSON Interfaces. No gap found — adopted as-is.
- Undone: Nothing. REQ-010 is docs-only and fully covered by the adopted WIP.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: No evidence file — this task is documentation prose with a single judgment-only CHK. Roll call for CHKs under REQ-010:
  - CHK-013 (every named section describes post-SPEC-0055 behavior with no stale sole-writer/no-locking claims): judgment-only -> no scriptable proof; the SPEC itself states "content check by reviewer, not substring assertions". Verified manually: a grep for the stale phrases (sole writer / sole serial writer / only writer / transcribe / No locking) in docs/ARCHITECTURE.md returns only negated or narrowed framings (lines 527, 634, 870, 1764, 2596), none asserting the stale claim. reviewer-business / reviewer-style judge the diff on read.
- Discovered issues: None.
- Procedural compliance: This block reconstructs a crashed pass's handoff that was never recorded — a prior session implemented the docs/ARCHITECTURE.md edits and flipped T-009 pending->in-progress, then crashed before writing journal/T-009.md. Per RECONCILE-ADOPT directives I treated the dirty paths (docs/ARCHITECTURE.md, the T-009 state flip in TASKS.md, both orchestrator-confirmed task-scoped) as adopt-in-place WIP, did not halt on the clean-tree precondition, and left the task at in-progress (a --to in-progress transition would be a same-state no-op). No resources/ skill-module edits were needed.
</implementer>
<review date="2026-06-10T19:19:09Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. Docs-only edit to docs/ARCHITECTURE.md; no auth, secret-handling, injection, or input-validation surface introduced. Verified the security-relevant contracts the prose now asserts against the shipped code: the concurrency section's advisory-lock narrative (10s blocking-acquire timeout, derive→validate→write under a per-file lock, byte-identical-on-failure) matches `speccy-cli/src/journal.rs` (`LOCK_TIMEOUT = Duration::from_secs(10)` at journal.rs:87 and the module-doc contract at journal.rs:20-25). The rewrite correctly distinguishes the *unlocked* task-claim race (git-conflict-resolved, explicitly "not locked") from the *locked* journal-append path, so no false race-free guarantee is planted that would mislead a future implementer toward an unsafe TOCTOU. The state-graph paragraph and CLI-surface table both list the six legal edges in the exact order of the shipped `LEGAL_TRANSITION_EDGES` (task_xml/mod.rs:132-139), with the same-state no-op described accurately. `tasks_hash` is documented as CLI-computed SHA-256 that callers cannot supply — the correct integrity posture, removing a caller-spoofable field. The narrowed exclusions row scopes the lock as internal to the append command, not a task-claim lease; no overreach. No blocking findings.
</review>
<review date="2026-06-10T19:19:12Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. REQ-010's five sections all reflect post-SPEC-0055 behavior in docs/ARCHITECTURE.md and both task scenarios hold. (1) CLI surface table documents `task transition`, `journal append`, and `journal show` with the closed legal graph, DEC-004 routing, the advisory lock, and `--json: schema_version=1` (lines ~185-229). (2) State-model "Who sets it" column rewritten to "via `speccy task transition`" plus the explicit closed-edge-graph paragraph (~626-645). (3) Per-task journal section and VET.md section both rewritten to the CLI-append contract; the behavior scenario is satisfied — the old "skill body is the **only writer** ... and the skill transcribes them" sentence is replaced with "Every block is written through `speccy journal append` ..." (~780-800). (4) Exclusions row narrowed to "Claim files / leases for task pickup", explicitly stating it "does *not* forbid append serialization" with a pointer to SPEC-0055's append-serialization decision (line 2596). (5) Lint catalogue gains the `VET-` prefix and VET-001/VET-002 entries (~2462-2538). Grepped the whole file for stale sole-writer/no-locking framings: every hit (lines 527, 634, 870, 1764, 2596) is a negated or narrowed framing, none asserting the stale claim — done-when "no section still asserts sub-agents return blocks for the orchestrator to transcribe as the only write path" holds. Cross-checked the documented JSON envelopes against the shipped `speccy-cli/src/journal_show_output.rs`: `schema_version`/`latest_round`/`blocks` for task journals and `invocations` (`number`/`date`/`blocks`) for VET.md match the built types verbatim — the docs describe what shipped, not a divergent envelope. No non-goal breach (docs-only, no new feature), no open question silently resolved.
</review>
<review date="2026-06-10T19:19:44Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="1">
Pass. All five required sections are present and accurate in docs/ARCHITECTURE.md.

(1) CLI surface table (lines 185–229): `task transition`, `journal append`, and `journal show` are documented with full flag descriptions and the `journal show --json` envelope appears in the JSON Interfaces section (lines 2420–2453).

(2) State-model "Who sets it" column (lines 582–596): rewritten to "Who sets it (via `speccy task transition`)" with the closed legal graph paragraph added below.

(3) Journal sections (lines 623–636, 798–820): the vet "skill body is the only writer" sentence is replaced by the CLI-append contract; the per-task journal section likewise describes CLI-mediated writes.

(4) Concurrency contract (lines 855–875): replaces the prose-level "sole serial writer" rule with the CLI-serialized append contract explaining the advisory lock.

(5) "What We Deliberately Don't Do" row (line 2596): narrowed to task claiming with explicit pointer to append-serialization decision; lint catalogue gains VET-001 and VET-002 (lines 2529–2539).

CHK-013 scan: all four surviving occurrences of "sole writer" / "sole serial writer" are used in negation or contrast (lines 527, 634, 870, 1764) — none assert the stale claim. The phrase "skill body is the only writer" does not appear anywhere in the document. No internal contradictions introduced; the JSON Interfaces intro at line 2134 correctly lists `journal show`.
</review>
<review date="2026-06-10T19:19:52Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="1">
Pass. All five required sections are present and accurate in docs/ARCHITECTURE.md.

(1) CLI surface table (lines 185-229): task transition, journal append, and journal show are documented with full flag descriptions and the journal show --json envelope appears in the JSON Interfaces section (lines 2420-2453).

(2) State-model "Who sets it" column (lines 582-596): rewritten to "Who sets it (via speccy task transition)" with the closed legal graph paragraph added below.

(3) Journal sections (lines 623-636, 798-820): the vet "skill body is the only writer" sentence is replaced by the CLI-append contract; the per-task journal section likewise describes CLI-mediated writes.

(4) Concurrency contract (lines 855-875): replaces the prose-level "sole serial writer" rule with the CLI-serialized append contract explaining the advisory lock.

(5) "What We Deliberately Don't Do" row (line 2596): narrowed to task claiming with explicit pointer to append-serialization decision; lint catalogue gains VET-001 and VET-002 (lines 2529-2539).

CHK-013 scan: all four surviving occurrences of "sole writer" / "sole serial writer" are used in negation or contrast (lines 527, 634, 870, 1764) - none assert the stale claim. The phrase "skill body is the only writer" does not appear anywhere in the document. No internal contradictions introduced; the JSON Interfaces intro at line 2134 correctly lists journal show.
</review>
<review date="2026-06-10T19:20:12Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. T-009 is a docs-only edit to `docs/ARCHITECTURE.md` whose sole covering scenario CHK-013 is correctly labelled `judgment-only` in the journal roll call — the SPEC's own CHK-013 text stipulates "content check by reviewer, not substring assertions", and project policy forbids prose-substring tests, so no deterministic test can or should gate it. Per the tests-persona contract a judgment-only CHK is not mine to block on; I confirmed the label is plausible (no scriptable proof exists) and that CHK-013 is the only CHK under REQ-010, so the roll call omits nothing. The "No evidence file" declaration is the honest, correct shape for a prose-only task — there is no red/green paper trail to fabricate. I did verify that every gated behavior the doc *newly describes* matches what tests actually enforce in earlier tasks, so the prose does not over-claim: the six legal transition edges enumerated in the CLI-surface and state-model sections match `LEGAL_TRANSITION_EDGES` in `speccy-core/src/parse/task_xml/mod.rs:132-139` exactly (exhaustively tested at `mod.rs:1245`); the VET-001/VET-002 family is registered at `Level::Error` in `speccy-core/src/lint/registry.rs:37-38` and gated by `speccy-cli/tests/lint_vet.rs`; the `journal show` `schema_version=1` envelope matches `speccy-cli/src/journal_show_output.rs:185,198`; and the "per-file advisory lock (blocking acquire, 10-second timeout)" claim matches `LOCK_TIMEOUT = Duration::from_secs(10)` in `speccy-cli/src/journal.rs:87` with a timeout test at `speccy-cli/tests/journal_append.rs:362-436`. The REQ-010 `<behavior>` is satisfied: the vet-journal "skill body is the **only writer**" sentence is replaced by the CLI-append contract, and the remaining "sole writer"/"file-locking" occurrences are all negated/narrowed framings (lines 526-527, 870, 1764) or scoped to the unlocked task-claim race (lines 2596, 2936), none asserting the stale claim.
</review>
<review date="2026-06-10T19:20:17Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="blocking" round="1">
The `journal show --json` VET-envelope description is wrong and self-contradicting. `docs/ARCHITECTURE.md:2447` states the VET envelope "carries `invocations` (each with its `number`, `date`, and `blocks`) instead of `task` / `latest_round`", asserting the VET envelope omits `latest_round`. But `JsonVetJournal` in `speccy-cli/src/journal_show_output.rs:96-109` declares a `latest_round: Option<u32>` field (skip-if-None), and `journal_show.rs:281-282` populates it with the last invocation section's highest round whenever `--round latest` is passed — so the real VET `--json` output emits `latest_round` exactly as the task envelope does. Only `task` is genuinely task-exclusive. The very next sentence (`:2450`) then describes `latest_round` as a present field ("`latest_round` reports the highest round present after filtering"), contradicting line 2447. Important — a harness author implementing the VET envelope strictly per :2447 would drop/reject `latest_round`, a field that appears in reachable output under `--round latest`. Fix: scope the "instead of" clause to `task` only (the VET envelope keeps `schema_version`, `spec`, `generated_at`, `latest_round`, and replaces `blocks` with `invocations`).

All other audited claims match the implementation: the legal state graph (`speccy-core/src/parse/task_xml/mod.rs:132-138`: pending→in-progress, in-progress→in-review, in-review→completed, in-review→pending, in-progress→pending, completed→pending) and the same-state idempotent no-op (`classify_transition` `NoOp`, :165-166) match the CLI-surface and state-model prose; the block-type→target mapping (DEC-004) routes task blocks to `journal/<task-id>.md` and vet blocks to `VET.md` (`speccy-cli/src/journal.rs:5-9, 250-251`); the 10s lock timeout matches (`journal.rs:87 LOCK_TIMEOUT = from_secs(10)`); `--model`/`--persona`/`--verdict` required-for claims match `AppendArgs` (`journal.rs:237-243`); and VET-001/VET-002 definitions (`speccy-core/src/lint/rules/vet.rs:8-20`, both `Level::Error`, fire only when VET.md exists per :36-38) match the lint-catalogue rows.
</review>
<review date="2026-06-10T19:20:33Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All five sections named in T-009 consistently describe post-SPEC-0055 behavior.

Stale claims checked: grep for "sole writer", "sole serial writer", "only writer", "transcribe", "No locking" returns only negated/narrowed framings (lines 527, 634, 870, 1764, 2596) — none asserting the old contract.

Internal consistency: (1) Legal graph edges are identical between the CLI Surface listing (lines 191–196) and the TASKS.md State Model paragraph (lines 589–592). (2) The `journal show` CLI surface entry and its JSON Interfaces section (`## speccy journal show --json`) describe the same behavior and flags. (3) JSON example uses fictional placeholder `SPEC-0042`, not a real Speccy spec identifier. (4) VET.md section, concurrent-pickup section, and exclusions-table row all consistently point to the CLI append-lock for serialization. (5) DEC-004/DEC-006 cross-references in the new text resolve to actual decisions defined in SPEC-0055/SPEC.md.

One pre-existing inconsistency in model-identifier form (dot vs hyphen, e.g. `claude-opus-4.8` vs `claude-opus-4-8`) exists at lines 570/577/581 vs 585/2437 — not introduced by this diff, out of scope.

No naming drift, dead prose, or contradictory claims introduced by the diff.
</review>
<blockers date="2026-06-10T19:22:11Z" round="1">
docs/ARCHITECTURE.md's `journal show --json` VET-envelope description misstates the shipped shape and self-contradicts — fix before re-review.

- correctness (blocking): `docs/ARCHITECTURE.md:2446-2447` says the VET.md envelope carries `invocations` "instead of `task` / `latest_round`", asserting `latest_round` is absent from the VET envelope. It is not. `JsonVetJournal` (`speccy-cli/src/journal_show_output.rs:96-109`) declares `latest_round: Option<u32>` (skip-if-none), and `journal_show.rs:281-282` populates it under `--round latest` with the last invocation section's highest round — identical to the task envelope. Only the top-level `task` field is genuinely task-exclusive. The claim also self-contradicts line 2450 ("`latest_round` reports the highest round present after filtering"), which treats `latest_round` as a present field. Fix: scope the "instead of" clause to `task` only — state that the VET envelope keeps `schema_version`, `spec`, `generated_at`, and `latest_round`, and replaces the top-level `task`/`blocks` with `invocations` (each carrying `number`/`date`/`blocks`).
</blockers>
<implementer date="2026-06-10T19:24:33Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Docs-only retry round addressing a single non-reuse (correctness) blocker. The round-1 reuse-related survey is not re-run; no new top-level symbol introduced (prose edit to docs/ARCHITECTURE.md), so the per-symbol floor is vacuous this round.
- Completed: Amended the round-1 WIP in docs/ARCHITECTURE.md in place to resolve the correctness blocker on the `journal show --json` VET envelope (lines ~2446-2447). Rescoped the "instead of" clause to `task` only: the paragraph now states the VET envelope keeps `schema_version`, `spec`, `generated_at`, and `latest_round`, and replaces the top-level `task` / `blocks` with `invocations` (each carrying `number` / `date` / `blocks`). This removes the false claim that `latest_round` is absent from the VET envelope and restores coherence with the very next sentence (~2450), which treats `latest_round` as a present post-filter field.
- Undone: Nothing. The single round-1 blocker is fully addressed; no other blocker was raised.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: No evidence file — documentation prose with a single judgment-only CHK. Roll call for CHKs under REQ-010:
  - CHK-013 (every named section describes post-SPEC-0055 behavior with no stale/contradictory claims): judgment-only → no scriptable proof; the SPEC stipulates "content check by reviewer, not substring assertions". Verified the corrected claim against shipped ground truth: JsonVetJournal (speccy-cli/src/journal_show_output.rs:96-106) declares latest_round: Option<u32> (skip-if-none), populated under --round latest at journal_show.rs:281-282 — identical to the task envelope; only top-level task is task-exclusive. The doc now matches shipped behavior. reviewer-correctness / reviewer-business judge the diff on read.
- Discovered issues: None.
- Procedural compliance: Retry round handled per the speccy-work retry branch — did not halt on the clean-tree gate, amended the prior pass's dirty WIP in docs/ARCHITECTURE.md in place (no restore/clean/checkout/rewrite-from-scratch), addressed only the round-1 blocker directive, and flipped pending to in-progress via speccy task transition. No resources/ skill-module edits were needed.
</implementer>
<review date="2026-06-10T19:26:03Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Pass (round 2). The round-1 correctness blocker is resolved at `docs/ARCHITECTURE.md:2446-2448`: the VET envelope description now keeps `schema_version`, `spec`, `generated_at`, and `latest_round` and replaces only the top-level `task` / `blocks` with `invocations`, matching `JsonVetJournal` (`speccy-cli/src/journal_show_output.rs:96-109`, where `latest_round: Option<u32>` is skip-if-none) and ending the self-contradiction with line 2452. No security-relevant misstatement was introduced or left behind. Re-checked the integrity- and concurrency-bearing contracts the prose still asserts: the CLI-surface table (`:198-218`) correctly documents the CLI as sole authority for `date`, `round`, `generated_at`, the gate block's `tasks_hash`, and VET invocation numbering with "no flag to override any of them" — the correct non-spoofable posture, so a harness author cannot be misled into forging `tasks_hash`. The concurrency section (`:853-875`) still cleanly separates the unlocked, git-conflict-resolved task-claim race from the locked read→derive→validate→write journal append (10s blocking acquire, byte-identical-on-reject), so no false TOCTOU-safe guarantee is planted. Docs-only; no auth, secret, injection, or input-validation surface. No blocking findings.
</review>
<review date="2026-06-10T19:26:20Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. The round-1 blocker is resolved. `docs/ARCHITECTURE.md:2446-2449` now reads "For VET.md the envelope keeps `schema_version`, `spec`, `generated_at`, and `latest_round`, and replaces the top-level `task` / `blocks` with `invocations` (each carrying its `number`, `date`, and `blocks`)." This (a) keeps `latest_round` in the VET envelope, matching `JsonVetJournal` which declares `latest_round: Option<u32>` (skip-if-none) at `speccy-cli/src/journal_show_output.rs:105-106`; (b) scopes the task-exclusivity correctly to the top-level `task` field only (`JsonVetJournal` has no `task` field, since VET.md is per-spec); and (c) removes the self-contradiction with the next sentence (`:2452` "`latest_round` reports the highest round present after filtering"), which now consistently treats `latest_round` as a present field. The `--round latest` population at `journal_show.rs:281-282` sets `latest_round = last_highest` (the last invocation section's highest round), identical to the task-envelope population at `journal_show_output.rs:189`, confirming the parity the prose asserts.

Re-audit of remaining documented behaviors against shipped code, all matching: VET block flattening (`JsonVetBlock` at `journal_show_output.rs:147-168`: variants drift-review/holistic-fix/simplifier-scan/simplifier-apply/gate, round/model present only for the round-bearing types, `tasks_hash` only on `gate`) matches the catalogue and the CLI-surface `journal append --block` enumeration (`:204-207`); the `--round latest|N` "scoped to the last invocation section for VET.md" claim (`:222-223`) matches the per-section scoping at `journal_show.rs:274-285,289-292`; "filters compose conjunctively / `--json` toggles representation never content" matches the conjunctive `render_json` path (`journal_show_output.rs:176-199`); the six legal transition edges and same-state no-op (CLI surface `:191-197`) match `LEGAL_TRANSITION_EDGES`; the 10s advisory lock, sole-authority attribute derivation, and DEC-004 block-type→target routing (`:198-218`) match `journal.rs`. No surviving contradiction; no correctness defect.
</review>
<review date="2026-06-10T19:26:25Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Pass (round 2). T-009 is a docs-only edit to `docs/ARCHITECTURE.md`; its sole covering scenario CHK-013 is correctly labelled `judgment-only` in the round-2 evidence roll call — the SPEC's CHK-013 text stipulates "content check by reviewer, not substring assertions" and project policy forbids prose-substring tests, so no deterministic test can or should gate it. CHK-013 is the only CHK under REQ-010, so the roll call omits nothing; the "No evidence file" declaration is the honest shape for a prose-only task with no red/green trail to fabricate.

The round-2 amendment resolves the round-1 correctness blocker, and the behavior the doc newly describes matches what tests enforce — verified against shipped ground truth so the prose does not over-claim: `JsonVetJournal` (`speccy-cli/src/journal_show_output.rs:96-109`) declares `latest_round: Option<u32>` (skip-if-none), `render_json` populates it (`:201`), and `journal_show.rs:281-282` resolves it under `--round latest` to the last invocation section's highest round — identical to the task envelope; only top-level `task` is task-exclusive. `docs/ARCHITECTURE.md:2446-2449` now keeps `latest_round` in the VET envelope and rescopes the "instead of" clause to `task` only, ending the self-contradiction with `:2452`. The documented VET `invocations` envelope is enforced by `spec_selector_shows_vet_invocations_and_blocks` (`speccy-cli/tests/journal_show.rs:202`) and the `--round latest`/`latest_round` slice by `round_latest_verdict_blocking_returns_single_block` (`journal_show.rs:94,123`). No blocking findings.
</review>
<review date="2026-06-10T19:26:28Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
PASS (round 2). The round-1 correctness blocker is resolved: docs/ARCHITECTURE.md:2446-2448 now scopes the "instead of" clause to `task` only — the VET envelope keeps `schema_version`, `spec`, `generated_at`, and `latest_round`, and replaces the top-level `task`/`blocks` with `invocations` (each carrying `number`/`date`/`blocks`). This matches the shipped `JsonVetJournal` (`speccy-cli/src/journal_show_output.rs:96-108`: `schema_version`, `spec`, `generated_at`, `latest_round: Option<u32>` skip-if-none, `invocations`) verbatim, and the prior self-contradiction with line 2452 ("`latest_round` reports the highest round present after filtering") is gone — both now treat `latest_round` as present in the VET envelope. REQ-010's two done-when clauses hold across all five named sections: (1) CLI surface documents `task transition`/`journal append`/`journal show` with the closed legal graph, DEC-004 routing, the 10s advisory lock, and `--json: schema_version=1` (lines 185-229); (2) state-model "Who sets it" column rewritten to "via `speccy task transition`" plus the closed-edge paragraph (582-593); (3) journal section replaces the sole-writer convention with the append-lock contract (633-636); (4) concurrency section replaces the "sole serial writer" rule with the CLI-serialized append contract (863-875); (5) exclusions row narrowed to task claiming, explicitly stating it does not forbid append serialization (2598), and the lint catalogue gains VET-001/VET-002 (2531-2541). REQ-010 behavior holds: the vet "only writer" sentence is replaced by the CLI-append contract. The only surviving "sole writer" hits (527, 1764) are negated framings ("no single session has to be the journal's sole writer"), not stale assertions. No non-goal breach (docs-only, no new feature), no goals regression, no open question silently resolved.
</review>
<review date="2026-06-10T19:26:48Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Pass. The round-2 fix resolves the round-1 correctness blocker cleanly.

docs/ARCHITECTURE.md lines 2446-2449: the VET-envelope paragraph now correctly
states the envelope "keeps `schema_version`, `spec`, `generated_at`, and
`latest_round`, and replaces the top-level `task` / `blocks` with `invocations`"
— scoping the "instead of" clause to `task` only, consistent with `JsonVetJournal`
in `speccy-cli/src/journal_show_output.rs:96-109` (which declares `latest_round:
Option<u32>` as a present field). The self-contradiction with line 2452 ("`latest_round`
reports the highest round present after filtering") is resolved: both sentences
now treat `latest_round` as a present field.

Stale-claim scan of the diff's added lines: all occurrences of "sole writer" /
"sole serial writer" / "only writer" / "transcribe" / "No locking" appear in
negated or narrowed framings (contrast with the new CLI-lock contract, or the
task-claim-only "No locking" caveat). None assert the old contract.

Internal consistency: the corrected VET paragraph in the JSON Interfaces section
is coherent with the concurrency-contract rewrite (~lines 855-875), the VET.md
per-SPEC section, and the CLI-surface table entry for `journal show`. No
naming drift, dead prose, or contradictory claims introduced by the round-2 diff.
</review>
<review date="2026-06-10T19:27:18Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="2">
Pass. Round-2 fix is coherent and resolves the round-1 correctness blocker with no new contradictions.

VET-envelope fix (docs/ARCHITECTURE.md:2446-2449): The paragraph now states the VET envelope "keeps `schema_version`, `spec`, `generated_at`, and `latest_round`, and replaces the top-level `task` / `blocks` with `invocations`". This matches `JsonVetJournal` at `speccy-cli/src/journal_show_output.rs:96-109` exactly (`schema_version`, `spec`, `generated_at`, `latest_round: Option<u32>` skip-if-none, `invocations`). The `task` field is task-exclusive and absent from `JsonVetJournal`. No self-contradiction: the immediately-following sentence at :2452 ("latest_round reports the highest round present after filtering") is now consistent with the opening clause.

All five required sections verified clean in the current file:
1. CLI surface (lines 185-229): `task transition`, `journal append`, `journal show` documented with closed legal graph, DEC-004 routing, advisory lock, `--json: schema_version=1`.
2. State-model "Who sets it" column (lines 582-593): rewritten to "via `speccy task transition`" with the six-edge legal graph enumerated verbatim.
3. Journal sections (lines 626-636, 790-798): CLI-append contract replaces any sole-writer sentence; the vet section describes per-sub-agent appends through `speccy journal append`.
4. Concurrency contract (lines 863-875): replaces "sole serial writer" rule with the CLI advisory lock narrative; explicitly scopes task claiming as unlocked.
5. Exclusions row (line 2598) and lint catalogue (lines 2531-2541): row narrowed to task claiming with pointer to SPEC-0055's decision; VET-001/VET-002 added.

CHK-013 scan: grep for "sole writer", "sole serial writer", "only writer", "transcribe" returns only negated or narrowed framings at lines 527, 634, 870, 1764, 2598 — none assert the stale claim. Phrase "skill body is the only writer" is absent from the file.

Both task scenarios hold:
- Scenario 1 (vet journal section): the "skill body is the only writer" sentence is replaced by the CLI-append contract (confirmed at lines 790-798).
- Scenario 2 / CHK-013: all named sections describe post-SPEC-0055 behavior with no stale sole-writer or no-locking claims.
</review>
<review date="2026-06-10T19:27:23Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="2">
Pass. Round-2 fix is coherent and resolves the round-1 correctness blocker with no new contradictions.
</review>
