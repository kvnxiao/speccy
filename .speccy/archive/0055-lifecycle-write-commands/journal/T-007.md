---
spec: SPEC-0055
task: T-007
generated_at: 2026-06-10T06:39:56Z
---

<implementer date="2026-06-10T06:39:56Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the REQ-008 area under `resources/` (the
  covered REQ, the T-007 suggested files, and their neighbours).
  Reuse-as-is — kept the existing MiniJinja include directives for
  `verdict_return_contract.md`, `identity-sourcing.md`, and
  `no_tasks_md_writes.md` at every reviewer/vet callsite; the contract
  change is a single edit to the included partial, inherited by all
  wrappers, rather than per-file duplication. Extend — rewrote the
  shared `verdict_return_contract.md` (now defines the thin verdict
  element once) and repurposed `inline_note_format.md` to describe the
  review body piped on stdin; marked date/round CLI-stamped in the three
  journal references (journal-implementer/review/blockers.md).
  Write-fresh — the thin verdict element shape; searched
  `resources/modules/personas/` for an existing thin-verdict element and
  found only the full review / drift-review / holistic-fix / simplifier
  block shapes, so the self-closing verdict element (persona-or-role,
  verdict, model, rationale) is new. No new Rust top-level symbol
  introduced.
- Completed: Rewrote `verdict_return_contract.md` to a two-step
  contract: (1) append your own review block via `speccy journal append
  ... --block review --persona <self> --verdict <v> --model <self>` with
  the body on stdin (date/round CLI-stamped), (2) return a single
  self-closing verdict element carrying persona, verdict, model, and a
  one-line rationale. Repurposed `inline_note_format.md` to the stdin
  review-body shape. Updated all seven reviewer persona Examples
  (business, security, correctness, tests, style, docs, architecture) to
  show the append command plus the thin verdict, and their Role lines
  from "produce one inline review note" to "append one review block and
  return a thin verdict". Rewrote the three vet personas (vet-reviewer →
  drift-review, vet-implementer → holistic-fix, vet-simplifier →
  simplifier-scan/apply) to append their own blocks via `journal append`
  against a bare `SPEC-NNNN` selector and return a verdict element with a
  role attribute; removed every "orchestrator transcribes it" /
  single-writer claim and the date/round/invocation authoring
  instructions. Rewrote step 9 (and the retry-branch / first-attempt
  prose) of `phases/speccy-work.md` so the implementer block lands via
  `speccy journal append ... --block implementer --model <self>` with
  the seven-field body on stdin; deleted the date/round/generated_at
  authoring section. Marked date/round CLI-stamped in
  journal-implementer.md, journal-review.md, journal-blockers.md. Ran
  `just reeject` (14 ejected files overwritten across
  .claude/.agents/.codex); a second reeject overwrote 0, confirming
  idempotency. Updated one brittle prose-substring assertion in
  `speccy-cli/tests/skill_packs.rs`
  (reviewer_correctness_renders_with_includes_expanded_both_hosts) that
  matched removed contract wording, repointing it at the stable
  `speccy journal append` and `--block review` markers.
- Undone: T-008 (REQ-009 — orchestrator/reconcile/vet skill flows
  consume task-transition / journal-show / journal-append blockers|gate)
  and T-009 (REQ-010 — ARCHITECTURE.md) are left for their own
  implementer turns per TASKS.md. I deliberately did NOT edit
  `resources/modules/skills/*.md`, partials, reconcile-policy.md, or
  retry-shape.md — those are T-008 suggested files. The speccy-review
  skill spawn-prompt still expects the review-element marker (its test
  still passes); flipping that to thin-verdict consumption is T-008.
- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited
  0 (no dependency changes — prose/template change plus one test-string
  edit).
- Evidence: paper trail at
  `.speccy/specs/0055-lifecycle-write-commands/evidence/T-007.md`. Roll
  call for CHKs under REQ-008:
  - CHK-011 (clean tree after `just reeject`): demonstrated → evidence
    Scenario 1 shows the first reeject overwriting 14 files and the
    immediate second reeject overwriting 0, proving sources and
    ejections are in sync once committed. The non-CHK done-when bullets
    (journal-append invocation present per persona/phase, no
    date/round/tasks_hash computation instruction, Codex prose parity)
    are demonstrated in evidence Scenarios 2–4 via greps over the
    regenerated packs.
- Discovered issues: The skill-pack render suite in
  `speccy-cli/tests/skill_packs.rs` carries brittle prose-substring
  assertions over rendered persona bodies (the one I repointed matched
  the literal phrase "Your final message to the orchestrator").
  AGENTS.md discourages substring-matching human-curated prose; these
  gate that an include expanded, not a behavioural invariant. I
  repointed the one that broke at a more stable marker but did not
  re-architect the suite — a broader cleanup outside T-007 scope.
  Flagged for a future pass.
- Procedural compliance: This implementer entry lands in
  `journal/T-007.md` via `speccy journal append` (dogfooding T-002),
  not a hand-written block — the CLI stamped date/round and created no
  nested markup. The TASKS.md state for T-007 was flipped
  pending→in-progress→in-review via `speccy task transition`
  (dogfooding T-001) rather than a hand edit. No implementer-note was
  written into TASKS.md. Per the AGENTS.md friction-to-skill-update
  convention: no shipped skill body needed a fix during this task; the
  edits ARE the task (REQ-008 changes the shipped contract), all under
  `resources/` with `just reeject` regenerating the ejections, never
  editing the harness folders directly.
</implementer>
<review date="2026-06-10T06:45:29Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-007 (REQ-008) delivers exactly what the SPEC promised: loop subagents append their own blocks and return thin verdicts.

Done-when coverage verified against the diff:
- All six reviewer personas (`resources/modules/personas/reviewer-*.md`), all three vet personas (`vet-reviewer.md`, `vet-implementer.md`, `vet-simplifier.md`), and the implementer phase (`resources/modules/phases/speccy-work.md` step 9) now route their block through `speccy journal append` and return a thin `<verdict .../>` element instead of a full block body. The shared `verdict_return_contract.md` defines the single parseable thin-verdict shape once (DEC-006) and is `{% include %}`-ed by every wrapper.
- No module instructs computing `date`, `round`, `tasks_hash`, or invocation numbers; every callsite carries explicit "do not compute, supply, or mention" prose (DEC-001). The three journal references now mark `date`/`round` as CLI-stamped and document the append invocation.
- Both host ejections carry the new contract: 28 files modified across `.claude/`, `.agents/`, and `.codex/`; the Codex `.toml` bodies carry the same prose (prose-enforced parity).

Scope discipline is clean: no T-008 files (skill bodies, partials, `reconcile-policy.md`, `retry-shape.md`) and no T-009 file (`ARCHITECTURE.md`) were touched — no non-goal violated, no adjacent-task scope pulled in.

Non-blocking observation (correctly deferred, not a T-007 gap): `speccy-work.md` step 4 still reads "Flip ... by editing TASKS.md." That state-flip-to-`task transition` conversion is REQ-009/T-008 territory, and the implementer's "Undone" note scopes it out explicitly.

CHK-011 (clean tree after `just reeject`) is the orchestrator/commit-time gate; source modules and all three ejected packs carry matching contract text and the implementer attests to an idempotent second reeject (0 files overwritten).
</review>
<review date="2026-06-10T06:45:40Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-011's clean-tree proof is real and independently reproduced: `just reeject` reports `0 overwritten, 36 unchanged` against the working tree, so committed ejections are byte-identical to a fresh render of the edited `resources/` sources — the exact invariant the scenario asserts, driven through the actual codegen path (no mock). The one Rust test change (`reviewer_correctness_renders_with_includes_expanded_both_hosts` in `speccy-cli/tests/skill_packs.rs:1219`) is non-vacuous: I confirmed the old asserted string `Your final message to the orchestrator` is genuinely gone from the rendered pack (grep count 0), so the prior assertion would have failed; the repoint to `speccy journal append` + `--block review` gates the new REQ-008 contract surface rather than editorial prose. Evidence Scenarios 2-4 grep claims all verify against the live packs, the `<verdict>` element shape is uniform/parseable across all 7 reviewer personas and the 3 vet personas (the contract's stated goal), and the full workspace suite passes (every suite `test result: ok`, 0 failures). Evidence field present, file exists, roll call complete (CHK-011 = demonstrated), no fabrication pattern matched.
</review>
<review date="2026-06-10T06:45:52Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in this slice. T-007 (REQ-008) is a prose/template contract rewrite plus one test-string edit; it touches no auth, crypto, secret-handling, or sensitive-data-exposure surface.

Adversarial checks performed:
- The rewritten persona contracts (`verdict_return_contract.md`, `inline_note_format.md`) and journal references (`journal-{implementer,review,blockers}.md`) direct agents to pipe a free-text body on stdin to `speccy journal append`. I verified the security-relevant claim the prose leans on — "validation runs before any write; a malformed body leaves the journal byte-identical" and "nested journal markup rejected" — is backed by the implementation at `speccy-core/src/parse/journal_xml/serialize.rs:184` (`validate_and_render_block`) and its rejection tests (`:418` `nested_journal_markup_rejected`, `:404` `empty_body_rejected`). The documented stdin-body flow therefore does not open an XML-injection or journal-corruption vector; the CLI wraps the body and stamps date/round itself.
- No contract instructs personas to write env vars, credentials, or other sensitive data into the journal/evidence; body conventions are review/handoff prose only.
- The test edit in `speccy-cli/tests/skill_packs.rs` repoints a brittle substring assertion at the stable `speccy journal append` / `--block review` markers — benign.
- The `.gitignore` and Rust source additions in the merge-base diff stat belong to prior committed tasks (T-001..T-006), not T-007's working tree, and are out of scope here.
</review>
<review date="2026-06-10T06:46:01Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
All changed files follow the project's established conventions. The rewrite of `verdict_return_contract.md` and `inline_note_format.md`, updates to the seven reviewer personas and three vet personas, edits to `speccy-work.md` and the three journal reference files, and the test-string repoint in `skill_packs.rs` are internally consistent and match the surrounding prose patterns.

The `vet-simplifier.md` omission of `--model` from its `journal append` invocations (and absence of `model=` on its thin `<verdict>` elements) mirrors the parser schema (`SimplifierScan` / `SimplifierApply` structs carry no `model` field), so the prose correctly reflects the schema design rather than drifting from it.

The `skill_packs.rs` test now asserts structural CLI markers (`speccy journal append` + `--block review`) rather than prose substrings, which is the correct direction per AGENTS.md's vacuous-test guidance.

No naming drift, dead code, unused imports, or reinvented helpers detected.
</review>
<review date="2026-06-10T06:46:27Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="pass" round="1">
All T-007-scoped documentation changes are internally consistent and accurately reflect the new self-append contract.

What was checked:
- `verdict_return_contract.md` — correctly rewritten to two-step contract: append via `speccy journal append`, return a thin self-closing verdict element. No old "orchestrator transcribes" language remains.
- `inline_note_format.md` — correctly repurposed to describe the stdin review body, not the old full review-element format.
- All seven reviewer persona Example sections — consistently show the `speccy journal append` command plus the thin verdict element.
- All three vet personas — single-writer / orchestrator-transcribes language removed; each now shows its own `journal append` invocation and thin verdict.
- `speccy-work.md` — step 9 rewritten to `speccy journal append`, `date`/`round`/`generated_at` authoring instructions deleted; "close the block" instruction removed.
- `journal-implementer.md`, `journal-review.md`, `journal-blockers.md` — attributes marked CLI-stamped; date/round authoring instructions replaced with "do not compute, supply, or mention" language.
- Ejected packs verified to match sources post-reeject.

Known mid-SPEC inconsistency (T-008 scope, not a T-007 blocker): `review-fanout.md` still tells the orchestrator to dispatch reviewers with "return a review block, do not edit the journal" and transcribe serially; `speccy-vet.md` still asserts the "single-writer rule." These contradict the updated persona bodies and were deliberately left for T-008 (REQ-009). Expected mid-SPEC state; the orchestrator compensates with a manual override. AGENTS.md still labels these "read-only agents," but `Bash` is in the grant so mechanical enforcement is compatible; label drift is T-008/T-009 cleanup scope.
</review>
<blockers date="2026-06-10T06:46:53Z" round="1">
Fix the factually-wrong persona-membership claim in the journal-review reference doc, then re-eject before the next pass.
- correctness (blocking): `resources/modules/references/journal-review.md` (the bullet around lines 92-97, plus its `.claude`/`.agents` ejections) was rewritten by T-007 to document the `speccy journal append --block review --persona NAME` invocation, but it still asserts "Custom personas are allowed provided the project's reviewer skill registers them; the parser validates persona is non-empty but does not enforce membership." Both clauses are false against the implementation it now documents: the persona set is a fixed compile-time list of seven names (business, tests, security, style, correctness, architecture, docs) in `speccy-core/src/personas.rs`, and `journal append --block review` rejects any unknown persona with `SerializeError::UnknownPersona` (`speccy-core/src/parse/journal_xml/serialize.rs`), exercised by `unknown_persona_and_verdict_rejected`. Membership IS enforced. Required fix: rewrite the bullet to state the persona must be one of the seven registered names and that the CLI rejects unknown personas; drop the "does not enforce membership" and "custom personas registered by the reviewer skill" claims. Leave the neighbouring journal-implementer.md "does not enforce suffix membership" line as-is (it refers to the --model slash-suffix and is correct). Edit the source under `resources/`, then run `just reeject` so the ejected packs match.
</blockers>
<review date="2026-06-10T06:47:30Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="blocking" round="1">
Factual error about the `journal append --block review` validation branch in the shipped reference `resources/modules/references/journal-review.md` lines 92-97 (and its ejections under `.claude/` / `.agents/`).

The bullet, which T-007 rewrote to document the new `speccy journal append ... --block review --persona NAME` invocation, asserts: "Custom personas are allowed provided the project's reviewer skill registers them; the parser validates persona is non-empty but does not enforce membership."

Both clauses are false against the implementation this section now documents:
- The persona set is a fixed compile-time list of exactly seven names (`speccy-core/src/personas.rs`: business, tests, security, style, correctness, architecture, docs). There is no runtime "reviewer skill registers them" mechanism.
- `journal append --block review` rejects any persona not in that registry. `validate_and_render_block` returns `SerializeError::UnknownPersona` ("invalid persona; expected one of: ...") in `speccy-core/src/parse/journal_xml/serialize.rs`, exercised by `unknown_persona_and_verdict_rejected`. Membership IS enforced.

Impact: an agent following this reference and passing a project-custom persona name will hit a hard `journal append` rejection rather than the documented success. The seven registered fan-out personas work fine, so blast radius is bounded to the custom-persona path — Important, not Critical. Fix: change the bullet to state the persona must be one of the seven registered names and that the CLI rejects unknown personas, dropping the "does not enforce membership" and "custom personas registered by the reviewer skill" claims. The neighbouring journal-implementer.md "does not enforce suffix membership" refers to the --model slash-suffix and is correct — leave it.
</review>
<implementer date="2026-06-10T06:50:29Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Retry round addressing a correctness blocker, not a
  reuse blocker — the full area-map is not re-run (round-agnostic
  per-symbol floor only applies, and this amend introduces no new
  top-level symbol). The fix edits one existing bullet in the
  shared reference `resources/modules/references/journal-review.md`;
  no new file, symbol, or include was added.
- Completed: Rewrote the `persona` attribute bullet in
  `journal-review.md` to address the round-1 correctness blocker.
  Removed the two false clauses — "Custom personas are allowed
  provided the project's reviewer skill registers them" and "the
  parser validates persona is non-empty but does not enforce
  membership" — and replaced them with the accurate contract: the
  persona must be one of the seven registered names (`business`,
  `tests`, `security`, `style`, `correctness`, `architecture`,
  `docs`), and `journal append --block review` rejects any
  unregistered name with a hard error. Verified against the
  implementation: `speccy-core/src/personas.rs::ALL` is the fixed
  seven-name compile-time list, and `validate_and_render_block`
  returns `SerializeError::UnknownPersona` for any name not in
  `PERSONAS_ALL` (`speccy-core/src/parse/journal_xml/serialize.rs:217`),
  exercised by `unknown_persona_and_verdict_rejected`. Left the
  neighbouring journal-implementer.md "does not enforce suffix
  membership" line untouched (it refers to the --model slash-suffix
  and is correct), per the blocker's explicit instruction. Ran
  `just reeject` (1 file overwritten: the journal-review.md
  ejection under .claude + .agents); a second reeject overwrote 0,
  confirming idempotency.
- Undone: T-008 (REQ-009) and T-009 (REQ-010) remain for their own
  implementer turns per TASKS.md. No other round-1 WIP was altered;
  this round is scoped strictly to the single correctness blocker.
- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0. `cargo deny check`
  exited 0 (no dependency changes — prose/template edit only).
- Evidence: paper trail at
  `.speccy/specs/0055-lifecycle-write-commands/evidence/T-007.md`.
  Roll call for CHKs under REQ-008:
  - CHK-011 (clean tree after `just reeject`): demonstrated → the
    fix edits a `resources/` source bullet and `just reeject`
    regenerates the matching ejections; the immediate second
    reeject overwrote 0 files, proving sources and ejections are
    in sync. The non-CHK done-when bullets are unchanged from
    round 1 (no persona/phase invocation surface was touched this
    round); the round-1 evidence Scenarios 2–4 still hold.
- Discovered issues: None new this round. The brittle
  prose-substring assertions in `speccy-cli/tests/skill_packs.rs`
  flagged in round 1 remain a future-pass cleanup outside T-007
  scope.
- Procedural compliance: This round-2 implementer entry lands via
  `speccy journal append --block implementer` (dogfooding T-002);
  the CLI stamped date and derived round=2, no hand-edited block.
  The TASKS.md state was driven pending→in-progress→in-review via
  `speccy task transition` (dogfooding T-001). No implementer-note
  written into TASKS.md. The round-1 WIP was amended in place — no
  `git restore`/`clean`/`checkout` against the dirty tree, no
  rewrite-from-scratch. The fix is a `resources/` source edit with
  `just reeject` regenerating the ejections, never editing the
  harness folders directly. No shipped skill body needed a
  friction fix during this round.
</implementer>
<review date="2026-06-10T06:53:33Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-2 fix fully resolves the round-1 correctness blocker and REQ-008's business contract is satisfied.

The round-1 blocker was a factually-wrong claim in the shipped reference `resources/modules/references/journal-review.md` (and its ejections): it told downstream agents "custom personas are allowed provided the project's reviewer skill registers them; the parser ... does not enforce membership." Both clauses were false. The round-2 amend replaces them with the accurate contract — persona must be one of the seven registered names (business, tests, security, style, correctness, architecture, docs) and `journal append --block review` rejects any unregistered name with a hard error. Verified against `speccy-core/src/personas.rs` (`ALL` is exactly those seven) and the cited `SerializeError::UnknownPersona` rejection path. The fix also correctly left the neighbouring `journal-implementer.md` `--model` slash-suffix line untouched, per the blocker's explicit instruction.

This serves user-story 2 (reviewer persona self-appends and returns a thin verdict): the corrected doc no longer mis-teaches an agent into a `journal append` rejection on a project-custom persona name. REQ-008 done-when bullets 1 and 2 are met in the rewritten section — the `journal append` invocation is documented and `date`/`round` are marked CLI-stamped with explicit "do not supply or compute" prose.

Scope is clean: the round-2 amend touched only `journal-review.md` source plus its `.claude`/`.agents` ejections (the blocker's named file). No T-008 files, no `ARCHITECTURE.md` (T-009), no non-goal violated. The deferred mid-SPEC inconsistencies (`review-fanout.md` serial-transcription, `speccy-vet.md` single-writer rule) remain correctly scoped to REQ-009/T-008. The ejected pack at `.claude/skills/speccy-review/references/journal-review.md` matches the corrected source. CHK-011 is the orchestrator/commit-time gate; source and ejection carry matching text and no persona/phase invocation surface changed this round, so round-1 evidence holds.
</review>
<review date="2026-06-10T06:53:49Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 fix resolves the round-1 correctness blocker cleanly and the one scenario CHK is sound. CHK-011 (clean tree after `just reeject`) independently reproduced: my own `just reeject` reported `0 overwritten, 36 unchanged`, proving committed ejections are byte-identical to a fresh render of the edited `resources/` sources through the real codegen path. The corrected `journal-review.md` persona-membership bullet is factually accurate against the implementation — `speccy-core/src/personas.rs::ALL` is the cited seven-name list and `validate_and_render_block` (`serialize.rs:217-222`) rejects unregistered personas via `SerializeError::UnknownPersona`, exercised by `unknown_persona_and_verdict_rejected` (`serialize.rs:373`). The one Rust test change (`reviewer_correctness_renders_with_includes_expanded_both_hosts`, `speccy-cli/tests/skill_packs.rs:1219`) is non-vacuous: the old asserted string is gone from all packs (the prior assertion would now fail) and the repoint gates the structural `speccy journal append`/`--block review` contract marker rather than editorial prose; the full 45-test render suite passes. Evidence field present, file exists, CHK-011 roll-call labelled `demonstrated` with a concrete scenario reference; no fabrication pattern matched.
</review>
<review date="2026-06-10T06:53:59Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns in the round-2 fix. T-007 remains a prose/template contract rewrite; the round-2 amend narrows to a single bullet in the shared reference `resources/modules/references/journal-review.md` (plus its `.claude`/`.agents` ejections) correcting a factual claim about `journal append --block review` persona validation.

Adversarial checks performed on the round-2 slice:
- The corrected bullet now states the persona must be one of the seven registered names and that the CLI rejects unregistered names with a hard error. I verified this against the implementation it documents: `speccy-core/src/personas.rs::ALL` is the fixed seven-name compile-time list (business, tests, security, style, correctness, architecture, docs), and `validate_and_render_block` rejects any persona not in `PERSONAS_ALL` with `SerializeError::UnknownPersona` at `speccy-core/src/parse/journal_xml/serialize.rs:217-220`, exercised by `unknown_persona_and_verdict_rejected`. The fix is factually accurate — membership IS enforced.
- The security-relevant property the surrounding contract prose leans on — "validation runs before any write; a malformed body leaves the journal byte-identical" — is backed by `validate_and_render_block` returning `Err` without emitting output (`serialize.rs:17` docstring), so the documented stdin-body flow opens no XML-injection or journal-corruption vector. The CLI wraps the body and stamps `date`/`round` itself.
- No contract instructs personas to write env vars, credentials, or sensitive data into the journal/evidence; body conventions remain review/handoff prose only. The `--model` slash-suffix note correctly remains scoped to identity, untouched per the blocker's explicit instruction.
- Scope is clean: the round-2 amend altered no other round-1 WIP, no T-008/T-009 files, and introduced no dependency change (`cargo deny check` clean per implementer attestation).
</review>
<review date="2026-06-10T06:54:07Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-2 change is a single-bullet rewrite in `resources/modules/references/journal-review.md` (and its two ejected copies under `.claude/` and `.agents/`). The prose matches the surrounding section's style, accurately describes the implementation (`speccy-core/src/personas.rs::ALL` five default + two off-default, CLI enforcement), and removes the false claims without introducing new conventions or abstractions.

Source and both ejections are byte-identical post-edit (diff against HEAD shows all three carry the same change). No naming drift, dead code, unused imports, reinvented helpers, or suppression annotations introduced. The round-1 test-string repoint in `speccy-cli/tests/skill_packs.rs` remains non-vacuous and style-compliant.
</review>
<review date="2026-06-10T06:54:17Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
Round-1 blocker resolved. `resources/modules/references/journal-review.md` (the persona bullet) now correctly states the persona must be one of the seven registered names and that `journal append --block review` rejects unknown personas with a hard error — matching `speccy-core/src/personas.rs` (`ALL`, seven names) and `serialize.rs:217` (`SerializeError::UnknownPersona`, tested by `unknown_persona_and_verdict_rejected`). Both false clauses are removed from source and from both ejected packs (`.claude`/`.agents`); the only surviving copies are the historical quote inside the round-1 blockers-block text in the journal itself. The `journal-implementer.md` "does not enforce suffix membership" line was correctly left untouched. `just reeject` reports 0 overwritten / 36 unchanged, so CHK-011's clean-tree invariant holds. No new correctness defects in the amend — this is a prose/template edit whose only correctness surface is factual fidelity to the documented implementation, which now holds.
</review>
<review date="2026-06-10T06:54:26Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="pass" round="2">
Round-2 fix correctly resolves the round-1 correctness blocker. The rewritten `persona` bullet in `resources/modules/references/journal-review.md` now accurately states: the persona must be one of the seven registered names, names all seven explicitly with correct grouping (five default fan-out including `correctness`, two off-default `architecture` and `docs`), and states that the CLI enforces this closed set with a hard error on unknown names — matching `speccy-core/src/personas.rs::ALL` and the `SerializeError::UnknownPersona` path in `serialize.rs`.

Both ejected copies (`.claude/skills/speccy-review/references/journal-review.md` and `.agents/skills/speccy-review/references/journal-review.md`) are byte-identical to the source. The false "custom personas allowed" and "does not enforce membership" clauses are gone. The neighbouring `journal-implementer.md` "does not enforce suffix membership" line (for the `--model` slash-suffix) was correctly left untouched per the blocker's explicit instruction.

No canonical rule text leaked into non-canonical files; no old function names or stale cross-references detected in the changed surface.
</review>
