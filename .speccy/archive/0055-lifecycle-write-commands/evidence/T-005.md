# Evidence for SPEC-0055 T-005

Red-then-green paper trail for `speccy journal show` (REQ-006). The
proof lives in the integration suite at
`speccy-cli/tests/journal_show.rs`, which drives the built `speccy`
binary against scratch workspaces. "red" is the pre-implementation
state — no `journal show` subcommand existed, so the binary rejected
the verb at argument-parse time and the fixtures had no command path
to call; "green" is the committed test passing under
`cargo test --workspace`.

## Coverage rule

REQ-006's only scenario CHK is CHK-009. It is `demonstrated` below.
The done-when bullets that are not scenario CHKs (the
`--block review --round N` completeness call site, the VET.md
spec-selector path, the `schema_version: 1` first-field / missing-file
non-zero exit) are also each demonstrated by a named integration test
and recorded here for completeness.

## Session 2026-06-09-T005-attempt-1

### Scenario 1 — `--round latest --verdict blocking` returns the single blocking block (CHK-009)

Drives CHK-009: a fixture journal with two rounds and five reviews in
round 2 (one `blocking`), queried with
`journal show SPEC-0042/T-001 --json --round latest --verdict
blocking`.

<red>
Before this task, `speccy journal` exposed only the `append`
subcommand, so `journal show` was rejected at argument-parse time and
no filtered JSON could be produced.
</red>
<green>
`cargo test -p speccy-cli --test journal_show`
test round_latest_verdict_blocking_returns_single_block ... ok
Asserts stdout parses as JSON whose `blocks` array has length 1, the
surviving block's `persona` is `security` and `verdict` is `blocking`
(matching the fixture), `round` is 2, the envelope surfaces
`latest_round: 2`, and `schema_version` equals 1.
</green>

### Scenario 2 — `--block review --round N` lists the round's personas (done-when: completeness call site)

Drives the done-when "a `--block review --round N` invocation lists
the personas that reviewed round N."

<red>
No `journal show` command existed to filter blocks by type and round.
</red>
<green>
`cargo test -p speccy-cli --test journal_show`
test block_review_round_n_lists_round_personas ... ok
Asserts the `blocks` array carries exactly the five round-2 reviewer
personas (`business`, `docs`, `security`, `style`, `tests`), excludes
the round-1 review, and contains only `review`-type blocks (no
`implementer` leakage through the conjunctive filter).
</green>

### Scenario 3 — spec selector resolves VET.md and surfaces its invocation sections (REQ-006 behavior / scenario 3)

Drives the REQ-006 behavior "given a spec selector, VET.md is parsed
and its invocation sections and blocks appear in the JSON."

<red>
No spec-selector routing to VET.md existed in any show path.
</red>
<green>
`cargo test -p speccy-cli --test journal_show`
test spec_selector_shows_vet_invocations_and_blocks ... ok
Asserts a bare `SPEC-0042` selector emits an envelope with
`schema_version: 1`, one invocation section (number 1) holding both
the `drift-review` and `gate` blocks in order, the gate's
`tasks_hash` surfaced, and no `round` field on the round-less gate.
</green>

### Scenario 4 — missing journal exits non-zero (done-when: missing journal is an anomaly)

Drives the done-when "a missing journal exits non-zero with a
diagnostic."

<red>
No command path existed to detect the absent file.
</red>
<green>
`cargo test -p speccy-cli --test journal_show`
test missing_journal_exits_nonzero ... ok
Asserts `journal show SPEC-0042/T-001 --json` against a workspace with
no journal file exits non-zero with a stderr diagnostic containing
`journal not found`.
</green>

### Scenario 5 — `--json` toggles representation, not content (REQ-006: --json never changes content)

Pins the workspace-wide convention that `--json` toggles
representation only: the text form lists the same filtered blocks the
JSON form does.

<red>
No text renderer existed for `journal show`.
</red>
<green>
`cargo test -p speccy-cli --test journal_show`
test text_mode_renders_same_filtered_blocks ... ok
Runs `journal show ... --block review --round 2` without `--json` and
asserts the text output carries five round-2 review lines and no
`implementer` line — the same surviving set the `--json` Scenario 2
asserts.
</green>

## Hygiene gate (catch-all for hygiene CHKs)

All four standard gates pass on the working tree:

- `cargo test --workspace` — exit 0 (0 failed)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0
- `cargo +nightly fmt --all --check` — exit 0
- `cargo deny check` — exit 0 (advisories / bans / licenses / sources ok)
