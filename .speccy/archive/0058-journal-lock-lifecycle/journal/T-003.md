---
spec: SPEC-0058
task: T-003
generated_at: 2026-06-11T05:47:16Z
---

<implementer date="2026-06-11T05:47:16Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: round-1 area map of the transition command + reap helper + CLI test harness. Reuse-as-is: `crate::journal::reap_lock_sidecar(&Utf8Path)` (the guarded, idempotent unlink built in T-002 — exactly the call the task names); `TaskState::Completed` (already imported at transition.rs:15); `location.spec_dir` / `location.task.id` (in scope in the Legal arm). Tests reuse-as-is: `common::Workspace`, `write_spec`, `spec_md_template`, `tasks_md_xml`, `task_xml`, and the `fs4::FileExt` held-lock pattern from `journal_append.rs:391-426`. Write-fresh: the guarded reap call in the `Legal` arm (searched transition.rs — no prior reap wiring); the new `transition_reap.rs` test binary (searched tests/ — no existing task-lock-reap coverage). New top-level symbols introduced: none in src (one inline `if` block); test helpers `workspace_with_task`/`journal_md`/`lock_sidecar`/`seed_journal`/`seed_free_sidecar`/`transition_to` are local test scaffolding modelled on the sibling `journal_append.rs` helpers.
- Completed: Wired `reap_lock_sidecar` into the `TransitionKind::Legal` arm of `speccy-cli/src/transition.rs`, gated on `to == TaskState::Completed`, after the `fs_err::write` of the spliced TASKS.md and before `Ok(())`. Sidecar path is `location.spec_dir.join("journal").join(format!("{}.md.lock", location.task.id))`. Removed the now-unfulfilled `#[cfg_attr(not(test), expect(dead_code, ...))]` on `reap_lock_sidecar` in `journal.rs` — this task is the first non-test call site, so the helper is no longer dead in release builds (clippy `unfulfilled_lint_expectations` flagged it). Added `speccy-cli/tests/transition_reap.rs` with four scenarios.
- Undone: The vet-lock reap on `journal append --block gate` (REQ-002) is T-004, not this task; left untouched.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok).
- Evidence: scenarios proven by the new `transition_reap.rs` integration binary (4 passed). Roll call for CHKs under REQ-001 and REQ-003:
  - CHK-001 (completed task's free sidecar reaped, journal byte-identical): demonstrated -> `completed_transition_reaps_free_sidecar_and_leaves_journal_intact` seeds journal+free sidecar, runs `--to completed`, asserts sidecar gone and journal bytes equal pre-transition.
  - CHK-002 (completed transition with no sidecar exits zero, journal unchanged): demonstrated -> `completed_transition_with_no_sidecar_exits_zero_and_leaves_journal_intact` asserts absent sidecar stays absent (never created) and journal bytes unchanged on a successful exit.
  - CHK-005 (held sidecar survives reap, transition exits zero): demonstrated -> `completed_transition_skips_held_sidecar_and_still_exits_zero` takes an exclusive `FileExt::lock` from the test process, runs `--to completed` to success, asserts the sidecar still exists (try_lock guard skipped it).
  - REQ-001 done-when negative (non-completed edge leaves sidecar in place): demonstrated -> `non_completed_transition_leaves_sidecar_in_place` runs `pending`->`in-progress` and asserts the sidecar persists.
- Discovered issues: none. The reap helper from T-002 dropped cleanly into the new call site.
- Procedural compliance: followed speccy-work first-attempt recipe (clean tree, journal absent). No `resources/` skill/module edits needed — no stale-instruction friction encountered. Source edits confined to `speccy-cli/`; no ejected harness files touched.
</implementer>
<review date="2026-06-11T05:48:40Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff faithfully delivers T-003's two covering requirements (REQ-001, REQ-003) with no scope creep. REQ-001 done-when fully mapped: the `--to completed` gate (`speccy-cli/src/transition.rs:135-141`) reaps only on the completed edge, never opens the journal `.md`, and runs after the load-bearing TASKS.md write — matching the goal and DEC-002. All three done-when bullets are observably covered: free-sidecar reap + byte-identical journal (`transition_reap.rs:85` CHK-001), absent-sidecar no-op + journal unchanged (`transition_reap.rs:109` CHK-002), and the non-completed edge leaving the sidecar in place (`transition_reap.rs:169`). REQ-003's try_lock guard is exercised at integration level by CHK-005 (`transition_reap.rs:138`), which holds an exclusive lock from the test process and asserts the sidecar survives with exit zero. The REQ-001 behavior prose names an `in-review`->`pending` blocking-round edge; the test substitutes `pending`->`in-progress`, but the production gate keys only on `to == Completed` (edge-agnostic on `from`), so the chosen edge exercises the same code path — acceptable, not a gap. Vet-lock reap (REQ-002) correctly left to T-004. Non-goals respected: no new subcommand, `Drop` and the lock path unmoved. OQ-a was resolved by amendment, not silently by the implementer. Changelog intent (delete-at-terminal-boundary, tracing diagnostic) matches the as-built reap.
</review>
<review date="2026-06-11T05:48:47Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blocker. The only mutual-exclusion-sensitive path — unlinking the advisory-lock sidecar — is correctly gated behind the non-blocking `try_lock` guard (REQ-003) and runs only on the `--to completed` terminal boundary (`transition.rs:135-141`); a held sidecar is skipped, never unlinked, preserving SPEC-0055 REQ-005 mutual exclusion (CHK-005 holds a real exclusive `FileExt::lock` and asserts the sidecar survives). `Drop for LockGuard` stays unlock-only, so the DEC-001 unlink-while-locked race is not reintroduced. The reap path appends `.md.lock` and never opens the journal `.md` (CHK-001/CHK-002 assert byte-identity), so there is no risk of deleting the artifact. `lock_path` is built from `location.spec_dir` (resolved by `scan`/`find_task`, not raw CLI input) plus literals and `location.task.id` from parsed TASKS.md — no path-traversal vector. The only failure-path output is `tracing::warn!` to stderr naming a filesystem path and an I/O error (no secret leak, stdout/JSON contract untouched). No new dependency, no auth/authz, crypto, or injection surface introduced by this slice.
</review>
<review date="2026-06-11T05:48:59Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The reap wiring in `speccy-cli/src/transition.rs:135-141` is logically sound: it fires only in the `TransitionKind::Legal` arm, gated on `to == TaskState::Completed`, after the load-bearing `fs_err::write` of TASKS.md succeeds and before `Ok(())`, so the cleanup never runs ahead of the mutation it follows and no non-completed edge reaches it. The sidecar path `spec_dir/journal/{task.id}.md.lock` matches the spec exactly. The helper's `try_lock` match (`journal.rs:747-757`) is exhaustive over `Ok`/`WouldBlock`/`Error` and mirrors the proven `acquire` path; the handle is dropped before `remove_file`, so no Windows sharing-violation no-op and no leaked handle on any exit path. NotFound/WouldBlock return silently as expected, errors warn-and-return — no swallowed error, no `?` that skips cleanup, no inverted condition. The four integration scenarios in `transition_reap.rs` map correctly to CHK-001/CHK-002/CHK-005 and the REQ-001 negative edge; CHK-005 holds the lock in the test process while the reap runs in a separate spawned `speccy` process, so the cross-process advisory contention is genuine and the guard is actually exercised. No off-by-one, boundary, or control-flow defect found at or above threshold.
</review>
<review date="2026-06-11T05:49:54Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. Diff is clean against project conventions.

Checked items:
- `transition.rs` wiring: the `if to == TaskState::Completed` guard and `crate::journal::reap_lock_sidecar(&lock_path)` call are correctly placed in the `Legal` arm after the `fs_err::write`. Code is minimal and follows surrounding style.
- `#[cfg_attr(not(test), expect(dead_code, ...))]` removal from `reap_lock_sidecar`: the annotation was forward-looking from T-002 and correctly dropped now that `transition.rs` provides the first non-test call site. No stale suppressions remain.
- `transition_reap.rs` header: `#![allow(clippy::expect_used, reason = "...")]` + `#![expect(clippy::panic_in_result_fn, reason = "...")]` matches the sibling `transition.rs` and every other integration-test binary byte-for-byte.
- Imports in `transition_reap.rs`: `use common::TestResult`, `Workspace`, `spec_md_template`, `task_xml`, `tasks_md_xml`, `write_spec` — exact mirror of `transition.rs`.
- Test helpers (`workspace_with_task`, `journal_md`, `lock_sidecar`, `seed_journal`, `seed_free_sidecar`, `transition_to`): local scaffolding modelled on `journal_append.rs`; no reinvention of project-wide helpers.
- Naming: `spec_id = "SPEC-0042"`, slug `"0042-example-slug"` — uses the fictional placeholders mandated by AGENTS.md for shipped templates; appropriate here as test-local scaffolding.
- No `unwrap()` / `expect()` in production code; `.expect("descriptive message")` used correctly in test code.
- No `[i]` indexing; no new lint suppressions in `src/`.
- Cargo.toml diff: `tracing` and `tracing-subscriber` added with `workspace = true`, consistent with workspace-managed dependency pattern.
- Reuse survey present and honest: named symbols (`reap_lock_sidecar`, `TaskState::Completed`, `location.spec_dir`, `location.task.id`) all confirmed to exist at the named paths and be the symbols the diff actually calls.
</review>
<review date="2026-06-11T05:50:23Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests genuinely exercise each covered CHK against the real system, not a mock: every test in `speccy-cli/tests/transition_reap.rs` drives the built `speccy` binary via `assert_cmd` against a scratch `common::Workspace`, executing the real `Legal`-arm TASKS.md state rewrite and the real `crate::journal::reap_lock_sidecar` wired at `transition.rs:135-140`. `(InReview, Completed)` and `(Pending, InProgress)` are both Legal edges (speccy-core task_xml.rs:132-139), so each `.success()` assertion is load-bearing — an illegal or failed transition exits non-zero.

CHK-001 (`completed_transition_reaps_free_sidecar_and_leaves_journal_intact`): seeds journal+free sidecar, captures journal bytes, runs `--to completed`, asserts sidecar gone and journal `.md` byte-identical. Catches a no-op reap (sidecar would persist) and any reap that opens/truncates the journal `.md` (byte compare fails) — the REQ-001 "journal never touched" invariant.

CHK-002 (`completed_transition_with_no_sidecar_exits_zero_and_leaves_journal_intact`): precondition asserts no sidecar, then asserts it is never created by the reap and the journal is unchanged on a successful exit. Catches a stray `create(true)` and a reap that fails the command.

CHK-005 (`completed_transition_skips_held_sidecar_and_still_exits_zero`): takes a real cross-process exclusive `fs4::FileExt::lock` from the test process (mirroring the proven held-lock pattern at `journal_append.rs:391-426`), runs `--to completed`, asserts exit zero and the sidecar survives. Catches an unguarded unconditional unlink that ignores the `try_lock` guard (REQ-003). Deterministic advisory lock — no sleep/time/ordering dependence, safe under parallel runs.

REQ-001 done-when negative (`non_completed_transition_leaves_sidecar_in_place`): `pending`->`in-progress` leaves an existing sidecar in place — catches removal of the `if to == TaskState::Completed` gate (would unlink the free sidecar on a non-terminal edge). Not vacuous.

Evidence roll call is complete and legible: every CHK under the covered REQs (REQ-001: CHK-001, CHK-002; REQ-003: CHK-005) plus the REQ-001 negative leg is labeled `demonstrated` and cites a specific in-diff test by name. The `Evidence:` field names no separate `evidence/T-003.md` path, but explicitly designates the in-diff `transition_reap.rs` integration binary as the proof — the same convention the tests persona accepted on sibling T-002 (`completed`), and consistent with the canonical evidence rule where the demonstrated-CHK backing is satisfied by falsifiable integration tests present in the diff. Not blocking on the absent separate file: the red-then-green proof is genuinely present and each assertion catches a wrong implementation. The lone journal.rs working-tree change is the removal of the now-unfulfilled `#[cfg_attr(not(test), expect(dead_code))]` on `reap_lock_sidecar`, correct since this task is its first non-test call site.
</review>
