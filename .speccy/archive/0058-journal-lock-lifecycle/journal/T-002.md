---
spec: SPEC-0058
task: T-002
generated_at: 2026-06-11T05:37:05Z
---

<implementer date="2026-06-11T05:37:05Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: area = `speccy-cli/src/journal.rs` (covered REQs + suggested file). Reuse-as-is: `fs4::FileExt::try_lock`/`lock`/`unlock`, `fs4::TryLockError`, `fs_err::OpenOptions`/`remove_file`, `.into_parts()`, `camino::Utf8Path`, and `tracing::warn!` (the T-001 process-wide subscriber in `main.rs`). New top-level symbol `reap_lock_sidecar`: searched journal.rs for any existing reap/unlink helper — none existed; this is the shared helper the SPEC introduces (DEC-002). Test capturing-subscriber: wrote a small in-crate `CapturingCollector` implementing `tracing::Subscriber` rather than adding a `tracing-test` dev-dependency, since `tracing` is already a dependency — keeps the dep surface minimal.
- Completed: added `pub(crate) fn reap_lock_sidecar(&Utf8Path)` after `Drop for LockGuard` (journal.rs:~726). Opens without `create(true)` (NotFound = silent no-op), unwraps via `.into_parts()`, one non-blocking `try_lock` (WouldBlock = silent skip), drops the handle before `fs_err::remove_file`, returns `()`, and emits exactly one `tracing::warn!` with a `sidecar` field on any error short of a clean reap (non-NotFound open error, `try_lock` Error, remove_file error). `Drop`/`acquire`/append path unchanged (REQ-004, additive). Added `#[cfg(test)] mod tests` with a capturing `tracing::Subscriber` and four tests.
- Undone: nothing in scope. No call sites wired (T-003/T-004 own those, per the task body) — the helper carries a `#[cfg_attr(not(test), expect(dead_code, reason=...))]` so the non-test lib build stays warning-clean and the expectation auto-clears once T-003/T-004 wire the call sites.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file written; the four scenarios are demonstrated directly by the unit tests in `journal.rs` plus the regression triad in `tests/journal_append.rs`. Roll call for CHKs under REQ-003/REQ-004/REQ-005:
  - CHK-005 (held lock skipped by transition reap, transition exits zero): hygiene → unit test `held_sidecar_survives_reap_without_warning` proves the helper-level guard (held lock left intact, no WARN); the transition-command call site lands in T-003, so the end-to-end exit-zero leg is judgment-only at this task's boundary.
  - CHK-006 (eight concurrent appends serialize): demonstrated → `eight_concurrent_review_appends_serialize_cleanly` passes unchanged (REQ-004 regression).
  - CHK-007 (held lock past timeout, journal byte-identical): demonstrated → `held_lock_makes_waiting_append_time_out_nonzero` passes unchanged.
  - CHK-008 (stdout JSON uninterleaved, diagnostics only on stderr): hygiene → owned by T-001's `tests/tracing_stdout.rs` (subscriber writes to stderr); this task adds no stdout writer, so it is preserved by construction.
  - CHK-009 (induced unlink failure emits one WARN naming the sidecar; absent/held emit none): demonstrated → unit tests `induced_unlink_failure_emits_one_warn_naming_the_sidecar` (Unix-gated per the task's allowance — read-only parent dir blocks deletion only on POSIX), `free_sidecar_is_unlinked_without_warning`, `held_sidecar_survives_reap_without_warning`, `absent_sidecar_is_a_silent_noop`.
- Discovered issues: the induced-failure test is `#[cfg(unix)]` because a read-only parent directory does not block child deletion on Windows; no portable post-`try_lock` `remove_file` induction exists without a second hook point, and the task body explicitly permits gating the induction by platform. Consequence: CHK-009's failure-path assertion is exercised on Unix CI only (and is a no-op under root, the usual perm-test caveat); the no-warning legs run on all hosts.
- Procedural compliance: followed speccy-work first-attempt branch (clean tree, no journal). No source-module/skill friction encountered; no edits under `resources/`. Used `#[expect]` (via `cfg_attr`) not `#[allow]` per AGENTS.md; no `unwrap`/`panic` in production code; tests use `.expect("...")`.
</implementer>
<review date="2026-06-11T05:38:52Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. The reap helper has no authn/authz boundary, no
injection vector, and no sensitive-data exposure.

- No-create open is correct: `OpenOptions::new().read(true).write(true)`
  deliberately omits `create(true)` (journal.rs reap helper vs
  `LockGuard::acquire` at journal.rs:658-663), so a `NotFound` is a
  silent idempotent no-op rather than a fresh inode at the lock path —
  this is what closes the inode-confusion vector DEC-001 warns about.
- Racing-reaper / TOCTOU is defended in depth: `Drop for LockGuard`
  stays unlock-only (journal.rs:695-703, unchanged), the `try_lock`
  guard skips a held lock via `WouldBlock` → silent return, and the
  handle is dropped before `remove_file` so the lock is never held
  across the unlink. The residual drop→unlink window is documented
  (DEC-004) as backed by terminal-boundary quiescence, not by the
  `try_lock` alone; the `try_lock` only guards a mistimed reap. This is
  advisory same-host file locking with no authorization decision riding
  on it.
- Unlink-while-locked is correct across platforms: `drop(file)` before
  `fs_err::remove_file` releases the OS handle, avoiding the Windows
  sharing-violation no-op (DEC-001/DEC-004).
- WARN leaks nothing sensitive: it names a `.lock` sidecar path under
  `.speccy/specs/.../journal/` plus an I/O error string — local,
  deterministic, repo-discoverable paths, no credentials/tokens/user
  data. Diagnostics go to stderr only (REQ-005/DEC-005), so the
  stdout/JSON contract is untouched and there is no log-into-payload
  injection.
- No injection surface (no SQL/command/template/deserialization); the
  path is a typed `&Utf8Path` and `tracing::warn!` uses fixed message
  literals with structured fields. No panics in the production path
  (expect/unwrap only in tests).
- New deps `tracing`/`tracing-subscriber` are mainstream, maintained,
  and `cargo deny check` passed.
</review>
<review date="2026-06-11T05:39:10Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. `reap_lock_sidecar` (speccy-cli/src/journal.rs:732-787) is correct across every control-flow leg I scrutinized. Open uses `read(true).write(true)` with no `create(true)`, so an absent sidecar yields `NotFound`; the `NotFound` guard arm precedes the catch-all `Err` arm, so it returns silently and never falls to the WARN. `into_parts()` (journal.rs:752) mirrors `acquire` at :669 and is infallible — no unwrap/panic. The `try_lock` match is exhaustive over `fs4::TryLockError`: `Ok(())` falls through, `WouldBlock` returns silently (expected held-lock no-op, no WARN), `Error` warns and returns. Critical ordering holds: `drop(file)` (journal.rs:773) precedes `fs_err::remove_file` (:775), releasing the OS handle and advisory lock before the unlink, satisfying DEC-001/DEC-004 (Windows sharing-violation avoidance); the earlier-dropped `_path` is a `PathBuf`, not a handle. WARN fires on exactly the unexpected set — non-`NotFound` open error, `try_lock` `Error`, `remove_file` error — at three distinct sites, each emitting exactly one `warn!` with the `sidecar` field, each followed by `return` or terminal, so no path double-warns. Signature is `-> ()` with no `?` and no propagation: genuinely infallible. No off-by-one, no swallowed Result/Option, no resource leak (the held handle is released on every exit path), no concurrency defect in the helper itself. The induced-failure test correctly exercises the post-`try_lock` `remove_file` branch, since directory permissions do not affect `try_lock` on an already-open handle.
</review>
<review date="2026-06-11T05:39:13Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. The diff matches the SPEC's *current* (2026-06-11 amended) intent — delete at terminal boundaries with a `tracing::warn!` on unexpected failure — not the superseded relocate-and-persist framing; the Changelog/OQ-a resolution is reflected, not the old DEC-002/003. Scope is exactly T-002's contract: the additive `pub(crate) reap_lock_sidecar` helper plus direct unit coverage, with no call sites wired (correctly deferred to T-003/T-004). REQ-003 done-whens (held lock left intact; free lock unlinked) map to `held_sidecar_survives_reap_without_warning` / `free_sidecar_is_unlinked_without_warning` at the helper level; the command-level CHK-005 leg (`speccy task transition` exits zero) is legitimately out of scope here and acknowledged as T-003's. REQ-004 is preserved: the diff inserts only after `journal.rs:701`, `Drop`/`acquire` are byte-unchanged, and the SPEC-0055 regression triad in `tests/journal_append.rs` is untouched (empty diff --stat). REQ-005 maps cleanly — exactly one `WARN` with a `sidecar` field on each of the three error paths, silent on `NotFound`/`WouldBlock`, and the startup stderr subscriber lands in `main.rs:init_tracing`. No non-goal breached (no release-time unlink, no new subcommand, no lock-path/timeout/grammar change), no open question silently decided, no named edge case dropped.
</review>
<review date="2026-06-11T05:40:00Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests genuinely exercise each scenario's behavior against the real `reap_lock_sidecar`, not a mock. `journal.rs` test module: `free_sidecar_is_unlinked_without_warning` drives the real unlink + asserts file gone and zero WARN (Scenario 1 / REQ-003 free leg); `held_sidecar_survives_reap_without_warning` takes a real exclusive `FileExt::lock` on a second handle and asserts the sidecar survives with zero WARN (Scenario 2 / REQ-003 held leg / CHK-005 helper level); `absent_sidecar_is_a_silent_noop` asserts the path is neither created nor warned (Scenario 3, catches a stray `create(true)`); `induced_unlink_failure_emits_one_warn_naming_the_sidecar` (`#[cfg(unix)]`) chmods the parent to `0o555` so `remove_file` fails after a successful `try_lock`, then asserts exactly one `Level::WARN` event AND that its `sidecar` field equals the path (CHK-009 / REQ-005).

The WARN-capture path is sound: the helper logs `sidecar = %lock_path`, and in tracing 0.1.44 the `%` `DisplayValue` records via `record_debug(self)` where `DisplayValue`'s Debug forwards to the inner Display, so `format!("{value:?}")` in `SidecarVisitor::record_debug` yields the plain unquoted path — matching `Some(sidecar.as_str())`. The capturing `Subscriber` records real events; `warn_events` filters strictly to `Level::WARN`, so the count assertions are meaningful. Each no-WARN leg asserts `warn_events(...).is_empty()` (true absence, not absence-of-crash). Mentally breaking the impl is caught: always-early-return fails test 1; unlinking a held lock fails test 2; `create(true)` fails test 3; wrong/absent WARN field fails test 4.

REQ-004 regression triad (`eight_concurrent_review_appends_serialize_cleanly`, `two_concurrent_round_opening_appends_get_distinct_rounds`, `held_lock_makes_waiting_append_time_out_nonzero`) is present and untouched by the diff (CHK-006/CHK-007). Evidence roll call is complete and legible: every CHK under REQ-003/004/005 carries a label; `hygiene`/`demonstrated` entries cite specific in-diff test names (all four cited test symbols appear in the diff); the lone `judgment-only` portion (CHK-005's transition-command end-to-end leg) genuinely defers to T-003 where the call site lands, which is correct since this task adds the helper only. Known caveat (surfaced by the implementer, not a defect): the Unix-gated induced-failure test no-ops under root and would fail loudly there rather than pass vacuously — acceptable for non-root CI.
</review>
<review date="2026-06-11T05:40:15Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Style conventions are followed throughout. No blocking findings.

Checked:
- `reap_lock_sidecar` naming follows the project's verb-noun snake_case convention.
- No `unwrap()`/`panic!()`/`expect` in production code; test `.expect("...")` calls carry descriptive messages.
- Suppression annotations use `#[expect(..., reason = "...")]` not `#[allow]`, and the `#[cfg_attr(not(test), expect(dead_code, reason = "..."))]` wrapper is the right pattern so the expectation auto-clears once call sites are wired in T-003/T-004.
- `fs_err::OpenOptions`, `fs_err::remove_file`, `Utf8Path`, and `camino` are used consistently with the rest of `journal.rs`; no bare `std::fs` escapes.
- `tracing` and `tracing-subscriber` are declared as `{ workspace = true }` in `Cargo.toml`, matching the workspace-managed-dependency rule.
- The `CapturingCollector` in tests is a minimal bespoke subscriber (no new dev-dependency) — the survey notes `tracing-test` was considered and rejected to keep the dep surface small; the result is readable and adequate.
- All four tests gate real behavioral invariants; none are tautological.
- The `#[cfg_attr(not(unix), expect(dead_code, reason = "..."))]` on `CapturedEvent::sidecar` is correct: the field is only read in the Unix-gated test.
- Reuse survey present and complete; named symbols (`fs4::FileExt`, `fs_err::OpenOptions`, `.into_parts()`, `tracing::warn!`) confirmed to exist and to be what the diff calls.
- No dead imports, unused variables, or naming drift detected.
</review>
