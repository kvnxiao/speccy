---
spec: SPEC-0058
task: T-004
generated_at: 2026-06-11T05:54:24Z
---

<implementer date="2026-06-11T05:54:24Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Reused `reap_lock_sidecar` (speccy-cli/src/journal.rs:726, the guarded idempotent unlink helper landed by T-002) and `VetBlockKind::Gate` (re-exported from speccy_core::parse at journal.rs:39) verbatim — no new symbols introduced. The gate-only `matches!(kind, VetBlockKind::Gate)` reap pattern mirrors the T-003 task-transition call site (speccy-cli/src/transition.rs:135-140) exactly. Tests reused the existing `journal_append_vet.rs` fixtures (`workspace_with_completed_task`, `vet_path`, `append`); the only new test helper is `vet_lock_path`, a one-line `spec_dir.join("journal").join("VET.md.lock")` sibling of the existing `vet_path` (searched the file for an existing lock-path helper — none existed, since this is the first vet-lock-aware test).
- Completed: Wired `reap_lock_sidecar` into `run_vet_append` (speccy-cli/src/journal.rs ~413-446). Restructured the tail so the append's `LockGuard` is bound inside an explicit lexical-scope block, dropped before the reap, and the bound `Ok`/`Err` result is returned after. The reap fires only on the `Ok` path and only for `VetBlockKind::Gate`; every non-gate vet block leaves `VET.md.lock` in place. The vet lock path and the append's lock acquisition are unmoved. Added four integration tests in journal_append_vet.rs.
- Undone: Nothing in scope deferred. T-004 covers REQ-002 only; the task-lock reap (REQ-001/REQ-003) was T-003 and the helper itself was T-002, both completed.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok; the cpufeatures dual-version note is a pre-existing transitive duplicate, bans still ok).
- Evidence: paper trail in tests at `speccy-cli/tests/journal_append_vet.rs`. Roll call for CHKs under REQ-002:
  - CHK-003 (gate append reaps VET.md.lock; VET.md parses with gate last): demonstrated -> `gate_append_reaps_the_vet_lock_sidecar` lands a drift-review (asserts VET.md + VET.md.lock both exist), runs `--block gate`, asserts VET.md.lock gone and `parse_vet_xml` yields the gate as the last block.
  - CHK-004 (drift-review leaves VET.md.lock): demonstrated -> `drift_review_append_leaves_the_vet_lock_sidecar` asserts the sidecar persists after a drift-review append. Reinforced by `holistic_fix_append_leaves_the_vet_lock_sidecar` (a second non-gate block) and the idempotency test `gate_append_with_absent_lock_is_idempotent` (gate exits zero with an already-absent sidecar).
- Discovered issues: None.
- Procedural compliance: First-attempt shape (clean tree, no prior journal). No skill/source-module friction encountered; no resources/ edits needed.
</implementer>
<review date="2026-06-11T05:55:53Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
PASS. T-004 satisfies REQ-002 end to end: `run_vet_append` (speccy-cli/src/journal.rs:419-446) binds the append result, closes the `LockGuard` lexical scope so the lock is provably released, then reaps `VET.md.lock` only on `appended.is_ok() && matches!(kind, VetBlockKind::Gate)`. Every REQ-002 done-when maps to a driving integration test: gate-reaps-and-VET.md-parses-with-gate-last (`gate_append_reaps_the_vet_lock_sidecar`, CHK-003), non-gate leaves the sidecar (`drift_review_append_leaves_the_vet_lock_sidecar` CHK-004, plus `holistic_fix_append_leaves_the_vet_lock_sidecar` as a second non-gate variant), and absent-lock idempotency (`gate_append_with_absent_lock_is_idempotent`). The gate-only guard is a single `matches!`, so the two non-gate variants tested are representative of the four-variant non-goal — no behavioral gap. All vet blocks dispatch through the one `run_vet_append` (journal.rs:300), so no gate path bypasses the reap. No scope sneaked in: Drop is untouched, no new subcommand, grammar/contents unchanged — consistent with goals, non-goals, the "spec vetted -> sidecar gone" user story, and the current changelog/OQ-a intent (delete-at-boundary + tracing), not the superseded relocate framing.
</review>
<review date="2026-06-11T05:55:59Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. The vet-lock reap wiring preserves SPEC-0055 REQ-005 mutual
exclusion: the append `LockGuard` is bound inside an explicit lexical
block (`speccy-cli/src/journal.rs:419-431`) and provably dropped — the
guard's `Drop`/handle-close releases the advisory lock — before
`reap_lock_sidecar` observes the sidecar at journal.rs:443-445. No
unlink-while-held race: reap runs only at the terminal `gate`
boundary, gated on `appended.is_ok() && matches!(kind,
VetBlockKind::Gate)`, so a failed append (file possibly mid-write) and
every non-gate block leave the lock intact for the next appender, and
the helper closes its handle before `remove_file` (DEC-001/DEC-004).
No injection vector: the lock path is `journal_dir.join("VET.md.lock")`
derived deterministically from the resolved spec dir, never from
attacker-controlled input — no path traversal. No sensitive-data
exposure: the only diagnostics are the existing `WARN` events carrying
the sidecar path and I/O error string to stderr — no journal contents,
credentials, or stdout/JSON pollution. No auth/authz boundary, crypto,
or network surface in this slice. Tests in
`speccy-cli/tests/journal_append_vet.rs` cover the gate-reaps,
non-gate-persists, and absent-lock-idempotent cases without weakening
any guarantee.
</review>
<review date="2026-06-11T05:56:07Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests genuinely exercise REQ-002 against the real binary; both covered CHKs are demonstrated, not mocked. The four new integration tests in `journal_append_vet.rs` drive `speccy journal append` end-to-end via `Command::cargo_bin("speccy")` + `current_dir(ws.root)` — no mocking of the system under test. CHK-003 (`gate_append_reaps_the_vet_lock_sidecar`) lands a real drift-review, asserts both `VET.md` and `VET.md.lock` exist as a load-bearing precondition (guarding against a vacuous pass where the sidecar was never created — `LockGuard::acquire` uses `create(true)` and `Drop` is unlink-only, so this precondition is real), then runs `--block gate`, asserts the lock is gone, and re-parses `VET.md` via the strict `parse_vet_xml` with `?` propagation, asserting the gate is the last block. CHK-004 (`drift_review_append_leaves_the_vet_lock_sidecar`) plus `holistic_fix_append_leaves_the_vet_lock_sidecar` give the negative path explicit assertions (`lock.exists()` after a non-gate append), not mere absence-of-crash. `gate_append_with_absent_lock_is_idempotent` covers the REQ-002 done-when (gate exits zero with sidecar already absent). Falsification: a no-op reap fails CHK-003; a reap-on-every-block fails the two non-gate tests; a reap that corrupts `VET.md` fails the `parse_vet_xml` propagation. The journal Evidence roll call lists CHK-003 and CHK-004 both `demonstrated` with the exact test names, which appear in the diff. No fabrication patterns matched (no invented test names, no red/green output to compare). The full-suite `cargo test --workspace` line in Hygiene checks is supplementary, not the demonstrating evidence — the named integration tests are. Naming/structure match the file's existing convention (`TestResult` return, descriptive snake_case, `VetBlock`/`parse_vet_xml` assertions).
</review>
<review date="2026-06-11T05:56:41Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defect found in the T-004 vet-lock reap wiring.

Verified `run_vet_append` (speccy-cli/src/journal.rs:413-446): the append's
`LockGuard` is bound inside an explicit lexical block and provably dropped
before the terminal reap runs, so the lock is never held across the unlink.
The reap condition `appended.is_ok() && matches!(kind, VetBlockKind::Gate)`
(journal.rs:443) is correct: `Result::is_ok` borrows (so the bound result is
still returnable at journal.rs:446) and `VetBlockKind` is `Copy` (so the
post-block `matches!` reads a copy, not a moved value). Only the gate reaps;
every non-gate block and every `Err` path skips the reap and returns the
bound result unchanged. The `LockGuard::acquire(...)?` early return at
journal.rs:420 fires before any append or held-lock state, so no resource
leaks and no reap is owed there.

The reaped helper it calls (`reap_lock_sidecar`) drops its own handle before
`remove_file` (DEC-004), making the unlink host-portable; absent sidecar
(`NotFound`) and held lock (`WouldBlock`) are silent no-ops.

Tests in speccy-cli/tests/journal_append_vet.rs falsify a wrong
implementation: an inverted reap condition would fail
`drift_review_append_leaves_the_vet_lock_sidecar` /
`holistic_fix_append_leaves_the_vet_lock_sidecar`, and a non-reaping gate
would fail `gate_append_reaps_the_vet_lock_sidecar`. Ran
`cargo test -p speccy-cli --test journal_append_vet`: 15 passed, 0 failed.

The handle-drop-to-unlink TOCTOU window and same-process advisory-lock
semantics are documented design decisions (DEC-004) / pre-existing SPEC-0055
behavior, not defects.
</review>
<review date="2026-06-11T05:56:56Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style conventions are met for both changed files.

`speccy-cli/src/journal.rs`: The `run_vet_append` restructuring (lexical-scope block to drop `_guard` before reap, then `if appended.is_ok() && matches!(kind, VetBlockKind::Gate)`) mirrors the sibling `transition.rs:135-140` pattern exactly. Comment style, naming (`appended`, `lock_path`), and inline SPEC reference format (`SPEC-0058 REQ-002 / DEC-003`) all match the surrounding file. The `#[cfg_attr(not(unix), expect(dead_code, reason = "..."))]` on `CapturedEvent::sidecar` carries the required `reason` attribute per AGENTS.md. Separate `use std::sync::Arc;` / `use std::sync::Mutex;` lines in the test module are consistent with the formatter's output (hygiene checks pass per the implementer's report).

`speccy-cli/tests/journal_append_vet.rs`: The new `vet_lock_path` helper is a one-liner that follows the exact same shape as the existing `vet_path` helper at line 52. The four new tests (`gate_append_reaps_the_vet_lock_sidecar`, `drift_review_append_leaves_the_vet_lock_sidecar`, `holistic_fix_append_leaves_the_vet_lock_sidecar`, `gate_append_with_absent_lock_is_idempotent`) drive the real binary via `assert_cmd`, assert structurally meaningful properties (lock presence/absence + `parse_vet_xml` on VET.md), and match the import and scaffolding conventions of the surrounding test file. No vacuous tests, no bare `#[allow]` suppressions, no dead imports, no reinvented helpers.

Reuse survey: present and honest. `reap_lock_sidecar` (journal.rs:746), `VetBlockKind::Gate`, and the `workspace_with_completed_task`/`vet_path`/`append` fixtures are all confirmed to exist at the named paths and are the symbols the diff calls. `vet_lock_path` is genuinely novel — no prior lock-path helper existed in this test file.
</review>
