---
spec: SPEC-0022
task: T-009
generated_at: 2026-05-21T09:22:08Z
---

<implementer date="2026-05-16T20:00:00Z" model="claude-sonnet-4-6[1m]/medium" round="1">
- Completed: Removed `xtask/migrate-task-report-xml-0022/` directory via `rm -rf` and dropped the workspace member entry from the root `Cargo.toml`. The `members` array now reads `["speccy-cli", "speccy-core"]`. The empty `xtask/` directory was left in place per task instructions (no parent `xtask` crate).
- Commands run: `rm -rf xtask/migrate-task-report-xml-0022` (pass), `cargo build --workspace` (pass, 0.04s, no rebuild), `cargo test --workspace` (pass, all suites green including workspace_xml's 6 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings` (pass, clean), `cargo +nightly fmt --all --check` (pass, EXIT=0; the `float_literal_trailing_zero` warnings are pre-existing rustfmt-config noise, not from this change), `grep -rn 'migrate-task-report-xml-0022\|migrate_task_report_xml_0022' .` (pass — only SPEC.md and TASKS.md hits, all documentary). Verified `Cargo.lock` has zero references via separate grep.
- Discovered issues: (none)
- Procedural compliance: (none)

(Migrated from legacy non-XML-wrapped `session-t009-2026-05-16` block per DEC-006. The legacy form lacked an `<implementer-note>` outer tag — author recovered the body verbatim and synthesised the wrapper.)
</implementer>
