---
spec: SPEC-0025
task: T-002
generated_at: 2026-05-21T09:22:08Z
---

<implementer date="2026-05-17T22:00:00Z" model="claude-sonnet-4-6[1m]/medium" round="1">
- Completed: Added `resources/agents/.claude/skills/speccy-brainstorm/SKILL.md.tmpl` and `resources/agents/.agents/skills/speccy-brainstorm/SKILL.md.tmpl` following the existing per-host wrapper pattern: YAML frontmatter with `name: speccy-brainstorm` and a `description:` line naming the brainstorming purpose + trigger phrases, then a single `{% include "modules/skills/speccy-brainstorm.md" %}` directive. Extended `speccy-cli/tests/skill_packs.rs::SKILL_NAMES` from 7 entries to 8 with `"speccy-brainstorm"`. Extended `speccy-cli/tests/init.rs::SKILL_NAMES` similarly, updating the array type from `[&str; 7]` to `[&str; 8]`. Updated two renderer-internal tests in `speccy-cli/src/render.rs::tests` (`render_host_pack_claude_code_emits_seven_skills` → `..._emits_eight_skills` and Codex equivalent) so the bundle-count assertions expect the new total. All renderer + skill-pack enumeration tests pass.
- Undone: Dogfood files at `.claude/skills/speccy-brainstorm/SKILL.md` and `.agents/skills/speccy-brainstorm/SKILL.md` are not yet committed; T-003 re-ejects.
- Commands run: `cargo test --workspace`
- Exit codes: fail (only `dogfood_outputs_match_committed_tree`, expected — fixed by T-003 re-eject)
- Discovered issues: (none)
- Procedural compliance: (none)

(Migrated from legacy `<implementer-note session="2026-05-17-T002-rev2">` per DEC-006.)
</implementer>

<review date="2026-05-17T22:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
metadata-only change. The two new `SKILL.md.tmpl` files carry static YAML frontmatter (`name:` plus a hardcoded `description:` of trigger phrases) and a single `{% include "modules/skills/speccy-brainstorm.md" %}` against a compile-time-embedded resource path — no runtime input, no user-controllable path, no template-injection surface. The `SKILL_NAMES` extensions in `speccy-cli/tests/skill_packs.rs:189` and `speccy-cli/tests/init.rs:59` (with the `[&str; 7]` → `[&str; 8]` bump) are compile-time-checked literals; no unsafe indexing, no arithmetic on lengths. No new dependencies, no secrets, no auth, no logging, no crypto. Nothing in this slice exposes attack surface.
</review>

<review date="2026-05-17T22:30:00Z" model="claude-sonnet-4-6[1m]/low" persona="style" verdict="blocking" round="1">
two byte-shape drifts from the existing seven-wrapper pattern. (1) Both new `.tmpl` files end with `%}\n` (trailing newline after the close of the include directive), while every existing wrapper under `resources/agents/.claude/skills/*/SKILL.md.tmpl` and `resources/agents/.agents/skills/*/SKILL.md.tmpl` ends with `%}` (no trailing newline). The drift propagates through `render_host_pack` into the dogfood mirrors. Strip the trailing newline on both `.tmpl` files and re-eject the dogfood mirrors so the byte-shape matches the other seven shipped skills. (2) The Claude and Codex `description:` lines diverge (Claude says `before invoking /speccy-plan`, Codex says `before invoking speccy-plan`). Every other Claude/Codex wrapper pair is byte-identical in its description. Pick one form and apply to both wrappers.
</review>

<blockers date="2026-05-17T23:00:00Z" round="1">
style blocked on two byte-shape drifts from the established seven-wrapper pattern. (1) Both new `.tmpl` files end with `%}\n`; every other wrapper ends with `%}` (no trailing newline). Strip the trailing newline on both `.tmpl` files and re-eject. (2) Claude and Codex `description:` lines diverge — drop or add the slash so both forms match. Business, security, tests pass; note the non-blocking "exactly_seven" naming carryover at `speccy-cli/tests/skill_packs.rs` lines 1149/1175/1258/1284 that the tests reviewer flagged for a separate follow-up.
</blockers>

<implementer date="2026-05-18T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" round="2">
- Completed: Resolved both style-blocker byte-shape drifts. (1) Stripped the trailing newline on both wrapper templates so both end with bytes `64 22 20 25 7d` (`d" %}`) and no final `0a`, matching every sibling wrapper. (2) Aligned the `description:` lines across hosts: dropped the leading `/` on the Claude side so both wrappers now read `before invoking speccy-plan on an unclear ask.` (bare form). The two `.tmpl` files are now byte-identical (both 592 bytes). Re-ejected the dogfood mirrors with `cargo run --release -- init --force --host claude-code` and `--host codex`; the four affected dogfood files now end with a single `0a` trailing newline and `dogfood_outputs_match_committed_tree` passes.
- Undone: (none)
- Commands run: `cargo build --release --bin speccy`; `.\target\release\speccy.exe next --kind implement --json`; `.\target\release\speccy.exe implement SPEC-0025/T-002`; `cargo run --release -- init --force --host claude-code`; `cargo run --release -- init --force --host codex`; `cargo test --workspace --no-fail-fast`; `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`; `cargo +nightly fmt --all --check`; `.\target\release\speccy.exe check SPEC-0025/T-002`.
- Exit codes: pass; pass; pass; pass; pass; pass (all suites green including the previously-failing `dogfood_outputs_match_committed_tree`); fail-local (43 pre-existing `result_large_err` errors on `speccy-core::error::ParseError` — NOT introduced by this task and matching T-003's documented Windows-only toolchain drift); pass; pass.
- Discovered issues: (none new; the local-only `result_large_err` clippy noise is the same one T-003's implementer note already flagged for a future spec touching `error.rs`).
- Procedural compliance: (none)

(Migrated from legacy `<implementer-note session="2026-05-17-T002-rev3">` per DEC-006.)
</implementer>

<review date="2026-05-18T01:00:00Z" model="claude-sonnet-4-6[1m]/low" persona="business" verdict="pass" round="2">
T-002's slice of REQ-001 lands cleanly. Both wrapper templates carry the `name: speccy-brainstorm` + descriptive `description:` + single `{% include "modules/skills/speccy-brainstorm.md" %}` shape that matches the existing speccy-* wrappers, satisfying REQ-001 done-when items 2-3. `SKILL_NAMES` in skill_packs.rs and init.rs both include `"speccy-brainstorm"` with the type bump to `[&str; 8]`, satisfying done-when item 4. The Codex description correctly drops the leading slash on `speccy-plan`. The renderer-internal test rename is the mechanical consequence of growing the bundle and is transparently called out in the implementer note. The byte-for-byte host-mirror rendering check is correctly punted to T-003's re-eject step. No non-goal violations.
</review>

<review date="2026-05-18T01:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="tests" verdict="pass" round="2">
the two wrapper `.tmpl` files are exercised by real assertions, not mocks. `t005_claude_code_wrapper_shape_and_body` and `t006_codex_wrapper_shape_and_body` iterate the now-8-entry `SKILL_NAMES`, read each `speccy-brainstorm/SKILL.md.tmpl` from disk, parse the frontmatter with `serde_saphyr`, assert `name == "speccy-brainstorm"`, require a non-empty single-line `description`, and require the body to equal the include directive byte-for-byte. The integration tests drive `speccy init` against a real temp dir and assert that for every entry in `SKILL_NAMES` the rendered SKILL.md exists, frontmatter parses, name matches, and body is non-empty. The single expected failure (`dogfood_outputs_match_committed_tree`) is correctly scoped to T-003. Non-blocking maintainability nit: lines 1149, 1175, 1258, 1284 still say "exactly_seven" in function names even though the comparator is now 8 entries — flag as follow-up.
</review>

<review date="2026-05-18T01:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
rev3 keeps the slice security-clean. Both wrappers are now byte-identical at 592 bytes (verified via byte-tail), carrying only static YAML frontmatter and a single include directive against a compile-time-embedded resource path. Aligning the Claude description removes a cross-host string mismatch but introduces no new surface. The `SKILL_NAMES` extension is compile-time literals. The re-eject path exercises `render_host_pack` already covered by dozens of existing tests. No new dependencies, no secrets, no auth, no logging, no crypto, no network surface, no template-injection or path-traversal vector introduced.
</review>

<review date="2026-05-18T01:00:00Z" model="claude-sonnet-4-6[1m]/low" persona="style" verdict="pass" round="2">
rev3 closes both prior blockers cleanly. (1) Trailing-newline drift fixed — both `.tmpl` files now end with bytes `25 7d` with no terminal `0a`, matching all seven sibling templates. The two new files are now byte-identical at 592 bytes. (2) Cross-host description divergence fixed — both wrappers now read the bare form, matching the established convention. The downstream dogfood mirrors all end with a single trailing `0a`. The `SKILL_NAMES` extensions insert `"speccy-brainstorm"` at the end of the list in matching position across both files; the `[&str; 8]` length bump is the idiomatic way to grow a fixed-length array; zero `#[allow]` / `#[expect]` suppressions added by this slice; `cargo test --test skill_packs` runs 42/42 green. The "exactly_seven" function-name carryover remains as the tests reviewer noted — not blocking; worth a FOLLOWUPS.md entry.
</review>
