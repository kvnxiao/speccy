# Evidence

## Session session-2026-05-20-t006-attempt-1

Red-then-green paper trail for T-006: self-review section in `speccy-amend.md`.

### Scenario 1 — Six shared property names + two amend-specific (CHK-004)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-amend.md`

```
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -in "routing fidelity\|atomization\|scope-traces\|internal consistency\|placeholder leakage\|ambiguity\|Changelog row presence\|surgical-diff shape"
(no output)
```
</red>

<green>
Post-edit: all eight property names present in working tree

```
$ grep -in "routing fidelity\|atomization\|scope-traces\|internal consistency\|placeholder leakage\|ambiguity\|Changelog row presence\|surgical-diff shape" resources/modules/skills/speccy-amend.md
62:   - **Routing fidelity.** Brainstorm artifacts landed in the
67:     ran for this SPEC. When brainstorm was skipped, scope-traces
71:   - **Atomization.** No `<requirement>` body contains "and"/"also"
76:   - **Scope-traces.** Every `<requirement>` traces to a brainstorm
81:   - **Internal consistency.** No contradictions exist across the
86:   - **Placeholder leakage.** No `TBD`, `TODO`, or untouched
90:   - **Ambiguity.** No `<requirement>` wording is interpretable in
95:   - **Changelog row presence.** The `## Changelog` section contains
100:   - **Surgical-diff shape.** Only the requirements and sections
```
</green>

### Scenario 2 — Literal `**Self-review caught:**` template string (CHK-007)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-amend.md`

```
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -n "Self-review caught"
(no output)
```
</red>

<green>
Post-edit: exactly one match, on a single line, embedded in the fixed template string

```
$ grep -n "Self-review caught" resources/modules/skills/speccy-amend.md
54:   `- [ ] {ordinal}. **Self-review caught:** {issue}`
```
</green>

### Scenario 3 — Tie-breaker line (CHK-006)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-amend.md`

```
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -in "if judging requires reading semantics"
(no output)
```
</red>

<green>
Post-edit: tie-breaker present

```
$ grep -in "if judging requires reading semantics" resources/modules/skills/speccy-amend.md
49:   chat. If judging requires reading semantics, it is semantic.
```
</green>

### Scenario 4 — No-loop instruction (CHK-009)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-amend.md`

```
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -in "do not re-check"
(no output)
```
</red>

<green>
Post-edit: no-loop instruction present

```
$ grep -in "do not re-check" resources/modules/skills/speccy-amend.md
36:   applying fixes.
```
</green>

### Scenario 5 — Section position

Self-review section (step 3, lines 35-104) appears after the SPEC.md diff-write and
Changelog-append step (step 2, lines 24-34) and before the next-step handoff
suggestion (line 125: `Suggest the next step: ...speccy-work SPEC-0007`).

### Commands run

```
$ cargo run -- check SPEC-0034
19 scenarios rendered across 1 specs
exit 0

$ cargo test --workspace
test result: FAILED. 34 passed; 1 failed
(only dogfood_outputs_match_committed_tree — pre-existing local model override)

$ cargo run -- next --json SPEC-0034
{"schema_version":1,"spec_id":"SPEC-0034","next_action":{"kind":"review","task_id":"T-006"},...}
exit 0
```
