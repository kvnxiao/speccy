# Evidence

## Session session-2026-05-20-t005-attempt-1

Red-then-green paper trail for T-005: self-review section in `speccy-plan.md`.

### Scenario 1 — Six property names (CHK-003)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-plan.md`

```
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -in "routing fidelity\|atomization\|scope-traces\|internal consistency\|placeholder leakage\|ambiguity"
(no output)
```
</red>

<green>
Post-edit: all six property names present in working tree

```
$ grep -in "Routing fidelity\|Atomization\|scope-traces\|Internal consistency\|Placeholder leakage\|Ambiguity" resources/modules/skills/speccy-plan.md
62:   - **Routing fidelity.** Brainstorm artifacts landed in the
71:   - **Atomization.** No `<requirement>` body contains "and"/"also"
67:     ran for this SPEC. When brainstorm was skipped, scope-traces
76:   - **Scope-traces.** Every `<requirement>` traces to a brainstorm
81:   - **Internal consistency.** No contradictions exist across the
86:   - **Placeholder leakage.** No `TBD`, `TODO`, or untouched
90:   - **Ambiguity.** No `<requirement>` wording is interpretable in
```
</green>

### Scenario 2 — Literal `**Self-review caught:**` template string (CHK-007)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-plan.md`

```
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -n "Self-review caught"
(no output)
```
</red>

<green>
Post-edit: exactly one match, on a single line, embedded in the fixed template string

```
$ grep -n "Self-review caught" resources/modules/skills/speccy-plan.md
54:   `- [ ] {ordinal}. **Self-review caught:** {issue}`
```
</green>

### Scenario 3 — Tie-breaker line (CHK-006)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-plan.md`

```
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -in "if judging requires reading semantics"
(no output)
```
</red>

<green>
Post-edit: tie-breaker present

```
$ grep -in "if judging requires reading semantics" resources/modules/skills/speccy-plan.md
49:   chat. If judging requires reading semantics, it is semantic.
```
</green>

### Scenario 4 — No-loop instruction (CHK-009)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-plan.md`

```
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -in "do not re-check"
(no output)
```
</red>

<green>
Post-edit: no-loop instruction present

```
$ grep -in "do not re-check" resources/modules/skills/speccy-plan.md
41:   SPEC.md. Do not re-check after applying fixes.
```
</green>

### Scenario 5 — Routing-fidelity brainstorm-only scope note

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-plan.md`

```
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -in "brainstorm was skipped"
(no output)
```
</red>

<green>
Post-edit: scope note present

```
$ grep -in "brainstorm was skipped" resources/modules/skills/speccy-plan.md
67:     ran for this SPEC. When brainstorm was skipped, scope-traces
```
</green>

### Scenario 6 — Section position (CHK-001 prerequisite)

Self-review section (step 3, lines 40-94) appears after the SPEC.md write step
(step 2, lines 32-38) and before the `/speccy-tasks` handoff suggestion
(step 5, line 106).

### Commands run

```
$ cargo run -- check SPEC-0034
19 scenarios rendered across 1 specs
exit 0

$ cargo test --workspace
test result: FAILED. 34 passed; 1 failed
(only dogfood_outputs_match_committed_tree — pre-existing local model override)

$ cargo run -- next --json SPEC-0034
{"schema_version":1,"spec_id":"SPEC-0034","next_action":{"kind":"review","task_id":"T-005"},...}
exit 0
```
