# Evidence for SPEC-0034 T-003

Update the `## Open Questions` format guidance in all three authoring-phase
skill templates to use `- [ ] a.` ... `- [ ] z.` alpha-prefix in lock-step,
add the 26-cap scope-smell note, and add the going-forward-only caveat to each.
The amend template additionally instructs the agent to preserve existing
ordinals and allocate the next free letter when appending (no renumbering on amend).

## Session session-2026-05-20-t003-attempt-1

Verifies three task-scenarios across three files:

1. `resources/modules/skills/speccy-plan.md` — contains `- [ ] a.` alpha-prefix
   format guidance for `## Open Questions`, the 26-cap note, and the
   going-forward-only caveat.
2. `resources/modules/skills/speccy-amend.md` — contains the same alpha-prefix
   format guidance, plus the preserve-existing-ordinals / allocate-next-free-letter
   instruction (no renumbering on amend).
3. `resources/modules/skills/speccy-brainstorm.md` — contains the same
   alpha-prefix format guidance and 26-cap note in both the open-questions
   artifact step (step 3.4) and the routing section.

### Pre-edit (git show HEAD — state before T-003 work)

Command: `git show HEAD:<path> | grep -n "a\.\|alpha\|ordinal\|26.*cap\|Open Questions"` (simplified)

<red>
$ git show HEAD:resources/modules/skills/speccy-plan.md \
  | grep -n "\- \[ \]\|alpha\|ordinal\|26"
54:4. Surface any material questions inline in `## Open Questions`.

(no alpha-prefix guidance; step 3 only says "Surface any material questions
 inline in ## Open Questions" — no `- [ ] a.` format, no 26-cap note,
 no going-forward-only caveat)

$ git show HEAD:resources/modules/skills/speccy-amend.md \
  | grep -n "\- \[ \]\|alpha\|ordinal\|26\|Open Questions"
(no output — zero matches; no Open Questions guidance in the file at all)

$ git show HEAD:resources/modules/skills/speccy-brainstorm.md \
  | grep -n "\- \[ \]\|alpha\|ordinal\|26"
90:      would change the SPEC shape. Use the `- [ ]` checkbox format
91:      that matches the PRD template's `## Open Questions` section,
135:- The **open questions** land under `## Open Questions` in the same
136:  `- [ ]` checkbox format the brainstorm produced.

(brainstorm used `- [ ]` unordered format — no alpha-prefix, no 26-cap note,
 no going-forward-only caveat in either the step body or the routing section)
</red>

### Post-edit (working tree after T-003 edits)

Command: `grep -n "a\.\|alpha\|ordinal\|26\|going forward" <file>` (simplified)

<green>
$ grep -n "\- \[ \] a\.\|alpha.prefix\|ordinal\|26.*questions\|going forward" \
    resources/modules/skills/speccy-plan.md
35:   the alpha-prefix format: `- [ ] a.`, `- [ ] b.`, ..., `- [ ] z.`.
36:   Each question gets the next free letter in sequence. If the section
37:   already exists, preserve existing ordinals and allocate the next free
38:   letter for any new question added (no renumbering). Reaching `z.`
39:   signals an over-scoped session — 26 open questions is a scope smell,
40:   not a format limitation. This format applies going forward only;
41:   existing SPECs retain their current `- [ ]` formatting unless
42:   explicitly amended.

$ grep -n "\- \[ \] a\.\|alpha.prefix\|ordinal\|26.*questions\|going forward" \
    resources/modules/skills/speccy-amend.md
27:   If editing `## Open Questions`, use the alpha-prefix format:
28:   `- [ ] a.`, `- [ ] b.`, ..., `- [ ] z.`. Preserve existing ordinals
29:   (do not renumber on amend); allocate the next free letter when
30:   appending a new question. Reaching `z.` signals an over-scoped
31:   session — 26 open questions is a scope smell, not a format
32:   limitation. This format applies going forward only; existing SPECs
33:   retain their current `- [ ]` formatting unless touched by this
34:   amendment.

$ grep -n "\- \[ \] a\.\|alpha.prefix\|ordinal\|26.*questions\|going forward" \
    resources/modules/skills/speccy-brainstorm.md
90:      `- [ ] a.`, `- [ ] b.`, ..., `- [ ] z.`, assigning the next
91:      free letter to each question. This format matches the PRD
92:      template's `## Open Questions` section so the output is
94:      Reaching `z.` signals an over-scoped session — 26 open
96:      format applies going forward only; sessions begun before this
142:  `- [ ] a.` alpha-prefix format the brainstorm produced — copy-paste
</green>

All three files contain `- [ ] a.` alpha-prefix guidance, the 26-cap scope-smell
note ("26 open questions is a scope smell, not a format limitation"), and the
going-forward-only caveat. The amend template uniquely carries the preserve-ordinals
/ allocate-next-free instruction and the explicit no-renumbering-on-amend caveat.
