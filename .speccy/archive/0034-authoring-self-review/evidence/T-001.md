# Evidence for SPEC-0034 T-001

Remove the amendment branch from `speccy-plan.md` and the amendment phrases
from the wrapper-template `description:` lines in both host skill packs.
Attempt 2 completes what attempt 1 left unaddressed: the two wrapper `.tmpl`
files and this evidence file.

## Session session-2026-05-20-t001-attempt-2

Verifies two task-scenarios across three files:

1. `resources/modules/skills/speccy-plan.md` — lede / "When to use" / "Steps"
   contain no "Amendment", "amend an existing", or "SPEC-NNNN argument" wording.
2. `resources/agents/.claude/skills/speccy-plan/SKILL.md.tmpl` line 3 —
   `description:` contains no "amend an existing one when intent shifts" or
   "asks to amend an existing spec by ID".
3. `resources/agents/.agents/skills/speccy-plan/SKILL.md.tmpl` line 3 —
   same as (2).

### Pre-edit (git show HEAD — state before any T-001 work)

Command: `git show HEAD:<path> | grep -n "Amendment|amend an existing|SPEC-NNNN argument"`

<red>
$ git show HEAD:resources/modules/skills/speccy-plan.md \
  | grep -n "Amendment\|amend an existing\|SPEC-NNNN argument"
21:- Amendment form (`{{ cmd_prefix }}speccy-plan SPEC-NNNN`): when an
41:   - **Amendment**: read the existing spec's current state:

(no "amend an existing" or "SPEC-NNNN argument" literals in the module file;
 "Amendment" appears at lines 21 and 41 — the "When to use" and "Steps" sections)

$ git show HEAD:resources/agents/.claude/skills/speccy-plan/SKILL.md.tmpl \
  | grep -n "Amendment\|amend an existing\|SPEC-NNNN argument"
3:description: '...or amend an existing one when intent shifts. Use when the
   user wants to "write a spec", "draft a SPEC", "spec out X", "plan a new
   feature with speccy", or asks to amend an existing spec by ID...'

$ git show HEAD:resources/agents/.agents/skills/speccy-plan/SKILL.md.tmpl \
  | grep -n "Amendment\|amend an existing\|SPEC-NNNN argument"
3:description: '...or amend an existing one when intent shifts. Use when the
   user wants to "write a spec", "draft a SPEC", "spec out X", "plan a new
   feature with speccy", or asks to amend an existing spec by ID...'
</red>

Note: attempt 1 cleaned the module file (lede / When to use / Steps). The
wrapper `.tmpl` files were not touched in attempt 1; their line 3 still carried
"amend an existing one when intent shifts" and "asks to amend an existing spec
by ID" when attempt 2 began.

### Post-edit (working tree after attempt 2)

Command: `grep -n "Amendment|amend an existing|SPEC-NNNN argument" <file>`

<green>
$ grep -n "Amendment\|amend an existing\|SPEC-NNNN argument" \
    resources/modules/skills/speccy-plan.md \
    resources/agents/.claude/skills/speccy-plan/SKILL.md.tmpl \
    resources/agents/.agents/skills/speccy-plan/SKILL.md.tmpl
(no output — exit 1, zero matches in all three files)
</green>
