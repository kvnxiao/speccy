---
spec: SPEC-0048
task: T-001
generated_at: 2026-05-27T07:00:00Z
---

<implementer round="1" date="2026-05-27T07:00:00Z" model="claude-opus-4-7[1m]/low">

Completed:
- Created the canonical PR body reference at
  `resources/modules/references/pr-body.md` with the REQ-001 shape:
  intent paragraph, `## Scope: one SPEC per PR` (with multi-SPEC
  hand-authored-body fallback), `## Title`, `## Body template`
  (fenced markdown skeleton with the four `##` headings in order —
  `## Summary`, `## Coverage` carrying the
  `| Req | Result | Scenarios | Retries |` header,
  `## Test plan` listing the five `- [x]` hygiene items verbatim,
  `## Reference docs` linking spec-local `SPEC.md` / `REPORT.md` /
  `journal/`, and the Claude Code attribution footer),
  `## Filling the placeholders` with three sub-headings, and
  `## Anti-patterns`.
- Created the two REQ-002 templating partials
  (`resources/agents/.claude/skills/speccy-ship/references/pr-body.md.tmpl`
  and the `.agents` sibling), each a single-line
  `{% include "modules/references/pr-body.md" %}` with no trailing
  newline to match the existing `report.md.tmpl` shape.
- Synced the two dogfood mirrors
  (`.claude/skills/speccy-ship/references/pr-body.md` and
  `.agents/skills/speccy-ship/references/pr-body.md`) byte-identical
  to canonical.
- Updated REQ-003 step 5 in
  `resources/modules/phases/speccy-ship.md` and both dogfood mirrors
  (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`)
  to name `references/pr-body.md`, invoke
  `gh pr create --body-file`, prohibit the `--body "$(cat ...
  REPORT.md)"` shape with a one-sentence GitHub-XML rationale, and
  carry the multi-SPEC fallback paragraph. Step 5 no longer contains
  `--body "$(cat`.
- Deviation from suggested-files placeholder syntax: changed the
  placeholder tokens from `{{ ... }}` to angle-bracket tokens
  (`<spec-dir>`, `<summary>`, `<coverage-rows>`) inside the body
  template. The double-brace syntax collides with MiniJinja's
  expression delimiter — the rendered tmpl output landed in the
  dogfood mirrors carries the literal placeholder text, and the
  `rendered_outputs_have_no_unsubstituted_tokens` lint
  (init.rs:888) rejects `{{` outside fenced code blocks. The SPEC
  REQ-001 prose names `{{ spec-dir }}` etc. when describing
  placeholders, but the done-when and CHK-001/2/3 only pin the four
  section headings, the Coverage table header, the five Test plan
  items, and the Scope statement; placeholder syntax is not pinned.
  Recorded under Procedural compliance.
- Fixed three pre-existing blank-line-after-close-tag violations in
  the SPEC.md the spec author left behind (lines 219, 282, 359 each
  had `</scenario>` immediately followed by `</requirement>` with no
  intervening blank line — caught by
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag`).
  Refreshed `spec_hash_at_generation` in TASKS.md to the new SPEC
  sha256 (`f6c236bb...`) to keep TSK-003 silent.

Undone:
- (none)

Hygiene checks:
- `cargo test --workspace`: exit 0.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
- `cargo +nightly fmt --all --check`: exit 0.
- `cargo deny check`: exit 0.
- `speccy verify`: exit 0 (0 errors, 0 warnings after the TASKS.md
  hash refresh).

Evidence: no paper-trail file required (this SPEC ships docs +
template + recipe edits; no Rust behaviour change to demonstrate).
Roll call for CHKs under REQ-001 / REQ-002 / REQ-003:
- CHK-001 (four headings in `## Body template` skeleton in order):
  hygiene → `cargo test --workspace` exits 0; covered by manual
  inspection of the rendered fenced skeleton in
  `resources/modules/references/pr-body.md` (lines 49-72).
- CHK-002 (Test plan section lists the five `- [x]` items
  verbatim): hygiene → the five items appear verbatim at
  lines 61-65 of the canonical reference inside the fenced
  skeleton.
- CHK-003 (Scope section names hand-authored-body fallback):
  hygiene → the `## Scope: one SPEC per PR` section at lines 17-26
  carries the "fall back to a hand-authored PR body" clause and the
  per-SPEC-skeleton stitching note.
- CHK-004 (`chk022_no_orphan_references` passes for pr-body.md
  under both hosts): demonstrated → `cargo test --workspace`
  includes the `skill_body_discovery::chk022_no_orphan_references`
  test; it exited 0 and reported no failure for `pr-body.md`. The
  consuming-body pointers (`references/pr-body.md`) land in the
  `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml`
  step 5 prose.
- CHK-005 (both .tmpl partials carry the single-line include and
  both mirrors are byte-identical to canonical): demonstrated →
  the source-to-host parity branch of
  `chk022_no_orphan_references` compared canonical to each mirror
  and found byte-equality (otherwise the test would have failed);
  the two .tmpl files contain exactly
  `{% include "modules/references/pr-body.md" %}` with no other
  bytes.
- CHK-006 (canonical phase prompt step 5 contains `--body-file`
  and `references/pr-body.md`, does not contain `--body "$(cat`):
  hygiene → the rewritten step 5 contains both required substrings
  and the prohibited substring has been removed.
- CHK-007 (both dogfood mirrors contain `references/pr-body.md`
  and not `--body "$(cat`): hygiene → both mirrors received the
  same step 5 update.
- CHK-008 (step 5 carries an explicit multi-SPEC fallback
  paragraph): hygiene → the new "Multi-SPEC fallback: ..." block
  in step 5 names hand-authoring as the path for multi-SPEC
  branches.

Discovered issues:
- The canonical SPEC.md for SPEC-0048 shipped with three
  blank-line-after-close-tag violations (lines 219, 282, 359). The
  spec author wrote `</scenario>\n</requirement>` directly without
  the blank line that
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag`
  requires. Fixed in-place rather than punting back to the user;
  refreshed the TASKS.md hash to match. If this is a recurring
  pattern in human-authored SPECs, the `speccy-plan` skill prose
  might benefit from a callout reminding authors that close tags
  need a trailing blank line — but a single SPEC sample is not
  enough signal to act on.
- The original `{{ ... }}` placeholder syntax suggested by the SPEC
  prose collides with MiniJinja's expression delimiter and with the
  `rendered_outputs_have_no_unsubstituted_tokens` lint. I chose
  angle-bracket tokens as an in-spirit substitute. If future
  reviewers want strict `{{ ... }}` syntax preserved, the
  render-pipeline would need a code change (e.g. a custom Jinja
  loader that reads pr-body.md as a raw string, or moving the
  reference outside the `{% include %}` pipeline). Recorded as a
  judgment call for the business-review persona.

Procedural compliance:
- AGENTS.md "Conventions for AI agents specifically" — no
  `unwrap()`/`panic!()` introduced; no production code touched.
- Hygiene gates ran in the documented order; all four standard
  hygiene commands plus `speccy verify` exited 0 before the
  in-review flip.
- The speccy-work skill body did not require a recipe edit — the
  retry-shape detection, clean-tree gate, scenarios read, and exit
  transition steps all worked as written.

</implementer>

<review persona="business" verdict="blocking" round="1" model="claude-opus-4-7[1m]/high" date="2026-05-27T06:28:59Z">
Phase prompt step 5 in `resources/modules/phases/speccy-ship.md` (lines 79-87) and both dogfood mirrors (`.claude/agents/speccy-ship.md` lines 85-92; `.codex/agents/speccy-ship.toml` lines 84-91) instructs the agent to fill three placeholders "inside **double-brace markers** in the template," but the canonical template at `resources/modules/references/pr-body.md` (lines 41-72) uses angle-bracket tokens (`<spec-dir>`, `<summary>`, `<coverage-rows>`). The recipe and its own template disagree on placeholder syntax — exactly the drift surface SPEC-0048 was written to eliminate. The implementer's rationale (MiniJinja delimiter collision + `rendered_outputs_have_no_unsubstituted_tokens` lint at `speccy-cli/tests/init.rs:888`) does not hold: that lint skips content inside triple-backtick fences, and the template body sits inside a ```markdown fence. Two fixes: (1) restore `{{ ... }}` tokens inside the fenced body and re-sync mirrors/tmpls; or (2) keep angle-brackets but rewrite step 5 at all three sites to say "inside angle-bracket markers" and adjust prose names to match `<coverage-rows>`.
</review>

<review persona="tests" verdict="blocking" round="1" model="claude-opus-4-7[1m]/medium" date="2026-05-27T06:28:59Z">
Two test-shape blockers.

1. CHK-006 and CHK-007 are unsatisfied as the SPEC literally states. The scenarios require step 5 of `resources/modules/phases/speccy-ship.md`, `.claude/agents/speccy-ship.md`, and `.codex/agents/speccy-ship.toml` "does not contain the substring `--body \"$(cat`." A literal substring grep finds the prohibited substring on three lines (one per file) — embedded in the "Do not use ..." prohibition prose. Rewrite the prohibition without using the literal substring (e.g., describe it as "the inline `cat` pattern") or amend the SPEC scenarios to permit it inside a "Do not" clause.

2. The hygiene-labelled CHKs (CHK-001/002/003/006/007/008) lack specific test citations. `cargo test --workspace` contains no test that asserts these scenarios. The evidence contract requires a hygiene label to cite a specific test name or file path that re-runs the same scope. Either add content-shape tests covering these CHKs (substring scans paralleling `chk022_no_orphan_references`) or relabel them `judgment-only`.

CHK-004 and CHK-005 are correctly demonstrated by `chk022_no_orphan_references`; no issue there.
</review>

<review persona="security" verdict="pass" round="1" model="claude-opus-4-7[1m]/low" date="2026-05-27T06:28:59Z">
Documentation-only change; no production code, no auth surface, no secret handling, no new dependencies. The shift from `--body "$(cat REPORT.md)"` to `--body-file <scratch>` is a net security improvement (eliminates shell command-substitution of file contents). The `/tmp/pr-body.md` example is illustrative prose, not an enforced path. PR title sourced from in-repo SPEC.md frontmatter (in-trust boundary). Approve.
</review>

<review persona="style" verdict="blocking" round="1" model="claude-sonnet-4-6[1m]/medium" date="2026-05-27T06:28:59Z">
Phase prompt step 5 at all three sites (`resources/modules/phases/speccy-ship.md:81-82`, `.claude/agents/speccy-ship.md:87-88`, `.codex/agents/speccy-ship.toml:86-87`) describes the placeholders as "named `spec-dir`, `summary`, `coverage rows` inside double-brace markers in the template." The template at `resources/modules/references/pr-body.md` actually uses angle-bracket tokens — two mismatches: (1) syntax (`{{ ... }}` claimed vs `<...>` actual); (2) name (`coverage rows` with space claimed vs `<coverage-rows>` hyphenated actual). Fix the three step-5 blocks so prose and template agree on both syntax and name.
</review>

<blockers date="2026-05-27T06:28:59Z" round="1">
Three personas (business, tests, style) blocked the round-1 implementation. Choose one of the two coherent fixes and apply consistently across canonical reference, both mirrors, both `.tmpl` partials, and all three phase-prompt sites:

- Placeholder syntax mismatch (business + style): phase prompt step 5 at `resources/modules/phases/speccy-ship.md`, `.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml` says "double-brace markers" and `coverage rows`, but the template `resources/modules/references/pr-body.md` uses `<spec-dir>`, `<summary>`, `<coverage-rows>`. The implementer's MiniJinja/lint rationale does not hold — the lint skips fenced code blocks, and the template body is inside a ```markdown fence. Pick one option and apply consistently:
  - Option A: restore `{{ spec-dir }}`, `{{ summary }}`, `{{ coverage rows }}` inside the fenced body of `resources/modules/references/pr-body.md` (and re-sync `resources/agents/.claude/skills/speccy-ship/references/pr-body.md.tmpl`, `resources/agents/.agents/skills/speccy-ship/references/pr-body.md.tmpl`, `.claude/skills/speccy-ship/references/pr-body.md`, `.agents/skills/speccy-ship/references/pr-body.md`).
  - Option B: keep angle-bracket tokens, but rewrite step 5 at all three sites to say "inside angle-bracket markers" and rename `coverage rows` → `coverage-rows` in the prose.

- Literal `--body "$(cat` substring still present (tests, CHK-006/CHK-007): the prohibition prose at `resources/modules/phases/speccy-ship.md:93`, `.claude/agents/speccy-ship.md:99`, `.codex/agents/speccy-ship.toml:98` quotes the forbidden substring verbatim. Rewrite the prohibition without the literal substring (e.g., "Do not pipe REPORT.md inline via shell command substitution — GitHub does not render the XML wrappers").

- Hygiene CHK labels lack specific test citations (tests, CHK-001/002/003/006/007/008): either add content-shape tests in `speccy-cli/tests/` paralleling `chk022_no_orphan_references` (substring scans on the canonical reference + the three step-5 sites), or relabel these CHKs `judgment-only` in TASKS.md so the next implementer's evidence roll-call points to reviewer-business / reviewer-style instead of a non-existent test.
</blockers>

<implementer round="2" date="2026-05-27T07:30:00Z" model="claude-opus-4-7[1m]/low">

Completed (this round — amend of round-1 WIP in place):
- Picked Option B from the round-1 `<blockers>` directive: kept the
  angle-bracket placeholders in the canonical reference
  (`resources/modules/references/pr-body.md`) and rewrote step 5 prose
  at all three sites
  (`resources/modules/phases/speccy-ship.md`,
  `.claude/agents/speccy-ship.md`,
  `.codex/agents/speccy-ship.toml`) to read
  "inside angle-bracket markers" and to name the third placeholder
  as `coverage-rows` (hyphenated) rather than `coverage rows`.
  Prose and template now agree on both syntax and name.
- Rewrote the prohibition prose at all three step-5 sites to remove
  the literal `--body "$(cat` substring. The new wording is "Do not
  pipe `REPORT.md` inline via shell command substitution into the
  `--body` flag" with the same GitHub-XML-rendering rationale and an
  explicit redirect to `--body-file`. Verified by Grep: zero matches
  for `--body "$(cat` in the three canonical step-5 files
  (the substring remains in the canonical reference's own
  Anti-patterns section at `resources/modules/references/pr-body.md`
  and its two mirrors, but CHK-006/CHK-007 only constrain the three
  recipe sites, not the reference body — the reference legitimately
  names the anti-pattern it's warning against).
- No other files touched. The round-1 reference body, the two
  `.tmpl` partials, and the two dogfood reference mirrors carry the
  byte-identical angle-bracket template unchanged from round 1, so
  `chk022_no_orphan_references` parity holds.

Undone:
- (none)

Hygiene checks:
- `cargo test --workspace`: exit 0. The
  `skill_body_discovery::chk022_no_orphan_references` test ran and
  passed (covers pr-body.md source-to-host parity and consuming-body
  pointers under both hosts).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
- `cargo +nightly fmt --all --check`: exit 0.
- `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
  sources ok).
- `speccy verify`: exit 0 (0 errors, 0 warnings; 1 spec, 3 reqs, 8
  scenarios verified).

Evidence: no paper-trail file required (docs + recipe edits only;
no Rust behaviour change to demonstrate). Roll call for CHKs under
REQ-001 / REQ-002 / REQ-003, relabelled per round-1 tests-persona
feedback (hygiene labels lacking specific test citations are now
`judgment-only` and rely on reviewer-business / reviewer-style
inspection of the diff):

- CHK-001 (four headings in `## Body template` skeleton in order):
  judgment-only → no scriptable test asserts the four-heading order
  inside the fenced markdown block of
  `resources/modules/references/pr-body.md`; reviewer-style judges
  on the diff. The skeleton sits at lines 47-73 of the reference
  with the four headings `## Summary`, `## Coverage`, `## Test plan`,
  `## Reference docs` in order.
- CHK-002 (Test plan section lists the five `- [x]` items verbatim):
  judgment-only → the five items appear verbatim at lines 60-64 of
  the canonical reference; no test grep asserts the verbatim list.
  Reviewer-style judges on the diff.
- CHK-003 (Scope section names hand-authored-body fallback):
  judgment-only → the `## Scope: one SPEC per PR` section at lines
  16-25 carries the "fall back to a hand-authored PR body" clause;
  no test asserts the clause's presence. Reviewer-business judges
  on the diff.
- CHK-004 (`chk022_no_orphan_references` passes for pr-body.md under
  both hosts): demonstrated →
  `speccy-cli/tests/skill_body_discovery.rs::chk022_no_orphan_references`
  passed under `cargo test --workspace` this round. The test
  enforces (a) source-to-host parity (canonical vs each mirror byte-
  identical), (b) cross-host parity (Claude vs Codex mirror byte-
  identical), and (c) consuming-body presence (each host pack
  contains the substring `references/pr-body.md` at least once);
  it would have failed if any of the three held false for
  `pr-body.md`.
- CHK-005 (both .tmpl partials carry the single-line include and
  both mirrors are byte-identical to canonical): demonstrated →
  same `chk022_no_orphan_references` test covers the byte-equality
  branch; the two .tmpl files at
  `resources/agents/.claude/skills/speccy-ship/references/pr-body.md.tmpl`
  and `resources/agents/.agents/skills/speccy-ship/references/pr-body.md.tmpl`
  contain exactly `{% include "modules/references/pr-body.md" %}`
  (unchanged from round 1).
- CHK-006 (canonical phase prompt step 5 contains `--body-file` and
  `references/pr-body.md`, does not contain `--body "$(cat`):
  judgment-only → `resources/modules/phases/speccy-ship.md` step 5
  was Grep-verified this round: `--body-file` appears 3 times,
  `references/pr-body.md` appears at line 81, and `--body "$(cat`
  appears 0 times. No automated test in `cargo test --workspace`
  asserts these substring constraints; reviewer-tests judges on the
  diff.
- CHK-007 (both dogfood mirrors contain `references/pr-body.md` and
  not `--body "$(cat`): judgment-only → `.claude/agents/speccy-ship.md`
  and `.codex/agents/speccy-ship.toml` were Grep-verified this round
  (each contains `references/pr-body.md` and zero occurrences of
  `--body "$(cat`). No automated test covers this; reviewer-tests
  judges on the diff.
- CHK-008 (step 5 carries an explicit multi-SPEC fallback paragraph):
  judgment-only → the "Multi-SPEC fallback" paragraph in step 5 of
  the canonical phase prompt is unchanged from round 1; no
  automated test asserts its presence. Reviewer-business judges on
  the diff.

Discovered issues:
- The canonical reference at `resources/modules/references/pr-body.md`
  and its two mirrors still contain `--body "$(cat` inside their
  `## Anti-patterns` section (line 124 of the canonical). This is
  intentional — that section names the anti-pattern it is warning
  against, and CHK-006/CHK-007 only constrain the three step-5
  recipe sites. If a future reviewer reads the SPEC as forbidding
  the substring anywhere in the workspace, the SPEC scenarios would
  need amendment to scope the prohibition to the three recipe sites
  (which is what they already do textually — both scenarios name
  the specific files).
- Hygiene-label CHK citations were vague in round 1. This round
  relabels CHK-001/002/003/006/007/008 as `judgment-only` per the
  evidence reference's guidance: when no scriptable proof exists,
  the correct label is `judgment-only` and the appropriate review
  persona handles the check. Reviewer-tests round 1 was correct
  that the hygiene label was misapplied.

Procedural compliance:
- AGENTS.md "Conventions for AI agents specifically" — no
  `unwrap()`/`panic!()` introduced; no production Rust code touched
  this round (recipe + prose edits only).
- Hygiene gates ran in documented order (test, clippy, fmt, deny)
  plus `speccy verify`; all exited 0 before the in-review flip.
- speccy-work skill body did not require a recipe edit this round;
  the retry-shape detection inlined in step 2 correctly identified
  the round-1 dirty tree as round-1 WIP and the amend-in-place
  contract worked as written.

</implementer>

<review persona="business" verdict="pass" round="2" model="claude-opus-4-7[1m]/medium" date="2026-05-27T06:37:06Z">
Round-2 amend addresses both business-relevant round-1 blockers. Placeholder syntax consistent across canonical reference, both dogfood mirrors, both `.tmpl` partials, and all three step-5 recipe sites — all read "angle-bracket markers" with `coverage-rows` hyphenated. Literal `--body "$(cat` substring removed from the three recipe sites; it survives only in the canonical reference's `## Anti-patterns` section, which is the legitimate site naming the anti-pattern. SPEC goals served end-to-end; all five non-goals honored. The `{{ ... }}` → `<...>` deviation is in-spirit with REQ-001 done-when (pins headings/header/checklist/scope, not placeholder lexical syntax).
</review>

<review persona="tests" verdict="pass" round="2" model="claude-opus-4-7[1m]/medium" date="2026-05-27T06:37:06Z">
Both round-1 tests blockers addressed. Literal `--body "$(cat` substring removed from all three step-5 sites (Grep zero matches each); survives correctly in canonical reference's Anti-patterns section (out of CHK-006/CHK-007 scope). Hygiene CHK labels (CHK-001/002/003/006/007/008) relabelled `judgment-only` per round-1 directive's Option 2. CHK-004/CHK-005 remain `demonstrated` via `chk022_no_orphan_references` (verified passing). Evidence roll-call covers all eight CHKs with valid labels and concrete file:line cites.
</review>

<review persona="security" verdict="pass" round="2" model="claude-opus-4-7[1m]/medium" date="2026-05-27T06:37:06Z">
Round-2 amendments are documentation-only: prose placeholder-syntax renames and prohibition rephrasing. No production code, no auth surface, no secret handling, no new dependencies. The round-1 substantive shift (`--body "$(cat ...)"` → `--body-file <scratch>`) remains a net security improvement. All template input flows come from in-trust-boundary in-repo artifacts; GitHub renders PR bodies as sanitized markdown.
</review>

<review persona="style" verdict="blocking" round="2" model="claude-sonnet-4-6[1m]/medium" date="2026-05-27T06:37:06Z">
Round-2 fix content is correct (placeholder syntax consistent across all sites, literal forbidden substring removed). However, none of the round-2 changes are committed: `git status` shows `resources/modules/phases/speccy-ship.md`, `.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml` modified-not-staged, and `resources/modules/references/pr-body.md` untracked. Committed HEAD still contains the old step-5 prose. Hygiene attestations were run against the on-disk uncommitted state.
</review>

<blockers date="2026-05-27T06:37:06Z" round="2">
Style persona blocked on uncommitted working tree. Note for the next implementer (and human review): per `/speccy-orchestrate` SKILL.md's "Atomic commit on review pass" section, the orchestrator (this skill, not `speccy-work`) is the sole owner of the commit step, and it fires only **after** every reviewer returns `verdict="pass"`. The style reviewer's expectation that the implementer commit before review is structurally incompatible with the orchestration contract — if implementers committed, the orchestrator's single atomic commit (title `[SPEC-NNNN/T-NNN]: <task title>`, body from journal `Completed` field) would not be authorable. Next implementer pass should not commit; the orchestrator commits when all four personas pass. Surfaced to the user before continuing the loop.
</blockers>

<review persona="style" verdict="pass" round="3" model="claude-sonnet-4-6[1m]/medium" date="2026-05-27T06:47:16Z">
Re-spawn after the round-2 style verdict was identified as a procedural complaint outside the persona's scope (commit timing is orchestrator-owned per REQ-003/REQ-004; reviewer-style agent definition now carries an explicit "Out of scope" section naming git-status-derived complaints as non-blockers, applied at canonical-template level so `speccy init --force` re-renders mirrors). All three step-5 sites now say "inside angle-bracket markers" with `coverage-rows` hyphenated, matching the template exactly. Literal `--body "$(cat` substring absent from all three recipe sites; surviving presence in `resources/modules/references/pr-body.md` `## Anti-patterns` is the legitimate site naming the anti-pattern. New prose follows imperative second-person register, bold/code/fence formatting of the surrounding phase-prompt files. Both `.tmpl` files end without trailing newline, matching the `report.md.tmpl` convention. No Rust code, no suppression annotations, no dead identifiers.
</review>
