# Evidence for SPEC-0033 T-001

Delete the five prompt-rendering CLI commands and the `trim_to_budget`
mechanism. The task is a deletion slice; the verification commands are
scoped greps against `task-scenarios`.

## Session session-2026-05-20-t001 (attempt 1)

Verifies three task-scenarios:

1. `speccy --help` does not list the five removed commands.
2. No source file contains `trim_to_budget`, `TrimResult`,
   `DEFAULT_BUDGET`, or the `budget.rs` module path.
3. `resources/modules/prompts/` does not exist.

Command: `cargo run --quiet -- --help | rg '^\s+(plan|tasks|implement|review|report)\b'`

<red exit="0">
$ cargo run --quiet -- --help
Deterministic feedback engine for spec-driven development.

Usage: speccy.exe <COMMAND>

Commands:
  init       Scaffold a `.speccy/` workspace and copy the host skill pack
  plan       Render the Phase 1 planning prompt (greenfield or amendment)
  tasks      Render the Phase 2 tasks prompt or commit the spec hash
  implement  Render the Phase 3 implementer prompt for a task
  review     Render the Phase 4 reviewer prompt for one persona
  report     Render the Phase 5 report prompt for a completed spec
  status     Print workspace overview (text by default; `--json` for envelope)
  next       Pick the next actionable task across the workspace
  check      Run command-form proofs from spec.toml
  verify     CI gate: lint + check execution with a binary exit code
  help       Print this message or the help of the given subcommand(s)

$ rg --type rust 'trim_to_budget|TrimResult|DEFAULT_BUDGET'
speccy-core/src/prompt/mod.rs:3
speccy-core/src/prompt/budget.rs:17
speccy-core/tests/prompt_budget.rs:11
speccy-cli/src/implement.rs:4
speccy-cli/src/report.rs:4
speccy-cli/src/review.rs:4
speccy-cli/src/plan.rs:4
speccy-cli/src/tasks.rs:4
(51 total occurrences across 8 files)

$ test -d resources/modules/prompts && echo "EXISTS"
EXISTS
</red>

<green exit="0">
$ cargo run --quiet -- --help
Deterministic feedback engine for spec-driven development.

Usage: speccy.exe <COMMAND>

Commands:
  init    Scaffold a `.speccy/` workspace and copy the host skill pack
  status  Print workspace overview (text by default; `--json` for envelope)
  next    Pick the next actionable task across the workspace
  check   Run command-form proofs from spec.toml
  verify  CI gate: lint + check execution with a binary exit code
  help    Print this message or the help of the given subcommand(s)

$ rg --type rust 'trim_to_budget|TrimResult|DEFAULT_BUDGET'
(no matches)

$ test -d resources/modules/prompts && echo "EXISTS" || echo "ABSENT"
ABSENT

$ cargo test --workspace --no-fail-fast
(all suites pass)
</green>
