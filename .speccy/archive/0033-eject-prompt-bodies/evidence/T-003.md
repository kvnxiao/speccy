# Evidence: SPEC-0033 T-003

## Red phase (before implementation)

```
$ cargo test --test vacancy
running 7 tests
test vacancy_outside_workspace_stdout_is_empty ... ok
test vacancy_appears_in_help_subcommands ... FAILED
test vacancy_empty_specs_dir_returns_spec_0001 ... FAILED
test vacancy_json_simple_workspace ... FAILED
test vacancy_json_with_flat_and_mission_specs ... FAILED
test vacancy_outside_workspace_exits_one_with_not_found_message ... FAILED
test vacancy_text_output_is_bare_spec_id ... FAILED

test result: FAILED. 1 passed; 6 failed
```

`speccy --help` before implementation — `vacancy` absent:
```
Commands:
  init    Scaffold a `.speccy/` workspace and copy the host skill pack
  status  Print workspace overview (text by default; `--json` for envelope)
  next    Pick the next actionable task across the workspace
  check   Run command-form proofs from spec.toml
  verify  CI gate: lint + check execution with a binary exit code
  lock    Record SPEC.md content hash and UTC timestamp into TASKS.md frontmatter
  help    Print this message or the help of the given subcommand(s)
```

`cargo run --quiet -- vacancy` returns `unrecognized subcommand 'vacancy'` with exit 2.

## Green phase (after implementation)

```
$ cargo test --test vacancy
running 7 tests
test vacancy_appears_in_help_subcommands ... ok
test vacancy_outside_workspace_stdout_is_empty ... ok
test vacancy_outside_workspace_exits_one_with_not_found_message ... ok
test vacancy_empty_specs_dir_returns_spec_0001 ... ok
test vacancy_text_output_is_bare_spec_id ... ok
test vacancy_json_simple_workspace ... ok
test vacancy_json_with_flat_and_mission_specs ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.16s
```

`speccy --help` after implementation — `vacancy` present alongside `lock`:
```
Commands:
  init     Scaffold a `.speccy/` workspace and copy the host skill pack
  status   Print workspace overview (text by default; `--json` for envelope)
  next     Pick the next actionable task across the workspace
  check    Run command-form proofs from spec.toml
  verify   CI gate: lint + check execution with a binary exit code
  lock     Record SPEC.md content hash and UTC timestamp into TASKS.md frontmatter
  vacancy  Report the next free SPEC-NNNN identifier
  help     Print this message or the help of the given subcommand(s)
```

Dogfooding against the live workspace (highest spec is SPEC-0034):
```
$ cargo run --quiet -- vacancy
SPEC-0035

$ cargo run --quiet -- vacancy --json
{"schema_version":1,"next_spec_id":"SPEC-0035"}
```

Hygiene gates:

| Command                                                                | Status        |
|------------------------------------------------------------------------|---------------|
| `cargo test --workspace`                                               | pass (exit 0) |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | pass (exit 0) |
| `cargo +nightly fmt --all --check`                                     | pass (exit 0) |
| `cargo deny check`                                                     | pass (exit 0) |
