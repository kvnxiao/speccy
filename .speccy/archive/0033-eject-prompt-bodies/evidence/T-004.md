# Evidence: T-004 — Drop `--kind` from `speccy next`; implement derived action-kind logic

## Red phase (pre-implementation)

```
$ cargo test --test next_derived
...
failures:
    chk007_per_spec_json_implement_after_review_done
    chk007_per_spec_json_in_review_priority
    chk008_workspace_text_decompose_when_no_tasks_md
    kind_flag_is_rejected
    per_spec_json_decompose_when_no_tasks_md
    per_spec_json_null_when_all_done_and_report_present
    per_spec_json_ship_when_all_done_no_report
    workspace_json_active_specs_listed

test result: FAILED. 3 passed; 8 failed; 0 ignored; 0 measured; 0 filtered out
```

Red: 8 failures on new tests covering the T-004 task scenarios. Root cause: `speccy next` still had `--kind` and no `SPEC-ID` positional; workspace form used the old tagged-union JSON shape.

## Green phase (post-implementation)

### `--kind` flag rejected by clap

```
$ cargo run --quiet -- next --kind implement
error: unexpected argument '--kind' found
  tip: to pass '--kind' as a value, use '-- --kind'
Usage: speccy.exe next [OPTIONS] [SPEC-ID]
```

### Per-spec JSON with in-review task (CHK-007)

```
$ cargo run --quiet -- next SPEC-0033 --json
{"schema_version":2,"spec_id":"SPEC-0033","next_action":{"kind":"implement","task_id":"T-005"}}
```

### Workspace text form with decompose (CHK-008)

```
$ cargo run --quiet -- next
SPEC-0033: implement T-005
SPEC-0034: decompose
```

SPEC-0034 has SPEC.md but no TASKS.md → `decompose`. SPEC-0033 has pending tasks → `implement T-005` (T-004 is in-progress).

### All hygiene gates

```
cargo test --workspace               → all suites pass (0 failed)
cargo clippy ... -- -D warnings      → pass (exit 0)
cargo +nightly fmt --all --check     → pass (exit 0)
cargo deny check                     → advisories ok, bans ok, licenses ok, sources ok
```

### New tests: 11/11 green

```
$ cargo test --test next_derived
running 11 tests
test kind_flag_is_rejected ... ok
test next_appears_in_help_subcommands ... ok
test per_spec_unknown_spec_id_exits_one ... ok
test per_spec_json_decompose_when_no_tasks_md ... ok
test chk008_workspace_text_decompose_when_no_tasks_md ... ok
test chk007_per_spec_json_implement_after_review_done ... ok
test chk007_per_spec_json_in_review_priority ... ok
test per_spec_json_ship_when_all_done_no_report ... ok
test per_spec_json_null_when_all_done_and_report_present ... ok
test workspace_json_active_specs_listed ... ok
test workspace_text_completed_spec_omitted ... ok

test result: ok. 11 passed; 0 failed
```
