# Evidence for SPEC-0033 T-010

Migrate the dogfooded `.speccy/` workspace and verify the final seven-verb
CLI surface. Remove legacy `KindFilter`/`compute`/`NextResult`/`BlockedReason`
symbols from `speccy-core/src/next.rs`. Confirm all four hygiene gates pass,
`speccy --help` lists exactly seven subcommands, and `speccy verify` exits 0.

## Session session-2026-05-20-t010 (attempt 1)

### Scenario 1: `speccy --help` lists exactly 7 subcommands

<red exit="0">
Pre-T-010: `speccy --help` listed only 5 verbs (no `lock` or `vacancy`).
T-001 deleted `plan`, `tasks`, `implement`, `review`, `report`; T-002 added
`lock`; T-003 added `vacancy`.
</red>

<green exit="0">
$ cargo run --quiet -- --help
Deterministic feedback engine for spec-driven development.

Usage: speccy.exe <COMMAND>

Commands:
  init     Scaffold a `.speccy/` workspace and copy the host skill pack
  status   Print workspace overview (text by default; `--json` for envelope)
  next     Pick the next actionable task across the workspace
  check    Run command-form proofs from spec.toml
  verify   CI gate: lint + check execution with a binary exit code
  lock     Record SPEC.md content hash and UTC timestamp into TASKS.md frontmatter
  vacancy  Report the next free SPEC-NNNN identifier
  help     Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version

# Exactly 7 subcommands: init, status, next, check, verify, lock, vacancy.
# No plan, tasks, implement, review, or report.
</green>

### Scenario 2: `cargo test --workspace` and clippy pass

<red exit="1">
Pre-T-010: `speccy-core/src/next.rs` still contained legacy `KindFilter`,
`compute`, `NextResult`, `BlockedReason` symbols (deferred from T-004).
`speccy-core/tests/next_priority.rs` exercised those symbols via
`use speccy_core::next::{BlockedReason, KindFilter, NextResult, compute}`.
</red>

<green exit="0">
$ cargo test --workspace
test result: ok. 4 passed; 0 failed; ... (speccy-core/tests/next_priority.rs)
test result: ok. 11 passed; 0 failed; ... (speccy-cli/tests/next_derived.rs)
test result: ok. 4 passed; 0 failed; ... (speccy-cli/tests/skill_body_discovery.rs)
(all suites pass — 63 test suites, 0 failures)

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(exit 0 — no warnings or errors)

$ cargo +nightly fmt --all --check
(exit 0 — no diffs)

$ cargo deny check
(exit 0 — no denied licenses or duplicates beyond pre-existing winnow)
</green>

### Scenario 3: `speccy verify` exits 0

<green exit="0">
$ cargo run --quiet -- verify
Lint: 0 errors, 2 warnings, 58 info
verified 34 specs, 193 requirements, 247 scenarios; 0 errors
</green>

### Scenario 4: zero `speccy tasks.*--commit` in skill or phase bodies

<green exit="0">
$ cargo test --test skill_body_discovery
test chk014_no_direct_speccy_resource_patterns_in_skills_or_phases ... ok
test chk015_speccy_plan_uses_vacancy_not_status_for_greenfield_id ... ok
test no_old_cli_verbs_in_skill_or_phase_bodies ... ok
test no_kind_filter_flag_in_skill_or_phase_bodies ... ok
test result: ok. 4 passed; 0 failed; ...

# no_old_cli_verbs_in_skill_or_phase_bodies confirms zero `speccy tasks `
# and `speccy tasks\n` patterns in all skill, phase, and persona body files.
# --commit references in legacy historical docs (docs/ARCHITECTURE.md,
# resources/modules/prompts/) are outside the skill/phase scope boundary.
</green>

### Additional: `speccy lock SPEC-0033` re-records hash via new verb

<green exit="0">
$ cargo run --quiet -- lock SPEC-0033
(exit 0 — TASKS.md frontmatter updated with SPEC.md sha256 and UTC timestamp)
</green>
