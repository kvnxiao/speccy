---
spec: SPEC-0052
task: T-004
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/low" round="1">
Completed: Added the shared identity-sourcing partial
`resources/modules/references/identity-sourcing.md` stating the
two-part rule — model segment from the host's in-context resolved
identifier transcribed verbatim with hyphen-form punctuation
preserved (definition-file `model:` fallback where the host states
none); effort suffix from the agent's own definition file
(`effort:` / `model_reasoning_effort`) and never from `CLAUDE_EFFORT`
or other inherited env — plus the documented `CLAUDE_CODE_EFFORT_LEVEL`
runtime-override limitation. Wrote it language- and project-agnostic
for downstream `speccy init`. Wired `{% include %}` of the partial at
five sites: the implementer phase (`phases/speccy-work.md`), the
reviewer verdict-return contract (`personas/verdict_return_contract.md`,
also trimming its now-redundant inline effort-suffix examples), and
the three vet personas (`vet-implementer.md`, `vet-reviewer.md`,
`vet-simplifier.md`). `just reeject` propagated the rule to `.claude/`,
`.agents/`, and `.codex/` (reviewer personas pick it up transitively
via the verdict contract include).

Undone: Nothing in scope. The pre-existing dot-form `claude-opus-4.8`
example strings in the implementer `<implementer>` attribute prose and
in `journal-implementer.md` are REQ-008 / T-005 scope, not touched here.

Hygiene checks:
- cargo test --workspace: exit 0
- cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
- cargo +nightly fmt --all --check: exit 0
- cargo deny check: exit 0

Evidence: no separate evidence file — all three covered REQs are
judgment-only (prose presence on the diff) or hygiene (render + tests).
Roll call for CHKs under REQ-005, REQ-006, REQ-007:
- CHK-006 (sub-agent records definition-file `/low` effort, not parent's):
  judgment-only / dogfooding → the partial forbids inherited-env effort
  and mandates definition-file effort; confirmed live this run — the
  speccy-work definition pins `effort: low`, so this block records
  `model="claude-opus-4-8[1m]/low"`.
- CHK-007 (model segment matches host in-context id, hyphens preserved):
  judgment-only / dogfooding → host stated `claude-opus-4-8[1m]`;
  this block transcribes it verbatim in hyphen form.
- CHK-008 (implementer phase, verdict contract, vet personas each carry
  the rule from the one partial, incl. override limitation): judgment-only
  → confirmed on the diff; `Sourcing your recorded identity` text ejected
  to all three Claude Code targets (and Codex) sourced from the single
  partial, override-limitation bullet included.
- CHK-009 (`just reeject` exits 0, partial resolves at every include site):
  hygiene → reeject exit 0 with no MiniJinja render error.

Discovered issues: None.

Procedural compliance: Followed the speccy-work first-attempt recipe
(clean tree confirmed, state pending→in-progress→in-review, journal
appended). Edited only source under `resources/`; never touched ejected
packs directly; ran `just reeject` to regenerate. Ran the self-review /
convention-checklist pass before handoff — the verdict-contract edit
deletes the inline examples now subsumed by the partial to avoid a
duplicate-shadow of the canonical source.
</implementer>

<review date="2026-05-29T00:19:35Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-004 delivers exactly what REQ-005, REQ-006, and REQ-007 promise. The new shared partial `resources/modules/references/identity-sourcing.md` states both sourcing rules (model = host-stated in-context long-form id transcribed verbatim with hyphen punctuation preserved and an explicit dot-form prohibition, with a definition-file `model:` fallback; effort = the agent's own definition file `effort:`/`model_reasoning_effort`, explicitly forbidding `CLAUDE_EFFORT`/inherited env) plus the documented `CLAUDE_CODE_EFFORT_LEVEL` override limitation (DEC-004). It is included at all REQ-007 done-when sites: the implementer phase, the verdict-return contract, and the three vet personas — and the six reviewer personas pick it up transitively via the verdict-contract include (verified in ejected `.claude/agents/*` and `.codex/agents/*`), so the rule is single-sourced with no restated copy. Non-goals hold: zero Rust changes, no out-of-scope persona-body edits, override deliberately not read. CHK-006/CHK-007 are dogfooded live — this task's journal block records `model="claude-opus-4-8[1m]/low"` (correct hyphen form, definition-file `/low` effort under a higher-effort orchestrator).
Non-blocking aside: the implementer's "Undone" note routes the dot-form strings in `<implementer>` attribute prose and `journal-implementer.md` to T-005, but T-005/REQ-008 only names `review-fanout.md` and `speccy-decompose.md` — those other occurrences may sit outside both tasks' explicit file lists. Worth the T-005 reviewer / vet pass confirming coverage; not a defect in T-004.
</review>

<review date="2026-05-29T00:19:35Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-004 (REQ-005/006/007) is prose-only and correctly carries no tests per DEC-005 and the project's anti-vacuous-test rule; a prose-substring test would be exactly the vacuous test the repo forbids. Evidence roll call is complete: every CHK under the covered REQs (CHK-006, -007, -008, -009) appears with a plausible label. CHK-006/-007 are correctly judgment-only/dogfooding and demonstrated live by this journal block: the `speccy-work` definition pins `effort: low` and the block records `model="claude-opus-4-8[1m]/low"` — definition-file effort not the parent's, hyphen-form model id verbatim. CHK-008 verified independently: grepped the identity-sourcing rule text and `CLAUDE_CODE_EFFORT_LEVEL` across all ejected `.claude/agents/*` (implementer phase, verdict contract → reviewers transitively, the three vet personas) with zero unrendered `{% include %}` directives surviving. CHK-009 (hygiene) cites the reeject + workspace-test gate, the SPEC's sole designated deterministic proof shape; re-ran `cargo test --workspace` → exit 0. No fabrication pattern: no invented test names, no fake red/green pair.
</review>

<review date="2026-05-29T00:19:35Z" model="claude-opus-4-8[1m]/low" persona="security" verdict="pass" round="1">
Prose/template-only change (shared identity-sourcing partial + five `{% include %}` wirings). No code, no auth/input/crypto/secret surface. The new partial `resources/modules/references/identity-sourcing.md` embeds no secrets or credentials; it names `CLAUDE_EFFORT` / `CLAUDE_CODE_EFFORT_LEVEL` only to forbid sourcing self-identity from inherited/override env, which is a mild hardening. Example model id `claude-opus-4-8[1m]` is a generic placeholder, not a Speccy-repo identifier. `just reeject` exits 0 and is idempotent (CHK-009 satisfied); the rule is present in all ejected reviewer agents. No security blockers.
</review>

<review date="2026-05-29T00:19:35Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All T-004 source changes (`identity-sourcing.md` new partial, five include wires, inline example removal in `verdict_return_contract.md`, `vet-implementer.md`, `vet-reviewer.md`) are stylistically clean. The single dot-form `claude-opus-4.8` in `identity-sourcing.md` is intentionally the negative example in a "never normalise it to a dotted form" clause — not a violation. Include syntax matches the project's existing template pattern, heading level is consistent with sibling reference partials, and no dead code, unused variables, or suppression annotations were introduced.
</review>
