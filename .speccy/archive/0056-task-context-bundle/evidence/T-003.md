# Evidence for SPEC-0056 T-003

Red-then-green paper trail for extending the `speccy context` bundle
with the selected task's verbatim `<task>` entry and the covering
requirements resolved through the shared core walk (REQ-003). Tests
live in `speccy-cli/tests/context.rs`. "Red" is established by the
absence of the `task` and `requirements` fields on the `ContextBundle`
envelope before this task — T-002 left the envelope with only
`schema_version`, `spec`, and `intent`, so the assertions below were
unreachable (the JSON carried no `task` or `requirements` keys, and
the new test fixtures `spec_md_five_requirements` /
`tasks_md_covering` did not exist).

## Session 2026-06-10-T003-attempt-1

### Scenario 1 — Two covered requirements present in full; other three absent (CHK-004)

<red>
At HEAD before this task, `ContextBundle` had no `requirements`
field. `serde_json::Value::get("requirements")` returned `None`, so
`bundle_carries_only_the_two_covered_requirements_with_full_content`
panicked at `.expect("bundle has requirements array")`. There was no
projection of covering requirements at all.
</red>

<green>
`bundle_carries_only_the_two_covered_requirements_with_full_content`
builds a five-requirement SPEC.md whose every requirement carries
distinct markers (`REQ{n:03}_BODY_MARKER`, `REQ{n:03}_DONEWHEN_MARKER`,
`REQ{n:03}_BEHAVIOR_MARKER`, `REQ{n:03}_SCENARIO_MARKER`,
`CHK-{n:03}`), with a task covering `REQ-001 REQ-003`. The test
asserts:

- `requirements` is exactly `[REQ-001, REQ-003]` in covers-list order.
- Each covered requirement carries its `done_when`, `behavior`, and
  one `scenarios` entry whose id and body match the marker for that
  requirement.
- None of `REQ-002 / REQ-004 / REQ-005`, their body markers, or their
  `CHK-002 / CHK-004 / CHK-005` ids appear anywhere in the raw
  payload.

The walk is `speccy_core::context::resolve_covering_requirements`
(landed in T-001), so `context` and `check` resolve covering
requirements through one function (REQ-003 / DEC-002). Test command:
`cargo test -p speccy-cli --test context`.
</green>

### Scenario 2 — Raw task body bytes alongside parsed id/state/covers (REQ-003 behavior)

<red>
Before this task, `ContextBundle` had no `task` field;
`serde_json::Value::get("task")` returned `None`. The task's raw
`<task>` body bytes were nowhere in the envelope.
</red>

<green>
`bundle_carries_task_entry_with_raw_body_and_parsed_fields` asserts
the `task` entry carries `id == "T-001"`, `state == "pending"`,
`covers == [REQ-001, REQ-003]` (source order), and a `body` containing
the `TASK_BODY_MARKER` planted in the TASKS.md `<task>` body — proving
the verbatim body bytes (`Task.body`) ride alongside the parsed
fields.

```
$ speccy context SPEC-0042/T-001 --json
{"schema_version":1,"spec":{...},"intent":{...},"task":{"id":"T-001","state":"pending","covers":["REQ-001","REQ-003"],"body":"...TASK_BODY_MARKER..."},"requirements":[{"id":"REQ-001",...},{"id":"REQ-003",...}]}
(exit 0)
```
</green>

### CHK-005 — existing `speccy check` integration tests pass unchanged

<red>
Not applicable in the red sense — CHK-005 is a byte-stability
invariant owned by T-001's refactor. This task adds a second consumer
of `resolve_covering_requirements` (`speccy context`) without touching
`check::run_task` or any check fixture/expectation.
</red>

<green>
The `speccy-cli/tests/check*.rs` suites run unchanged under
`cargo test --workspace` and pass, confirming `speccy check`'s text
output remains byte-identical across this task (no `check.rs` or check
fixture was edited).
</green>

## Hygiene-gate evidence

The four standard gates all pass on the final tree (one line per
gate; full transcript captured in the `<implementer>` journal block
`Hygiene checks` field):

- `cargo test --workspace` — exit 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0
- `cargo +nightly fmt --all --check` — exit 0
- `cargo deny check` — exit 0
