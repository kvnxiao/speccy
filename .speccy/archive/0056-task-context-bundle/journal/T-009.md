---
spec: SPEC-0056
task: T-009
generated_at: 2026-06-10T23:22:07Z
---

<implementer date="2026-06-10T23:22:07Z" model="claude-opus-4-8[1m]/low" round="1">
- Reuse survey: docs-only task (ARCHITECTURE.md prose); no new top-level code symbols introduced, so the per-symbol floor is N/A. Reused the file's two existing structures rather than inventing new ones: (1) the aligned-column CLI surface table in the `text` code fence (matched `speccy check`/`speccy verify` column layout and the existing `--json: schema_version=1 envelope` phrasing); (2) the Phase 3 / Phase 4 bullet-list prose style. The size-invariant and envelope-section wording was lifted from the SPEC's REQ-002..REQ-007 done-when clauses and DEC-001/004/005 to keep the doc and the spec aligned.
- Completed: REQ-009 both done-when items. (1) Added the `speccy context TASK-SELECTOR` entry to the CLI surface table (after `speccy check`, both being read commands) documenting selector grammar, `--json` schema_version=1 contract, all nine envelope sections (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency), and the REQ-007 size invariant stated as a contract. (2) Updated Phase 3 (implementer) and Phase 4 (reviewer fan-out) prose to describe the single `speccy context --json` entry read replacing the former full-SPEC + full-TASKS + journal + `speccy check` recipe; noted `speccy next` still precedes `context` on the no-selector path, the retry-shape rule reads its journal from the bundle, `reviewer-tests`'s `speccy check` exit-code caveat is unrelated to the entry read, and vet personas keep their full reads.
- Undone: nothing for REQ-009.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0. (No Rust changed; docs-only edit.)
- Evidence: paper trail is the diff to `docs/ARCHITECTURE.md` itself; this is a documentation task whose sole CHK is a reviewer content check, not a scriptable proof. Roll call for CHKs under REQ-009:
  - CHK-013 (ARCHITECTURE.md documents `speccy context`, its envelope, and the size invariant; no stale full-file entry-read contract remains): judgment-only → no scriptable proof by design (the scenario itself says "content check by reviewer, not substring assertions"; AGENTS.md forbids substring-matching curated prose). reviewer-business / reviewer-style judge the diff: CLI surface table now carries the command + envelope + invariant; Phase 3/4 prose now describe the bundle entry read; a grep for "full SPEC.md / full TASKS.md" in the file returns only the new prose that explicitly frames those reads as the superseded recipe.
- Discovered issues: none. The three other ARCHITECTURE.md mentions of personas reading specific elements (Changelog at ~1189, Decisions blocks at ~1707, the reviewer prompt at ~1824) are element-scoped reads, not full-file entry-read contracts, so they are correct as-is and out of scope for REQ-009.
- Procedural compliance: followed speccy-work first-attempt branch (clean tree, no prior journal). No resources/ skill-module friction encountered, so no source-module edit or reeject was needed. No Speccy-repo identifiers were introduced into shipped templates (this edit is to docs/ARCHITECTURE.md, not a shipped resources/ body).
</implementer>
<review date="2026-06-10T23:23:56Z" model="claude-opus-4-8[1m]/low" persona="security" verdict="pass" round="1">
Pass. Docs-only edit to `docs/ARCHITECTURE.md` documenting `speccy context`; no security regression. Reviewed the new CLI-surface entry (lines ~174-223) and the Phase 3/4 prose updates against the as-built command (T-002..T-008 landed). Findings: (1) `speccy context` is documented as a pure read command ("performs no writes anywhere") — a positive no-side-effect property for a context fetch. (2) The envelope describes a least-disclosure posture: `intent` explicitly excludes summary/user-stories/non-covered requirement bodies, and `siblings` is "id, state, covers only — never bodies", so the bundle does not over-share. The inlined `journal`, `task`, and `requirements` payloads are project spec artifacts, not secrets/credentials, so no sensitive-data-exposure concern. (3) The only externally-runnable string, `diff_command`, is documented as a fixed `git diff <base>...HEAD` template against a git-`symbolic-ref`-derived ref with degrade-not-error semantics (verified in `speccy-cli/src/git.rs:68-108`); base is a git ref name emitted for the agent to run, not shell-interpolated by the CLI — no injection vector introduced or mis-documented. (4) No auth/authz boundary, no network surface, no crypto, no rate-limit-relevant endpoint is implicated by this local read command. `docs/ARCHITECTURE.md` is internal design prose (not a shipped `resources/` body), so the AGENTS.md no-real-identifiers-in-templates rule does not bite here, and the entry uses structural descriptions rather than concrete leaked IDs.
</review>
<review date="2026-06-10T23:23:59Z" model="claude-opus-4-8[1m]/low" persona="business" verdict="pass" round="1">
REQ-009 both done-when items satisfied. Done-when 1: the CLI surface table now lists `speccy context TASK-SELECTOR` (`docs/ARCHITECTURE.md:174-224`) with selector grammar, schema_version=1 contract, all nine envelope sections REQ-009 names (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency), and the REQ-007 size invariant stated explicitly as a contract. Done-when 2: Phase 3 (`:525-533`) and Phase 4 (`:579-588`) now open the per-task read with `speccy context --json`; the former `speccy check`-as-entry bullet is gone. The three surviving "full SPEC.md/TASKS.md" mentions are not stale entry-read contracts: `:530`/`:581` frame them as the superseded recipe, `:1187` is Changelog-scoped reviewer prose, and `:484` is the decompose phase (whole-SPEC read, out of REQ-008 scope by the same logic as the vet carve-out). Non-goals respected — the entry actively documents the absence of a bare-spec form and content-mode flags and asserts `--json` toggles representation not content; no CLI behavior changed (docs-only). Goals/user-stories served (one-call entry read, size invariant, sibling reuse index, drift surfacing). TASKS.md change is the lone pending->in-review flip with no journal elements in the task body.
</review>
<review date="2026-06-10T23:24:16Z" model="claude-opus-4-8[1m]/low" persona="tests" verdict="pass" round="1">
Pass. Docs-only edit to `docs/ARCHITECTURE.md`; CHK-013 (the sole CHK under REQ-009) is correctly labelled `judgment-only` in the evidence roll call and that label is warranted — the scenario text itself says "content check by reviewer, not substring assertions" and AGENTS.md forbids substring-asserting curated prose, so no deterministic command can prove this scenario. The absence of an executable test is appropriate; adding one would be vacuous prose-matching.

Both done-when clauses are met by the working-tree diff. (1) CLI surface table (`docs/ARCHITECTURE.md:174-224`) now lists `speccy context TASK-SELECTOR` with selector grammar, all nine envelope sections (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency), the `schema_version=1` contract, and a "Size invariant (contract, not implementation detail)" paragraph — matching the REQ-009 enumeration. (2) No stale full-file persona entry-read contract remains: a file-wide search for "full SPEC.md / TASKS.md" returns only the two new passages (`:530`, `:581`) that explicitly frame those reads as the *superseded* recipe, plus one unrelated structural mention at `:1556`; the old Phase 3 "uses `speccy check` only to render scenarios" bullet is replaced by the bundle entry read, and Phase 4 correctly preserves the `reviewer-tests` exit-code caveat and the vet-persona whole-SPEC exclusion.

Verified the new prose makes no fabricated implementation claim: `task_lookup::parse_ref` and `report_lookup_error` exist; `schema_version` is 1 and serialized first; all nine sections appear in `speccy-cli/src/context_output.rs` and the suite at `speccy-cli/tests/context.rs`; and the doc's "Enforced by a property-style test" claim is backed by `bundle_size_scales_with_task_not_spec` (`speccy-cli/tests/context.rs:1397`), which exercises exactly the three growth cases the doc enumerates (one sibling adds one entry; uncovered requirement adds nothing; foreign-task journal round adds nothing). Evidence field present; roll call complete for REQ-009's single CHK; no red/green-fabrication surface for a docs CHK.
</review>
<review date="2026-06-10T23:24:21Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="1">
Pass. Verified each documented `speccy context` contract claim in docs/ARCHITECTURE.md against the T-001..T-008 implementation; no documented claim contradicts the code. Selector grammar ("same as `speccy check`, via `task_lookup::parse_ref` then `find`", shared `report_lookup_error`) matches speccy-cli/src/context.rs:143,150 and main.rs:694. `--json` schema_version=1-as-first-field, no bare-spec form, no content-mode flag: matches context.rs:205 and ContextArgs (only `selector`+`json`); guarded by the test at tests/context.rs:426. The nine envelope sections (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency) are exactly the nine ContextBundle fields in context_output.rs:36-69. Intent exclusions (Summary/user-stories/notes/non-covered reqs) hold — build_intent reads only goals/non_goals/decisions (context.rs:372). Journal absent → explicit `exists:false` marker with exit 0 (build_journal NotFound arm, context.rs:301). diff_command is merge-base triple-dot against the default branch, degrading to a `main` baseline and never erroring (git.rs:68-108). consistency surfaces workspace status with drifts filtered to the selected task and never refuses on drift (build_consistency, context.rs:227-243). The size invariant is enforced by the property-style test `bundle_size_scales_with_task_not_spec` (tests/context.rs:1398), which asserts the exact bounded-growth claims (one sibling = one entry; foreign journal round / uncovered requirement add nothing). Selector failures exit non-zero before any stdout write (run resolves the selector at context.rs:143,150 before out.write_all). No correctness defects found.
</review>
<review date="2026-06-10T23:25:18Z" model="claude-sonnet-4-6[1m]/low" persona="style" verdict="blocking" round="1">
Blocking: `--json:` flag formatting in the CLI surface table breaks column alignment.

All other `--json:` lines in the `text` code-fence table use 14 spaces between
the colon and the value to hit the shared value column (visible in `speccy status`,
`speccy next`, `speccy verify`):

    --json:              schema_version=1 envelope

The new `speccy context` entry uses only 1 space after the colon:

    --json: schema_version=1 envelope (first field).

This breaks the column-alignment convention established by every neighbouring
command. The continuation lines at indent=44 are also misaligned: they align
under `--json: ` (8 chars past the 36-space indent) instead of under the value
column at 57 spaces (36 + 6 + 1 + 14).

Fix: change `--json: schema_version=1 envelope (first field).` to
`--json:              schema_version=1 envelope (first field).` (14 spaces
after the colon), and adjust the three continuation lines at 44 spaces to the
value column at 57 spaces. The multi-sentence expansion can stay as continuation
prose; it just needs to be aligned with the value column rather than with the
literal `--json: ` prefix.

All other aspects of the diff pass:
- The `speccy context` command line itself aligns correctly at column 34.
- Continuation prose at 34-space indent matches `speccy next` convention.
- Envelope section key-value pairs at 36-space indent match flag sub-items.
- Phase 3 / Phase 4 bullet-list changes are well-integrated with existing prose style.
- No dead code, no reinvented helpers, no stale comments.
- Reuse survey is present and complete; named paths/helpers verified in the codebase.
</review>
<review date="2026-06-10T23:26:01Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="1">
Pass with one observation.

(1) CLI surface table (docs/ARCHITECTURE.md:174-224): `speccy context TASK-SELECTOR` entry is accurate and complete. Selector grammar matches the shared `task_lookup::parse_ref` + `find` path. Nine envelope sections are documented (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency) with correct descriptions. `--json` semantics ("toggles representation, never content") and no bare-spec form are correct. Size invariant stated as contract with property-test enforcement noted. Internal consistency with `speccy check` grammar description and `speccy verify`/`speccy lock` column layout: confirmed.

(2) Phase 3 prose (docs/ARCHITECTURE.md:525-543): new `speccy context SPEC-NNNN/T-NNN --json` bullet correctly lists bundle contents (task entry, covering requirements+scenarios, full journal, sibling index, suggested diff command), names the retry-shape rule as reading journal from the bundle, and frames the old recipe as superseded. `speccy next` still precedes `context` on the no-selector path — correctly noted. Removed bullet was "uses `speccy check SPEC-NNNN/T-NNN` only to render the scenarios it is satisfying" (was ARCHITECTURE.md:476 in pre-diff HEAD, the reference T-008 flagged): confirmed gone.

(3) Phase 4 prose (docs/ARCHITECTURE.md:579-588): reviewer fan-out correctly describes `speccy context` entry call dispatched from the shared spawn prompt; correctly carves out `reviewer-tests` exit-code caveat as unrelated to entry read; correctly excludes vet personas with rationale (whole-SPEC holistic scope). Internally consistent with Phase 3.

(4) T-008 docs reviewer's two flagged items:
- `docs/ARCHITECTURE.md:476` (old "uses `speccy check` only to render scenarios"): addressed — line removed, replaced by the `speccy context` bullet.
- `resources/modules/skills/speccy-work.md:26` (old "read `<spec-dir>/journal/T-NNN.md` (if it exists)"): still present in current working tree; not addressed by T-009. This is outside T-009's explicit scope (task body: "Suggested files: `docs/ARCHITECTURE.md`") and the T-008 reviewer marked it non-blocking with "may" language. The skill-trigger file's redirect semantics (it points agents to the full phase body, which was updated by T-008) reduce the practical impact. No other pending task appears to own this cleanup.

(5) ARCHITECTURE.md "Lifecycle reading" section (~line 840-844): still describes "reads TASKS.md to find the next `state=pending` task, then reads `journal/T-NNN.md` (directly, or via `speccy journal show`)" — the old read recipe. This is schema/lifecycle documentation for the journal file format, not phase-level skills prose for persona entry reads. T-009's mandate targeted "skills layer reads" prose around lines 348-497; this section is in the per-task journal schema sub-section and is more abstractly describing how the journal system is consumed. Not a blocking omission for T-009's scope.

CHK-013: ARCHITECTURE.md documents `speccy context` with envelope and size invariant; no stale full-file entry-read contract remains in the skills-layer prose sections — confirmed.
</review>
<blockers date="2026-06-10T23:27:05Z" round="1">
Fix the `--json:` column alignment in the `speccy context` CLI surface table entry (single style blocker).

- style (blocking): in `docs/ARCHITECTURE.md` (line ~184), the `speccy context` entry's `--json:` flag uses only 1 space after the colon, breaking the 14-space value-column alignment every neighbouring command uses (`speccy status`, `speccy next`, `speccy verify` all render `--json:` + 14 spaces + value). Fix: change `--json: schema_version=1 envelope (first field).` to `--json:` followed by 14 spaces then `schema_version=1 envelope (first field).` (value column at 57 spaces = 36 indent + 6 + 1 + 14). Also re-align the three continuation lines currently at 44-space indent to the 57-space value column so they sit under the value, not under the literal `--json: ` prefix. Prose content unchanged; whitespace-only realignment.
</blockers>
<implementer date="2026-06-10T23:29:31Z" model="claude-opus-4-8[1m]/low" round="2">
- Reuse survey: retry round addressing a single non-reuse style blocker (whitespace column alignment); the full area-map is not re-run per the round-1-only rule, and no new top-level symbol was introduced, so the per-symbol floor is N/A. The fix reuses the file's own established `--json:` value-column convention (col 57 = indent 36 + `--json:` 7 chars + 14 spaces), matched verbatim from the `speccy verify` entry's `--json:` line at line ~227.
- Completed: addressed the round-1 style blocker by realigning the `speccy context` entry's `--json:` flag in the CLI surface table (docs/ARCHITECTURE.md:184). Changed the 1-space gap after `--json:` to 14 spaces so the value lands in the shared 57-column value column, and re-indented the three continuation lines (185-187) from indent 44 to indent 57 so they sit under the value rather than under the literal `--json: ` prefix. Whitespace-only; prose bytes unchanged.
- Undone: nothing for REQ-009.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0. (Docs-only edit; no Rust changed.)
- Evidence: paper trail is the diff to `docs/ARCHITECTURE.md` itself. Verified post-fix that line 184 and the reference `speccy verify` line 227 both place their value at column 57, and continuation lines 185-187 are at indent 57. Roll call for CHKs under REQ-009:
  - CHK-013 (ARCHITECTURE.md documents `speccy context`, its envelope, and the size invariant; no stale full-file entry-read contract remains): judgment-only -> no scriptable proof by design (the scenario says "content check by reviewer, not substring assertions"; AGENTS.md forbids substring-matching curated prose). The five round-1 content reviewers (business, tests, security, correctness, docs) already passed on content; this round only realigns whitespace flagged by reviewer-style, leaving the content intact.
- Discovered issues: none.
- Procedural compliance: followed speccy-work retry branch — amended the prior pass's uncommitted WIP in place (no git restore/clean/checkout), did not rewrite the touched lines from scratch, flipped state via `speccy task transition` (never edited TASKS.md directly). No resources/ skill-module friction encountered, so no source-module edit or reeject. No Speccy-repo identifiers introduced into shipped templates (edit is to docs/ARCHITECTURE.md, internal design prose).
</implementer>
<review date="2026-06-10T23:30:52Z" model="claude-opus-4-8[1m]/low" persona="security" verdict="pass" round="2">
Pass. Round-2 change is whitespace-only column realignment of the `--json:` flag in the `speccy context` CLI-surface entry of `docs/ARCHITECTURE.md` (line ~184 plus continuation lines 185-187); no prose bytes, no code, no behavior changed (`git diff` confined to `docs/ARCHITECTURE.md` documentation prose; no Rust touched). Carries zero security surface and does not disturb the round-1 security findings, which I re-confirm against the as-built command: `speccy context` is documented as a pure read ("performs no writes anywhere"); the envelope keeps a least-disclosure posture (`intent` excludes Summary/user-stories/non-covered requirement bodies; `siblings` is id/state/covers only, never bodies; inlined journal/task/requirements are project spec artifacts, not secrets); the only runnable string `diff_command` is a documented git-ref merge-base template that degrades-not-errors and is emitted for the agent to run, not shell-interpolated by the CLI — no injection vector introduced or mis-documented. No auth/authz boundary, network surface, crypto, or rate-limit-relevant endpoint is implicated. `docs/ARCHITECTURE.md` is internal design prose, not a shipped `resources/` template, so the AGENTS.md no-real-identifiers rule does not bite.
</review>
<review date="2026-06-10T23:31:16Z" model="claude-opus-4-8[1m]/low" persona="business" verdict="pass" round="2">
REQ-009 intent still satisfied after round-2; no regression. Round 2 amended only the round-1 style blocker (whitespace column alignment of the `speccy context` `--json:` entry); prose bytes unchanged. Verified: done-when 1 holds — the CLI surface table (`docs/ARCHITECTURE.md:174-217`) lists `speccy context TASK-SELECTOR` with selector grammar, schema_version=1 contract, all nine envelope sections REQ-009 names (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency), and the size invariant stated explicitly as a contract. Done-when 2 holds — Phase 3 (`:522-531`) and Phase 4 (`:579-588`) open the per-task read with `speccy context --json`; the only surviving full-SPEC/full-TASKS mentions (`:530`, `:581`) explicitly frame those reads as the superseded recipe, and `:197` ("each covering requirement in full") is envelope wording, not an entry-read contract. The round-2 fix lands the `--json:` value at column 57 (matching the reference `speccy verify` `--json:` at `:227`) and re-aligns continuation lines 185-187 to indent 57 — the exact fix the round-1 blocker directed, content-neutral. Goals/non-goals/user-stories from the prior business pass remain served and unchanged.
</review>
<review date="2026-06-10T23:31:20Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
Pass. Round-2 amended only whitespace column alignment in the `speccy context` CLI surface entry (`docs/ARCHITECTURE.md:184-187`): the `--json:` flag now hits the shared 14-space value column matching `speccy verify` (`:227`), and the three continuation lines are re-indented to the value column. Confirmed the change is whitespace-only — no contract prose byte changed — so every documented claim round-1 correctness verified against the T-001..T-008 implementation still holds. Re-spot-checked the load-bearing claims against the code: `schema_version` is the first serialized field pinned at 1 and the nine envelope sections (identity, intent, task, requirements, journal, siblings, paths, diff_command, consistency) are exactly the `ContextBundle` fields in declared order (`speccy-cli/src/context_output.rs:36-69`); intent exclusions (Summary/user-stories/notes/non-covered reqs) match the `Intent` struct doc + fields (`:91-98`); absent-journal is an explicit empty marker with exit 0 (`:50`). No documented claim contradicts the implementation. No correctness defects on any reachable path.
</review>
<review date="2026-06-10T23:31:32Z" model="claude-sonnet-4-6[1m]/low" persona="docs" verdict="pass" round="2">
Pass. Round-2 amended only whitespace column-alignment in the `speccy context` CLI surface table entry (docs/ARCHITECTURE.md:184-187); documentation content is unchanged from round 1.

Verified the realignment:
- Line 184: `--json:` at indent 36, followed by 14 spaces, value starts at column 57 — matches every neighbouring `--json:` line (lines 154, 159, 227).
- Lines 185-187: continuation lines at indent 57, aligned to the value column — consistent with the surrounding table convention.

Checked via python character-count that the column positions are exact: 36 leading spaces + 7 chars (`--json:`) + 14 spaces = column 57 for the value; the three continuation lines all have exactly 57 leading spaces. No content bytes changed; prose, envelope section list, size invariant, and Phase 3/4 bullet-list integration are identical to round 1, which this persona passed.

CHK-013: ARCHITECTURE.md documents `speccy context` with envelope and size invariant; no stale full-file entry-read contract remains — confirmed (unchanged from round 1 pass).
</review>
<review date="2026-06-10T23:31:38Z" model="claude-sonnet-4-6[1m]/low" persona="style" verdict="pass" round="2">
Pass. The round-1 blocking finding is fully resolved.

`docs/ARCHITECTURE.md:184` — `--json:` now uses 14 spaces after the colon, placing
the value (`schema_version=1 envelope (first field).`) at column 57, matching the
reference line at `docs/ARCHITECTURE.md:227` (`speccy verify` entry, also column 57).

`docs/ARCHITECTURE.md:185-187` — all three continuation lines are at indent 57,
sitting under the value column rather than under the literal `--json: ` prefix.

Verified by byte-level character-position check: value column 57 on line 184 matches
value column 57 on line 227 exactly. Prose content is byte-for-byte identical to the
round-1 content; this is a whitespace-only realignment.

No new style issues introduced. The Phase 3/4 prose additions were already reviewed
and passed in round 1; no changes to those blocks in round 2.
</review>
<review date="2026-06-10T23:31:42Z" model="claude-opus-4-8[1m]/low" persona="tests" verdict="pass" round="2">
Pass. Round-2 retry is whitespace-only and introduces no verification regression. The sole CHK under REQ-009 is CHK-013, correctly labelled judgment-only in the evidence roll call — the scenario text itself says "content check by reviewer, not substring assertions" and AGENTS.md forbids substring-asserting curated prose, so no deterministic test can or should prove this docs scenario; adding one would be vacuous prose-matching. The judgment-only label is plausible and is deferred to reviewer-business / reviewer-style, not mine to block on.

Verified the round-2 fix addresses the round-1 style blocker exactly and changes nothing testable: in `docs/ARCHITECTURE.md`, the `speccy context` `--json:` value (`schema_version`, line 184) now starts at column 57 — byte-identical alignment to the reference `speccy verify` `--json:` line (line 227) — and the three continuation lines (185-187) are re-indented from column 44 to column 57 so they sit under the value column. The prose bytes of the `--json` block are unchanged (still: schema_version=1 envelope first-field, `--json` toggles representation never content, agents always pass it, no bare-spec form, no content-mode flag). No code, test, or scenario-bearing content changed; the five round-1 content reviewers passed on content this round leaves intact. Evidence field present on both implementer rounds; REQ-009's single CHK appears in the roll call. No red/green-fabrication surface exists for a docs CHK whose paper trail is the diff itself.
</review>
