---
spec: SPEC-0056
task: T-003
generated_at: 2026-06-10T21:12:06Z
---

<implementer date="2026-06-10T21:12:06Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Area = `speccy context` command + its output structs + integration tests (covers REQ-003; suggested files context.rs, context_output.rs, tests/context.rs). Reuse-as-is: `speccy_core::context::resolve_covering_requirements` (landed T-001) for the covers→requirements walk — `context` now consumes it so it cannot drift from `check`; `TaskLocation.task` (parsed `Task`) and `Task.body` (verbatim `<task>` body bytes) already on the resolved location; `Requirement`/`Scenario` core structs supply done_when/behavior/scenarios. Extend: `ContextBundle` (added `task` + `requirements` fields), `assemble_bundle` (populate via two new helpers), `render_text` (render the two new sections, since `--json` toggles representation not content). Write-fresh: new `Serialize` structs `TaskEntry`, `CoveringRequirement`, `ScenarioEntry` in context_output.rs — searched context_output.rs; T-002 added only identity/intent structs, no task-entry or requirement projection existed. `build_task_entry` and `build_requirements` new top-level fns in context.rs, modelled on T-002's existing `build_intent`. Deliberately did NOT reach for `JsonJournalBlock` (that is T-004's journal slice).
- Completed: Extended the `speccy context` envelope with the selected task's verbatim `<task>` entry (parsed id/state/covers + raw `Task.body` bytes) and the covering requirements (id, verbatim body, done_when, behavior, scenarios), resolved through the shared `resolve_covering_requirements` core walk so `context` and `check` resolve identically (REQ-003 / DEC-002). Requirements arrive deduplicated in covers-list order; a `covers` token with no matching requirement is skipped by the shared walk exactly as `check` reports. Updated text renderer for content parity. Added two integration tests (CHK-004 + the raw-body behavior) and two fixtures (`spec_md_five_requirements`, `tasks_md_covering`).
- Undone: Nothing in REQ-003 scope. Journal section (T-004), sibling index/paths/diff command (T-005), and consistency section (T-006) remain on the envelope's future-task list per the SPEC's task split — not this task.
- Hygiene checks: `cargo test --workspace` exit 0 (222 + all integration suites green, including unchanged `check` tests — CHK-005); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-003.md`.
  Roll call for CHKs under REQ-003:
  - CHK-004 (five-req spec, task covers two; two appear with done-when/behavior/scenario, other three absent): demonstrated → evidence Scenario 1 covers the red (no `requirements` field) / green (`bundle_carries_only_the_two_covered_requirements_with_full_content` asserts exactly [REQ-001, REQ-003] in full and absence of REQ-002/004/005 ids, bodies, and CHKs from the raw payload). The raw-body behavior pair is demonstrated in evidence Scenario 2 (`bundle_carries_task_entry_with_raw_body_and_parsed_fields`).
  - CHK-005 (existing `check` integration tests pass unchanged): hygiene → the `check*.rs` suites run unmodified under `cargo test --workspace` and pass byte-stable; no `check.rs` or check fixture touched this task (the invariant is owned by T-001's refactor and re-proven here by leaving `check` untouched).
- Discovered issues: None. T-001's shared walk dropped in cleanly as the single resolution path.
- Procedural compliance: First-attempt shape (no T-003 journal at entry); clean tree confirmed via `git status --porcelain` before dispatch. State flipped pending→in-progress→in-review via `speccy task transition` only. No shipped skill/module friction encountered; no `resources/` edits needed.
</implementer>
<review date="2026-06-10T21:14:16Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. `speccy context` is a local, read-only command:
selector resolution reuses the shared `task_lookup` path (same access
surface as `speccy check`, no divergence), and T-003 only adds in-memory
projection of already-parsed local SPEC.md/TASKS.md content into the
bundle. Checked: no authz boundary, no network/endpoint; no injection
surface — `resolve_covering_requirements` (`speccy-core/src/context.rs:35`)
is pure string-match projection, bodies are emitted via
`serde_json::to_string` (JSON-escaped) and `writeln!` with markers passed
as arguments, not format strings (`speccy-cli/src/context.rs:247-298`); no
user-controlled path construction (no shell-out — git merge-base is T-005,
correctly out of scope); error variants disclose only the caller's own
input/spec id (`context.rs:54-77`) and selector failures write no partial
stdout (asserted in `tests/context.rs` `invoke_json_err`); no secret/env/
credential read; no new dependency.
</review>
<review date="2026-06-10T21:14:22Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-003 task slice delivered faithfully. Verified the diff against each
done-when bullet: the `task` entry carries verbatim `Task.body` plus
parsed id/state/covers (TaskEntry in `context_output.rs`); covering
requirements arrive as full bodies with done_when/behavior/scenarios,
deduplicated in covers-list order, resolved through the shared
`resolve_covering_requirements` walk (no duplicate walk in the CLI —
DEC-002 honored). CHK-004 proven by
`bundle_carries_only_the_two_covered_requirements_with_full_content`
(asserts exactly [REQ-001, REQ-003] in full and absence of REQ-002/004/005
ids, bodies, and CHKs from the raw payload); the raw-body behavior proven
by `bundle_carries_task_entry_with_raw_body_and_parsed_fields`. CHK-005
honored — no `check.rs` or check fixture touched; context suite (7 tests)
green. Scope is surgical: only the `task`/`requirements` envelope fields
plus the parallel text renderer; no out-of-scope journal/sibling-index/
consistency sections leaked in, and `--json` still toggles representation
not content (non-goals respected). The missing-covers-token done-when
bullet is satisfied structurally by the shared walk's silent-skip
semantics (tested at the core layer in T-001); T-003's named scenarios do
not require a context-layer retest, so this is a reasonable scope split,
not a silent drop. No goals abandoned, no open questions resolved.
</review>
<review date="2026-06-10T21:14:45Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Tests drive the real code path, not mocks. `bundle_carries_only_the_two_covered_requirements_with_full_content` (context.rs) calls the real `run` -> `assemble_bundle` -> `build_requirements` -> `speccy_core::context::resolve_covering_requirements`, asserting exactly [REQ-001, REQ-003] in covers-list order, each with its done_when/behavior/scenario marker, and the absence of REQ-002/004/005 ids, body markers, and CHK-002/004/005 from the raw payload -- directly covering CHK-004. `bundle_carries_task_entry_with_raw_body_and_parsed_fields` asserts the verbatim `<task>` body (`TASK_BODY_MARKER`) rides alongside parsed id/state/covers. Both fail under obvious mutations (return all five reqs -> absence asserts fire; drop scenarios -> scenario_ids asserts fire; placeholder body -> marker assert fires). Verified all 7 context tests green via `cargo test -p speccy-cli --test context`.

Evidence roll call complete for the one covered REQ (REQ-003): CHK-004 demonstrated (cites a test present in the diff), CHK-005 hygiene with a verifiable cite. The DEC-002 parity claim is real -- both `check.rs:303` and `context.rs:186` consume the single shared `resolve_covering_requirements`, no duplicate. CHK-005's hygiene label is sound: this task's working-tree diff touches no `check.rs` or check fixture, so `check`'s byte-stable output (owned and golden-tested by T-001) is left undisturbed; re-proving it here by non-disturbance is the right shape. Red/green evidence is structurally plausible (field-absent `.expect` panic -> populated payload), scoped per-slice, no fabrication patterns matched.
</review>
<review date="2026-06-10T21:14:48Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="1">
Pass. Reviewed the T-003 slice for logic and control-flow defects.
`build_task_entry` (`speccy-cli/src/context.rs:169-177`) projects
`task.id`/`state.as_str()`/`covers`/`body` — every field exists on
`Task` (`speccy-core/src/parse/task_xml/mod.rs:252-268`) and `body`
is the verbatim `<task>` body, so the projection is faithful.
`build_requirements` (context.rs:185-203) delegates to the shared
`resolve_covering_requirements` (`speccy-core/src/context.rs:35-52`)
and maps each `Requirement` to `CoveringRequirement`, iterating
`req.scenarios` in order; dedup, covers-order, and silent
missing-id skip are all owned by the infallible shared walk, so no
off-by-one or arm-ordering hazard sits in the projection. Shared-walk
parity with `check` holds: `context.rs:186` and `check.rs:303` call
the identical function with the same args, so the two cannot diverge
(CHK-005). Uncovered-requirement leakage is impossible by
construction — `build_requirements` emits only resolved requirements
and `build_intent` reads only goals/non-goals/decisions, never
requirement bodies — so the CHK-004 absence assertions rest on real
exclusion, not coincidental marker uniqueness. Empty-covers and
missing-id paths resolve to an empty Vec, and `render_text`
(context.rs:273-290) handles the empty branch as "(none)" with no
panic. No `unwrap`/`expect`/`[i]` in the production paths.
</review>
<review date="2026-06-10T21:15:14Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
Two stale doc comments introduced by T-003's changes violate the "Docs match code" invariant.

`speccy-cli/src/context.rs:130`: the private `assemble_bundle` function doc says
"Assemble the T-002 bundle slice (identity + intent) from a resolved task location."
T-003 extended this function to also call `build_task_entry` and `build_requirements`,
so the doc no longer describes what the function does. It should read something like
"Assemble the context bundle (identity + intent from REQ-002; task entry and covering
requirements from REQ-003) from a resolved task location."

`speccy-cli/src/context.rs:88-89`: the public `run` function doc says
"assembles the identity + intent bundle (REQ-002)" — again omitting that T-003's
additions also assemble the task entry and covering requirements (REQ-003). Public
API docs that describe only part of what the function does are misleading to readers.

No other style issues found. Naming, imports, suppression annotations, and
test invariants all align with project conventions. The `#![allow(clippy::expect_used,
reason = "...")]` pattern in context.rs test file matches the established convention
in sibling test files (e.g., check.rs:1-4). The reuse survey is present and accurate.
</review>
<blockers date="2026-06-10T21:15:58Z" round="1">
Fix two stale doc comments in `speccy-cli/src/context.rs` that T-003 left describing only the T-002 (identity + intent) slice, so the docs match the code (style, blocking).

- `assemble_bundle` doc (~line 130): now also calls `build_task_entry` and `build_requirements`; update the doc to cover identity + intent (REQ-002) **and** task entry + covering requirements (REQ-003).
- `run` doc (~lines 88-89): says "assembles the identity + intent bundle (REQ-002)"; extend it to note the task entry and covering requirements (REQ-003) are assembled too.

No other persona raised a blocker. Touch only the two doc comments; do not alter behavior.
</blockers>
<implementer date="2026-06-10T21:18:11Z" model="claude-opus-4-8[1m]" round="2">
- Reuse survey: No new top-level symbols introduced this round, so the per-symbol floor is satisfied vacuously. Round-1 blocker was style-only (stale doc comments), not reuse-related, so the full area-map is not re-run per the round-2 reuse semantics.
- Completed: Addressed the round-1 style blocker by correcting two stale doc comments in `speccy-cli/src/context.rs` that described only the T-002 (identity + intent) slice. The `run` doc now states it assembles the identity + intent bundle (REQ-002) along with the selected task entry and covering requirements (REQ-003); the `assemble_bundle` doc now reads "identity + intent from REQ-002; task entry and covering requirements from REQ-003". Docs now match code.
- Undone: Nothing. The blocker scoped the fix to exactly these two doc comments and explicitly forbade behavior changes; no behavior was touched.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-003.md` (unchanged from round 1 — this round is a doc-comment fix with no test delta). Roll call for CHKs under REQ-003 (unchanged): CHK-004 demonstrated; CHK-005 hygiene. Doc-comment edits are judgment-only — reviewer-style judges on the diff; no scriptable proof applies to prose accuracy.
- Discovered issues: None.
- Procedural compliance: Retry shape detected from the journal (round-1 implementer handoff plus matching round-1 blockers directive); amended the prior pass's uncommitted WIP in place — no git restore/clean/checkout, no rewrite from scratch. State flipped in-progress to in-review via speccy task transition only. No shipped skill/module friction; no resources/ edits needed.
</implementer>
<review date="2026-06-10T21:19:38Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. Both stale doc comments are fixed.

`speccy-cli/src/context.rs:88-90`: the `run` doc now reads "assembles the identity + intent bundle (REQ-002) along with the selected task entry and its covering requirements (REQ-003)" — matches the function's actual behavior.

`speccy-cli/src/context.rs:131-133`: the `assemble_bundle` doc now reads "Assemble the context bundle (identity + intent from REQ-002; task entry and covering requirements from REQ-003)" — matches the function's actual behavior.

No other style issues found. Module-level doc comment updated correctly to describe both T-002 and T-003 slices. Naming, imports, suppression annotations, and test invariants all align with project conventions. Reuse survey present and honest.
</review>
<review date="2026-06-10T21:19:41Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns; round-2 delta is doc-comment-only and changes no
behavior, so the round-1 security posture stands. Confirmed the retry
touched only the two prose doc comments (`speccy-cli/src/context.rs:88-92`
`run`, `:131-133` `assemble_bundle`) — no production logic, no new
dependency, no network/authz/secret surface. Re-checked the body-emission
paths: `render_text` writes all SPEC.md/TASKS.md bodies as `writeln!`
arguments, not format strings (`context.rs:244-285`), and `--json` emits
via `serde_json::to_string` (JSON-escaped), so no format-string or
injection vector on verbatim task/requirement bodies. `speccy context`
remains a local, read-only command (active-spec scan, no shell-out, no
user-controlled path construction); requirement resolution reuses the
shared `resolve_covering_requirements` walk so no leakage of non-covered
requirement bodies. Error variants disclose only the caller's own
selector/spec id.
</review>
<review date="2026-06-10T21:20:03Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-2 pass. REQ-003 task slice still delivered faithfully and the round-1
style blocker is cleared without business-scope regression. Verified the
round-2 diff against each done-when bullet:

- Raw `<task>` entry + parsed fields: `build_task_entry`
  (`speccy-cli/src/context.rs:177-185`) projects id/state/covers plus
  verbatim `Task.body`; proven by
  `bundle_carries_task_entry_with_raw_body_and_parsed_fields`
  (asserts T-001/pending/[REQ-001,REQ-003] and `TASK_BODY_MARKER`).
- Exactly the covering requirements, full bodies + scenarios,
  deduplicated in declared order via the shared
  `resolve_covering_requirements` walk (`build_requirements`,
  context.rs:193-211; no duplicate walk — `check.rs` now consumes the
  same function, DEC-002 honored). CHK-004 proven by
  `bundle_carries_only_the_two_covered_requirements_with_full_content`
  (exactly [REQ-001,REQ-003] with done_when/behavior/scenario markers;
  REQ-002/004/005 ids, bodies, and CHKs absent from the raw payload).
- Missing-covers-token done-when bullet satisfied structurally by the
  shared walk's silent-skip semantics (tested at the core layer in
  T-001); reasonable scope split, not a silent drop.
- CHK-005 honored — `check.rs` only swaps the inlined walk for the
  shared function; no check fixture or expectation touched.

Round-1 blocker (two stale doc comments describing only the T-002 slice)
is fixed: the `run` doc (context.rs:96-97) now reads "identity + intent
bundle (REQ-002) along with the selected task entry and its covering
requirements (REQ-003)" and the `assemble_bundle` doc (context.rs:137-139)
now reads "identity + intent from REQ-002; task entry and covering
requirements from REQ-003". Docs match code; the fix touched only prose,
no behavior (as the blocker required).

Scope is surgical: only the `task`/`requirements` envelope fields plus the
parallel text renderer; no out-of-scope journal/sibling-index/consistency
sections leaked in, and `--json` still toggles representation not content
(non-goals respected). Goals and user stories served (one call hands a
persona the task + its requirements + scenarios). No open questions
resolved, no goals abandoned.
</review>
<review date="2026-06-10T21:20:11Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
Pass. Round-2 review of the T-003 slice for logic and control-flow
defects. The round-2 delta is doc-comment-only (the `run` and
`assemble_bundle` docs in `speccy-cli/src/context.rs` now describe
both the REQ-002 identity+intent and REQ-003 task+requirements slices
accurately); prose edits cannot introduce a correctness defect, and no
production control flow changed since the round-1 pass. Re-verified the
unchanged logic: `build_requirements` (context.rs:187-205) delegates to
the infallible shared `resolve_covering_requirements`
(`speccy-core/src/context.rs:35-52`) which owns dedup, covers-list
order, and silent missing-id skip, so the projection has no off-by-one
or arm-ordering hazard. Shared-walk parity with `check` holds verbatim:
`context.rs:188` and `check.rs:303` call the identical function with the
same args, so resolution cannot diverge (CHK-005). `build_task_entry`
(context.rs:171-179) projects `id`/`state.as_str()`/`covers`/`body`,
each a faithful clone of the real `Task` fields
(`speccy-core/src/parse/task_xml/mod.rs:252-268`). `render_text`
(context.rs:243-294) handles the empty-`covers` and empty-`requirements`
branches as "(none)" with no panic, and empty/missing-id covers resolve
to an empty Vec. No `unwrap`/`expect`/panic/`[i]` in production paths;
error paths return without writing partial output to `out`.
</review>
<review date="2026-06-10T21:21:07Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Tests drive the real code path, not mocks. Verified against the working-tree T-003 diff (`git diff HEAD`, since the implementer leaves WIP uncommitted; the merge-base diff only shows committed T-001/T-002).

CHK-004: `bundle_carries_only_the_two_covered_requirements_with_full_content` (context.rs) calls the real `run` -> `assemble_bundle` -> `build_requirements` -> `speccy_core::context::resolve_covering_requirements`. It asserts `requirements == [REQ-001, REQ-003]` in covers-list order, each carrying its done_when/behavior markers and its own scenario id+body marker, and that all three uncovered REQ ids, body markers, and CHK ids are absent from the raw payload -- a faithful translation of the scenario's Given/When/Then. Mutation-tested: replacing the shared walk with `spec_doc.requirements.iter().collect()` (return all five) fails the order assertion (`left: [REQ-001..REQ-005]` vs `right: [REQ-001, REQ-003]`). The raw-body behavior (`bundle_carries_task_entry_with_raw_body_and_parsed_fields`) asserts the verbatim `<task>` body (`TASK_BODY_MARKER`) rides alongside parsed id/state/covers; mutation-tested by replacing `task.body.clone()` with a placeholder -> the marker assert fires. Neither test is vacuous.

CHK-005 (hygiene): the working-tree diff touches only context.rs/context_output.rs/tests/context.rs/TASKS.md -- no `check.rs` or check fixture. Re-ran `cargo test -p speccy-cli --test check` (22 green) confirming `check`'s output is undisturbed by the shared-walk consumption; non-disturbance is the right shape for a byte-stability invariant owned by T-001.

Round-2 scope (style blocker) confirmed addressed: the three stale doc comments (module header, `run`, `assemble_bundle`) now describe the REQ-003 task-entry/covering-requirements slice; behavior bytes unchanged from round 1.

Evidence roll call complete for the one covered REQ (REQ-003): CHK-004 demonstrated (cites a test present in the diff), CHK-005 hygiene with a re-runnable file-path scope cite. Red phase (field-absent `.expect` panic on the T-002 envelope, confirmed against the committed T-002 `ContextBundle`) -> green (populated payload) is materially distinct, scoped per-slice (`cargo test -p speccy-cli --test context`, not the workspace hygiene run), and names no symbol absent from the diff. No fabrication patterns matched. All 7 context tests green.
</review>
