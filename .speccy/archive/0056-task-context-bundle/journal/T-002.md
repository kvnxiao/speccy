---
spec: SPEC-0056
task: T-002
generated_at: 2026-06-10T21:00:00Z
---

<implementer date="2026-06-10T21:00:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the `speccy context` command-introduction area (main.rs clap+dispatch, check.rs sibling read command, next_output.rs envelope convention, core parse structs, task_lookup, tests/). Reuse-as-is: `task_lookup::parse_ref`/`find` (selector resolution, REQ-001); `report_lookup_error` helper at main.rs:664 (selector-diagnostic parity with `speccy check` — routed `context` through it, no inline mapping); `workspace::{find_root,scan_with_archive}`; `ParsedSpec::{spec_md_ok,spec_doc_ok}`; `SpecFrontmatter.{id,title,status}` + `SpecStatus::as_str`; `SpecDoc.{goals,non_goals,decisions}` + `Decision.{id,body}`; `flush_best_effort`/`cwd::resolve` in main.rs. Extend: none. Write-fresh (each searched and found absent): `speccy-cli/src/context.rs` run entry + assembly (no context CLI module in lib.rs list); `speccy-cli/src/context_output.rs` serde structs (modelled on next_output.rs JsonPerSpec — no context envelope existed); `Command::Context` clap arm + `run_context` dispatcher (grepped main.rs enum, absent); `speccy-cli/tests/context.rs` (globbed tests dir, absent). `ContextError::TaskLookup(#[from] LookupError)` mirrors `CheckError::TaskLookup`.
- Completed: Added `speccy context <task-selector> --json` (REQ-001 skeleton + REQ-002 intent slice). New `context_output.rs` (`ContextBundle` with `schema_version` declared first, `SpecIdentity`, `Intent`, `DecisionEntry`) and `context.rs` (`run` → parse_ref → scan → find → assemble; `build_intent` reads only goals/non_goals/decisions; JSON + text renderers; `--json` toggles representation only). Wired `Command::Context` + dispatch + `run_context` (selector failures routed through `report_lookup_error` for `check` parity; exit 2 on cwd error, 1 otherwise). Registered both modules in lib.rs. Five integration tests in tests/context.rs covering CHK-001 (lib + binary-boundary ambiguity parity), CHK-002 (one JSON doc, schema_version first via raw-prefix assertion), CHK-003 (identity + intent present, Summary/user-story markers absent), plus invalid-format selector with empty-stdout assertion.
- Undone: Nothing in T-002 scope. The envelope deliberately carries only identity + intent; task entry/covering requirements (T-003), journal (T-004), sibling index/paths/diff command (T-005), and consistency (T-006) are later tasks that extend this same envelope. No `speccy context SPEC-NNNN` bare-spec form (DEC-006 / non-goal).
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-002.md`. Roll call for CHKs under REQ-001 + REQ-002:
  - CHK-001 (ambiguous unqualified selector → same diagnostic class as `speccy check`): demonstrated → evidence Scenario 1 (red: `context` not a subcommand at HEAD; green: shared `LookupError::Ambiguous` at lib level + identical "T-001 is ambiguous; matches in 2 specs." stderr at the CLI boundary vs `speccy check T-001`).
  - CHK-002 (qualified selector → one JSON doc, schema_version=1 first field): demonstrated → evidence Scenario 2 (green: parses as one `serde_json::Value` with `schema_version==1`; raw string starts with `{"schema_version":1`).
  - CHK-003 (goals/non-goals/both decisions present, Summary marker absent): demonstrated → evidence Scenario 3 (green: marker assertions on goals/non_goals/decisions present; `SUMMARY_NARRATIVE_MARKER` + `USER_STORY_MARKER` absent from payload).
  - CHK-005 (existing `check` tests pass unchanged — REQ-003, owned by T-001): hygiene → the 22-test `check.rs` suite runs unchanged under `cargo test --workspace` and passes; no fixture/expectation edits made this task.
  - CHK-004 (REQ-003 covering-requirements), CHK-006/CHK-007 (REQ-004 journal), CHK-008 (REQ-005), CHK-009 (REQ-006), CHK-010 (REQ-007), CHK-011/CHK-012 (REQ-008), CHK-013 (REQ-009): out of T-002 scope — owned by later tasks, not this task's covers (REQ-001 REQ-002).
- Discovered issues: None. The `clippy::unwrap_in_result` lint fires on `.expect()` over `serde_json::from_str` (a `Result`) inside `-> TestResult` test bodies even with `allow-expect-in-tests`; extracted JSON parsing into a non-Result `parse_one_json` helper (matching the project's existing posture) and replaced a `panic!` arm with a let-else returning `Err`.
- Procedural compliance: Followed speccy-work first-attempt branch (clean tree confirmed, no journal → first-attempt shape). Flipped pending→in-progress and in-progress→in-review via `speccy task transition` (never edited TASKS.md state directly). All four hygiene gates run and green before the in-review flip. No source-module/skill edits needed (CLI-only task). No edits to ejected `.claude/.agents/.codex` files.
</implementer>
<review date="2026-06-10T21:01:45Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in this slice. `speccy context` is a read-only
local command that performs no filesystem writes (`context.rs:91-121`
reads via `scan_with_archive` and writes only to the passed `out`
stream) and spawns no subprocess. Selector input is validated through
the shared `parse_ref` before any workspace access, with no partial
stdout on failure (`invoke_json_err` asserts empty output;
`context.rs:103`); the selector never constructs a filesystem path, so
no path-traversal vector. The emitted goals/non-goals/decisions are
project authoring artifacts, not secrets — CHK-003 confirms the
Summary narrative and user-stories are excluded (scope, not a leak),
and error messages carry only spec ids (`ContextError`), no sensitive
data. No new dependencies, no crypto/auth/cookie surface, and no
panic-prone paths in production code (the workspace catch-all maps via
`io::Error::other`, the `resolve_spec` not-found branch is handled).
Git diff machinery (T-005) and journal inlining (T-004) are out of
this task's scope.
</review>
<review date="2026-06-10T21:02:06Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. T-002 covers REQ-001 + REQ-002; both requirements' done-when bullets map to the diff and the observable behavior holds. REQ-001: selector resolves via `parse_ref`/`find` and CLI selector failures route through the shared `report_lookup_error` helper (`main.rs:670`), so the ambiguity diagnostic class matches `speccy check` — the binary test asserts the identical headline "T-001 is ambiguous; matches in 2 specs." against both commands (CHK-001). `schema_version` is the first struct field of `ContextBundle` (`context_output.rs:28`) and the raw-prefix assertion `{"schema_version":1` proves it serializes first (CHK-002). No-writes contract holds: `run` only scans/parses and writes to the `out` sink (`context.rs:91-121`). REQ-002: identity from frontmatter and `build_intent` reads only goals/non_goals/decisions (`context.rs:161-175`), so Summary narrative, user-stories, and non-covered requirement bodies are excluded by construction; CHK-003 asserts both goal/non-goal/decision markers present and SUMMARY_NARRATIVE_MARKER + USER_STORY_MARKER absent. All 5 integration tests pass. Non-goals honored: no bare-spec form (DEC-006), `--json` toggles representation only — `render_text` emits the same goals/non_goals/decisions content (DEC-001), no role-shaped flags. No `<open-question>` elements were silently resolved; the diff reflects the latest changelog intent (grounding correction), not the superseded draft. Minor non-blocking note: REQ-001's `<behavior>` names a happy-path "unqualified selector resolves, exit 0" case that no CHK scenario pins directly — the tests exercise unqualified parsing only via the ambiguous branch and the success path only via a qualified selector; covered transitively by the shared resolver, and all three CHK scenarios are green, so this is a coverage observation, not a contract gap.
</review>
<review date="2026-06-10T21:02:35Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defects in the T-002 slice (REQ-001 selector contract + REQ-002 identity/intent envelope).

Traced every control-flow path in `speccy-cli/src/context.rs` and the `run_context` dispatcher (`speccy-cli/src/main.rs:673-700`):
- Error mapping is sound: cwd-resolve failure exits 2; `ContextError::TaskLookup` routes through the shared `report_lookup_error("context", "", &e)` (exit 1), every other variant exits 1. The `""` disambig suffix is correct for a command with no trailing arg, and the helper at `main.rs:710-741` reproduces `speccy check`'s exact ambiguity diagnostic class (headline, "Disambiguate with one of:", per-candidate lines) — verified against `run_check` (`main.rs:640-669`) and the passing `binary_ambiguous_selector_matches_check_diagnostic_class` test.
- Partial-stdout-on-failure invariant holds: `parse_ref?`, `find_task?`, and `assemble_bundle?` all short-circuit before any `out.write_all`; `invoke_json_err` asserts empty stdout.
- JSON and text branches are mutually exclusive (no double-write); `schema_version` is the first struct field so serde emits it first (raw-prefix test confirms).
- `build_intent` reads only goals/non_goals/decisions, so Summary/user-stories are excluded by construction; CHK-003 asserts both presence and absence of markers.
- `assemble_bundle` re-resolves the spec via `resolve_spec` scanning `ws.specs` by `spec_id` rather than using `location.spec_md`/`location.spec_doc` directly — a benign redundancy, not a defect: `collect_candidates` already gates candidacy on `spec_md_ok().is_some()`, qualified lookup scopes to one spec_id, and a duplicate-id workspace falls to the NotFound arm before `assemble_bundle` is reached, so the re-scan resolves the same spec. `spec_doc_ok()==None` is handled via `SpecDocUnavailable`.
No off-by-one, boundary, Option/Result-mishandling, concurrency, or resource-leak issues. `cargo test -p speccy-cli --test context` green (5/5).
</review>
<review date="2026-06-10T21:03:11Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Pass. The diff introduces `context.rs`, `context_output.rs`, and `tests/context.rs` with no style violations found.

Import ordering and per-item line style in `context.rs` matches the established pattern of `journal_show.rs` and `check.rs` (one `use` per item from sibling modules). The `find_root` error match arms mirror `check.rs:109-111` exactly. All declared imports are used; no dead code introduced.

`context_output.rs` follows the `next_output.rs` convention: module-level doc, `use serde::Serialize;` only, `schema_version: u32` first field, `pub` struct fields with doc comments.

`tests/context.rs` follows the `#![allow(clippy::expect_used, reason = "...")]` + `#![expect(clippy::panic_in_result_fn, reason = "...")]` preamble that every sibling test file uses. The `parse_one_json` helper is extracted out of `-> TestResult` bodies matching the project's `unwrap_in_result` posture (noted in the implementer journal). All five tests gate real invariants (ambiguity class, schema_version first-field order, intent content inclusion/exclusion, CLI diagnostic class parity, invalid-format selector).

Main-wiring in `main.rs` follows the existing `run_check` / `run_journal_append` dispatcher shape; `run_context` uses the shared `report_lookup_error` path for `LookupError` variants as specified in the task.

No `#[allow(...)]` suppressions without justification, no `unwrap()` in production code, no reinvented helpers, no unused variables or imports.

Reuse survey: field present; named symbols (`task_lookup::parse_ref`, `find`, `report_lookup_error`, `scan_with_archive`, `ParsedSpec::spec_md_ok`/`spec_doc_ok`, `SpecFrontmatter`, `SpecDoc.goals`/`non_goals`/`decisions`) all confirmed to exist and are called in the diff.
</review>
<review date="2026-06-10T21:03:20Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
All three covered scenarios are driven by real assertions against the actual `speccy context` code path, not mocks. CHK-001: `ambiguous_unqualified_selector_surfaces_lookup_ambiguity` (context.rs:163) destructures the real `ContextError::TaskLookup(LookupError::Ambiguous)` and asserts both candidate specs are named; `binary_ambiguous_selector_matches_check_diagnostic_class` (context.rs:345) drives the real binary, asserting non-zero exit, empty stdout, the shared ambiguity headline, and a disambiguation line per candidate, then asserts `speccy check T-001` emits the same headline class — both route through `report_lookup_error` (main.rs:710), so the parity is genuine, not asserted-by-coincidence. CHK-002: `qualified_selector_emits_json_with_schema_version_first` (context.rs:206) asserts both `schema_version==1` and the load-bearing raw-prefix `{"schema_version":1`, which catches field reordering that the parsed-value check alone would miss. CHK-003: `bundle_carries_intent_and_excludes_summary_marker` (context.rs:245) asserts both goals markers, the non-goal marker, both DEC ids in declared order with body content, AND the absence of `SUMMARY_NARRATIVE_MARKER`/`USER_STORY_MARKER` from the whole payload — a real positive+negative pair that fails if `build_intent` (context.rs:161) ever leaked the Summary. Mentally mutating the impl (swallow ambiguity and emit a bundle; reorder schema_version; serialize the Summary narrative) breaks at least one assertion each. Negative path also covered beyond scenarios: `invalid_selector_format_surfaces_lookup_invalid_format` asserts the specific `InvalidFormat` variant carrying the raw selector, and `invoke_json_err` asserts empty stdout on every selector failure (REQ-001 no-partial-output done-when). Evidence at evidence/T-002.md is a credible red-then-green trail: red is the legitimate net-new-command absence (compile failure + unrecognized subcommand), green output is materially distinct, the scoped command `cargo test -p speccy-cli --test context` is not the full-suite hygiene run, and every test name cited appears in the diff. Roll call covers CHK-001/002/003 for REQ-001+REQ-002 as `demonstrated`; out-of-scope CHKs correctly deferred. Ran `cargo test -p speccy-cli --test context`: 5/5 pass. Test naming/structure match the project's existing integration-test conventions.
</review>
