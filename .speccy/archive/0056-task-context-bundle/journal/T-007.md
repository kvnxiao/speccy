---
spec: SPEC-0056
task: T-007
generated_at: 2026-06-10T22:56:59Z
---

<implementer date="2026-06-10T22:56:59Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: bounded to `speccy-cli/tests/context.rs` and its `common` module (the only files this test-only task touches). Reuse-as-is: `Workspace::new`, `write_spec`, `invoke_json`, `parse_one_json`, `write_journal` (existing `context.rs`/`common` helpers — called directly). Reuse-as-is across crates: `sha2`/`const_hex` for the re-lock hash (already dev-deps, mirroring `common::sha256_hex`'s encoding). Write-fresh: `bundle_size_scales_with_task_not_spec` (the property test — searched `context.rs` for any existing grow-spec/re-emit/diff helper; none — T-007 is the first size-invariant test), plus four local fixture builders (`spec_md_n_requirements`, `tasks_md_with_hash`, `journal_one_round`, `spec_hash`) and a `normalize_consistency` helper. I wrote `spec_md_n_requirements` fresh rather than reuse `spec_md_five_requirements` because the size test needs a parameterized requirement count (3 → 4) to grow the spec; the existing helper is fixed at five.
- Completed: Added the REQ-007 property-style integration test `bundle_size_scales_with_task_not_spec` to `speccy-cli/tests/context.rs`. It emits the bundle for a fixed `T-001`, grows the spec three ways (one uncovered requirement REQ-004, one new sibling task T-002, one foreign-task journal round on T-002), re-locks `spec_hash_at_generation` to the edited SPEC.md SHA-256, re-emits, normalizes the `consistency` section, and asserts the two payloads differ by exactly one added sibling-index entry (T-002, id/state/covers only) and are otherwise deep-equal. Added a focused evidence file at `.speccy/specs/0056-task-context-bundle/evidence/T-007.md`. Updated the module doc comment to describe the T-007 slice.
- Undone: Nothing in scope for T-007. T-008 (persona/phase module migration) and T-009 (ARCHITECTURE.md) remain pending and are out of this task's scope.
- Hygiene checks: cargo test --workspace — exit 0 (all suites green incl. 16 context tests); cargo clippy --workspace --all-targets --all-features -- -D warnings — exit 0; cargo +nightly fmt --all --check — exit 0 (one collapse-the-insert reflow applied, re-verified clean); cargo deny check — exit 0 (advisories/bans/licenses/sources ok).
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-007.md`. Roll call for CHKs under REQ-007:
  - CHK-010 (grown-spec bundle differs by exactly one sibling entry, consistency normalized): demonstrated → evidence Scenario 1 covers the before/after emit pair; the test normalizes `consistency`, asserts `after.siblings.len() == 1` (the one T-002 entry, keys exactly covers/id/state), and asserts the siblings-stripped `before`/`after` payloads are deep-equal — the executable form of "nothing else changed." Non-vacuous: explicit guards prove REQ-004 was added to SPEC.md yet is absent from T-001's payload, and the foreign T-002 journal exists on disk yet leaves T-001's `journal.exists == false`.
- Discovered issues: The consistency detector (`speccy-core/src/consistency.rs::detect`) does NOT correlate against `spec_hash_at_generation` at all — drift is computed from task-state-vs-git-commit correlation and journal-parse-ability, neither of which depends on SPEC.md content. So in a non-git fixture the `consistency` section is `{status: ok, drifts: []}` both before and after the SPEC edit, and the re-lock is a no-op for the bundle. The SPEC/CHK-010 framing ("re-lock the hash", "consistency fields affected by the SPEC edit") anticipated a hash-drift mechanism that the current code does not implement. I honoured the recipe faithfully (re-lock + normalize consistency) so the test is correct regardless of whether consistency ever does shift, but flagging the framing-vs-implementation gap for the reviewer: it is not a defect in this SPEC's scope, and no requirement intent changed.
- Procedural compliance: First-attempt shape (no prior journal for T-007); clean tree at entry confirmed via git status --porcelain. Transitioned pending → in-progress → in-review via `speccy task transition` (never edited the state attribute directly). No friction in the shipped skill/CLI required a `resources/modules/` edit. Test-only change; no production code touched.
</implementer>
<review date="2026-06-10T22:58:50Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. T-007 is a test-only slice — the sole code change is the `bundle_size_scales_with_task_not_spec` property test plus four local fixture builders in `speccy-cli/tests/context.rs`; no production code, no `common`-helper changes. Reviewed for the persona's surface: (1) no new dependencies — `sha2`/`const_hex` are pre-existing dev-deps used only to compute a fixture content hash, not a security primitive; (2) no secrets or sensitive data in fixtures — `model="m/low"` is a placeholder and all ids/dates are fictional; (3) no injection vectors — fixture bodies are written to a temp workspace via `fs_err::write` with no shell interpolation, and `invoke_json` execs the compiled binary against that sandboxed workspace; (4) no auth/authz boundary, network call, log/telemetry emission, or sensitive-data exposure introduced. The `SPEC-0042`/`0042-alpha` placeholders are fictional and confined to a test fixture.
</review>
<review date="2026-06-10T22:59:16Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. REQ-007's primary done-when is satisfied as an executable contract: `bundle_size_scales_with_task_not_spec` (`speccy-cli/tests/context.rs:1364`) emits the T-001 bundle, grows the spec the three REQ-007-enumerated ways (uncovered REQ-004, sibling T-002, foreign T-002 journal round), re-emits, and asserts (a) exactly one added sibling entry carrying only id/state/covers, and (b) siblings-stripped deep-equality of the two payloads — the executable form of "nothing else changed." Both REQ-007 `<behavior>` clauses (uncovered-req-adds-nothing, sibling-adds-one-entry) are directly exercised. The test is non-vacuous: the `requirements` field is a real `Vec<CoveringRequirement>` (`speccy-cli/src/context_output.rs:48`), so the `!after_str.contains("REQ-004")` and `journal.exists == false` guards would fire on a real `covers`-filter or foreign-journal-leak regression.

The implementer's flagged framing issue is assessed and does not block: `consistency::detect` (`speccy-core/src/consistency.rs:302`) correlates task-state-vs-git-commit and journal-parse-ability, never `spec_hash_at_generation`, and `Workspace::new` is a non-git temp dir, so the consistency section is `{ok, []}` before and after and `normalize_consistency` is a no-op here. CHK-010 prescribes normalizing consistency before the diff and the test does so; normalize-then-assert-equality is strictly more conservative than REQ-007's "identical except for consistency fields" allowance and still proves the load-bearing invariant. The implementer surfaced the SPEC-prose-vs-implementation gap for a human rather than silently deciding an open question — correct handling; no requirement intent changed.

Scope is clean: test-only change, no production code touched, no non-goal scope crept in. REQ-007's second done-when (ARCHITECTURE.md documentation of the invariant) is T-009/REQ-009 scope, explicitly out of this task's body — not a T-007 gap. Test passes (`cargo test -p speccy-cli --test context bundle_size_scales_with_task_not_spec` — 1 passed).
</review>
<review date="2026-06-10T22:59:27Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The REQ-007 size-invariant test `bundle_size_scales_with_task_not_spec` (speccy-cli/tests/context.rs:1407) is logically sound. Control flow is correct: emit before (3 reqs / 1 task), grow the spec three ways, re-lock the hash, re-emit, normalize consistency on both sides, then assert 0 siblings before vs exactly 1 after (keys exactly covers/id/state at :1478, id T-002 at :1483) plus deep-equality of all non-sibling fields via `strip_siblings` (:1498). I confirmed `SiblingEntry` (context_output.rs:196) serializes exactly those three keys, so the key-set assertion is accurate, and the `n_requirements` loop (:1328) is an inclusive `1..=n` with no off-by-one.

On the implementer's flagged concern: the consistency detector does not correlate against `spec_hash_at_generation`, so the section is `ok/[]` both before and after in the non-git fixture and the re-lock is effectively a no-op for the bundle. This does NOT weaken the invariant proof. `normalize_consistency` (:1400) replaces the whole `consistency` value with the same sentinel on both sides, so consistency contributes nothing to the deep-equal either way — exactly what CHK-010 prescribes ("consistency fields normalized"). The masking hides no field that should legitimately differ: `paths`/`diff_command` are identical (same T-001 selector, same non-git temp workspace), and any requirement or journal leak is caught by independent guards, not by the masked compare.

Non-vacuity holds: REQ-004 present in SPEC.md yet absent from the serialized payload (:1508-1516), and the foreign T-002 journal exists on disk yet T-001's `journal.exists` stays false (:1517-1528). A broken bundle (always-empty siblings, leaked uncovered requirement, or leaked foreign journal) would fail. No reachable wrong-result, crash, or resource-leak path found.
</review>
<review date="2026-06-10T22:59:39Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-010 is demonstrated by a non-vacuous, mutation-sensitive test that drives the real code path. `bundle_size_scales_with_task_not_spec` (`speccy-cli/tests/context.rs:1407`) calls the production `speccy_cli::context::run` entry against an on-disk workspace — no mock of the system under test — and exercises all three growth operations REQ-007 enumerates: one uncovered REQ-004, one new sibling task T-002, and a foreign-task journal round on T-002 (lines 1436-1450). The load-bearing assertion is the `strip_siblings` deep-equal at 1498-1502: it removes only the `siblings` array and compares every remaining envelope field (requirements, journal, paths, diff_command, identity, intent) verbatim, so a leak of REQ-004 into `requirements` or the foreign journal round into `journal` breaks the test. Two explicit guards back this: `!after_str.contains("REQ-004")` (1513) and `journal.exists == false` (1521), each paired with a positive "the growth actually happened" assertion (`spec_after.contains("REQ-004")` at 1508, journal file `.exists()` at 1517) so the test cannot pass vacuously by failing to grow the spec. The sibling-entry key-set assertion `["covers","id","state"]` (1478) gates against body-text leakage. I traced each guard to a concrete regression it catches (drop the `covers` filter, widen the journal read, add a sibling body field) and confirmed each would fail the suite. Ran `cargo test -p speccy-cli --test context bundle_size_scales_with_task_not_spec` — green.

On the implementer's discovered-issue flag: verified against `speccy-core/src/consistency.rs` that the closed `DriftKind` set is entirely task-state-vs-git-commit and journal-parse based — none keys off `spec_hash_at_generation`, so in a non-git fixture the consistency block is `{status: ok, drifts: []}` both before and after the SPEC edit. `normalize_consistency` (1400) is therefore a harmless no-op here, not a mask over a real difference, and CHK-010 explicitly licenses "consistency fields normalized." The framing-vs-implementation gap the implementer flagged is a SPEC-prose observation for the spec author / reviewer-business, not a T-007 test defect; the test honors the recipe faithfully and stays correct regardless of whether consistency ever becomes hash-sensitive. Evidence roll call is complete: REQ-007 covers only CHK-010, labelled `demonstrated` with a named test, and the red phase ties each guard to a concrete mutation. Evidence command is correctly scoped (per-test `cargo test`), not the full hygiene-suite invocation.
</review>
<review date="2026-06-10T23:00:04Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
`spec_hash` in `speccy-cli/tests/context.rs` (line ~1384) duplicates `common::sha256_hex` verbatim — both are `sha2::Sha256::digest` → `const_hex::encode`. The existing helper is `pub fn sha256_hex(bytes: &[u8]) -> String` in `speccy-cli/tests/common/mod.rs:138`, already imported in `journal_show.rs`, `journal_append_vet.rs`, and `next_json.rs` / `next_text.rs`. The call site would become `common::sha256_hex(spec_md.as_bytes())`, removing the need for the local `spec_hash` wrapper entirely.

The reuse survey names `sha2`/`const_hex` as "reused dev-deps" but does not mention `common::sha256_hex`, so the reinvention slipped past the survey's self-check. The fix is one import addition (`use common::sha256_hex;`) and replacing `spec_hash(&spec_before)` / `spec_hash(&spec_after)` call sites with `sha256_hex(spec_before.as_bytes())` / `sha256_hex(spec_after.as_bytes())`, deleting the `spec_hash` function.

No other style or convention issues found: hygiene gates all pass (cargo test / clippy / fmt / deny — verified on-disk), module doc comment update matches the code, fixture helpers are well-scoped, test assertions are non-vacuous with concrete failure descriptions.
</review>
<blockers date="2026-06-10T23:01:23Z" round="1">
Replace the locally reinvented `spec_hash` helper in the new test with the existing `common::sha256_hex`.

- style (blocking): the size-invariant test in `speccy-cli/tests/context.rs` defines a local `fn spec_hash` (line ~1388) that duplicates `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`) verbatim — both are `sha2::Sha256::digest` → `const_hex::encode`. The reuse survey missed the existing helper. Fix: delete the local `spec_hash` function and replace its two call sites (lines ~1414, ~1442) with `common::sha256_hex(spec_before.as_bytes())` / `common::sha256_hex(spec_after.as_bytes())`, adding `use common::sha256_hex;` if not already in scope. No behavior change; the helper is byte-identical.
</blockers>
<implementer date="2026-06-10T23:04:46Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: per-symbol floor only (retry addressing a reuse blocker; the round-1 area map stands). No new top-level symbol introduced this round — the change deletes one. Reuse-as-is: `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`), the existing dev-helper the style blocker named; the local `spec_hash` wrapper that duplicated it is removed.
- Completed: Addressed the round-1 style blocker by deleting the locally reinvented `fn spec_hash` from `speccy-cli/tests/context.rs` and replacing its two call sites with `common::sha256_hex(spec_before.as_bytes())` / `common::sha256_hex(spec_after.as_bytes())`, adding `use common::sha256_hex;`. The hash-relock comment moved to the surviving `&sha256_hex(spec_after.as_bytes())` call site. Byte-identical hashing (sha2 then const_hex), no behavior change; the size-invariant test still passes.
- Undone: Nothing in scope for T-007. T-008/T-009 remain pending and out of scope.
- Hygiene checks: cargo test --workspace — exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings — exit 0; cargo +nightly fmt --all --check — exit 0; cargo deny check — exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-007.md` (unchanged — the amend is a pure helper-reuse refactor, the proof shape is identical). Roll call for CHKs under REQ-007:
  - CHK-010 (grown-spec bundle differs by exactly one sibling entry, consistency normalized): demonstrated → `bundle_size_scales_with_task_not_spec` still emits the before/after pair, normalizes `consistency`, asserts the one T-002 sibling (keys covers/id/state) and siblings-stripped deep-equality, with guards proving REQ-004 stays absent and the foreign T-002 journal leaves T-001 `journal.exists == false`. The hash now comes from the shared helper instead of a local copy; the asserted invariant is unchanged.
- Discovered issues: None this round. The round-1 consistency-framing observation stands as a SPEC-prose note already adjudicated non-blocking by reviewer-business; no requirement intent changed.
- Procedural compliance: Retry shape (round-1 implementer block plus matching blocker directive); amended the prior pass WIP in place — no git restore/clean/checkout, no rewrite-from-scratch. Transitioned in-review then in-progress then in-review via `speccy task transition`. No shipped skill/CLI friction required a `resources/modules/` edit.
</implementer>
<review date="2026-06-10T23:06:01Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns. Round-2 change is a pure test-only helper-reuse refactor: the local `spec_hash` wrapper in `speccy-cli/tests/context.rs` is deleted and its two call sites now route through the existing `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`), imported at `context.rs:32` and called at `context.rs:1405`/`1433`. The swap is byte-identical (`sha2::Sha256::digest` then `const_hex::encode`); the hash is fixture-content addressing, not a security primitive — no cost-factor/key-size/IV surface. Re-confirmed the round-1 surface still holds: no production code touched, no new dependencies (`sha2`/`const_hex` are pre-existing dev-deps), no secrets or sensitive data in fixtures (`model` strings, ids, and dates are placeholders), no injection vector (fixture bodies written to a sandboxed temp workspace via `fs_err::write`, no shell interpolation; `invoke_json` execs the compiled binary against that workspace), and no auth/authz, network, or log/telemetry path introduced. `SPEC-0042`/`0042-alpha` are fictional fixture placeholders.
</review>
<review date="2026-06-10T23:06:11Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
PASS (round 2). The round-1 style blocker is addressed and the REQ-007 business contract is unchanged. The amend deleted the locally reinvented `fn spec_hash` and routed both call sites through `common::sha256_hex(spec_before.as_bytes())` / `common::sha256_hex(spec_after.as_bytes())` (`speccy-cli/tests/context.rs`, `use common::sha256_hex;` added); `fn spec_hash` is gone (grep: no matches) and `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`) is byte-identical hashing (sha2 → const_hex), so no behavior change.

REQ-007's primary done-when still holds as an executable contract: `bundle_size_scales_with_task_not_spec` emits the T-001 bundle, grows the spec the three REQ-007-enumerated ways (uncovered REQ-004, sibling T-002, foreign T-002 journal round), re-locks the hash via the shared helper, re-emits, normalizes `consistency`, and asserts (a) exactly one added sibling entry carrying only id/state/covers (id T-002) and (b) siblings-stripped deep-equality — the executable form of "nothing else changed." Both `<behavior>` clauses (uncovered-req-adds-nothing, sibling-adds-one-entry) are exercised, and the non-vacuity guards (REQ-004 present in SPEC.md yet absent from payload; foreign T-002 journal on disk yet `journal.exists == false`) survived the refactor untouched. Test green (`cargo test -p speccy-cli --test context bundle_size_scales_with_task_not_spec` — 1 passed).

The round-1 consistency-framing observation stands as an already-adjudicated SPEC-prose note (no requirement intent changed), correctly surfaced for a human rather than silently decided. REQ-007's second done-when (ARCHITECTURE.md documentation) is T-009/REQ-009 scope, explicitly out of T-007's body — not a gap here. Scope is clean: test-only change, no production code touched, no non-goal scope crept in. No regression introduced by the round-2 amend.
</review>
<review date="2026-06-10T23:06:12Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. The local `spec_hash` wrapper is deleted; `use common::sha256_hex;` is imported at line 32; both call sites (`spec_before.as_bytes()` at line 1405 and `spec_after.as_bytes()` at line 1433) route through `common::sha256_hex`. No orphaned imports or dead code introduced by the fix. Module doc comment updated in round-1 remains intact (lines 19-24). No new style issues found.
</review>
<review date="2026-06-10T23:06:36Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. Round-2 amend is byte-identical and the size-invariant proof is intact. The only change since round 1 is `use common::sha256_hex;` plus two call sites `sha256_hex(spec_before.as_bytes())` / `sha256_hex(spec_after.as_bytes())` in `speccy-cli/tests/context.rs`, replacing the deleted local `spec_hash`. `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`) is `const_hex::encode(sha2::Sha256::digest(bytes))` — identical digest and lowercase-hex encoding over identical input bytes, so the re-locked hash value is unchanged. No control-flow, Option/Result, boundary, or resource-leak defect introduced.

The invariant proof `bundle_size_scales_with_task_not_spec` still holds: emit-before (3 reqs / 1 task) → grow three ways (uncovered REQ-004, sibling T-002, foreign T-002 journal) → re-lock → re-emit → normalize consistency on both sides → assert exactly one sibling after with keys sorted `["covers","id","state"]` and id `T-002`, plus siblings-stripped deep-equality. Verified `SiblingEntry` (`speccy-cli/src/context_output.rs:196`) serializes exactly id/state/covers, so the key-set assertion is accurate. `normalize_consistency` replaces the same sentinel on both sides, so the masked field contributes nothing to the deep-equal and hides no field that should legitimately differ. Non-vacuity guards confirmed: REQ-004 present in SPEC.md yet absent from the payload, and the foreign T-002 journal exists on disk yet T-001's `journal.exists` stays false. Test runs green (`cargo test -p speccy-cli --test context bundle_size_scales_with_task_not_spec` — 1 passed).
</review>
<review date="2026-06-10T23:06:49Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
CHK-010 stays demonstrated by a non-vacuous, mutation-sensitive test, and the round-1 style blocker is correctly resolved. `bundle_size_scales_with_task_not_spec` (`speccy-cli/tests/context.rs:1397`) drives the real code path — `invoke_json` calls production `speccy_cli::context::run` against an on-disk workspace (no mock of the system under test) — and exercises all three REQ-007 growth operations: one uncovered REQ-004 (T-001 covers only REQ-001), sibling task T-002, and a foreign T-002 journal round via `write_journal` (lines 1427-1441). The load-bearing assertion is the `strip_siblings` deep-equal (1489-1493); I traced each backing guard to a concrete mutation: dropping the `covers` filter leaks REQ-004 into `requirements` (breaks deep-equal + `!after_str.contains("REQ-004")` at 1504, paired with the positive `spec_after.contains("REQ-004")` at 1499 so growth is proven non-vacuous); widening the journal read flips `journal.exists` to true (breaks deep-equal + the `== false` guard at 1512, paired with the on-disk `.exists()` at 1508); a body field on the sibling breaks the `["covers","id","state"]` key-set at 1469; always-empty siblings breaks `len() == 1` at 1456.

Round-2 fix verified: the local `spec_hash` duplicate is deleted, `use common::sha256_hex;` is added (line 32), and both relock sites call `sha256_hex(spec_before.as_bytes())` / `sha256_hex(spec_after.as_bytes())` (1405, 1433). `common::sha256_hex` (`speccy-cli/tests/common/mod.rs:138`) is byte-identical (`sha2::Sha256::digest` -> `const_hex::encode`); the asserted invariant is unchanged and the four guards survive verbatim, so the size-invariant test remains non-vacuous after the refactor.

Evidence roll call is complete: REQ-007 covers only CHK-010, labelled `demonstrated` with the named test; the evidence command (`cargo test -p speccy-cli --test context`) is per-slice scoped, not the workspace hygiene run. The consistency-framing observation is correctly surfaced as a SPEC-prose note (already adjudicated non-blocking) — `normalize_consistency` (1391) replaces the same sentinel on both sides and masks no field that should legitimately differ; the REQ-004/foreign-journal leaks are caught by the independent guards, not the masked compare.
</review>
