---
spec: SPEC-0042
task: T-008
generated_at: 2026-05-23T00:00:00Z
---

<implementer date="2026-05-23T00:00:00Z" model="claude-opus-4-7[1m]" round="1">
- Completed: Updated `AGENTS.md` `### V1.0 outcome` to count-agnostic
  phrasing that defers the command list to `docs/ARCHITECTURE.md`.
  Updated `docs/ARCHITECTURE.md` CLI surface: softened the "Seven flat
  commands" intro, added `--include-archive` to the `status` flag list
  with a SPEC-0042 REQ-007 cite, added a SPEC-0042 REQ-005 note under
  the `vacancy` entry, and added a new `speccy archive SPEC-NNNN`
  entry alongside its `--reason`, `--force`, and `--json` flags.
  `CLAUDE.md` is a git symlink (mode 120000) to `AGENTS.md` so it
  tracks the edit automatically.
- Undone: nothing in scope.
- Hygiene checks: `cargo test --workspace` pass; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` pass;
  `cargo +nightly fmt --all --check` pass; `cargo deny check` pass.
- Evidence: paper trail at
  `.speccy/specs/0042-archive-completed-specs/evidence/T-008.md`.
  Roll call for CHKs under REQ-010:
  - CHK-025 (`AGENTS.md` has no "Seven-command" phrasing):
    demonstrated → `rg -n 'Seven-command|seven-command|7-command'
    AGENTS.md` prints zero matches, recorded in the evidence file.
  - CHK-026 (`docs/ARCHITECTURE.md` documents `speccy archive`
    inside the CLI surface section): demonstrated → `rg -nU 'speccy
    archive' docs/ARCHITECTURE.md` matches line 186, which sits
    inside the `# CLI Surface` section beginning at line 136.
- Discovered issues: none.
- Procedural compliance: doc-only task; followed AGENTS.md's
  "Surgical changes" rule by touching only the two prose blocks the
  task body called out, plus the one-word softening of the "Seven
  flat commands" intro in `docs/ARCHITECTURE.md` so that section is
  internally consistent with the new count-agnostic AGENTS.md
  language.
</implementer>

<review date="2026-05-23T11:30:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
REQ-010's `<done-when>` criteria are all satisfied. CHK-025 passes: `rg -n 'Seven-command|seven-command|7-command' AGENTS.md` returns zero matches. CHK-026 passes: `rg -nU 'speccy archive' docs/ARCHITECTURE.md` matches at line 186, inside the `# CLI Surface` section (header at line 136).

The replacement AGENTS.md V1.0-outcome line is count-agnostic, names `docs/ARCHITECTURE.md` as the authoritative surface, and contains no specific integer count. The "Stay small" item under `## Core principles` retains its "seven commands" phrasing — explicitly permitted by REQ-010's fourth done-when bullet ("The `## Core principles` section's 'Stay small' item is unmodified by this SPEC").

`docs/ARCHITECTURE.md` lines 186-195 document `speccy archive SPEC-NNNN` with `--reason`, `--force`, and `--json` flags. The two ancillary cross-references are present: `--include-archive` SPEC-0042 REQ-007 note at lines 150-151, and the SPEC-0042 REQ-005 note under `vacancy` at lines 182-185.

Advisory (non-blocking, outside REQ-010's strict scope): `docs/ARCHITECTURE.md` still contains two stale count references at lines 1618 and 1691. REQ-010's done-when does not require sweeping those mentions; a follow-up SPEC may want to address them.
</review>

<review date="2026-05-23T11:30:00Z" model="claude-opus-4-7[1m]/high" persona="tests" verdict="pass" round="1">
Doc-only task; both CHKs demonstrated via scoped `rg` runs that re-execute identically now. CHK-025 `rg -n 'Seven-command|seven-command|7-command' AGENTS.md` → zero matches. CHK-026 `rg -nU 'speccy archive' docs/ARCHITECTURE.md` → match at line 186 inside the `# CLI Surface` fenced block. Two extra task-scenario assertions also hold: `vacancy` entry notes archive scan with SPEC-0042 REQ-005 cite (`docs/ARCHITECTURE.md:182-185`), and `--include-archive` is documented on `status` only with REQ-007 cite (`docs/ARCHITECTURE.md:150-151`). Evidence roll call at `.speccy/specs/0042-archive-completed-specs/evidence/T-008.md` covers both CHKs with `demonstrated` labels and reproducible commands; no fabrication patterns matched.
</review>

<review date="2026-05-23T11:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Doc-only task: updates to `AGENTS.md` and `docs/ARCHITECTURE.md` only. No code paths, no auth boundaries, no input surfaces, no secret handling, and no dependency changes. All security-sensitive-looking terms in the diff are illustrative example prose inside the architecture documentation. No security concerns.
</review>

<review date="2026-05-23T11:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`AGENTS.md:127` introduces the phrase "seven commands" as part of the rewritten `## Core principles` principle 5 ("Stay small"), but the same diff adds `speccy archive` to `docs/ARCHITECTURE.md` (line 186), bringing the CLI surface to eight commands. These two edits land in the same commit and are mutually inconsistent.

The old `AGENTS.md` did not contain this "seven commands" line — the diff added it fresh. The task's scope (CHK-025) required removing the "Seven-command" count claim from the `### V1.0 outcome` bullet, which was done correctly, but the rewritten `## Core principles` block reintroduced a count claim elsewhere.

Fix: update `AGENTS.md:127` to match the actual count (eight) or — consistent with the count-agnostic approach applied to `### V1.0 outcome` — replace the hard count with "a small set of flat commands" and defer the authoritative list to `docs/ARCHITECTURE.md`.
</review>

<blockers date="2026-05-23T11:30:00Z" round="1">
One blocker raised by the style reviewer; verification shows the blocker rests on a factual error.

- **style**: claims `AGENTS.md:127` ("seven commands" in `## Core principles` "Stay small") was freshly introduced by the T-008 diff. **This is incorrect.** `git diff HEAD -- AGENTS.md` shows the T-008 diff only touches `### V1.0 outcome` (lines 47-54 region); `git show HEAD:AGENTS.md | sed -n '123,124p'` shows "five nouns ... seven commands" was already present in HEAD at line 124. The reviewer misread the diff origin. Furthermore, SPEC.md REQ-010 `<done-when>` item 4 (SPEC.md:823-825) explicitly mandates: "The `## Core principles` section's 'Stay small' item is unmodified by this SPEC (the principle survives the count removal)." Touching `AGENTS.md:127` would violate this SPEC gate, not satisfy it.

Round-2 directive: append a rebuttal `<implementer>` block that quotes the SPEC gate, cites the `git show HEAD:AGENTS.md` line-124 pre-existence proof, and re-flips state pending → in-review without modifying AGENTS.md:127. Do not touch `## Core principles` Stay small. The advisory about stale "seven-command/seven-verb" mentions at `docs/ARCHITECTURE.md` lines 1618 and 1691 is also out of scope for REQ-010 — leave them.
</blockers>

<implementer date="2026-05-23T12:00:00Z" model="claude-opus-4-7[1m]/low" round="2">
- Completed: nothing. This is a rebuttal round. No source or doc files
  were modified in round 2. The round-1 edits to `AGENTS.md`
  `### V1.0 outcome` and `docs/ARCHITECTURE.md` CLI surface stand
  unchanged.
- Undone: nothing. The style reviewer's requested "fix" at
  `AGENTS.md:127` is explicitly forbidden by SPEC.md REQ-010
  done-when item 4 (SPEC.md:824-825): "The `## Core principles`
  section's 'Stay small' item is unmodified by this SPEC (the
  principle survives the count removal)." Editing line 127 would
  fail REQ-010, not satisfy it.
- Hygiene checks: no code edits in round 2; the round-1 hygiene
  results (`cargo test --workspace`, `cargo clippy ... -D warnings`,
  `cargo +nightly fmt --all --check`, `cargo deny check`, all pass)
  remain authoritative since the tree is unchanged from round-1
  in-review state.
- Evidence: the style blocker rests on a factual error about diff
  origin. Proof that `AGENTS.md:127` "seven commands" predates this
  task:
  ```
  $ git show HEAD:AGENTS.md | grep -n "seven commands\|Stay small"
  123:5. **Stay small.** Five nouns (Mission, Spec, Requirement, Task, Check),
  124:   seven commands, no mode toggles, no orchestration runtime. Speccy
  ```
  HEAD's pre-T-008 AGENTS.md already contained "seven commands" at
  line 124 under `## Core principles` "Stay small". The T-008 diff
  does not touch that region. (Working-tree line numbering shifts
  slightly relative to HEAD because the round-1 `### V1.0 outcome`
  rewrite changed earlier line counts; the reviewer's quoted
  `AGENTS.md:127` is the post-diff position of the unchanged
  pre-existing line.) CHK roll call under REQ-010 unchanged from
  round 1:
  - CHK-025: demonstrated (round 1) — `rg -n
    'Seven-command|seven-command|7-command' AGENTS.md` returns zero
    matches.
  - CHK-026: demonstrated (round 1) — `rg -nU 'speccy archive'
    docs/ARCHITECTURE.md` matches line 186, inside the
    `# CLI Surface` section.
- Discovered issues: none. The style reviewer's advisory about stale
  count mentions at `docs/ARCHITECTURE.md` lines 1618 and 1691 is
  out of REQ-010 scope per the round-1 business reviewer note;
  a follow-up SPEC owns those.
- Procedural compliance: per AGENTS.md "Surgical changes",
  round 2 makes zero edits to `AGENTS.md`, `docs/ARCHITECTURE.md`,
  or any other source file. State flipped `pending` →
  `in-progress` at start of round and `in-progress` → `in-review`
  at exit, per the speccy-work skill protocol.
</implementer>

<review date="2026-05-23T12:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="2">
REQ-010's `<done-when>` items 1-4 all remain satisfied after the round-2 rebuttal. The rebuttal correctly refuses to edit `AGENTS.md:127` because done-when item 4 (SPEC.md:824-825) explicitly mandates "The `## Core principles` section's 'Stay small' item is unmodified by this SPEC". Verified the rebuttal's factual claim: `git show HEAD:AGENTS.md` line 124 contains "seven commands" pre-existing at HEAD. `git diff HEAD -- AGENTS.md` shows the T-008 diff only touches the `### V1.0 outcome` block at lines 47-54.

REQ-010 done-when roll call green:
- Item 1 (`rg -n 'Seven-command' AGENTS.md` → 0): CHK-025 demonstrated.
- Item 2 (`archive` in CLI section): CHK-026 demonstrated.
- Item 3 (replacement names `docs/ARCHITECTURE.md`, no integer count): confirmed.
- Item 4 (`## Core principles` "Stay small" unmodified): confirmed.
</review>

<review date="2026-05-23T12:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Doc-only task; round 2 is a rebuttal with zero file edits, so no test surface changed. CHK-025 and CHK-026 remain demonstrated by the round-1 scoped runners that re-execute identically against the unchanged tree. Independently verified the rebuttal's diff-origin claim: `git show HEAD:AGENTS.md | grep -n "seven commands"` returns the pre-existing line at HEAD:124, confirming the T-008 diff did not introduce the `## Core principles` "Stay small" wording the round-1 style reviewer flagged.
</review>

<review date="2026-05-23T12:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round 2 is a zero-edit rebuttal block. No files were modified. The security posture is identical to round 1: doc-only prose changes, no code paths, no auth boundaries, no input surfaces. Pass unchanged from round 1.
</review>

<review date="2026-05-23T12:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocking verdict was a factual error. Verified via `git diff HEAD -- AGENTS.md`: the T-008 diff only modifies the `### V1.0 outcome` section (lines 47-54 region), replacing a hard-coded command count with a pointer to `docs/ARCHITECTURE.md`. The phrase "seven commands" at `AGENTS.md:124` is in the untouched `## Core principles` "Stay small" item — it predates this task and the diff does not touch that section. No style violation introduced by T-008.
</review>
