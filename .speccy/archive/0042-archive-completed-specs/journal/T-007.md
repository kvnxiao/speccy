---
spec: SPEC-0042
task: T-007
generated_at: 2026-05-23T00:00:00Z
---

<implementer date="2026-05-23T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: pinned the REQ-006 invariant end-to-end with a new
  integration binary `speccy-cli/tests/archive_invisibility.rs`
  driving the real `speccy` binary via `assert_cmd` against a
  workspace where SPEC-0001 has been archived through the real
  `speccy archive` command. Five tests cover the three named CHKs
  (CHK-014/15/16) plus the supplementary `speccy lock` scenario
  from the task body and a `speccy next --json` invisibility
  regression guard. Documented the active-only contract on
  `speccy_core::workspace::scan` (the single shared hot-path
  discovery helper used by `status`/`next`/`check`/`verify`; `lock`
  globs `.speccy/specs/` directly via `locate_spec_dir`) with a
  paragraph naming SPEC-0042 REQ-006 and pointing at the
  `scan_archive_specs` opt-in as the archive-side counterpart.
- Undone: did not extract a new `discover_active_specs` wrapper. The
  task body conditioned the extraction on "if today's code paths
  each construct their own glob inline (rather than calling a shared
  helper)"; today they all route through `workspace::scan`, which
  already is the shared helper. Adding a single-call-site wrapper
  would be cosmetic indirection that violates the AGENTS.md
  "surgical changes" rule.
- Hygiene checks: `cargo test --workspace` green;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  green; `cargo +nightly fmt --all --check` green; `cargo deny check`
  green (`advisories ok, bans ok, licenses ok, sources ok`).
- Evidence: paper trail at `.speccy/specs/0042-archive-completed-specs/evidence/T-007.md`.
  Roll call for CHKs under REQ-006:
  - CHK-014 (status --json default omits archived spec):
    demonstrated → evidence scenario
    `status_json_default_omits_archived_spec`.
  - CHK-015 (`speccy check SPEC-0001` exits non-zero with a
    not-found message referencing SPEC-0001): demonstrated →
    evidence scenario
    `check_archived_spec_exits_nonzero_with_not_found_message`.
  - CHK-016 (`speccy verify` passes pre- and post-archive; JSON
    reflects only still-active specs): demonstrated → evidence
    scenario `verify_after_archive_exits_zero_and_omits_archived_spec`.
- Discovered issues: none. The shared `scan` helper already enforced
  the contract; the missing piece was the regression test pinning
  it and the doc-comment making the invariant explicit so a future
  glob widening cannot land silently.
- Procedural compliance: followed the `speccy-work` skill — flipped
  T-007 to `in-progress` before editing, kept the change surgical
  (one new test binary + one doc comment; no production code path
  changes), wrote real scoped runner output into the evidence file
  per the T-005 round-2 / T-006 pattern, and stopped after one task
  per the single-task primitive contract.

</implementer>

<review date="2026-05-23T11:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-007 covers REQ-006 cleanly. The new `speccy-cli/tests/archive_invisibility.rs` integration test binary pins all three named CHKs (CHK-014 `status --json` default, CHK-015 `check SPEC-0001` not-found, CHK-016 `verify` post-archive exit 0 with only active specs in JSON) end-to-end via the real `speccy` binary against workspaces archived through the real `speccy archive` command. Two supplementary tests pin REQ-006 done-when items 2 (`speccy next --json`) and 5 (`speccy lock` against archived ID exits non-zero, leaves archived TASKS.md byte-identical). All 5 tests green.

The implementer's decision to skip the optional `discover_active_specs` wrapper extraction is correct per the task body's conditional — all hot-path commands already route through `speccy_core::workspace::scan` or `lock::locate_spec_dir`. Doc-comment added to `scan` cites SPEC-0042 REQ-006 and points at the separate `scan_archive_specs` opt-in, future-proofing against silent glob widening.
</review>

<review date="2026-05-23T11:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
Tests for T-007 / REQ-006 are real and meaningful. CHK-014 asserts both `archived_count == 0` and `active_count == 1`, avoiding a trivial empty-output pass. CHK-015 asserts failure exit, stderr contains `SPEC-0001`, and stderr matches the real `check_selector` error phrasing. CHK-016 is the strongest: runs `verify` pre-archive (asserts success, making the post-archive assertion meaningful), archives via the real command, then asserts post-archive `verify --json` exits 0, stdout does not mention SPEC-0001, and `/summary/shape/specs == 1`.

Supplementary `lock_archived_spec_exits_nonzero_and_leaves_archive_untouched` captures archived TASKS.md bytes before the failed lock and asserts byte-equality after, catching both the visibility regression and any hypothetical write-into-archive regression.

Evidence file carries CHK-by-CHK roll call with `demonstrated` labels naming specific test functions. Scoped runner output (`cargo test -p speccy-cli --test archive_invisibility`) is per-binary not workspace-wide; re-ran and got matching shape.
</review>

<review date="2026-05-23T11:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. T-007 adds a doc comment to `workspace::scan` asserting the REQ-006 invariant and five integration tests that drive the real binary against ephemeral temp-dir workspaces. No new production code paths, no new inputs validated, no new secrets or credentials handled. Glob patterns in `scan` and `scan_archive_specs` are both anchored to the project root resolved by `find_root`; no path traversal risk. No new dependencies.
</review>

<review date="2026-05-23T11:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style checks pass. The new `speccy-cli/tests/archive_invisibility.rs` matches the project-wide test convention exactly: `#![allow(clippy::expect_used, reason = "...")]` is used by every existing test binary. The `#![expect(clippy::panic_in_result_fn, reason = "...")]` form is used for the remaining suppression, consistent with the rule that `#[expect]` is preferred. The two-line doc addition on `workspace::scan` is correctly scoped, names REQ-006, and cross-references `scan_archive_specs`. No dead imports, unused variables, or naming drift.
</review>
