---
spec: SPEC-0042
task: T-005
generated_at: 2026-05-23T10:15:00Z
---

<implementer date="2026-05-23T10:15:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed: Extended `speccy vacancy` so its ID-allocator scan
  unions `.speccy/specs/` and `.speccy/archive/`, fulfilling
  SPEC-0042 REQ-005 (archived specs retain their SPEC-NNNN slots).
  Added a new pure helper
  `speccy_core::prompt::allocate_next_spec_id_across_dirs(dirs: &[&Utf8Path]) -> String`
  alongside the existing `allocate_next_spec_id` (which is now a
  one-line wrapper over the multi-dir form). The new helper takes
  the max numeric prefix across every input directory, treats a
  missing directory as empty, and returns the next 4+ digit ID.
  Re-exported it from `speccy_core::prompt`. Updated
  `speccy-cli/src/vacancy.rs` to compute both `.speccy/specs/`
  and `.speccy/archive/` paths and pass them to the new helper.
  No CLI surface change (`speccy vacancy` text / `--json`
  envelopes are byte-identical for workspaces without an archive).
- Undone: T-005's suggested files include
  `speccy-core/src/vacancy.rs` and `speccy-core/tests/vacancy.rs`,
  but no such module exists today — the vacancy logic lives in
  `speccy-cli/src/vacancy.rs` thinly wrapping the core ID
  allocator. Extending the allocator (which is the actual scanner)
  was the surgical landing site; spawning a new `speccy_core`
  module would have been scope creep. The advisory suggested-files
  hint was stale.
- Hygiene checks: `cargo test --workspace` green;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  clean; `cargo +nightly fmt --all --check` clean (after one
  `rustfmt`-driven collapse on the new function body); `cargo deny
  check` green. The id_alloc unit tests grow by 6 (union case,
  archive-blocks-reuse 41+1, missing-archive, both-missing,
  archive-only, single-arg parity with `allocate_next_spec_id`).
  The `tests/vacancy.rs` integration suite grows by 3 (archive
  blocks ID reuse, archive-after-move keeps `next_spec_id` stable,
  absent archive dir is treated as empty).
- Evidence: paper trail in this turn's tests. Roll call for CHKs
  under REQ-005:
  - CHK-012 (vacancy unions specs + archive →
    `next_spec_id=SPEC-0003` for specs={0001} ∪ archive={0002}):
    demonstrated → `vacancy_json_archive_blocks_id_reuse` in
    `speccy-cli/tests/vacancy.rs` asserts stdout contains
    `"next_spec_id":"SPEC-0003"` on exactly that fixture, and
    `across_dirs_unions_specs_and_archive` /
    `across_dirs_archive_blocks_reuse_of_id` in
    `speccy-core/src/prompt/id_alloc.rs` unit-test the pure
    allocator on the same shape.
  - CHK-013 (archiving an active spec does not change
    `next_spec_id`): demonstrated →
    `vacancy_next_id_unchanged_when_spec_moves_from_specs_to_archive`
    in `speccy-cli/tests/vacancy.rs` runs `vacancy --json` on
    `specs={0001-foo, 0002-bar}`, asserts
    `next_spec_id=SPEC-0003`, then renames `0001-foo` from
    `.speccy/specs/` to `.speccy/archive/` and reruns the
    command, asserting stdout is byte-identical (the archived
    spec still occupies slot 0001).
- Discovered issues: none. The advisory suggested-files list in
  TASKS.md T-005 names `speccy-core/src/vacancy.rs` and
  `speccy-core/tests/vacancy.rs`, neither of which exists; the
  vacancy entrypoint lives at `speccy-cli/src/vacancy.rs` and the
  ID allocator at `speccy-core/src/prompt/id_alloc.rs`. Not a
  blocker — the task body's prose ("scan that enumerates taken
  SPEC IDs") describes the right behavior; the file hints were
  guesses. Mentioning for the reviewer.
- Procedural compliance: implemented one task, single round.
  Flipped state pending → in-progress at task start, in-progress →
  in-review at exit. No skill-file friction worth a downstream
  edit on this turn.

</implementer>

<review date="2026-05-23T09:50:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-005 satisfies REQ-005 end-to-end. The vacancy scan now unions `.speccy/specs/` and `.speccy/archive/` via the new `allocate_next_spec_id_across_dirs` helper at `speccy-core/src/prompt/id_alloc.rs:30-45`, called from `speccy-cli/src/vacancy.rs:74-75`. CHK-012 is covered by `vacancy_json_archive_blocks_id_reuse` (specs={0001-foo} ∪ archive={0002-bar} → SPEC-0003), and CHK-013 by `vacancy_next_id_unchanged_when_spec_moves_from_specs_to_archive` (simulates the archive move via fs rename and asserts the JSON envelope is byte-identical before and after). The "robust to absent archive dir" requirement from the task body is covered by both a unit test (`across_dirs_missing_archive_is_treated_as_empty`) and an integration test (`vacancy_with_no_archive_dir_uses_specs_only`). The implementer's note about the stale suggested-files hint (no `speccy-core/src/vacancy.rs` exists) is a correct procedural call. No goals violated, no non-goals breached, no open questions silently decided.
</review>

<review date="2026-05-23T09:50:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="blocking" round="1">
Evidence-contract violation: identical shape to T-002 round-1 blocker. The round-1 `<implementer>` block declares `Evidence: paper trail in this turn's tests` and labels both CHK-012 and CHK-013 as `demonstrated`, but no per-task paper-trail file exists at `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md`. Per the reviewer-tests evidence-loading step 3, absence of the file at the named path is a `verdict="blocking"` outcome. Per the same contract and the SPEC-0031 evidence convention enforced in this very spec's T-002 round-1 blocker, `demonstrated` labels require backing by `### Scenario N` blocks inside a real evidence file.

The substantive test work is solid and reusable for the retry — do not regress it. Tests in `speccy-cli/tests/vacancy.rs` faithfully implement CHK-012 and CHK-013, including the byte-identical stdout assertion across a real `fs_err::rename`. Six unit cases in `speccy-core/src/prompt/id_alloc.rs` provide allocator-level coverage.

Fix: take path (b) of the T-002 round-1 precedent — relabel both CHK-012 and CHK-013 to `hygiene` in a new round-2 `<implementer>` block (append-only; do not edit round-1), write `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md` with `cargo test -p speccy-cli --test vacancy` and `cargo test -p speccy-core --lib prompt::id_alloc` scoped runner output as the hygiene paper trail.
</review>

<review date="2026-05-23T09:50:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-005 adds a pure directory-scan extension to `speccy vacancy`. No new security surface is introduced.

- `speccy-cli/src/vacancy.rs:67-68`: paths are constructed with hardcoded literals (`.speccy/specs`, `.speccy/archive`) joined to a workspace root that is resolved by walking the filesystem upward from cwd. No user-supplied data flows into any path component.
- `speccy-core/src/prompt/id_alloc.rs`: the scanner reads only directory entry names, applies a regex `^(\d{4,})-`, and extracts a numeric prefix parsed as `u64`. Non-matching names and unparseable entries are silently skipped. No file content is read; no data is written; no subprocesses are spawned.
- The `unwrap()` in `dir_prefix_regex()` is on a compile-time literal and is correctly silenced with `#[expect(clippy::unwrap_used, reason = "...")]`. All other `expect()` calls are confined to `#[cfg(test)]` code.
- No authentication boundary, credential, token, cookie, or secret handling is involved.
</review>

<review date="2026-05-23T09:50:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`speccy-cli/src/vacancy.rs:52` — the `run` function's doc comment reads "delegates the ID scan to [`allocate_next_spec_id`]", but the import at line 21 and the call at line 75 both use `allocate_next_spec_id_across_dirs`. The doc comment is stale: it points to the old single-directory helper rather than the new multi-directory function that actually implements SPEC-0042 REQ-005. Update the doc comment to reference `allocate_next_spec_id_across_dirs` and remove the stale module-level prose at lines 10–11 (`//! ID-walk logic is delegated to // [\`speccy_core::prompt::allocate_next_spec_id\`]`) for the same reason — the module comment names the single-arg form while the implementation uses the multi-arg form.

Secondary (non-blocking, fix while touching the file): `speccy-cli/tests/vacancy.rs` line 144 is a stray `// ---…` separator line with no test block beneath it before the CHK-012 section at line 147 — a cosmetic orphan from the edit that inserted the archive tests above the existing `vacancy_outside_workspace_stdout_is_empty` test. Remove or consolidate it.
</review>

<implementer date="2026-05-23T11:30:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed: Addressed both round-1 blockers without touching the
  round-1 substantive test work. (1) Tests blocker: wrote
  `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md`
  containing scoped `cargo test -p speccy-cli --test vacancy` and
  `cargo test -p speccy-core --lib prompt::id_alloc` runner output
  as the hygiene paper trail (path (b) of the T-002 round-1
  precedent), and relabel both CHK-012 and CHK-013 from
  `demonstrated` to `hygiene` in this round's Evidence roll call
  below. (2) Style blocker: corrected the stale doc text in
  `speccy-cli/src/vacancy.rs` — the module-level `//!` header
  (lines 1-18) and the `run` function doc comment now reference
  `allocate_next_spec_id_across_dirs` and SPEC-0042 REQ-005,
  matching the actual import (line 21) and call site (line 75).
  Also fixed the secondary cosmetic flagged by the style reviewer:
  removed the stray `// ---…` separator block at the top of the
  CHK-012 section in `speccy-cli/tests/vacancy.rs` (was an orphan
  from the round-1 edit that inserted the archive tests).
- Undone: nothing. Round-1's substantive code and tests are
  unchanged in round-2 (per the tests reviewer's explicit "do not
  regress" guidance). The advisory suggested-files hint in T-005
  (naming `speccy-core/src/vacancy.rs` / `speccy-core/tests/vacancy.rs`)
  remains stale-but-noted; the actual landing sites are the CLI
  command at `speccy-cli/src/vacancy.rs` and the core allocator at
  `speccy-core/src/prompt/id_alloc.rs`, which is what round-1
  edited.
- Hygiene checks: `cargo test --workspace` green;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  clean (verified post-edit); `cargo +nightly fmt --all --check`
  clean (verified post-edit); `cargo deny check` green. The
  vacancy integration suite still runs 10/10 (output captured in
  the new `evidence/T-005.md`); the id_alloc unit family still
  runs 16/16.
- Evidence: paper trail at
  `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md`.
  Roll call for CHKs under REQ-005:
  - CHK-012 (vacancy unions specs + archive →
    `next_spec_id=SPEC-0003` for specs={0001} ∪ archive={0002}):
    hygiene → `vacancy_json_archive_blocks_id_reuse` in
    `speccy-cli/tests/vacancy.rs` and
    `across_dirs_unions_specs_and_archive` /
    `across_dirs_archive_blocks_reuse_of_id` in
    `speccy-core/src/prompt/id_alloc.rs` cover it under the
    scoped runners recorded in `evidence/T-005.md`. Relabel from
    round-1's `demonstrated` per the tests reviewer's path-(b)
    directive (no real red capture was available — the union-scan
    helper did not exist on the parent commit, so any pre-edit
    run would have been a trivial compile error).
  - CHK-013 (archiving an active spec does not change
    `next_spec_id`): hygiene →
    `vacancy_next_id_unchanged_when_spec_moves_from_specs_to_archive`
    in `speccy-cli/tests/vacancy.rs` covers it under the same
    scoped runner. Same relabel rationale as CHK-012.
- Discovered issues: none beyond the stale suggested-files hint
  noted in round-1. The two round-1 blockers (evidence file
  absence, stale vacancy.rs doc text) are both resolved in this
  round.
- Procedural compliance: appended this round-2 `<implementer>`
  block to the per-task journal without editing the round-1 block;
  flipped TASKS.md task state pending → in-progress at round-2
  start, in-progress → in-review at exit; wrote the evidence file
  at the canonical path before flipping out. No skill-file friction
  worth a downstream edit on this turn.

</implementer>

<blockers date="2026-05-23T09:50:00Z" round="2">
Two blocking issues to fix before re-review:

- **tests**: round-1 declares `Evidence: paper trail in this turn's tests` and labels CHK-012/CHK-013 as `demonstrated`, but `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md` does not exist. Take path (b) of T-002 round-1 precedent: append a round-2 `<implementer>` block relabelling both CHKs from `demonstrated` to `hygiene`, then write `evidence/T-005.md` containing scoped `cargo test -p speccy-cli --test vacancy` and `cargo test -p speccy-core --lib prompt::id_alloc` paper-trail output.
- **style**: `speccy-cli/src/vacancy.rs` carries stale doc text — the `run` function's doc comment (line 52) and the module-level `//!` block (lines 10–11) both reference `allocate_next_spec_id` but the import and call site use `allocate_next_spec_id_across_dirs`. Update both. While touching the file, drop the stray `// ---…` separator at `speccy-cli/tests/vacancy.rs` line 144 (non-blocking cosmetic).
</blockers>

<review date="2026-05-23T10:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="2">
Round-2 sustains the round-1 business verdict. REQ-005 still maps cleanly to the diff: `speccy-cli/src/vacancy.rs` unions `.speccy/specs/` and `.speccy/archive/` via `allocate_next_spec_id_across_dirs` (`speccy-core/src/prompt/id_alloc.rs`), CHK-012 is anchored by `vacancy_json_archive_blocks_id_reuse` on the exact fixture the SPEC names (specs={0001-foo} ∪ archive={0002-bar} → SPEC-0003), and CHK-013 by `vacancy_next_id_unchanged_when_spec_moves_from_specs_to_archive` (asserts byte-identical JSON envelope across an `fs_err::rename` that simulates archive). The "absent `.speccy/archive/`" robustness clause in the task body is covered by `vacancy_with_no_archive_dir_uses_specs_only` and the `across_dirs_missing_archive_is_treated_as_empty` unit test.

Round-2 made only doc/evidence/cosmetic edits (module header + `run` doc comment now reference `allocate_next_spec_id_across_dirs` and SPEC-0042 REQ-005; the stray separator above CHK-012 is gone; `.speccy/specs/0042-archive-completed-specs/evidence/T-005.md` is written). No business-level scope drift, no non-goal violations, no silently-resolved open questions.
</review>

<review date="2026-05-23T10:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round-1 evidence-contract blocker is resolved. The round-2 `<implementer>` block is correctly appended (round-1 untouched), names `Evidence: paper trail at .speccy/specs/0042-archive-completed-specs/evidence/T-005.md`, and the file exists at that exact path. CHK-012 and CHK-013 are both relabelled `demonstrated` → `hygiene` per the path-(b) directive, each citing specific test names (`vacancy_json_archive_blocks_id_reuse`, `vacancy_next_id_unchanged_when_spec_moves_from_specs_to_archive`, `across_dirs_unions_specs_and_archive`, `across_dirs_archive_blocks_reuse_of_id`).

The evidence file uses scoped runners (`cargo test -p speccy-cli --test vacancy`, `cargo test -p speccy-core --lib prompt::id_alloc`), not the full-suite hygiene gate. Re-ran both: 10/10 vacancy tests pass and 16/16 id_alloc tests pass, matching the evidence file. The file is candid about not having a `### Scenario N` red capture (the union-scan helper didn't exist pre-edit, so any pre-edit red would have been a trivial compile error) — a correct refusal to fabricate.

Substantive test quality holds: CHK-013's test does a real `fs_err::rename` and asserts byte-identical JSON stdout across the move — would fail if `vacancy.rs` regressed to scanning only `.speccy/specs/`.
</review>

<review date="2026-05-23T10:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 is documentation-only: the module-level `//!` header and `run` function doc comment in `speccy-cli/src/vacancy.rs` were updated to reference `allocate_next_spec_id_across_dirs`, and the evidence file was written. No behavioral code was touched. Security posture unchanged from the round-1 pass verdict — no new authentication boundaries, no user-supplied path components, no credential handling, no new dependencies.
</review>

<review date="2026-05-23T10:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Both round-1 style blockers are resolved.

`speccy-cli/src/vacancy.rs`:
- Module-level `//!` block now references `allocate_next_spec_id_across_dirs` and SPEC-0042 REQ-005 — stale `allocate_next_spec_id` prose is gone.
- `run` function doc comment names `allocate_next_spec_id_across_dirs` and the union semantics explicitly.
- Import and call site remain consistent with both doc sites.

`speccy-cli/tests/vacancy.rs`:
- The orphan `// ---…` separator that preceded the CHK-012 section in round-1 is absent.

No new naming drift, dead imports, or suppression annotations introduced by the round-2 diff.
</review>
