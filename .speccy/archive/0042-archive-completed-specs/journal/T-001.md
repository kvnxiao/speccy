---
spec: SPEC-0042
task: T-001
generated_at: 2026-05-23T18:00:00Z
---

<implementer date="2026-05-23T18:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Extended `SpecFrontmatter` and `RawFrontmatter` in
  `speccy-core/src/parse/spec_md.rs` with two optional fields,
  `archived_at: Option<jiff::civil::Date>` and `archived_reason:
  Option<String>`, both `#[serde(default)]`. Added them to
  `HASH_EXCLUDED_FRONTMATTER_FIELDS` so `canonical_frontmatter_for_hash`
  skips them when feeding bytes to `Sha256`. Updated the doc comment on
  `SpecMd.sha256` (frontmatter exclusion list and SPEC-0042 DEC-001
  cross-reference). Renamed the legacy
  `hash_excluded_frontmatter_fields_contains_only_status` test to
  `hash_excluded_frontmatter_fields_set` and asserted the exclusion set
  is exactly `["archived_at", "archived_reason", "status"]`. Added
  three new unit tests:
  `sha256_invariant_under_archive_fields_addition`,
  `archive_fields_parse_when_present`,
  `archive_fields_absent_when_missing`. Updated the `sample_frontmatter()`
  helper and the supersession test-helper `fake_spec()` to construct
  the new `None`-defaulted fields. Wired the two frontmatter fields
  into `JsonSpec` (`speccy-cli/src/status_output.rs` and `status.rs`)
  using `#[serde(skip_serializing_if = "Option::is_none")]` so JSON
  output for non-archived specs is byte-identical to pre-SPEC-0042.
- Undone: None within T-001's scope. The CLI `archive` subcommand,
  warning logic, vacancy scan extension, and `status --include-archive`
  flag belong to downstream tasks T-002…T-008.
- Hygiene checks: `cargo test --workspace --all-features` (all
  passing, including the four new spec_md tests). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` clean.
  `cargo +nightly fmt --all --check` clean. `cargo deny check` clean
  (the two `unmatched license allowance` warnings are pre-existing
  workspace-level deny.toml entries, not caused by this change).
- Evidence: no separate paper-trail file required; the four new
  spec_md tests are the demonstrated evidence for the two REQs.
  Roll call for CHKs under REQ-003 / REQ-004:
  - CHK-007 (`--reason` writes `archived_at` + `archived_reason` in
    order after `supersedes:`): judgment-only at this layer →
    T-001 only adds the parser and JSON shape. The frontmatter-mutation
    write path is T-002's responsibility; this task ensures the
    fields are recognised when they appear on disk.
  - CHK-008 (no `archived_reason` when `--reason` omitted):
    judgment-only at this layer → same as CHK-007; T-001 confirms
    the field stays `None` when absent (test
    `archive_fields_absent_when_missing`).
  - CHK-009 (newline in `--reason` rejected at clap parse time):
    judgment-only at this layer → clap value-parser belongs to T-002.
  - CHK-010 (sha256 invariant under archive-field addition):
    demonstrated → test `sha256_invariant_under_archive_fields_addition`
    parses two fixtures (with and without `archived_at` /
    `archived_reason`) and asserts byte-equal `SpecMd.sha256`.
  - CHK-011 (sha256 still changes on body byte edit):
    hygiene → the pre-existing test `sha256_changes_on_one_byte_edit`
    covers this regression guard and continues to pass.
- Discovered issues: None.
- Procedural compliance: Followed the speccy-work skill steps —
  flipped T-001 to `in-progress` before editing code, re-read REQ-003
  and REQ-004 (with their CHKs) before writing tests, kept the change
  surgical (no touching the `archive` subcommand surface or any
  downstream task scope), and ran all four hygiene gates locally
  before flipping to `in-review`. No shipped skill files needed
  amending for this task.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:39:41Z" round="1">
T-001's slice is correctly scoped to parser support + JSON shape; the
CLI-mutation half of REQ-003 (writing `archived_at` / `archived_reason`
into frontmatter) is explicitly deferred to T-002, and the journal roll
call labels CHK-007 / CHK-008 / CHK-009 as `judgment-only at this layer`
matching that deferral. REQ-004's done-when items are all met by the
diff: `HASH_EXCLUDED_FRONTMATTER_FIELDS` now contains all three strings
(`"status"`, `"archived_at"`, `"archived_reason"`), and the public
`SpecMd.sha256` rustdoc was updated to mention both new fields and
cross-reference SPEC-0042 DEC-001. No non-goals violated (no unarchive
command, no bulk archive, no auto-recursion). No silent resolution of
open questions (the SPEC's `## Open Questions` section is empty).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:39:41Z" round="1">
The four CHKs under T-001's covered REQs are covered exactly as the
roll call claims: CHK-010 (hash invariance under archive-field
addition) → `sha256_invariant_under_archive_fields_addition` parses
two fixtures differing only by the two new YAML keys and asserts
byte-equal `SpecMd.sha256`; CHK-011 (body-byte edit still changes
sha256) → pre-existing `sha256_changes_on_one_byte_edit` continues to
pass (regression guard cited verbatim in the roll call). Two
additional unit tests cover the parser surface itself:
`archive_fields_parse_when_present` asserts both `Some(...)` values
parse correctly; `archive_fields_absent_when_missing` asserts both
`Option` fields default to `None`. `hash_excluded_frontmatter_fields_set`
locks the exclusion set literal so a future silent addition is a
deliberate edit. The `Evidence:` field on the round-1 `<implementer>`
deviates from the canonical "name a per-task evidence file" shape — it
inlines the justification rather than naming a path — but for a pure
parser/data-structure addition with no CLI surface to red-then-green
against, the in-source unit tests are themselves the concrete
demonstration, and the cited hygiene test
(`sha256_changes_on_one_byte_edit`) actually exists at
`speccy-core/src/parse/spec_md.rs:656`. Full workspace test run
exits 0 (verified locally during this review).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:39:41Z" round="1">
No security-relevant surface introduced. The two new frontmatter
fields are `Option<jiff::civil::Date>` and `Option<String>`, deserialised
via the existing serde-saphyr path with `#[serde(default)]` so absent
keys do not error. No new I/O, no subprocess invocation, no
user-controlled path expansion, no error-message interpolation of
attacker-influenced content. The hash exclusion of `archived_at` /
`archived_reason` is a deliberate identity-stability design (SPEC-0042
DEC-001), not a silent omission — the `HASH_EXCLUDED_FRONTMATTER_FIELDS`
constant is small, alphabetically sorted, and inspectable; future
additions require a deliberate constant edit. The newline-rejection on
`--reason` happens at clap value-parser time in T-002, not here, so
this slice cannot smuggle a YAML-injection vector. JSON serialisation
uses `skip_serializing_if = "Option::is_none"` matching the existing
`mission_md_path` convention; archived specs surface their fields via
T-006's `--include-archive` opt-in path rather than leaking into
default `status` output.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:39:41Z" round="1">
Diff stays within the suggested files (`speccy-core/src/parse/spec_md.rs`,
`speccy-core/src/parse/supersession.rs`, `speccy-cli/src/status.rs`,
`speccy-cli/src/status_output.rs`). The `HASH_EXCLUDED_FRONTMATTER_FIELDS`
literal is alphabetically sorted (`"archived_at", "archived_reason",
"status"`), matching the existing convention. New `pub` struct fields
carry rustdoc comments cross-referencing SPEC-0042 REQ-003. The two
new `RawFrontmatter` fields use `#[serde(default)]` for absent-key
tolerance, matching the existing `supersedes` pattern. The JSON serde
attributes use `skip_serializing_if = "Option::is_none"` consistent
with the pre-existing `mission_md_path` field. Tests use `indoc!` raw
strings and `expect("descriptive message")` per project conventions.
No `unwrap()` / `panic!()` / `todo!()` / `#[allow(...)]` introduced in
production code. `cargo +nightly fmt --all --check` clean,
`cargo clippy --workspace --all-targets --all-features -- -D warnings`
clean (verified locally during this review). Minor docs nit (not
blocking): the doc comment on the private `canonical_frontmatter_for_hash`
function (line 273) still reads "(`status` today)" — the parenthetical
became stale when the exclusion set grew. The public `SpecMd.sha256`
doc was correctly updated, so `cargo doc -p speccy-core` does surface
both new fields; the internal helper's lagged comment is a docs-only
inconsistency a follow-up can polish without blocking this slice.
</review>
