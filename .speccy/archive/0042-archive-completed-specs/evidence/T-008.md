# Evidence: SPEC-0042 / T-008

Doc-only task. Two CHKs, both demonstrated via scoped runners against
the source tree at HEAD after the task's edits.

## CHK-025 — `AGENTS.md` contains no integer-count phrasing

Command:

```
rg -n 'Seven-command|seven-command|7-command' AGENTS.md
```

Output:

```
(zero matches; rg exits 1)
```

`AGENTS.md` line 50–55 now reads "A lean Rust CLI implementing the
surface in `docs/ARCHITECTURE.md`. ..." with no integer count of
commands and a pointer to `docs/ARCHITECTURE.md` as authoritative.

## CHK-026 — `docs/ARCHITECTURE.md` documents `speccy archive`

Command:

```
rg -nU 'speccy archive' docs/ARCHITECTURE.md
```

Output:

```
186:speccy archive SPEC-NNNN          Relocates a shipped, dropped, or superseded SPEC
```

The match falls inside the `# CLI Surface` section (header at line
136, fenced command block starting at line 142), satisfying the
"inside the CLI command surface section" requirement of CHK-026.

## Hygiene roll-up

- `cargo test --workspace` — pass
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — pass
- `cargo +nightly fmt --all --check` — pass (no output)
- `cargo deny check` — pass (`advisories ok, bans ok, licenses ok, sources ok`)
