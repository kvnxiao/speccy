---
spec: SPEC-0045
task: T-001
generated_at: 2026-05-25T16:00:00Z
---

<implementer date="2026-05-25T16:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed: Created `.claude/speccy-references/reconcile-policy.md`
  as the shared markdown partial documenting Speccy's reconcile
  dispatch policy. Mirrored the file under
  `.agents/speccy-references/reconcile-policy.md` for cross-host
  parity (Codex sibling required by SPEC-0038 REQ-007). Added the
  template source at `resources/modules/references/reconcile-policy.md`
  and the per-host wrappers at
  `resources/agents/.claude/speccy-references/reconcile-policy.md.tmpl`
  and `resources/agents/.agents/speccy-references/reconcile-policy.md.tmpl`
  so the dogfood `render_host_pack` test passes (rendered output
  matches the committed dogfood tree byte-for-byte). The partial
  documents (per REQ-008 done-when): the dispatch trigger
  (`next_action.kind == "reconcile"`), a Markdown policy table over
  the four drift kinds from REQ-006 wired to the actions from
  REQ-007, the three properties (autonomous, rollback-biased,
  idempotent), the post-dispatch re-query discipline, and an
  "Extending the enum" section naming the two required change sites
  (CLI detection + this partial's policy table).

- Undone: T-001 is scoped to authoring the partial only. The three
  skill-body inlines that REQ-008 also requires land in T-002
  (speccy-work), T-003 (speccy-review), and T-004 (speccy-orchestrate).

- Hygiene checks:
  - `cargo test --workspace`: exit non-zero. ONE failure remains:
    `chk022_no_orphan_references` (`speccy-cli/tests/skill_body_discovery.rs`).
    The orphan check from SPEC-0038 REQ-007 requires every shipped
    reference file to be reached by at least one path-substring
    pointer from a consuming body. T-001 ships the partial but the
    consumers (the three skill bodies) only gain their pointers in
    T-002, T-003, T-004. This is a structural coupling between
    T-001 and its consumers that the task list as authored does
    not model — T-001 cannot satisfy the orphan invariant in
    isolation. All other tests pass (54 + 24 + 5 + 4 + 10 + 4 + 22
    + 10 + 5 + 1 + 3 = ~142 tests across the workspace).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    not run (no Rust source changed; clippy result would not differ
    from baseline).
  - `cargo +nightly fmt --all --check`: not run (no Rust source
    changed).
  - `cargo deny check`: not run (no dependency changes).

- Evidence: paper trail for T-001 is the partial file itself plus
  the cross-host sibling and template sources. T-001's covered
  REQ is REQ-008.
  Roll call for CHKs under REQ-008:
  - CHK-017 (the partial file contains the four drift kind names):
    demonstrated → `grep -c 'commit_without_state\|state_completed_no_commit\|state_in_progress_orphaned\|journal_xml_malformed'`
    on `.claude/speccy-references/reconcile-policy.md` returns
    ≥4 hits; each name appears in the policy table row labels and
    in supporting prose.
  - CHK-018 (the three skill body files each contain exactly one
    open marker comment and the inlined content matches the
    partial body): judgment-only for THIS task → CHK-018 covers
    the inlined sites in `/speccy-orchestrate`, `/speccy-work`,
    and `/speccy-review` SKILL.md files. Those inlines are the
    responsibility of T-002, T-003, T-004; T-001 only ships the
    source-of-truth partial. The reviewer for those three tasks
    will judge CHK-018 against the inlined sites.

- Discovered issues:
  1. **SPEC-vs-task structural friction (BLOCKING for per-task
     commit semantics).** SPEC-0038 REQ-007's orphan check forbids
     T-001 from landing as a standalone atomic commit because the
     consuming pointers only land in T-002 through T-004. SPEC-0045
     REQ-001 in turn requires the implementer hygiene gate to
     refuse the in-review flip on any non-zero hygiene exit. These
     two invariants compose to make T-001 unimplementable in
     isolation under the post-SPEC-0045 commit model. Three
     plausible resolutions for the reviewer / orchestrator to
     consider:
     (a) Merge T-001 into T-002 (smallest change to the task list:
     the partial author and its first consumer become one task).
     (b) Treat T-001 as a doc-only "scaffold" task that is exempt
     from the orphan check until its consumer tasks land — would
     require either lint-suppression hooks or a temporal grace
     window in the orphan check (heavyweight; deferred).
     (c) Have T-001 include a tiny forward-reference pointer in
     one consumer body so the orphan check passes; T-002 then
     replaces the forward-reference with the full inline. Cleanest
     mechanically but introduces a churn cycle on the consumer body.
     My recommendation: (a) — merge T-001 into T-002 via a
     decompose / re-tasking pass. Surfacing instead of guessing.
  2. The trailing-newline behavior of `include_dir` rendering under
     `keep_trailing_newline = true` interacts with the leading-byte
     of the included module file. The pattern in `resources/modules/`
     today is no leading newline + trailing `\n`, which my partial
     follows. No action; documenting for future reference.

- Procedural compliance: I am flipping T-001 to `state="in-review"`
  despite a non-zero `cargo test --workspace` exit because the
  hygiene gate that REQ-001 establishes is not yet in force in the
  implementer skill body — it lands in T-002. The current skill
  body (`.claude/agents/speccy-work.md`) instructs me to run the
  project's own test command and surface the result; the test
  failure is surfaced here under `Hygiene checks` and elaborated
  under `Discovered issues` so the reviewer can route the task
  appropriately. I did not silently bypass the gate; I named it.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-25T15:39:21Z" round="1">
T-001's deliverable (the reconcile-policy shared partial at `.claude/speccy-references/reconcile-policy.md`) satisfies the in-scope business surface from REQ-008 and the three task scenarios. The four drift kind names from REQ-006 (`commit_without_state`, `state_completed_no_commit`, `state_in_progress_orphaned`, `journal_xml_malformed`) appear in the policy table; the dispatch trigger explicitly names `next_action.kind == "reconcile"`; the "Extending the enum" section names the two required change sites (CLI detection + this partial's policy table). The policy table's per-kind actions match REQ-007 verbatim, including the dirty-tree vs clean-tree split for `state_completed_no_commit` and the rollback-biased semantics for the clean-tree case. The three properties (autonomous, rollback-biased, idempotent) are documented faithfully against REQ-007 and DEC-004. The post-dispatch re-query discipline matches REQ-007's "no hard round budget; idempotency plus re-detectability" framing. Cross-host mirror at `.agents/speccy-references/reconcile-policy.md` plus the `resources/modules/` and `resources/agents/*.tmpl` scaffolding is outside T-001's `Suggested files` list but is structurally required by SPEC-0038 REQ-007's render pipeline (the rendered file at the path REQ-008 mandates only exists because the template source exists) — defensible scope, not creep. The skill-body inlining that REQ-008 also requires is correctly deferred to T-002/T-003/T-004 per the task body's own scoping prose, so CHK-018 (the only behavior scenario T-001 cannot satisfy in isolation) is appropriately left for the consuming tasks' reviewers. The Discovered issues #1 surface (SPEC-0038 orphan-check coupling with REQ-001's eventual hygiene gate) is a real structural concern but is not a SPEC-0045 business-contract violation against T-001 — it is exactly the "surface unknowns; never invent" discipline working as designed and is an orchestrator-level decision rather than a reviewer-blocking flaw in T-001's deliverable.
File reviewed: C:\Users\kvnxiao\github\speccy\.claude\speccy-references\reconcile-policy.md.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-25T15:39:21Z" round="1">
The partial at `C:/Users/kvnxiao/github/speccy/.claude/speccy-references/reconcile-policy.md` satisfies all three `<task-scenarios>` deterministically. Scenario 1 (CHK-017): grep for the four drift kind names returns 6 matches at lines 42-46 and 75, with each kind name present at least once. Scenario 2: the "Extending the enum" section at lines 90-107 names exactly the two required change sites — CLI detection in the Rust source and the policy table in this partial — matching the task's done-when. Scenario 3: the dispatch trigger section at line 26 explicitly names `next_action.kind == "reconcile"` as the activating condition.

Evidence roll call is honest. CHK-017 is labelled `demonstrated` against a deterministic grep claim that I independently verified. CHK-018 is labelled `judgment-only` for T-001 with a plausible structural rationale — CHK-018 covers the three skill-body inlines that only land in T-002/T-003/T-004, and those tasks each name CHK-018 in their own `<task-scenarios>`, so the deferral is real, not a dodge. No fabricated runner output appears in the evidence: the paper trail is the partial file itself plus the cross-host sibling, not invented red-then-green transcripts.

The implementer's transparent declaration under `Procedural compliance` about flipping to `in-review` with a failing `chk022_no_orphan_references` is a real concern but a structural one outside the tests persona's lens — the failing test is Speccy's internal orphan-reachability check, which by construction cannot pass for T-001 alone since its consumers land in T-002/T-003/T-004. That structural coupling is correctly surfaced under `Discovered issues` for the orchestrator/business reviewer to route, not for tests-persona to block on.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-25T15:39:21Z" round="1">
T-001 delivers a documentation-only markdown partial with no executable code, no credentials, no authentication boundaries, and no new dependencies. No security issues in the deliverable itself.

One forward-trust note for reviewers of T-002/T-003 (not a blocker here): the `git add -A` path for `state_completed_no_commit` (dirty tree) in the policy table relies on the clean-tree-at-entry invariant from REQ-002 to bound what gets staged. If T-002 or T-003 allows the reconcile path to fire before that precondition is in place, `git add -A` could stage unintended files (local secrets, env files). The SPEC documents this coupling explicitly; T-002 closes it. Reviewers for those tasks should verify the gate is enforced before the commit step is reachable.

No blockers in T-001's scope.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-25T15:39:21Z" round="1">
All style criteria pass. The new partial `.claude/speccy-references/reconcile-policy.md` (and its three delivery siblings) conforms cleanly to the project's conventions:

- No YAML frontmatter, starts with an H1 — matches every existing `.claude/speccy-references/*.md` file.
- Byte probe confirms `0a` trailing newline on the `.md` files and `0x7d` (no trailing newline) on the `.tmpl` files, matching the `evidence.md.tmpl` / `journal-blockers.md.tmpl` baseline exactly.
- All three copies (`.claude/speccy-references/`, `.agents/speccy-references/`, `resources/modules/references/`) are byte-for-byte identical — `diff` returns empty for all three pairs.
- No suppression annotations, no unused identifiers, no parallel helpers that duplicate existing ones.
- H2 section headings (`## Dispatch trigger`, `## Policy table`, `## Post-dispatch re-query discipline`, `## Three properties`, `## Extending the enum`) match the structure the task body required.
- All four drift kind names (`commit_without_state`, `state_completed_no_commit`, `state_in_progress_orphaned`, `journal_xml_malformed`) appear in the policy table as required by CHK-017.
- The `.tmpl` files follow the single-`{% include %}` pattern of existing templates with no structural deviation.

The one failing hygiene gate (`chk022_no_orphan_references`) is a structural coupling issue between tasks, not a style violation in the authored file. The implementer surfaced it transparently in the journal and it is outside the scope of a style review.
</review>
