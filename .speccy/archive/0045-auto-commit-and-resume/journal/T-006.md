---
spec: SPEC-0045
task: T-006
generated_at: 2026-05-25T21:30:00Z
---

<implementer date="2026-05-25T21:30:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Added `speccy-cli/tests/consistency.rs` — six end-to-end integration tests driving the production `speccy_cli::next::run` entry point against real on-disk git fixtures built per-test by a `make_fixture` helper that runs `git init --initial-branch=main` + local `user.email` / `user.name` / `commit.gpgsign=false` config in a tempdir, lays down `.speccy/specs/0099-fixture/{SPEC.md,TASKS.md,journal/T-NNN.md}`, and seeds one bootstrap commit so `git log --grep` queries against `[SPEC-0099/T-NNN]:` titles return reliably. The six tests are: `state_completed_no_commit_with_dirty_tree_is_blocking` (CHK-012), `journal_xml_malformed_reports_kind_path_and_offset` (CHK-013; on-disk fixture with unclosed `<review>` tag plus a matching commit to isolate the journal drift), `commit_without_state_reports_40_hex_sha` (CHK-010; asserts 40 hex chars + SHA equals the seeded HEAD), `state_in_progress_orphaned_reports_dirty_files_count` (asserts `dirty_files_count == 4`), `ok_status_when_completed_task_has_matching_commit` (asserts `status == "ok"` and `next_action.kind != "reconcile"`), and `no_mutating_git_commands_in_source` (CHK-011; recursively walks `speccy-cli/src/` and `speccy-core/src/`, skips lines whose first non-whitespace token starts with `//`, `///`, `//!`, `/*`, or `*`, and asserts none of `git add`, `git commit`, `git restore`, `git clean`, `git stash` appear). T-005 round 2 already landed `speccy-core/tests/consistency_detect.rs` covering all four drift kinds via FakeProbe at the `detect()` boundary, so this task focuses on the orthogonal layer T-005 punted on: real git repos driving the public CLI `run` API and the source-grep CHK-011 regression guard.

- Undone: T-007 (architecture doc update) remains. Nothing else in T-006 punted.

- Hygiene checks:
  - cargo test --workspace: exit 0
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0

- Evidence: paper trail at `speccy-cli/tests/consistency.rs` (six integration tests against real git repos via `speccy_cli::next::run`). Roll call for CHKs under REQ-005 / REQ-006 in scope for T-006:
  - CHK-010 (commit_without_state drift, status drift, severity auto_fixable, 40-hex commit_sha): demonstrated → `commit_without_state_reports_40_hex_sha` builds a real git repo with TASKS.md T-002 at `in-review`, creates a `[SPEC-0099/T-002]: some task` commit via `git commit -m`, drives `run`, parses the JSON, and asserts `consistency.status == "drift"`, `kind == "commit_without_state"`, `severity == "auto_fixable"`, `details.commit_sha.len() == 40`, every char is `is_ascii_hexdigit`, the SHA matches the seeded HEAD, and `next_action.kind == "reconcile"`.
  - CHK-011 (no mutating git commands in src/): demonstrated → `no_mutating_git_commands_in_source` walks `speccy-cli/src/` and `speccy-core/src/` recursively, strips comment-only lines, asserts the five mutating invocations appear nowhere. The test currently passes (the only references inside `speccy-core/src/consistency.rs` are inside `//!` module doc-comment lines that the comment filter skips).
  - CHK-012 (state_completed_no_commit, severity blocking, working_tree_dirty true): demonstrated → `state_completed_no_commit_with_dirty_tree_is_blocking` builds a fixture with TASKS.md T-001 at `completed`, seeds two tracked files via `git add -A && git commit -m` then re-writes them so porcelain reports modifications, drives `run`, and asserts `status == "blocked"`, exactly one `state_completed_no_commit` entry on T-001, `severity == "blocking"`, `details.working_tree_dirty == true`, and `next_action.kind == "reconcile"`.
  - CHK-013 (journal_xml_malformed kind with journal_path matching on-disk path + byte offset at close of `<implementer>`): demonstrated → `journal_xml_malformed_reports_kind_path_and_offset` writes a journal body with a clean `<implementer>...</implementer>` followed by an unclosed `<review persona="business"` (matches the SPEC fixture body byte-for-byte), pairs it with a matching commit so the only drift is journal-malformed, drives `run`, asserts `kind == "journal_xml_malformed"`, `details.journal_path` ends with `journal/T-001.md`, and `details.last_well_formed_byte_offset` equals `body.find("</implementer>") + "</implementer>".len()`.
  Out-of-scope-for-T-006 CHK roll-call:
  - CHK-009 (state_completed_no_commit, status blocked, next_action.kind reconcile): hygiene → covered by T-005's `consistency_detect.rs::detect_state_completed_no_commit_dirty_tree_is_blocking` plus `apply_reconcile_override` tests in `next_output.rs`; this task's CHK-012 fixture incidentally re-exercises the same shape end-to-end through the JSON envelope.

- Discovered issues: None. The shipped skill body and the task body together were sufficient to land this work in one round.

- Procedural compliance: Followed the speccy-work skill steps — read scenarios via `speccy check SPEC-0045/T-006`, flipped state to `in-progress` before any edit, ran the four hygiene gates after implementation (`cargo +nightly fmt --all` re-formatted the new test file once; the subsequent `--check` was clean), flipped state to `in-review`, append this `<implementer round="1">` block to the per-task journal file. No shipped-skill friction surfaced.

</implementer>

<review date="2026-05-26T03:10:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-006 lands six integration tests in `speccy-cli/tests/consistency.rs` exercising the four drift kinds, the all-healthy `consistency.status == "ok"` path, and the `no mutating git commands in source` regression guard. Mapping from task scenarios to implementation: CHK-010 covered by `commit_without_state_reports_40_hex_sha`; CHK-011 by `no_mutating_git_commands_in_source`; CHK-012 by `state_completed_no_commit_with_dirty_tree_is_blocking`; CHK-013 by `journal_xml_malformed_reports_kind_path_and_offset`; orphan path by `state_in_progress_orphaned_reports_dirty_files_count`; happy path by `ok_status_when_completed_task_has_matching_commit`. Every test drives `speccy_cli::next::run` against a real on-disk git repo. Goals served, non-goals respected, hygiene gates all exit 0. No business-side drift.
</review>

<review date="2026-05-26T03:10:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
Six tests drive the production `speccy_cli::next::run` entry point against real on-disk git fixtures and assert against the parsed JSON envelope. Non-vacuity verified per test: CHK-012 fails if dirty flag or reconcile override regresses; CHK-013 offset assertion would fail if production shortcuts to 0; CHK-010 asserts SHA equals seeded HEAD, not just any 40-hex; CHK-011 source-grep would surface a future `Command::new("git").arg("add")` since the comment filter does not suppress it. Non-overlapping with T-005's `speccy-core/tests/consistency_detect.rs` — that file drives `detect()` directly via `FakeProbe` against Rust types; this file drives the full CLI through `serde_json::Value`. Different layers, complementary signal. Evidence roll call accurate; hygiene gates exit 0.
</review>

<review date="2026-05-26T03:10:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
All six integration tests free of security issues. Subprocess isolation: every `Command` uses `.arg()`/`.args()` (not a shell string), stdin `Stdio::null()`, stderr discarded. `--grep` argument construction: `regex_escape` sanitizes prefix; task ID input constrained to `^T-\d{3,}$` by parser. Path construction in `detect_journal_drift` (`spec.dir.join("journal").join(format!("{task_id}.md"))`) blocks traversal via the same task ID validation. Test fixtures use `tempfile::tempdir()` (OS-assigned unpredictable paths) with local identity config — no cross-test contamination. Read-only posture confirmed and now regression-guarded by CHK-011 source-grep.
</review>

<review date="2026-05-26T03:10:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All conventions satisfied. `#[allow(clippy::expect_used, reason = "...")]` paired with `#[expect(clippy::panic_in_result_fn, reason = "...")]` matches the pattern in `check.rs`, `next_json.rs`, and other test files. No `unwrap()` calls; all `.expect()` uses carry descriptive messages. No slice indexing; all `serde_json::Value` accesses go through `.get()` / `.pointer()` / `.as_array()` / `.as_str()`. Local helpers (`git`, `make_fixture`, `dirty_tracked`, `task_commit`, `walk_rs_files`) serve a git-repo-backed fixture purpose with no equivalent in `common/mod.rs` — the parallel definitions are justified rather than duplicative. Import ordering matches the surrounding test suite style.
</review>
