---
spec: SPEC-0045
task: T-005
generated_at: 2026-05-25T20:00:00Z
---

<implementer date="2026-05-25T20:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Added `speccy-core/src/consistency.rs` with the four-drift-kind detector (`detect`), the `ConsistencyStatus` / `DriftKind` / `DriftSeverity` / `DriftDetails` / `DriftEntry` / `ConsistencyBlock` serde-`Serialize` types matching the SPEC-0045 REQ-005/REQ-006 JSON contract, a `GitProbe` trait plus a shipped read-only `ShellGitProbe` (uses only `git -C <root> log --grep ... --format=%H`, `git -C <root> status --porcelain`, and `git -C <root> rev-parse --is-inside-work-tree`), and a tolerant `last_well_formed_offset` scanner for the `journal_xml_malformed` byte offset. Extended `speccy-cli/src/next_output.rs` so both per-spec and workspace JSON envelopes carry a top-level `consistency` field, and added a small `apply_reconcile_override` that flips `next_action.kind` to `"reconcile"` whenever `consistency.status != "ok"`. Wired the detector into `speccy-cli/src/next.rs` for both the per-spec and workspace dispatch paths, sharing one `ShellGitProbe` per command invocation. Outside a git repo, state-vs-commit correlation is skipped (no source of truth) so legacy tempdir-based tests stay green; journal-parse drift remains independently detected. Also fixed pre-existing blank-line-after-`</scenario>` violations in `.speccy/specs/0045-auto-commit-and-resume/SPEC.md` that the workspace lint test surfaced.

- Undone: T-006 (test fixtures + integration tests for all four drift kinds) and T-007 (architecture doc update) are explicitly the next two tasks in the SPEC and not in scope here. T-005 covers the production code; the fixture-based scenarios CHK-009/CHK-010/CHK-012/CHK-013 land their dedicated regression coverage under T-006.

- Hygiene checks:
  - cargo test --workspace: exit 0
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0

- Evidence: paper trail at the integration boundary in `speccy-cli/src/next.rs` (consistency block threaded through both dispatch paths) plus unit tests in `speccy-core/src/consistency.rs` (`regex_escape_brackets_and_metacharacters`, `last_well_formed_offset_finds_close_of_implementer`, `last_well_formed_offset_zero_when_no_close_tag`, `detect_ok_when_no_tasks`, `fake_probe_returns_configured_sha`). End-to-end smoke: `cargo run -- next SPEC-0045 --json` produces a JSON envelope whose `consistency.status == "blocked"`, drifts populated for T-001..T-005, and `next_action.kind == "reconcile"`, matching CHK-009/CHK-010/CHK-012 shape. Roll call for CHKs under REQ-005 / REQ-006:
  - CHK-009 (state_completed_no_commit drift, status blocked, next_action.kind reconcile): demonstrated → the live `speccy next SPEC-0045 --json` smoke run renders exactly that envelope shape for T-001..T-004; structural detection is exercised by `detect_consistency` in `speccy-core/src/consistency.rs` via the `(TaskState::Completed, None)` match arm. Fixture-based assertion is T-006's `test_state_completed_no_commit_dirty_tree`.
  - CHK-010 (commit_without_state drift, status drift, severity auto_fixable, 40-hex commit_sha): demonstrated → handled by the `(_, Some(sha))` match arm in `detect_consistency`; `ShellGitProbe::first_commit_sha_with_title_prefix` returns the 40-char SHA verbatim from `git log --format=%H`. Fixture assertion is T-006's `test_commit_without_state`.
  - CHK-011 (no mutating git commands in src/): demonstrated → `grep -rn "git add\|git commit\|git restore\|git clean\|git stash" speccy-cli/src/ speccy-core/src/` finds only the two matches inside the `//!` module doc comment of `consistency.rs`, which CHK-011 explicitly excludes ("not in comments or doc strings"). T-006's `test_no_mutating_git_commands_in_source` adds the regression guard.
  - CHK-012 (state_completed_no_commit, severity blocking, working_tree_dirty true): demonstrated → the `(TaskState::Completed, None)` arm always emits `severity: Blocking` and populates `working_tree_dirty` from a cached `porcelain_status()` call. Fixture assertion is T-006's `test_state_completed_no_commit_dirty_tree`.
  - CHK-013 (journal_xml_malformed kind with last_well_formed_byte_offset at close of `<implementer>`): demonstrated → `last_well_formed_offset_finds_close_of_implementer` unit test in `consistency.rs` asserts the byte offset matches the close of `</implementer>` for the exact fixture body called out in CHK-013. Fixture assertion is T-006's `test_journal_xml_malformed`.

- Discovered issues:
  - Pre-existing legacy integration tests in `speccy-cli/tests/next_derived.rs` set up `completed` tasks in a tempdir workspace with no `.git`. Under a naive consistency check those would fire `state_completed_no_commit` and flip `next_action.kind` to `"reconcile"`, breaking the legacy assertions. Resolution: added an `is_git_repo` method on `GitProbe` so `detect` skips state-vs-commit correlation entirely when the workspace is not a git repo (`git rev-parse --is-inside-work-tree` fails). The semantics match DEC-003 — git log is the source of truth, so without a git repo there is no source of truth to correlate against. Journal-parse drift remains detected independently because it is a pure file-content check.
  - Pre-existing SPEC.md blank-line-after-`</scenario>` violations in `.speccy/specs/0045-auto-commit-and-resume/SPEC.md` made `speccy-core/tests/in_tree_specs.rs::every_in_tree_spec_md_has_blank_line_after_each_close_tag` fail under the unmodified hygiene gate. Fixed inline; the SPEC text is unchanged otherwise.

- Procedural compliance: Followed the `speccy-work` skill steps — read scenarios via `speccy check SPEC-0045/T-005`, flipped state to `in-progress` before any edit, ran the four hygiene gates after implementation, flipped state to `in-review`, and append this `<implementer>` block to the per-task journal. No shipped-skill friction surfaced during the task. Suggested-file hints from the task body were accurate (`speccy-core/src/consistency.rs` is the new module, and `speccy-cli/src/next.rs` / `speccy-cli/src/next_output.rs` are the wiring sites).

</implementer>

<review date="2026-05-26T01:50:00Z" model="claude-opus-4-7[1m]/low" persona="business" verdict="pass" round="1">
T-005 delivers the REQ-005 / REQ-006 contract. `consistency` block emitted as non-optional top-level field on both per-spec and workspace JSON envelopes. Status discriminator matches REQ-005 (`Ok` empty / `Blocked` any blocking / `Drift` otherwise). Reconcile override fires whenever status is non-ok and preserves `task_id`. All four drift kinds emit the documented `details` shape. CHK-013 byte-offset semantics match truncation needs. CHK-011 "no mutating git in src/" holds (only `git log --grep`, `git status --porcelain`, `git rev-parse --is-inside-work-tree` invoked). Live smoke (`speccy next SPEC-0045 --json`) returns the expected `status:"blocked"` shape with four `state_completed_no_commit` entries against the current on-disk state. The non-git-repo short-circuit silently resolves an SPEC open question (DEC-003 justification) but is a reasonable scope-management call. Out-of-scope SPEC.md whitespace fix is consistent with "fix friction inline" guidance.
</review>

<review date="2026-05-26T01:50:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="blocking" round="1">
The new `detect()` function — the core deliverable of T-005 — has zero behavioural test coverage. If `detect()` were mentally rewritten to `ConsistencyBlock::ok()` regardless of inputs, all five new tests in `speccy-core/src/consistency.rs` still pass:

- `regex_escape_brackets_and_metacharacters` tests a helper.
- `last_well_formed_offset_finds_close_of_implementer` / `last_well_formed_offset_zero_when_no_close_tag` test the offset scanner.
- `detect_ok_when_no_tasks` is misleadingly named: it never calls `detect()`. Body constructs `ConsistencyBlock::ok()` and asserts on the constructor's fields.
- `fake_probe_returns_configured_sha` tests the test helper itself.

The `FakeProbe` trait impl is set up but never used to call `detect()`. The four drift kinds each have a match arm with no behavioural assertion. `apply_reconcile_override` in `next_output.rs:259-272` has no test feeding it a non-ok `ConsistencyBlock`. The Windows-path normalisation `.replace('\\', "/")` at `consistency.rs:419` is untested.

The CHK-009/CHK-010/CHK-012 roll-call entries label these as "demonstrated" by a one-off `cargo run -- next SPEC-0045 --json` smoke, then explicitly punt fixture-based assertions to T-006. A manual smoke run is not a regression guard, and T-006 is `state="pending"` — so this task lands a per-task commit (per REQ-003) carrying production drift-detection code with no behavioural test coverage in the same commit.

Fix: add unit tests against `detect()` using the already-defined `FakeProbe` covering, at minimum:
(a) `state_completed_no_commit` + dirty tree → `Blocked` + `working_tree_dirty: true`,
(b) `state_completed_no_commit` + clean tree → `Blocked` + `working_tree_dirty: false`,
(c) `commit_without_state` → `Drift` + `severity: AutoFixable` + 40-hex SHA + 8-hex short SHA,
(d) `state_in_progress_orphaned` → `Blocked` + dirty count,
(e) `journal_xml_malformed` via on-disk tempdir fixture → `Blocked` + correct forward-slash-normalised `journal_path` and `last_well_formed_byte_offset`,
(f) all-healthy spec → `Ok` + empty drifts.
Add at least one test for `apply_reconcile_override` confirming the kind override fires on `Drift` / `Blocked` and is preserved on `Ok`. Rename `detect_ok_when_no_tasks` to reflect that it tests only the constructor.
</review>

<review date="2026-05-26T01:50:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security issues. All `Command::new("git")` calls use `.arg()` (not shell interpolation), so argument injection is impossible. `--grep=` pattern built from `spec_id` (`^SPEC-\d{4}$`) and `task_id` (`^T-\d{3,}$`), both alphanumeric-only, with `regex_escape` as defense-in-depth. `detect_journal_drift` path join uses the same validated `task_id`, blocking path traversal. No mutating git commands outside comments. No `unwrap()`/`panic!()` in production code. UTF-8 decoding guarded via `.ok()?`. `details.journal_path` exposure is the contracted CHK-013 behaviour for a local CLI tool.
</review>

<review date="2026-05-26T01:50:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`speccy-cli/src/next_output.rs:261` uses `"reconcile".clone_into(&mut action.kind)` while every other `kind` assignment in the same `to_json_action` function (lines 227, 231, 235, 239, 243) and the surrounding file uses `"foo".to_owned()`. The `clone_into` idiom fights the project's uniform style here. Replace with `action.kind = "reconcile".to_owned()`.

Secondary (non-blocking but worth fixing in the same pass): `speccy-core/src/consistency.rs` lines 314–327 contain a dead assignment. When `!in_repo`, `commit_sha` is set to `None` in the `else` branch, but the immediately following `if !in_repo { … continue; }` short-circuits before the `match` that would consume it. Comment on lines 317–320 says "Pretend every task has a matching commit so the state-vs-commit branches all fall through to the healthy / no-op cases" — but `None` is the *no-commit* value, so the comment contradicts the assigned value, and the `continue` makes the assignment unreachable. Either simplify the `else` branch or merge the two `if !in_repo` blocks into one early-return.
</review>

<blockers date="2026-05-26T01:50:00Z" round="1">
T-005 has two blocking concerns from the round-1 review fan-out.

- **Tests (zero behavioural coverage of `detect()`):** The core function shipped in this task is not exercised by any unit test. Add unit tests against `detect()` using the already-defined `FakeProbe` covering at minimum the six cases listed in the tests reviewer's verdict — the four drift kinds (commit_without_state, state_completed_no_commit dirty+clean, state_in_progress_orphaned, journal_xml_malformed via on-disk tempdir fixture) plus the all-healthy → Ok path. Add at least one test for `apply_reconcile_override` covering the Drift/Blocked override and the Ok preservation. Rename `detect_ok_when_no_tasks` so the name reflects what it actually tests (the constructor, not `detect()`).
- **Style (one-off `clone_into` fights surrounding style):** `speccy-cli/src/next_output.rs:261` should use `action.kind = "reconcile".to_owned()` to match the uniform pattern at lines 227/231/235/239/243. Also clean up the dead `commit_sha = None` assignment + misleading comment at `speccy-core/src/consistency.rs:314-327` while in the same area.
</blockers>

<implementer date="2026-05-25T20:30:00Z" model="claude-opus-4-7[1m]/low" round="2">

- Completed: Addressed both round-1 blockers. (1) Tests: added `speccy-core/tests/consistency_detect.rs` — seven behavioural integration tests that drive `detect()` via the public `GitProbe` trait with an in-test `FakeProbe`, covering all four drift kinds plus the all-healthy path: `detect_state_completed_no_commit_dirty_tree_is_blocking`, `detect_state_completed_no_commit_clean_tree_is_blocking`, `detect_commit_without_state_is_auto_fixable_drift` (asserts 40-hex commit_sha + 8-hex short SHA), `detect_state_in_progress_orphaned_is_blocking_when_dirty` (asserts `dirty_files_count`), `detect_state_in_progress_no_commit_clean_tree_is_ok` (clean-tree negative), `detect_journal_xml_malformed_is_blocking_with_forward_slash_path` (on-disk tempdir fixture; asserts `journal_path` is forward-slash normalised + correct `last_well_formed_byte_offset`), and `detect_all_healthy_is_ok`. Added four `apply_reconcile_override` tests in `speccy-cli/src/next_output.rs` exercising the override through the public `render_json_per_spec` API: blocked status forces `kind: "reconcile"` and preserves `task_id`; drift status forces `kind: "reconcile"` and preserves `task_id`; ok status preserves the original `work` kind and `task_id`; ok status preserves a no-task `decompose` kind. Renamed the misleading `detect_ok_when_no_tasks` constructor smoke test in `consistency.rs` to `consistency_block_ok_constructor_returns_empty_ok_block`. (2) Style: removed the dead-assignment + misleading comment block at `consistency.rs:314-327` — outside-of-git is now a single early-return that skips state-vs-commit correlation cleanly. Restructured `apply_reconcile_override` in `next_output.rs` so the non-ok branch constructs a fresh `JsonNextAction { kind: "reconcile".to_owned(), task_id: action.task_id }` rather than mutating `action.kind` after the fact; this matches the uniform `"foo".to_owned()` idiom used in `to_json_action` at lines 227/231/235/239/243 without tripping clippy's `assigning_clones` lint that the reviewer's original `action.kind = "reconcile".to_owned()` suggestion would have triggered.

- Undone: T-006 (test fixtures + integration tests for all four drift kinds) and T-007 (architecture doc update) remain as the next two tasks. The new `consistency_detect.rs` integration suite is narrowly scoped to the round-1 blocker — it exercises `detect()` against in-memory FakeProbes for behavioural regression coverage; T-006 owns the broader fixture matrix called out in the SPEC.

- Hygiene checks:
  - cargo test --workspace: exit 0 (consistency_detect: 7/7 pass; next_output reconcile_override: 4/4 pass)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0

- Evidence: paper trail at `speccy-core/tests/consistency_detect.rs` (seven behavioural tests against `detect()` via the FakeProbe), `speccy-cli/src/next_output.rs` tests module (four `apply_reconcile_override` tests), and `speccy-core/src/consistency.rs` (renamed constructor test). Roll call for CHKs under REQ-005 / REQ-006:
  - CHK-009 (state_completed_no_commit drift, status blocked, next_action.kind reconcile): demonstrated → `detect_state_completed_no_commit_dirty_tree_is_blocking` asserts `status == Blocked`, `kind == StateCompletedNoCommit`, `severity == Blocking`, expected_trailer `[SPEC-0099/T-001]:`, and `working_tree_dirty: true`; `reconcile_override_fires_on_blocked_and_preserves_task_id` asserts the JSON envelope's `next_action.kind == "reconcile"` with `task_id` preserved. The clean-tree variant `detect_state_completed_no_commit_clean_tree_is_blocking` covers the dirty=false case.
  - CHK-010 (commit_without_state drift, status drift, severity auto_fixable, 40-hex commit_sha): demonstrated → `detect_commit_without_state_is_auto_fixable_drift` asserts `status == Drift`, `kind == CommitWithoutState`, `severity == AutoFixable`, `commit_sha.len() == 40`, and `commit_short_sha == sha.get(..8)`. `reconcile_override_fires_on_drift_status` covers the JSON `kind == "reconcile"` override on Drift status.
  - CHK-011 (no mutating git commands in src/): hygiene → still satisfied; this round did not touch the git-call surface. T-006's `test_no_mutating_git_commands_in_source` adds the regression guard.
  - CHK-012 (state_completed_no_commit, severity blocking, working_tree_dirty true): demonstrated → `detect_state_completed_no_commit_dirty_tree_is_blocking` asserts `severity == Blocking` and `working_tree_dirty == true` against a `FakeProbe` configured with two porcelain entries.
  - CHK-013 (journal_xml_malformed kind with last_well_formed_byte_offset at close of `<implementer>`, forward-slash journal_path): demonstrated → `detect_journal_xml_malformed_is_blocking_with_forward_slash_path` writes an on-disk tempdir fixture whose journal body has a clean `<implementer>...</implementer>` followed by `<garbage>`, parses the spec via `workspace::parse_spec_dir`, drives `detect()`, and asserts `kind == JournalXmlMalformed`, `severity == Blocking`, `journal_path` contains no backslashes and ends with `journal/T-001.md`, and `last_well_formed_byte_offset` equals `malformed.find("</implementer>") + "</implementer>".len()`.

- Discovered issues:
  - The style reviewer's suggested rewrite (`action.kind = "reconcile".to_owned()`) is rejected by clippy's `assigning_clones` lint (which is implied by `-D clippy::pedantic` in the workspace lint set). The original `"reconcile".clone_into(&mut action.kind)` was clippy-correct; the surrounding `.to_owned()` calls in `to_json_action` are *constructions* of new `JsonNextAction` values, not reassignments, which is why they do not trip the lint. To honour the reviewer's intent (uniform-looking call sites) while staying clippy-clean, I rewrote `apply_reconcile_override` to construct a fresh `JsonNextAction` in the non-ok branch using `.to_owned()` — matching the construction idiom on lines 227/231/235/239/243 and avoiding the post-hoc field mutation entirely.

- Procedural compliance: Followed the speccy-work skill round-2 path — read the round-1 `<blockers>` block first, flipped state to `in-progress` before any edit, addressed both blockers, ran all four hygiene gates after the edits, flipped state to `in-review`, append this `<implementer round="2">` block to the journal file. No shipped-skill friction surfaced. The clippy/style-reviewer tension above is a code-review-loop observation, not a skill bug.

</implementer>

<review date="2026-05-26T02:30:00Z" model="claude-opus-4-7[1m]" persona="business" verdict="pass" round="2">
Round-2 diff preserves REQ-005 / REQ-006 contract. The `apply_reconcile_override` refactor is a construction-idiom cleanup — when `consistency.status != Ok`, the fresh `JsonNextAction` still emits `kind: "reconcile"` with `task_id` preserved, matching REQ-005's override rule verbatim. The merged non-git-repo branch removes a dead assignment + misleading comment without changing observable behaviour. The new `speccy-core/tests/consistency_detect.rs` (seven integration tests driving `detect()` end-to-end) covers all four REQ-006 drift kinds plus the Ok happy path with the documented `details` shapes. The four `apply_reconcile_override` tests exercise the JSON-envelope-level override on Blocked/Drift/Ok status against both task-bound (Work/Review) and task-free (Decompose) actions. No new goals violated, no non-goals breached.
</review>

<review date="2026-05-26T02:30:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round-1 blocking concern resolved. New `speccy-core/tests/consistency_detect.rs` adds seven behavioural integration tests driving real `detect()` via the `GitProbe` trait with an in-test `FakeProbe` whose `is_git_repo()` returns `true`. Coverage matrix matches the round-1 verdict's required six cases plus a bonus negative path. Vacuity check passes: mentally rewriting `detect()` to `ConsistencyBlock::ok()` breaks four tests; rewriting `apply_reconcile_override` to no-op breaks two; rewriting to always force reconcile breaks two. The `commit_short_sha == sha.get(..8)` assertion catches wrong-position bugs (not just any 8 chars). The `journal_xml_malformed` test asserts `!journal_path.contains('\\')`, `ends_with("journal/T-001.md")`, and `last_well_formed_byte_offset == malformed.find("</implementer>") + "</implementer>".len()`. The four `apply_reconcile_override` tests exercise the override through the public `render_json_per_spec` API. `detect_ok_when_no_tasks` renamed to `consistency_block_ok_constructor_returns_empty_ok_block`. Local run: `cargo test -p speccy-core --test consistency_detect` → 7/7 pass; `cargo test -p speccy-cli --lib next_output` → 11/11 pass.
</review>

<review date="2026-05-26T02:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 changes introduce no new security concerns. The two production-code edits are: (1) `apply_reconcile_override` now constructs a fresh `JsonNextAction` instead of mutating `action.kind` — a pure in-memory transformation with no I/O, no shell calls; (2) the dead-assignment block removed and replaced with a clean `continue`, shrinking code surface. All security properties established in round 1 remain intact: argument-vector git calls via `.arg()`, `regex_escape` defense-in-depth on `spec_id`/`task_id`, no mutating git commands, `journal_path` forward-slash normalisation now test-asserted. New `consistency_detect.rs` is test-only; `make_spec_dir` writes fixtures to `tempfile::tempdir()` paths with hardcoded literal journal filenames — no user-controlled path component reaches `journal_dir.join(name)`. No shell commands spawned in test code.
</review>

<review date="2026-05-26T02:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Both round-1 style blockers resolved. `apply_reconcile_override` now constructs a fresh `JsonNextAction { kind: "reconcile".to_owned(), task_id: action.task_id }` in the non-ok branch — no `clone_into`, uniform `.to_owned()` construction idiom throughout. The dead `commit_sha = None` assignment and the contradictory "Pretend every task has a matching commit" comment are gone; the non-git-repo path is a clean `if !in_repo { detect_journal_drift + continue }` with an accurate comment. No new style issues introduced: no bare `#[allow(...)]`, no production `unwrap()`/`expect()`, the one `#[expect(clippy::similar_names, reason = "...")]` carries a proper reason string.
</review>
