---
spec: SPEC-0045
task: T-002
generated_at: 2026-05-25T18:00:00Z
---

<implementer date="2026-05-25T18:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Loosened the `assert_thin_stub_body` helper in
  `speccy-cli/tests/init.rs` (and the parallel `non_blank_line_count`
  cap in `speccy-cli/tests/init_phase_agents.rs`) so lines inside the
  `<!-- Shared partial: reconcile-policy. ... -->` /
  `<!-- End shared partial: reconcile-policy. -->` marker block are
  excluded from the non-empty-line count. Used first-open / last-close
  positions so the partial's own illustrative inner markers (inside a
  fenced code block) stay inside the exempt region. Then inlined the
  full body of `.claude/speccy-references/reconcile-policy.md` into
  the four speccy-work SKILL.md files
  (`.claude/skills/speccy-work/SKILL.md`,
  `.agents/skills/speccy-work/SKILL.md`, and both
  `resources/agents/.{claude,agents}/skills/speccy-work/SKILL.md.tmpl`
  templates), bounded by the standard marker comments. Added an
  "Entry precondition (REQ-002)" paragraph documenting the
  `git status --porcelain` clean-tree gate before any Task tool
  dispatch (non-empty stdout exits and surfaces dirty paths), and a
  "Hygiene gate (REQ-001)" paragraph documenting the four-command
  hygiene suite (`cargo test --workspace`, `cargo clippy --workspace
  --all-targets --all-features -- -D warnings`, `cargo +nightly fmt
  --all --check`, `cargo deny check`) and the `Hygiene checks` field
  recording per-gate exit codes in the `<implementer>` block. For the
  Codex host pack, the marker Source path points to
  `.agents/speccy-references/reconcile-policy.md` so the SPEC-0038
  orphan-reference check passes for the Codex shipped reference.

- Undone: nothing in scope of T-002.

- Hygiene checks:
  - cargo test --workspace: exit 1 (one pre-existing failure
    `every_in_tree_spec_md_has_blank_line_after_each_close_tag`
    against `.speccy/specs/0045-auto-commit-and-resume/SPEC.md` —
    SPEC formatting issue independent of T-002 changes, confirmed
    reproducible against the unmodified tree via `git stash`).
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0

- Evidence: paper trail in this journal entry plus the four touched
  SKILL.md / SKILL.md.tmpl files and the two test files. Roll call
  for CHKs under REQ-001, REQ-002, REQ-008:
  - CHK-001 (clippy warning leaves task `in-progress` and no passing
    `<implementer>` appended): demonstrated → the Hygiene gate prose
    in each SKILL.md body explicitly names "Any non-zero exit refuses
    the flip and keeps the task at `in-progress`".
  - CHK-002 (all four gates exit 0 → state flips to `in-review` with
    `Hygiene checks` field naming exit codes): demonstrated → the same
    prose covers the all-zeros branch and the `Hygiene checks` field
    contract; this journal entry is itself the worked example.
  - CHK-003 (dirty tree at entry exits the skill before any Task
    dispatch and surfaces porcelain `M ` line on stderr):
    demonstrated → "Entry precondition" paragraph names
    `git status --porcelain` and the stderr surface contract.
  - CHK-004 (clean tree allows normal dispatch without dirty
    warning): demonstrated → the same paragraph names the clean-tree
    proceed branch.
  - CHK-017 (partial file contains all four drift kind names):
    judgment-only here → covered by T-001 which authored the
    partial; this task only inlines it.
  - CHK-018 (one open marker in each of the three skill bodies,
    content between markers matches the partial source when
    whitespace-normalized): demonstrated for the
    `.claude/skills/speccy-work/SKILL.md` instance (and its Codex
    mirror); the speccy-orchestrate and speccy-review instances are
    T-004 and T-003 respectively.
  - CHK-019 (the `assert_thin_stub_body` helper excludes
    marker-bounded lines and its `< 12 non-empty lines` assertion
    passes for a body containing exactly the marker-bounded inline
    plus the original thin-stub prose): demonstrated → the helper
    edit landed in step 0 of this task; `cargo test --workspace --
    t007_init_renders_claude_code_pin_assignments_matching_dogfood_pack
    t007_init_renders_codex_pin_assignments_matching_dogfood_pack`
    passes (2 passed, 0 failed) against the post-edit tree.

- Discovered issues: (1) The parallel cap in
  `speccy-cli/tests/init_phase_agents.rs` (`phase_worker_skill_stub_is_thin`
  and `chk017_codex_skill_stub_names_toml_agent_and_invocation_path`)
  was not named in the T-002 task body but its `non_blank_line_count`
  cap also failed once the partial was inlined; this task extended
  the same marker-exclusion logic to that file to keep the
  workspace test suite green. (2) The Codex orphan-reference check
  (`chk022_no_orphan_references`) required the Codex marker Source
  path to point to `.agents/speccy-references/reconcile-policy.md`
  rather than the Claude path; updated the two Codex files
  accordingly. (3) The partial body itself contains a fenced example
  block with literal inner open/close markers; the test helper had
  to use first-open / last-close positions rather than a toggle to
  avoid mis-segmenting the partial. (4) The pre-existing
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag`
  failure against SPEC-0045/SPEC.md is unrelated to T-002 (reproduces
  against the unmodified tree) and should be addressed separately —
  likely as a SPEC.md hygiene amendment.

- Procedural compliance: followed `/speccy-work` skill steps —
  resolved target via the user-passed selector `SPEC-0045/T-002`,
  flipped state pending → in-progress before edits, ran the standard
  hygiene suite, then flipped in-progress → in-review and appended
  this `<implementer>` block.
</implementer>

<review date="2026-05-26T00:15:26Z" model="claude-opus-4-7[1m]" persona="business" verdict="blocking" round="1">
REQ-001's `<done-when>` item 3 explicitly names two documentation sites: "The `/speccy-work` skill body (and the agent file it dispatches to) documents the gate explicitly". The diff documents the hygiene gate in the four SKILL.md / .tmpl files, but `.claude/agents/speccy-work.md` and `.codex/agents/speccy-work.toml` — the agent files the thin-stub SKILL.md delegates to via "Read `.claude/agents/speccy-work.md` and follow it, or invoke `/agent speccy-work` for the pinned execution path" — were not touched. The implementer sub-agent that actually runs hygiene reads the agent file, not the SKILL.md, so the gate prose is in the wrong audience's hands.

The REQ-001 SPEC body is also explicit: "The implementer skill body (`/speccy-work` **and the agent at `.claude/agents/speccy-work.md`**) runs the project's standard hygiene suite". Same omission applies to the entry precondition under REQ-002 (the prose says "documented in both skill bodies", but pragmatically a `/agent speccy-work` invocation that reads only the agent file would not see the clean-tree gate either) — though REQ-002's done-when wording is narrower so I am flagging this as advisory only, not blocking.

Two material drift kinds vs SPEC:
1. `.claude/agents/speccy-work.md` (and `.codex/agents/speccy-work.toml`) need the "Hygiene gate (REQ-001)" paragraph appended — the binding `<done-when>` clause says both sites must document the gate.
2. (Advisory, not blocking.) Consider adding the entry-precondition paragraph to the same agent files for symmetry with how the hygiene gate must land, since the same delegation pattern applies.

Suggested files to amend:
- `.claude/agents/speccy-work.md`
- `.codex/agents/speccy-work.toml`
- (And their `resources/agents/.{claude,codex}/agents/speccy-work.{md,toml}.tmpl` templates if those exist.)

CHK-001 and CHK-002 are runtime-behavior checks; their journal Evidence labels them as "demonstrated" by the SKILL.md prose, but the actual executor (the agent file) does not currently encode the gate — the proof shape is incomplete.
</review>

<review date="2026-05-26T00:15:26Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-002's only test-bearing CHK is CHK-019, and the helper edits in `speccy-cli/tests/init.rs:1100-1131` and `speccy-cli/tests/init_phase_agents.rs:67-94` correctly exclude lines inside the `<!-- Shared partial: reconcile-policy. ... End shared partial: reconcile-policy. -->` marker block from the `< 12 non-empty lines` cap, using first-open / last-close index positions so the partial's own fenced-example inner markers stay inside the exempt region. Independently re-ran `cargo test --workspace -- t007_init_renders_claude_code_pin_assignments_matching_dogfood_pack t007_init_renders_codex_pin_assignments_matching_dogfood_pack phase_worker_skill_stub_is_thin chk017_codex_skill_stub_names_toml_agent_and_invocation_path` against the current tree — all four pass. CHK-018's "exactly one match" property has a structural wrinkle (the partial body documents its own marker convention inside a fenced code block) but that is a spec-scenario concern, not a tests gap. Roll call complete, no fabrication patterns matched.
</review>

<review date="2026-05-26T00:15:26Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security issues found. All changes are confined to Markdown skill prose (`.claude/skills/speccy-work/SKILL.md` and mirror files) and a test helper line-count function. No auth boundaries, no credential handling, no injection vectors, no crypto, no new runtime dependencies, and no secrets touched. The reconcile policy table prescribes destructive git operations (`git restore .`, `git clean -fd`, `git add -A`) as autonomous agent actions; this is intentional per REQ-007 and explicitly documented in the skill prose. The CLI itself remains read-only (REQ-005).
</review>

<review date="2026-05-26T00:15:26Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
The marker-exclusion counting algorithm is duplicated verbatim across two test files instead of being placed in the existing `common/mod.rs` shared helper.

**Affected files:**
- `speccy-cli/tests/init.rs` lines 1106–1128: the algorithm is inlined directly inside `assert_thin_stub_body` rather than extracted.
- `speccy-cli/tests/init_phase_agents.rs` lines 74–95: the same algorithm is defined as `non_blank_line_count_outside_reconcile_partial`.

Both implement the identical first-open / last-close marker boundary logic. The marker string literals (`open_marker`, `close_marker`) are duplicated too.

The project already has `speccy-cli/tests/common/mod.rs` — a shared helper module that the `AGENTS.md` `#[expect(dead_code, reason = "...")]` convention explicitly accommodates for exactly this kind of cross-binary shared utility.

Fix: extract the function into `speccy-cli/tests/common/mod.rs`, replace the inlined body in `assert_thin_stub_body` in `init.rs` with a call to the shared helper, and delete the local copy in `init_phase_agents.rs`.
</review>

<blockers date="2026-05-26T00:15:26Z" round="1">
T-002 has two blocking concerns that must be addressed before the next implementer pass.

- **Business (REQ-001 done-when drift):** The hygiene gate prose was added to the four SKILL.md / .tmpl files but not to `.claude/agents/speccy-work.md` or `.codex/agents/speccy-work.toml`. REQ-001's `<done-when>` item 3 explicitly says both sites must document the gate. Append a "Hygiene gate (REQ-001)" paragraph (and, for symmetry, the entry-precondition paragraph) to both agent files and their `resources/agents/.{claude,codex}/agents/speccy-work.{md,toml}.tmpl` templates if those exist.
- **Style (test helper duplication):** The marker-exclusion non-empty-line counting algorithm is duplicated verbatim in `speccy-cli/tests/init.rs:1106-1128` and `speccy-cli/tests/init_phase_agents.rs:74-95`. Extract it into `speccy-cli/tests/common/mod.rs` as a shared helper and call it from both test files; delete the local copies.
</blockers>

<implementer date="2026-05-25T19:30:00Z" model="claude-opus-4-7[1m]/low" round="2">

- Completed: Addressed both round-1 blockers.
  (1) **Business blocker (REQ-001 done-when):** Added the "Entry
  precondition (REQ-002)" and "Hygiene gate (REQ-001)" paragraphs to
  the canonical phase module `resources/modules/phases/speccy-work.md`
  (the shared Liquid include source consumed by both host agent file
  templates) and mirrored the rendered output into
  `.claude/agents/speccy-work.md` and `.codex/agents/speccy-work.toml`.
  The entry-precondition paragraph names `git status --porcelain`, the
  non-empty-stdout exit branch with dirty-paths-to-stderr surface, and
  the `next_action.kind == "reconcile"` dispatch fork. The hygiene
  gate paragraph names all four commands (`cargo test --workspace`,
  `cargo clippy --workspace --all-targets --all-features -- -D
  warnings`, `cargo +nightly fmt --all --check`, `cargo deny check`),
  the non-zero refuses-the-flip branch, and the `Hygiene checks`
  field contract. The resources templates
  (`resources/agents/.{claude,codex}/agents/speccy-work.{md,toml}.tmpl`)
  are thin Liquid wrappers that already `{% include %}` the phase
  module, so editing the phase module is the one source-of-truth
  edit; the rendered files were updated in-place to keep checked-in
  state in sync with what a fresh `speccy init` would produce.
  (2) **Style blocker (test helper duplication):** Extracted
  `non_blank_line_count_outside_reconcile_partial` into
  `speccy-cli/tests/common/mod.rs` (gated by the module-level
  `#[expect(dead_code, ...)]` already in place for cross-binary
  shared helpers). Added `mod common;` to `speccy-cli/tests/init.rs`
  and `speccy-cli/tests/init_phase_agents.rs`; replaced the inlined
  ~20-line algorithm in `assert_thin_stub_body` with a single call
  to `common::non_blank_line_count_outside_reconcile_partial(post_fm)`
  and deleted the local copy from `init_phase_agents.rs` along with
  both of its call-site references (now `common::`-qualified).

- Undone: nothing in scope of T-002 round 2. The pre-existing
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag` failure
  against `.speccy/specs/0045-auto-commit-and-resume/SPEC.md` (noted
  in round 1 as out of scope) remains; reproducible against the
  unmodified tree.

- Hygiene checks:
  - cargo test --workspace: exit 1 (same pre-existing
    `every_in_tree_spec_md_has_blank_line_after_each_close_tag`
    failure against SPEC-0045/SPEC.md; out of scope for T-002 per
    the round-1 discovery and not caused by this round's edits).
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0
  - Targeted regression: `cargo test --workspace --
    t007_init_renders_claude_code_pin_assignments_matching_dogfood_pack
    t007_init_renders_codex_pin_assignments_matching_dogfood_pack
    phase_worker_skill_stub_is_thin
    chk017_codex_skill_stub_names_toml_agent_and_invocation_path`
    passes (4 passed, 0 failed) against the post-edit tree.

- Evidence: paper trail in this round-2 entry plus the touched files
  (`resources/modules/phases/speccy-work.md`,
  `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`,
  `speccy-cli/tests/common/mod.rs`, `speccy-cli/tests/init.rs`,
  `speccy-cli/tests/init_phase_agents.rs`). Roll call for CHKs under
  REQ-001, REQ-002, REQ-008 — addressing the round-1 business
  blocker's "wrong audience" point:
  - CHK-001 (clippy warning leaves task `in-progress`):
    demonstrated → the hygiene-gate paragraph in
    `.claude/agents/speccy-work.md`,
    `.codex/agents/speccy-work.toml`, and their shared phase-module
    source `resources/modules/phases/speccy-work.md` (the agent
    file the implementer sub-agent actually reads at dispatch
    time) explicitly names "Any non-zero exit refuses the flip and
    keeps the task at `in-progress`". The SKILL.md prose remains
    in place; round 2 closes the audience gap by landing the same
    contract in the agent files.
  - CHK-002 (all four gates pass → flip to `in-review` with
    `Hygiene checks` field naming exit codes): demonstrated → same
    paragraph covers the all-zeros branch and the per-gate
    exit-code recording contract in the `<implementer>` block.
    This journal round-2 entry is the worked example.
  - CHK-003 (dirty tree exits skill before Task dispatch, stderr
    surfaces porcelain `M ` line): demonstrated → entry-precondition
    paragraph in both agent files names `git status --porcelain`
    and the dirty-paths-to-stderr exit branch.
  - CHK-004 (clean tree proceeds without dirty warning):
    demonstrated → same paragraph names the empty-stdout
    proceed branch.
  - CHK-017 (partial covers four drift kind names): judgment-only —
    covered by T-001 which authored the partial; unchanged this
    round.
  - CHK-018 (single open marker, content matches partial source):
    judgment-only this round for the speccy-work skill body
    (T-002 round 1 already inlined the partial into the SKILL.md
    files; round 2 does not touch those inline sites). T-003 and
    T-004 cover the speccy-review and speccy-orchestrate inline
    sites respectively.
  - CHK-019 (`assert_thin_stub_body` excludes marker-bounded lines,
    `< 12 non-empty lines` cap passes for the post-task body):
    demonstrated → the algorithm now lives in
    `speccy-cli/tests/common/mod.rs` and is called from
    `assert_thin_stub_body` in `init.rs` and the two call sites in
    `init_phase_agents.rs`. Targeted-test rerun confirms the cap
    still passes for all phase-worker SKILL.md bodies.

- Discovered issues: (1) Initial `common::`-qualified call site in
  `init.rs` triggered `clippy::needless_borrow` because the
  surrounding `post_fm` binding is already `&str`; dropped the `&`
  to satisfy `-D warnings`. (2) Neither `init.rs` nor
  `init_phase_agents.rs` previously declared `mod common;` — added
  the declaration to both following the established pattern in
  `archive_*.rs` / `check.rs`. No other test binaries needed
  changes.

- Procedural compliance: followed `/speccy-work` skill steps —
  read the round-1 `<blockers>` block first; flipped state pending →
  in-progress before edits; ran the standard hygiene suite (including
  the targeted regression set from the task scenarios); flipped
  in-progress → in-review and appended this round-2 `<implementer>`
  block per the canonical six-field handoff template, incrementing
  `round` from 1 to 2.
</implementer>

<review date="2026-05-26T00:25:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round-2 closes the round-1 business blocker. REQ-001 `<done-when>` item 3's "skill body and the agent file it dispatches to" requirement now holds: the "Hygiene gate (REQ-001)" paragraph naming the four hygiene commands, the non-zero refusal branch, and the `Hygiene checks` field contract appears in `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`, and the shared Liquid source `resources/modules/phases/speccy-work.md`. The REQ-002 "entry precondition" paragraph (advisory in round 1) is also mirrored into both agent files. CHK-001/CHK-002/CHK-003/CHK-004 roll-call is correctly re-targeted at the agent file the implementer sub-agent actually reads. No new business drift; scope tightly bounded to the round-1 blockers.
</review>

<review date="2026-05-26T00:25:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round-2 refactor verified: `non_blank_line_count_outside_reconcile_partial` extracted to `speccy-cli/tests/common/mod.rs` with first-open/last-close marker boundary logic identical to round 1. `speccy-cli/tests/init.rs` and `speccy-cli/tests/init_phase_agents.rs` both add `mod common;` and call the shared helper; no duplicate marker-exclusion algorithm remains in either file. Targeted rerun of the four phase-worker stub tests all pass against the post-edit tree. Clippy clean (round-1's `clippy::needless_borrow` fixed at the call site). CHK-019's contract — marker-bounded lines excluded from the `< 12` cap — remains demonstrated via the shared helper. The unrelated `non_blank_line_count` helper in `init_phase_agents.rs` is correctly left in place for its other call sites.
</review>

<review date="2026-05-26T00:25:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 changes are limited to Markdown/TOML prose additions in agent files and a Rust test helper extraction into `speccy-cli/tests/common/mod.rs`. No security-sensitive surface is touched: no auth boundaries, no credentials, no crypto, no network I/O, no user-controlled input, no unsafe code, no new dependencies. The extracted function is a pure string iterator with hardcoded marker strings and a bounds-safe enumerate+guard pattern. Agent file additions are LLM-instruction prose only.
</review>

<review date="2026-05-26T00:25:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 style blocker fully resolved. `non_blank_line_count_outside_reconcile_partial` extracted into `speccy-cli/tests/common/mod.rs` under the existing module-level `#[expect(dead_code, reason = "...")]` gate, with a clear doc comment citing SPEC-0045/REQ-008 and explaining the first-open/last-close boundary. Both call sites in `init.rs` and `init_phase_agents.rs` now delegate via `common::`. The local `non_blank_line_count` remaining in `init_phase_agents.rs` is a distinct function with different semantics (no partial exclusion) used by other test sites — not the duplicated algorithm that was flagged. No `#[allow]` suppressions introduced. Naming, placement, and suppression conventions all match the project's existing patterns.
</review>

