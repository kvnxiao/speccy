---
spec: SPEC-0045
task: T-004
generated_at: 2026-05-25T00:00:00Z
---

<implementer date="2026-05-25T00:00:00Z" model="claude-opus-4-7[1m]" round="1">
- Completed: Wired the clean-tree precondition and the reconcile-policy
  shared partial into `speccy-orchestrate`. Edits landed in the source
  of truth `resources/modules/skills/speccy-orchestrate.md`: a
  "Reconcile-pass dispatch" block under "## Startup integrity check"
  with a `{% include %}` of `modules/references/reconcile-policy.md`
  bounded by the standard marker comments, and a "Clean-tree
  precondition (REQ-002)" block at the head of "## Work dispatch" that
  halts the outer loop on non-empty `git status --porcelain` before
  any speccy-work sub-agent is spawned. The same edits are mirrored
  in `.claude/skills/speccy-orchestrate/SKILL.md` and
  `.agents/skills/speccy-orchestrate/SKILL.md` (regenerated via
  `cargo run -p speccy-cli -- init --force --host claude-code` and
  `--host codex`, which confirms the module is the source of truth
  and the dogfood renderer output now matches the committed tree).
  The `.tmpl` files under `resources/agents/...` are include-only
  one-liners and needed no direct edit. Loop-iteration re-query is
  covered by the surrounding prose at "Startup integrity check"
  ("Each subsequent loop iteration's `speccy next SPEC-NNNN --json`
  ... re-runs the same check") — per the task body, a second inline
  is not duplicated when surrounding prose carries the contract.
- Undone: Nothing in scope. The `every_in_tree_spec_md_has_blank_line_after_each_close_tag` test fails for SPEC.md formatting issues at lines 325/405/517/611/740/853 — confirmed pre-existing on the branch before T-004 edits (verified via `git stash`), out of scope for this task.
- Hygiene checks:
  - `cargo test --workspace`: pre-existing SPEC.md formatting failure only; all other tests pass (exit 1, but unrelated to T-004).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: skipped — doc-only changes to skill bodies, no Cargo manifest or dependency surface touched.
- Evidence: roll call for CHKs under REQ-002 / REQ-007 / REQ-008 that
  fall to T-004 per the task body:
  - CHK-003 (orchestrate dispatch path halts on dirty tree before any
    Task dispatch): demonstrated → the new "Clean-tree precondition
    (REQ-002)" block at the head of "## Work dispatch" in
    `.claude/skills/speccy-orchestrate/SKILL.md` explicitly states
    "halt the outer loop ... no `speccy-work` sub-agent is spawned"
    on non-empty `git status --porcelain`. Grep-verifiable.
  - CHK-018 (exactly one inlined site of the reconcile partial, body
    matches source): demonstrated → `grep -c '^<!-- Shared partial:
    reconcile-policy\.'` returns 2 in
    `.claude/skills/speccy-orchestrate/SKILL.md` (one real outer
    marker plus one marker quoted inside the partial's own example
    code fence, same shape as the accepted T-002/T-003 landings in
    speccy-work and speccy-review). Body bytes match
    `.claude/speccy-references/reconcile-policy.md` because the
    installed file was regenerated from the module via `speccy init
    --force` and the renderer's dogfood test
    (`dogfood_outputs_match_committed_tree`) now passes.
  - REQ-007 autonomy property (no AskUserQuestion in reconcile
    dispatch path): demonstrated → the only two `AskUserQuestion`
    occurrences in the file are negative statements *inside* the
    partial body declaring its absence ("pass is autonomous — no
    `AskUserQuestion` ..." and "No `AskUserQuestion` invocation ...
    appears anywhere in the dispatch path"). No invocation present.
- Discovered issues: The `every_in_tree_spec_md_has_blank_line_after_each_close_tag` test fails on the SPEC-0045 SPEC.md itself (six close-tag-without-blank-line violations). Pre-existing on the branch; surfaced here but not fixed because the task is scoped to the orchestrate skill body only. The SPEC author or the reviewer round should address it before ship.
- Procedural compliance: Followed speccy-work skill steps. Working tree was dirty at task entry because SPEC-0045 is the meta-spec defining the very clean-tree gate — every T-NNN here lands on top of the prior task's uncommitted changes until the REQ-003 commit step (T-003) lands and back-fills. Proceeded under the standing dogfood exemption for this SPEC. No agent-behavior knobs added; edits are doc-only prose in the orchestrator skill body and its module source.
</implementer>

<review date="2026-05-26T01:20:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-004 satisfies its three covered requirements. REQ-002's clean-tree precondition for orchestrate work dispatch is documented explicitly at `.claude/skills/speccy-orchestrate/SKILL.md:318-328` ("halt the outer loop ... no speccy-work sub-agent is spawned"), satisfying CHK-003 for the orchestrate path. REQ-007's autonomy property is preserved — both `AskUserQuestion` occurrences in the file are negative statements inside the reconcile dispatch description and the partial body itself; no actual invocation appears in the dispatch path. REQ-008's shared-partial inline lands at lines 131-244 bounded by the standard marker comments, matching the accepted T-002/T-003 landings (grep returns 2 marker hits because the partial body contains a code-fenced example of the markers — same pattern accepted in prior tasks). The task body explicitly permits "inline once + describe the loop re-query in prose" instead of duplicating the partial twice; the implementer chose that path correctly. Mirrors in `.agents/skills/speccy-orchestrate/SKILL.md` and `resources/modules/skills/speccy-orchestrate.md` carry the same edits. Pre-existing SPEC.md close-tag formatting test failure is honestly out-of-scope and acknowledged.
</review>

<review date="2026-05-26T01:20:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-004 is a documentation-only skill-body edit; task-scenarios are grep-verifiable prose assertions, not unit-testable behaviours. Independently verified: CHK-003 prose at lines 318-328 of orchestrate SKILL.md; CHK-018 inlined body matches `reconcile-policy.md` byte-for-byte under the renderer (the grep returns 2 due to the partial's self-documenting fenced-example marker — same wrinkle accepted in T-002/T-003); REQ-007 autonomy occurrences are negative statements only. Dogfood guard `dogfood_outputs_match_committed_tree` passes, confirming source-of-truth chain `resources/modules/` → renderer → committed mirrors is intact. The single pre-existing `every_in_tree_spec_md_has_blank_line_after_each_close_tag` failure is in SPEC-0045/SPEC.md authoring prose (close-tag whitespace), not in any code path T-004 touched. Evidence shape compliant — inline roll call with `demonstrated`/`hygiene`/`judgment-only` labels per the accepted T-002/T-003 convention for doc-only tasks. No fabrication patterns matched.
</review>

<review date="2026-05-26T01:20:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Pure skill-prose change with no new code, dependencies, or secret-handling surface. The three security-relevant automated git operations described in the inlined partial (git add -A on recovery, git restore . / git clean -fd on orphan discard, journal truncation via CLI-supplied path) are all correctly scoped: commit body routed through single-quoted HEREDOC preventing shell injection, git add -A blast radius bounded by the clean-tree precondition at work-dispatch entry, destructive discard path documented as REQ-007 autonomy. No authentication boundaries, credentials, tokens, or external I/O involved.
</review>

<review date="2026-05-26T01:20:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Diff is narrow and well-scoped. Markdown prose additions in the orchestrate skill body match surrounding register (bold leads, numbered lists, fenced code blocks). Inlined partial appears between standard marker comments per the established T-002/T-003 pattern. Source-of-truth chain followed: edits land in `resources/modules/skills/speccy-orchestrate.md`, mirrors regenerated via `speccy init --force` for both hosts. No `#[allow]` suppressions, no `unwrap`, no `[i]` slice indexing, no naming drift. Codex mirror's marker `Source:` path correctly flipped to `.agents/speccy-references/reconcile-policy.md` per established convention.
</review>
