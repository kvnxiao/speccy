---
spec: SPEC-0045
task: T-007
generated_at: 2026-05-25T23:55:00Z
---

<implementer date="2026-05-25T23:55:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Added a new `### `consistency` block (SPEC-0045 REQ-006)` subsection to `docs/ARCHITECTURE.md` immediately after the per-spec-form discussion in the `## `speccy next --json`` section. The subsection documents (a) the top-level `consistency` envelope shape with `status` and `drifts` fields, (b) the three `status` values (`ok`, `drift`, `blocked`) and when each is emitted, (c) the override rule that forces `next_action.kind` to `"reconcile"` whenever `consistency.status != "ok"`, (d) the shared `DriftEntry` shape plus a four-row table mapping each `kind` (`commit_without_state`, `state_completed_no_commit`, `state_in_progress_orphaned`, `journal_xml_malformed`) to its `severity` and `details` shape, (e) per-kind prose explaining what each drift means, (f) the "CLI stays read-only" constraint naming the five forbidden mutating git commands (`git add`, `git commit`, `git restore`, `git clean`, `git stash`), and (g) an "Extending the enum" paragraph describing the two-site change procedure with a cross-reference to `.claude/speccy-references/reconcile-policy.md`.

- Undone: nothing — T-007 is the final task in SPEC-0045 and is doc-only.

- Hygiene checks:
  - cargo test --workspace: exit 0 (regression guard for doc-only change; no source files touched)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0

- Evidence: paper trail at `docs/ARCHITECTURE.md` (new `### `consistency` block (SPEC-0045 REQ-006)` subsection inside `## `speccy next --json``). Roll call for CHKs under REQ-006 in scope for T-007:
  - REQ-006 is a documentation requirement covered structurally by this task; no per-CHK numbered checks gate T-007 directly. The three task-scenarios act as the acceptance criteria:
    - "consistency appears inside speccy next JSON section": demonstrated → grep against the final file finds the new `### `consistency` block` heading nested under `## `speccy next --json``, and the substring `consistency` appears 18 times across the doc (counting heading, code-block fields, prose).
    - "all four drift kind names present": demonstrated → each of `commit_without_state`, `state_completed_no_commit`, `state_in_progress_orphaned`, `journal_xml_malformed` appears at least once in the new table and in the per-kind prose bullets.
    - "reconcile-policy partial path cross-referenced": demonstrated → the literal path `.claude/speccy-references/reconcile-policy.md` appears in the "Extending the enum" paragraph and again in the closing pointer prose, satisfying the "at least one cross-reference" criterion.
    - "cargo test --workspace exits 0": demonstrated → ran the full workspace test suite after the doc edit; every test binary reported `test result: ok` with zero failures.

- Discovered issues: none. The existing `## `speccy next --json`` section had a clean insertion point right after the per-spec-form / `no_active_specs` discussion, so the new subsection slots in without disrupting surrounding prose.

- Procedural compliance: working tree was non-empty at task entry (carryover from in-flight SPEC-0045 tasks T-001 through T-006 staged but uncommitted as a single end-of-spec batch). Per the SPEC-0045 workflow, the clean-tree precondition documented in the speccy-work SKILL.md is the entry check for *new* implementer dispatches inside an orchestrate loop; this single-task primitive invocation against a doc-only task was driven directly by the user, who held the dirty tree intentionally for batched commit. No skill-file friction encountered. No `AGENTS.md` or rule-file updates triggered by this turn.

</implementer>

<review date="2026-05-26T03:30:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="blocking" round="1">
The new `### consistency block (SPEC-0045 REQ-006)` subsection in `docs/ARCHITECTURE.md` lands the right structural pieces — placement inside `## speccy next --json`, all four drift kinds named, override rule, three status values, the read-only constraint with all five forbidden git commands, the cross-reference to `.claude/speccy-references/reconcile-policy.md`, and the two-change "Extending the enum" procedure. The four `<task-scenarios>` for T-007 are mechanically satisfied.

But the docs contradict the contract on one detail: the `details` shape table at `docs/ARCHITECTURE.md:2159` documents `commit_without_state` as `{ "commit_sha": "<40-hex>", "commit_short_sha": "<7-hex>" }`. SPEC REQ-006 explicitly says `<8-hex>`, the Rust implementation in `speccy-core/src/consistency.rs:84` documents `commit_short_sha` as "Eight-character abbreviation", and the integration test `speccy-core/tests/consistency_detect.rs:165-166` asserts `commit_short_sha.len() == 8`. Anyone reading the architecture doc will write code expecting a 7-char short SHA and break when they get 8. Change `<7-hex>` to `<8-hex>`.
</review>

<review date="2026-05-26T03:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="blocking" round="1">
One factual error at `docs/ARCHITECTURE.md:2159`. The table entry for `commit_without_state` documents `commit_short_sha` as `"<7-hex>"`; the implementation at `speccy-core/src/consistency.rs:343` produces an 8-character abbreviation (`sha.get(..8).unwrap_or(sha).to_owned()`), and the field's Rust doc comment reads "Eight-character abbreviation of `commit_sha`." Fix: change `"commit_short_sha": "<7-hex>"` to `"commit_short_sha": "<8-hex>"`.

Minor secondary (non-blocking on its own): `docs/ARCHITECTURE.md:2128` says `drifts` is `[]` "(or omitted)" in the `ok` case. The `ConsistencyBlock` struct carries no `#[serde(skip_serializing_if)]` on the `drifts` field — it is always emitted, never omitted. Remove the "(or omitted)" parenthetical to avoid misleading consumers who might branch on field presence.
</review>

<review date="2026-05-26T03:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All substantive style criteria pass. Minor inconsistency noted but not blocking: `docs/ARCHITECTURE.md:2111` wraps `consistency` in backticks (only H2 headings carry that convention in this file), but the local coherence with the parent `## speccy next --json` makes it cosmetically reasonable. Table separators, prose wrap width, trailing newline, no lint suppressions — all consistent with surrounding sections.
</review>

<blockers date="2026-05-26T03:30:00Z" round="1">
T-007 has one blocking documentation-vs-contract drift.

- **Docs / business (`commit_short_sha` length mismatch):** `docs/ARCHITECTURE.md:2159` documents the `commit_without_state` `details` shape as `{ "commit_sha": "<40-hex>", "commit_short_sha": "<7-hex>" }`. The implementation in `speccy-core/src/consistency.rs:343` and the field doc comment both specify an 8-character short SHA, and `speccy-core/tests/consistency_detect.rs:165-166` asserts `commit_short_sha.len() == 8`. Change `<7-hex>` to `<8-hex>`. While in the same area, also remove the "(or omitted)" parenthetical at `docs/ARCHITECTURE.md:2128` — the `drifts` field carries no `#[serde(skip_serializing_if)]`, so it is always emitted, never omitted.
</blockers>

<implementer date="2026-05-26T04:00:00Z" model="claude-opus-4-7[1m]/low" round="2">

- Completed: Applied the two docs-vs-contract fixes flagged in round 1 blockers. (1) `docs/ARCHITECTURE.md:2159` — changed `commit_short_sha` from `<7-hex>` to `<8-hex>` in the `commit_without_state` `details` shape row, matching `speccy-core/src/consistency.rs:343` (`sha.get(..8)...`) and the `consistency_detect.rs:165-166` assertion. (2) `docs/ARCHITECTURE.md:2128` — removed the misleading "(or omitted)" parenthetical from the `ok`-status `drifts` description; `ConsistencyBlock.drifts` has no `#[serde(skip_serializing_if)]` so it is always emitted.

- Undone: nothing.

- Hygiene checks: doc-only changes, no source touched; round 1 already validated `cargo test`/`clippy`/`fmt`/`deny` green.

- Evidence: see edits at `docs/ARCHITECTURE.md` lines 2128 and 2159. Both blocker items now match the Rust contract.

- Discovered issues: none.

- Procedural compliance: surgical edit per blockers directive; no skill-file friction.

</implementer>

<review date="2026-05-26T04:10:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round-1 blocker resolved. `docs/ARCHITECTURE.md:2159` now documents `commit_short_sha` as `<8-hex>`, matching SPEC REQ-006, `speccy-core/src/consistency.rs:343` (`sha.get(..8)`), and the `consistency_detect.rs:165-166` assertion. The secondary "(or omitted)" parenthetical at `docs/ARCHITECTURE.md:2128` is also gone. The four T-007 task-scenarios remain satisfied. No new business drift introduced.
</review>

<review date="2026-05-26T04:10:00Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="pass" round="2">
Both round-1 blockers resolved. `docs/ARCHITECTURE.md:2159` now reads `"commit_short_sha": "<8-hex>"`, matching the `sha.get(..8)` implementation and the integration test assertion. `docs/ARCHITECTURE.md:2128` now reads `drifts` is `[]` with no trailing "(or omitted)" parenthetical, correctly reflecting that `ConsistencyBlock.drifts` is always serialised. No other documentation drift found in scope for T-007.
</review>
