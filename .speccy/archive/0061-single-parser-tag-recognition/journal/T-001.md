---
spec: SPEC-0061
task: T-001
generated_at: 2026-06-12T07:44:15Z
---

<implementer date="2026-06-12T07:44:15Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: area = the two test crates' VET.md-construction surfaces and the exported `speccy_core::parse` renderers. Reuse-as-is: the production renderers `render_fresh_vet_frontmatter` and `render_vet_section_heading` (already `pub`, re-exported from `speccy_core::parse`) and `validate_and_render_vet_block` + `VetBlockInputs` / `VetBlockKind` (also exported) — all composed directly by both helpers, so even the gate block is grammar-valid by construction rather than hand-formatted. Extend: `speccy-cli/tests/common/mod.rs::write_fresh_pass_vet_md` (gained a `spec_id` param and now delegates to the new `write_vet_md`); `speccy-core/tests/next_priority.rs::write_vet_md` (gained a `spec_id` param, now delegates to a new private `render_vet_md` over the renderers). Write-fresh: the cli `render_vet_md` / `write_vet_md` / `render_vet_block` + `VetTestBlock` enum in `common/mod.rs`, and the core-crate `render_vet_md` in `next_priority.rs` — searched both test crates for an existing renderer-backed VET.md builder (`grep render.*vet`, `Invocation`/`<gate>` scans) and found only hand-rolled string literals, so a per-crate helper is genuinely new (DEC-004 requires one per crate; integration-test binaries cannot share a module).
- Completed: Added a renderer-backed VET.md helper to each test crate (cli: `render_vet_md` + `write_vet_md` + `render_vet_block`/`VetTestBlock` in `speccy-cli/tests/common/mod.rs`; core: `render_vet_md` + rewritten `write_vet_md` in `speccy-core/tests/next_priority.rs`), both composing the exported production renderers plus a `validate_and_render_vet_block` gate. Rewrote `write_fresh_pass_vet_md` to delegate (signature gained `spec_id`); updated its three callers in `next_derived.rs`. Migrated the hand-rolled valid VET.md in `next_text.rs`, `next_json.rs`, the `next_priority.rs` private builder, and the valid `journal_show.rs` fixture onto the helpers. Left `lint_vet.rs` and the open (gate-less) `journal_show.rs` fixture hand-rolled per DEC-005. Added one CHK-007 round-trip test per crate. `extra_body_line` (gate-body) and `leading` (pre-gate blocks) params thread through for T-002's spoof fixture and the drift-review+gate fixture respectively.
- Undone: Nothing in REQ-004's scope. CHK-008's REPORT.md audit entry is recorded at ship time (speccy-ship), not by this task. T-002 (the spoof fixture using `extra_body_line`) and T-003 remain pending by design.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0061-single-parser-tag-recognition/evidence/T-001.md`. Roll call for CHKs under REQ-004:
  - CHK-007 (renderer-backed helper output parses; terminal gate carries the (verdict, tasks_hash)): demonstrated → evidence Scenario 1 covers the red/green pair, proven by `chk007_core_helper_output_round_trips_through_parser` (`speccy-core/tests/next_priority.rs`) and `chk007_cli_helper_output_round_trips_through_parser` (`speccy-cli/tests/next_json.rs`).
  - CHK-008 (post-migration audit: no hand-rolled valid `## Invocation`/`<gate>` outside a helper, recorded in REPORT.md): judgment-only → the migration is complete in the diff (all valid fixtures routed; `lint_vet.rs` + open `journal_show.rs` fixture carved out per DEC-005), but the auditable "recorded in REPORT.md" half is a ship-time artifact; reviewer-business / reviewer-style judge the diff-level completeness.
- Discovered issues: The in-tree SPEC-0061 SPEC.md (authored by speccy-plan/amend, not this task) violated the project's own `blank-line-after-each-close-tag` convention — five `</scenario>` immediately followed by `</requirement>` — which fails the `speccy-core` `in_tree_specs::every_in_tree_spec_md_has_blank_line_after_each_close_tag` test and thus blocks the `cargo test --workspace` hygiene gate. Inserted the five missing blank lines (whitespace-only; speccy normalizes whitespace before hashing, so no spec/tasks hash drift surfaced — confirmed via `speccy next SPEC-0061 --json` showing no out-of-sync drift). This was the minimum edit needed to clear the gate; it touches no Requirement text.
- Procedural compliance: Followed /speccy-work first-attempt branch (clean tree, no journal → first-attempt shape). No source-module (`resources/`) edits were needed — the friction was in dogfood SPEC data, not a shipped skill/template. Mid-task I briefly ran `git stash` to confirm the in_tree lint failure pre-existed my code; immediately `git stash pop`-ed and verified all changes restored intact before continuing.
</implementer>
<review date="2026-06-12T07:45:46Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security exposure in this slice. Changes are confined to test fixtures
(`speccy-cli/tests/common/mod.rs`, `next_priority.rs`, `journal_show.rs`,
`next_json.rs`, `next_text.rs`, `next_derived.rs`) plus a whitespace-only
SPEC.md edit and the TASKS.md state flip — no production code, no auth/authz
boundary, no secret handling, no new input-validation surface. The helper
params (`extra_body_line`, `leading`) feed test-controlled strings into the
production VET renderers, not any privileged path, and fixtures use inert
placeholder values (`feedface…` hash, model "m", fixed dates). The
gate-spoofing hardening that is SPEC-0061's security payoff lands in T-002/T-003
(the parser swap); T-001 keeps the byte-scanner by design and weakens no
existing protection.
</review>
<review date="2026-06-12T07:46:26Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
Pass. Test-only fixture migration; no production code touched. Reviewed control flow of both renderer-backed helpers: cli `render_vet_md`/`render_vet_block`/`write_vet_md` in `speccy-cli/tests/common/mod.rs` and core `render_vet_md`/`write_vet_md` in `speccy-core/tests/next_priority.rs`. No inverted conditions, off-by-ones, or boundary defects: the `extra_body_line` `if let Some` append and the `leading`-block loop are straight concatenation; verdict/error handling propagates via `?`/`TestResult` so an out-of-domain verdict fails loud rather than producing a malformed fixture. `write_fresh_pass_vet_md` preserves prior semantics (`sha256_hex(tasks_md)` + hard-coded `passed`) and the three `next_derived.rs` callers pass each test's matching `spec_id`. The CHK-007 round-trip tests use a binding `matches!` guard, not a vacuous assertion. Verified green locally: `chk007_core_helper_output_round_trips_through_parser`, `chk007_cli_helper_output_round_trips_through_parser`, and all migrated files (next_json 13, next_text 6, journal_show 6, next_derived 11) pass. The extra blank line the cli helper inserts between `leading` blocks and the terminal gate is tolerated by `parse_vet_in_flight` (journal_show round-trip is green), so it is not a defect.
</review>
<review date="2026-06-12T07:46:40Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-007 is demonstrated by two real round-trip tests, not mocks:
`chk007_core_helper_output_round_trips_through_parser`
(`speccy-core/tests/next_priority.rs`) and
`chk007_cli_helper_output_round_trips_through_parser`
(`speccy-cli/tests/next_json.rs`) both render via the per-crate helper and
drive the production `parse_vet_in_flight`, asserting the terminal
`VetBlock::Gate` carries the requested `(verdict, tasks_hash)`. The assertion
would fail on a wrong verdict/hash and the `?` would fail on missing/malformed
frontmatter, so it gates the real behaviour. CHK-008's `judgment-only` label is
plausible (the "recorded in REPORT.md" half is a ship-time artifact); I
independently audited diff-level migration completeness via a `## Invocation` /
`<gate>` scan across both test crates: every valid VET.md fixture now routes
through a renderer-backed helper, and the only remaining hand-rolled strings are
the DEC-005 carve-outs (`lint_vet.rs`, the open gate-less
`journal_show.rs:263` fixture) plus the append-boundary spoof/malformed inputs
in `journal_append_vet.rs` (out of the REQ-004 migration set — those are block
bodies / deliberately-invalid grammar, not valid VET.md fixtures). Evidence
red/green pair uses scoped per-test invocations, halves differ materially, and
both test names appear in the diff — no fabrication pattern matched. Naming
follows the existing `chkNNN_*` convention.
</review>
<review date="2026-06-12T07:47:06Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Style is clean; no blocking findings.

Conventions checked:
- Naming: `render_vet_md`, `write_vet_md`, `render_vet_block`, `VetTestBlock` all follow the project's snake_case / PascalCase Rust conventions and align with the surrounding helper names in both crates.
- Inline `use` placement: `common/mod.rs` uses inline `use` statements inside function bodies for the new renderer imports. This matches the pre-existing pattern (`sha2::Digest` at line 139); not a deviation.
- `#![allow(clippy::expect_used, ...)]` in `next_priority.rs` is pre-existing and not introduced by this diff.
- Dead code suppression: the pre-existing `#![expect(dead_code, reason = "...")]` in `common/mod.rs` correctly covers the new `VetTestBlock` enum and helper functions, which are a subset of the shared-helper pattern the attribute was written to cover.
- No unused imports: `render_vet_md` and `write_vet_md` both have call sites in `next_json.rs`; all other imports in `next_text.rs`, `journal_show.rs`, and `next_priority.rs` are used.
- Reuse survey: present and honest. The survey names `render_fresh_vet_frontmatter`, `render_vet_section_heading`, `validate_and_render_vet_block`, `VetBlockInputs`, `VetBlockKind` — all exist in `speccy_core::parse` and the diff calls them. No reinvention found.
- Migration completeness: no hand-rolled `## Invocation` / `<gate>` literal string remains in `next_text.rs`, `next_json.rs`, or `next_priority.rs`. The surviving hand-rolled string in `journal_show.rs:274` is the intentionally-open (gate-less) DEC-005 carve-out fixture.
- Docstrings on new `pub` helpers are present and accurate.
- Hygiene markers (cargo test, clippy, fmt, deny) reported passing in the implementer block.
</review>
<review date="2026-06-12T07:47:52Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-004 satisfied end to end. Each test crate gained exactly one renderer-backed VET.md helper composed from the exported production renderers — `speccy-cli/tests/common/mod.rs` (`render_vet_md` / `write_vet_md` / `render_vet_block`) and `speccy-core/tests/next_priority.rs` (`render_vet_md` / `write_vet_md`) — matching DEC-004's per-crate shape with no test-construction surface added to production `speccy-core/src`. All four `<done-when>` bullets map: (1) the audit grep shows no hand-rolled valid `## Invocation`/`<gate>` string outside a helper — remaining hits are doc comments, the carved-out `lint_vet.rs`, the carved-out open `journal_show.rs` fixture, and `journal_append_vet.rs` (which drives the production `speccy journal append` command and never writes a hand-rolled fixture, so it is correctly out of the migration set REQ-004/DEC-005 enumerated); (2) `next_priority.rs`, `next_text.rs`, `next_json.rs`, `common/mod.rs`, and the valid drift-review+gate fixture in `journal_show.rs` are all routed through a helper; (3) `lint_vet.rs` is untouched and the gate-less `journal_show.rs` fixture stays hand-rolled per DEC-005; (4) `cargo test --workspace` is green (verified). CHK-007 is proven by one round-trip test per crate (`chk007_core_helper_output_round_trips_through_parser`, `chk007_cli_helper_output_round_trips_through_parser`) — both ran and passed, and each asserts the parsed terminal `VetBlock::Gate` carries the exact `(verdict, tasks_hash)`, so a malformed helper would fail the test. CHK-008's "recorded in REPORT.md" half is a ship-time artifact outside T-001's `<done-when>`, not a per-task blocker. The whitespace-only SPEC.md edit (five blank lines after `</scenario>`) is a formatting-gate fix, not a Requirement-text change, and does not shift the contract. No non-goal breached: no grammar change, no new tag-recognition primitive, two per-crate helpers rather than one shared function.
</review>
