# Evidence: T-002 — README pin-assignment table and override example

## Verification probe: speccy-work + sonnet[1m] on same line

### Before (git HEAD, pre-edit)

```
$ git show HEAD:README.md | Select-String -Pattern 'speccy-work.*sonnet\[1m\]'

| `speccy-work`           | `model: sonnet[1m]`, `effort: medium`   | `model = "gpt-5.5"`, reasoning effort medium | yes               |
```

One match found (line 256 of HEAD:README.md).

### After (working tree, post-edit)

```
$ Select-String -Pattern 'speccy-work.*sonnet\[1m\]' README.md

(no output — zero matches)
```

Zero matches. The scenario "when grepped for any line containing both `speccy-work` and `sonnet[1m]`, then zero matches are found" passes.

## Git diff confirming both surgical edits

```diff
-| `speccy-work`           | `model: sonnet[1m]`, `effort: medium`   | `model = "gpt-5.5"`, reasoning effort medium | yes               |
+| `speccy-work`           | `model: opus[1m]`, `effort: low`        | `model = "gpt-5.5"`, reasoning effort medium | yes               |

-  reproducibility: change `model: sonnet[1m]` to
-  `model: claude-sonnet-4-6[1m]` in
+  reproducibility: change `model: opus[1m]` to
+  `model: claude-opus-4-7[1m]` in
```

Two hunks, no other README lines changed. Every other table row is byte-identical to HEAD.

## Hygiene gates (post-edit working tree)

```
$ cargo test --workspace
test result: ok. ... finished in ...s   (all crates)
exit: 0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s)
exit: 0

$ cargo +nightly fmt --all --check
exit: 0

$ cargo deny check
advisories ok, bans ok, licenses ok, sources ok
exit: 0
```

All four gates exit 0.
