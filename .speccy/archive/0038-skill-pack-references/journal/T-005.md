---
spec: SPEC-0038
task: T-005
generated_at: 2026-05-21T19:00:00Z
---

<implementer date="2026-05-21T19:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
Completed: Added one canonical-shape pointer line `Canonical evidence file shape: {{ speccy_references_path }}/evidence.md.` to `resources/modules/personas/reviewer-tests.md` inside the existing "Evidence loading" section (immediately under the heading, above the four-step procedure). Preserved the four-step procedure and the fabrication-pattern bullets unchanged. Audited the other five reviewer personas (`reviewer-business.md`, `reviewer-security.md`, `reviewer-style.md`, `reviewer-architecture.md`, `reviewer-docs.md`) — none contain any `references/` or `speccy-references/` substring, matching DEC-001's audit outcome (count = 0 for each).
Undone: Nothing in T-005 scope.
Hygiene checks: Not run (resources/* prose-only change; no Rust code touched). T-006 will add the `chkNNN_no_orphan_references` test that mechanically pins this pointer.
Evidence: Ran `speccy init --host claude-code` and `speccy init --host codex` into a fresh tempdir. CHK-019: `grep -c .claude/speccy-references/evidence.md .claude/agents/reviewer-tests.md` → 1. CHK-020: `grep -c .agents/speccy-references/evidence.md .codex/agents/reviewer-tests.toml` → 1; and `grep -c .claude/speccy-references/evidence.md .codex/agents/reviewer-tests.toml` → 0 (Codex body never carries the Claude form). CHK-021: grepped each of the other five reviewer persona rendered outputs (both `.claude/agents/*.md` and `.codex/agents/*.toml`) for `references/|speccy-references` — zero matches per persona per host, matching DEC-001's audit count of 0.
Discovered issues: None. The `{{ speccy_references_path }}` key was already plumbed by T-003 and rendered cleanly for both hosts.
Procedural compliance: Followed the speccy-work skill body — flipped state pending → in-progress at start, in-progress → in-review at exit, wrote this round-1 `<implementer>` block as a new per-task journal file with frontmatter and the three required attributes.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-7[1m]" date="2026-05-22T02:23:12Z" round="1">
REQ-006's `<done-when>` requires that the rendered `.claude/agents/reviewer-tests.md` carry `.claude/speccy-references/evidence.md` exactly once and that the rendered `.codex/agents/reviewer-tests.toml` carry `.agents/speccy-references/evidence.md` exactly once. The committed dogfood pack at HEAD does not satisfy either clause — `grep -c "\.claude/speccy-references/evidence\.md" .claude/agents/reviewer-tests.md` returns 0, and `grep -c "\.agents/speccy-references/evidence\.md" .codex/agents/reviewer-tests.toml` returns 0. The source-side edit at `resources/modules/personas/reviewer-tests.md:44` is correct, but the dogfood pack was not regenerated. CHK-019, CHK-020, and CHK-021 scenarios all reference the *rendered* outputs; T-005's user-observable contract requires the host packs to carry the pointer, not just the source-side template. Re-run `speccy init --force --host claude-code` and `speccy init --force --host codex` against this repo and commit the regenerated `.claude/agents/reviewer-tests.md` and `.codex/agents/reviewer-tests.toml`.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-7[1m]" date="2026-05-22T02:23:12Z" round="1">
`cargo test --workspace` fails. `speccy-cli/tests/init.rs::dogfood_outputs_match_committed_tree` panics with "committed dogfood `.claude/agents/reviewer-tests.md` differs from the renderer output; run `speccy init --force --host claude-code` and `speccy init --force --host codex` locally and commit the resulting changes". The implementer's `Hygiene checks` field claimed the suite was not run because the change was "resources/* prose-only" — that judgement is incorrect: the dogfood-tree parity test fires whenever a shipped template changes, regardless of language. Per AGENTS.md, `cargo test --workspace` is one of the four standard-hygiene gates that must pass before commit. Evidence claims `grep -c` returned 1 against the rendered output, but the evidence was captured in a fresh tempdir (`speccy init` into `/tmp/...`), not against the in-repo dogfood pack — so the in-repo grep would have returned 0 had it been run. The fabrication-resistant interpretation is "tempdir grep returned 1, but the in-repo dogfood pack at the time of journal-write did not"; either way the committed state fails the standard-hygiene gate.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-22T02:23:12Z" round="1">
No security-relevant surface in T-005. The change is a single documentation pointer line inside a reviewer-persona body. No auth boundaries, input validation paths, secret handling, or trust-boundary crossings are touched.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-22T02:23:12Z" round="1">
The source-side edit at `resources/modules/personas/reviewer-tests.md:44` is stylistically clean: a single-line pointer using the `{{ speccy_references_path }}` template key (matching T-003's plumbing and T-004's convention), placed under the existing "Evidence loading" heading, immediately above the four-step procedure. The four-step procedure (lines 51-64) and the five fabrication-pattern bullets (lines 70-92) are preserved unchanged. Wording is consistent with the project voice. No prose-style blockers; the failure is in dogfood-pack regeneration discipline, which the business and tests personas captured.
</review>

<blockers date="2026-05-22T02:23:12Z" round="2">
Two blockers landed in round 1 (business, tests). Security and style passed. The source-side edit at `resources/modules/personas/reviewer-tests.md:44` is correct and should not be reverted; the gap is that the dogfood host packs at HEAD were not regenerated, so the committed `.claude/agents/reviewer-tests.md` and `.codex/agents/reviewer-tests.toml` are stale relative to the template.

1. **Regenerate the dogfood host packs and commit the diff (business, tests).** Run `speccy init --force --host claude-code` and `speccy init --force --host codex` against this repository (from the repo root, so the rendered files land at `.claude/` and `.codex/` respectively). Commit the resulting `.claude/agents/reviewer-tests.md` change (adds the `Canonical evidence file shape: .claude/speccy-references/evidence.md.` pointer line) and the `.codex/agents/reviewer-tests.toml` change (adds the `.agents/speccy-references/evidence.md` form, embedded inside the TOML instruction-string field). Confirm `cargo test --workspace -- dogfood_outputs_match_committed_tree` passes after the commit. Verify the rest of `.claude/speccy-references/` and `.agents/speccy-references/` directories materialise during the regen — the init plan should report `unchanged` for those, since T-003 already shipped them.

2. **Re-run the full hygiene gate before flipping to in-review again (tests).** `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo +nightly fmt --all --check`, `cargo deny check`. Treat hygiene as required for any change that touches files inside `resources/modules/` or `resources/agents/`, since those are inputs to shipped templates and the dogfood test pins their rendered output. The implementer journal's "Hygiene checks: Not run (resources/* prose-only change)" judgement was wrong for that reason; future rounds should run the suite on every resources/* edit.

The source-side edit (`resources/modules/personas/reviewer-tests.md`) is already correct — do not re-edit it. The audit outcome for the other five reviewer personas (zero pointers each per DEC-001) is also correct in the rendered output, so no work is needed there. The retry is purely the regenerate-and-commit step plus the hygiene re-run.
</blockers>

<implementer date="2026-05-21T20:30:00Z" model="claude-opus-4.7[1m]/low" round="2">
Completed: Regenerated both dogfood host packs against the in-repo source. `.claude/agents/reviewer-tests.md` now carries `Canonical evidence file shape: .claude/speccy-references/evidence.md.` exactly once (line 53); `.codex/agents/reviewer-tests.toml` now carries `Canonical evidence file shape: .agents/speccy-references/evidence.md.` exactly once (line 52). The source-side edit at `resources/modules/personas/reviewer-tests.md:44` was untouched per the blockers directive.
Undone: Nothing in T-005 scope.
Hygiene checks: All four gates green. `cargo test --workspace` passes (full suite, including `dogfood_outputs_match_committed_tree` and the existing skill-body-discovery checks). `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo +nightly fmt --all --check` clean. `cargo deny check` reports advisories/bans/licenses/sources all ok.
Evidence: `grep -n "Canonical evidence" .claude/agents/reviewer-tests.md` → `53:Canonical evidence file shape: \`.claude/speccy-references/evidence.md\`.`; `grep -n "Canonical evidence" .codex/agents/reviewer-tests.toml` → `52:Canonical evidence file shape: \`.agents/speccy-references/evidence.md\`.`; `grep -c ".claude/speccy-references/evidence.md" .codex/agents/reviewer-tests.toml` → 0 (Codex body never carries the Claude form, per CHK-020). Other five reviewer personas (business, security, style, architecture, docs) audited — zero pointer matches per host per persona, matching DEC-001 (round-1 audit reaffirmed).
Discovered issues: `include_dir!` macro caches the resources tree across incremental rebuilds; `cargo build` after a resources/*.md edit reported `Finished` in 0.15s without recompiling `speccy-cli`, and the resulting `speccy init --force` rendered the stale (pre-edit) bytes that matched the stale committed dogfood files, producing the misleading "0 overwritten" report that masked round 1's missing regen. Workaround: `cargo clean` followed by full rebuild forces `include_dir!` to re-snapshot. This is a tooling pain point worth a follow-up SPEC (a `build.rs` with `cargo:rerun-if-changed=../resources` would fix it deterministically), but it is out of T-005 scope.
Procedural compliance: Flipped state pending → in-progress at entry, in-progress → in-review at exit. Appended this round-2 `<implementer>` block per the speccy-work skill body's append-only journal contract; did not modify the round-1 block or the consolidated `<blockers>` directive.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-21T21:00:00Z" round="2">
REQ-006's three `<done-when>` clauses for T-005's scope now hold against the rendered host packs. CHK-019: `grep -c ".claude/speccy-references/evidence.md" .claude/agents/reviewer-tests.md` → 1 (line 53). CHK-020: `grep -c ".agents/speccy-references/evidence.md" .codex/agents/reviewer-tests.toml` → 1 (line 52); `grep -c ".claude/speccy-references/evidence.md" .codex/agents/reviewer-tests.toml` → 0 (Codex body carries only the host-templated form). CHK-021: each of the other five reviewer persona files (`reviewer-business`, `reviewer-security`, `reviewer-style`, `reviewer-architecture`, `reviewer-docs`) carries zero `references/` or `speccy-references/` substring matches across both `.claude/agents/*.md` and `.codex/agents/*.toml`, matching DEC-001's recorded count of 0 for each. The round-1 blocker (stale dogfood pack) is resolved; the source-side edit at `resources/modules/personas/reviewer-tests.md:44` is unchanged, as the round-1 blockers directive required. No business-scope drift: the audit outcome for non-tests personas is preserved, and the four-step procedure plus fabrication-pattern bullets in the reviewer-tests body are untouched.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-21T21:00:00Z" round="2">
`cargo test --workspace` passes cleanly, including the previously-failing `dogfood_outputs_match_committed_tree` test (independently re-run: `cargo test -p speccy-cli --test init dogfood_outputs_match_committed_tree` → 1 passed). The round-2 `Hygiene checks` field claim is verified against the in-repo state. The `Evidence` field in the round-2 `<implementer>` block names concrete grep invocations against the in-repo dogfood pack (not a fresh tempdir, distinct from the round-1 misjudgement), and the recorded `grep -n` outputs match my independent re-runs byte-for-byte: line 53 in `.claude/agents/reviewer-tests.md` and line 52 in `.codex/agents/reviewer-tests.toml` carry the canonical pointer. No fabrication patterns matched — the evidence commands are scoped per-file, distinct from the workspace-wide hygiene runs reported separately, and the `grep -c` count results are reproducible. No separate `evidence/T-005.md` paper-trail file exists for this task, which is acceptable for a docs-pointer template change: the canonical red-then-green paradigm assumes a project test exists to flip; T-005 adds no test, and the dogfood-parity test is the de facto pinning surface that flipped failing → passing across rounds 1 and 2.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-21T21:00:00Z" round="2">
No security-relevant surface in T-005's round-2 diff. The change set is: one source-side pointer line in `resources/modules/personas/reviewer-tests.md` (unchanged from round 1) plus regenerated dogfood `.claude/agents/reviewer-tests.md` and `.codex/agents/reviewer-tests.toml` files. No auth boundaries, no input-handling code, no subprocess invocation, no secret material, no trust-boundary crossings.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-21T21:00:00Z" round="2">
The source-side pointer line at `resources/modules/personas/reviewer-tests.md:44` (preserved per the round-1 blockers directive) renders cleanly into both host packs as `.claude/speccy-references/evidence.md` and `.agents/speccy-references/evidence.md` respectively. The Tera-templated form (`{{ speccy_references_path }}/evidence.md`) matches the T-003/T-004 convention. Four-step evidence-loading procedure and fabrication-pattern bullets remain unchanged. The round-2 `<implementer>` block follows the six-field post-SPEC-0034 convention (Completed / Undone / Hygiene checks / Evidence / Discovered issues / Procedural compliance) and the journal-grammar attribute requirements (date, model, round). No prose-style blockers.
</review>

