---
spec: SPEC-0038
task: T-002
generated_at: 2026-05-22T01:30:00Z
---

<implementer date="2026-05-22T01:30:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Deleted the four orphan paths (`resources/modules/personas/implementer.md`, `resources/modules/personas/planner.md`, `resources/modules/examples/evidence.md`, and the now-empty `resources/modules/examples/` directory). Salvaged planner bullets (bounded-scope + decisions-belong-in-Decisions guidance) into a new `## What to consider` section in `resources/modules/skills/speccy-plan.md`. Salvaged implementer bullets (re-read `<done-when>`/`<behavior>`, suggested-files-hint may be stale, no surprise feature flags / abstraction layers) into a new `## What to consider` section in `resources/modules/phases/speccy-work.md`. Removed `"personas/implementer.md"` and `"personas/planner.md"` from `PERSONA_FILES` in `speccy-cli/tests/skill_body_discovery.rs`. Retired `render_speccy_examples_pack` (from `render.rs`) and `append_speccy_examples_items` (from `init.rs`) along with the now-dead `collect_all_files` walker and `use crate::render::render_speccy_examples_pack` imports. Deleted four `t002_*examples*` tests plus `dogfood_examples_pack_matches_committed_tree` and the three `examples`/`render_speccy_examples_pack` unit tests under `render.rs`'s test module that depended on the retired pack. Dropped `planner.md`/`implementer.md` from `PERSONA_FILES` and the `["planner.md", "implementer.md"]` required-set in `speccy-cli/tests/skill_packs.rs`'s `persona_names_match_registry`; removed the now-broken `implementer_persona_friction_reference` test (SPEC-0014 CHK-004 anchor; the friction convention now lives in AGENTS.md, which `agents_md_friction_paragraph` still covers). Removed the stale `.speccy/examples/` directory from the dogfooded workspace. Re-ran `speccy init --force` for both hosts to refresh dogfood (only `.claude/agents/speccy-work.md`, `.claude/skills/speccy-plan/SKILL.md`, `.agents/skills/speccy-plan/SKILL.md`, `.codex/agents/speccy-work.toml` changed).
- Undone: (none)
- Commands run: `cargo build --workspace`, `cargo run -- init --force --host claude-code`, `cargo run -- init --force --host codex`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo +nightly fmt --all --check`
- Exit codes: 0, 0, 0, 0, 0, 0
- Discovered issues: Task body lists only file deletes, but `speccy-cli/src/render.rs::render_speccy_examples_pack`, `speccy-cli/src/init.rs::append_speccy_examples_items`, two `tests/init.rs` test fixtures, three `render.rs` unit tests, and a third pair of `tests/skill_packs.rs` assertions all hard-coded `modules/examples/evidence.md`. Without retiring those, `cargo test --workspace` would have broken on missing-bundle-subpath errors. The SPEC text in T-003 explicitly says "the existing `render_speccy_examples_pack` that this SPEC's T-002 retires", so this work is in-scope for T-002 even though the task body didn't enumerate it; I treated the `cargo test --workspace` clean-run scenario as load-bearing and retired the now-dead plumbing. Also discovered that `skill_packs.rs::implementer_persona_friction_reference` (anchored to the now-deleted `personas/implementer.md`) was the only consumer of SPEC-0014 CHK-004's persona-side assertion; the AGENTS.md-side assertion (`agents_md_friction_paragraph`) still holds, which is where the friction convention now actually lives.
- Procedural compliance: (none)
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T02:00:41Z" round="1">
REQ-005 satisfied: four orphan paths deleted, planner/implementer bullets salvaged into `speccy-plan.md` / `speccy-work.md` matching the SPEC's named targets (bounded-scope, decisions-belong-in-Decisions, feature-flag/abstraction-layer guardrail, suggested-files-hint warning, `<done-when>` / `<behavior>` re-reading). The discovered-issue scope-expansion (retiring `render_speccy_examples_pack`, `append_speccy_examples_items`, and their dependent tests in `init.rs` / `render.rs` / `skill_packs.rs` plus the `.speccy/examples/` dogfood directory) is required by task-scenario #5 (`cargo test --workspace` must pass) and explicitly anticipated by T-003's body ("the existing `render_speccy_examples_pack` that this SPEC's T-002 retires"). No SPEC non-goal violated; salvage discipline guard (no `ARCHITECTURE.md` mentions in `resources/modules/`) holds.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T02:00:41Z" round="1">
All five task-scenario predicates verified directly: (1) four orphan paths absent; (2) `phases/speccy-work.md` carries salvaged feature-flag/abstraction-layer guardrail bullet; (3) `skills/speccy-plan.md` carries salvaged bounded-scope bullet; (4) `PERSONA_FILES` const in `skill_body_discovery.rs` no longer contains `personas/implementer.md` or `personas/planner.md`; (5) `cargo test --workspace` passes (every crate green). `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all --check` also pass. Note: this task is a deletion/salvage task with no behavior-changing code path under test, so the absent `Evidence:` field in the implementer journal entry does not block — the journal field semantics (`Hygiene checks` / `Evidence` per SPEC-0034) are not yet taught by the implementer phase body (that update is T-004's job), and the pre-SPEC-0034 `Commands run` / `Exit codes` labels the implementer used reflect the prompt they actually read, which is also the diagnosis SPEC-0038's SPEC.md explicitly calls out for SPEC-0037/T-007.
</review>

<review persona="security" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T02:00:41Z" round="1">
No security surface touched. Diff is: four file deletions, two doc-bullet salvages into Markdown skill bodies, removal of dead Rust functions plus their test consumers (`render_speccy_examples_pack`, `append_speccy_examples_items`, `t002_*examples*`, `dogfood_examples_pack_matches_committed_tree`, `implementer_persona_friction_reference`), and a `.speccy/examples/` directory removal. No auth boundaries, no input validation, no secret handling, no new code paths exposed to network or user input.
</review>

<review persona="style" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T02:00:41Z" round="1">
Salvaged prose matches the surrounding `## What to consider` heading convention used by other phase bodies and is placed at the conventional position between `## When to use` and `## Steps`. Bullet form, hyphen prefix, sentence casing all consistent with existing bullets in `speccy-plan.md` / `speccy-work.md`. The retired Rust code removal is clean: imports dropped, dead-code-only `collect_all_files` walker removed, three associated unit tests removed in lockstep, no stranded `#[expect(...)]` or `#[allow(...)]` annotations left behind. `cargo +nightly fmt --all --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` both pass.
</review>

