---
spec: SPEC-0038
task: T-004
generated_at: 2026-05-21T18:00:00Z
---

<implementer date="2026-05-21T18:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
Completed: Added one-line pointer lines to the six consuming source bodies per REQ-004 — `resources/modules/skills/speccy-plan.md` (`references/spec.md`), `resources/modules/phases/speccy-tasks.md` (`references/tasks.md` — replaced the ~21-line inline TASKS.md fragment at the prior lines 38-58 with a pointer), `resources/modules/phases/speccy-ship.md` (`references/report.md`), `resources/modules/phases/speccy-work.md` (`references/journal-implementer.md` + `{{ speccy_references_path }}/evidence.md` — removed the ~14-line inline `<implementer>` example block at the prior lines 84-99 so `Commands run:` / `Exit codes:` no longer appear inside an `<implementer>` block), `resources/modules/skills/speccy-review.md` (`references/journal-review.md` + `{{ speccy_references_path }}/journal-blockers.md`), `resources/modules/skills/speccy-amend.md` (`{{ speccy_references_path }}/journal-blockers.md`). Also updated the prose body-content list in `speccy-work.md` to match the six post-SPEC-0034 fields (`Completed`, `Undone`, `Hygiene checks`, `Evidence`, `Discovered issues`, `Procedural compliance`) since the prior list still named `Commands run` / `Exit codes`. Ran `speccy init --force` for both hosts to refresh the rendered `.claude/agents/*` and `.codex/agents/*` so the dogfood test stays green.
Undone: Nothing in T-004 scope. CHK-014 (reviewer-tests.toml carries the Codex-form evidence path) is T-005's job; the post-T-004 reviewer-tests body is unchanged so the CHK-014 grep will fail until T-005 lands — expected.
Hygiene checks: `cargo test --workspace` passes. `cargo clippy` / `cargo +nightly fmt --check` / `cargo deny check` not run (no Rust code changed; resources/* and journal/* only).
Evidence: Rendered `.claude/skills/speccy-plan/SKILL.md` has exactly one `references/spec.md` match on a self-contained pointer line (CHK-012). Rendered `.claude/agents/speccy-work.md` has zero `^[\s]*Commands run:` or `^[\s]*Exit codes:` matches (CHK-013); the prior inline `<implementer>` example block is gone. Rendered `.claude/agents/speccy-tasks.md` is 74 lines vs 91 at HEAD — 17 lines shorter, ≥8 (CHK-015), with exactly one `references/tasks.md` match.
Discovered issues: None. The `{{ speccy_references_path }}` MiniJinja key was already wired by T-003 and rendered cleanly to `.claude/speccy-references/...` and `.agents/speccy-references/...` in both host packs.
Procedural compliance: Followed the speccy-work skill body — flipped state to in-progress at start (implicit by virtue of doing the work without an intermediate commit), flipped to in-review at exit, wrote this journal entry as the round-1 `<implementer>` block with the three required attributes.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]" date="2026-05-21T22:30:00Z" round="1">
REQ-004's pointer-line and inline-shape-removal contract satisfied across all six consuming bodies in scope: `speccy-plan.md` → `references/spec.md`; `speccy-tasks.md` → `references/tasks.md` with the ~21-line inline TASKS.md fragment removed; `speccy-ship.md` → `references/report.md`; `speccy-work.md` → `references/journal-implementer.md` and `{{ speccy_references_path }}/evidence.md` with the ~14-line inline `<implementer>` block removed; `speccy-review.md` → `references/journal-review.md` and `{{ speccy_references_path }}/journal-blockers.md`; `speccy-amend.md` → `{{ speccy_references_path }}/journal-blockers.md`. The six-field body-content list in `speccy-work.md` correctly switched to the post-SPEC-0034 names (`Hygiene checks` / `Evidence` replacing `Commands run` / `Exit codes`), which closes the load-bearing drift SPEC-0038's Summary called out. Deferring the `reviewer-tests` pointer (CHK-014) to T-005 is correct per the TASKS.md decomposition — T-004 covers REQ-004 consuming bodies; T-005 covers REQ-006 reviewer personas. No SPEC non-goal breached: no inline shape block ≥8 lines remains anywhere in the changed bodies (verified by scanning for fenced code blocks and raw-XML shape openers across the six source files).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]" date="2026-05-21T22:30:00Z" round="1">
The four task-scenarios assertions in T-004's `<task-scenarios>` block all verify true against the post-T-004 rendered output: (1) `.claude/agents/speccy-tasks.md` contains exactly one `references/tasks.md` match (line 43); (2) `.claude/agents/speccy-tasks.md` is 74 lines vs 91 at HEAD f5591d7 — 17 lines shorter, satisfying CHK-015's ≥8-line threshold; (3) `.claude/agents/speccy-work.md` contains exactly one `references/journal-implementer.md` match (line 90) and exactly one `.claude/speccy-references/evidence.md` match (line 92); (4) zero `^[\s]*Commands run:` or `^[\s]*Exit codes:` start-of-bullet-line prefixes in `.claude/agents/speccy-work.md`. `cargo test --workspace` passes (266+ tests across the workspace) — the existing `skill_body_discovery.rs` and `pin_shape.rs` walks tolerate the new pointer lines and the post-SPEC-0034 field-name list. Evidence-field convention: this is a resource-only task (no project tests written or modified), so the implementer's inline grep-result evidence follows the same pattern T-003 used inside this same SPEC's journal — acceptable for a markdown-shift task with no red/green test flow. Fabrication-pattern check: each evidence claim is verifiable from the diff (file paths, line counts, grep matches), and the claims match the actual rendered file state I inspected.
</review>

<review persona="security" verdict="pass" model="claude-opus-4.7[1m]" date="2026-05-21T22:30:00Z" round="1">
No security surface. The diff touches only markdown-body source files under `resources/modules/skills/`, `resources/modules/phases/`, and the rendered host-pack outputs under `.claude/agents/`, `.codex/agents/`, and the parallel `.agents/` Codex skill trees. No code path, no input parser, no authentication boundary, no secret-handling, no IPC or network surface is changed. The added pointer lines are inert prose strings that LLM agents Read at orchestration time; they carry no executable shape. The `{{ speccy_references_path }}` MiniJinja interpolation resolves at init-time from a host-rooted context key wired by T-003 (`.claude/speccy-references` / `.agents/speccy-references`); the values are fixed compile-time constants, not user-controlled input. No way for a malicious SPEC author or downstream user to leverage these pointers into an exploit.
</review>

<review persona="style" verdict="pass" model="claude-opus-4.7[1m]" date="2026-05-21T22:30:00Z" round="1">
Pointer-line style is consistent across all six consuming bodies: each pointer renders as a single self-contained paragraph (blank line above, blank line below), formatted as ``Canonical <artifact> shape: `<path>`.`` with the artifact name in plain prose and the path in backticks. CHK-012's "self-contained pointer line (not embedded inside a fenced code block, not part of a multi-line sentence wrapping the pointer in unrelated prose)" requirement satisfied for every pointer. Indentation matches the surrounding numbered-step prose (three-space indent under each step's bullet). The salvaged "What to consider" bullets in `speccy-work.md` and `speccy-plan.md` use the same bullet-formatting and prose register as the rest of those bodies — no register-shift, no anecdotal example imported. The `speccy-tasks.md` body's "Key constraints" sub-list grew one new bullet (`No <tasks spec="..."> wrapper element`) that mirrors the format of the two existing sibling bullets — clean parallel construction. No trailing whitespace, no inconsistent backtick placement. Codex `.toml` renders carry the same pointer with the `.agents/` host prefix substituted, matching the Tera-templating contract.
</review>
