---
spec: SPEC-0038
task: T-006
generated_at: 2026-05-22T01:30:00Z
---

<implementer date="2026-05-22T01:30:00Z" model="claude-opus-4.7[1m]/low" round="1">
Completed: Added `chk022_no_orphan_references` test to `speccy-cli/tests/skill_body_discovery.rs`. The test enumerates ejected reference files by directory-walking the in-tree dogfood host packs (`.claude/skills/*/references/*.md`, `.claude/speccy-references/*.md`, and the `.agents/` Codex counterparts) — no hard-coded file list, so a future eighth reference does not require a test update. For each reference file it asserts: (1) orphan check — at least one consuming body (`.md` under `.claude/skills/` recursive, `.md` directly under `.claude/agents/`, `.md` under `.agents/skills/` recursive, `.toml` directly under `.codex/agents/`) contains the path-substring pointer (`references/<file>` for skill-local; `.claude/speccy-references/<file>` or `.agents/speccy-references/<file>` for host-shared); (2) cross-host byte-identical parity between Claude Code and Codex copies; (3) source-to-host parity against `resources/modules/references/<file>` for both hosts. Failures name the diverging path(s) and the byte offset of the first difference. Consumer enumeration excludes any path containing a `references/` segment so reference files cannot trivially satisfy their own pointer requirement. The test passes against the workspace at HEAD.

Undone: Nothing in scope. T-007 (the final ARCHITECTURE.md documentation update) is the remaining pending task in this SPEC and intentionally lies outside T-006.

Hygiene checks: `cargo test -p speccy-cli --test skill_body_discovery` exits 0 (all 6 tests pass, including the new `chk022_no_orphan_references`); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0; `cargo +nightly fmt --all --check` exits 0; `cargo test --workspace` exits 0 (no regressions in the broader suite).

Evidence: The orphan / parity assertions all run real `fs_err::read[_dir]` operations against the committed dogfood tree at workspace root; the test enumerated 7 reference files (5 skill-local: `speccy-plan/spec.md`, `speccy-tasks/tasks.md`, `speccy-ship/report.md`, `speccy-work/journal-implementer.md`, `speccy-review/journal-review.md`; 2 host-shared: `evidence.md`, `journal-blockers.md`) and confirmed each has a consumer hit on both hosts plus byte-identical parity across source / Claude Code / Codex. The test would fail loudly on any of the four fault-injection scenarios CHK-022..CHK-026 describe (orphan reference file, cross-host divergence, removed Codex sub-agent pointer, source-to-host divergence) because each invariant fires its own assertion with a named-orphan / named-pair message.

Discovered issues: None. The clippy strict config flagged two minor stylistic warnings (`manual_let_else` and `too_many_lines` on the merged enumeration helper) that I addressed by splitting the helper into `collect_skill_local_refs` / `collect_shared_refs` and rewriting one `match` as `let ... else`.

Procedural compliance: Followed the speccy-work skill — flipped state pending → in-progress before implementing, ran `speccy check SPEC-0038/T-006` to read the rendered CHK-022..CHK-026 scenarios, implemented against those exact scenario texts, ran the standard-hygiene quartet (test/clippy/fmt; deny was not run locally — CI will catch any deny regression), flipped state in-progress → in-review on exit. Used the per-task journal file at `.speccy/specs/0038-skill-pack-references/journal/T-006.md` (no inline `<implementer-note>` in TASKS.md). No shipped-skill friction encountered during this task — `/speccy-work` and `speccy check SPEC-0038/T-006` behaved as documented; no skill-body edit required.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-21T22:10:00Z" round="1">
REQ-007's seven done-when clauses all land. (1) New test `chk022_no_orphan_references` follows the `chkNNN_*` convention and the next-free-number rule (CHK-022..026 in the rendered scenarios). (2) `cargo test --workspace` exits 0 with the new test passing — confirmed locally. (3) The orphan injection scenario (CHK-023) is structurally satisfied: the per-host orphan assertion message embeds `r.claude_path.display()` / `r.codex_path.display()`, which for an `orphan.md` planted reference would contain that basename. (4) Cross-host divergence (CHK-024) maps to the explicit "differs from sibling" message naming both paths. (5) Source-to-host divergence (CHK-026) maps to two separate `first_diff` checks against `r.source` that name the source path, the host pack copy, and the byte offset. (6) `.toml` scanning is enabled via `shallow_collect(&root.join(".codex").join("agents"), "toml", ...)` — Codex sub-agent pointers register as consumers per CHK-025. (7) Enumeration is glob-based via `read_dir` walks, not a hard-coded list, so a future eighth reference file does not require a test edit. No business-scope drift: the test lives inside `speccy-cli/tests/skill_body_discovery.rs` per the SPEC, not in `speccy verify` (REQ-007 non-goal honoured — no new lint family added to the CLI). All seven REQ-007 behavior clauses have a direct code-side implementation, and the implementation matches REQ-002's seven-row mapping (5 skill-local + 2 host-shared = 7 reference files, confirmed via the directory listing). Coverage is full and on-spec.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-21T22:10:00Z" round="1">
The test is real, exercises real bytes, and would deterministically fail under each of the four documented fault injections. Real evidence: the implementer's `Hygiene checks` field claims `cargo test -p speccy-cli --test skill_body_discovery` exits 0; I reran it and confirmed (6/6 passing including `chk022_no_orphan_references`). The test uses `fs_err::read[_dir]` against the committed dogfood tree at workspace root — no mocking, no fake fixtures. Enumeration walks `.claude/skills/*/references/`, `.claude/speccy-references/`, and pairs each with its `.agents/` sibling — found exactly 7 reference files per host (5 skill-local + 2 shared), matching REQ-002's seven-row table. Each reference file fires three invariants: orphan (per-host, with the reference-self-exclusion guard so a reference cannot trivially satisfy its own pointer), cross-host parity, source-to-host parity. The four CHK-022..CHK-026 panic-message substrings (`orphan.md`, both diverging paths, host-pack label "Claude Code"/"Codex", byte offset) are all present in the corresponding assertion messages — I traced each scenario to the assertion that would fire. The `first_diff` helper correctly handles unequal-length inputs (returns `Some(min)` when the shorter prefix matches but lengths differ). Soft caveat: no separate evidence/T-006.md paper trail was written for this task, but the test IS the evidence — its current pass-state plus the structural mapping to each fault scenario satisfies the proof-shape obligation for a "test added" task. No fabrication patterns: assertion messages reference real path fields, real byte offsets, real host pack labels. Pass.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-21T22:10:00Z" round="1">
Test-code-only diff; zero production code paths touched, zero new dependencies, zero `unsafe` blocks. Path handling: paths are derived from `env!("CARGO_MANIFEST_DIR")` then `.parent()`-walked one level (matching the `tests/init.rs` helper) and joined with literal subdir names — no external input, no path-traversal surface. Filesystem reads go through `fs_err::read[_dir]` (per the project's preferred-crates rule), not bare `std::fs`. No string-slice indexing, no `[i]` indexing on slices/Vec — the test uses `.get(i)` semantics implicitly via iteration. No `unwrap()` outside the file-scope `#![allow(clippy::expect_used, reason = "test code may .expect() with descriptive messages")]` (which is reasoned and pre-existing) and the centralised `panic_with_message` helper (which carries a scoped `#[expect(clippy::panic, reason = "...")]`). No `expect()` on results without a descriptive message — every callsite uses `unwrap_or_else(|err| panic_with_message(&format!(...)))` with a clear failure message. No secrets, no auth boundaries, no subprocess invocation, no log-injection surface. The only "input" the test consumes is the bytes of reference files inside the same repo. Pass.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-21T22:10:00Z" round="1">
Hygiene quartet all green locally: `cargo test --workspace` (6/6 in the affected file plus the rest of the workspace), `cargo clippy --workspace --all-targets --all-features -- -D warnings` (clean), `cargo +nightly fmt --all --check` (no diff), `cargo deny check` (advisories/bans/licenses/sources all ok — the implementer noted deny was deferred to CI, but it passes locally). Code matches the existing file's style: centralised `panic_with_message` test-only failure path, reasoned `#[expect]` attributes over silent `#[allow]`, `fs_err` everywhere, function decomposition into single-job helpers (`collect_skill_local_refs`, `collect_shared_refs`, `walk_collect`, `shallow_collect`, `first_diff`). The `ReferenceFile` struct is plain data with no over-engineering; the `ConsumerHost` enum is closed-set (matches the prefer-enums-over-booleans rule). The reference-self-exclusion guard (`!s.contains("references/") && !s.contains("references\\")`) handles both forward and backslash path separators — Windows-aware. Doc comments on each helper explain intent at the right level of granularity. Minor non-blocking nit: a few `unwrap_or_else(|| panic_with_message(...))` callsites repeat the "name must be UTF-8" pattern that could fold into one helper, but this is well below the simplify-or-block threshold for a test file. Diff stays within the suggested file (`speccy-cli/tests/skill_body_discovery.rs` only). Pass.
</review>
