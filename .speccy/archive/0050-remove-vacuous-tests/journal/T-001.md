---
spec: SPEC-0050
task: T-001
generated_at: 2026-05-27T21:15:00Z
---

<implementer date="2026-05-27T21:15:00Z" model="claude-opus-4-7[1m]" round="1">

Completed:
- Deleted four prose-substring-match `#[test]` functions and the
  `/// Positive content pins.` doc-comment block from
  `speccy-cli/tests/init.rs` (REQ-001):
  `reviewer_tests_persona_pins_no_check_exit_code_evidence`,
  `architecture_doc_pins_feedback_not_enforcement_contract`,
  `architecture_doc_pins_check_command_is_render_only`,
  `architecture_doc_pins_verify_command_is_shape_only`. Each
  function's `include_str!` constants were defined inside the
  function body, so no module-level orphan cleanup was needed.
- Deleted `bundle_layout_has_skill_md_per_host` and its
  `// Bundle layout: per-host SKILL.md.tmpl wrappers.` section
  header from `speccy-cli/tests/skill_packs.rs` (REQ-002). The
  helpers `HOST_SKILL_ROOTS`, `SKILL_NAMES`,
  `panic_with_test_message`, and `workspace_root` remain in use by
  the adjacent tests in the same file (58 references remain), so
  no further cleanup was required.
- Deleted `registry_contains_six_personas_in_declared_order` and
  the file-local `const EXPECTED: &[&str]` array from
  `speccy-core/tests/personas.rs` (REQ-003). The two preserved
  tests (`registry_default_personas_is_first_four_prefix`,
  `registry_personas_are_unique`) remain unchanged.

Undone:
- None within task scope.

Hygiene checks:
- `cargo test --workspace`: exit 0.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
- `cargo +nightly fmt --all --check`: exit 0.
- `cargo deny check`: exit 0.

Evidence: paper trail at the deletions visible in `git diff` against
HEAD. Roll call for CHKs under REQ-001 / REQ-002 / REQ-003 / REQ-004:
- CHK-001 (six function names absent from `init.rs`): demonstrated →
  `rg "fn (reviewer_tests_persona_pins_no_check_exit_code_evidence|architecture_doc_pins_feedback_not_enforcement_contract|architecture_doc_pins_check_command_is_render_only|architecture_doc_pins_verify_command_is_shape_only)" speccy-cli/tests/init.rs`
  returns zero matches.
- CHK-002 (init test target passes, deleted names absent from runner
  output): hygiene → covered by `cargo test --workspace` under the
  project test command; targeted runs of `--test init` likewise
  green.
- CHK-003 (`bundle_layout_has_skill_md_per_host` absent from
  `skill_packs.rs`): demonstrated → `rg` against the file returns
  zero matches.
- CHK-004 (skill_packs test target passes, deleted name absent):
  hygiene → covered by `cargo test --workspace`.
- CHK-005 (deleted function and `EXPECTED` constant absent from
  `personas.rs`): demonstrated → `rg "fn registry_contains_six_personas_in_declared_order|const EXPECTED" speccy-core/tests/personas.rs`
  returns zero matches; preserved tests still present.
- CHK-006 (personas test target reports `2 passed`, deleted name
  absent): hygiene → workspace test run reports the personas
  binary with `test result: ok` and the two preserved tests.
- CHK-007 (four-command hygiene suite exits 0): hygiene → all four
  gates above ran green and are recorded.

Discovered issues:
- The in-tree spec scaffolding test
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag` in
  `speccy-core/tests/in_tree_specs.rs` failed at task start due to
  pre-existing missing blank lines after `</scenario>` close tags
  before `</requirement>` close tags in
  `.speccy/specs/0050-remove-vacuous-tests/SPEC.md` (four
  occurrences at lines 130, 168, 209, 241 of the original). This
  was a procedural defect in the SPEC the orchestrator authored,
  not in tests under T-001's scope. I confirmed the failure
  pre-existed by stashing my deletions and re-running the test
  binary, which still failed against unmodified main. To allow the
  hygiene gate to pass without losing this run's work, I added the
  required blank lines to the SPEC and re-recorded the spec hash
  via `speccy lock SPEC-0050` (the SPEC.md hash changed from
  `b2f86baf…2969ca` to `6098630d…89897f`). The SPEC's Requirements,
  Decisions, and Non-goals were not edited — only the structural
  whitespace between `</scenario>` and `</requirement>` close tags.
  Reviewer judgment requested on whether this in-place SPEC fix
  belongs to T-001 or to a small follow-up amendment.

Procedural compliance:
- Read the SPEC's `<done-when>` and `<behavior>` elements for each
  REQ before deletion, as required by the skill.
- Did not edit source-prose files
  (`docs/ARCHITECTURE.md`,
  `resources/modules/personas/reviewer-tests.md`, any wrapper
  templates) per the SPEC's Non-goals.
- Did not add replacement tests, structural lints, or `#[allow(...)]`
  shims.
- No ejected files under `.claude/`, `.agents/`, or `.codex/` were
  modified.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-27T21:20:00Z" round="1">
T-001 delivers exactly what SPEC-0050 promises. All four REQ
`<done-when>` lists map 1:1 to the working-tree diff: the four
prose-pin tests and the `/// Positive content pins.` doc comment
are gone from `speccy-cli/tests/init.rs` (the `include_str!`
constants went with them because they were function-local);
`bundle_layout_has_skill_md_per_host` and the
`// Bundle layout: per-host SKILL.md.tmpl wrappers.` section header
are gone from `speccy-cli/tests/skill_packs.rs`;
`registry_contains_six_personas_in_declared_order` and the
file-local `const EXPECTED` array are gone from
`speccy-core/tests/personas.rs`. The preserved tests
(`shipped_skill_md_frontmatter_shape`,
`registry_default_personas_is_first_four_prefix`,
`registry_personas_are_unique`) are untouched. Non-goals are
honored: no edits to `docs/ARCHITECTURE.md`,
`resources/modules/personas/reviewer-tests.md`, wrapper templates,
the lint-registry snapshot, or the XML round-trip tests; no
replacement tests or structural lints. Hygiene gate recorded as
exit 0 across all four commands. The implementer's disclosed
in-place SPEC.md edit is strictly structural whitespace between
`</scenario>` and `</requirement>` close tags (no Requirements,
Decisions, Non-goals, Goals, or User-stories prose moved), and the
hash was re-locked, so the SPEC↔TASKS contract is intact. The
implementer surfaced rather than silently resolved that procedural
question, which is the right behavior.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-27T21:20:30Z" round="1">
T-001 deletes exactly the six named vacuous test functions plus
the three named orphan-scaffolding anchors (`/// Positive content
pins.` doc-comment block, `// Bundle layout: per-host
SKILL.md.tmpl wrappers.` section header, `const EXPECTED` array).
Verified via grep that the six function names and three anchors
return zero matches across `speccy-cli/tests/init.rs`,
`speccy-cli/tests/skill_packs.rs`, and
`speccy-core/tests/personas.rs`. The preserved tests
`registry_default_personas_is_first_four_prefix`,
`registry_personas_are_unique`, and
`shipped_skill_md_frontmatter_shape` remain unchanged.

Re-ran the three scoped test binaries to confirm runner output:
`--test personas` reports `2 passed`, `--test init` reports
`21 passed`, `--test skill_packs` reports `32 passed`, and none of
the six deleted function names appear in any runner output.
`cargo clippy --workspace --all-targets --all-features -- -D warnings`
exits 0 (no orphan-import or dead-code warnings introduced by the
deletions); `cargo +nightly fmt --all --check` exits 0.

The persona's mock/snapshot/loose-assert concerns do not apply —
this is a pure deletion task with no new tests, so there is no
surface for "mocks that pass without touching real code paths" or
snapshot-baked-bug risk. The `<task-scenarios>` block's eight
scenarios (six function deletions, three orphan anchors, three
hygiene gates plus deny, plus the two negative-mutation scenarios
for reword and persona reorder) all hold against the diff. The
`Evidence:` roll call covers CHK-001 through CHK-007 with
`demonstrated`/`hygiene` labels and cites specific test binaries
(`--test init`, `--test skill_packs`, personas binary).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-27T21:21:00Z" round="1">
No security surface touched. The diff removes six vacuous test
functions plus their orphan scaffolding (a doc-comment block, a
section header, and a file-local `EXPECTED` array) from
`speccy-cli/tests/init.rs`, `speccy-cli/tests/skill_packs.rs`, and
`speccy-core/tests/personas.rs`. No production code, dependencies,
authentication, authorization, input validation, secret handling,
logging, or cryptographic primitives are affected. The collateral
SPEC.md whitespace fix is editorial structural prose and
introduces no security implications. None of the deleted tests
gated security-relevant invariants — they substring-matched prose
or re-asserted a hard-coded copy of a constant, both of which the
AGENTS.md "vacuous tests" rule explicitly calls out as
non-protective.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-27T21:21:30Z" round="1">
All three test-file deletions are surgical and correct. No orphan
imports, dead code, suppression annotations, or formatter
violations introduced. Trailing newlines present on all three
files (`0x0a`). The SPEC.md diff adds only structural blank lines
between `</scenario>` and `</requirement>` close tags with no
prose changes. Nothing in the diff invents parallel helpers or
fights the project's existing style.
</review>
