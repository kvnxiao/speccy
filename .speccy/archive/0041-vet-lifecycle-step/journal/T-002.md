---
spec: SPEC-0041
task: T-002
generated_at: 2026-05-22T00:00:00Z
---

<implementer date="2026-05-22T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">
## Completed
- `git mv` of all six holistic-gate paths to their `speccy-vet` /
  `VET.md` equivalents:
  - `resources/modules/skills/speccy-holistic-gate.md` →
    `resources/modules/skills/speccy-vet.md`.
  - `.claude/skills/speccy-holistic-gate/` →
    `.claude/skills/speccy-vet/`.
  - `.agents/skills/speccy-holistic-gate/` →
    `.agents/skills/speccy-vet/`.
  - `resources/agents/.claude/skills/speccy-holistic-gate/` →
    `resources/agents/.claude/skills/speccy-vet/`.
  - `resources/agents/.agents/skills/speccy-holistic-gate/` →
    `resources/agents/.agents/skills/speccy-vet/`.
  - `.speccy/specs/0038-skill-pack-references/journal/HOLISTIC.md` →
    `.speccy/specs/0038-skill-pack-references/journal/VET.md`.
- Global token rewrite (`speccy-holistic-gate` → `speccy-vet`,
  `HOLISTIC.md` → `VET.md`) across every non-historical file that
  carried either string. 25 files updated in one pass (PowerShell
  `-creplace` over the explicit file list, no glob recursion).
  Excluded: `.speccy/specs/0039-*/` and `.speccy/specs/0041-*/` per
  the SPEC's historical-record carve-out.
- Frontmatter `name:` fields on the moved skill bodies (claude /
  agents / .tmpls) already read `speccy-vet` after the token sweep;
  no separate edit needed.

## Undone
- Persona file renames (`holistic-reviewer` / `holistic-implementer`
  → `vet-*`) and the `vet-simplifier` introduction are T-003's
  scope. Those files still carry the `holistic-*` filenames; only
  their *content* references to `speccy-holistic-gate` / `VET.md`
  were retokenized here, per the T-002 prose audit scope.

## Hygiene checks
- `cargo test -p speccy-cli --test skill_packs`: 37 passed, 0 failed
  — the renamed `.claude/skills/speccy-vet/` / `.agents/skills/speccy-vet/`
  directory assertions resolve cleanly.
- `cargo test --workspace`: 154 speccy-core unit tests pass; 2 pre-
  existing failures in `speccy-core/tests/docs_sweep.rs` are
  unrelated to this task — the test joins `.speccy/ARCHITECTURE.md`
  but the file lives at `docs/ARCHITECTURE.md` (a regression from
  commit `af84caf "Move ARCHITECTURE.md to docs folder"`). Flagged
  under "Discovered issues" below.

## Evidence
- `rg -n 'speccy-holistic-gate' resources/modules/ resources/agents/ .claude/ .agents/ speccy-core/src/ speccy-cli/src/ speccy-cli/tests/ README.md AGENTS.md docs/ARCHITECTURE.md` → zero hits (CHK-007 first half).
- `rg -n 'HOLISTIC\.md' resources/modules/ resources/agents/ .claude/ .agents/ speccy-core/src/ speccy-cli/src/ speccy-cli/tests/ README.md AGENTS.md docs/ARCHITECTURE.md` → zero hits (CHK-007 second half).
- `ls .claude/skills/speccy-vet/SKILL.md .agents/skills/speccy-vet/SKILL.md .speccy/specs/0038-skill-pack-references/journal/VET.md` → all three present and non-empty (CHK-008).
- `ls .claude/skills/speccy-holistic-gate/ 2>&1` → exits non-zero
  (directory removed by `git mv`).

## Discovered issues
- `speccy-core/tests/docs_sweep.rs` lines 29 and 54 reference
  `root.join(".speccy").join("ARCHITECTURE.md")` but the canonical
  path is `docs/ARCHITECTURE.md` after commit `af84caf`. Two tests
  panic on file-open. Out of scope for T-002 (it does not touch
  `speccy-core/src/` or `docs_sweep.rs`); separate fix needed.

## Procedural compliance
- Followed the `speccy-work` skill's single-task primitive: flipped
  T-002 `pending` → `in-progress`, performed renames + content
  sweeps, flipped to `in-review`, wrote this journal block. No
  iteration into T-003.
- No skill file under `skills/` needed updating — the friction was
  a stale test path, not a stale skill instruction.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:55:50Z" round="1">
T-002 covers REQ-004 (the lifecycle-skill + journal-artifact rename).
The two binding scenarios in the task body pass cleanly:
- CHK-007 first half — `rg 'speccy-holistic-gate'` across
  `resources/`, `.claude/`, `.agents/`, `speccy-core/src/`,
  `speccy-cli/`, `README.md`, `AGENTS.md`, `docs/ARCHITECTURE.md`:
  zero hits.
- CHK-007 second half — `rg 'HOLISTIC\.md'` across the same scope:
  zero hits.
- CHK-008 — `ls .claude/skills/speccy-vet/SKILL.md`,
  `.agents/skills/speccy-vet/SKILL.md`,
  `.speccy/specs/0038-skill-pack-references/journal/VET.md` all
  resolve to non-empty files; the old
  `.claude/skills/speccy-holistic-gate/` directory is gone (exit 2).
All six `git mv` moves appear in `git status` as renames (history
preserved). YAML `name:` fields on the four renamed SKILL bodies
(`.claude/`, `.agents/`, and the two host-pack `.tmpl` files) read
`speccy-vet`. T-003 persona work was correctly excluded per the
in-SPEC carve-out. No business-scope drift beyond REQ-004's
explicit scenarios.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:55:50Z" round="1">
Test surface verified end-to-end:
- `cargo test -p speccy-cli --test skill_packs` exits 0
  (37 passed, 0 failed). `SKILL_NAMES` in
  `speccy-cli/tests/skill_packs.rs:160` now lists `"speccy-vet"`
  rather than the old `"speccy-holistic-gate"`, and the renamed
  `.claude/skills/speccy-vet/` and `.agents/skills/speccy-vet/`
  directories satisfy the wrapper-existence assertions.
- `speccy verify` on the workspace at HEAD exits clean
  (0 errors across 41 specs / 237 requirements / 364 scenarios).
- `speccy check SPEC-0041/T-002` renders both CHK-007 and CHK-008
  scenarios — the binding contract for this task — and both are
  satisfied as verified above.
The implementer's noted pre-existing `docs_sweep.rs` panic on
`.speccy/ARCHITECTURE.md` vs the canonical `docs/ARCHITECTURE.md`
is genuinely outside T-002's scope (T-002 does not touch
`speccy-core/src/` or `speccy-core/tests/`) and was flagged
correctly under Discovered issues for follow-up.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:55:50Z" round="1">
Pure file rename plus text-token replacement. No new code paths,
no external invocations, no secret material handled. The renamed
files retain their existing skill bodies verbatim modulo the two
token rewrites (`speccy-holistic-gate` → `speccy-vet`,
`HOLISTIC.md` → `VET.md`); no new attack surface introduced. The
`.tmpl` files continue to use the `{% include "modules/skills/..." %}`
directive — no new dynamic-evaluation hook. Nothing under
`speccy-core/src/` or `speccy-cli/src/` changed.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:55:50Z" round="1">
The task observed its scope discipline strictly: persona renames
(T-003) and `<gate>` write mechanics (T-004) are visibly untouched
in the working tree. All renames used `git mv` so blame chains
survive. Frontmatter `name:` fields land in the right place
(under the `---` fence, single-line). The implementer's journal
follows the AGENTS.md "Procedural compliance" convention — flagged
the orphan `docs_sweep.rs` issue rather than silently widening
the diff to fix it. No `#[allow(...)]` or `unwrap()` introduced
(non-code task). Hygiene-gate prose in the task body is reflected
in the implementer's evidence record. The closing-suggestion
chain to T-003 is implicit in the `Undone` section, which is
the right shape for a rename-only single-task primitive.
</review>
