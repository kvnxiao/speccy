---
spec: SPEC-0041
task: T-003
generated_at: 2026-05-22T00:00:00Z
---
<implementer date="2026-05-22T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

**Completed**
- Part A renames via `git mv` for all 10 persona/agent files:
  `resources/modules/personas/{holistic-reviewer,holistic-implementer}.md` →
  `vet-reviewer.md` / `vet-implementer.md`; matching ejected copies under
  `.claude/agents/` and `.codex/agents/`; matching templates under
  `resources/agents/.claude/agents/` and `resources/agents/.codex/agents/`.
- Updated `name:` frontmatter (YAML for Claude, TOML key for Codex) in
  every renamed ejected copy and `.tmpl`.
- Updated `{% include %}` paths in the four `.tmpl` files to point at
  the renamed module bodies.
- Updated the one remaining in-prose reference inside
  `resources/modules/personas/vet-reviewer.md` (the line about "the
  holistic-implementer leaves its changes uncommitted") to
  `vet-implementer`. Propagated to ejected `.claude/agents/vet-reviewer.md`
  and `.codex/agents/vet-reviewer.toml`.
- Updated dispatch sites in `resources/modules/skills/speccy-vet.md` (Phase 1
  reviewer + implementer, Phase 2 simplifier × scan and apply) and
  propagated to both ejected SKILL.md copies under `.claude/skills/speccy-vet/`
  and `.agents/skills/speccy-vet/`.
- Part B: created
  `resources/modules/personas/vet-simplifier.md` adapted from the
  five-point code-simplifier template with the task-required
  "Phase 2 scope boundary" point and Project Standards generalization
  (AGENTS.md + rule-file refs instead of TypeScript/React).
- Ejected `.claude/agents/vet-simplifier.md` and
  `.codex/agents/vet-simplifier.toml`; created matching templates
  `resources/agents/.claude/agents/vet-simplifier.md.tmpl` and
  `resources/agents/.codex/agents/vet-simplifier.toml.tmpl`.
- Regenerated codex ejection via `speccy init --force --host codex` to
  match renderer output (caught a `cmd_prefix` / trailing-newline drift
  via the dogfood test).

**Undone**
- Nothing in scope for T-003.

**Hygiene checks**
- `cargo test -p speccy-cli`: 0 failures.
- `cargo test -p speccy-core`: 154 passed in lib; the two
  `docs_sweep` integration tests (`architecture_md_*`) fail by
  reading `.speccy/ARCHITECTURE.md` instead of `docs/ARCHITECTURE.md`.
  Verified pre-existing (`git stash` reproduces the same failure on
  base commit) — this is fallout from commit `af84caf` "Move
  ARCHITECTURE.md to docs folder" that T-002 did not catch. Out of
  scope for T-003.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: green.

**Evidence**
- CHK-009: `rg -n 'holistic-reviewer|holistic-implementer' resources/ .claude/ .agents/ speccy-core/src/ speccy-cli/src/ speccy-cli/tests/` → 0 matches.
- CHK-010: all nine paths exist with non-empty bodies (verified via
  `ls`).
- CHK-011: `rg -n 'code-simplifier' resources/modules/skills/speccy-vet.md .claude/skills/speccy-vet/SKILL.md .agents/skills/speccy-vet/SKILL.md` → 0 matches.
- `vet-simplifier.md` body contains the "## 5. Phase 2 scope boundary"
  section naming "the cumulative SPEC-NNNN diff against the merge base".

**Discovered issues**
- `speccy-core/tests/docs_sweep.rs` lines 29 and 54 read
  `root.join(".speccy").join("ARCHITECTURE.md")` but
  `ARCHITECTURE.md` lives at `docs/ARCHITECTURE.md` (per commit
  `af84caf`). Both tests fail before any of my edits. Pre-existing
  drift; leaving for a separate fix (not blocking T-003's CHKs).

**Procedural compliance**
- Followed the skill: flipped `state="in-progress"`, did the work,
  ran verification commands, flipped to `state="in-review"`, wrote
  this journal block.
- No edits to skill files other than the dispatch sites mandated by
  the task body.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T03:11:45Z" round="1">
REQ-005 done-when bullets satisfied. Part A: all ten persona/agent files moved via `git mv` (status shows rename pairs preserved); `name:` frontmatter on the four ejected `.claude/agents/vet-*.md` reads `vet-reviewer`/`vet-implementer`; the four `.codex/agents/vet-*.toml` files read the same; the four `.tmpl` wrappers under `resources/agents/` likewise. Part B: `resources/modules/personas/vet-simplifier.md` carries a five-point body — Preserve Functionality / Apply Project Standards / Enhance Clarity / Maintain Balance / Phase 2 scope boundary — with the three required speccy adaptations: (a) "Apply Project Standards" points at `AGENTS.md` + `.claude/rules/`-equivalent rule files, removing TypeScript/React inline naming; (b) point 5 "Phase 2 scope boundary" names "the cumulative SPEC-NNNN working-tree diff against the merge base"; (c) "Focus Scope" tightened away from "recently modified code in the current session". Ejected copies at `.claude/agents/vet-simplifier.md` and `.codex/agents/vet-simplifier.toml` exist; templates at `resources/agents/.claude/agents/vet-simplifier.md.tmpl` and `resources/agents/.codex/agents/vet-simplifier.toml.tmpl` use the same `{% include %}` shape as the other vet-* templates. Phase 2 dispatch sites in `resources/modules/skills/speccy-vet.md` (lines 362, 418) and both ejected SKILL.md copies dispatch `vet-simplifier` on both Claude Code and Codex; zero `code-simplifier` mentions survive. Cross-host parity achieved per DEC-005. No scope creep into T-004 (gate-block write) or T-005 (suggestion-line routing).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T03:11:45Z" round="1">
T-003 is a persona-rename + persona-creation task with no production Rust changes; test surface verified via the binding bash scenarios in the task body and the shipped skill-pack assertion suite. CHK-009: `rg -n 'holistic-reviewer|holistic-implementer' resources/ .claude/ .agents/ speccy-core/src/ speccy-cli/src/ speccy-cli/tests/` → 0 matches across the search scope. CHK-010: all nine paths (`resources/modules/personas/vet-{reviewer,implementer,simplifier}.md`, `.claude/agents/vet-{reviewer,implementer,simplifier}.md`, `.codex/agents/vet-{reviewer,implementer,simplifier}.toml`) exist and have non-empty bodies. CHK-011: `rg -n 'code-simplifier' resources/modules/skills/speccy-vet.md .claude/skills/speccy-vet/SKILL.md .agents/skills/speccy-vet/SKILL.md` → 0 matches. `cargo test -p speccy-cli --test skill_packs` exits 0 (37 passed) — the shipped skill-pack render-shape assertions accept the renamed persona files. `cargo test --workspace` regresses only the two pre-existing `docs_sweep.rs` failures introduced by commit `af84caf` ("Move ARCHITECTURE.md to docs folder"); they panic on `.speccy/ARCHITECTURE.md` vs `docs/ARCHITECTURE.md` and reproduce on a clean stash, confirmed unrelated to T-003 (T-003 touches no `speccy-core/tests/` content; the one `docs_sweep.rs` line edit in the working tree is a `cargo fmt` line-wrap only — see `git diff HEAD -- speccy-core/tests/docs_sweep.rs`). `speccy verify` exits 0 across 41 specs / 237 reqs / 364 scenarios. Implementer's `## Evidence` section enumerates touched files; the de-facto evidence convention in this codebase (T-001/T-002 journals, T-001 reviewer-tests verdict) accepts an inline evidence section over a separate `evidence/T-NNN.md` file.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T03:11:45Z" round="1">
No security surface introduced. T-003 is pure file renames (`git mv`) plus one new persona prose body (`vet-simplifier.md` + ejected copies + templates). No new code paths in `speccy-core/src/` or `speccy-cli/src/`. No new transitive dependency in `Cargo.toml` (the new `sha2` workspace dev-dep was T-001's). The renamed persona bodies and the new simplifier body are sub-agent prompt content — they instruct dispatched LLMs but are not themselves executed by the speccy CLI. Dispatch-target strings (`vet-reviewer`, `vet-implementer`, `vet-simplifier`) are identifiers consumed by the host harness; no injection vector. No secrets, no auth boundary, no logging changes, no I/O of untrusted data.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T03:11:45Z" round="1">
All ten Part-A renames used `git mv` (rename pairs visible in `git status`, blame chains preserved). Frontmatter shape on the new and renamed ejected files matches existing convention: `.claude/agents/vet-*.md` carries a 4-line YAML block (`name`, `description`, `model`, `effort`); `.codex/agents/vet-*.toml` carries 4 keys (`name`, `description`, `model`, `model_reasoning_effort`) followed by a `developer_instructions = """ {% include %} """` block; templates under `resources/agents/.claude/agents/` and `resources/agents/.codex/agents/` use the same `{% include "modules/personas/<name>.md" %}` shape as the existing `vet-reviewer`/`vet-implementer` templates. The source `resources/modules/personas/vet-simplifier.md` correctly carries no YAML frontmatter — matching the existing `vet-reviewer.md` / `vet-implementer.md` convention where the frontmatter lives in the `.tmpl` wrapper, not in the persona module body (the task's "set name: vet-simplifier in YAML frontmatter" phrasing resolves to the ejected and template files, where the frontmatter actually appears). Heading structure (`# Vet Simplifier` h1, `## Role`, then five numbered `## N. Title` subheaders) consistent with the other persona bodies' structural shape. No `unwrap()`/`expect()`/`#[allow(...)]` introduced (non-code task). Trailing newlines verified via `tail -c 1 | od -An -tx1` → `0a` on all three new persona files. `cargo +nightly fmt --all --check` clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
</review>
