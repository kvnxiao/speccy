# Evidence for SPEC-0059 T-006

Red-then-green paper trail for T-006: the new commit step (step 7) added to
`resources/modules/skills/speccy-amend.md` (REQ-005) that, after the step-6
`TSK-003`-clear check, runs the branch-guard prelude via
`{% include "modules/references/branch-guard.md" %}` and then commits the
amend's reconcile delta via
`{% include "modules/references/commit-recipe.md" %}` with narrow
`git add <paths>` staging (SPEC.md always, the reconciled TASKS.md when one
exists, and any journal blocker files appended this run) and the title
`[SPEC-NNNN]: amend — <why>`, where `<why>` is sourced from the newest
`## Changelog` row. The covered CHKs (CHK-006, CHK-007) are content-property
assertions over the embedded `RESOURCES` bundle and the rendered Claude Code
pack, proven by the four new `amend_*` / `rendered_amend_*` tests in
`speccy-cli/tests/authoring_commit.rs`.

The red phase is captured against the pre-edit tree: at HEAD,
`speccy-amend.md` had no commit step at all — step 6 (`speccy status`
re-check) went straight to the `### Loop exit criteria` / next-step suggestion,
with no `{% include %}` directives, no `[SPEC-NNNN]: amend — <why>` title, and
no `git add`. The new tests were authored first and run against that pre-edit
source (the live source the `include_dir`-embedded `RESOURCES` bundle compiles
against), then re-run green after the edit + `just reeject`.

## Session 2026-06-11-T006

### Pre-edit source inspection (HEAD skill, before the commit step)

```
$ git show HEAD:resources/modules/skills/speccy-amend.md \
    | grep -cE 'commit-recipe.md|branch-guard.md'
0
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -c 'amend — <why>'
0
$ git show HEAD:resources/modules/skills/speccy-amend.md | grep -c 'git add'
0
```

The pre-edit skill carries the step-6 `TSK-003`-clear check and jumps straight
to `### Loop exit criteria` with no commit step, no includes, no
`amend — <why>` title, and no `git add`. All four new T-006 assertions fail
against this source.

### Scenario 1 — Shared recipe + branch-guard pulled via `{% include %}` (CHK-006 recipe property, REQ-005)

<red>
Pre-edit: `speccy-amend.md` at HEAD has no commit step and neither include, so
the include-present assertion fails.

```
$ cargo test --test authoring_commit amend_includes_shared_commit_recipe_and_branch_guard
---- amend_includes_shared_commit_recipe_and_branch_guard ----
speccy-amend.md must pull the shared commit recipe via
`{% include "modules/references/commit-recipe.md" %}` (CHK-006); the amend
reconcile-delta commit step must delegate to the shared recipe
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the new step 7 pulls both the branch-guard prelude and the shared
commit recipe via `{% include %}`.

```
$ cargo test --test authoring_commit amend_includes_shared_commit_recipe_and_branch_guard
test amend_includes_shared_commit_recipe_and_branch_guard ... ok
```
</green>

### Scenario 2 — `[SPEC-NNNN]: amend — <why>` title, `<why>` sourced from newest Changelog row, after the `TSK-003`-clear check (CHK-006)

<red>
Pre-edit: the skill has neither the `[SPEC-NNNN]: amend — <why>` title nor a
post-`TSK-003`-clear commit recipe, so the title assertion fails first.

```
$ cargo test --test authoring_commit amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear
---- amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear ----
speccy-amend.md must title the commit `[SPEC-NNNN]: amend — <why>` (CHK-006)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the commit titles `[SPEC-NNNN]: amend — <why>`, names the newest
`## Changelog` row as the `<why>` source, and the `confirm `TSK-003` cleared`
text (step 6) precedes the recipe include in document order.

```
$ cargo test --test authoring_commit amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear
test amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear ... ok
```
</green>

### Scenario 3 — Staging set covers SPEC.md + conditional TASKS.md + journal blockers, narrow staging, absent-TASKS.md tolerated (CHK-006 staging property, CHK-007)

<red>
Pre-edit: the skill has no `git add` and no staging-path prose at all, so the
`<spec-dir>/SPEC.md` staging assertion fails.

```
$ cargo test --test authoring_commit amend_stages_spec_md_and_tolerates_absent_tasks_md
---- amend_stages_spec_md_and_tolerates_absent_tasks_md ----
speccy-amend.md must stage the spec's `SPEC.md` in the amend delta (CHK-006)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the staging-breadth bullet names `git add <paths>` covering
`<spec-dir>/SPEC.md`, the reconciled `<spec-dir>/TASKS.md` *when one exists*,
and each `<spec-dir>/journal/T-NNN.md` blocker file, and states the absent
`TASKS.md` is dropped from the list "without failing on the absent tasks file"
(CHK-007).

Note on the staging negative-assertion: this scenario does **not** bare-substring
the forbidden `git add -A` / `git add .` forms, because the step's prose
legitimately *forbids* those forms by name (so a substring check would
false-match the prohibition prose — the same trap the sibling `plan` and
`decompose` staging tests already note). It anchors instead on the positive
`git add <paths>` narrow-staging form plus the explicit `<spec-dir>/SPEC.md`
and `<spec-dir>/journal/T-NNN.md` path commands, which the whole-tree forms
would replace.

```
$ cargo test --test authoring_commit amend_stages_spec_md_and_tolerates_absent_tasks_md
test amend_stages_spec_md_and_tolerates_absent_tasks_md ... ok
```
</green>

### Scenario 4 — Rendered amend skill fully expands both includes (CHK-006 render side)

<red>
Pre-edit: the rendered `.claude/skills/speccy-amend/SKILL.md` carries no commit
recipe, so the assertion that the rendered body contains the recipe's
`git diff --cached --quiet` text fails. (The pre-edit rendered body is also
MiniJinja-clean, since the pre-edit source had no includes — so the meaningful
red is the missing recipe text, proving the include had not yet been added and
expanded.)

```
$ cargo test --test authoring_commit rendered_amend_skill_fully_expands_includes
---- rendered_amend_skill_fully_expands_includes ----
rendered `.claude/skills/speccy-amend/SKILL.md` must carry the recipe's
`git diff --cached --quiet` idempotency check, proving the commit-recipe
include expanded into the amend skill body
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit + `just reeject`: the rendered `.claude/skills/speccy-amend/SKILL.md`
expands branch-guard, commit-recipe, and the nested identity-sourcing include,
with zero residual `{{` / `{%` / `{#` markup, and carries the recipe's
`git diff --cached --quiet` text.

```
$ just reeject
$ cargo test --test authoring_commit rendered_amend_skill_fully_expands_includes
test rendered_amend_skill_fully_expands_includes ... ok
```
</green>

### Full file — all twenty-four tests green (T-001 + T-002 + T-003 + T-004 + T-005 + T-006)

```
$ cargo test --test authoring_commit
test result: ok. 24 passed; 0 failed; 0 ignored
```

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all passed)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(no warnings)
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-006 diff landed.

## CHK roll call

- **CHK-006** (the `speccy-amend` commit step invokes the shared commit recipe
  with the title `[SPEC-NNNN]: amend — <why>`, a staging set covering SPEC.md
  plus TASKS.md-when-present plus appended journal files, runs after the
  `TSK-003`-clear check, and sources `<why>` from the newest `## Changelog`
  row): demonstrated → Scenarios 1–3,
  `amend_includes_shared_commit_recipe_and_branch_guard`,
  `amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear`,
  `amend_stages_spec_md_and_tolerates_absent_tasks_md`, plus Scenario 4's
  `rendered_amend_skill_fully_expands_includes` proving the recipe (which
  carries the single-parent HEREDOC commit and the `git diff --cached --quiet`
  idempotency check) expands into the rendered amend skill body.
- **CHK-007** (a spec with no `TASKS.md` is tolerated: the staging set commits
  SPEC.md and any journal files rather than requiring the tasks file to exist):
  demonstrated → Scenario 3's `amend_stages_spec_md_and_tolerates_absent_tasks_md`,
  asserting the `TASKS.md`-when-present qualification and the
  "without failing on the absent tasks file" prose.
