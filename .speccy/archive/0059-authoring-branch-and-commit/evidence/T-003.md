# Evidence for SPEC-0059 T-003

Red-then-green paper trail for T-003: the behaviour-preserving refactor of the
"Atomic commit on review pass" section in
`resources/modules/skills/partials/review-fanout.md` onto the shared
`{% include "modules/references/commit-recipe.md" %}`. The covered CHKs
(CHK-009 review side, CHK-010) are content-property assertions over the
embedded `RESOURCES` bundle and the rendered Claude Code pack, proven by the
five new `review_fanout_*` / `rendered_review_skill_*` tests in
`speccy-cli/tests/authoring_commit.rs`.

The red phase is captured against the pre-refactor tree (HEAD before this
SPEC's review-fanout edit), where the section still carried the hand-rolled
inline copy: a `git status --porcelain` pre-check (HEAD line 154), an inline
commit-message-format block, and an inline `Co-Authored-By` trailer, and no
`commit-recipe.md` include.

## Session 2026-06-11-T003-attempt-1

### Scenario 1 — Shared-recipe include replaces the inline copy (CHK-009 review side)

<red>
Pre-edit: `review-fanout.md` at HEAD carries the hand-rolled inline commit
copy and no recipe include, so both the include-present and the inline-drop
assertions fail.

```
$ git show HEAD:resources/modules/skills/partials/review-fanout.md \
    | grep -c 'commit-recipe.md'
0
$ git show HEAD:resources/modules/skills/partials/review-fanout.md \
    | grep -n 'git status --porcelain'
154:1. Run `git status --porcelain`. If stdout is empty, **skip the

$ cargo test --test authoring_commit review_fanout_includes_shared_commit_recipe
---- review_fanout_includes_shared_commit_recipe ----
review-fanout.md must pull the shared commit recipe via
`{% include "modules/references/commit-recipe.md" %}` (CHK-009 review side)
test result: FAILED

$ cargo test --test authoring_commit review_fanout_drops_inline_status_porcelain_precheck
---- review_fanout_drops_inline_status_porcelain_precheck ----
review-fanout.md must no longer contain the inline `git status --porcelain`
pre-check (CHK-009 review side, DEC-004)
test result: FAILED
```
</red>

<green>
Post-edit: the section pulls the shared recipe via `{% include %}`; the inline
`git status --porcelain` pre-check and the inline `git diff --cached --quiet`
restatement are gone (the recipe owns the unified idempotency check now).

```
$ cargo test --test authoring_commit review_fanout_includes_shared_commit_recipe
test review_fanout_includes_shared_commit_recipe ... ok

$ cargo test --test authoring_commit review_fanout_drops_inline_status_porcelain_precheck
test review_fanout_drops_inline_status_porcelain_precheck ... ok
```
</green>

### Scenario 2 — `git add -A` staging and `[SPEC-NNNN/T-NNN]:` title prefix retained (CHK-010)

<red>
Pre-edit baseline: `git add -A`, the `[SPEC-NNNN/T-NNN]:` title prefix, and the
single-parent commit shape are all present in the HEAD inline copy, so the
retention assertions already pass against the base tree — this is the
behaviour-preserving invariant. The meaningful red for this scenario is that
the refactor must *not* drop them while replacing the inline copy with the
include; a refactor that lost `git add -A` or the title prefix (e.g. by
parameterising the recipe to `git add <paths>` or a different title) would
flip this test red.

```
$ git show HEAD:resources/modules/skills/partials/review-fanout.md \
    | grep -nE 'git add -A|\[SPEC-NNNN/T-NNN\]:'
159:2. If stdout is non-empty, run `git add -A` followed by `git commit`
166:- **Title:** `[SPEC-NNNN/T-NNN]: <task title>` — `<task title>` is
190:[SPEC-NNNN/T-NNN]: <task title>
```
</red>

<green>
Post-edit: the recipe is parameterised to `git add -A` staging and the
`[SPEC-NNNN/T-NNN]: <task title>` title with the `Completed`-field body, so
both are retained verbatim through the refactor.

```
$ cargo test --test authoring_commit review_fanout_retains_add_dash_a_and_title_prefix
test review_fanout_retains_add_dash_a_and_title_prefix ... ok
```
</green>

### Scenario 3 — Unguarded branch statement retained, no branch-guard include (REQ-008 review side)

<red>
Pre-edit baseline: the unguarded "Commits land on whatever HEAD is" statement
is present (HEAD line 208) and no branch-guard include exists, so the
assertions pass against the base tree. The meaningful red is that the refactor
must not (a) drop the unguarded statement nor (b) add a
`{% include "modules/references/branch-guard.md" %}`; either change would flip
this test red. The branch-guard module did not exist at the pre-SPEC base, so
adding the include here would also have failed to render.

```
$ git show HEAD:resources/modules/skills/partials/review-fanout.md \
    | grep -n 'Commits land on whatever HEAD is'
208:Commits land on whatever HEAD is.
$ git show HEAD:resources/modules/skills/partials/review-fanout.md \
    | grep -c 'branch-guard.md'
0
```
</red>

<green>
Post-edit: the unguarded statement is retained and no branch-guard include was
added — the review-pass commit stays unguarded.

```
$ cargo test --test authoring_commit review_fanout_retains_unguarded_branch_statement_no_branch_guard
test review_fanout_retains_unguarded_branch_statement_no_branch_guard ... ok
```
</green>

### Scenario 4 — Rendered review skill fully expands the included recipe (CHK-009 / CHK-010 render side)

<red>
First green attempt panicked: T-001's `commit-recipe.md` carried a live
self-referential `{% include "modules/references/commit-recipe.md" %}` in its
line-5 documentation prose. Harmless while no callsite included the recipe, but
T-003's first live include made MiniJinja recurse to its limit.

```
$ cargo test --test authoring_commit rendered_review_skill_fully_expands_commit_recipe
---- rendered_review_skill_fully_expands_commit_recipe ----
render_host_pack(claude-code) must succeed: ... recursion limit exceeded
(BadInclude: modules/references/commit-recipe.md)
test result: FAILED
```
</red>

<green>
Post-fix: line 5 of `commit-recipe.md` was reworded to describe the include in
prose without a live directive. The only remaining live directive in the
recipe targets `identity-sourcing.md`. The rendered
`.claude/skills/speccy-review/SKILL.md` carries the recipe's
`git diff --cached --quiet` text with zero residual `{{` / `{%` / `{#` markup.

```
$ cargo test --test authoring_commit rendered_review_skill_fully_expands_commit_recipe
test rendered_review_skill_fully_expands_commit_recipe ... ok
```
</green>

### Full file — all eleven tests green (T-001 + T-002 + T-003)

```
$ cargo test --test authoring_commit
test result: ok. 11 passed; 0 failed; 0 ignored
```

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all passed)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(no warnings)
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-003 diff landed.

## CHK roll call

- **CHK-009** (review side: `review-fanout.md` includes the shared recipe via
  `{% include %}` and no longer restates the inline `git status --porcelain` /
  `git diff --cached --quiet` recipe): demonstrated → Scenario 1,
  `review_fanout_includes_shared_commit_recipe` +
  `review_fanout_drops_inline_status_porcelain_precheck`. The decompose side of
  CHK-009 is scoped to sibling task T-004, not this task.
- **CHK-010** (title format `[SPEC-NNNN/T-NNN]:`, `git add -A` staging, and the
  single-parent commit shape retained verbatim): demonstrated → Scenario 2,
  `review_fanout_retains_add_dash_a_and_title_prefix`, plus Scenario 4's
  `rendered_review_skill_fully_expands_commit_recipe` proving the recipe (which
  carries the single-parent HEREDOC commit) expands into the review skill body.
- **REQ-008 review side** (unguarded statement retained, no branch-guard
  include): demonstrated → Scenario 3,
  `review_fanout_retains_unguarded_branch_statement_no_branch_guard`.
