# Evidence for SPEC-0059 T-001

Red-then-green paper trail for T-001: creation of the shared commit-recipe
reference module at `resources/modules/references/commit-recipe.md`. The
covered CHKs (CHK-008, CHK-013) are content-property assertions over the
embedded `RESOURCES` bundle, proven by `speccy-cli/tests/authoring_commit.rs`.

## Session 2026-06-11-T001-attempt-1

### Scenario 1 — Recipe stated once + trailer delegated via include (CHK-008)

<red>
Pre-edit: the module file does not exist. The three tests in
`authoring_commit.rs` fail at the `commit_recipe_body()` fixture lookup
because `RESOURCES.get_file("modules/references/commit-recipe.md")`
returns `None`.

```
$ cargo test --test authoring_commit   # against tree without commit-recipe.md
commit-recipe.md ... RESOURCES bundle must contain
`modules/references/commit-recipe.md`; SPEC-0059 T-001 requires this
shared module to be created
test result: FAILED. 0 passed; 3 failed
```
</red>

<green>
Post-edit: the module exists. `commit_recipe_states_idempotency_check_exactly_once`
asserts exactly one `git diff --cached --quiet` occurrence; 
`commit_recipe_delegates_trailer_via_include` asserts the
`{% include "modules/references/identity-sourcing.md" %}` directive is present
and the inline `## Sourcing your recorded identity` heading is absent.

```
$ cargo test --test authoring_commit
test commit_recipe_delegates_trailer_via_include ... ok
test commit_recipe_states_idempotency_check_exactly_once ... ok
test result: ok. 3 passed; 0 failed
```
</green>

### Scenario 2 — No-git-repository short-circuit (CHK-013, commit-recipe side)

<red>
Pre-edit: module absent, so `commit_recipe_specifies_no_git_short_circuit`
fails at the same fixture lookup as Scenario 1 (no `git rev-parse
--is-inside-work-tree` probe text exists to find).
</red>

<green>
Post-edit: the test confirms the module probes via
`git rev-parse --is-inside-work-tree` and states (case-insensitively) both
"not a git repository" and "without erroring", so the no-git short-circuit
skips the commit step without surfacing an error.

```
$ cargo test --test authoring_commit commit_recipe_specifies_no_git_short_circuit
test commit_recipe_specifies_no_git_short_circuit ... ok
test result: ok. 1 passed; 0 failed
```
</green>

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok.  (all suites green; e.g. 227 passed in the largest suite)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(no warnings)
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
advisories ok, bans ok, licenses ok, sources ok
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-001 diff landed.
