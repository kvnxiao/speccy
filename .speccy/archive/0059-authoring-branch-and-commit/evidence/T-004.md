# Evidence for SPEC-0059 T-004

Red-then-green paper trail for T-004: the rework of step 4 of
`resources/modules/phases/speccy-decompose.md` (REQ-004, REQ-007 decompose
side) onto the shared `{% include "modules/references/commit-recipe.md" %}`
behind a `{% include "modules/references/branch-guard.md" %}` prelude. The
covered CHK (CHK-005), plus the CHK-009 decompose side that this task's
scenarios name, are content-property assertions over the embedded `RESOURCES`
bundle and the rendered Claude Code pack, proven by the five new `decompose_*`
/ `rendered_decompose_*` tests in `speccy-cli/tests/authoring_commit.rs`.

The red phase is captured against the pre-edit tree (HEAD before this task's
decompose edit), where step 4 still carried the hand-rolled inline bootstrap
commit: the combined `[SPEC-NNNN]: create spec and decompose tasks` title, an
inline `git diff --cached --quiet` idempotency check, `SPEC.md` staged
alongside `TASKS.md`, and no `{% include %}` directives. The red was produced
by stashing the working-tree WIP edit of `speccy-decompose.md` so the
`include_dir`-embedded bundle compiled against HEAD's pre-edit phase source,
running each scoped test, then `git stash pop` to restore the WIP.

## Session 2026-06-11-T004-attempt-2

### Pre-edit source inspection (HEAD phase, before the rework)

```
$ git show HEAD:resources/modules/phases/speccy-decompose.md | grep -c 'commit-recipe.md'
0
$ git show HEAD:resources/modules/phases/speccy-decompose.md | grep -c 'branch-guard.md'
0
$ git show HEAD:resources/modules/phases/speccy-decompose.md | grep -n 'create spec and decompose tasks'
102:      - **Title:** `[SPEC-NNNN]: create spec and decompose tasks`
122:[SPEC-NNNN]: create spec and decompose tasks
$ git show HEAD:resources/modules/phases/speccy-decompose.md | grep -n 'git diff --cached --quiet'
95:   2. Run `git diff --cached --quiet`. If exit code is 0 (nothing
$ git show HEAD:resources/modules/phases/speccy-decompose.md | grep -n 'git add'
80:   narrow file-list staging (never `git add -A` or `git add .`), so
85:   1. Stage exactly the two SPEC artefacts via narrow `git add`:
88:      git add <spec-dir>/SPEC.md <spec-dir>/TASKS.md
91:      Do not use `git add -A` or `git add .`. Staging unchanged
```

The pre-edit phase stages `SPEC.md` alongside `TASKS.md` (line 88), carries the
combined title (lines 102, 122), restates `git diff --cached --quiet` inline
(line 95), and pulls in neither shared include.

### Scenario 1 — Shared recipe + branch-guard pulled via `{% include %}` (CHK-005 recipe property, REQ-007 decompose side)

<red>
Pre-edit: `speccy-decompose.md` at HEAD carries the hand-rolled inline
bootstrap commit and no includes, so the include-present assertion fails. (Red
captured with the WIP edit stashed so the embedded bundle compiled against
HEAD's phase source.)

```
$ git stash push -- resources/modules/phases/speccy-decompose.md
$ cargo test --test authoring_commit decompose_includes_shared_commit_recipe_and_branch_guard
---- decompose_includes_shared_commit_recipe_and_branch_guard ----
speccy-decompose.md must pull the shared commit recipe via
`{% include "modules/references/commit-recipe.md" %}` (CHK-005); the
hand-rolled inline bootstrap commit must be removed
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: step 4 pulls both the shared commit recipe and the branch-guard
prelude via `{% include %}`; the hand-rolled inline bootstrap commit is gone.

```
$ git stash pop
$ cargo test --test authoring_commit decompose_includes_shared_commit_recipe_and_branch_guard
test decompose_includes_shared_commit_recipe_and_branch_guard ... ok
```
</green>

### Scenario 2 — `[SPEC-NNNN]: decompose tasks` title, narrow TASKS.md-only staging, after `speccy lock` (CHK-005)

<red>
Pre-edit: the phase has neither the `[SPEC-NNNN]: decompose tasks` title nor a
narrow `git add <spec-dir>/TASKS.md` staging command, so the title assertion
fails first.

```
$ cargo test --test authoring_commit decompose_stages_tasks_md_alone_with_decompose_title
---- decompose_stages_tasks_md_alone_with_decompose_title ----
speccy-decompose.md must title the commit `[SPEC-NNNN]: decompose tasks` (CHK-005)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the commit titles `[SPEC-NNNN]: decompose tasks`, stages
`git add <spec-dir>/TASKS.md` narrowly (no `git add -A`/`git add .`, SPEC.md
dropped from the staging set per DEC-005), and the `speccy lock SPEC-NNNN` text
still precedes the recipe include in document order.

```
$ cargo test --test authoring_commit decompose_stages_tasks_md_alone_with_decompose_title
test decompose_stages_tasks_md_alone_with_decompose_title ... ok
```
</green>

### Scenario 3 — Combined bootstrap title absent (CHK-005 absent-string property, DEC-005)

<red>
Pre-edit: the combined `create spec and decompose tasks` title is present
(HEAD lines 102, 122), so the absent-string assertion fails.

```
$ cargo test --test authoring_commit decompose_drops_combined_bootstrap_title
---- decompose_drops_combined_bootstrap_title ----
speccy-decompose.md must no longer carry the combined
`create spec and decompose tasks` title — it is split into a per-skill
`[SPEC-NNNN]: decompose tasks` commit (CHK-005, DEC-005)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the combined title string is deleted; `SPEC.md` is now committed by
speccy-plan (T-005), not here.

```
$ cargo test --test authoring_commit decompose_drops_combined_bootstrap_title
test decompose_drops_combined_bootstrap_title ... ok
```
</green>

### Scenario 4 — Inline `git diff --cached --quiet` recipe dropped, delegated to the include (CHK-009 decompose side)

<red>
Pre-edit: the phase restates `git diff --cached --quiet` inline (HEAD line 95),
so the inline-drop assertion fails.

```
$ cargo test --test authoring_commit decompose_drops_inline_diff_cached_recipe
---- decompose_drops_inline_diff_cached_recipe ----
speccy-decompose.md must not restate the recipe's `git diff --cached --quiet`
idempotency check inline; it is delegated to the included recipe (CHK-009)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the inline idempotency restatement is gone; the recipe owns the
unified `git diff --cached --quiet` skip-if-empty check, reached only through
the include.

```
$ cargo test --test authoring_commit decompose_drops_inline_diff_cached_recipe
test decompose_drops_inline_diff_cached_recipe ... ok
```
</green>

### Scenario 5 — Rendered decompose agent fully expands both includes (CHK-005 / CHK-009 render side)

<red>
This scenario's meaningful red is the recursion panic that the sibling-T-003
evidence already documented: T-002's `branch-guard.md` carried a live
self-referential `{% include "modules/references/branch-guard.md" %}` in its
line-6 documentation prose. Harmless while no callsite included branch-guard,
but T-004's step-4 prelude is the module's first live include, which made
MiniJinja recurse to its limit at render time.

```
$ cargo test --test authoring_commit rendered_decompose_agent_fully_expands_includes
---- rendered_decompose_agent_fully_expands_includes ----
render_host_pack(claude-code) must succeed: ... recursion limit exceeded
(BadInclude: modules/references/branch-guard.md)
test result: FAILED
```

(Against the bare pre-edit HEAD phase — no includes at all — the test passes
vacuously: HEAD's phase has zero MiniJinja markup and the rendered review skill
already carries the recipe's `git diff --cached --quiet` text. The interesting
red is the recursion above, which appeared the moment the branch-guard include
went live.)
</red>

<green>
Post-fix: line 6 of `branch-guard.md` was reworded to describe its own include
in prose without a live directive (matching the prose form `commit-recipe.md`
uses). The rendered `.claude/agents/speccy-decompose.md` expands branch-guard,
commit-recipe, and the nested identity-sourcing include exactly once each, with
zero residual `{{` / `{%` / `{#` markup, and carries the recipe's
`git diff --cached --quiet` text.

```
$ cargo test --test authoring_commit rendered_decompose_agent_fully_expands_includes
test rendered_decompose_agent_fully_expands_includes ... ok
```
</green>

### Full file — all sixteen tests green (T-001 + T-002 + T-003 + T-004)

```
$ cargo test --test authoring_commit
test result: ok. 16 passed; 0 failed; 0 ignored
```

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all passed)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(no warnings)
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-004 diff landed.

## CHK roll call

- **CHK-005** (decompose commit step invokes the shared recipe with the
  `[SPEC-NNNN]: decompose tasks` title and a staging set limited to the spec's
  `TASKS.md`, runs after `speccy lock`, and the combined
  `create spec and decompose tasks` string is absent from the source):
  demonstrated → Scenarios 1–4,
  `decompose_includes_shared_commit_recipe_and_branch_guard`,
  `decompose_stages_tasks_md_alone_with_decompose_title`,
  `decompose_drops_combined_bootstrap_title`, plus Scenario 5's
  `rendered_decompose_agent_fully_expands_includes` proving the recipe (which
  carries the title and single-parent HEREDOC commit) expands into the rendered
  decompose agent body.
- **CHK-009 decompose side** (the scenarios this task names: `speccy-decompose.md`
  pulls the recipe via `{% include %}` and no longer restates the inline
  `git diff --cached --quiet` recipe): demonstrated → Scenario 1
  (`decompose_includes_shared_commit_recipe_and_branch_guard`, include-present)
  and Scenario 4 (`decompose_drops_inline_diff_cached_recipe`, inline-drop).
  CHK-009 lives under REQ-007, formally covered by sibling task T-003 (review
  side); this task carries `covers="REQ-004"` and accounts for the decompose
  side here because its task-scenarios block names it.
