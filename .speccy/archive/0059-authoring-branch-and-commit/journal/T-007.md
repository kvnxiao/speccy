---
spec: SPEC-0059
task: T-007
generated_at: 2026-06-11T18:11:05Z
---

<implementer date="2026-06-11T18:11:05Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: bounded to `speccy-cli/tests/authoring_commit.rs` and the source modules it asserts over. Reuse-as-is: existing `RESOURCES`, `HostChoice`, `render_host_pack`, and the `panic_with_message` test fixture — the four new tests call them directly. Write-fresh: one new helper `module_body(rel)` (a generic bundle-relative reader; searched the file for an existing arbitrary-path reader — only per-module named readers like `commit_recipe_body()` existed, none generic, so wrote fresh) plus four new cross-set test fns (`branch_guard_included_by_exactly_the_three_authoring_sources`, `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate`, `rendered_authoring_skills_fully_expand_with_guard_and_recipe`, `reeject_leaves_working_tree_clean`). No extend tier — the existing per-callsite tests assert per-file properties; the cross-set invariants are genuinely new. No source module edited (task is the verification-only slice); reeject confirmed sources and packs are already in sync.
- Completed: added the four T-007 cross-set assertions to `speccy-cli/tests/authoring_commit.rs`. (1) Branch-guard scoping (REQ-008/CHK-011/CHK-001): the `branch-guard.md` include is present in exactly `skills/speccy-plan.md`, `phases/speccy-decompose.md`, `skills/speccy-amend.md` and absent from `phases/speccy-work.md`, `phases/speccy-ship.md`, `skills/partials/review-fanout.md`, which retains its "Commits land on whatever HEAD is" statement. (2) Narrow staging / no clean-tree gate (REQ-009/CHK-012): each authoring body carries its narrow `git add <path>` command and none contains a `git stash` gate. (3) Rendered expansion: the three rendered authoring skills carry no `{{`/`{%`/`{#` markup and the branch-guard `git switch -c` and recipe `git diff --cached --quiet` text survive. (4) Reeject-clean: `just reeject` leaves `.claude/`/`.agents/`/`.codex/` clean.
- Undone: nothing in scope is deferred. All four REQ-008/REQ-009 task scenarios are covered.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail is the test suite `speccy-cli/tests/authoring_commit.rs` (28 tests pass). Roll call for CHKs under REQ-008/REQ-009:
  - CHK-011 (branch-guard included by exactly plan/decompose/amend, absent from work/ship/review, review retains unguarded statement): demonstrated → `branch_guard_included_by_exactly_the_three_authoring_sources` enumerates the include across all six sources and asserts the retained statement.
  - CHK-001 (review-pass commit lands unguarded on whatever HEAD is): demonstrated → same test asserts the "Commits land on whatever HEAD is" statement is present in `review-fanout.md` and the guard include is absent there.
  - CHK-012 (narrow staging, no clean-tree gate): demonstrated → `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate` asserts each body's narrow `git add <path>` command and the absence of `git stash`.
- Discovered issues: my first draft of `reeject_leaves_working_tree_clean` ran `git status --porcelain` over the whole tree, which fails on any unrelated in-flight dirty path (the task's own WIP, TASKS.md state flips) — that is not ejected-pack drift and would make the test order-dependent. Scoped the cleanliness check to `-- .claude .agents .codex`, which gates exactly the CHK property ("committed ejected packs match their sources"). Reeject itself produced no pack changes, confirming sources and packs are in sync at HEAD.
- Procedural compliance: followed speccy-work first-attempt branch (clean tree, no journal). No source module under `resources/` needed editing — this is the verification-only slice and reeject surfaced no drift, so no `just reeject` regeneration commit was required. Avoided a fragile `!contains("git add -A")` negative assertion because all three authoring bodies legitimately forbid the broad form by name in prohibition prose; anchored CHK-012 on the positive narrow-staging command and the stable `git stash` token instead, consistent with the T-005/T-006 tests' documented rationale.
</implementer>
<review date="2026-06-11T18:12:36Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in this test-only verification slice. The diff adds
four cross-set assertions to `speccy-cli/tests/authoring_commit.rs` (no
production code, no auth boundary, no secret handling, no crypto, no
untrusted-input parsing). Reviewed the security-adjacent angles: (1) the
`reeject_leaves_working_tree_clean` test shells out to `just reeject`
and `git status` via `Command::args([...])` with a vector arg list — no
shell interpolation, and `repo_root` derives from the compile-time
`CARGO_MANIFEST_DIR`, so there is no command-injection surface
(`authoring_commit.rs:1087-1136`). (2) The CHK-012 narrow-staging
invariant the test locks in is security-positive: it gates that the
authoring commits use `git add <path>` rather than `git add -A`, so
unrelated dirty files in the working tree (which could carry credentials)
are not inadvertently staged/committed, and confirms no `git stash`
gate (`authoring_commit.rs:992-1035`). (3) Failure messages print only
git status over the ejected pack dirs — no sensitive-data exposure.
</review>
<review date="2026-06-11T18:13:06Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The T-007 additions to `speccy-cli/tests/authoring_commit.rs` follow established conventions throughout.

- `#![allow(clippy::expect_used, reason = "...")]` at the crate level matches the identical header carried by every sibling test file. The `allow-expect-in-tests = true` setting in `clippy.toml` means the lint does not fire independently, but the annotation is a pre-existing project convention, not a violation.
- The `module_body(rel)` helper at line 935 is a justified fresh helper — the implementer's survey is present and accurately notes that only named per-module readers (e.g. `commit_recipe_body()`) existed; no generic reader was available to reuse.
- All four new test functions follow the project's snake_case long-descriptive-name pattern and the `#[test]` annotation style of the surrounding suite.
- Tests anchor on stable structural tokens (`{% include "modules/references/branch-guard.md" %}`, `git add <spec-dir>/SPEC.md`, `git stash`, `Commits land on whatever HEAD is`) and avoid substring-matching curated prose sentences, consistent with `persona_snippets.rs` and `skill_body_discovery.rs` suites.
- `reeject_leaves_working_tree_clean` is scoped to `-- .claude .agents .codex`, not the whole tree — the correct fix for avoiding false failures from in-flight WIP, and the reasoning is documented inline.
- No unused imports, no dead code, no `unwrap()` in new production paths. The `#[expect(clippy::panic, reason = "...")]` on `panic_with_message` matches the suppression pattern already present at line 176.
- No reinvention found. The reuse survey names `RESOURCES`, `HostChoice`, `render_host_pack`, and `panic_with_message` — all confirmed present in the file and called by the new tests.
</review>
<review date="2026-06-11T18:13:06Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Reviewed the four T-007 cross-set assertions in `speccy-cli/tests/authoring_commit.rs:946-1136` for logic/control-flow correctness. No defects found. Verified against ground truth: the branch-guard include graph in the loop at `:951-975` matches the sources (present in plan/decompose/amend, absent from work/ship/review-fanout) and the present/absent direction is not inverted; the narrow-staging positive tokens at `:1000-1018` (`git add <spec-dir>/SPEC.md`, `git add <spec-dir>/TASKS.md`, `git add <paths>`) match the source bodies exactly; the `!contains("git stash")` negative at `:1029-1034` holds (no `git stash` in any of the three sources). The `reeject_leaves_working_tree_clean` test at `:1086-1136` asserts `just reeject` success then `git status --porcelain -- .claude .agents .codex` trims-to-empty — sound control flow, no swallowed error (status non-success would fail the assert). `module_body` at `:935-940` uses `get_file`/`and_then`/`unwrap_or_else` with no slice indexing and no Option mishandling. No off-by-one, inverted condition, resource leak, or concurrency issue in the diff. Suite runs 28/28 green.
</review>
<review date="2026-06-11T18:13:15Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. The whole-set verification slice satisfies REQ-008 and REQ-009 as the SPEC named them, and the four T-007 tests gate genuine cross-set regressions (not curated prose). I verified every assertion against the real source state, not just the test text.

REQ-008 done-when mapping: branch-guard include present in exactly plan/decompose/amend (`speccy-plan.md:144`, `speccy-decompose.md:94`, `speccy-amend.md:175`) and absent from work/ship/review (`branch_guard_included_by_exactly_the_three_authoring_sources`, `authoring_commit.rs:947`); review-fanout retains its unguarded "Commits land on whatever HEAD is" statement (`review-fanout.md:188`, asserted at `authoring_commit.rs:979`). The negative loop also covers review-fanout — broader than done-when item 2 but exactly CHK-011's "by no others" clause, so it strengthens the mapping.

REQ-009 done-when mapping: narrow `git add <path>` staging per callsite asserted positively (`authoring_commit.rs:992`), confirmed in source (`speccy-plan.md:149`, `speccy-decompose.md:99`, `speccy-amend.md:180`); no clean-tree gate via `!contains("git stash")` over all three bodies, anchored on the SPEC's own no-stashing non-goal token. Done-when item 2 (unrelated dirty paths stay modified at runtime) is a behavioral property the SPEC scopes to CHK-012 as a source-inspection check, matching the task's explicit "whole-set verification over a stable structural surface" framing — no gap.

Non-goals respected: diff touches only `speccy-cli/tests/authoring_commit.rs` (no source module, no CLI verb, no mode toggle); `just reeject` is a no-op (ejected packs already in sync) and left `.claude`/`.agents`/`.codex`/`resources` byte-clean after the run. All 28 tests pass. No open question silently resolved.
</review>
<review date="2026-06-11T18:13:44Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="blocking" round="1">
Blocking on the evidence paper trail, not the test code. The four T-007 tests are sound and non-vacuous: `branch_guard_included_by_exactly_the_three_authoring_sources` enumerates the `branch-guard.md` include across all six skill/phase sources and gates the REQ-008 scoping invariant (present in plan/decompose/amend, absent from work/ship/review-fanout, review retains "Commits land on whatever HEAD is"); `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate` anchors CHK-012 on the positive narrow `git add <path>` command per body plus a `git stash` absence (correctly avoiding the `!contains("git add -A")` trap, since each body's prohibition prose names the broad form); `rendered_authoring_skills_fully_expand_with_guard_and_recipe` gates full include expansion; `reeject_leaves_working_tree_clean` gates pack-vs-source parity. All 28 tests pass and the asserted strings exist in the sources (verified via grep).

The blocker is the evidence-loading contract (step 3). The T-007 implementer-block `Evidence:` field names no per-task evidence file path — it reads "paper trail is the test suite `speccy-cli/tests/authoring_commit.rs` (28 tests pass)" — and no `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` exists on disk (only T-001..T-006 are present). Yet the roll call labels CHK-011, CHK-001, and CHK-012 all `demonstrated`, and the coverage rule (`resources/modules/references/evidence.md`) requires every `demonstrated` label to be backed by exactly one `### Scenario N` block in that file. Those `demonstrated` labels are therefore unbacked. Every sibling in this spec produced a real per-task evidence file (T-005 explicitly flagged writing "a real `evidence/T-005.md` red->green file" as procedural compliance after a sibling was blocked for omitting one); T-007 is the lone gap.

To clear: write `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` with the red->green pairs backing the `demonstrated` CHKs (red = the cross-set invariant assertion failing against the state before T-003..T-006 landed, e.g. the include absent from a source or present in work/ship; green = the assertion passing at HEAD), and update the `Evidence:` field to name that path with each `### Scenario N` cited verbatim — or, if the cross-set assertions genuinely cannot produce a red phase, relabel the CHKs `hygiene` and cite the specific test name and file per the hygiene-label rule. As written, the field neither names a file nor cites a hygiene test for the `demonstrated` CHKs.
</review>
<blockers date="2026-06-11T18:14:34Z" round="1">
Back the `demonstrated` CHK labels with a real evidence file before re-review (tests persona). The test code itself is sound and non-vacuous; the gap is the evidence paper trail.

- tests: The T-007 implementer handoff `Evidence:` field names no per-task evidence file (it reads "the test suite ... 28 tests pass") and no `evidence/T-007.md` exists on disk, yet the roll call labels CHK-011, CHK-001, and CHK-012 as `demonstrated`. The coverage rule (`resources/modules/references/evidence.md`) requires every `demonstrated` label to be backed by exactly one `### Scenario N` block in a named evidence file, so those labels are currently unbacked. Every sibling T-001..T-006 produced a real evidence file.

To clear, pick one:
1. Write `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` with red->green pairs backing each `demonstrated` CHK (red = the cross-set invariant failing against the pre-T-003..T-006 state, e.g. the branch-guard include absent from an authoring source or present in work/ship; green = the assertion passing at HEAD), and update the `Evidence:` field to name that path with each `### Scenario N` cited verbatim.
2. If the cross-set assertions genuinely cannot produce a red phase, relabel CHK-011/CHK-001/CHK-012 as `hygiene` and cite the specific test name + file (`speccy-cli/tests/authoring_commit.rs`) per the hygiene-label rule.
</blockers>
<implementer date="2026-06-11T18:17:55Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: retry round addressing a non-reuse blocker (the tests persona blocked on the evidence paper trail, not the test code), so the full area-map is not re-run. No new top-level symbol introduced this round — the round-1 `module_body` helper and four test fns are unchanged. The only artifact added this round is the per-task evidence file `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md`, which is prose, not a code symbol.
- Completed: addressed the round-1 tests blocker. Wrote `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` with red->green pairs backing the three `demonstrated` CHKs, and updated the round-2 `Evidence:` field below to name that path with each Scenario N cited verbatim. The red baseline is the pre-callsite source tree at the T-002 boundary (`57c0a89`), where the branch-guard include and narrow-staging commands are absent from all three authoring sources; verified against real git history via `git show 57c0a89:<path> | grep -c` (counts 0 red, 1 green). No test code changed this round — the round-1 review found the four tests sound and non-vacuous; the gap was purely the missing evidence file.
- Undone: nothing in scope is deferred.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md`. Roll call for CHKs under REQ-008/REQ-009:
  - CHK-011 (branch-guard included by exactly plan/decompose/amend, absent from work/ship/review, review retains its unguarded statement): demonstrated → evidence Scenario 1 covers the red/green pair (include count 0 across the three authoring sources at `57c0a89` -> count 1 at HEAD, work count 0, review-fanout statement retained), backed by `branch_guard_included_by_exactly_the_three_authoring_sources`.
  - CHK-001 (review-pass commit lands unguarded on whatever HEAD is): demonstrated → evidence Scenario 1's same test asserts the "Commits land on whatever HEAD is" statement is retained in `review-fanout.md` and the guard include is absent there.
  - CHK-012 (narrow staging per callsite, no clean-tree refusal gate): demonstrated → evidence Scenario 2 covers the red/green pair (narrow `git add <path>` command absent at `57c0a89` -> present at HEAD; no `git stash` gate), backed by `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate`.
- Discovered issues: none this round. The round-1 implementer handoff Evidence field named no evidence file while labelling the CHKs `demonstrated`, violating the coverage rule in `resources/modules/references/evidence.md`; this round closes that gap rather than relabelling, because the cross-set assertions genuinely have a red phase against the pre-callsite source tree (verified above).
- Procedural compliance: followed the speccy-work retry branch — read the latest blockers round (round 1, tests persona) and the prior implementer handoff from the context bundle's journal section, amended the prior pass's WIP in place (no `git restore`/`git clean`/`git checkout`, no test rewrite, no state reset), flipped through in-progress -> in-review, and appended this round-2 block. No source module under `resources/` needed editing — the blocker was about the evidence artifact, not a stale skill instruction.
</implementer>
<review date="2026-06-11T18:19:10Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns (round-2 re-review). The only new artifact this round
is the prose evidence file
`.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` — no
production code, no test code changed since the round-1 pass. The file
carries only git/cargo command transcripts and red->green prose; no
secrets, credentials, tokens, or sensitive data are embedded, and it
introduces no auth boundary, injection vector, or crypto surface. The
underlying test diff (`speccy-cli/tests/authoring_commit.rs`) was reviewed
and passed in round 1: its `reeject_leaves_working_tree_clean` test shells
out via `Command::args([...])` vector arg lists (no shell interpolation,
`repo_root` from compile-time `CARGO_MANIFEST_DIR`), and the CHK-012
narrow-staging invariant it locks in remains security-positive — gating
`git add <path>` over `git add -A` so unrelated dirty files that could
carry credentials are not inadvertently staged.
</review>
<review date="2026-06-11T18:19:40Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Pass. Round-2 retry resolves the round-1 tests blocker on the evidence paper trail, and the whole-set verification slice still satisfies REQ-008/REQ-009 as the SPEC named them.

Blocker resolution (verified, not taken on trust): `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` now exists with red->green pairs, the round-2 `Evidence:` field names that path, and each `demonstrated` CHK cites its scenario verbatim (CHK-011 -> Scenario 1, CHK-001 -> Scenario 1 review-side, CHK-012 -> Scenario 2). The coverage rule (`resources/modules/references/evidence.md`) holds: both scenario blocks are cited, no orphans, and the render/reeject tests are correctly left unbacked as whole-set guards rather than distinct demonstrated CHKs. The evidence red/green counts are truthful against real git history: at the T-002 boundary (`57c0a89`) the branch-guard include and narrow-staging commands are absent from all three authoring sources (count 0); at HEAD they are present in exactly plan/decompose/amend (count 1) and absent from work/ship/review-fanout, with review-fanout retaining "Commits land on whatever HEAD is" (count 1) -- all reproduced independently.

REQ-008 done-when mapping: branch-guard included by exactly plan/decompose/amend and absent from work/ship, review-pass commit retains its unguarded statement -- gated by `branch_guard_included_by_exactly_the_three_authoring_sources` (`authoring_commit.rs:947`), confirmed against the live sources. REQ-009 done-when mapping: narrow `git add <path>` staging per callsite plus no clean-tree gate (`!contains("git stash")`) -- gated by `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate` (`authoring_commit.rs:992`). The runtime done-when items (unrelated dirty paths stay modified at commit time) are scoped by the SPEC's own CHK-012 to source-inspection, matching the task's explicit "whole-set verification over a stable structural surface" framing -- no gap.

Non-goals respected: the diff touches only `speccy-cli/tests/authoring_commit.rs` plus the new evidence/journal artifacts -- no CLI verb, no mode toggle, no source-module edit; `just reeject` is a no-op confirming packs are in sync. All 28 tests pass. No open question silently resolved.
</review>
<review date="2026-06-11T18:19:49Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. Round-2 re-review of the evidence paper trail at
`.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md`; no
test code changed since round 1 (verified the four test fns at
`authoring_commit.rs:947-1035` are unchanged and match the round-1
correctness pass). Verified every red-baseline and green claim in the
evidence file against git ground truth.

Red baseline at `57c0a89` (evidence lines 47-56): `git show
57c0a89:<path>` confirms branch-guard include count 0 in
`speccy-plan.md`, `speccy-decompose.md`, `speccy-amend.md`, and narrow
`git add <spec-dir>/SPEC.md` / `git add <paths>` count 0 — all match.
Because `branch_guard_included_by_exactly_the_three_authoring_sources`
asserts `contains(guard_include)` over each authoring source, a count-0
source makes that assertion fail, so the claimed red FAILED output is
the correct behaviour-direction for the reasoned baseline.

Green at HEAD (evidence lines 82-140): branch-guard present in exactly
plan/decompose/amend (1 each), absent from work/ship/review-fanout (0
each), `review-fanout.md` retains "Commits land on whatever HEAD is"
(1), narrow `git add <path>` per callsite (plan SPEC.md, decompose
TASKS.md, amend <paths> — 1 each), and no `git stash` in any of the
three authoring sources — all confirmed against the live tree. The CHK
roll call (CHK-011, CHK-001 via Scenario 1; CHK-012 via Scenario 2) is
backed by matching `### Scenario N` blocks. No logic/control-flow
defect in the evidence claims or the unchanged tests.
</review>
<review date="2026-06-11T18:19:57Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 re-review clears the round-1 evidence blocker. The retry implementer wrote `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md` and the round-2 `Evidence:` field names it. Roll call is complete and honest: REQ-008/REQ-009 cover CHK-011, CHK-001, CHK-012, all three labelled `demonstrated`, each backed by exactly one `### Scenario N` block (CHK-011 + CHK-001 share Scenario 1, matching the task's own grouping of the include-scoping invariant; CHK-012 → Scenario 2). The render/reeject tests are honestly disclosed as whole-set guards subsumed by Scenarios 1-2 rather than padded into fake CHK blocks.

Verified the red->green pairs are honest, not fabricated. Red baseline is grounded in real git history at `57c0a89` (the T-002 commit, confirmed via `git log -1`): I independently reproduced the evidence's count-0 claims via `git show 57c0a89:<path>` — branch-guard include count 0 in plan/decompose/amend, narrow-staging tokens count 0, and `speccy-amend.md` already existed at that commit (so count-0 is a real "include absent" red, not a missing-file artifact). Green at HEAD: branch-guard include count 1 in exactly plan/decompose/amend and 0 in work/ship/review-fanout; narrow `git add <path>` token count 1 per body; `git stash` absent from all three; review-fanout retains "Commits land on whatever HEAD is" — all confirmed via rg against the live sources. Red and green cargo output differ materially (FAILED 0/1 with the verbatim assertion message vs `ok`), the cited test fns all exist in the diff, and the evidence command is scoped per-test, not the workspace hygiene run.

The four T-007 tests remain sound and non-vacuous (re-confirmed from round 1): `branch_guard_included_by_exactly_the_three_authoring_sources` (authoring_commit.rs:947) gates the present/absent scoping in both directions; `authoring_bodies_stage_narrow_paths_with_no_clean_tree_gate` (:992) anchors CHK-012 on the positive narrow command plus `git stash` absence, correctly avoiding the `!contains("git add -A")` prohibition-prose trap; `rendered_authoring_skills_fully_expand_with_guard_and_recipe` (:1041) gates include expansion; `reeject_leaves_working_tree_clean` (:1087) gates pack-vs-source parity scoped to the ejected dirs. No mock-the-SUT, no snapshot-bakes-the-bug, no empty-body, no flake surface.
</review>
<review date="2026-06-11T18:20:18Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Pass. Round-2 diff is exclusively `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-007.md`; the test code in `speccy-cli/tests/authoring_commit.rs` was not changed from round 1, which already received a style pass.

The evidence file follows the same structural conventions as its six siblings (T-001..T-006): top-level heading, session block, `### Scenario N` sections with `<red>`/`<green>` blocks, and a CHK roll call at the foot. No novel patterns introduced; no curated prose substring-matched. The two non-scenario tests (`rendered_authoring_skills_fully_expand_with_guard_and_recipe`, `reeject_leaves_working_tree_clean`) are correctly noted as render/parity guards rather than behaviour-direction proofs, and their omission from the red/green pairs is explained inline — consistent with the evidence rule's hygiene-label guidance. No naming drift, no dead content, no reinvention.
</review>
