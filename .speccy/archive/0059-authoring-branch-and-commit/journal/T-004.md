---
spec: SPEC-0059
task: T-004
generated_at: 2026-06-11T07:43:03Z
---

<implementer date="2026-06-11T07:43:03Z" model="claude-opus-4-8[1m]/medium" round="1">
- Reuse survey: Bounded to the decompose step-4 area, REQ-004, and the shared reference modules. Reuse-as-is: `modules/references/commit-recipe.md` and `modules/references/branch-guard.md` (both created by T-001/T-002), pulled into `speccy-decompose.md` via `{% include %}` — no new commit/branch prose written. Extend: mirrored the parameterization-prose shape T-003 established in `review-fanout.md` (Staging breadth / Title and body bullets) for the decompose callsite; extended the existing `authoring_commit.rs` test module reusing its `panic_with_message`, `RESOURCES`, and `render_host_pack` helpers. Write-fresh: one new test helper `decompose_body()` and five new tests — searched `authoring_commit.rs` for any decompose-phase coverage and found none (T-001/T-002/T-003 only), so the decompose assertions had no prior home.
- Completed: Reworked step 4 of `resources/modules/phases/speccy-decompose.md` — inserted a `{% include "modules/references/branch-guard.md" %}` prelude ahead of the commit and replaced the hand-rolled bootstrap commit body with `{% include "modules/references/commit-recipe.md" %}` parameterised to narrow `git add <spec-dir>/TASKS.md` staging and the title `[SPEC-NNNN]: decompose tasks`. Removed `SPEC.md` from the staging set and deleted the combined title string `create spec and decompose tasks`. Commit still runs after `speccy lock`. Added five tests to `speccy-cli/tests/authoring_commit.rs` (CHK-005 + CHK-009 decompose side + render expansion). Ran `just reeject`; regenerated `.claude/agents/speccy-decompose.md` and `.codex/agents/speccy-decompose.toml`.
- Undone: Nothing in scope deferred. Sibling tasks T-005 (speccy-plan, the SPEC.md commit), T-006, T-007 remain pending and are out of scope here.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0 (after applying `cargo +nightly fmt --all`); `cargo deny check` exit 0.
- Evidence: source-and-render assertions in `speccy-cli/tests/authoring_commit.rs` (no separate evidence file — this is a prose-template task with no runtime behaviour to red/green). Roll call for CHKs under REQ-004:
  - CHK-005 (decompose commit step invokes shared recipe with `[SPEC-NNNN]: decompose tasks` title, TASKS.md-only staging, runs after `speccy lock`, combined string absent): demonstrated → `decompose_includes_shared_commit_recipe_and_branch_guard`, `decompose_stages_tasks_md_alone_with_decompose_title`, and `decompose_drops_combined_bootstrap_title` assert each clause over the embedded source; `rendered_decompose_agent_fully_expands_includes` proves the include expands with no residual MiniJinja markup.
- Discovered issues: Latent recursion bug in `branch-guard.md` (T-002): its line 6 documented its own include using the executable literal `` `{% include "modules/references/branch-guard.md" %}` `` inside backticks, which MiniJinja evaluated as a real self-include — infinite recursion at render. It never surfaced because T-004 is the module's first real callsite. Fixed by rewording to the prose form `commit-recipe.md` already uses ("Each callsite pulls it in with a MiniJinja `include` directive naming ..."). No test asserted on that literal in branch-guard.md's own body, so the fix broke nothing.
- Procedural compliance: Edited only under `resources/` then ran `just reeject` per the skill-pack source-of-truth rule; did not touch `.claude/`/`.codex/` by hand. The `branch-guard.md` fix is an in-scope friction fix (my callsite was the first to trip it) per AGENTS.md's friction-to-source-update rule. Dropped one brittle negative-string check (`!body.contains("git add -A"/"git add .")`) from a test because the phase prose legitimately names those forms to forbid them — replaced with positive narrow-staging and absent-SPEC.md-staging assertions, which are the real invariants.
</implementer>
<review date="2026-06-11T07:45:05Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
Stale module-doc header in `speccy-cli/tests/authoring_commit.rs`.

The crate-level `//!` opening sentence (line 5) still reads
"Tests for SPEC-0059 T-001, T-002, and T-003" after T-004's
additions. T-004 appends a full T-004 section to the module doc
(visible in the diff as the `+` block starting at line ~793) and
adds five new test functions, but does not update the opening
summary sentence to include T-004. The docs no longer match the
code — any reader of the module header gets a false picture of
what the file covers.

Fix: change the opening line to
"Tests for SPEC-0059 T-001, T-002, T-003, and T-004:"
(or equivalent phrasing that names all four tasks).

File: `speccy-cli/tests/authoring_commit.rs`, line 5.
</review>
<review date="2026-06-11T07:45:05Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No injection surface introduced by the decompose branch-guard/commit rework. Branch-name derivation (`git switch -c spec-NNNN-slug`, `resources/modules/references/branch-guard.md:166`) builds the name from the spec-dir basename; the slug is lint-constrained to lowercase ASCII with hyphens (`docs/ARCHITECTURE.md:2870`), and the fixed `spec-` prefix means the name can never be parsed as a leading-`-` option, so no command- or option-injection vector. Staging path `git add <spec-dir>/TASKS.md` (`resources/modules/phases/speccy-decompose.md:744`) is the same ASCII-constrained path under narrow `git add` (no `-A`/`.`), leaving unrelated dirty paths untouched. Attacker-controlled SPEC frontmatter flows only into the commit body, which is passed through a single-quoted heredoc `<<'EOF'` (`commit-recipe.md` "Stage, then skip-if-empty, then commit"), disabling parameter/command expansion — a `title:` of `$(...)` or backticks is committed literally, not executed. The `[focus]` segment is dropped before name derivation, so no path-traversal via mission folders. No secret handling, auth boundary, or sensitive-data exposure in scope; the branch-creation notice emits only a branch name. Detection tiers read only branch-name config (`origin/HEAD`, `init.defaultBranch`), no credentials.
</review>
<review date="2026-06-11T07:45:10Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-004 done-when fully satisfied by the diff. The decompose step-4 commit
now stages `git add <spec-dir>/TASKS.md` alone (SPEC.md removed from the set
per DEC-005), titles the commit `[SPEC-NNNN]: decompose tasks`, and runs
after `speccy lock` — confirmed in `resources/modules/phases/speccy-decompose.md`
and in the rendered `.claude/agents/speccy-decompose.md` and
`.codex/agents/speccy-decompose.toml` (includes fully expanded, no residual
MiniJinja markup). The combined `create spec and decompose tasks` title is
absent from source and both rendered packs. Idempotency (unchanged TASKS.md →
no commit) is preserved by delegating to the shared recipe's
`git diff --cached --quiet` skip-if-empty check rather than inlining it.
SPEC.md is correctly deferred to speccy-plan (T-005), not silently dropped;
the commit body still sources SPEC.md's `title:` frontmatter, keeping the
per-skill commit traceable. No non-goal breached (no CLI change, no
toggle/flag, no confirmation prompt, no stashing). All Open Questions were
already closed in SPEC.md; nothing silently resolved. The in-scope
branch-guard.md recursion fix rewords the self-referential include line to the
prose form commit-recipe.md uses, preserving the single-source-of-truth
statement without behaviour change. 16/16 authoring_commit tests pass.
</review>
<review date="2026-06-11T07:45:20Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="blocking" round="1">
No per-task evidence file and no red->green trail. The implementer
handoff's Evidence field names no evidence-file path -- it states "no
separate evidence file ... no runtime behaviour to red/green" -- and no
`evidence/T-004.md` exists at
`.speccy/specs/0059-authoring-branch-and-commit/evidence/`. Per the
evidence-loading contract, the absence of a named evidence file is a
blocking verdict.

The "no runtime behaviour to red/green" rationale does not hold: this
is the same prose-template, content-property-assertion class as sibling
T-003, which DID produce `evidence/T-003.md` with a genuine red phase --
it ran the new tests against the pre-edit tree (`git show HEAD:... | grep`
plus `cargo test` capturing FAILED) and then the post-edit `ok`. The red
phase for T-004 is equally constructible: the five new `decompose_*` tests
in `speccy-cli/tests/authoring_commit.rs:422-544` would all fail against
the pre-edit `resources/modules/phases/speccy-decompose.md`, which still
carried the combined `create spec and decompose tasks` title, no
`{% include %}` directives, and an inline `git diff --cached --quiet`.
Produce `evidence/T-004.md` with that red->green trail (scoped per-test
runner output, not the full hygiene suite) and name it in the Evidence
field.

Roll-call gap (secondary): the task-scenarios block references CHK-009
(decompose side, exercised by `decompose_drops_inline_diff_cached_recipe`
at `authoring_commit.rs:501`) but the journal roll call lists only CHK-005.
CHK-009 lives under REQ-007, which this task does not formally cover
(covers="REQ-004"), so this alone is not the hard block -- but the evidence
file should account for the CHK-009 decompose-side coverage the scenarios name.

The implementation and tests themselves are sound: the rendered
`.claude/agents/speccy-decompose.md` fully expands both includes (recipe's
`git diff --cached --quiet` present, no residual MiniJinja markup, combined
title gone), and the five tests genuinely exercise the CHK-005 clauses --
title, narrow `git add <spec-dir>/TASKS.md` staging, absent SPEC.md staging,
`speccy lock` ordering, absent combined string, and full include expansion --
each of which would flip red under a wrong implementation. The block is the
missing evidence trail, not the code.
</review>
<review date="2026-06-11T07:45:21Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Control flow and ordering in the reworked decompose step 4 are correct.
- Commit-step ordering is sound: step 3 runs `speccy lock SPEC-NNNN` (rewrites TASKS.md frontmatter in place), then step 4 runs the branch-guard prelude (`resources/modules/phases/speccy-decompose.md:94`) before the commit recipe (`:117`). The branch-guard's `git switch -c` after lock carries the locked TASKS.md onto the new branch (git preserves uncommitted working-tree changes across `switch -c`), so the committed content is the locked artifact. The "commit runs after speccy lock" requirement (REQ-004) holds.
- Self-include recursion is genuinely fixed: `branch-guard.md` carries no `{% include %}` directive (its former self-referential executable literal at line 6 is reworded to prose "Each callsite pulls it in with a MiniJinja `include` directive naming ..."). The rendered `.claude/agents/speccy-decompose.md` expands branch-guard, commit-recipe, and the nested identity-sourcing include exactly once each with zero residual MiniJinja markup (grep for `{%` returns no matches). No infinite recursion at render.
- Branch-guard control flow is correct: no-git short-circuit on non-zero `git rev-parse --is-inside-work-tree`; ordered three-tier default-branch detection with each tier gated on the prior not resolving; create path on (default-branch OR detached HEAD), reuse otherwise — the detached case correctly maps to create since `git symbolic-ref --quiet --short HEAD` exits non-zero. No inverted condition or off-by-one.
- Commit-recipe idempotency check (`git diff --cached --quiet`, exit 0 = skip, non-zero = commit) matches git's `--quiet` semantics. Narrow `git add <spec-dir>/TASKS.md` staging leaves unrelated dirty paths untouched; SPEC.md is dropped from staging (now committed by speccy-plan).
- `cargo test --test authoring_commit` is green (16 passed), including `rendered_decompose_agent_fully_expands_includes`.
No correctness defect found at >=80 confidence.
</review>
<blockers date="2026-06-11T07:46:07Z" round="1">
Two blockers before the next pass: add the missing evidence file and update the stale test-module doc header.

- **tests**: No `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-004.md` exists and the round-1 Evidence field names no per-task path. Per the evidence-loading contract, an Evidence field that does not name an existing per-task evidence file is blocking. The "no runtime behaviour" rationale does not excuse it — the red→green trail is constructible and was produced for the identical-class sibling T-003 (e.g. a scoped `cargo test --test authoring_commit <test>` failing against the pre-refactor decompose phase, then passing). Write `evidence/T-004.md` following the T-003 shape (per-scenario red/green captured output plus a CHK roll call for CHK-005 and the CHK-009 decompose side) and point the Evidence field at it.
- **style**: `speccy-cli/tests/authoring_commit.rs:5` — the module doc `//!` header still reads "Tests for SPEC-0059 T-001, T-002, and T-003 ..." but T-004 added five new test functions and a T-004 doc section to the same file. Docs-match-code drift introduced by T-004's own additions. Widen the one-line header to also cover T-004 (the decompose-phase branch-guard + commit-recipe rewrite), matching the existing per-task description pattern.
</blockers>
<implementer date="2026-06-11T07:50:06Z" model="claude-opus-4-8[1m]/medium" round="2">
- Reuse survey: Retry round addressing two non-reuse blockers (missing evidence file, stale doc header); no new top-level symbols introduced, so the per-symbol floor is vacuous and no area re-survey is owed (neither blocker was reuse-related). Reuse-as-is: the round-1 `decompose_*` / `rendered_decompose_*` tests and the existing `evidence/T-003.md` shape, which `evidence/T-004.md` mirrors.
- Completed: Addressed both round-1 blockers by amending the prior pass's WIP in place. (1) tests blocker — wrote `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-004.md` with a real red->green trail following the T-003 shape: stashed the WIP edit of `speccy-decompose.md` so the embedded bundle compiled against HEAD's pre-edit phase, ran each scoped `decompose_*` test (4 of 5 failed red), then `git stash pop` and re-ran green; pre-edit source inspected via `git show HEAD:...`; per-scenario red/green plus a CHK roll call for CHK-005 and the CHK-009 decompose side. (2) style blocker — updated the stale crate-level `//!` header at `speccy-cli/tests/authoring_commit.rs:5` from "T-001, T-002, and T-003" to name all four tasks including T-004's decompose-phase rewrite.
- Undone: Nothing in scope deferred. Sibling tasks T-005/T-006/T-007 remain pending and out of scope.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (one pre-existing unmatched-license-allowance warning, not a failure).
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-004.md`. Roll call for CHKs under REQ-004:
  - CHK-005 (decompose commit step invokes shared recipe with `[SPEC-NNNN]: decompose tasks` title, TASKS.md-only staging, runs after `speccy lock`, combined string absent): demonstrated -> evidence Scenarios 1-4 (`decompose_includes_shared_commit_recipe_and_branch_guard`, `decompose_stages_tasks_md_alone_with_decompose_title`, `decompose_drops_combined_bootstrap_title`) each captured failing red against HEAD's pre-edit phase then passing green; Scenario 5 (`rendered_decompose_agent_fully_expands_includes`) proves the include expands with no residual MiniJinja markup.
  - CHK-009 decompose side (task-scenarios names it; formally under REQ-007 / T-003): demonstrated -> evidence Scenarios 1 and 4 (`decompose_drops_inline_diff_cached_recipe`).
- Discovered issues: None new this round. The branch-guard.md self-include recursion fix from round 1 stands and is recorded in the evidence Scenario 5 red phase.
- Procedural compliance: Amended WIP in place per retry-shape — no `git restore`/`clean`/`checkout` against the WIP; the stash/pop was scoped to a single file purely to capture the red phase and the WIP was restored byte-for-byte. The test-file header fix is a Rust source edit (not under `resources/`), so no `just reeject` was needed; the round-1 reejected decompose harness output was left untouched.
</implementer>
<review date="2026-06-11T07:51:35Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Round-2 delta is security-inert. The only changes since the round-1 pass are an added evidence paper trail (`.speccy/specs/0059-authoring-branch-and-commit/evidence/T-004.md`) and a one-line doc-header rewording at `speccy-cli/tests/authoring_commit.rs:5` — neither touches production prose, branch-name derivation, staging commands, or commit-body handling. The evidence file is documentation only: command transcripts plus illustrative `[SPEC-NNNN]` placeholders, no real credentials, secrets, tokens, or env-var values committed. No new injection surface, auth boundary, or sensitive-data-exposure path. The round-1 production analysis stands unchanged: branch name is `spec-` + lint-constrained ASCII slug (no leading-`-`/option-injection), narrow `git add <spec-dir>/TASKS.md` (no `-A`/`.`), and the commit body flows through a single-quoted `<<'EOF'` heredoc disabling parameter/command expansion. No regression.
</review>
<review date="2026-06-11T07:51:37Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. Round-2 delta carries no correctness surface: the only changes since the round-1 pass are a new `evidence/T-004.md` (documentation, no executable code) and a one-line crate-doc `//!` header edit in `speccy-cli/tests/authoring_commit.rs:5-13` widening "T-001, T-002, and T-003" to name T-004. Neither touches control flow, Option/Result handling, boundary conditions, concurrency, or resource lifetimes — a doc comment and a per-task evidence note cannot introduce a logic defect. The substantive decompose step-4 rework (branch-guard prelude before commit-recipe, commit after `speccy lock`, narrow `git add <spec-dir>/TASKS.md` staging, SPEC.md dropped, idempotent `git diff --cached --quiet` skip, branch-guard self-include recursion fix) is unchanged from the round-1 tree and was reviewed and passed for correctness in round 1; re-reasoning over it confirms no defect. No correctness finding at >=80 confidence.
</review>
<review date="2026-06-11T07:52:24Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
REQ-004 done-when remains fully satisfied after round 2; the two round-1
blockers were documentation/evidence-only and did not shift the requirement
mapping. The decompose step-4 commit stages `git add <spec-dir>/TASKS.md` alone
(SPEC.md dropped per DEC-005), titles the commit `[SPEC-NNNN]: decompose tasks`,
runs after `speccy lock`, and delegates idempotency to the shared recipe's
`git diff --cached --quiet` skip-if-empty check -- confirmed in
`resources/modules/phases/speccy-decompose.md` and in the rendered
`.claude/agents/speccy-decompose.md` / `.codex/agents/speccy-decompose.toml`
(both includes fully expanded, no residual MiniJinja markup, combined
`create spec and decompose tasks` title absent). DEC-005's two-commit split is
honoured: SPEC.md is deferred to speccy-plan (T-005), not silently dropped, and
the commit body still sources SPEC.md's `title:` frontmatter so the per-skill
commit stays traceable. No non-goal breached (no CLI change, no toggle/flag, no
confirmation prompt, no stashing). Open Questions are all resolved in SPEC.md
and the changelog carries a single initial entry, so the diff cannot reflect
stale intent and nothing was silently decided. Round-2 fixes are scoped to the
blockers: `evidence/T-004.md` carries a genuine red->green trail mirroring the
T-003 shape (4 of 5 decompose tests failing against the pre-edit HEAD phase,
then green) with a CHK roll call for CHK-005 and the CHK-009 decompose side,
and the `authoring_commit.rs:5` doc header now names all four tasks. The five
`decompose_*` tests would each flip red under a wrong implementation (title,
narrow staging, absent SPEC.md staging, lock ordering, absent combined string,
full include expansion).
</review>
<review date="2026-06-11T07:52:37Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="2">
Round-2 evidence trail is present, real, and not fabricated. The named file `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-004.md` exists and is named in the round-2 implementer block's Evidence field; it follows the T-003 shape (per-scenario red/green captured output plus a CHK roll call).

Verified the red-phase grep claims against ground truth at HEAD: the pre-edit `resources/modules/phases/speccy-decompose.md` carries the combined `create spec and decompose tasks` title (HEAD lines 102, 122), the inline `git diff --cached --quiet` (HEAD line 95), `git add <spec-dir>/SPEC.md <spec-dir>/TASKS.md` (HEAD line 88), and zero recipe/branch-guard includes -- exactly as the evidence's pre-edit inspection (evidence lines 26-44) reports. Each of the four content-property `decompose_*` tests would therefore flip red against HEAD's phase source: include-present, `[SPEC-NNNN]: decompose tasks` title, absent-combined-string, and inline-diff-cached-drop assertions each invert under the pre-edit body. Scenario 5's vacuous-pass-on-bare-HEAD vs. recursion-panic-once-the-include-goes-live distinction is disclosed honestly (evidence lines 157-177) and corroborated: HEAD `branch-guard.md:6` carried a live self-referential include-of-itself inside backticks (confirmed via `git show HEAD:`), reworded to prose in the working tree with zero live self-includes -- the recursion red is real.

Green half confirmed independently: `cargo test --test authoring_commit` is 16/16 green, and the rendered `.claude/agents/speccy-decompose.md` has zero residual MiniJinja markup, carries the recipe's `git diff --cached --quiet` text once, drops the combined title, and carries `[SPEC-NNNN]: decompose tasks`.

CHK roll call complete for the covered REQ-004: CHK-005 = demonstrated (Scenarios 1-4 plus render Scenario 5, each test named and present in the diff at `speccy-cli/tests/authoring_commit.rs:426-547`). The CHK-009 decompose side the task-scenarios name is also accounted for as demonstrated (Scenarios 1, 4) with the correct note that CHK-009 is formally under REQ-007/T-003. Evidence command is scoped per-test, not the workspace hygiene run. No fabrication pattern matched: red/green outputs differ materially, test names appear in the diff, runner-format lines are consistent with cargo. The five tests genuinely gate the CHK-005 clauses -- each would catch a wrong implementation.
</review>
<review date="2026-06-11T07:52:39Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Prior round-1 blocker resolved: `speccy-cli/tests/authoring_commit.rs` line 5 now reads "Tests for SPEC-0059 T-001, T-002, T-003, and T-004" and accurately describes T-004's decompose-phase rewrite alongside the three prior tasks.

Style checks all pass:
- Module-doc header matches the file's actual coverage (all four tasks named, T-004 prose added at lines 11-13).
- Five new T-004 tests (`decompose_includes_shared_commit_recipe_and_branch_guard`, `decompose_stages_tasks_md_alone_with_decompose_title`, `decompose_drops_combined_bootstrap_title`, `decompose_drops_inline_diff_cached_recipe`, `rendered_decompose_agent_fully_expands_includes`) and `decompose_body()` helper follow the same naming and structural conventions as the sibling T-001/T-002/T-003 tests — pattern is consistent.
- No unused imports or dead code introduced.
- Each `assert!` carries a descriptive failure message, matching the existing style.
- `decompose_body()` docstring accurately describes what the function does.
- The `resources/modules/phases/speccy-decompose.md` prose edits are internally consistent; the step-4 description in the source matches what the ejected `.claude/agents/speccy-decompose.md` renders (no residual MiniJinja markup confirmed by grep).
- `#[expect(clippy::panic, reason = "...")]` suppression at line 74 carries a justification — compliant with the project's suppression convention. The crate-level `#![allow(clippy::expect_used, reason = "...")]` is pre-existing (introduced by T-001) and out of scope for this round.
- Reuse survey present in the round-2 implementer block; no reinvention found.
</review>
