---
spec: SPEC-0059
task: T-001
generated_at: 2026-06-11T06:53:33Z
---

<implementer date="2026-06-11T06:53:33Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area = the three authoring skills' commit prose + the two existing divergent copies named in DEC-002. Consolidating onto one fresh module (DEC-002 mandates exactly one shared module). Three new top-level "symbols" (prose sections): (1) no-git short-circuit — write-fresh; searched `resources/modules/references/` and the two existing commit copies (`review-fanout.md`, `phases/speccy-decompose.md`) for an existing git-repo probe, none present (REQ-010/CHK-013 is new). (2) stage→diff→skip→commit recipe — extends the unified form distilled from the two existing copies (`review-fanout.md` `git add -A` + `git status --porcelain`; `speccy-decompose.md` narrow-stage `git diff --cached --quiet`), reconciled to DEC-004's single `git diff --cached --quiet` form. (3) trailer — reuse-as-is via `{% include "modules/references/identity-sourcing.md" %}` (same directive used by `phases/speccy-work.md:252` and `personas/verdict_return_contract.md:39`).
- Completed: Created `resources/modules/references/commit-recipe.md` defining the stage → `git diff --cached --quiet` → skip-if-empty → commit recipe exactly once, opening with a `git rev-parse --is-inside-work-tree` no-git short-circuit (REQ-010/CHK-013), parameterised by staging breadth + title/body, with the `Co-Authored-By` trailer delegated via `{% include "modules/references/identity-sourcing.md" %}` and the message passed through a single-quoted HEREDOC. All illustrative values are fictional (`SPEC-NNNN`). Added `speccy-cli/tests/authoring_commit.rs` with three tests over the embedded RESOURCES bundle (one per task scenario: recipe-once, trailer-delegated, no-git short-circuit). No callsite wired (T-003..T-006 do that).
- Undone: Nothing in scope. The four consumers of this module land in T-003..T-006 per the task body.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-001.md`. Roll call for CHKs under REQ-006 and REQ-010:
  - CHK-008 (one shared module defines the stage→diff→commit recipe once, parameterised, delegating the trailer): demonstrated → evidence Scenario 1 covers the red/green pair via `commit_recipe_states_idempotency_check_exactly_once` (exactly one `git diff --cached --quiet`) and `commit_recipe_delegates_trailer_via_include` (include present, inline identity rule absent) in `speccy-cli/tests/authoring_commit.rs`.
  - CHK-013 (branch-guard AND commit-recipe modules specify a no-git short-circuit): demonstrated (commit-recipe side only) → evidence Scenario 2 covers `commit_recipe_specifies_no_git_short_circuit`. The branch-guard-module side of CHK-013 is out of scope here; it lands in T-002.
- Discovered issues: The committed SPEC.md (from the create-spec step at 8942ada) failed the pre-existing `every_in_tree_spec_md_has_blank_line_after_each_close_tag` lint at 10 `</scenario>`→`</requirement>` boundaries, which blocked the unconditional `cargo test --workspace` hygiene gate for ALL tasks in this SPEC. This is decompose/create-spec artifact drift unrelated to T-001's requirements.
- Procedural compliance: Followed speccy-work first-attempt branch (clean tree confirmed, journal absent → first-attempt shape). To unblock the hygiene gate I inserted one blank line at each of the 10 `</scenario>`→`</requirement>` boundaries in SPEC.md — purely mechanical formatting, zero requirements/scenario content change; `speccy check SPEC-0059` still renders 13 scenarios and `speccy next` shows no spec-hash drift (only the expected `state_in_progress_orphaned` in-flight drift). Flagging for reviewer awareness since editing SPEC.md is normally the authoring layer's job; if the reviewer prefers, this could instead be carried by a speccy-amend pass, but leaving it red would block every task here.
</implementer>
<review date="2026-06-11T06:55:03Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blocker in this slice. The shared commit recipe emits git
shell commands, so the injection surface is the caller-supplied
title/body; the recipe passes the message via a single-quoted HEREDOC
delimiter (`git commit -m "$(cat <<'EOF' ... EOF)"`,
`resources/modules/references/commit-recipe.md:62`), which disables
`$(...)`, backtick, and `$VAR` expansion inside the body — an
attacker-influenced spec title cannot trigger command substitution.
The no-git short-circuit uses the read-only probe `git rev-parse
--is-inside-work-tree` (line 28) and fails safe by skipping, not
erroring. The trailer delegates to `identity-sourcing.md`, which sources
only a model identifier and effort from a definition file and
explicitly forbids reading `CLAUDE_EFFORT`/inherited env vars — no
credential, token, or env-var secret is written into the commit. The
test `speccy-cli/tests/authoring_commit.rs` is read-only over the
embedded RESOURCES bundle: no file writes, shell exec, network, or
secret handling. No authz boundary, crypto, or TOCTOU surface in this
task.
</review>
<review date="2026-06-11T06:55:14Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. T-001 adds a prose recipe module (`resources/modules/references/commit-recipe.md`) and three Rust assertion tests (`speccy-cli/tests/authoring_commit.rs`); the only executable control flow is the test harness, which I reviewed for correctness-lane defects.

- The no-git short-circuit prose is logically sound: `git rev-parse --is-inside-work-tree` non-zero -> skip the whole commit step, file left on disk (commit-recipe.md:24-35). No inverted branch.
- The unified idempotency check correctly maps `git diff --cached --quiet` exit 0 -> skip (nothing staged), non-zero -> commit (commit-recipe.md:46-56). Exit-code semantics match git's contract; not inverted.
- Test `commit_recipe_body()` uses `unwrap_or_else` with a descriptive panic in test-only code; the lookup `None` path is handled, no silent unwrap (authoring_commit.rs:26-36).
- The three assertions key off literals that are actually present: one `git diff --cached --quiet` occurrence (count == 1 matches the single literal at commit-recipe.md:49), the include string (commit-recipe.md:81), the negative inline-heading check against `## Sourcing your recorded identity` (verified as identity-sourcing.md's first heading, so the delegation assertion is not vacuous), and the lowercased `not a git repository` + `without erroring` substrings (both present at commit-recipe.md:32-33). No off-by-one or boundary defect in the count assertion.

No reachable logic, control-flow, Option/Result, boundary, concurrency, or resource-leak defect found in scope.
</review>
<review date="2026-06-11T06:55:20Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff satisfies every <done-when> for REQ-006 and the commit-recipe side
of REQ-010, and stays inside the non-goals. The new
`resources/modules/references/commit-recipe.md` states the stage → `git
diff --cached --quiet` skip-if-empty → commit recipe exactly once
(`commit-recipe.md:46-56`), explicitly forbidding the per-caller `git
status --porcelain` variant per DEC-004; parameterises only staging
breadth and title/body (`:8-20`); delegates the trailer via `{% include
"modules/references/identity-sourcing.md" %}` (`:81`) with no inline
restatement; opens with a `git rev-parse --is-inside-work-tree` no-git
short-circuit that skips the commit "without erroring" (`:22-35`,
CHK-013); and passes the message via a single-quoted HEREDOC (`:62-69`).
Serves the maintainer user story ("recipe defined once and included
everywhere"). No callsite wired (correctly deferred to T-003..T-006), no
CLI change, no mode toggle — non-goals honoured. Open Questions are
None and the idempotency-unification resolution matches the SPEC; the
single Changelog row reflects current intent. The undocumented
`Speccy Skill Pack` trailer fallback (`:84-86`) is a reasonable
completion of the delegation, not scope creep — non-blocking.
</review>
<review date="2026-06-11T06:55:41Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
All three task scenarios map to an executable test in `speccy-cli/tests/authoring_commit.rs` that exercises the real module body via the embedded `RESOURCES` bundle (`include_dir!` of `../resources`, so it reflects the working-tree file). Ran `cargo test --test authoring_commit`: 3 passed.

Falsification holds for each: `commit_recipe_states_idempotency_check_exactly_once:51` counts `git diff --cached --quiet` and asserts exactly 1 — fails on the "stated twice" DEC-004 anti-pattern and on a `git status --porcelain` substitution (count would be 0), gating CHK-008's "recipe stated once" over a command token, not curated prose. `commit_recipe_delegates_trailer_via_include:66` asserts the `{% include "modules/references/identity-sourcing.md" %}` directive is present AND the canonical `## Sourcing your recorded identity` heading is absent inline — fails on either dropped include or inline restatement (CHK-008 delegation). `commit_recipe_specifies_no_git_short_circuit:89` anchors on the `git rev-parse --is-inside-work-tree` probe token plus the no-error contract phrasing — fails if the short-circuit is missing (CHK-013 commit-recipe side). All three are structural assertions over stable surfaces, consistent with the cited `persona_snippets.rs`/`skill_body_discovery.rs` precedent, not vacuous prose matches.

Evidence at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-001.md`: roll call covers CHK-008 (demonstrated) and CHK-013 (demonstrated, commit-recipe side; branch-guard side correctly deferred to T-002), each with a specific test cite. No phantom test names — all three named tests appear in the diff. Red phase (module absent → `commit_recipe_body()` fixture panic) matches the implementation; red→green transition is materially different (3 failed → 3 passed); evidence command is scoped per-test, not the workspace hygiene run. No fabrication pattern matched.
</review>
<review date="2026-06-11T06:57:47Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No blocking style violations found.

Reviewed `resources/modules/references/commit-recipe.md` and `speccy-cli/tests/authoring_commit.rs`.

**commit-recipe.md** — prose is well-structured; uses only fictional `SPEC-NNNN` placeholders (never a real Speccy spec id); delegates the trailer via `{% include "modules/references/identity-sourcing.md" %}` matching the exact directive form used by `speccy-work.md:252` and `verdict_return_contract.md:39`; no inline restatement of identity-sourcing prose; trailing newline present (`0x0a` via byte probe).

**authoring_commit.rs** — matches established sibling conventions: `#![allow(clippy::expect_used, reason = "...")]` at file header, centralised `panic_with_message` helper scoped under `#[expect(clippy::panic, reason = "...")]` — identical shape to `skill_body_discovery.rs:48-56`, `persona_snippets.rs:76-82`, and four other siblings. `clippy --workspace --all-targets --all-features -- -D warnings` exits 0. No unused imports or dead code introduced.

**Reuse survey** — present in the implementer block; named symbols verified: `{% include "modules/references/identity-sourcing.md" %}` confirmed at `resources/modules/references/identity-sourcing.md`; include directive confirmed used by `speccy-work.md`, `verdict_return_contract.md`, and three persona bodies. Survey is honest.

**Borderline note (non-blocking):** `authoring_commit.rs:99-103` substring-checks `"not a git repository"` and `"without erroring"` against `commit-recipe.md`. AGENTS.md's explicit prohibition targets `AGENTS.md`, `README.md`, and `SPEC.md` bodies; `commit-recipe.md` is a skills reference module. The `git rev-parse --is-inside-work-tree` token check at line 93 already gates the structural fact; the prose assertion adds fragility for legitimate rewrites but does not violate the project's stated rule by name. Surfaced as a non-blocking aside.
</review>
