---
spec: SPEC-0051
task: T-001
generated_at: 2026-05-27T22:00:00Z
---

<implementer date="2026-05-27T22:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Authored `resources/modules/references/agents-md-speccy-conventions.md` carrying the canonical `## Speccy conventions` body. Opens with the upsert-contract preamble naming `/speccy-init` as the section's manager. Carries five subsections in REQ-001 order: When to use which skill (one-liner per shipped skill — `/speccy-init`, `/speccy-brainstorm`, `/speccy-plan`, `/speccy-amend`, `/speccy-decompose`, `/speccy-work`, `/speccy-review`, `/speccy-vet`, `/speccy-ship`, `/speccy-orchestrate` — with trigger essence lifted from each skill's existing wrapper description); The dev loop (Plan → Tasks → Impl → Review → Ship with the `.speccy/specs/NNNN-slug/journal/T-NNN.md` journal-path pointer); Test hygiene (all five vacuous-test anti-patterns in language-neutral phrasing plus the investigate-flakes rule); Commit hygiene (`Co-Authored-By` trailer + narrow commits); CI gate (suggestion) (one paragraph naming `speccy verify` and multiple CI platforms by name, no vendor wiring shipped). Body contains no language-specific examples (no `is_ok`, `unwrap`, `expect(`, `Result<`), no platform-specific CI artifacts (no `runs-on:`, `jobs:`, `.github/workflows`), and no Speccy-internal references (no `just reeject`, no `resources/modules/`, no `SPEC-0051`). Side fix: SPEC.md authored by `/speccy-plan` was missing the in-tree blank-line-after-close-tag convention between `</scenario>` and `</requirement>` at seven sites; inserted the blank lines so `cargo test --workspace` passes. This is outside T-001's strict scope but required to clear the hygiene gate; flagged here so the reviewer can decide if it belongs elsewhere.
- Undone: T-002 (skill body upsert step + include directive), T-003 (north-star adaptive flow refactor), T-004 (wrapper description updates + reeject), T-005 (hygiene gate run on the cumulative tree). The canonical body is authored but not yet consumed — until T-002 lands the `{% include %}` directive in the skill body, the new module file is dead prose. T-001 deliberately stops there per the task boundary.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0051-init-agents-md-bootstrap/evidence/T-001.md`. Roll call for CHKs under REQ-001:
  - CHK-001 (reviewer audits the new reference module for file existence, upsert-contract preamble, five named subsections in order, and language-agnostic / vendor-agnostic phrasing): judgment-only → no scriptable proof; the reviewer-business / reviewer-style personas judge the prose on the diff. Negative substring checks (no `is_ok`, `unwrap`, `expect(`, `Result<`, `runs-on:`, `jobs:`, `.github/workflows`, `just reeject`, `resources/modules/`, `SPEC-0051`) all returned zero matches at author time and are recorded in the evidence file as a one-shot confirmation, but the per-task scenarios in TASKS.md explicitly call for reviewer-audit framing, not automated grep.
- Discovered issues: SPEC.md generated by `/speccy-plan` failed the in-tree `every_in_tree_spec_md_has_blank_line_after_each_close_tag` convention at seven sites (one blank line missing between `</scenario>` and `</requirement>` at lines 285, 365, 454, 504, 581, 647, 680). Fixed inline so the hygiene gate could pass; the underlying `/speccy-plan` template likely needs a follow-up to emit the blank line automatically, but that is a separate concern outside SPEC-0051's scope.
- Procedural compliance: followed `/speccy-work` recipe — clean tree at entry, flipped state pending → in-progress, ran the four-gate hygiene suite before flipping to in-review, did not modify `.claude/`, `.agents/`, or `.codex/` directly (T-001 touches only `resources/modules/references/`, which is the canonical source). No new dependencies, no Rust source touched, no skill wrapper edits (deferred to T-004).

</implementer>

<review date="2026-05-27T21:43:02Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-001 delivers `resources/modules/references/agents-md-speccy-conventions.md` with the upsert-contract preamble, all five named subsections in REQ-001 order, the ten shipped skills enumerated, the journal path and five-phase dev loop, all five vacuous-test anti-patterns in language-neutral phrasing plus the no-flake-retry rule, the `Co-Authored-By` + narrow-commits expectation, and the platform-agnostic CI-gate paragraph naming `speccy verify` and five CI services in prose without shipping any vendor config. The negative criteria hold: no `is_ok`/`unwrap`/`expect(`/`Result<` strings, no `runs-on:`/`jobs:`/`.github/workflows` artifacts, no Speccy-internal references (`just reeject`, `resources/modules/`, `SPEC-0051`) that would embarrass downstream users. The journal-pointer prose paraphrases `<implementer>` / `<review>` / `<blockers>` as "implementer notes, reviewer verdicts, and blocker directives" — acceptable per the SPEC Notes' explicit "content shape, not exact wording" allowance, and arguably better for downstream consumers reading their own `AGENTS.md`. Forward-referencing prose in the `/speccy-init` skill entry ("seeding both the product north star and this conventions section") describes the post-SPEC-0051 cumulative end state, which is intended given REQ-002/REQ-003/REQ-006 land the wiring. Non-goals respected: no marker fencing, no vendor CI shipped, no Rust CLI touched. The seven blank-line side-fix to SPEC.md is transparently flagged and is editorial mechanics, not requirement drift. Goals and user stories served: a stable canonical heading carrying when-to-use-which-skill, dev-loop, test hygiene, commit hygiene, and a CI-gate suggestion is exactly what the SPEC's "agent quoting rules without grepping Speccy's own repo" user story names.
</review>

<review date="2026-05-27T21:43:02Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-001 is a prose-only authoring task; the SPEC and the task's `<task-scenarios>` block frame CHK-001 as reviewer-audit, and AGENTS.md plus the SPEC's Notes section explicitly forbid substring-grepping curated prose as a test (anti-pattern #1 in the no-vacuous-tests rule). The implementer's `judgment-only` classification in `.speccy/specs/0051-init-agents-md-bootstrap/evidence/T-001.md` is the correct label per the evidence-loading contract — automated assertions over the canonical body would themselves be vacuous and would gate editorial decisions, not behavior. The reference module at `resources/modules/references/agents-md-speccy-conventions.md` carries the upsert-contract preamble, the five subsections in REQ-001 order, all ten shipped skills in the when-to-use list, the journal file pointer at `.speccy/specs/NNNN-slug/journal/T-NNN.md`, the five vacuous-test anti-patterns in language-neutral phrasing followed by the no-flake-retry rule, the Co-Authored-By + narrow-commits commit hygiene, and the platform-agnostic CI-gate paragraph naming speccy verify across GitHub Actions/GitLab/Jenkins/CircleCI/Buildkite without shipping vendor YAML. No project tests are required for this slice and none are missing; the four-gate hygiene suite ran clean (per the implementer block). No fabrication signals in the evidence file. Verdict from the tests-persona lens: nothing to block on.
</review>

<review date="2026-05-27T21:43:02Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
Pure documentation prose at `resources/modules/references/agents-md-speccy-conventions.md` with no executable surface, no secrets, no auth boundaries, no crypto primitives, and no new dependencies. The body contains no Jinja directives that re-evaluate at render time, no shell commands beyond the project's own `speccy verify`, no credentials or env-var references, and no internal-host or internal-identifier leaks (negative substring checks for `SPEC-0051`, `resources/modules/`, `just reeject` all confirm absent). The "CI gate (suggestion)" subsection correctly frames `speccy verify` as informational rather than overstating it as a security control. No security findings.
</review>

<review date="2026-05-27T21:43:02Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
`resources/modules/references/agents-md-speccy-conventions.md` passes all style checks. Filename is lowercase kebab-case consistent with sibling modules. No language-specific idioms, no platform CI wiring, no upstream-maintainer-only references. Five subsections appear in spec-mandated order with consistent `###` heading levels. The opening `##`-level heading (rather than `#`) is intentional — the file is designed to be inserted verbatim as a section body, not read as a standalone document. The side-fix to SPEC.md (inserting blank lines between `</scenario>` and `</requirement>` at seven sites) is pure whitespace hygiene required by the in-tree convention and introduces no style concerns. No dead code, unused imports, or duplicate helpers.
</review>
