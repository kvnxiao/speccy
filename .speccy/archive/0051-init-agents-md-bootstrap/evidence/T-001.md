# Evidence: SPEC-0051 T-001

## CHK-001 — canonical `## Speccy conventions` reference module

**Classification:** judgment-only (reviewer audits prose on the diff).

**Artifact:** `resources/modules/references/agents-md-speccy-conventions.md`

**Shape confirmation at author time:**

- Opens with the upsert-contract preamble naming `/speccy-init` and
  warning that in-section edits are overwritten on re-run.
- Five subsections in REQ-001 order: "When to use which skill",
  "The dev loop", "Test hygiene", "Commit hygiene",
  "CI gate (suggestion)".
- When-to-use enumerates all ten shipped skills required by REQ-001
  done-when: `/speccy-init`, `/speccy-brainstorm`, `/speccy-plan`,
  `/speccy-amend`, `/speccy-decompose`, `/speccy-work`,
  `/speccy-review`, `/speccy-vet`, `/speccy-ship`,
  `/speccy-orchestrate`.
- Dev loop names the five phases in order and points at
  `.speccy/specs/NNNN-slug/journal/T-NNN.md` where
  `<implementer>`, `<review>`, and `<blockers>` blocks live.
- Test hygiene enumerates all five vacuous-test anti-patterns in
  language-agnostic phrasing and closes with the
  investigate-flakes-don't-retry rule.
- Commit hygiene names the `Co-Authored-By` trailer and the
  narrow-commits preference.
- CI gate names `speccy verify` and multiple CI platforms
  (GitHub Actions, GitLab CI, Jenkins, CircleCI, Buildkite) in
  prose without shipping vendor wiring.

**One-shot negative substring confirmations (author-time only — the
per-task scenario is reviewer-audit framing):**

- No `is_ok`, `unwrap`, `expect(`, `Result<` (language-specific
  anti-pattern names): 0 matches.
- No `runs-on:`, `jobs:`, `.github/workflows` (vendor CI wiring):
  0 matches.
- No `just reeject`, `resources/modules/`, `SPEC-0051`
  (upstream-maintainer / Speccy-internal references): 0 matches.

The reviewer-business and reviewer-style personas confirm the
final shape on the diff.
