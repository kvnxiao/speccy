---
spec: SPEC-0044
task: T-001
generated_at: 2026-05-24T00:00:00Z
---

<implementer date="2026-05-24T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed: removed the reviewer-persona carve-out from `speccy
  init --force`. Deleted `is_host_native_reviewer_file` and its
  caller branch in `append_host_pack_items`
  (`speccy-cli/src/init.rs`); dropped the
  `speccy_core::personas::ALL as PERSONAS_ALL` import; rewrote the
  module docstring, the `Action` enum docstring, the
  `Action::Unchanged` variant docstring, the `build_plan` comment,
  the `execute_plan` comment, and the `InitArgs::force` docstring
  to describe one uniform Create / Unchanged / Conflict rule.
  Updated `docs/ARCHITECTURE.md` in the three locations that
  mentioned `Skip-on-exists`. Renamed and inverted the two
  preservation tests in `speccy-cli/tests/init.rs` to
  `t002_claude_reviewer_agent_files_overwrite_user_edits_under_force`
  and `t002_codex_reviewer_agent_files_overwrite_user_edits_under_force`
  (now assert sentinel is gone, file matches bundle, plan summary
  shows `(!) overwritten`). Renamed
  `t002_claude_reviewer_agent_files_recreate_when_deleted_under_force`
  to `t002_claude_reviewer_agent_files_recreate_when_deleted` and
  dropped the `Skip-on-exists` framing. Deleted
  `t002_claude_init_force_plan_summary_marks_reviewer_agents_and_skills_unchanged`
  (its remaining assertion — reviewer paths appear in plan output —
  is redundant with the rewritten CHK-001 / CHK-002 tests, and the
  inversion check no longer holds; deletion was the SPEC-permitted
  smaller-file choice).

- Undone: nothing within scope. The task is complete.

- Hygiene checks:
  - `cargo test --workspace` → all green
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean
  - `cargo +nightly fmt --all --check` → clean
  - `cargo deny check` → advisories ok, bans ok, licenses ok, sources ok

- Evidence: paper trail at
  `.speccy/specs/0044-force-overwrites-all-shipped-files/evidence/T-001.md`.
  Roll call for CHKs under REQ-001 and REQ-002:
  - CHK-001 (Claude reviewer file overwritten under `--force`):
    demonstrated → evidence Scenario 1 covers the red/green pair
    via the renamed test
    `t002_claude_reviewer_agent_files_overwrite_user_edits_under_force`
    in `speccy-cli/tests/init.rs`.
  - CHK-002 (Codex reviewer file overwritten under `--force`):
    demonstrated → evidence Scenario 2 covers the red/green pair
    via the renamed test
    `t002_codex_reviewer_agent_files_overwrite_user_edits_under_force`.
    Test uses `reviewer-business.toml` while CHK-002 names
    `reviewer-security.toml`; both flow through the deleted
    carve-out branch, so behaviour is proven identically.
  - CHK-003 (no `is_host_native_reviewer_file` or `Skip-on-exists`
    in `init.rs`; no unused `PERSONAS_ALL` import; clippy clean):
    hygiene → covered by the standard hygiene gate above; grep over
    `speccy-cli/src/init.rs` returns zero hits for each forbidden
    substring.
  - CHK-004 (no `Skip-on-exists` in `docs/ARCHITECTURE.md`; no
    surviving prose claims edits survive `--force`): hygiene →
    grep over `docs/ARCHITECTURE.md` returns zero hits for
    `Skip-on-exists`; the three updated passages now describe the
    uniform rule.
  - CHK-005 (no `Skip-on-exists` in `init.rs`; `InitArgs::force`
    and `Action` docstrings describe the uniform rule):
    judgment-only → no scriptable assertion captures the
    docstring's prose intent; reviewer-style judges the rewritten
    docstrings on the diff.
  - CHK-006 (any surviving `t002_*` test referencing reviewer
    paths asserts overwrite-under-force; `cargo test --workspace`
    exits 0): hygiene → the two rewritten tests assert overwrite,
    not preservation; the workspace test gate above is green.

- Discovered issues: none. The carve-out was self-contained in
  `init.rs`; no consumers outside that file referenced
  `is_host_native_reviewer_file`. The remaining `PERSONAS_ALL`
  uses in `speccy-core` and other tests are unaffected.

- Procedural compliance: followed `speccy-work` end-to-end. No
  shipped-skill drift was encountered. The plan-summary test
  deletion was chosen over rewrite per the SPEC's "pick whichever
  keeps the file smaller" directive in T-001's task body.

</implementer>

<review date="2026-05-24T23:39:57Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-001 delivers REQ-001 and REQ-002 as the SPEC promises: the `is_host_native_reviewer_file` helper and its branch in `append_host_pack_items` are gone, every rendered host-pack file now flows through `classify_content` uniformly, `docs/ARCHITECTURE.md` no longer claims persona-body edits survive `--force` in any of the three named passages, and the two preservation tests are renamed and inverted to assert overwrite-under-force with the `(!) overwritten` plan-summary check.

Goals/non-goals/user-stories all map cleanly to the diff:
- Goal 1-2 (overwrite + plan summary) — `speccy-cli/tests/init.rs:627-635` (Claude) and `:702-717` (Codex) assert sentinel removal, byte-identical match, and `(!) overwritten` plan line.
- Goal 3 (helper + branch deleted) — `speccy-cli/src/init.rs` diff shows both removed; `PERSONAS_ALL` import also gone.
- Goal 4 (ARCHITECTURE.md updates) — all three named regions (`~323`, `~1684`, `~1889`) rewritten to describe the uniform rule.
- Goal 5 (docstrings) — `InitArgs::force`, `Action` enum doc, `Action::Unchanged` variant doc, `build_plan` comment, `execute_plan` comment all rewritten to drop the carve-out language.
- Non-goals respected: no new flag, no `.bak` mechanism, no rendering-pipeline change, SPEC-0027 REQ-001/003/004 untouched, `Action` variants unchanged.

One minor disclosure to note (not blocking): CHK-002 names `.codex/agents/reviewer-security.toml`, but the renamed test exercises `.codex/agents/reviewer-business.toml`. The implementer flagged this in the journal and is correct on the merits — the deleted carve-out branch iterated over every persona uniformly, so any one Codex reviewer file proves the behavior for all six. This is an equivalence-of-effect rather than a literal scenario match, and the SPEC's permission to delete the plan-summary test ("pick whichever keeps the file smaller") signals that uniform-rule coverage trumps per-persona coverage at the test layer.

Behavior 4 in REQ-001 ("byte-identical reviewer files show `unchanged`") no longer has a dedicated assertion after the plan-summary test deletion, but `classify_content` returns `Unchanged` on byte-equal content by construction, and the SPEC's done-when item for that test explicitly permitted deletion. Acceptable.

No silent resolution of open questions, no scope sneak-in, no dropped error paths. The dogfood-driven rationale in the Summary aligns with the diff: one uniform rule replaces a partial-meaning carve-out.
</review>

<review date="2026-05-24T23:39:57Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
Tests adequately drive CHK-001, CHK-002, and CHK-006 with strong assertions; CHK-003/CHK-004/CHK-005 hygiene/judgment labels are valid.

CHK-001 coverage in `speccy-cli/tests/init.rs:599-642` (`t002_claude_reviewer_agent_files_overwrite_user_edits_under_force`) drives the full Given/When/Then literally: init, append sentinel `# sentinel-edit-12345`, re-init with `--force`, then assert (a) sentinel substring absent, (b) `after == initial` byte-for-byte, and (c) stdout contains `(!) overwritten` for `.claude/agents/reviewer-business.md`. The `assert_eq!(after, initial)` guard is particularly strong — a "trim only the sentinel" implementation would pass the negative check but fail the byte-equality, and a no-op would fail both. Cargo test run confirms all three t002 tests pass green.

CHK-002 coverage in `speccy-cli/tests/init.rs:676-733` (`t002_codex_reviewer_agent_files_overwrite_user_edits_under_force`) exercises `.codex/agents/reviewer-business.toml` while CHK-002's scenario literally names `.codex/agents/reviewer-security.toml`. The implementer's evidence file at `.speccy/specs/0044-force-overwrites-all-shipped-files/evidence/T-001.md:39-51` calls out the discrepancy and argues both files flow through the same deleted branch in `append_host_pack_items`. Verified independently: the removed `is_host_native_reviewer_file` helper iterated `PERSONAS_ALL` with no per-persona logic, and `render_host_pack` in `speccy-cli/src/render.rs:103-158` treats all reviewer template files identically (alphabetic sort, no special-casing). The post-removal classify-and-overwrite code path is shape-identical across all six Codex personas, so the literal path divergence is cosmetic. Non-blocking, but flagging for future amendment: aligning the test path to CHK-002's literal `reviewer-security.toml` would close the small literal-vs-behavioural gap at zero cost.

Carve-out removal verified in `speccy-cli/src/init.rs` (no remaining hits for `is_host_native_reviewer_file`, `PERSONAS_ALL`, or `Skip-on-exists`) and in `docs/ARCHITECTURE.md` (no hits for `Skip-on-exists`); CHK-003 and CHK-004 hygiene checks satisfied. CHK-005's judgment-only label is plausible — docstring prose semantics resist deterministic assertion, and the rewritten docstrings at `init.rs:67-76` and `init.rs:80-95` describe the uniform rule without carve-out language. Plan-summary test deletion (per SPEC's "pick whichever keeps the file smaller" allowance) is consistent with CHK-006's "or the test is absent entirely" clause; no surviving t002 reviewer-path test asserts preservation. Evidence file shape is plausible runner output for a `cargo test ... <filter>` invocation; no red/green byte-identical signal or fabricated test-name signal.
</review>

<review date="2026-05-24T23:39:57Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security-relevant changes. The diff removes a special-case branch in a local filesystem write path (`speccy-cli/src/init.rs:232-244` pre-removal), replacing it with the uniform `classify_content` call already used for all other shipped files. Reviewer persona files written by `render_host_pack` use statically embedded bundle paths that flow through `destination_rel_path`, which requires an `agents/` prefix and `.tmpl` suffix; no user-controlled input feeds the destination path. No credentials, tokens, or secrets are involved. The `--force` flag is explicit user authorization and now applies uniformly as intended. No new dependencies.
</review>

<review date="2026-05-24T23:39:57Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style obligations are met. The on-disk `speccy-cli/src/init.rs` has no `is_host_native_reviewer_file`, no `PERSONAS_ALL` import, and no `Skip-on-exists` occurrences. Docstrings (`InitArgs::force`, the `Action` enum, `Action::Unchanged` variant, `build_plan` comment, `execute_plan` comment) uniformly describe the Create / Unchanged / Conflict rule without carve-out language. `docs/ARCHITECTURE.md` is clean of `Skip-on-exists`. Test names and assertion direction (`t002_claude_reviewer_agent_files_overwrite_user_edits_under_force`, `t002_codex_reviewer_agent_files_overwrite_user_edits_under_force`) match the renamed contract. The single surviving `Skip-on-exists` token in `speccy-cli/tests/init.rs:596` is a prose comment saying the old carve-out is removed — not a live term; it lives in `tests/init.rs`, which CHK-003 and CHK-005 do not scope. Import ordering is consistent with the project's grouped-then-alphabetical style; no dead imports remain.
</review>
