---
spec: SPEC-0060
task: T-004
generated_at: 2026-06-12T05:44:10Z
---

<implementer date="2026-06-12T05:44:10Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Docs-only task; no new top-level symbols introduced. Full
  area-map of `docs/ARCHITECTURE.md`: the `speccy context` envelope table
  (~L188-228), the size-invariant prose, and the two entry-flow narrative
  passages (implementer ~L528, reviewer ~L583). Reused (as the field-for-field
  source of truth) the shipped `BundleJournal` shape in
  `speccy-cli/src/context_output.rs` (`exists`, optional `spec`/`task`/
  `generated_at`, `blocks: Vec<JsonJournalBlock>`, `prior_rounds:
  Vec<JsonJournalBlockAttrs>`) and `JsonJournalBlockAttrs` in
  `speccy-cli/src/journal_show_output.rs` (block/date/round + optional
  model/persona/verdict, no body). No code reused/extended — prose only.
- Completed: Reworded the journal bullet in the `speccy context` envelope table
  to describe `blocks` as the latest round's blocks with full bodies and
  `prior_rounds` as the attributes-only index (block/date/round +
  model/persona/verdict, no body), plus the `journal show --round N` drill-down
  and the explicit `exists:false` absent-journal marker. Added a within-task
  clause to the size-invariant prose noting prior rounds add only bounded index
  entries, never bodies. Made both entry-flow narrative passages
  (implementer + reviewer) consistent: each now says "latest journal round in
  full plus an attributes-only index of prior rounds" instead of "the full
  journal" / "the journal".
- Undone: Nothing in scope deferred.
- Hygiene checks: cargo test --workspace exit=0; cargo clippy --workspace
  --all-targets --all-features -- -D warnings exit=0; cargo +nightly fmt --all
  --check exit=0; cargo deny check exit=0.
- Evidence: no evidence file — REQ-004 is review-verified documentation with no
  scriptable proof. Roll call for CHKs under REQ-004:
  - CHK-009 (ARCHITECTURE.md journal-section docs match the emitted JSON
    field-for-field; no full-journal claim remains): judgment-only →
    no scriptable proof; reviewer-business / reviewer-docs judges the prose
    against the shipped `BundleJournal` shape on the diff. Cross-checked by hand
    against `context_output.rs` (`blocks`/`prior_rounds`/`exists`) and
    `journal_show_output.rs` (`JsonJournalBlockAttrs` omits `body`); a repo-wide
    grep for "full journal"/"all rounds"/"across rounds"/"inlined" found no
    remaining bundle-inlines-all-rounds claim (the one residual "across rounds"
    hit is about `round` derivation, unrelated).
- Discovered issues: none.
- Procedural compliance: docs-only change to a checked-in design doc, not an
  ejected skill-pack file — no `resources/` source edit or `just reeject`
  needed. No friction with shipped skills.
</implementer>
<review date="2026-06-12T05:45:24Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Docs-only change to `docs/ARCHITECTURE.md`; no code, auth, input-validation,
crypto, secret-handling, or dependency surface touched — no security-relevant
attack surface in scope. The change is security-favorable: it documents that
prior-round journal bodies are no longer inlined in the `speccy context`
bundle, only an attributes-only `prior_rounds` index (block/date/round +
model/persona/verdict, no body) is emitted, narrowing what prose leaves the
journal in a single read. The documented index shape matches
`JsonJournalBlockAttrs` (`speccy-cli/src/journal_show_output.rs:157`), whose
`body` is verifiably absent from serialization, so the "no body" claim is
accurate. No sensitive-data exposure, logging, or error-disclosure regression.
</review>
<review date="2026-06-12T05:45:30Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The diff is docs-only: prose edits to `docs/ARCHITECTURE.md`
(the `speccy context` envelope table, the size-invariant paragraph,
and the two implementer/reviewer entry-flow narrative passages). No
executable code, control flow, error handling, boundary logic,
concurrency, or resource management is touched, so there is no
correctness surface in this lane to defect on. Confirmed the
documented field names (`blocks`, `prior_rounds`, `exists`) match the
shipped `BundleJournal` struct in `speccy-cli/src/context_output.rs`;
factual fidelity of the prose is the business/docs lane's call.
</review>
<review date="2026-06-12T05:45:51Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
REQ-004 all three done-when clauses satisfied against the shipped shape.
(1) Envelope journal bullet (`docs/ARCHITECTURE.md:202-216`) describes
`journal.blocks` as the latest round's blocks with full bodies and
`journal.prior_rounds` as the attributes-only index (block/date/round +
model/persona/verdict, no body) — field-for-field matching `BundleJournal`
(`speccy-cli/src/context_output.rs:175-201`) and `JsonJournalBlockAttrs`
(`speccy-cli/src/journal_show_output.rs:156-173`). (2) No surviving
"full journal"/"all rounds"/"inlines all rounds" claim: both entry-flow
narrative passages (`docs/ARCHITECTURE.md:542-549` implementer,
`598-604` reviewer) now read "latest journal round in full plus an
attributes-only index of prior rounds"; the two residual "across rounds"/
"inlined" grep hits (L1933 round-derivation, L3066 spawn-prompt) are
unrelated. (3) Size-invariant prose (`L234-237`) notes within-task prior
rounds add only bounded `prior_rounds` entries, never bodies. The
absent-journal marker (`exists:false`, empty blocks/prior_rounds) and the
`journal show --round N` drill-down are both documented and match DEC-003/
the code. No non-goal violated (no schema_version bump, no field rename);
docs-only change, no scope creep; no open question silently resolved.
</review>
<review date="2026-06-12T05:46:04Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Docs-only REQ-004; sole scenario CHK-009 is `(Review-verified.)` and the
journal roll call labels it `judgment-only` — a plausible label, since
"no full-journal claim remains" over hand-curated prose cannot be a
deterministic substring test (AGENTS.md forbids substring-matching curated
prose) and the spec author annotated the scenario as review-verified. No
evidence file is expected or fabricated. Verified the prose against the
shipped shape field-for-field: `docs/ARCHITECTURE.md:202-216` documents
`journal.blocks` as latest-round entries with full bodies and
`journal.prior_rounds` as an attributes-only index (block/date/round +
optional model/persona/verdict, no body), matching `JsonJournalBlock`
(with `body`) and `JsonJournalBlockAttrs` (no `body` key) in
`speccy-cli/src/journal_show_output.rs:125-173`, and `BundleJournal` in
`speccy-cli/src/context_output.rs:176-201` (incl. the `exists:false` empty
marker). Both entry-flow passages (`docs/ARCHITECTURE.md:542-549`,
`599-606`) now say "latest journal round in full plus an attributes-only
index of prior rounds." Size-invariant prose at `234-237` notes prior
rounds add only bounded index entries, never bodies. A repo-wide grep for
full-journal phrasings leaves only the unrelated `round`-derivation line at
`1933` ("across rounds"). No remaining bundle-inlines-all-rounds claim.
</review>
<review date="2026-06-12T05:46:15Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Documentation-only diff passes style review.

The journal bullet in the `speccy context` envelope table (docs/ARCHITECTURE.md ~L202-216) accurately describes `blocks` as the latest round's entries with full bodies and `prior_rounds` as the attributes-only index (block/date/round + optional model/persona/verdict, no body), matching `BundleJournal` and `JsonJournalBlockAttrs` in `speccy-cli/src/context_output.rs` and `speccy-cli/src/journal_show_output.rs` field-for-field. The `exists:false` empty marker is correctly documented with empty `blocks`/`prior_rounds`. The `speccy journal show <selector> --round N` drill-down is noted. The size-invariant prose (~L234-238) has the correct within-task clause. Both entry-flow narrative passages (~L544-546, ~L602-604) now read "latest journal round in full plus an attributes-only index of prior rounds" — no remaining "full journal"/"all rounds"/"across rounds" claim found (grep confirmed). Reuse survey present and credible (docs-only task; no new symbols). No naming drift, no dead prose, no suppression annotations, no invented abstraction.
</review>
