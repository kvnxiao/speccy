---
spec: SPEC-0065
task: T-007
generated_at: 2026-06-14T04:19:22Z
---

<implementer date="2026-06-14T04:19:22Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area-mapped the two producer skill bodies (`resources/modules/skills/speccy-plan.md`, `speccy-amend.md`) named by the task. Reuse-as-is: the plan body's existence-guard idiom `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` (plan.md:132) — copied verbatim into the amend staging step rather than inventing a parallel guard. Extend: the amend body's existing narrow-`git add <paths>` staging bullet (amend.md step 7) — widened by the guarded backlog path. Write-fresh: none; no new top-level symbol introduced (prose-only skill-body edit, no Rust).
- Completed: Edited `resources/modules/skills/speccy-amend.md` step 7 in two places — the prose intro now names `.speccy/BACKLOG.md` (dirty) as part of the commit's coverage with the brainstorm-framed-amendment rationale, and the staging-breadth bullet now adds the existence-guarded backlog stage with the verbatim `test -f` idiom mirroring the plan path. Ran `just reeject`; the guarded stage ejected into both `.claude/skills/speccy-amend/SKILL.md` and `.agents/skills/speccy-amend/SKILL.md` (parity confirmed at lines 167/291/299 in each).
- Undone: Nothing deferred. Both done-when bullets satisfied.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: REQ-008 covered. CHK-010 (reviewer reads ejected amend staging step, sees existence-guarded `.speccy/BACKLOG.md` stage tolerating absence) — demonstrated by the ejected bodies at `.claude/skills/speccy-amend/SKILL.md:291,299` and `.agents/skills/speccy-amend/SKILL.md:291,299` carrying the guarded `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` stage; adequacy of guard placement is the persona-review judgment the scenario reserves. Reejection-parity done-when — demonstrated: reeject report showed exactly the amend SKILL.md overwritten under both hosts, all other files unchanged.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work recipe — context bundle, clean-tree gate (empty, first-attempt), reuse survey, edit-source-not-ejected (`resources/` then `just reeject`), hygiene gate before in-review flip. No friction with shipped instructions; no source-module fixes needed.
</implementer>
<review date="2026-06-14T04:20:38Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The only logic in this prose-only skill edit is the embedded staging idiom `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` (resources/modules/skills/speccy-amend.md:138, ejected at .claude/ and .agents/ speccy-amend/SKILL.md:299). Control flow is correct on both branches: file present -> `test -f` succeeds -> `git add` runs (no-op when unchanged, stages when dirty or first-append untracked); file absent -> short-circuits, `git add` never runs, stage does not fail. This satisfies REQ-008's two behavior scenarios exactly. The edit is additive — the existing SPEC.md / TASKS.md / journal-blocker staging breadth is unchanged, no condition inverted, no path made to fail on an absent file. Ejected `.claude/` and `.agents/` bodies are byte-identical in the staging block (parity confirmed). No correctness defects.
</review>
<review date="2026-06-14T04:20:51Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. The added staging instruction is a fixed-literal
`test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` with no untrusted
interpolation, so no command-injection vector. The change preserves the narrow
`git add <paths>` posture and the explicit "Do not use `git add -A` or `git
add .`" guard — the one security-adjacent property (avoiding over-staging of
secrets/unrelated files) is unweakened; it widens by exactly one fixed path.
`.speccy/BACKLOG.md` is a user-owned, git-tracked convention file by design, so
committing it leaks nothing not already intended for the repo. Existence guard
correctly tolerates absence and catches a first-append untracked file. Parity
verified across `resources/modules/skills/speccy-amend.md` and both ejected
packs (`.claude/`/`.agents/` SKILL.md).
</review>
<review date="2026-06-14T04:21:13Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
PASS. REQ-008 both done-when bullets are satisfied. The amend staging-breadth bullet (`resources/modules/skills/speccy-amend.md:132-140`) now stages `.speccy/BACKLOG.md` alongside SPEC.md / TASKS.md / journal blocker files under the verbatim plan-path guard `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md`, which tolerates an absent file (no-op stage) and catches a first-append untracked file `git diff` would miss — covering both <behavior> branches (dirty-backlog committed; absent-backlog tolerated). Reejection parity holds: the added block is byte-identical in `.claude/skills/speccy-amend/SKILL.md:287-303` and `.agents/skills/speccy-amend/SKILL.md:287-303`.

Non-goals respected: no `speccy backlog` subcommand, no CLI/verify participation, no review/vet producer wiring. Critically, the edit keeps amend a *committer* of an inherited entry, not an *originating producer* — no append instruction was introduced (grep of the amend body shows BACKLOG.md only in the commit/staging step), exactly honoring DEC-002's appender-set boundary and REQ-008's "Amend stays a committer ... not an originating producer." The one descriptive prose addition (amend.md:109-111) is coverage narration of the reconcile commit, not scope creep. Generic placeholders only; no artifact-ID provenance leaked into the (non-reference) amend body.
</review>
<review date="2026-06-14T04:21:45Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The diff adds the existence-guarded `.speccy/BACKLOG.md` stage to `resources/modules/skills/speccy-amend.md` step 7 and rejects cleanly through `just reeject` into both `.claude/skills/speccy-amend/SKILL.md` and `.agents/skills/speccy-amend/SKILL.md` at identical line positions (parity confirmed by diff of BACKLOG-matching lines: identical, no output).

Reuse survey: implementer named `plan.md:132` as the source of the `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` idiom; confirmed that line exists and the amend body uses the verbatim form. No reinvention.

No-provenance rule: `grep` for real SPEC/REQ/CHK/DEC IDs over the three changed files returned empty. Generic placeholders (`SPEC-NNNN`, `T-NNN`) only.

No dead imports, no new suppressions, no false complexity. `cargo test --test resource_prose_hygiene` passes (5/5). Style matches surrounding step prose — intro summary at step 7 header, guard code block with inline rationale, consistent with plan.md's parallel section.
</review>
<review date="2026-06-14T04:21:57Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. T-007 is a prose-only skill-body edit and the spec design (DEC-001, non-goals) deliberately forbids any CLI/lint/verify/test participation for the backlog, so no executable project test exists or should — correct, not a gap. The single covered CHK-010 is explicitly authored as "a persona-review judgment" (reviewer reads the ejected staging step), an inherently judgment-by-reading scenario with no deterministic command to drive it.

The Evidence roll-call names CHK-010 (labeled demonstrated) and the reejection-parity done-when, both via concrete file:line artifact citations rather than any fabricated runner output — no red/green fabrication smell, no test name absent from the diff, and the full-suite `cargo test` claim is scoped as hygiene, not as the CHK demonstration. I independently verified the cited artifacts: the guard `test -f .speccy/BACKLOG.md && git add .speccy/BACKLOG.md` lands in source (`resources/modules/skills/speccy-amend.md:140`) and ejects byte-identically into both `.claude/skills/speccy-amend/SKILL.md:299` and `.agents/skills/speccy-amend/SKILL.md:299` (parity holds; speccy-amend has no Codex wrapper, so two-host parity is the correct scope). The guard satisfies both REQ-008 `<behavior>` branches — present-and-dirty stages (including a first-append untracked file the guard catches over a bare `git diff`), absent short-circuits without failing the stage — and the narrow-add breadth is otherwise unchanged. Falsifying by reasoning: a wrong implementation here would be a missing/incorrect guard in the body, which a body-reading reviewer would catch directly; the body unambiguously carries the correct idiom.
</review>
