---
spec: SPEC-0065
task: T-004
generated_at: 2026-06-14T03:23:27Z
---

<implementer date="2026-06-14T03:23:27Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Reuse-as-is — the `backlog-ledger.md` reference module (T-001) carrying the file header, four-field shape, provenance convention, and promotion-by-deletion discipline; pulled in via `{% include %}` exactly as `speccy-plan.md` and `speccy-brainstorm.md` already do. Extend — step 3 (ship-time retro) of `resources/modules/phases/speccy-ship.md`, where the `memory-ledger.md` include already lives; added one mirror paragraph plus the adjacent backlog include. Write-fresh — none; searched `resources/modules/skills/speccy-plan.md` and `speccy-brainstorm.md` for the existing append-step wording and include pattern and mirrored it (no new symbol introduced; this is a resource-prose edit).
- Completed: Edited the ship PHASE body (not a skill body) to add a per-item "its own future SPEC, or just a limitation of this one?" judgment over the REPORT "Deferred / known limitations" entries, mirroring ONLY the future-spec-worthy subset into `.speccy/BACKLOG.md` as four-field entries with `SPEC-NNNN, ship` provenance and self-creating header; local limitations stay in REPORT.md only. Added `{% include "modules/references/backlog-ledger.md" %}` adjacent to the existing `memory-ledger.md` include. Ran `just reeject`; both `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml` regenerated with the new content.
- Undone: Nothing in scope. T-005 (REQ-007, bootstrap conventions line) and T-006 (REQ-002) remain pending, owned by their own tasks.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: REQ-001 — CHK-001 demonstrated: the ship phase now includes `backlog-ledger.md` and the four-field shape ejects into both host packs (grep confirmed parity, no orphaned reference); CHK-002 judgment-only: clarity/parallelism with `memory-ledger.md` is a persona-review judgment. REQ-005 — CHK-007 judgment-only: the deferred-section gating on the per-item "own future spec?" judgment and exclusion of local limitations is a persona-review judgment over the ejected ship body (now present in the prose).
- Discovered issues: None.
- Procedural compliance: Edited the source module under `resources/modules/phases/`, never the ejected files; ran `just reeject` to regenerate both host packs. State transitions via `speccy task transition` only. No source-module instruction friction encountered.
</implementer>
<review date="2026-06-14T03:24:49Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security-relevant surface in this slice. The change is prose wiring the
backlog-mirror step into the ship phase body
(`resources/modules/phases/speccy-ship.md`) plus its ejected twins
(`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`). Per
DEC-001 the CLI never reads, parses, lints, or verify-gates `.speccy/BACKLOG.md`,
so no injection, deserialization, or parser surface is introduced. The only
write is an agent appending four-field markdown entries to a fixed literal path
`.speccy/BACKLOG.md` — no path-traversal vector, no dynamic path construction.
No secret handling: the four fields (Title / What & why / Deferred-because /
Provenance) describe future-spec candidates, and Provenance carries only spec
ids and phase names. The file is git-tracked and user-owned like `MEMORY.md`,
and nothing in the authored prose directs capture of credentials, tokens, logs,
or sensitive runtime data. Ejected output is verbatim-parity with the reference
module. No auth boundaries, rate-limiting, crypto, or TOCTOU considerations
apply to a documentation-prose change.
</review>
<review date="2026-06-14T03:25:30Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defect found. This is a prose-only edit to the ship phase body with no executable control flow. Verified: the new `{% include "modules/references/backlog-ledger.md" %}` (resources/modules/phases/speccy-ship.md:129) resolves — the reference exists and its four-field shape (Title / What & why / Deferred-because / Provenance) ejects with parity into both `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml`. The mirror step (lines 116-127) references the REPORT "Deferred / known limitations" entries from step 2, which step 2 (lines 41-46) genuinely writes via `<coverage result="deferred">` rows and out-of-scope items, so the cross-step reference is accurate. The gating logic — per-item "own future SPEC vs. local limitation" judgment, mirror only the future-spec subset, local limitations stay in REPORT.md — is internally consistent and self-creates the file header when absent.

Noted but below the reporting bar: step 3's new prose states the backlog append "lands in the same ship commit (step 6)", yet step 6's enumerated commit file list (line 150) names `.speccy/MEMORY.md` but omits `.speccy/BACKLOG.md`, breaking the parallel with the adjacent memory-ledger callout. The step-3 prose does explicitly direct committing it, so this is a consistency/completeness gap rather than a ≥80-confidence control-flow defect, and the commit-step wording also leans into style/business lanes I do not own.
</review>
<review date="2026-06-14T03:25:33Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff satisfies both covered requirements. REQ-005: the ship phase body (`resources/modules/phases/speccy-ship.md:116-127`) instructs the per-item "its own future SPEC, or just a limitation of this one?" judgment over the REPORT "Deferred / known limitations" entries, mirrors ONLY the future-spec-worthy subset into `.speccy/BACKLOG.md` as four-field entries with `SPEC-NNNN, ship` provenance, and leaves local limitations (bug-level caveats, small follow-ups) in REPORT.md only — covering both done-when bullets and both Given/When/Then behavior cases. REQ-001: `{% include "modules/references/backlog-ledger.md" %}` lands adjacent to the existing `memory-ledger.md` include (line 129), and both ejected packs (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`) carry the four-field entry shape, so resource-to-ejected parity holds with no orphaned reference. The mirror prose references real REPORT surfaces (`<coverage result="deferred">` rows and out-of-scope items from step 2), not a phantom section. Non-goals respected: no CLI subcommand/grammar/lint, no review- or vet-phase producer wiring (only the authorized ship producer touched), and the file self-creation is skill-side, consistent with the convention-only DEC-001 framing. No open questions silently resolved; changelog carries only the initial entry, so no stale intent.
</review>
<review date="2026-06-14T03:25:37Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests persona pass. Prose-only slice; no executable code path exists by design (DEC-001: CLI never reads the backlog), so the testable surface is resource-to-ejected parity, and the spec scopes the gating-judgment scenarios to persona review.

Evidence roll call (inline in the `<implementer>` block, all covered CHKs present, no silent omission): REQ-001 CHK-001 demonstrated, CHK-002 judgment-only; REQ-005 CHK-007 judgment-only. The two judgment-only labels are plausible -- both scenario bodies (CHK-002, CHK-007) themselves declare "persona-review judgment, not a scriptable assertion," so they are correctly deferred to reviewer-style/reviewer-business, not the tests persona.

CHK-001 "demonstrated" verified against the real invariant: `{% include "modules/references/backlog-ledger.md" %}` at `resources/modules/phases/speccy-ship.md:129` resolves and the four-field shape ejects verbatim into both `.claude/agents/speccy-ship.md` (line 215, "The four-field entry shape") and `.codex/agents/speccy-ship.toml` (lines 186/221) -- grep-confirmed, no orphaned reference. This parity is genuinely gated by the suite, not a one-time grep: the init three-way test (`speccy-cli/tests/init_three_way.rs:158` chk020) fails non-zero on any ejected file diverging from `render_host_pack` output, so a stale eject (source edited, packs not regenerated) would have failed `cargo test --workspace`, which the implementer reports green. The reference is also `{% include %}`d by brainstorm and plan, satisfying REQ-001's all-three-authoring-modules done-when.

No fabrication patterns: no spurious red/green claim (none expected for a parity+judgment slice), hygiene is the standard four-command suite, and the substantive gating prose -- per-item "own future SPEC vs. local limitation" judgment, mirror-only-the-future-spec-subset, `SPEC-NNNN, ship` provenance, and the explicit "local limitations stay in REPORT.md only and are not appended" exclusion -- is present in both source and ejected output, so CHK-007's read holds.
</review>
<review date="2026-06-14T03:26:42Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style conventions are satisfied.

Reuse survey: present and honest. `backlog-ledger.md` (T-001) is confirmed to exist at `resources/modules/references/backlog-ledger.md` and is the actual symbol the diff includes via `{% include "modules/references/backlog-ledger.md" %}` in `resources/modules/phases/speccy-ship.md:129`. No reinvention found.

No meta-annotation: `resources/modules/phases/speccy-ship.md` and `resources/modules/references/backlog-ledger.md` carry no SPEC/REQ/CHK/DEC id citations as rationale, no lint codes cited by number, and no doc-pointer prose. The worked example in `backlog-ledger.md` correctly uses the allowed `SPEC-0042` placeholder.

No banned real SPEC ids in any resource module touched by this task.

Resource-to-ejected parity holds: `Mirror the future-spec subset` and `Backlog ledger entry shape` appear in both `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml` after reeject.

Include placement: The `{% include "modules/references/backlog-ledger.md" %}` at `resources/modules/phases/speccy-ship.md:129` is unindented while the surrounding step-3 content is 3-space indented. This is consistent with the pattern already set by T-002 (`speccy-plan.md:80`) and T-003 (`speccy-brainstorm.md:206`), so it is a project-wide convention, not a per-task violation.

No dead code, unused imports, or invented parallel helpers.
</review>
