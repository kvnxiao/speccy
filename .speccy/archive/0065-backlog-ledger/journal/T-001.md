---
spec: SPEC-0065
task: T-001
generated_at: 2026-06-14T03:01:34Z
---

<implementer date="2026-06-14T03:01:34Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: write-fresh for the new reference module `backlog-ledger.md`; searched `resources/modules/references/` for an existing backlog/deferral reference and found none (only `memory-ledger.md`, which is the structural template I parallel, not a thing to extend). Reuse-as-is of the documented preamble pattern from `memory-ledger.md`'s header and worked-example sections (mirrored, not duplicated — distinct content). No new top-level code symbols introduced (resource prose only).
- Completed: created `resources/modules/references/backlog-ledger.md` structurally parallel to `memory-ledger.md`, documenting (1) the `.speccy/BACKLOG.md` file header preamble a producer copies in on self-create, (2) the four-field entry shape one line per field — Title / What & why / Deferred-because / Provenance, (3) authoring discipline (terse, honest provenance resolving to real spec+phase or `manual`, promotion-by-deletion per DEC-003), (4) the per-spec-add-rate focus-smell-not-threshold guidance, and a `SPEC-0042`-family worked example under the references carve-out. Ran `just reeject`: 0 ejected delta, exactly as the task body predicts (a reference ejects only once a consumer includes it; brainstorm/plan/ship wiring is T-002/T-003/T-004).
- Undone: the `{% include %}` wiring into brainstorm/plan/ship modules and the resulting ejected-parity (CHK-001) are explicitly T-002/T-003/T-004 scope per the task body, not done here.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: CHK-001 (orphan/ejected-parity) — judgment-only for this task; gated by the consuming includes that T-002/T-003/T-004 add, not by this task (reeject shows 0 delta now, as designed). CHK-002 (clarity/parallelism vs memory-ledger) — judgment-only by the scenario's own text ("a persona-review judgment, not a scriptable assertion"); the new module mirrors `memory-ledger.md`'s section structure (file lifecycle header, field-shape list, authoring discipline, worked example) and names exactly the four fields, ready for reviewer-business/style judgment.
- Discovered issues: SPEC.md at HEAD violated the blank-line-after-close-tag convention at 7 sites (`</scenario>` immediately followed by `</requirement>`), failing the pre-existing `every_in_tree_spec_md_has_blank_line_after_each_close_tag` test in speccy-core/tests/in_tree_specs.rs — a decompose-time defect unrelated to T-001's content. Fixed mechanically by inserting one blank line before each `</requirement>` (whitespace only, no requirement content changed); `speccy check SPEC-0065` still renders cleanly with no spec-hash-mismatch.
- Procedural compliance: followed speccy-work first-attempt recipe (clean-tree gate passed, in-progress flip, reuse survey, implement, self-review against reviewer north-star and prose-hygiene carve-out, hygiene gate, in-review flip). The SPEC.md blank-line fix above is a working-tree edit outside the new reference file; flagging it here per the discovered-issue discipline. No `resources/` skill-module friction encountered, so no reeject-source patch needed beyond the run that confirmed 0 delta.
</implementer>
<review date="2026-06-14T03:02:44Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface in this slice. The new `resources/modules/references/backlog-ledger.md` is documentation-only prose for a user-owned, git-tracked markdown file that DEC-001 keeps entirely outside the CLI (never read, parsed, linted, or verify-gated), so there is no injection, deserialization, auth, or parsing vector to assess. The documented four-field entry shape (Title / What & why / Deferred-because / Provenance) carries no credential, token, or PII field, and the authoring-discipline guidance ("provenance must resolve to a real spec and phase, never a fabricated one") steers away from fabricated/sensitive content rather than toward it. The header-preamble path reference lands only in a user's own repo, not in any response to an untrusted caller, so it is not an information-disclosure vector. No secrets, no crypto, no rate-limited endpoint, no TOCTOU.
</review>
<review date="2026-06-14T03:03:01Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff (new untracked `resources/modules/references/backlog-ledger.md`) satisfies REQ-001's in-scope slice. The module names exactly the four fields Title / What & why / Deferred-because / Provenance plus the file-header preamble, structurally parallel to `memory-ledger.md` (file-lifecycle header -> field list -> authoring discipline -> worked example). Cross-checked against intent and decisions: file header mirrors MEMORY.md lifecycle and the spec-local-vs-repo-wide boundary verbatim from non-goals item 2 (goals item 2, DEC-004); Provenance examples list only `ship`, `plan`, `manual` and never review/vet, honoring DEC-002; promotion-by-deletion / no-tombstone is documented (DEC-003); the per-spec-add-rate focus-smell-not-threshold guidance is present; the worked example uses the carved-out SPEC-0042 family per the references prose-hygiene exception. Non-goals respected: no CLI surface or `speccy verify` participation (DEC-001, "the CLI never reads it"), no status/priority/ordering field, flat unordered list. REQ-001 done-when items 2 (the `{% include %}` wiring into brainstorm/plan/ship) and 3 (ejected parity / CHK-001) are explicitly deferred to siblings T-002/T-003/T-004 by this task's own body and TASKS.md decomposition, so the zero ejected delta is by design, not dropped scope; the implementer note states this correctly. No open question silently resolved. CHK-002's clarity/parallelism judgment holds.
</review>
<review date="2026-06-14T03:03:03Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. T-001 adds only the documentation reference module `resources/modules/references/backlog-ledger.md`; there is no executable logic, no control flow, no Option/Result handling, no boundary or concurrency or resource-management surface in my lane. Checked the one correctness-adjacent build risk — the file contains no MiniJinja `{% %}`/`{{ }}` directives (grep clean), so it cannot break `render_host_pack`, and as an unconsumed reference it correctly produces 0 ejected delta (consumer includes are T-002/T-003/T-004). Clarity/parallelism (CHK-002) and ejected-parity/orphaning (CHK-001) are deferred by the scenarios to business/style and the consuming tasks, not correctness. No reachable-path defect found.
</review>
<review date="2026-06-14T03:03:18Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. `resources/modules/references/backlog-ledger.md` follows the project's established conventions.

Structure mirrors `memory-ledger.md` faithfully: intro paragraph, `### The file header`, `### The four-field entry shape`, `### Authoring discipline`, `### Worked example`. Section ordering and prose shape are parallel.

Prose-hygiene carve-out honoured: provenance field uses `SPEC-NNNN` as placeholder in field descriptions and `SPEC-0042, ship` in the worked example — both valid under the `modules/references/` carve-out. No real Speccy SPEC ids used outside that boundary.

The header preamble includes `See resources/modules/references/backlog-ledger.md for the entry shape.` — this names an artifact the skill operates on (the shape reference itself), which falls under the "naming an artifact the code operates on is data, not meta-annotation" carve-out in AGENTS.md.

No doc-pointer meta-annotations, no Speccy id citations as rationale, no dead imports or vacuous tests (resource prose only). Trailing newline confirmed present (last byte 0x0a).

SPEC.md diff is whitespace-only blank-line insertions before `</requirement>` tags — a pre-existing lint hygiene fix, no style concern.

Reuse survey is present in the implementer block and honest: `memory-ledger.md` named as structural template, not duplicated.
</review>
<review date="2026-06-14T03:03:37Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. Prose-only reference-module task with no executable behavior to drive.
Both CHKs under REQ-001 are correctly labeled `judgment-only` in the Evidence
roll-call and are genuinely non-scriptable for this task's scope: CHK-001
(orphan/ejected-parity) is gated by the `{% include %}` wiring that
T-002/T-003/T-004 add — confirmed zero ejected delta and no resource references
`backlog-ledger.md` yet, so parity cannot be demonstrated here; CHK-002
(clarity/parallelism) is persona-review judgment by the scenario's own text.
The one deterministic gate that does exercise this task's content — the
resource-prose-hygiene scan in speccy-cli/tests/resource_prose_hygiene.rs (the
references carve-out branch) — passes non-vacuously and would catch a wrong
implementation: backlog-ledger.md:75 uses the whitelisted `SPEC-0042` in its
worked example and no other concrete SPEC id or `TSK-`/`JNL-` lint code, so had
the example cited the real SPEC-0065 or a numbered lint code the test would
fail. Four fields (Title / What & why / Deferred-because / Provenance) and the
file header are present and exactly named. No fabrication risk: the task has no
red/green execution artifacts to fabricate, and the Evidence field's roll-call
is complete (every covered CHK present with a plausible label).
</review>
