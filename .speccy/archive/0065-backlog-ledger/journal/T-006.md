---
spec: SPEC-0065
task: T-006
generated_at: 2026-06-14T03:36:07Z
---

<implementer date="2026-06-14T03:36:07Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to `speccy-cli/tests/init.rs` and its sibling `tests/common/mod.rs`. Reuse-as-is: `project_with_name`, `write_file`, `read_file`, `Command::cargo_bin` test scaffolding, and `common::sha256_hex` (the existing SHA-256 helper at `tests/common/mod.rs:138`) — the latter satisfies CHK-003's "sha256 unchanged" phrasing directly rather than re-deriving byte-equality. Extend: none. Write-fresh: the two test functions `force_preserves_speccy_backlog_ledger` and `fresh_init_does_not_create_speccy_backlog_ledger`, modeled on the adjacent MEMORY pair (`force_preserves_speccy_memory_ledger` / `fresh_init_does_not_create_speccy_memory_ledger`); searched `tests/` for any pre-existing BACKLOG coverage and found none. No new top-level production symbol introduced (test-only task).
- Completed: Added the MEMORY-mirrored test pair for `.speccy/BACKLOG.md` in `speccy-cli/tests/init.rs`. CHK-003: seed a sentinel backlog, run `init --force` for both hosts, assert sha256 unchanged and that the plan-summary stdout never reports `BACKLOG.md` among ejected paths. CHK-004: fresh `init` then `verify` in sequence, assert the file is absent and `verify` exits 0 with no backlog-attributable diagnostic. No production change — init's never-planned-against bucket already excludes the file, matching the MEMORY precedent; no exclusion list was added (per the task's explicit warning).
- Undone: Nothing deferred. Both CHK-003 and CHK-004 are covered, including the optional CHK-004 verify extension named in the task body.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0 (advisories/bans/licenses/sources ok; the pre-existing MPL-2.0 license-not-encountered warning is unrelated config noise).
- Evidence: CHK roll-call for REQ-002. CHK-003 (init --force leaves a present backlog sha256-identical and unreported as ejected) — demonstrated by `force_preserves_speccy_backlog_ledger`, which seeds a sentinel, runs both hosts, and asserts sha256 equality plus stdout exclusion. CHK-004 (fresh init does not create the file; verify exits 0 with no backlog diagnostic) — demonstrated by `fresh_init_does_not_create_speccy_backlog_ledger`.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work first-attempt recipe — clean-tree gate (empty stdout), bounded reuse survey, tests-only implementation, self-review against the reviewer north-star, four-gate hygiene suite, state transitions via `speccy task transition`. No shipped-skill friction encountered; no `resources/` edit needed.
</implementer>
<review date="2026-06-14T03:37:14Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface in this test-only diff to `speccy-cli/tests/init.rs`. No production change, no auth/authorization boundary, no injection vector, no secret handling, no sensitive-data exposure, and no crypto parameter choice in scope. Seed content is a static non-sensitive string written to a per-test temp fixture; all paths are fixed literals (no traversal), and the iterated `host` values are hardcoded, not untrusted input. The byte-stability check rests on a genuine SHA-256 (`common::sha256_hex` over `sha2::Sha256` at `speccy-cli/tests/common/mod.rs:138`), so a real file mutation would fail the assertion rather than slip through.
</review>
<review date="2026-06-14T03:38:02Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-002's three done-when items map cleanly to the diff. Item 1 (fresh init does not create the file) -> `fresh_init_does_not_create_speccy_backlog_ledger` asserts non-existence. Item 2 (init --force leaves bytes identical) -> `force_preserves_speccy_backlog_ledger` asserts sha256 equality across BOTH hosts (claude-code, codex), matching the MEMORY pair's parity and CHK-003's sha256 phrasing; the reeject arm is covered by equivalence since reeject is `init --force`. Item 3 (verify emits no backlog diagnostic) -> CHK-004's binding scenario (the absence case) is covered by `fresh_init_does_not_create_speccy_backlog_ledger`'s post-verify `!stdout.contains("backlog")` check.

Non-goals respected: diff is test-only, zero production change, and no init exclusion list was added (the task's explicit anti-pattern). Confirmed `speccy-cli/src` references "backlog" nowhere, so the "no backlog diagnostic" assertions are faithful by construction, not vacuous.

Falsification checks pass: the `!stdout.contains("BACKLOG.md")` assertion is non-vacuous because `print_plan` (`speccy-cli/src/init.rs:258-271`) writes every enumerated destination path to stdout, so an init that enumerated the backlog would surface it and fail the test. The sha256 and `.exists()` assertions would fail under the wrong (overwrite/create) implementation. Both new tests pass under `cargo test -p speccy-cli --test init backlog`.

Minor, non-blocking: the implementer's journal prose claims CHK-004 also covers the present-sentinel verify case, but the test only exercises the absence path (which is exactly what the binding CHK-004 scenario names). The present/malformed verify path was an "optional" extension in the task body and is provably inert given zero backlog logic in the CLI, so the omission is not a contract gap.
</review>
<review date="2026-06-14T03:38:09Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Test-only change adding `force_preserves_speccy_backlog_ledger` and `fresh_init_does_not_create_speccy_backlog_ledger` to `speccy-cli/tests/init.rs`. Verified the assertions are sound regression guards, not false-passes: the sha256-equality check reads back real seeded content and fails on any init create/overwrite/delete; the `!stdout.contains("BACKLOG.md")` check is meaningful because `print_plan` (`speccy-cli/src/init.rs:264-269`) emits every enumerated plan path to stdout, so the guard fires if a regression adds BACKLOG.md to the manifest. CHK-004's fresh-init absence assertion and `speccy verify` exit-0 path are consistent with `scaffold_gitkeep` (verify finds the workspace via `.gitkeep` and exits 0 on a spec-less repo). No control-flow, boundary, Option/Result, concurrency, or resource defects observed.
</review>
<review date="2026-06-14T03:38:27Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The two new tests in `speccy-cli/tests/init.rs` are structurally clean and idiomatic.

Naming matches the MEMORY precedent exactly (`force_preserves_speccy_backlog_ledger` / `fresh_init_does_not_create_speccy_backlog_ledger`). All helpers (`project_with_name`, `write_file`, `read_file`, `common::sha256_hex`) are reused from the existing scaffolding — the survey claimed, and the diff confirms, no new production symbols. No dead imports introduced.

No provenance annotations (no `// per REQ-NNN`, `// CHK-003`, etc.) in test comments — the comments describe behavior, not spec references.

The `#[allow]` suppressions at the file top already carry `reason =` attributes (pre-existing, not introduced by the diff); no new suppressions were added.

One minor observation, not blocking: `fresh_init_does_not_create_speccy_backlog_ledger` checks `stdout.to_ascii_lowercase().contains("backlog")` for the "no diagnostic" assertion, but `speccy verify` emits diagnostics to stderr (per `verify.rs` test patterns). The substantive gate is `.success()` (exit 0), which is correct and sufficient; the stdout probe is supplementary. The test cannot false-positive but could miss a backlog-string on stderr. Given that exit-0 is the load-bearing check and the task body marks CHK-004's verify extension as "optional," this is acceptable.

The `.success().code(0)` at `init.rs:331–332` is redundant (`.success()` already implies exit 0) but matches a pre-existing pattern at line 887, so it is consistent with the surrounding style.
</review>
<review date="2026-06-14T03:38:41Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Both scenarios drive real `speccy init` / `speccy verify` invocations through the binary, not mocks. CHK-003 (`force_preserves_speccy_backlog_ledger`, `init.rs:273`) seeds a sentinel `.speccy/BACKLOG.md`, runs `init --force` for both hosts, and asserts two independent invariants: `sha256_hex(after) == seed_hash` (catches an overwrite) and `!stdout.contains("BACKLOG.md")` — non-vacuous because `print_plan` (`init.rs:264-268`) writes every planned destination path to stdout, so a regression adding the file to the eject manifest would surface there. CHK-004 (`fresh_init_does_not_create_speccy_backlog_ledger`, `init.rs:309`) asserts `!exists()` plus `verify` `.code(0)`; a backlog-attributable lint error would bump the gating count and flip the exit code, which `.code(0)` catches. Falsification holds for the realistic wrong implementations (init creating the file; verify gating on it).

One soft spot, non-blocking: CHK-004's `!stdout.to_ascii_lowercase().contains("backlog")` scans stdout, but `verify` text mode writes only the two summary count lines to stdout while individual lint diagnostics route to stderr (`main.rs:306`). A warning-level backlog diagnostic on stderr would slip past that substring check. It is not load-bearing here: per DEC-001 the CLI has zero backlog logic by construction, so the exit-0 assertion is what actually demonstrates "no backlog-attributable diagnostic," and a warning would not change the exit code regardless.

Evidence roll-call: the inline `Evidence:` field labels CHK-003 and CHK-004 `demonstrated`, each citing a named test in the diff. No separate `evidence/T-NNN.md` file exists, consistent with all five completed siblings (T-001..T-005) in this spec — the spec-wide convention is the inline roll-call. Test-only task, no production change, so no red-then-green transition is applicable; the tests are regression guards on already-correct never-planned-against behavior, and the implementer correctly declined to add an exclusion list per the task warning. Naming mirrors the MEMORY pair as instructed.
</review>
