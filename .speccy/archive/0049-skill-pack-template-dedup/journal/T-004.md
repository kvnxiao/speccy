---
spec: SPEC-0049
task: T-004
generated_at: 2026-05-27T12:00:00Z
---

<implementer round="1" date="2026-05-27T12:00:00Z" model="claude-opus-4.7[1m]/low">

- Completed:
  - Rewrote `resources/agents/.claude/skills/speccy-work/SKILL.md.tmpl`
    and `resources/agents/.agents/skills/speccy-work/SKILL.md.tmpl`
    to the DEC-001(a) pure-include shape: YAML frontmatter plus a
    single `{% include "modules/skills/speccy-work.md" %}` directive.
    All inline retry-shape rule body, reconcile-policy table,
    "Entry precondition" paragraph, and "Hygiene gate" paragraph
    that previously lived in the wrapper bodies are removed; that
    content now lives once in the module from T-001.
  - Restructured `resources/modules/skills/speccy-work.md` from the
    raw 1:1 port (T-001) into a recipe-shape body with `## When to
    use` and `## Steps` sections plus a `speccy next --json` code
    fence, matching the speccy-vet / speccy-orchestrate /
    speccy-review pattern. The DEC-002 invariant text for
    retry-shape and reconcile-policy, plus the agent-file
    delegation pointer, are preserved verbatim from T-001.
  - Ran `just reeject` to propagate to `.claude/skills/speccy-work/SKILL.md`
    and `.agents/skills/speccy-work/SKILL.md`. Init pipeline reports
    1 file overwritten per host, 32 unchanged; no other ejected
    file diffed.
  - Adjusted four pre-SPEC-0049 test files whose assertions encoded
    the older "speccy-work is stub-delegate" convention that
    DEC-001 supersedes for this verb:
    - `speccy-cli/tests/init.rs`: split the `CLAUDE_PINNED_PHASES`
      iteration into a new `CLAUDE_STUB_DELEGATE_PHASES = [
      "decompose", "ship" ]` so the `assert_thin_stub_body` cap
      no longer applies to `work`.
    - `speccy-cli/tests/init_phase_agents.rs`: removed `work` from
      the two ≤10-line stub-cap iterations (`phase_worker_skill_stub_is_thin`
      and the `chk017_codex_skill_stub` line-count branch). The
      agent-file-pointer / `/agent` invocation assertions still
      apply to all three phases.
    - `speccy-cli/tests/skill_packs.rs`: removed `speccy-work` from
      `PINNED_STUB_PHASES`, which routes it through the
      pure-include `{% include "modules/skills/speccy-work.md" %}`
      assertion branch (matches speccy-vet/orchestrate/review).
    - `speccy-cli/tests/skill_stub_shape.rs`: added a new
      `STUB_ONLY_PHASES = ["decompose", "ship"]` constant and
      pointed `stub_skill_has_no_steps_or_when_to_use_*` at it.
      The smaller-than-agent-body check still iterates the full
      `PINNED_PHASES` (it remains true for `work`: 3.5 KB skill
      body vs 11.8 KB agent body).
  - Each test edit carries a comment naming SPEC-0049 / REQ-003 /
    DEC-001 as the convention that supersedes the prior assertion.

- Undone: nothing. T-005 (dogfood pass) is the remaining task in
  SPEC-0049 and is out of scope for T-004.

- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.

- Evidence: structural slimming verified by Grep against the ejected
  packs and by content audit of the resulting wrapper files.
  Roll call for CHKs under REQ-002, REQ-003, REQ-004:
  - CHK-004 (non-owner ejected files carry DEC-002 invariant +
    pointer; canonical references retain full rule body):
    judgment-only → Grep over `{.claude,.agents,.codex}/**/*` for the
    distinctive retry-shape sentence `is in **retry shape** at`
    returned only the two canonical reference files
    (`.claude/speccy-references/retry-shape.md` and its `.agents/`
    sibling); no non-owner ejected file matched. Reviewer
    judgment-only confirms structural slimming.
  - CHK-005 (orchestrate ejected files: reconcile-policy table
    absent, invariant present): judgment-only → Grep for the
    distinctive table row pattern `commit_without_state.*auto_fixable`
    returned only the two canonical `reconcile-policy.md` files;
    the orchestrate SKILL.md files carry the invariant text inline.
  - CHK-006 (speccy-vet retains full Phase 0/1/2/3 grammar):
    demonstrated → Grep for `### Phase 0 — bootstrap` matched
    exactly `.claude/skills/speccy-vet/SKILL.md` and
    `.agents/skills/speccy-vet/SKILL.md` (canonical-owner
    exception preserved).
  - CHK-007 (every wrapper conforms to DEC-001's two patterns):
    judgment-only → all 20 wrapper templates under
    `resources/agents/.<host>/skills/<skill>/SKILL.md.tmpl` are 4-9
    lines (frontmatter + include). The
    `claude_code_wrapper_shape_and_body` and
    `t006_codex_wrapper_shape_and_body` tests pass, which
    structurally verifies every non-stub wrapper resolves to the
    single `{% include "modules/skills/<verb>.md" %}` directive.
  - CHK-008 (speccy-work wrappers are pure-include per DEC-001(a)):
    demonstrated → both wrapper files are exactly 5 lines: 3 lines
    of frontmatter (`---`, `name`, `description`, `---`) plus one
    body line `{% include "modules/skills/speccy-work.md" %}`.
    The wrapper-shape test in `skill_packs.rs` asserts this
    structurally.
  - CHK-009 (four hygiene gates exit 0): demonstrated → see
    Hygiene checks above; all four gates exited 0.

- Discovered issues:
  - Four test files encoded the prior convention that `speccy-work`
    is stub-delegate. Updating them was unavoidable scope creep
    from SPEC-0049's wrapper-shape change. All edits carry
    SPEC-0049 / REQ-003 / DEC-001 attribution comments so a future
    reader sees why the assertion-set narrowed.
  - The T-001 module body did not contain `## When to use` or
    `## Steps` sections; the existing recipe-shape test
    (`recipe_content_shape`) required them once `speccy-work`
    became a recipe-shaped skill. SPEC-0049 Notes explicitly
    allows the implementer to reorganize the module body for
    clarity if a meaningful improvement exists; the recipe-shape
    headings are that improvement and align the module with its
    sibling skills (`speccy-vet`, `speccy-orchestrate`,
    `speccy-review`).

- Procedural compliance: read the speccy-work skill body, applied
  the retry-shape rule against the missing `T-004.md` journal
  (first-attempt shape), confirmed clean tree, resolved target via
  `speccy next SPEC-0049 --json`, then implemented. Ran the four
  hygiene gates before flipping `state` to `in-review`.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" round="1" date="2026-05-27T16:00:00Z">
T-004 satisfies REQ-002, REQ-003, REQ-004 as written. Pure-include shape achieved: both `resources/agents/.claude/skills/speccy-work/SKILL.md.tmpl` and `resources/agents/.agents/skills/speccy-work/SKILL.md.tmpl` are exactly frontmatter + one body line `{% include "modules/skills/speccy-work.md" %}` — pattern matches the speccy-vet/orchestrate/review wrappers per DEC-001(a) and CHK-008. Test-file edits are not scope creep — all four mechanically narrow pre-SPEC-0049 iteration sets that encoded the older `speccy-work is stub-delegate` invariant; REQ-003 explicitly supersedes that. The `## When to use` / `## Steps` restructure of `resources/modules/skills/speccy-work.md` preserves the load-bearing DEC-002 invariants and pointers; SPEC Notes explicitly authorise this. CHK-004/005/006 satisfied; canonical-owner exception preserved. No silently resolved open questions; no non-goal violations.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" round="1" date="2026-05-27T16:00:00Z">
T-004's four test-file edits narrowly carve out speccy-work from invariants the DEC-001(a) pure-include shape genuinely contradicts, without loosening anything else. `CLAUDE_STUB_DELEGATE_PHASES`, `PINNED_STUB_PHASES`, `STUB_ONLY_PHASES` all retain both stub-delegate phases (`decompose`, `ship`); narrowing `PINNED_STUB_PHASES` actually *strengthens* the suite for speccy-work — it now flows into the `else` branches where `assert_eq!(body.trim(), "{% include \"modules/skills/speccy-work.md\" %}")` enforces CHK-008's pure-include shape exactly. The agent-file-pointer and `/agent speccy-work` invocation assertions still iterate all three phases. `stub_skill_body_smaller_than_agent_body_*` still iterates `PINNED_PHASES` and bites on work (3,549 vs 11,828 bytes). No vacuous-test patterns introduced. Independently re-verified the four hygiene gates on the dirty tree — all green.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" round="1" date="2026-05-27T16:00:00Z">
No security-relevant surface touched. T-004 restructures `resources/agents/.<host>/skills/speccy-work/SKILL.md.tmpl` into pure-include wrappers and extends `resources/modules/skills/speccy-work.md` with recipe-shape headings — all prose for agent guidance, no executable code paths affected. Test-file edits narrow stub-shape assertions for the `work` verb; no auth/crypto/data-handling tests were weakened. No new dependencies, no secret/credential surface, no log or error-message changes that could leak information. Hygiene gates clean.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" round="1" date="2026-05-27T16:00:00Z">
`speccy-cli/tests/skill_stub_shape.rs` lines 18–19: the module-level doc comment says

```
//! (iii) each of those six rendered SKILL.md bodies does not contain
//!       `## Steps` or `## When to use`.
```

This was accurate before T-004. T-004 migrated `speccy-work` to pure-include shape, giving its SKILL.md body `## When to use` and `## Steps`. The per-function comments and the `STUB_ONLY_PHASES` constant on line 41 were updated correctly, but the module-level doc comment on lines 18–19 was not. After T-004 the claim "each of those six bodies" no longer holds — the `speccy-work` SKILL.md bodies on both hosts now carry both headings. The doc comment should be updated to match the narrowed invariant: assertion (iii) now applies only to `STUB_ONLY_PHASES` (`decompose`, `ship`), not to the full `PINNED_PHASES` set.

No other style violations found. The module body (`resources/modules/skills/speccy-work.md`) contains no stale partial/include references. Both wrapper templates are clean pure-include shape. Rust suppressions are `#[expect(..., reason = "...")]` throughout. The `STUB_ONLY_PHASES` and `CLAUDE_STUB_DELEGATE_PHASES` constant names are clear and correctly attributed with SPEC-0049/REQ-003/DEC-001 comments.
</review>

<blockers date="2026-05-27T16:00:00Z" round="1">
Update the module-level doc comment in `speccy-cli/tests/skill_stub_shape.rs` lines 18–19 to match the post-T-004 invariant.

The comment currently claims "(iii) each of those six rendered SKILL.md bodies does not contain `## Steps` or `## When to use`." After T-004, the speccy-work SKILL.md bodies on both hosts carry both headings (the work skill is now a recipe, not a stub-delegate). Rewrite assertion (iii) so it applies only to `STUB_ONLY_PHASES` (`decompose`, `ship`) — e.g. "(iii) the four rendered SKILL.md bodies for the stub-delegate phases (`decompose`, `ship`) do not contain `## Steps` or `## When to use`; the two `speccy-work` SKILL.md bodies are recipe-shape and carry both headings per SPEC-0049 / DEC-001(a)."

Re-run the four hygiene gates after the edit.
</blockers>

<implementer round="2" date="2026-05-27T18:00:00Z" model="claude-opus-4.7[1m]/low">

- Completed:
  - Rewrote the module-level doc comment in
    `speccy-cli/tests/skill_stub_shape.rs` (lines 18–19 of the
    round-1 file) to narrow assertion (iii) to the stub-delegate
    phases. The new prose mirrors the wording the style reviewer
    suggested verbatim: "(iii) the four rendered SKILL.md bodies for
    the stub-delegate phases (`decompose`, `ship`) do not contain
    `## Steps` or `## When to use`; the two `speccy-work` SKILL.md
    bodies are recipe-shape and carry both headings per SPEC-0049 /
    DEC-001(a)." All other round-1 WIP in the tree is unchanged —
    only the doc comment was amended in place per the retry-shape
    contract.

- Undone: nothing.

- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.

- Evidence: doc-comment-only edit; the four reviewer personas in
  round 1 already attested the CHK roll call demonstrated /
  hygiene / judgment-only labels; nothing new to score.

- Discovered issues: none.

- Procedural compliance: detected retry shape via the journal's
  `<blockers round="1">` block matching the highest `<implementer>`
  round, amended the dirty tree in place rather than rebuilding,
  re-ran all four hygiene gates, then flipped state to `in-review`
  and appended this `round="2"` block.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" round="2" date="2026-05-27T17:00:00Z">
T-004 round-2 satisfies the round-1 style blocker without regressing any business-level commitment. The amend was scoped exactly to what the blocker requested: `speccy-cli/tests/skill_stub_shape.rs` module-level doc comment (lines 18-22) now reads "(iii) the four rendered SKILL.md bodies for the stub-delegate phases (`decompose`, `ship`) do not contain `## Steps` or `## When to use`; the two `speccy-work` SKILL.md bodies are recipe-shape and carry both headings per SPEC-0049 / DEC-001(a)." Nothing else moved. REQ-002 (ejected slimming), REQ-003 (wrapper pure-include shape), REQ-004 (hygiene gates) remain satisfied. No goals/non-goals/user-stories drift, no silently resolved open questions, no scope creep beyond the doc-comment edit.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" round="2" date="2026-05-27T17:00:00Z">
Round-2 amend is a doc-comment-only edit to `speccy-cli/tests/skill_stub_shape.rs` lines 15-22. No test body, constant, or iteration set is touched in this round. Coverage shape preserved end-to-end: `stub_skill_body_smaller_than_agent_body_*` still iterates `PINNED_PHASES` (size invariant still bites on `work`); the `/agent` + agent-file-pointer assertions still cover all three pinned phases; the negative `## Steps` / `## When to use` assertions still bite on both stub-delegate phases. New doc-comment text accurately describes the narrowed invariant. Hygiene gates exit 0. No vacuous-test patterns. No mocks introduced. No CHK in T-004's covered set loses its proof.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" round="2" date="2026-05-27T17:00:00Z">
No security-relevant surface touched in round 2. The amend is a doc-comment-only edit at `speccy-cli/tests/skill_stub_shape.rs` lines 18-22, narrowing the module-level description of assertion (iii) to match the post-T-004 invariant. No code paths, no executable logic, no auth/crypto/secrets/logging surface, no dependency changes, no error-message changes. Round-1 security verdict (pass) carries through unchanged.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" round="2" date="2026-05-27T17:00:00Z">
The round-1 blocker is resolved. The module-level doc comment now reads exactly the suggested wording: "(iii) the four rendered SKILL.md bodies for the stub-delegate phases (`decompose`, `ship`) do not contain `## Steps` or `## When to use`; the two `speccy-work` SKILL.md bodies are recipe-shape and carry both headings per SPEC-0049 / DEC-001(a)." The `STUB_ONLY_PHASES` constant is correctly attributed, and both `stub_skill_has_no_steps_or_when_to_use_*` test functions iterate over it. No other style violations introduced.
</review>
