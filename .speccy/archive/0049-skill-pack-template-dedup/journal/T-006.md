---
spec: SPEC-0049
task: T-006
generated_at: 2026-05-27T00:00:00Z
---

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="1">
- Completed: Split the orchestrator dispatch tree in
  `resources/modules/skills/speccy-orchestrate.md` so it matches the
  CLI's `next_action.kind` enum. Edits: (1) lifecycle ASCII tree now
  has a `vet` branch (inline speccy-vet body) separate from `ship`
  (ask user + spawn speccy-ship); (2) Loop step 2 dispatch enum adds
  a `vet` bullet between `review` and `ship`; (3) renamed the former
  "Ship dispatch" section to "Vet dispatch" with body unchanged
  (Phase 0-3 inline) and the `verdict="pass"` branch now re-queries
  rather than asking the user, with `verdict="fail"` reaction prose
  retained; (4) added a new "Ship dispatch" section bound to the
  `ship` kind that frames the kind, asks via `AskUserQuestion` /
  Codex equivalent under a Jinja host conditional, spawns
  `speccy-ship` on confirm (Task tool with `subagent_type:
  "speccy-ship"` for `.claude`, Codex spawn against
  `.codex/agents/speccy-ship.toml` for `.agents`), and stops on
  decline; (5) Stop conditions enum updated to list `work, review,
  vet, ship, decompose`; (6) status reporting examples updated:
  `holistic gate` → `vet`, `ready to ship — confirm…` retained.
  Ran `just reeject` — both
  `.claude/skills/speccy-orchestrate/SKILL.md` and
  `.agents/skills/speccy-orchestrate/SKILL.md` regenerated with the
  five-kind dispatch tree, the host-conditional Ship dispatch
  spawn, and the updated Stop conditions enum. No wrapper edits
  needed (pure-include per DEC-001(a)).
- Undone: nothing.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail is the diff in
  `resources/modules/skills/speccy-orchestrate.md` and the two
  ejected `.claude` / `.agents` SKILL.md files.
  Roll call for CHKs under REQ-006:
  - CHK-011 (resources module shows split Vet/Ship dispatch tree):
    demonstrated → `resources/modules/skills/speccy-orchestrate.md`
    Loop step 2 enumerates `vet` and `ship` as distinct kinds; the
    "Vet dispatch" section inlines the speccy-vet body (Phase 0-3)
    and carries the vet-verdict pass/fail reaction; the separate
    "Ship dispatch" section asks the user and spawns speccy-ship on
    `ship` without re-running vet.
  - CHK-012 (ejected `.claude` and `.agents` SKILL.md carry the
    post-amendment dispatch tree): demonstrated → both
    `.claude/skills/speccy-orchestrate/SKILL.md` and
    `.agents/skills/speccy-orchestrate/SKILL.md` (regenerated via
    `just reeject`) show Loop step 2 enumerating five kinds, a Vet
    dispatch section bound to `vet` that inlines the speccy-vet
    body, a Ship dispatch section bound to `ship` performing only
    user-confirmation + speccy-ship spawn (host-conditional via
    Jinja `{% if host == "claude-code" %}`), and a Stop conditions
    enum listing all five kinds.
  - CHK-013 (four hygiene gates pass): hygiene → covered by the
    four-gate roll call above.
- Discovered issues: none.
- Procedural compliance: edited source under `resources/modules/`,
  then ran `just reeject` to regenerate `.claude` / `.agents` —
  followed `## Skill pack source of truth` rule. No direct edits to
  ejected files.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-27T20:09:40Z" round="1">
REQ-006 fully satisfied. Loop step 2 enumerates five kinds (`work`,
`review`, `vet`, `ship`, `decompose`); Vet dispatch section binds to
`vet` and runs speccy-vet inline (Phase 0-3 plus vet-verdict pass/fail
reaction); new Ship dispatch section binds to `ship` and only asks
the user via `AskUserQuestion` / Codex equivalent then spawns
`speccy-ship` on confirm without re-running vet; Lifecycle ASCII tree
shows the split; Stop conditions enum lists all five kinds; status
reporting examples updated. Both ejected
`.claude/skills/speccy-orchestrate/SKILL.md` and
`.agents/skills/speccy-orchestrate/SKILL.md` carry the post-amendment
dispatch tree with correct host-conditional rendering. The
Vet→re-query→Ship flow matches the SPEC's `<behavior>` contract.
CHK-011 and CHK-012 demonstrated. Non-goals respected (template-only,
no Rust changes, no new lints). Hygiene gates reported green by
implementer.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-27T20:09:40Z" round="1">
Both task scenarios are content-shape checks over prose templates
("a reviewer reads X, then they see Y") — appropriate per AGENTS.md
"verifying prose-template changes" rule; no test code is warranted
for a pure dispatch-tree prose edit. CHK-011 / CHK-012 verified
directly against `resources/modules/skills/speccy-orchestrate.md` and
both ejected SKILL.md files: Loop step 2 lists all five kinds
(`work, review, vet, ship, decompose`), Vet dispatch is bound to
`vet` and inlines the speccy-vet Phase 0-3 body with the pass/fail
reaction, Ship dispatch is bound to `ship` and only asks-then-spawns
(host-conditional via `{% if host == "claude-code" %}`), and Stop
conditions enum lists all five kinds. CHK-013 (hygiene) re-verified
locally: `cargo test --workspace`, `cargo clippy ... -- -D warnings`,
and `cargo +nightly fmt --all --check` each exit 0; evidence's roll
call covers all three CHKs with appropriate labels. Non-blocking
observation: first Stop conditions bullet still references "holistic
gate" — pre-existing prose, out of scope for T-006's REQ-006 edits.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-27T20:09:40Z" round="1">
No security concerns. The diff is prose-only edits to orchestrator
skill body markdown that split the `ship` kind into distinct `vet`
and `ship` kinds and add a dedicated "Ship dispatch" section. No
auth, secrets, input validation, deserialization, crypto, dependency,
or command-execution surfaces are touched. The explicit
user-confirmation gate before the irreversible `speccy-ship` spawn
("Ship opens a PR — irreversible — so this confirmation is always
explicit; never auto-ship") is preserved and now sits at a
structurally clearer boundary. Sub-agent invocation strings
(`subagent_type: "speccy-ship"`, `.codex/agents/speccy-ship.toml`)
are static literals in markdown — no injection vector. Host-
conditional Jinja follows the existing established pattern. Ejected
`.claude` / `.agents` SKILL.md files were regenerated via
`just reeject`, so no hand-edits to ejected output bypassed the
canonical source.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-27T20:09:40Z" round="1">
All changes in T-006 are prose-only edits to
`resources/modules/skills/speccy-orchestrate.md` and its two ejected
SKILL.md files. No Rust code, no dead imports, no suppression
annotations, no naming inconsistencies, and no duplicate helpers were
introduced. The two Jinja inline-conditional lines added match the
pattern already pervasive throughout the file and other skill modules
(existing lines in `speccy-work.md` reach 684 chars). The
`.editorconfig` declares no `max_line_length` constraint, so this is
not a violation. Non-blocking observation: the pre-existing
`holistic gate` reference in the Stop conditions section was not
touched by this diff and remains out of scope for T-006.
</review>
