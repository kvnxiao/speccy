---
spec: SPEC-0049
task: T-001
generated_at: 2026-05-27T09:00:00Z
---

<implementer round="1" date="2026-05-27T09:00:00Z" model="claude-opus-4.7[1m]/low">

Completed:
- Added `resources/modules/skills/speccy-work.md` as the new
  host-neutral canonical skill body for `speccy-work`. The new
  module opens with a `# {{ cmd_prefix }}speccy-work` heading and a
  host-conditional pointer to the agent file (`.claude/agents/speccy-work.md`
  vs `.codex/agents/speccy-work.toml`). It carries the
  load-bearing Entry precondition paragraph, the DEC-002
  retry-shape invariant (with a pointer to
  `{{ speccy_references_path }}/retry-shape.md`), the DEC-002
  reconcile-policy invariant (with a pointer to
  `{{ speccy_references_path }}/reconcile-policy.md`), and the
  Hygiene gate paragraph. The two verbatim canonical-rule blocks
  from the wrapper template — the retry-shape rule statement +
  read-only-scope + worked examples + edge case, and the full
  reconcile-policy table + three-properties + extension protocol —
  are replaced inline with the DEC-002 invariant + pointer
  formulations.
- Per the task body's "Notes" reorganization allowance, the new
  module preserves the wrapper body's structure 1:1 except for the
  two DEC-002 substitutions. No reorganization beyond that.
- New module has no consumers yet (T-004 switches the wrappers).
  `just reeject` after the edit confirmed `0 created, 0 overwritten,
  33 unchanged` — no ejected diff produced from this task alone, per
  the task's fourth scenario.
- Fixed pre-existing friction in two places along the way:
  1. `speccy-cli/tests/skill_stub_shape.rs`'s
     `phase_body_files_moved_to_modules_phases` test asserted that
     `resources/modules/skills/speccy-work.md` must NOT exist (a
     T-009 CHK-010 invariant from an earlier SPEC). SPEC-0049's
     T-001 explicitly creates that file as a distinct artifact from
     the phase/agent body at `resources/modules/phases/speccy-work.md`.
     Updated the test to skip `work` from the old-path-gone
     assertion with a comment documenting the SPEC-0049 reasoning.
     The other three phases (`decompose`, `ship`, `init`) retain
     the rename invariant.
  2. `speccy-core/tests/in_tree_specs.rs`'s
     `every_in_tree_spec_md_has_blank_line_after_each_close_tag`
     test was flagging five pre-existing `</scenario></requirement>`
     pairs in SPEC.md missing a blank-line separator (lines 235,
     325, 394, 427, 481 before the fix). Fixed all five by
     inserting a blank line, then ran `speccy lock SPEC-0049` to
     re-record the spec hash in TASKS.md frontmatter.

Undone:
- T-002 through T-005 remain pending (wrapper refactors, downstream
  module edits, hygiene re-run, and dogfood pass). Out of scope for
  this single-task primitive.

Hygiene checks:
- `cargo test --workspace`: exit 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
- `cargo +nightly fmt --all --check`: exit 0
- `cargo deny check`: exit 0

Evidence: paper trail at `resources/modules/skills/speccy-work.md`
(the new module body itself is the evidence; the task is a content
edit). Roll call for CHKs under REQ-001:
- CHK-001 (retry-shape reaches each consuming site via include or
  invariant-partial, no verbatim copy): judgment-only → reviewer
  reads `resources/modules/skills/speccy-work.md` and confirms the
  retry-shape rule statement (`is in **retry shape** at` … `iff`)
  is absent, replaced by the DEC-002 invariant + pointer. The
  wrapper still carries the verbatim body at this point; T-004
  switches the wrapper to pure-include, at which point CHK-001 is
  globally satisfied.
- CHK-002 (reconcile-policy reaches each consuming site via include
  or invariant-partial, no verbatim copy of the policy table):
  judgment-only → reviewer reads
  `resources/modules/skills/speccy-work.md` and confirms the
  `commit_without_state | auto_fixable` row pattern is absent,
  replaced by the DEC-002 invariant + pointer. T-002 / T-003 / T-004
  finish the global satisfaction.
- CHK-003 (orchestrate carries DEC-002 vet-phases pointer without
  full include; vet keeps the canonical include): judgment-only →
  not in scope for T-001; T-002 makes the orchestrate edit, vet is
  untouched throughout.

Discovered issues:
- The two pre-existing test failures above were independent of this
  refactor but blocked the four-gate hygiene suite. Fixed in this
  task; called out under Procedural compliance below.

Procedural compliance:
- Updated `speccy-cli/tests/skill_stub_shape.rs` to reflect the
  SPEC-0049 reality (work skill-body and phase-body coexist as
  distinct files).
- Fixed five `</scenario></requirement>` whitespace issues in
  SPEC.md and ran `speccy lock SPEC-0049` to re-record the hash.
  This is a within-task SPEC content fix (a missing blank line, not
  a Requirements change), so no `speccy-amend` orchestration was
  required.
- New module uses `{{ cmd_prefix }}`, `{{ speccy_references_path }}`,
  and a `{% if host == "claude-code" %}` conditional per the task
  body's templating instructions.
- Source edit (not ejected file) per AGENTS.md § "Skill pack source
  of truth"; ejected output remains untouched (confirmed by
  `just reeject` reporting zero overwrites).

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]" round="1" date="2026-05-27T09:30:00Z">
T-001 faithfully delivers its slice: creates `resources/modules/skills/speccy-work.md` with the DEC-002 retry-shape and reconcile-policy invariants inline (verbatim match to the SPEC's DEC-002 wording), preserves the wrapper body 1:1 otherwise, and `just reeject` produces zero diffs as required (new module unconsumed by any wrapper yet — wrapper switching is correctly punted to T-004). All four task-scenarios verified: heading references `{{ cmd_prefix }}speccy-work`, the `is in **retry shape** at` ... `iff` rule statement is absent from the new module, the `commit_without_state | auto_fixable` policy-table row is absent, and `just reeject` shows 0 overwritten. The implementer's CHK roll-call honestly reports that global CHK-001/CHK-002 satisfaction lands later (T-002/T-003/T-004) — no overreach on this task's scope. SPEC.md edits (blank-line insertions between `</scenario></requirement>` pairs) and the `speccy lock` re-hash are procedurally appropriate per AGENTS.md (within-task content fix, no Requirements semantics change); the test-edit in `speccy-cli/tests/skill_stub_shape.rs` is a legitimate sibling fix to allow SPEC-0049's deliberate coexistence of the `speccy-work` skill-body and phase-body. No silent resolution of open questions; no scope creep against non-goals; the slice serves the maintainer and AI-agent user stories.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" round="1" date="2026-05-27T09:30:00Z">
T-001 is a content-edit task that creates `resources/modules/skills/speccy-work.md` with no behavior code under test. All three CHKs (CHK-001/002/003) under REQ-001 are explicitly defined by SPEC text as reviewer judgment ("no automated prose substring match is asserted per the no-vacuous-tests rule"), and the evidence roll call correctly labels each `judgment-only`.

Task-scenarios verified: the file at `resources/modules/skills/speccy-work.md` opens with `# {{ cmd_prefix }}speccy-work` (scenario 1); grep for `is in **retry shape** at` returns no match (scenario 2); grep for `commit_without_state` / `auto_fixable` returns no match (scenario 3); the implementer's `just reeject` output of `0 created, 0 overwritten, 33 unchanged` satisfies scenario 4's "no ejected diff" check (the new module has no consumers until T-004).

The collateral test edit at `speccy-cli/tests/skill_stub_shape.rs:343-363` is a legitimate, narrowly-scoped invariant amendment — only `work` is excluded from the old-path-gone assertion, with a doc comment citing SPEC-0049's skill-body vs phase/agent-body distinction; the other three phases (`decompose`, `ship`, `init`) retain the original T-009 CHK-010 invariant. This is not a vacuous-test pattern; it tracks a real SPEC change. Four-gate hygiene (cargo test/clippy/fmt/deny) reported exit 0 in evidence.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" round="1" date="2026-05-27T09:30:00Z">
Prose-only template addition (`resources/modules/skills/speccy-work.md`) plus a narrow test-assertion carve-out in `speccy-cli/tests/skill_stub_shape.rs`. No executable code paths, no input-validation surface, no secrets, no auth/authz boundaries, no crypto, no new dependencies (`cargo deny check` exit 0 per journal), no logging changes. The two new Jinja substitutions (`{{ cmd_prefix }}`, `{{ speccy_references_path }}`) and the `{% if host == "claude-code" %}` conditional are pre-existing template variables — render-time substitution, not runtime input — so no template-injection or path-traversal vector is introduced. The DEC-002 invariant-plus-pointer pattern changes what an LLM reads, not what executes. Test narrowing in `skill_stub_shape.rs:344-363` only relaxes a stub-shape invariant (CHK-010), which is not a security gate, and the carve-out is justified by SPEC-0049's explicit introduction of the new artifact. Nothing to block on.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" round="1" date="2026-05-27T09:30:00Z">
All style checks pass. The new `resources/modules/skills/speccy-work.md` has a trailing newline (`0x0a` confirmed by byte probe), uses the correct Jinja template variables and host conditional, and the two task-scenario sentinel strings (`is in **retry shape** at` and `commit_without_state` adjacent to `auto_fixable`) are both absent. The modified `speccy-cli/tests/skill_stub_shape.rs` is idiomatic Rust: the `continue` guard is placed after the `new_path` assertion so the existence invariant still fires for `work`, the doc-comment on the test function accurately describes the exception, and `cargo clippy` and `cargo +nightly fmt --all --check` both exit 0 with no suppressions added.
</review>
