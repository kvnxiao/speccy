---
spec: SPEC-0049
task: T-002
generated_at: 2026-05-27T12:00:00Z
---

<implementer round="1" date="2026-05-27T12:00:00Z" model="claude-opus-4.7[1m]/low">
- Completed: Refactored `resources/modules/skills/speccy-orchestrate.md`
  to remove the three verbatim canonical-rule bodies per DEC-002.
  (1) Replaced the `<!-- Shared partial: reconcile-policy. -->`
  block (lines ~130-132 — the `{% include "modules/references/reconcile-policy.md" %}`
  and bounding marker comments) with the DEC-002 reconcile-policy
  invariant prose plus a pointer to
  `{{ speccy_references_path }}/reconcile-policy.md`. (2) Replaced
  the `<!-- Shared rule: retry-shape. -->` block (lines ~209-291 —
  the full Rule statement / Read-only scope / Worked example 1 /
  Worked example 2 / Edge case body) with the DEC-002 retry-shape
  invariant prose plus a pointer to
  `{{ speccy_references_path }}/retry-shape.md`. (3) Replaced the
  `{% include "modules/skills/partials/vet-phases.md" %}` line
  (~341) with the DEC-002 vet-phases pointer prose, using a
  `{% if host == "claude-code" %}` Jinja conditional to substitute
  the host-specific ejected path
  (`.claude/skills/speccy-vet/SKILL.md` vs
  `.agents/skills/speccy-vet/SKILL.md`). Left
  `resources/modules/skills/speccy-vet.md` untouched — it still
  includes the vet-phases partial as canonical owner per DEC-002.
  Ran `just reeject`; only the orchestrate ejected files changed
  (`.claude/skills/speccy-orchestrate/SKILL.md` and
  `.agents/skills/speccy-orchestrate/SKILL.md`).
- Undone: none — all three substitutions in scope for T-002 are
  landed. T-003 picks up the remaining consumer files
  (`speccy-review.md` reconcile include and
  `phases/speccy-work.md` retry-shape inline body); T-004 switches
  the speccy-work wrappers to pure-include.
- Hygiene checks:
  - cargo test --workspace: exit 0
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0
- Evidence: refactor is prose-only over Jinja templates; verified
  by grep over the source module and the two ejected files.
  Roll call for CHKs under REQ-001:
  - CHK-001 (no inline retry-shape rule body, no reconcile-policy
    include, no vet-phases include in
    `modules/skills/speccy-orchestrate.md`): demonstrated → grep
    for `is in **retry shape** at`, `commit_without_state` adjacent
    to `auto_fixable`, and `### Phase 0 — bootstrap` returns no
    matches in the source file; visual inspection confirms each
    former site now carries DEC-002 invariant prose plus a pointer.
  - CHK-002 (ejected
    `.claude/skills/speccy-orchestrate/SKILL.md` and
    `.agents/skills/speccy-orchestrate/SKILL.md` carry no verbatim
    retry-shape statement, no reconcile-policy table row, no Phase
    0/1/2/3 grammar): demonstrated → same grep over the two
    ejected files returns no matches.
  - CHK-003 (`modules/skills/speccy-vet.md` still carries the
    vet-phases include): demonstrated → grep for `vet-phases.md`
    in `modules/skills/speccy-vet.md` returns the include line
    intact; the ejected `.claude/skills/speccy-vet/SKILL.md` still
    contains the `### Phase 0 — bootstrap` heading per the
    canonical-owner exception.
- Discovered issues: none.
- Procedural compliance: First-attempt shape (no prior T-002
  journal, clean tree before edits). Edited source modules under
  `resources/` only; ran `just reeject` to regenerate ejected
  output; no direct edits to files under `.claude/`, `.agents/`,
  or `.codex/`. All four hygiene gates exited 0 before flipping
  the task `state` from `in-progress` to `in-review`.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-7[1m]/high" round="1" date="2026-05-27T10:30:00Z">
T-002 lands the three DEC-002 substitutions cleanly (reconcile-policy invariant, retry-shape invariant, vet-phases pointer), the ejected files at `.claude/skills/speccy-orchestrate/SKILL.md` and `.agents/skills/speccy-orchestrate/SKILL.md` no longer carry the verbatim rule bodies, and the canonical-owner exception in `resources/modules/skills/speccy-vet.md` is preserved. CHK-001 / CHK-002 / CHK-003 are demonstrated.

What's blocking: the edit left stale connective prose orphaned to its own change in `resources/modules/skills/speccy-orchestrate.md`. Per AGENTS.md "Surgical changes" → "Clean up orphans your changes created," these need fixing in T-002 rather than getting reeject-baked into the ejected output:

- Line 145 (Startup integrity check): "If `next_action.kind == "reconcile"`, run the reconcile pass per **the shared partial above**, then re-query and continue." There is no shared partial above anymore — the include was replaced with the DEC-002 invariant prose. The "per the shared partial above" pointer is now a dead reference.
- Lines 264-267 (Ship dispatch): "The shared partial below is the single source of truth, included by both this orchestrator's ship dispatch and the `{{ cmd_prefix }}speccy-vet` skill body." After the refactor, the orchestrator no longer includes the vet-phases partial; only speccy-vet does. The "included by both" claim is now factually false, directly contradicting the DEC-002 invariant prose the implementer added two lines below.

Both stale snippets feed the same LLM that the SPEC's primary user story is engineered to serve. Telling that LLM to look for a "shared partial above/below" that no longer exists, or asserting the orchestrator co-owns a partial it now only points to, is the kind of comprehension hazard this refactor is supposed to eliminate, not introduce.

Fix: rewrite both passages so the prose reflects the post-refactor structure. Then re-eject.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" round="1" date="2026-05-27T10:30:00Z">
T-002's task-scenarios are reviewer-judgment scenarios over a prose-only Jinja-template refactor; no executable tests are owed. Independently verified each scenario: (1) `resources/modules/skills/speccy-orchestrate.md` no longer carries the inline retry-shape rule, the reconcile-policy include, or the vet-phases include — each site now carries DEC-002 invariant prose plus a pointer. (2) Greps for `is in **retry shape** at`, `commit_without_state`, and `### Phase 0 — bootstrap` against `.claude/skills/speccy-orchestrate/SKILL.md` and `.agents/skills/speccy-orchestrate/SKILL.md` all return zero matches; both ejected files render the DEC-002 substitutions with correct host-specific pointer paths. (3) `resources/modules/skills/speccy-vet.md` still carries `{% include "modules/skills/partials/vet-phases.md" %}` unchanged — canonical-owner exception preserved. Journal Evidence roll call labels all three SPEC-level CHKs `demonstrated` with specific grep targets; no fabrication smell. Hygiene gates pass.
</review>

<review persona="security" verdict="pass" model="claude-opus-4.7[1m]/low" round="1" date="2026-05-27T10:30:00Z">
Prose-only refactor of `resources/modules/skills/speccy-orchestrate.md` swapping verbatim canonical-rule bodies for invariant summaries plus pointers. No code paths touched, no auth/authz boundaries, no secret handling, no I/O surface, no crypto. The new Jinja constructs (`{{ speccy_references_path }}`, `{% if host == "claude-code" %}`) use template variables already present elsewhere in the same wrapper and resolve to static literals at render time — no template-injection vector introduced. The host conditional emits one of two hardcoded paths; no user-controlled data flows in. Nothing to flag.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" round="1" date="2026-05-27T10:30:00Z">
`resources/modules/skills/speccy-orchestrate.md` retains stale prose at three sites after T-002's substitutions:

1. **Line 113** (Startup integrity check section): "dispatch the reconcile pass per the shared partial inlined immediately below" — there is no longer a shared partial inlined below; the reconcile-policy block is now DEC-002 pointer prose.

2. **Line 145**: "run the reconcile pass per the shared partial above" — same stale reference; no shared partial exists above that point any longer.

3. **Lines 264–267** (Ship dispatch section): "The shared partial below is the single source of truth, included by both this orchestrator's ship dispatch and the `{{ cmd_prefix }}speccy-vet` skill body." — the vet-phases include was removed by T-002 and replaced with the DEC-002 pointer; this sentence now describes a phantom include relationship. Rewrite to reflect that the vet-phases grammar lives canonically in `speccy-vet.md` and that this site carries a DEC-002 pointer rather than an inline partial.

These are orphaned cross-reference sentences that T-002 created when it removed the three `{% include %}` / verbatim-copy sites but did not update the surrounding guide prose. The sentences actively misdirect an agent reading the file about the structural relationship between orchestrate and the canonical rule files. Update lines 113, 145, and 264–267 to match the new DEC-002 pointer structure before this task passes style review.
</review>

<blockers date="2026-05-27T10:30:00Z" round="1">
Rewrite stale cross-reference prose in `resources/modules/skills/speccy-orchestrate.md` that was orphaned by the three DEC-002 substitutions, then re-eject.

- Line 113 (Startup integrity check): "dispatch the reconcile pass per the shared partial inlined immediately below" — there is no longer a shared partial inlined below; rewrite to point at the DEC-002 invariant prose and the canonical reference file.
- Line 145: "run the reconcile pass per the shared partial above" — same stale reference; rewrite analogously.
- Lines 264-267 (Ship dispatch): "The shared partial below is the single source of truth, included by both this orchestrator's ship dispatch and the `{{ cmd_prefix }}speccy-vet` skill body." — the orchestrator no longer includes the vet-phases partial; only speccy-vet does. Rewrite to reflect that the vet-phases grammar lives canonically in `speccy-vet.md` and that this site carries a DEC-002 pointer.

After the prose fix, run `just reeject` to propagate to `.claude/skills/speccy-orchestrate/SKILL.md` and `.agents/skills/speccy-orchestrate/SKILL.md`, then re-run the four-gate hygiene suite.
</blockers>

<implementer round="2" date="2026-05-27T13:00:00Z" model="claude-opus-4.7[1m]/low">
- Completed: Rewrote the three stale cross-reference passages in
  `resources/modules/skills/speccy-orchestrate.md` flagged by the
  round-1 business and style reviewers. (1) Line 113 (Startup
  integrity check, Reconcile-pass dispatch paragraph): "dispatch
  the reconcile pass per the shared partial inlined immediately
  below" → "dispatch the reconcile pass per the **Reconcile
  policy** summary below (canonical policy at
  `{{ speccy_references_path }}/reconcile-policy.md`)". (2) Line
  145 (numbered step 2 under the Reconcile policy heading): "run
  the reconcile pass per the shared partial above" → "run the
  reconcile pass per the **Reconcile policy** summary above
  (canonical policy at `{{ speccy_references_path }}/reconcile-policy.md`)".
  (3) Lines 264-267 (Ship dispatch lead paragraph): replaced the
  "shared partial below is the single source of truth, included by
  both this orchestrator's ship dispatch and the speccy-vet skill
  body" claim with prose stating the vet-phases grammar lives
  canonically in the speccy-vet skill body (which includes the
  partial) and this site carries only a pointer summary. Ran
  `just reeject`; only the two orchestrate ejected files changed.
- Undone: none — all three round-1 blocker items addressed.
- Hygiene checks:
  - cargo test --workspace: exit 0
  - cargo clippy --workspace --all-targets --all-features -- -D warnings: exit 0
  - cargo +nightly fmt --all --check: exit 0
  - cargo deny check: exit 0
- Evidence: amend is prose-only over the same Jinja-template file
  touched in round 1; the three flagged passages no longer
  reference a "shared partial above/below" or claim the
  orchestrator co-owns the vet-phases partial.
  Roll call for CHKs under REQ-001:
  - CHK-001 (no inline retry-shape rule body, no reconcile-policy
    include, no vet-phases include in
    `modules/skills/speccy-orchestrate.md`): demonstrated → state
    from round 1 preserved; the round-2 amend only rewrote
    connective prose, did not reintroduce any of the three
    removed bodies.
  - CHK-002 (ejected
    `.claude/skills/speccy-orchestrate/SKILL.md` and
    `.agents/skills/speccy-orchestrate/SKILL.md` carry no verbatim
    retry-shape statement, no reconcile-policy table row, no Phase
    0/1/2/3 grammar): demonstrated → `just reeject` regenerated
    both ejected files; the round-1 grep targets remain absent.
  - CHK-003 (`modules/skills/speccy-vet.md` still carries the
    vet-phases include): demonstrated → untouched in this round;
    the round-2 amend's lines 264-267 rewrite explicitly names
    speccy-vet as the canonical owner of the partial.
- Discovered issues: none.
- Procedural compliance: Retry shape (round-1 journal carried
  matching `<implementer round="1">` and `<blockers round="1">`).
  Amended the existing WIP in the working tree in place — no
  `git restore`, no `git clean`, no rewrite from scratch. Edited
  the source module under `resources/` only; ran `just reeject`
  to regenerate the ejected output; no direct edits to files
  under `.claude/` or `.agents/`. All four hygiene gates exited 0
  before flipping the task `state` from `in-progress` to
  `in-review`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" round="2" date="2026-05-27T13:30:00Z">
T-002 round 2 lands clean against the business persona criteria. The three round-1 stale cross-reference passages were all rewritten correctly: line 113 now points at the "**Reconcile policy** summary below" matching the DEC-002 invariant prose, line 145 points at the "**Reconcile policy** summary above" likewise, and the Ship dispatch lead paragraph at lines 262-270 now truthfully states that the vet-phases grammar "lives canonically in the `{{ cmd_prefix }}speccy-vet` skill body (which includes the `modules/skills/partials/vet-phases.md` partial)" and that this site carries only a pointer summary. The remaining "shared partial below" occurrence at line 243 is the Review dispatch's `review-fanout.md` reference — that include is genuinely still present and is out of T-002's scope.

REQ-001 done-when (orchestrate portion): verbatim retry-shape rule sentence, reconcile-policy table, and Phase 0/1/2/3 grammar are all absent from `resources/modules/skills/speccy-orchestrate.md` and the two ejected `speccy-orchestrate/SKILL.md` files. CHK-001/CHK-002/CHK-003 demonstrated for the orchestrate site. Canonical-owner exception in `resources/modules/skills/speccy-vet.md` preserved. DEC-002 invariant wording carries the load-bearing trigger plus pointer — serving user story #1. Non-goals respected.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" round="2" date="2026-05-27T13:30:00Z">
Round 2's amend is prose-only over `resources/modules/skills/speccy-orchestrate.md` connective text and changes nothing about the test-coverage shape for T-002's scenarios, which are reviewer-judgment-only over a Jinja-template refactor — no executable tests are owed. Independently re-verified each CHK against the current tree: (CHK-001) greps for `is in **retry shape** at`, `commit_without_state`, and `### Phase 0 — bootstrap` against `resources/modules/skills/speccy-orchestrate.md` all return zero matches; each former site carries DEC-002 invariant prose plus a pointer. (CHK-002) same three greps against `.claude/skills/speccy-orchestrate/SKILL.md` and `.agents/skills/speccy-orchestrate/SKILL.md` all return zero matches. (CHK-003) `resources/modules/skills/speccy-vet.md` still carries `{% include "modules/skills/partials/vet-phases.md" %}` unchanged and `.claude/skills/speccy-vet/SKILL.md` retains `### Phase 0 — bootstrap` — canonical-owner exception preserved. Hygiene gates exit 0.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/medium" round="2" date="2026-05-27T13:30:00Z">
Round-2 amend is a prose-only rewrite of three connective sentences in `resources/modules/skills/speccy-orchestrate.md` (lines ~113, ~145, ~264-273) plus the corresponding regenerated bytes in the two ejected `SKILL.md` files. No code paths touched, no auth/authz boundaries, no secret handling, no I/O surface, no crypto, no new dependencies. The only Jinja constructs in the new prose (`{{ speccy_references_path }}`, `{% if host == "claude-code" %}…{% else %}…{% endif %}`) reuse template variables already present elsewhere in the same wrapper and resolve to hardcoded literal strings at render time — no template-injection vector, no user-controlled data flows in. Round-1 security verdict carries forward unchanged.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" round="2" date="2026-05-27T13:30:00Z">
All three round-1 blocking passages are resolved in the current on-disk state of `resources/modules/skills/speccy-orchestrate.md`.

- Former line 113: now "per the **Reconcile policy** summary below (canonical policy at `{{ speccy_references_path }}/reconcile-policy.md`)" — dead "shared partial inlined immediately below" reference removed.
- Former line 145: now "per the **Reconcile policy** summary above (canonical policy at `{{ speccy_references_path }}/reconcile-policy.md`)" — dead "shared partial above" reference removed.
- Former lines 264-267: now states the vet-phases grammar lives canonically in the speccy-vet skill body and "this site carries only a pointer summary" — the phantom "included by both" claim is gone.

The Review dispatch section (lines 243-258) retains "The shared partial below" and "the partial above" language, but that section still legitimately includes `{% include "modules/skills/partials/review-fanout.md" %}` — those references are accurate, not orphaned. No new stale cross-references, dead identifiers, or naming inconsistencies introduced by the round-2 amend. Ejected files regenerated via `just reeject`.
</review>
