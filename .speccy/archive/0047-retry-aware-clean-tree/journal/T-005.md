---
spec: SPEC-0047
task: T-005
generated_at: 2026-05-26T21:30:00Z
---

<implementer round="1" date="2026-05-26T21:30:00Z" model="claude-opus-4.7[1m]/low">

- Completed: Added the REQ-004 bootstrap commit step to the canonical
  `/speccy-decompose` recipe at
  `resources/modules/phases/speccy-decompose.md`. Inserted a new step 4
  between the existing `speccy lock SPEC-NNNN` step and the
  "Suggest the next step" line, and renumbered the suggest-next-step
  line to 5. The new step documents:
  - Narrow staging via `git add <spec-dir>/SPEC.md <spec-dir>/TASKS.md`,
    with explicit prose forbidding `git add -A` and `git add .`.
  - Idempotency via `git diff --cached --quiet` (exit 0 skips commit
    silently; non-zero proceeds to commit).
  - Commit title `[SPEC-NNNN]: create spec and decompose tasks` with
    `SPEC-NNNN` substituted at runtime.
  - Body sourced from the SPEC's `title:` YAML frontmatter value
    (trimmed).
  - `Co-Authored-By: <model> <noreply@anthropic.com>` trailer per
    SPEC-0045/REQ-004's host-harness convention, including the
    `Speccy Skill Pack` fallback when the host exposes no model.
  - HEREDOC invocation pattern so the title-with-special-chars body
    survives verbatim.
  Regenerated host-portable mirrors via `cargo run -- init --force`
  for both `claude-code` and `codex`; the renderer overwrote
  `.claude/agents/speccy-decompose.md` and
  `.codex/agents/speccy-decompose.toml` with the rebuilt phases body.
  Skill bodies at `.claude/skills/speccy-decompose/SKILL.md` and
  `.agents/skills/speccy-decompose/SKILL.md` were left unchanged
  because they are thin wrappers that defer to the agent prompt
  (matches the task-scenario "option (a)" branch).

- Undone: Nothing in scope. Out-of-scope per the SPEC Non-goals: no
  analogous bootstrap commit step for `/speccy-plan` or
  `/speccy-amend`.

- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites pass).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok, sources ok).

- Evidence: this task touches docs/prompts only — no Rust code path
  changed and no new unit test was added. Roll call for CHKs under
  REQ-004:
  - CHK-009 (commit shape: title + name-only listing + clean status
    after a fresh decompose): judgment-only → no scriptable proof
    in the workspace; reviewer-business judges by reading the new
    step 4 prose in `resources/modules/phases/speccy-decompose.md`
    and confirming the title literal, the narrow `git add` listing
    both `SPEC.md` and `TASKS.md`, and the HEREDOC body source.
  - CHK-010 (narrow staging scope; unrelated dirty paths remain
    untouched): judgment-only → reviewer reads the new step 4 and
    confirms the explicit `git add -A` / `git add .` prohibition
    and the narrow file-list `git add <spec-dir>/SPEC.md
    <spec-dir>/TASKS.md` invocation.
  - CHK-011 (idempotency on re-run against already-committed
    artefacts): judgment-only → reviewer reads step 4 sub-step 2
    and confirms the `git diff --cached --quiet` guard plus the
    documented silent skip behaviour.
  Mirror sync is demonstrated by the `speccy init --force` runs
  for both hosts producing `1 overwritten, 31 unchanged` (the
  single overwrite was `speccy-decompose.md` / `.toml`
  respectively), proving the canonical phases file is the sole
  source and the templating pipeline propagates it.

- Discovered issues: None during implementation. The `cmd_prefix`
  Jinja substitution flows cleanly through the existing renderer for
  the new step 4 body.

- Procedural compliance: Followed the speccy-work recipe. Entry
  precondition was clean. Edited the canonical
  `resources/modules/phases/speccy-decompose.md` only (single source
  of truth) and let the renderer fan out to the two host-portable
  copies; did not edit the rendered outputs directly. Did not touch
  the skill body wrappers because they defer to the agent prompt
  (option (a) in the task-scenarios). Ran the four hygiene gates
  before flipping `state` to `in-review`.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-26T22:30:00Z" round="1">
T-005 satisfies SPEC-0047/REQ-004 cleanly. Canonical phases body inserts a new step 4 between `speccy lock SPEC-NNNN` and "Suggest the next step": narrow `git add <spec-dir>/SPEC.md <spec-dir>/TASKS.md` with explicit prohibition on `git add -A` / `git add .`; `git diff --cached --quiet` idempotency guard with silent skip on exit 0; title `[SPEC-NNNN]: create spec and decompose tasks`; body from trimmed `title:` frontmatter; single `Co-Authored-By: <model>` trailer with `Speccy Skill Pack` fallback matching SPEC-0045/REQ-004 verbatim; HEREDOC invocation pattern for special characters. Mirrors regenerated correctly via the templating pipeline. Skill-body wrappers correctly left untouched (option (a)). Goals/non-goals respected.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
Docs-only task; all three CHKs (009/010/011) correctly labelled `judgment-only` with plausible reason (/speccy-decompose is a skill invocation, not a CLI subcommand, so end-to-end git observations cannot be driven from a deterministic test harness). The diff documents narrow `git add`, idempotency guard, title literal, body source, trailer convention, and HEREDOC pattern verbatim matching REQ-004. Both skill body wrappers correctly identified as thin "read the agent prompt" stubs matching option-(a) branch. `cargo test --workspace` re-run completes 0 failures. One nit (non-blocking): task-scenario 2 reads "grepped for `git add -A` ... zero matches found" but the prose contains two matches as prohibition language — the scenario's intent (that the *invocation* uses narrow staging) is satisfied; prohibition prose is a feature.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
No exploitable security surface. Single-quoted HEREDOC delimiter (`<<'EOF'`) correctly suppresses all shell expansion inside the body, so a SPEC `title:` value containing shell metacharacters cannot be evaluated. Narrow `git add` form uses two fixed schema-known paths resolved from the CLI's own JSON output — no path traversal or accidental sweep. The `git diff --cached --quiet` guard is read-only. No secrets embedded, no tokens logged. All three rendered copies byte-identical in the new step — no divergence introducing a weaker form in one host.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
The HEREDOC example at `resources/modules/phases/speccy-decompose.md` lines 107-114 (and the rendered mirrors at `.claude/agents/speccy-decompose.md` lines 113-120 and `.codex/agents/speccy-decompose.toml`) indents the closing `EOF` and `)"` with 6 spaces inside the fenced code block. This is both a style inconsistency AND a functional bug: bash `<<'EOF'` requires the closing delimiter at column 0 with no leading whitespace; the indented form would never terminate the heredoc. Every other HEREDOC example in the project places `EOF` and `)"` at column 0 inside the fenced block — `.claude/skills/speccy-orchestrate/SKILL.md:563-570`, `.claude/skills/speccy-review/SKILL.md:337-344`, and `resources/modules/skills/partials/review-fanout.md:137-144` all follow this pattern consistently. Fix the canonical template source and let the rendering pipeline regenerate the two host mirrors.
</review>

<blockers date="2026-05-26T22:32:00Z" round="1">
One blocker landed in round 1 (style). Business, tests, security passed.

1. **HEREDOC closing delimiter indented (style + functional bug).** The exemplar HEREDOC at `resources/modules/phases/speccy-decompose.md` lines 107-114 indents the closing `EOF` and `)"` with 6 spaces inside the fenced block:

   ```
         git commit -m "$(cat <<'EOF'
         [SPEC-NNNN]: create spec and decompose tasks

         <SPEC title from frontmatter>

         Co-Authored-By: <model> <noreply@anthropic.com>
         EOF
         )"
   ```

   Bash `<<'EOF'` requires the closing `EOF` at column 0 with no leading whitespace. An agent literally copying the indented form would produce a heredoc that never terminates. All other project HEREDOC examples (`.claude/skills/speccy-orchestrate/SKILL.md:563-570`, `.claude/skills/speccy-review/SKILL.md:337-344`, `resources/modules/skills/partials/review-fanout.md:137-144`) place the closing delimiter at column 0.

   **Fix:** Edit the canonical `resources/modules/phases/speccy-decompose.md` to dedent the heredoc body and closing delimiter so the closing `EOF` and `)"` sit at column 0 inside the fenced code block (the leading whitespace inside the fenced block is preserved literally by the parser; only the heredoc body / closing delimiter indentation needs to drop). Then re-run `speccy init --force` so both host mirrors (`.claude/agents/speccy-decompose.md` and `.codex/agents/speccy-decompose.toml`) regenerate.

The mechanical floor (the step 4 insertion, narrow git add, idempotency guard, title literal, body source, trailer convention) is clean and reusable for the retry. The semantic half (the HEREDOC exemplar being a working bash recipe rather than a syntactically invalid example) must land for T-005 to satisfy style review.
</blockers>

<implementer round="2" date="2026-05-26T23:00:00Z" model="claude-opus-4.7[1m]/low">

- Completed: Amended the round-1 WIP in place per the round-1 style
  blocker. Dedented the HEREDOC exemplar in the canonical
  `resources/modules/phases/speccy-decompose.md` (fenced code block
  around lines 107-114) so the body lines, the closing `EOF`, and
  the closing `)"` all sit at column 0 inside the fenced bash block.
  This restores a functionally-correct heredoc (bash `<<'EOF'`
  requires the closing delimiter at column 0) and aligns with the
  project's other heredoc exemplars. Regenerated both host mirrors
  via `cargo run -- init --force` for the `claude-code` host and
  `cargo run -- init --force --host codex` for the `codex` host;
  both runs reported `(!) overwritten` on the respective
  speccy-decompose artefact. Verified by re-reading
  `.claude/agents/speccy-decompose.md` lines 113-120 and
  `.codex/agents/speccy-decompose.toml` lines 112-119 — both now
  show the heredoc body and closing delimiters at column 0 inside
  the fenced block. No other edits.

- Undone: Nothing in scope. Did not touch any non-blocker prose; did
  not re-edit the round-1 step 4 content; did not run `git restore`
  / `git clean` / `git checkout`.

- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok, sources ok).

- Evidence: docs/prompts amend only. Round-2 roll call for CHKs
  under REQ-004 (unchanged labels from round 1; this amend only
  fixed the heredoc exemplar shape):
  - CHK-009 (commit shape): judgment-only → reviewer-style now
    confirms the heredoc exemplar is a working bash recipe (closing
    `EOF` at column 0).
  - CHK-010 (narrow staging): judgment-only → unchanged from round 1.
  - CHK-011 (idempotency): judgment-only → unchanged from round 1.

- Discovered issues: `speccy init` only re-renders the host pack
  matching the current default; the codex pack needs an explicit
  `--host codex` invocation. The round-1 implementer prose claimed
  both mirrors regenerated from a single `init --force`, but in
  practice this requires two invocations. Surfaced here for
  awareness; not a procedural blocker for this round.

- Procedural compliance: Followed the speccy-work retry-mode recipe
  per SPEC-0047/REQ-003 — detected retry shape from the journal
  (round-1 implementer + round-1 blockers), amended the WIP in
  place without `git restore`/`git clean`/`git checkout`, monotonic
  round increment (1 → 2), `Completed` describes only the amend
  (not the cumulative task work). Hygiene gate passed before
  flipping `state` to `in-review`.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-26T23:30:00Z" round="2">
Round-2 amend cleanly resolves the round-1 style blocker (HEREDOC closing delimiter dedented to column 0) without disturbing REQ-004 contract. All six done-when clauses still map to documented prose. Mirrors regenerated correctly. Non-goals respected. User stories served. Round-2 `Completed` describes only the amend per REQ-003 retry-mode convention. The init-host-flag discovered-issue is procedural awareness, not a REQ-004 gap.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-2 amend tightly scoped to round-1 style blocker. `cargo test --workspace` re-runs clean. Evidence roll call CHKs 009/010/011 remain `judgment-only` with plausible reason. Procedural compliance to REQ-003 retry-mode correct: in-place amend, monotonic round increment, `Completed` describes only the amend, no destructive git invocations.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-2 amend dedents the HEREDOC exemplar; security posture identical to round 1 and remains clean. Single-quoted `<<'EOF'` still suppresses shell expansion. Narrow `git add` unchanged. No new secrets, auth surfaces, or dependencies.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-1 blocker resolved. All three files (canonical + Claude mirror + Codex mirror) have the HEREDOC body and closing `EOF` / `)"` at column 0 inside the fenced bash block, matching the project-wide convention. No other style issues.
</review>
