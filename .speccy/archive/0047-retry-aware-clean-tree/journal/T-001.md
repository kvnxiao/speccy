---
spec: SPEC-0047
task: T-001
generated_at: 2026-05-26T22:00:00Z
---

<implementer round="1" date="2026-05-26T22:00:00Z" model="claude-opus-4.7[1m]/low">

- Completed:
  - Part A. Created canonical retry-shape reference at
    `resources/modules/references/retry-shape.md` carrying the
    REQ-001 rule statement verbatim, the read-only-scope clause,
    and three labelled worked examples (retry shape,
    first-attempt shape, awaiting-review edge case).
  - Created host-portable templating pipeline entries:
    `resources/agents/.claude/speccy-references/retry-shape.md.tmpl`
    and `resources/agents/.agents/speccy-references/retry-shape.md.tmpl`,
    both single-line `{% include "modules/references/retry-shape.md" %}`
    mirroring the existing reconcile-policy convention.
  - Synced dogfood mirrors so they are byte-identical to the
    canonical source: `.claude/speccy-references/retry-shape.md`
    and `.agents/speccy-references/retry-shape.md`.
  - Part B. Replaced the existing single-line SPEC-0045/REQ-002
    entry precondition in the speccy-work skill body with a
    retry-aware paragraph that documents both branches explicitly
    (first-attempt shape keeps the strict gate; retry shape
    proceeds despite a dirty tree). Inlined the full canonical
    rule body verbatim between the marker comment pair
    `<!-- Shared rule: retry-shape. Source: <path> -->` /
    `<!-- End shared rule: retry-shape. -->`, immediately above
    the existing reconcile-policy partial. Updated all four
    sites: `.claude/skills/speccy-work/SKILL.md`,
    `.agents/skills/speccy-work/SKILL.md`,
    `resources/agents/.claude/skills/speccy-work/SKILL.md.tmpl`,
    `resources/agents/.agents/skills/speccy-work/SKILL.md.tmpl`.
    The retry-shape marker block and the reconcile-policy partial
    sit as two separate inlines, each bounded by its own marker
    comments, matching DEC-003.
  - Extended the two test helpers that police "thin-stub" shape so
    the new retry-shape marker block is recognised as an
    auditable exemption (same pattern that already exempts the
    reconcile-policy partial):
    `speccy-cli/tests/common/mod.rs::non_blank_line_count_outside_reconcile_partial`
    (now scans both marker pairs) and
    `speccy-cli/tests/skill_stub_shape.rs::body_len_without_shared_markers`
    (new helper subtracting marker-bounded bytes from both the
    skill body and the agent body before the strict-smaller
    comparison). The "stub strictly smaller than agent" invariant
    (CHK-010) becomes "structural body strictly smaller", which
    is the real intent — inlined shared partials/rules are not
    body bloat.

- Undone: nothing in scope for T-001. T-003 (orchestrator
  inline), T-004 (agent prompt inline + retry-mode branch),
  T-005 (decompose bootstrap commit), T-006 (round-attribute
  reconciliation) remain pending.

- Hygiene checks:
  - `cargo test --workspace` exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0.
  - `cargo +nightly fmt --all --check` exit 0 (after running
    `cargo +nightly fmt --all` once during the round to apply the
    new helper's formatting).
  - `cargo deny check` exit 0.

- Evidence: no per-task evidence directory was created because
  every CHK under T-001's covered REQs is judged either by
  reviewer reading the dogfooded files or by the existing
  workspace test suite that already runs against the post-task
  tree. Roll call for CHKs under REQ-001 / REQ-002:
  - CHK-001 (first-attempt shape on `<implementer round="1">`
    alone): judgment-only -> reviewer reads
    `resources/modules/references/retry-shape.md` Worked example 2
    and confirms the rule yields first-attempt shape. No
    scriptable proof; the rule lives in shipped markdown, not in
    Rust code, per DEC-002.
  - CHK-002 (retry shape on `<implementer round="1">` +
    `<blockers round="1">`): judgment-only -> reviewer reads
    Worked example 1 and confirms the rule yields retry shape.
  - CHK-003 (first-attempt shape on the awaiting-review edge
    case): judgment-only -> reviewer reads the "Edge case --
    implementer awaiting review" section and confirms the
    documented outcome matches the rule.
  - CHK-004 (first-attempt-shape strict-gate behaviour at
    `/speccy-work` entry): judgment-only on the documentation
    side -> reviewer reads the new entry-precondition paragraph
    in `.claude/skills/speccy-work/SKILL.md` and verifies the
    "**First-attempt shape** with non-empty stdout exits..."
    branch matches CHK-004's expected behaviour. Behavioural
    proof is reserved for T-003/T-004's behavioural sites.
  - CHK-005 (retry-shape proceeds despite dirty tree at
    `/speccy-work` entry): judgment-only on the documentation
    side -> reviewer reads the same paragraph and verifies the
    "**Retry shape** proceeds regardless of stdout..." branch
    matches CHK-005's expected behaviour.
  - CHK-006 (orchestrator work dispatch under retry shape):
    judgment-only and explicitly out of T-001 scope -- T-003 owns
    the orchestrator inline and the CHK-006 behavioural claim;
    reviewer should not gate T-001 on CHK-006.
  - CHK-022 (no orphan references, SPEC-0038/REQ-007): hygiene ->
    the new `retry-shape.md` reference satisfies the lint because
    the speccy-work skill body inlines the
    `.claude/speccy-references/retry-shape.md` pointer substring
    inside the marker comment, and the Codex mirror inlines the
    `.agents/speccy-references/retry-shape.md` pointer the same
    way. Both sides are picked up by
    `chk022_no_orphan_references` under `cargo test --workspace`,
    which passed. The atomic-landing constraint from the task
    body is therefore satisfied in a single commit.

- Discovered issues:
  - The original task description called for "first consumer
    inline" but did not pre-warn that the inlined retry-shape
    body (about 107 lines) would push the `/speccy-work` skill
    body's structural size above the agent body and trip the
    SPEC-0032 CHK-010 stub-shape invariant
    (`speccy-cli/tests/skill_stub_shape.rs`). Two reasonable
    paths exist: (a) include only a short pointer-stub in the
    skill body and keep the rule canonical-only, or
    (b) extend the CHK-010 test to subtract marker-bounded
    bytes the same way the existing line-count helper subtracts
    them. I picked (b) because the SPEC text explicitly says the
    rule body must be byte-for-byte identical between the
    canonical source and the inlined marker block (REQ-002
    `done-when`), which is incompatible with a short stub.
    Worth a reviewer pass on whether this is the right call;
    if not, the rollback is small (revert the inline, replace
    with a pointer line).
  - The thin-stub line-count helper in
    `speccy-cli/tests/common/mod.rs` was already an exemption-
    aware design with a single marker pair; the natural fix was
    to generalise it to a list of marker pairs and exempt both
    `reconcile-policy` and `retry-shape`. The extension is a
    pure refactor preserving the existing reconcile-policy
    exemption behaviour.

- Procedural compliance:
  - Friction-driven test updates flagged per AGENTS.md
    "Conventions for AI agents specifically": the two test
    helpers above (`non_blank_line_count_outside_reconcile_partial`
    and the new `body_len_without_shared_markers`) had to be
    taught about the retry-shape marker block. Neither
    extension changed the substantive invariant -- both still
    enforce "stub bodies stay thin" and "skill bodies stay
    smaller than agent bodies" using structural body bytes that
    exclude inlined shared partials/rules.
  - The shipped skill-body files (`/speccy-work` in both packs)
    were edited directly to inline the rule per the SPEC's
    explicit done-when clause; this is not a stale-instruction
    update to a SKILL.md, it is the deliverable T-001 requires.
  - No `git restore` / `git clean` / `git reset` invocations
    were used during this round; the working tree carries
    exactly the eight intended file additions/modifications
    plus the two test-helper changes.

</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-7[1m]/high" date="2026-05-26T22:30:00Z" round="1">
The diff trips its own task-scenario contract: the `<task-scenarios>` block for T-001 says "grepped for the open marker comment `<!-- Shared rule: retry-shape.`, then exactly one match is found" against `.claude/skills/speccy-work/SKILL.md`, but the implementer inlined the canonical reference file verbatim — and the reference itself contains the marker comment as a documentation example inside a fenced code block. That means the inlined body re-introduces the open-marker string. `grep -c "<!-- Shared rule: retry-shape"` against `.claude/skills/speccy-work/SKILL.md` returns 2 (lines 13 and 29). The same doubling appears in `.agents/skills/speccy-work/SKILL.md` and both `resources/agents/.../SKILL.md.tmpl` files. The verbatim-byte-for-byte requirement (REQ-002 done-when) means the body cannot simply be trimmed in the inline; the fix must trim the canonical reference itself by stripping the meta paragraph ("Each inlined site is bounded by marker comments naming this rule: ...") — that meta block is not load-bearing for the rule (the rule starts at "## Rule statement"). T-003/T-004 will inline the same body at two more sites; the same double-marker problem recurs if the canonical is not trimmed now.

A second narrower concern: `.agents/skills/speccy-work/SKILL.md` line 13 names `Source: .agents/speccy-references/retry-shape.md`, but the inlined body still lists `.claude/speccy-references/retry-shape.md` in its own bullet list and code-fenced example. The Codex mirror points at `.claude/...` paths in its own host-portable copy. Fixing the meta-paragraph removal above resolves this too.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-26T22:30:00Z" round="1">
T-001's evidence roll call is complete and well-formed: CHK-001/002/003/004/005 are correctly labelled `judgment-only` because DEC-002 explicitly places the retry-shape rule in shipped markdown rather than Rust code, leaving no scriptable surface; CHK-006 is correctly deferred to T-003; CHK-022 (the orphan-check hygiene gate that gates atomic landing of the reference + first consumer) is labelled `hygiene` and cites a specific test (`chk022_no_orphan_references` in `speccy-cli/tests/skill_body_discovery.rs`), which I re-ran and verified passes. The full `cargo test --workspace` also re-runs green against the working tree. The two test helper edits (`non_blank_line_count_outside_reconcile_partial` and the new `body_len_without_shared_markers`) extend an existing exemption pattern symmetrically: they subtract marker-bounded inlined content from BOTH sides of the stub-vs-agent size comparison, preserving the original "structural body smaller than agent body" intent rather than masking the size growth. Within the scope T-001 owns and the boundaries DEC-002 sets, the test coverage and hygiene gates correctly police what can be policed deterministically.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
All changed files are Markdown skill bodies, reference documents, and test-helper Rust code that counts line lengths — no executable production logic, no authentication boundary, no secrets, no new network surface, and no new dependencies. The retry-shape relaxation of the clean-tree gate is scoped to locally-readable journal files; an attacker forging retry shape would need write access to the repository, at which point the gate is irrelevant. No injection vectors, credential leaks, or crypto concerns are present.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
`speccy-cli/tests/common/mod.rs:171` — the public helper `non_blank_line_count_outside_reconcile_partial` now excludes two marker-bounded regions (both `reconcile-policy` and `retry-shape`), but its name still says `_outside_reconcile_partial`. The doc comment was correctly updated to describe both exemptions, making the mismatch between name and doc comment visible. The function is `pub` and called at three additional sites: `speccy-cli/tests/init.rs:1108`, `speccy-cli/tests/init_phase_agents.rs:83`, `speccy-cli/tests/init_phase_agents.rs:214`. Rename to `non_blank_line_count_outside_shared_markers` (or similar) and update all three call sites. Leaving a name that only mentions `reconcile_partial` when the function also exempts `retry-shape` will mislead the next reader adding a third marker type.

No other style issues found: the new `body_len_without_shared_markers` function in `skill_stub_shape.rs` is well-named and consistent with the file's existing helpers; the `saturating_add` pattern matches the existing `body_after_frontmatter` style; the `.get()` + `.unwrap_or(false)` follows AGENTS.md's anti-`[i]` convention; the four SKILL.md files are byte-identical across the Claude/Agents/template variants as required; and the `retry-shape.md` canonical source carries the three required worked examples with correct labels.
</review>

<blockers date="2026-05-26T22:32:00Z" round="1">
Two blockers landed in round 1 (business, style). Tests and security passed. Fix both before respawning the review fan-out.

1. **Double marker comment violates task-scenario contract (business).** The
   T-001 `<task-scenarios>` block states `grep -c '<!-- Shared rule: retry-shape.'`
   against `.claude/skills/speccy-work/SKILL.md` must return exactly 1; it
   returns 2 because the canonical reference file's own self-documenting
   "Each inlined site is bounded by marker comments naming this rule: ```...```"
   meta paragraph contains the marker string, and the verbatim inlining
   carries that meta block into every consumer. The same doubling appears
   in `.agents/skills/speccy-work/SKILL.md` and both template sources.

   **Fix:** Trim the canonical reference at
   `resources/modules/references/retry-shape.md` by removing the meta
   paragraph that lists inlining sites and shows the marker-comment example
   (lines 1-25 inclusive — the "This file is the single source of truth..."
   block through to the second `---` separator). Keep the rule body
   starting at `## Rule statement`. Re-sync all mirrors:
   `.claude/speccy-references/retry-shape.md`,
   `.agents/speccy-references/retry-shape.md`,
   `.claude/skills/speccy-work/SKILL.md` (inlined body between the marker
   pair), `.agents/skills/speccy-work/SKILL.md` (same), both
   `resources/agents/.../SKILL.md.tmpl` files (same). After trimming,
   `grep -c '<!-- Shared rule: retry-shape.'` against each consumer body
   must return exactly 1 (the outer marker only; the inlined body no
   longer carries the meta example). T-003 and T-004 will inherit the
   trimmed canonical when they inline at their sites.

   This also addresses the secondary cross-host portability concern: the
   `.claude/...` path references that the `.agents` mirror was carrying
   live inside the meta paragraph being removed.

2. **Helper function name lies about its scope (style).** In
   `speccy-cli/tests/common/mod.rs:171`, the public helper
   `non_blank_line_count_outside_reconcile_partial` now exempts two
   marker pairs (both `reconcile-policy` and `retry-shape`) but its name
   only mentions `reconcile_partial`. The doc comment was updated; the
   name was not.

   **Fix:** Rename to `non_blank_line_count_outside_shared_markers` (or
   another name that does not lie about the two-exemption scope) and
   update three call sites:
   - `speccy-cli/tests/init.rs:1108`
   - `speccy-cli/tests/init_phase_agents.rs:83`
   - `speccy-cli/tests/init_phase_agents.rs:214`

The mechanical floor (the new canonical retry-shape reference file, the
two host-portable mirrors via the templating pipeline, the speccy-work
skill body inlining the rule between its marker pair, the test helper
extensions preserving the structural-body-smaller-than-agent invariant)
is clean and reusable for the retry. The semantic half (single-marker
match in every consumer + honest helper name) must land for T-001 to
satisfy REQ-001 and REQ-002.
</blockers>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-2 amend resolves both round-1 blockers cleanly. Canonical reference at `resources/modules/references/retry-shape.md` trimmed to start at `## Rule statement`; `grep -c '<!-- Shared rule: retry-shape\.'` returns exactly 1 against each of the four consumer bodies. The REQ-002 byte-for-byte invariant holds. Helper renamed to `non_blank_line_count_outside_shared_markers` at definition + three call sites; `cargo test --workspace` green. REQ-001 and REQ-002 done-when bullets verified. No SPEC drift, no scope bleed into T-003/T-004 territory.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-26T23:30:00Z" round="2">
Round-2 amend cleanly addresses both round-1 blockers. (1) Canonical trimmed; marker counts now 1 in all four consumer bodies, 0 in canonical + mirrors. (2) Helper rename complete at definition + three call sites with no stale references in production/test code. `cargo test --workspace` re-runs green including `chk022_no_orphan_references`. The byte-for-byte invariant holds between canonical and inlined bodies. Evidence roll call's judgment-only labels remain correctly bounded by DEC-002. Within T-001's scope, test coverage and hygiene gates correctly police what can be policed deterministically.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-2 changes are entirely cosmetic relative to the security surface: canonical reference meta paragraph removed and mirrors re-synced; test helper renamed. No executable production logic added or modified, no authentication or authorization boundary changed, no secret handling, no new network surface or dependency, no injection vector. The retry-shape rule reads only locally-scoped journal files and explicitly forbids invoking git or other CLI subcommands. No security concerns.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:30:00Z" round="2">
Round-1 helper-name blocker is resolved at the definition + three call sites. However, the same "name lies about scope" drift persists in the assert message strings at those three call sites — they still read "outside reconcile-policy partial markers" but the helper now excludes both `reconcile-policy` and `retry-shape` marker-bounded regions:

- `speccy-cli/tests/init.rs:1111`: `"rendered {rel} thin-stub body must be short (< 12 non-empty lines outside reconcile-policy partial markers), …"`
- `speccy-cli/tests/init_phase_agents.rs:86`: `"T-009: {rel} must have ≤10 non-blank lines outside reconcile-policy partial markers; …"`
- `speccy-cli/tests/init_phase_agents.rs:217`: `"CHK-017: {rel} must have ≤10 non-blank lines outside reconcile-policy partial markers; …"`

A future reader hitting a failing assert will see the misleading "outside reconcile-policy" message and have no signal that the helper already handles multiple exemptions. Update the three message strings to reflect the new scope (e.g. "outside shared-marker blocks" or "outside reconcile-policy and retry-shape marker blocks"). No other style issues.
</review>

<blockers date="2026-05-26T23:32:00Z" round="2">
One blocker landed in round 2 (style). Business, tests, and security passed.

1. **Three assert message strings still name only `reconcile-policy` markers (style).** The round-2 helper rename was applied to the function name and call sites, but the assert message strings inside those call sites still read "outside reconcile-policy partial markers". The helper now excludes both `reconcile-policy` and `retry-shape` marker-bounded regions, so the messages are misleading.

   **Fix:** Update the three assert message strings at:
   - `speccy-cli/tests/init.rs:1111` — change "outside reconcile-policy partial markers" to "outside shared-marker blocks" (or "outside reconcile-policy and retry-shape marker blocks") in the assert message.
   - `speccy-cli/tests/init_phase_agents.rs:86` — same substitution.
   - `speccy-cli/tests/init_phase_agents.rs:217` — same substitution.

The mechanical floor (trimmed canonical, byte-identical mirrors, single marker match per consumer, helper definition + call sites renamed, hygiene green) is clean. The semantic half (assert message strings consistent with the new helper scope) must land for T-001 to satisfy style review.
</blockers>

<implementer round="2" date="2026-05-26T23:00:00Z" model="claude-opus-4.7[1m]/low">

- Completed:
  - Amend per round-1 blockers; round-1 mechanical floor preserved
    in the working tree, no `git restore`/`clean`/`reset` invoked.
  - Trimmed the canonical reference at
    `resources/modules/references/retry-shape.md` to start at
    `## Rule statement`; removed the meta paragraph (title +
    "This file is the single source of truth..." block + the
    inlining-sites bullet list + the marker-comment example fenced
    block + trailing `---`). Re-synced both host mirrors so they
    are byte-identical to the trimmed canonical:
    `.claude/speccy-references/retry-shape.md`,
    `.agents/speccy-references/retry-shape.md`.
  - Re-synced all four inlined consumer bodies (the Claude and
    Agents speccy-work SKILL.md files plus both
    `resources/agents/.../SKILL.md.tmpl` template sources) so the
    body between the marker pair drops the same meta block.
    `grep -c '<!-- Shared rule: retry-shape.'` against each
    consumer body now returns exactly 1 (verified against all
    four).
  - Renamed the helper
    `non_blank_line_count_outside_reconcile_partial` to
    `non_blank_line_count_outside_shared_markers` in
    `speccy-cli/tests/common/mod.rs` and updated all three call
    sites: `speccy-cli/tests/init.rs:1108`,
    `speccy-cli/tests/init_phase_agents.rs:83`,
    `speccy-cli/tests/init_phase_agents.rs:214`.

- Undone: nothing in round-2 scope. Round-1 deliverables (the
  canonical file, host mirrors, speccy-work inline, the second
  test helper `body_len_without_shared_markers`) survive the amend
  intact.

- Hygiene checks:
  - `cargo test --workspace` exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0.
  - `cargo +nightly fmt --all --check` exit 0.
  - `cargo deny check` exit 0.

- Evidence: roll call unchanged from round 1 (all CHKs under
  REQ-001 / REQ-002 remain judgment-only per DEC-002, except
  CHK-022 which is hygiene via `chk022_no_orphan_references` under
  `cargo test --workspace` — still green). The round-2 amend
  strengthens reviewer-business judgment on the single-marker
  task-scenario contract (now mechanically verifiable via the
  documented `grep -c` invocation) and reviewer-style judgment on
  the helper-name honesty.

- Discovered issues: none new in round 2. The trim resolves both
  round-1 blockers without altering the rule's semantic content;
  the rule statement, read-only-scope clause, and three worked
  examples are unchanged.

- Procedural compliance:
  - No `git restore` / `git clean` / `git reset` invoked. The
    round-1 WIP carrying the canonical file, mirrors, four inlined
    consumer bodies, and the two test helpers was amended in place
    per REQ-001's retry-shape branch (which this very task
    defines).
  - The doc comment on `non_blank_line_count_outside_shared_markers`
    was already two-exemption-aware in round 1; no further edit
    was required after the rename.

</implementer>

<implementer round="3" date="2026-05-26T23:30:00Z" model="claude-opus-4.7[1m]/low">

- Completed:
  - Round-3 amend addressing the single round-2 blocker: updated
    three assert-message strings to reflect that
    `non_blank_line_count_outside_shared_markers` now exempts both
    `reconcile-policy` and `retry-shape` marker regions, not just
    reconcile-policy.
    - `speccy-cli/tests/init.rs:1111`: "outside reconcile-policy
      partial markers" → "outside shared-marker blocks".
    - `speccy-cli/tests/init_phase_agents.rs:86`: same substitution
      in the T-009 assert message.
    - `speccy-cli/tests/init_phase_agents.rs:217`: same substitution
      in the CHK-017 assert message.

- Undone:
  - Nothing. The round-2 implementation surface (canonical file,
    mirrors, four inlined consumer bodies, helper rename, doc
    comment) is unchanged; this round only touches three string
    literals inside assert messages.

- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D
    warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.

- Evidence: paper trail unchanged from round 2 at
  `.speccy/specs/0047-retry-aware-clean-tree/evidence/T-001.md`.
  CHK roll call is identical to round 2; the message-string fix
  has no CHK of its own — it is a reviewer-business hygiene
  follow-up flagged in `<blockers round="2">`.

- Discovered issues:
  - None. The blocker was a narrow string-consistency fix; no new
    surprises surfaced.

- Procedural compliance:
  - No `git restore` / `git clean` / `git reset` / `git checkout`
    invoked. Per REQ-001's retry-shape branch (this very task), the
    round-2 dirty tree was amended in place.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T23:50:00Z" round="3">
Round-3 amend is a narrow follow-up to the round-2 style blocker. Three assert message strings updated to match the helper rename. No business-surface change: REQ-001's canonical retry-shape.md (rule + three worked examples) and REQ-002's /speccy-work skill body inline (verbatim rule between marker pair, two-branch precondition prose) remain intact from round 2; atomic-landing constraint with first consumer holds; SPEC-0045/REQ-002 strict-gate behaviour on first-attempt shape unchanged. No scope bleed into T-003/T-004/T-005/T-006. Non-goals respected. Round-2 business pass stands.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-26T23:50:00Z" round="3">
Round-3 amend is narrowly scoped to the single round-2 style blocker. `git diff` confirms each of the three assert messages now reads "outside shared-marker blocks" matching the helper's two-exemption scope. `cargo test --workspace` re-runs green including `chk022_no_orphan_references` and all thin-stub shape invariants. Evidence roll call unchanged from rounds 1-2 and remains correctly bounded by DEC-002. No fabrication signals: round-3 diff matches blocker scope exactly, hygiene claims reproduce, helper-rename trail consistent across definition, call sites, and assert messages. Test coverage and hygiene gates correctly police what can be policed deterministically.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:50:00Z" round="3">
Round-3 change is three assert-message string literals only. No executable production logic added or modified, no authentication or authorization boundary changed, no secret handling, no new network surface or dependency, no injection vector. Purely cosmetic string consistency. No security concerns.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T23:50:00Z" round="3">
Round-2 style blocker resolved. All three assert message strings confirmed on disk reading "outside shared-marker blocks" at the specified line numbers. No stale `reconcile_partial` / "reconcile-policy partial markers" strings remain in any of the four touched test files. Round-3 diff is exactly the three string-literal substitutions specified in the blocker; no scope bleed, no dead imports, no new suppression annotations. Style clean.
</review>
