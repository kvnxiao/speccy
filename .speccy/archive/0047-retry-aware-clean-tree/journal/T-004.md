---
spec: SPEC-0047
task: T-004
generated_at: 2026-05-26T21:30:00Z
---

<implementer date="2026-05-26T21:30:00Z" model="claude-opus-4.7[1m]/low" round="1">

- Completed: Extended the speccy-work agent prompt with retry-shape
  awareness per REQ-003. Rewrote the entry precondition in
  `resources/modules/phases/speccy-work.md` (the canonical source
  included by both `.claude/agents/speccy-work.md.tmpl` and
  `.codex/agents/speccy-work.toml.tmpl`) to walk three substeps
  (resolve target → apply retry-shape rule on
  `<spec-dir>/journal/T-NNN.md` → run `git status --porcelain`) and
  documented the two branches: first-attempt shape keeps today's
  SPEC-0045/REQ-002 strict gate (non-empty stdout exits with the
  dirty-paths surface), retry shape proceeds regardless of stdout
  because the dirty paths are the prior pass's WIP. Inserted a new
  step 2 that reads the journal and applies the rule, with the
  REQ-001 rule body inlined verbatim between
  `<!-- Shared rule: retry-shape. ... -->` and
  `<!-- End shared rule: retry-shape. -->` markers — byte-identical
  to the canonical source at
  `resources/modules/references/retry-shape.md` and to the prior
  T-001 inline in the `/speccy-work` SKILL.md and T-003 inline in
  the `/speccy-orchestrate` SKILL.md (verified via sha256). Added a
  new step 3 that names the two branches: the first-attempt branch
  falls through to today's recipe; the retry branch reads the
  latest `<implementer>` for prior `Completed` context, reads the
  latest `<blockers>` (the one whose `round` matches the highest
  implementer round) for the feedback to address, amends the WIP
  in place without running `git restore`, `git clean`, or
  `git checkout` against the dirty paths, runs the same hygiene
  gate, and appends `<implementer round="N+1">` with the
  `Completed` field describing only the amend (not the cumulative
  work). Renumbered the subsequent steps 4–8 (state flip, scenario
  read, implement, exit transition + hygiene, exit). Extended the
  "When to use" prose to mention that the agent detects retry
  shape automatically — no flag is passed. Regenerated the two
  dogfood mirrors `.claude/agents/speccy-work.md` (cmd_prefix=`/`,
  speccy_references_path=`.claude/speccy-references`) and
  `.codex/agents/speccy-work.toml` (cmd_prefix empty,
  speccy_references_path=`.agents/speccy-references`) by running
  `target/release/speccy.exe init` for each host in a temp
  workspace and copying the rendered files into the dogfood tree.
- Undone: nothing for the T-004 scope. The remaining SPEC-0047
  tasks (T-005 decompose bootstrap commit, T-006 blockers round
  unification) are intentionally separate.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (`advisories ok, bans ok, licenses ok, sources ok`).
- Evidence: roll call for CHKs under REQ-003 (the REQ this task
  covers):
  - CHK-007 (round-2 implementer appends `<implementer round="2">`
    with `Completed` referencing the blocker-driven amend, not the
    full round-1 restatement): demonstrated → the retry-branch
    bullet list in step 3 of the agent prompt states explicitly
    "the retry-mode `Completed` field describes the amend (what
    changed this round in response to the blockers), not a
    restatement of the cumulative task work", and the
    `<implementer round="N+1">` instruction names "the highest
    prior `<implementer>` round in the journal, monotonically
    incremented by exactly 1". A reader of the prose can trace
    the documented behaviour to a round-1→round-2 transition that
    matches CHK-007.
  - CHK-008 (after two prior rounds, the next implementer writes
    `<implementer round="3">` referencing round-2 blockers
    specifically): demonstrated → the same retry-branch prose
    derives `N+1` from "the highest prior `<implementer>` round"
    (not round 1) and points the `Completed` field at "the
    latest `<blockers>` block (the one whose `round` matches the
    highest `<implementer>` round)". For a journal with rounds 1
    and 2 already complete, the highest implementer round is 2,
    so the next block is `round="3"` and the `Completed` field
    references the round-2 blockers — matching CHK-008.
  - Grep-shape scenarios from the task body (matching markers,
    no `git restore`/`git clean`/`git checkout` in the retry-mode
    branch, Codex variant byte-equality): demonstrated → manual
    sha256 comparison across all four inline sites
    (`resources/modules/phases/speccy-work.md`,
    `.claude/agents/speccy-work.md`,
    `.codex/agents/speccy-work.toml`,
    `resources/modules/skills/speccy-orchestrate.md`) yielded the
    same 16-byte prefix `19dcb0c83d37b98b`; the canonical source
    `resources/modules/references/retry-shape.md` byte-matches
    via `diff` (see "MATCH" output in the work log). The
    retry-branch bullet list explicitly forbids `git restore`,
    `git clean`, and `git checkout` against the dirty paths.
- Discovered issues: none. The existing stub-shape test helper
  `body_len_without_shared_markers` in
  `speccy-cli/tests/skill_stub_shape.rs` already exempts the
  `retry-shape` marker pair from both the skill body and the
  agent body comparison (T-001 prepared the helper for this round
  per the comment "after T-004 lands, into the speccy-work agent
  prompt too"), so the stub-shape invariant CHK-010 continues to
  hold without further test code changes.
- Procedural compliance: followed the `/speccy-work` skill recipe
  end-to-end. The clean-tree precondition was satisfied at entry
  (`git status --porcelain` was empty besides the SPEC-0047 spec
  dir already tracked). No new feature flags, abstractions, or
  configurability beyond what REQ-003 asked for.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
T-004 satisfies REQ-003 end-to-end. Retry-shape rule inlined byte-identical (sha256 match) across .claude/agents/speccy-work.md, .codex/agents/speccy-work.toml, canonical resources/modules/phases/speccy-work.md, and matches T-001 inline at /speccy-work skill body and T-003 inline at /speccy-orchestrate skill body plus both .agents/... mirrors and the canonical retry-shape.md reference. Retry-mode branch documents reading most recent `<implementer>` for prior Completed context, reading latest `<blockers>` whose round matches the highest implementer round, amending WIP in place with explicit prohibition on `git restore`/`git clean`/`git checkout` (those commands appear ONLY inside the "Do not run …" instruction in both Claude and Codex variants), routing through the same SPEC-0045/REQ-001 hygiene gate, and appending `<implementer round="N+1">` with `Completed` describing the amend not cumulative work. Six-field handoff template and Evidence CHK roll-call convention unchanged per non-goals. "When to use" prose updated to describe automatic retry-shape detection per the task body. CHK-007 and CHK-008 traceable to the retry-branch prose. Goals, non-goals, and user stories from SPEC.md all served; no open questions silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-26T22:30:00Z" round="1">
T-004 is documentation-only — no production code paths change, so CHK-007/CHK-008 are inherently judgment-only (agent-runtime behaviour an LLM produces from prose). Deterministic invariants all hold: `cargo test --workspace` passes; `body_len_without_shared_markers` in skill_stub_shape.rs (already prepared by T-001 per its anticipatory comment) exempts the retry-shape marker pair in the agent body comparison, preserving the stub-shape invariant; rule body byte-identical across all six inline sites and the canonical source (sha256 verified); `git restore`/`git clean`/`git checkout` appear only inside the "Do not run …" instruction in both agent variants. Evidence-field labelling imprecision (CHK-007/CHK-008 labelled `demonstrated` rather than `judgment-only`) is minor and accepted under this SPEC's precedent — reviewer-business territory, not a tests-persona blocker.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
Pure markdown change — no executable code, no new dependencies, no secret handling, no network surface, no auth boundary changes. The retry-shape rule explicitly restricts journal-read scope (no git, no CLI subcommands) and the retry branch forbids destructive git operations, both of which narrow rather than widen the agent action surface. The only theoretical prompt-injection vector (a malicious `<blockers>` block influencing retry-mode behavior) is identical in threat model to the existing journal-read pattern already present in the skill — it requires local write access to a repository file and is not a new surface introduced by this task. No security findings.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:30:00Z" round="1">
All style criteria pass. Diff is purely Markdown prose additions to three prompt files (template + two rendered mirrors). Rule body between shared markers is byte-identical to `resources/modules/references/retry-shape.md` after template variable substitution. Step renumbering consistent across all three locations. "When to use" prose extension matches the task spec. No `#[allow]`/suppression annotations introduced. All three modified files end with `0x0a` (trailing newline). No dead code introduced; no adjacent code refactored beyond requested scope.
</review>
