---
spec: SPEC-0047
task: T-003
generated_at: 2026-05-26T21:30:00Z
---

<implementer date="2026-05-26T21:30:00Z" model="claude-opus-4.7[1m]/low" round="1">

- Completed: Extended the `/speccy-orchestrate` work-dispatch
  precondition with retry-shape awareness. Rewrote the
  `## Work dispatch` "Clean-tree precondition" paragraph in
  `resources/modules/skills/speccy-orchestrate.md` to walk three
  steps (resolve task → apply retry-shape rule on
  `<spec-dir>/journal/T-NNN.md` → run `git status --porcelain`) and
  documented the two branches: first-attempt shape keeps the
  SPEC-0045/REQ-002 strict gate (non-empty stdout halts the outer
  loop with the dirty-paths surface), retry shape proceeds to spawn
  the `speccy-work` sub-agent regardless of stdout. Linked the retry
  branch to the existing
  `SPEC-NNNN → work T-NNN (retry 2/5 after blocking review)` status
  line. Inlined the REQ-001 rule body verbatim between the marker
  comment pair
  `<!-- Shared rule: retry-shape. Source: {{ speccy_references_path }}/retry-shape.md -->`
  / `<!-- End shared rule: retry-shape. -->`, byte-identical to the
  canonical `resources/modules/references/retry-shape.md` source (and
  by extension byte-identical to the T-001 inline in the
  `/speccy-work` skill body). Reused the existing
  `{{ speccy_references_path }}` host-templating variable so the
  Claude and Agents mirrors render with the correct host-specific
  reference path, mirroring how T-001 produced its two host-specific
  Source lines. Regenerated both host-portable mirrors
  (`.claude/skills/speccy-orchestrate/SKILL.md` and
  `.agents/skills/speccy-orchestrate/SKILL.md`) via
  `speccy init --force --host claude-code` and
  `speccy init --force --host codex` so the dogfooded files stay in
  sync with the module source.
- Undone: None — the task scope is exactly the orchestrator
  precondition extension and the single canonical inline.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites green).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
    sources ok).
- Evidence: paper trail in this journal entry and the rendered
  mirrors at `.claude/skills/speccy-orchestrate/SKILL.md` line 325
  (open marker) plus the byte-identical rule body extracted between
  the markers; `diff` against
  `.claude/speccy-references/retry-shape.md` reports IDENTICAL.
  Roll call for CHKs under REQ-002 that T-003 covers:
  - CHK-004 (first-attempt dirty tree halts with dirty-paths
    surface): judgment-only → the documentation in the
    `## Work dispatch` paragraph names the strict-gate branch
    verbatim with the SPEC-0045 dirty-paths surface; no scriptable
    proof exists at the skill-body layer, reviewer judges on the
    prose.
  - CHK-005 (retry-shape dirty tree proceeds without halting):
    judgment-only → the same paragraph names the retry branch and
    explicitly states "no dirty-paths surface is written"; reviewer
    judges on the prose.
  - CHK-006 (orchestrator work dispatch proceeds for retry-shape
    T-001 without halting): demonstrated → the inlined three-step
    precondition documents the retry branch as "proceeds to spawn
    the `speccy-work` sub-agent regardless of stdout" at
    `resources/modules/skills/speccy-orchestrate.md` (rendered into
    both host mirrors); the scenario's documented behaviour appears
    verbatim in the work-dispatch prose.
- Discovered issues: None.
- Procedural compliance: Followed `speccy-work` skill recipe; the
  hygiene-gate ran cleanly the first time; no skill-file friction
  encountered (the `/speccy-work` skill body and the SPEC's T-003
  body documented the marker pair and `{{ speccy_references_path }}`
  templating variable clearly, no edits to shipped skills needed).

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T21:35:00Z" round="1">
T-003 maps cleanly to REQ-002's done-when and to CHK-006. Diff rewrites the orchestrator work-dispatch "Clean-tree precondition" paragraph to a three-step procedure naming both branches explicitly: first-attempt shape with non-empty stdout halts the outer loop with the dirty-paths surface; retry shape proceeds to spawn the sub-agent regardless of stdout, no dirty-paths surface. Retry case linked to existing `SPEC-NNNN → work T-NNN (retry 2/5 after blocking review)` status line per SPEC.md T-003 body. REQ-001 rule inlined verbatim between the marker pair; `grep -c '<!-- Shared rule: retry-shape\.'` returns 1 across all three orchestrate files; inline body byte-identical to canonical reference and to the T-001 inline in /speccy-work. Goals/non-goals respected. One non-blocking nit: TASKS.md T-003 body uses stale "T-002 inline" wording (T-002 was merged into T-001); implementer correctly interpreted as the /speccy-work inline.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-26T22:00:00Z" round="1">
T-003's three task-scenarios are satisfied. Verified: `grep -c '<!-- Shared rule: retry-shape\.'` returns exactly 1 across the three orchestrate sites; rule body extracted between markers in the Claude mirror is byte-identical to `.claude/speccy-references/retry-shape.md` and to the T-001 /speccy-work inline; Agents mirror inline byte-identical to `.agents/speccy-references/retry-shape.md`; work-dispatch prose documents both branches verbatim. Evidence roll call covers CHK-004/CHK-005/CHK-006 with plausible labels. `cargo test --workspace` passes locally including `chk022_no_orphan_references` (further satisfied by the new consumer). No fabrication signals.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:00:00Z" round="1">
Markdown-only edit to three skill body files; no executable code, secrets, cryptographic primitives, authentication/authorization boundaries, shell injection vectors, or sensitive data exposure introduced. The relaxed clean-tree gate is bounded to a local journal file read with no user-controlled path component. No security findings.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-26T22:00:00Z" round="1">
All three modified files carry the same change. Retry-shape inline byte-identical to canonical source and to T-001 inline in /speccy-work (confirmed by direct content comparison). `resources/agents/` template files are thin `{% include %}` wrappers around the module source, not edited directly, consistent with T-001's approach. No dead code, no lint suppressions, no naming drift.
</review>
