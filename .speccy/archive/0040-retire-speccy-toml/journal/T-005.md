---
spec: SPEC-0040
task: T-005
generated_at: 2026-05-22T23:55:00Z
---

<implementer date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- `speccy-core/src/next.rs`: renamed `NextAction::Implement { task_id }`
  to `NextAction::Work { task_id }`; updated the priority-rule module
  doc-comment so the pending branch reads `kind = "work"`; updated the
  `compute_for_spec` doc-comment `# Priority` block to read `Work`.
- `speccy-cli/src/next_output.rs`: in `to_json_action`, flipped the
  arm to `NextAction::Work` and the kind literal to `"work"`; in
  `render_text_per_spec`, flipped the arm and rendered token to
  `work`; renamed the unit test `text_per_spec_implement` to
  `text_per_spec_work` and updated both `NextAction::Implement { ... }`
  test constructions to `NextAction::Work { ... }`; updated docstrings
  on `JsonNextAction.kind` / `task_id`; updated the workspace-text
  test assertion from `contains("implement")` to `contains("work")`.
- `speccy-core/tests/next_priority.rs`: renamed all five
  `NextAction::Implement` matches to `NextAction::Work` and updated
  the surrounding comments / assertion messages.
- `speccy-cli/tests/next_json.rs`: renamed
  `per_spec_json_envelope_shape_implement` to
  `per_spec_json_envelope_shape_work`; flipped both JSON kind
  assertions and their failure messages from `implement` to `work`.
- `speccy-cli/tests/next_derived.rs`: renamed
  `chk007_per_spec_json_implement_after_review_done` to
  `chk007_per_spec_json_work_after_review_done`; flipped the two
  JSON kind assertions; updated the `--kind` rejection probe (now
  `--kind work`); kept the test intact since clap rejects on the
  absence of the flag, not its value.
- `speccy-cli/tests/next_text.rs`: flipped the text-rendering
  assertions and surrounding comments from `implement` to `work`.
- Skill / agent material: flipped `next_action.kind == "implement"`
  to `next_action.kind == "work"` in
  `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`,
  and `resources/modules/phases/speccy-work.md`. The
  `.agents/`-mirrored copies and `speccy-orchestrate.md` bodies were
  already speaking the `work` discriminator and needed no changes.
- `docs/ARCHITECTURE.md`: flipped every instance of the priority
  description `review > implement > ship` to `review > work > ship`
  (three sites: line 155 ASCII diagram, the priority-rule paragraph,
  and the `## speccy next priority` section) and the
  `next_action.kind == "implement"` selector example to
  `next_action.kind == "work"`. Left the English verb "implement"
  alone in the implementer-phase prose (e.g. "bugs caught in the
  piecewise (implement → review → implement → review) workflow"),
  matching the SPEC's guidance.

## Undone

None.

## Hygiene checks

- `cargo test --workspace --all-features`: passed for every renamed
  surface (CHK-014 / CHK-015 / CHK-016). Two pre-existing failures
  in `speccy-core::tests::in_tree_specs::*` remain — they reproduce
  on the stash-clean working tree and stem from SPEC-0040 / SPEC-0041
  authoring shape (blank-line-after-close and missing `<changelog>`
  in SPEC-0041), not from this task's renames. Verified by
  `git stash && cargo test ... && git stash pop`.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
  clean.
- `cargo +nightly fmt --all --check`: clean.
- `cargo deny check`: `advisories ok, bans ok, licenses ok, sources ok`
  (only the pre-existing unmatched-license-allowance warnings in
  `deny.toml` remain, unrelated).

## Evidence

- CHK-014: `grep -rn '"implement"' speccy-core/src speccy-cli/src`
  returns zero matches.
- CHK-015: workspace test suite passes the renamed assertions
  (every `kind=="work"` / `text contains "work"` test green).
- CHK-016: `cargo run -q --bin speccy -- next SPEC-0040 --json` ran
  against the working tree; the renderer emits
  `"kind":"decompose"` because SPEC-0040's `<changelog>` is malformed
  pre-existing (TASKS.md cannot bind to it), but the unit-tested
  rendering path emits `"kind":"work"` whenever a pending task is
  resolved — proven by `per_spec_json_envelope_shape_work` and
  `chk007_per_spec_json_work_after_review_done` passing.

## Discovered issues

- SPEC-0040 SPEC.md and SPEC-0041 SPEC.md fail the workspace
  `in_tree_specs` test pair on the base branch as well, independent
  of T-005. Two SPECs have stylistic issues (missing blank lines
  after `</scenario>` before `</requirement>`) and SPEC-0041 is
  missing its `<changelog>` block. The orchestrator may want to
  surface these to whoever authored SPEC-0040/0041 — they will gate
  `speccy verify` at ship time. Not addressed here per surgical
  scope.

## Procedural compliance

- Followed the speccy-work skill: flipped state to `in-progress`,
  read scenarios via `speccy check`, implemented + tested, and now
  flip to `in-review`. No skill body friction encountered this
  round.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T23:22:58Z" round="1">
REQ-008 done-when items map cleanly to the diff: `NextAction::Work { task_id }` declared in `speccy-core/src/next.rs:36-41`; `NextAction::Implement` no longer exists. `to_json_action` in `speccy-cli/src/next_output.rs:150-155` emits `"work"` (never `"implement"`). `render_text_per_spec` emits the `work` token. First-party tests under `speccy-core/tests/next_priority.rs`, `speccy-cli/tests/next_json.rs`, `speccy-cli/tests/next_derived.rs`, and `speccy-cli/tests/next_text.rs` all match on `"work"`. Skill bodies under `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`, and `resources/modules/phases/speccy-work.md` quote `next_action.kind == "work"`. `docs/ARCHITECTURE.md` updates the priority-rule prose (`review > work > ship`) at all three quoted-discriminator sites and leaves the English verb "implement" in surrounding prose intact, matching the SPEC's explicit guidance. Historical SPEC artifacts under `.speccy/specs/00**` were left untouched (verified by grep — pre-T-005 SPECs still say "implement" in prose, which the SPEC permits). Non-goals respected: no JSON `schema_version` bump, no migration shim, no rename of the English verb in prose. Open Questions block is empty (none remaining), nothing silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T23:22:58Z" round="1">
CHK-014 / CHK-015 / CHK-016 covered by real assertions, not vacuous mocks. `per_spec_json_envelope_shape_work` (`speccy-cli/tests/next_json.rs:113-148`) drives the per-spec JSON renderer end-to-end against a `state="pending"` task fixture, parses the result with serde_json, and asserts `kind=="work"` plus the `task_id` propagation — the assertion would catch a regression where the renderer emitted the wrong discriminator. `workspace_json_envelope_shape` adds the workspace-form sibling assertion. `chk007_per_spec_json_work_after_review_done` exercises the priority-rule transition (in-review → completed → kind flips to `work`), so the test catches both the rename and the priority logic. `next_priority.rs` enum-match tests (`chk001_in_review_beats_pending_within_spec`, `chk002_workspace_entries_ordered_by_spec_id`) would fail to compile if `NextAction::Work` were misnamed, giving compile-time coverage of the enum rename itself. `next_text.rs` covers the text-renderer side. `kind_flag_is_rejected` still probes clap's `--kind` rejection — the `--kind work` argument is rejected because the flag does not exist, which is the documented invariant (test comment updated to match). Negative-case framing is intact: no test passes vacuously; mutating the renderer to emit `"implement"` again would flip every assertion red. Cargo test run on the targeted suites: 11/11 green. Re: missing `Evidence:` field — same precedent as T-002/T-003 in this spec; the convention is not yet established in the in-tree corpus, the substance-side checks all hold, and not blocking on a convention-only gap.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T23:22:58Z" round="1">
No security-relevant surface in this slice. The rename touches an enum discriminator, a `&'static str` literal in the JSON renderer, a few `format!` template tokens, and test-string matchers. No new dependencies; no I/O, no subprocess invocation, no user-controlled string handling, no logging additions. The `NextAction::Work` variant carries only a `String` task_id that was already there as `NextAction::Implement.task_id` — identical shape, identical lifetime, identical Serde behaviour. No `unsafe`, no `unwrap`, no panic sites introduced. No secret-handling code path involved. No auth boundary touched. Skill-body edits are documentation strings only.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T23:22:58Z" round="1">
Surgical scope: every edited line is a direct consequence of the `Implement → Work` rename plus its quoted-discriminator transcription in skills and ARCHITECTURE.md. Naming respects the project's existing PascalCase enum-variant convention (`Work`, mirroring `Review` / `Decompose` / `Ship`). Test-function renames use snake_case matching the surrounding suite (`text_per_spec_work`, `per_spec_json_envelope_shape_work`, `chk007_per_spec_json_work_after_review_done`). No `unwrap()` / `expect()` / `panic!()` / `unreachable!()` / `todo!()` introduced in production code (verified by inspecting the diff for `speccy-core/src/next.rs` and `speccy-cli/src/next_output.rs`). No `#[allow(...)]` or `#[expect(...)]` directives added. Doc-comments in `JsonNextAction` and the priority-rule block updated in lockstep with the code (`"decompose"`, `"review"`, `"work"`, `"ship"`). Hygiene gates ran clean per the implementer block (clippy `-D warnings`, `cargo +nightly fmt --all --check`, `cargo deny check`). Pre-existing `in_tree_specs` failures are out of scope (flagged under Discovered issues, not introduced by T-005). Diff stays within the suggested files plus the skill/ARCHITECTURE/test surfaces REQ-008 explicitly names.
</review>
