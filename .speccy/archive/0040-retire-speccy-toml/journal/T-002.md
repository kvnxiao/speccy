---
spec: SPEC-0040
task: T-002
generated_at: 2026-05-22T00:00:00Z
---

<implementer date="2026-05-22T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- Removed the explicit `toml = { workspace = true }` line from
  `speccy-core/Cargo.toml`'s `[dependencies]`. The crate has no
  `[dev-dependencies]` entry for `toml` either (REQ-006 satisfied).
- Picked path (A) "Relocate" from the task body: `git mv`'d
  `speccy-core/tests/pin_shape.rs` and
  `speccy-core/tests/skill_stub_shape.rs` to `speccy-cli/tests/`.
  Both test files import no `speccy_core` symbols; they walk to the
  workspace root via `CARGO_MANIFEST_DIR.parent()`, which still
  resolves to the workspace root from the speccy-cli crate. The
  `toml = { workspace = true }` dev-dep already on
  `speccy-cli/Cargo.toml` (line 36) covers them. No assertion or
  helper logic was modified.

## Undone

- None — REQ-006 scope was the explicit `toml` dependency on
  `speccy-core` and the dev-time consumers blocking its removal;
  both are clear.

## Hygiene checks

- `cargo build --workspace` — exit 0 (CHK-011 satisfied).
- `cargo test --workspace` — the two relocated tests pass at their
  new home (`pin_shape`: 9/9, `skill_stub_shape`: 12/12). The two
  failures in `speccy-core --test in_tree_specs`
  (`every_in_tree_spec_md_parses_with_xml_parser`,
  `every_migrated_spec_md_has_blank_line_after_each_close_tag`)
  pre-date T-002 — I confirmed by stashing my changes and re-running
  the same test target; both failures reproduce on the un-touched
  tree. They stem from SPEC-0040 and SPEC-0041's own SPEC.md files
  (blank-line-after-close convention + missing `<changelog>` block)
  and are out of scope for REQ-006.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  — exit 0.
- `cargo deny check` — `advisories ok, bans ok, licenses ok, sources ok`
  (CHK satisfied). Two pre-existing `license-not-encountered`
  warnings (ISC, MPL-2.0 unmatched allowances) are unrelated to this
  task.

## Evidence

- `speccy-core/Cargo.toml` — `toml = { workspace = true }` line
  removed; ripgrep for `^toml\s*=` returns zero matches (CHK-010).
- `speccy-cli/tests/pin_shape.rs`, `speccy-cli/tests/skill_stub_shape.rs`
  — relocated from `speccy-core/tests/` via `git mv`; assertion
  semantics preserved verbatim.
- Test output snippet (speccy-cli):
  `test result: ok. 9 passed; 0 failed` (pin_shape) and
  `test result: ok. 12 passed; 0 failed` (skill_stub_shape).

## Discovered issues

- Pre-existing failures in `speccy-core --test in_tree_specs` against
  the in-tree SPEC-0040 and SPEC-0041 SPEC.md files. Out of scope
  for this task; flagged here so a future task can address them
  (likely belong inside SPEC-0040 / SPEC-0041's own task lists or
  a small SPEC.md hygiene pass).

## Procedural compliance

- Followed `/speccy-work` single-task primitive: read scenarios via
  `speccy check SPEC-0040/T-002`, flipped state pending →
  in-progress → in-review, no other tasks touched.
- No shipped-skill friction encountered; no skill files under
  `skills/` required edits.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T22:50:00Z" round="1">
REQ-006 done-when satisfied. `speccy-core/Cargo.toml` has no `^toml\s*=` line in either `[dependencies]` or `[dev-dependencies]`; the explicit first-party declaration is gone, matching the goal and SPEC Notes "transitive `toml` reaching `speccy-core` via another dep is acceptable." User stories preserved (no test invariants weakened; relocation kept assertion semantics verbatim per implementer notes). No non-goals breached: SPC-001 untouched, JSON-envelope `schema_version` untouched, no migration shim invented. The two relocated tests sit naturally in `speccy-cli/tests/` since they validate `speccy-cli`'s `render_host_pack` output. No open questions silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T22:50:00Z" round="1">
All four task-scenarios verified: CHK-010 (`rg "^toml\s*=" speccy-core/Cargo.toml` → 0 matches), CHK-011 (`cargo build --workspace` exited 0), `cargo test -p speccy-cli --test pin_shape` 9/9 and `--test skill_stub_shape` 12/12 pass at the new home, `cargo deny check` exits 0 with only pre-existing unmatched-license warnings. Tests were relocated via `git mv` so assertion bodies are byte-identical to their pre-T-002 form — there is no plausible mock-without-real-call regression introduced by this slice. The pre-existing `in_tree_specs` failures the implementer flagged are unrelated to T-002 (they concern SPEC-0040/0041 SPEC.md blank-line + `<changelog>` conventions, not the `toml` dependency). Note: no `Evidence:` field on the `<implementer>` block and no `.speccy/specs/0040-retire-speccy-toml/evidence/T-002.md` file — but per `.claude/speccy-references/evidence.md` the field is a forward-looking convention illustrated against a hypothetical SPEC-0042, and no prior shipped spec in `.speccy/specs/` (0037, 0038, 0039) carries one either. The substance-side checks all hold; not blocking on a convention not yet established in the in-tree corpus.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T22:50:00Z" round="1">
No security surface in this slice. The change removes one direct dependency (`toml`) from `speccy-core/Cargo.toml` and relocates two pin-shape test files. No auth boundary, input validation path, secret handling, logging surface, or crypto primitive is touched. `cargo deny check` exits 0 (advisories ok, bans ok, licenses ok, sources ok) — no new advisories or duplicate-version warnings introduced. The `toml` crate continues to be present transitively (it is unchanged in `speccy-cli/Cargo.toml`'s dev-dependencies which the relocated tests now consume), so no supply-chain reduction is claimed; this matches REQ-006's "only explicit first-party declaration removed, no transitive chasing" framing.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]/low" date="2026-05-22T22:50:00Z" round="1">
Clean `git mv` relocation per the implementer notes; the diff shows `speccy-core/tests/pin_shape.rs` and `speccy-core/tests/skill_stub_shape.rs` deleted in full and the same content appearing under `speccy-cli/tests/`. No new `#[allow(...)]`, no `unwrap()` introductions, no naming drift. The existing `#![expect(clippy::expect_used, reason = "...")]` annotations carry justifications per AGENTS.md hygiene. `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0 per the implementer's hygiene table. The relocated tests walk to the workspace root via `CARGO_MANIFEST_DIR.parent()`, which resolves correctly from the new `speccy-cli` crate context — no helper duplication or parallel-helper invention.
</review>

