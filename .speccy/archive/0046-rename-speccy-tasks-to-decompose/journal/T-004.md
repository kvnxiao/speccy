---
spec: SPEC-0046
task: T-004
generated_at: 2026-05-26T19:30:00Z
---

<implementer date="2026-05-26T19:30:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed: Swept every `speccy-tasks` occurrence in test files +
  inline `render.rs` unit tests to `speccy-decompose`. Files touched:
  `speccy-cli/tests/init.rs` (skill name list, two slash-prefix
  assertion blocks, the `CLAUDE_PINNED_PHASES` array and two loop
  literals that build `speccy-{phase}` paths),
  `speccy-cli/tests/init_phase_agents.rs` (10 phase-list literals),
  `speccy-cli/tests/pin_shape.rs` (`UNPINNED_CLAUDE_SKILLS` paths),
  `speccy-cli/tests/skill_packs.rs` (`SKILL_NAMES`,
  `PINNED_STUB_PHASES`, prose comments),
  `speccy-cli/tests/skill_body_discovery.rs` (`PHASE_FILES`, CHK-019
  prose), `speccy-cli/tests/skill_stub_shape.rs` (`PINNED_PHASES` and
  one loop literal — out-of-scope-listed but it broke the same way
  the in-scope tests did, so it was clearly part of the same sweep),
  and `speccy-cli/src/render.rs` (4 inline assertion strings + failure
  messages across the two `render_host_pack_*_speccy_plan` tests).
  Also added blank lines after four `</scenario>` close-tags in
  `.speccy/specs/0046-rename-speccy-tasks-to-decompose/SPEC.md`
  (lines 142, 178, 226, 270) to satisfy
  `speccy-core::in_tree_specs::every_in_tree_spec_md_has_blank_line_after_each_close_tag`
  — see "Discovered issues" below.
- Undone: None.
- Hygiene checks:
  - `cargo test --workspace`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo deny check`: exit 0
- Evidence: paper trail inline in the journal (no separate evidence
  file — all CHKs are hygiene-shaped).
  Roll call for CHKs under REQ-004:
  - CHK-006 (`cargo test --workspace` exits 0, no new ignores):
    hygiene → `cargo test --workspace` exit 0; ignored count
    unchanged (one pre-existing ignored test in `lints_acc_001.rs`
    is unrelated to this SPEC).
  - CHK-007 (clippy + fmt + deny exit 0): hygiene → each gate exit 0,
    captured under Hygiene checks above.
- Discovered issues:
  - SPEC-0046 SPEC.md was authored without the blank-line-after-close
    convention in four spots (`</scenario>` immediately followed by
    `</requirement>` on the next line). The
    `every_in_tree_spec_md_has_blank_line_after_each_close_tag` test
    in `speccy-core/tests/in_tree_specs.rs` flagged this. T-003's
    journal records the hygiene failure already; my read is the test
    was failing on T-001 through T-003 too, but those slices weren't
    expected to fix it. I fixed it here because T-004's CHK-006
    explicitly requires `cargo test --workspace` exit 0. Pure
    whitespace; no semantic SPEC change. The TASKS.md
    `spec_hash_at_generation` is now stale relative to the edited
    SPEC.md, but `speccy next SPEC-0046 --json` does not surface this
    as a drift (it reports only the expected
    `state_in_progress_orphaned` mid-task drift). Reviewer should
    confirm whether a follow-up `speccy amend` or hash refresh is
    needed before ship.
- Procedural compliance: Followed the speccy-work skill's full
  precondition + hygiene-gate + journal contract. Used the Edit tool
  for all substitutions per the implementer notes warning about
  PowerShell Unicode corruption. No skill-file friction encountered.

</implementer>

<review date="2026-05-26T18:46:10Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-004 implementation satisfies REQ-004's done-when contract end-to-end and serves the SPEC's goals/user-stories without violating any non-goal.

On the two flagged scope-expansion points:

1. `speccy-cli/tests/skill_stub_shape.rs` inclusion is justified by REQ-004's done-when "No test file under `speccy-cli/tests/` contains the literal `speccy-tasks`" combined with T-004's explicit "the list below is the floor, not the ceiling — verify exhaustively via grep" language. The file referenced the phase name `"tasks"` in `PINNED_PHASES` and a loop literal, which would have failed `cargo test --workspace` (CHK-006) after the rename. The implementer's expansion is necessary, not opportunistic.

2. The four-blank-line SPEC.md edit is pure whitespace clearing a pre-existing tree-wide lint (`every_in_tree_spec_md_has_blank_line_after_each_close_tag`) that blocks CHK-006's `cargo test --workspace` exit 0 requirement. No Requirement / done-when / behavior / scenario / goal / non-goal / user-story / decision text changed. The edit is causally required to satisfy T-004's own hygiene gate, and the implementer transparently surfaced the resulting `spec_hash_at_generation` drift as an open question for human resolution rather than silently editing TASKS.md frontmatter. This is the right shape: the SPEC edit is in-scope hygiene; the hash refresh is the orchestrator's call. A full `/speccy-amend` ceremony is overkill for whitespace — refreshing `spec_hash_at_generation` post-review (or letting `/speccy-ship` reconcile it) is sufficient and lighter-weight.

No drift between SPEC intent and the diff. All four hygiene gates recorded exit 0. CHK-006 and CHK-007 both satisfied. Open question on hash refresh was correctly escalated rather than silently resolved.
</review>

<review date="2026-05-26T18:46:10Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-004 test edits genuinely exercise the renamed code paths, not vacuous string swaps. Verified end-to-end:

1. The `init.rs` assertions (`contains("/speccy-decompose")` at line 314 and the Codex positive/negative pair at lines 372–378) drive the real renderer over the real templates — `resources/modules/skills/speccy-plan.md:119` contains the `{{ cmd_prefix }}speccy-decompose` token T-002/T-003 landed, so the assertion would fail loudly if either the template or the renderer's `cmd_prefix` substitution regressed.
2. The `init_phase_agents.rs` substitutions iterate `["speccy-work", "speccy-decompose", "speccy-ship"]` against `run_init_claude(...)` output and read the resulting `.claude/skills/<phase>/SKILL.md` and `.claude/agents/<phase>.md` files — file-not-found would fire if the rename were incomplete.
3. The `skill_body_discovery.rs` CHK-019 test reads `phases/speccy-decompose.md` (confirmed present, with `speccy-tasks.md` gone) and asserts substantive content shape (`# Tasks: SPEC-`, `covers="REQ-001 REQ-002"`) — content assertions, not vacuous slug checks.
4. The Codex assertion's `!contains("/speccy-decompose")` invariant correctly preserves the "Codex uses bare command names" semantic from the original test.

Reproduced the hygiene claims on a clean local run: `cargo test --workspace` (all suites pass, 1 pre-existing ignored test `bless_snapshot` in `speccy-core/tests/lint_registry.rs` introduced in commit 2821a92 — predates SPEC-0046, matches CHK-006's "no test newly skipped relative to main"), `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0, `cargo +nightly fmt --all --check` exit 0, `cargo deny check` exit 0. CHK-006 and CHK-007 confirmed reproducible.

Grep cross-check: `speccy-tasks` survives only inside SPEC-0046's own SPEC.md/TASKS.md/journal files (the scenario's explicit exclusion). Third scenario satisfied.

Roll-call shape: both CHK-006 and CHK-007 are inherently hygiene-suite CHKs (their scenarios literally invoke the hygiene gates), and the `Hygiene checks` block enumerates the four exit-0 commands — re-run scope is unambiguous. Hygiene-only labelling is honest here.

The implementer's SPEC.md blank-line fix (four `</scenario>` close-tag positions to satisfy `every_in_tree_spec_md_has_blank_line_after_each_close_tag`) is pure whitespace and is properly disclosed under Discovered issues — out of strict T-004 scope but necessary for `cargo test --workspace` to hit exit 0, so acceptable. The note flags the resulting `spec_hash_at_generation` staleness for downstream attention.

The implementer's inclusion of `speccy-cli/tests/skill_stub_shape.rs` outside the suggested-files list is justified — the test iterates `PINNED_PHASES = ["tasks", "work", "ship"]` and references `resources/modules/phases/speccy-tasks.md`, both of which would have failed hard had they been left unchanged.
</review>

<review date="2026-05-26T18:46:10Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-004 is a mechanical string-rename sweep — every changed line replaces the literal `speccy-tasks` with `speccy-decompose` in test assertions, constant arrays, and inline test failure messages inside `speccy-cli/tests/` and `speccy-cli/src/render.rs`. No authentication, authorization, secret handling, input validation, cryptographic, or process-execution surface is touched or introduced. The diff contains no new dependencies, no new I/O paths, and no changes to production code outside `#[cfg(test)]` blocks. Nothing in this slice presents a security concern.
</review>

<review date="2026-05-26T18:46:10Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All T-004 working-tree changes are mechanical slug substitutions (`speccy-tasks` → `speccy-decompose`) across six test files and `render.rs`. No style issues found:

- Every modified constant (`PINNED_PHASES`, `SKILL_NAMES`, `PINNED_STUB_PHASES`, `CLAUDE_PINNED_PHASES`) follows existing array-literal style.
- All `#[allow(...)]` suppressions pre-exist with `reason = "..."` attributes; T-004 adds none.
- No new helpers invented; `read_file`, `Workspace`, `spec_md_template`, `require_module` are the same helpers used throughout.
- Comment strings in test failure messages are updated in lockstep with the assertions they describe.
- The SPEC.md blank-line additions satisfy the `every_in_tree_spec_md_has_blank_line_after_each_close_tag` structural requirement, not cosmetic drift.
- No unused imports or dead code introduced.
</review>

