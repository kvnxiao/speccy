---
spec: SPEC-0046
task: T-001
generated_at: 2026-05-26T18:30:00Z
---

<implementer date="2026-05-26T18:30:00Z" model="claude-opus-4-7[1m]" round="1">
- Completed: Used `git mv` to rename the four installed skill
  artifacts from `speccy-tasks` to `speccy-decompose`:
  `.claude/skills/speccy-tasks/` → `.claude/skills/speccy-decompose/`
  (whole directory including `references/tasks.md`),
  `.claude/agents/speccy-tasks.md` →
  `.claude/agents/speccy-decompose.md`,
  `.agents/skills/speccy-tasks/` → `.agents/skills/speccy-decompose/`
  (whole directory including `references/tasks.md`),
  `.codex/agents/speccy-tasks.toml` →
  `.codex/agents/speccy-decompose.toml`. Inside each renamed file
  updated frontmatter `name:` (or TOML `name = `) to
  `speccy-decompose`, the level-1 heading, and self-referential body
  lines (`/agent speccy-tasks` references and the
  `.claude/agents/speccy-tasks.md` / `.codex/agents/speccy-tasks.toml`
  cross-pointers inside the two SKILL.md shim files). The
  `references/tasks.md` companion files contained no `speccy-tasks`
  tokens and were not modified beyond their parent-directory rename.

- Undone: Nothing in T-001 scope. Templates under `resources/`
  (T-002), cross-skill references in `speccy-plan` /
  `speccy-brainstorm` / `speccy-orchestrate` / `speccy-work` and
  docs (T-003), and tests/inline render assertions (T-004) are
  intentionally out of sync at this slice — the SPEC task body
  explicitly calls this out.

- Hygiene checks: not run at this slice. The SPEC task body
  explicitly states "Leaving the templates and tests out of sync
  mid-spec is expected; the spec-final state will reconcile them."
  Running the four hygiene gates here would fail by construction —
  `cargo test --workspace` already fails on
  `dogfood_outputs_match_committed_tree` because the committed
  templates still produce the old `speccy-tasks` paths, and the
  rendered-tree expectation lives in code that T-004 owns. Spot-check:
  `cargo +nightly fmt --all --check` exits 0 (no Rust source touched
  in this slice).
    - cargo test --workspace: not run (T-004 reconciles; pre-spot-check fails on `dogfood_outputs_match_committed_tree` as expected per SPEC scope).
    - cargo clippy --workspace --all-targets --all-features -- -D warnings: not run (no Rust source touched).
    - cargo +nightly fmt --all --check: exit 0.
    - cargo deny check: not run (no dependency changes).

- Evidence: paper trail demonstrated by the working-tree shape
  itself; no scriptable test file is in scope at this slice.
  Roll call for CHKs under REQ-001:
    - CHK-001 (grep for `speccy-tasks` outside `.speccy/archive/`
      returns no matches): judgment-only at this slice → still has
      matches under `resources/`, other shipped skills, docs, and
      tests, which is T-002 / T-003 / T-004 scope by SPEC design. The
      sweep over T-001's own scope (`.claude/skills/`,
      `.claude/agents/`, `.agents/skills/`, `.codex/agents/`) returns
      no matches; that subscope was verified via Grep.
    - CHK-002 (each renamed file exists and its `name:` /
      `name = ` frontmatter reads `speccy-decompose`): demonstrated
      → after the edits, `.claude/skills/speccy-decompose/SKILL.md`
      line 2 reads `name: speccy-decompose`,
      `.claude/agents/speccy-decompose.md` line 2 reads
      `name: speccy-decompose`,
      `.agents/skills/speccy-decompose/SKILL.md` line 2 reads
      `name: speccy-decompose`, and
      `.codex/agents/speccy-decompose.toml` line 1 reads
      `name = "speccy-decompose"`. Reviewer can confirm with Grep
      against the four absolute paths.

- Discovered issues: Initial attempt used PowerShell
  `Get-Content -Raw` + `Set-Content -NoNewline` to do the in-place
  token rewrite. That round-trip corrupted Unicode characters in the
  files (em-dash `—` became ` E`, right-arrow `→` became `ↁE`) due to
  encoding mismatches between PowerShell's default text codecs and
  the source UTF-8. Reverted with `git show HEAD:<old-path> >
  <new-path>` to restore byte-for-byte original content, then redid
  the slug edits via the harness Edit tool which preserves UTF-8
  cleanly. Reviewer should diff against HEAD on each renamed file
  to confirm no stray Unicode replacement.

- Procedural compliance: Followed the speccy-work skill flow:
  precondition `git status --porcelain` clean, `speccy next --json`
  resolved T-001, flipped state pending → in-progress in TASKS.md,
  did the work with `git mv` (preserving rename for `git log
  --follow` post-commit), edited frontmatter / heading / body lines
  per the task body, then flipped state in-progress → in-review and
  wrote this journal file. Skipped the hygiene-gate flip blocker
  because the SPEC task body explicitly declares mid-spec
  out-of-sync expected; the next implementer slices (T-002 → T-004)
  bring the tree back to green.
</implementer>

<review date="2026-05-26T18:13:07Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
REQ-001 done-when items are satisfied within T-001's scope. The four installed skill artifacts at `.claude/skills/speccy-decompose/SKILL.md`, `.claude/agents/speccy-decompose.md`, `.agents/skills/speccy-decompose/SKILL.md`, and `.codex/agents/speccy-decompose.toml` exist with frontmatter `name: speccy-decompose` (and TOML `name = "speccy-decompose"`), level-1 headings updated, and self-referential body lines (`/agent speccy-tasks`, cross-pointers to `.claude/agents/speccy-tasks.md` / `.codex/agents/speccy-tasks.toml`) rewritten. `git diff -M HEAD --name-status` confirms all six files (4 primary + 2 `references/tasks.md`) are recognized as renames with 74-100% similarity, satisfying DEC-002's history-preservation requirement. No `speccy-tasks` token remains under T-001's four scoped directories. Non-goals respected: no behavior changes, no CLI edits, no alias shim, no other skill renamed, no archive edits. Out-of-sync templates / cross-skill refs / tests are explicitly deferred to T-002 / T-003 / T-004 per the task body's own carve-out, and the journal documents the Unicode-corruption mishap and clean revert + re-edit. User stories (developer scanning skill list, harness author chaining the loop) advance correctly for the on-disk shape of this slice.
</review>

<review date="2026-05-26T18:13:07Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-001 is a pure rename slice with no executable test code in scope — T-004 owns all test updates. The three `<task-scenarios>` are shape assertions (file exists, frontmatter slug, history preserved) and each is satisfiable by deterministic Read/Grep/git inspection. I independently verified scenario 1 (no `speccy-tasks` in the four pack subtrees: `.claude/skills/speccy-decompose/`, `.claude/agents/speccy-decompose.md`, `.agents/skills/speccy-decompose/`, `.codex/agents/speccy-decompose.toml`) and scenario 2 (each file's frontmatter `name:` / `name = "speccy-decompose"` field is correct). Scenario 3 (`git log --follow`) is structurally guaranteed by `git status` reporting `R` markers but will only resolve post-commit. The implementer's Evidence roll call covers CHK-001 (`judgment-only`, with the T-001-subscope sweep cited as demonstrable — accurate given the slice cannot reach SPEC-final tree state) and CHK-002 (`demonstrated`, with specific path+line cites I re-verified). No fabrication signals: the implementer correctly declares hygiene checks "not run" and explains why (mid-spec out-of-sync is SPEC-scope expected), so there is no runner output to scrutinize. The PowerShell Unicode corruption-and-revert story checks out — em-dashes and right-arrows are intact in the renamed files. Label-precision quibble (not blocking): CHK-001 could arguably be `slice-scope-demonstrated` rather than `judgment-only` since it *is* a deterministic grep at SPEC final, just out of T-001's reach — but the implementer named the partial sweep explicitly, so reviewer reproducibility is preserved.
</review>

<review date="2026-05-26T18:13:07Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Pure file rename — four skill artifact files moved via `git mv` with frontmatter slug updates only. No Rust source touched, no auth boundaries, secrets, injection vectors, or new dependencies introduced. Unicode corruption noted by implementer was reverted before staging; diff content is clean UTF-8.
</review>

<review date="2026-05-26T18:13:07Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All four renamed skill artifacts (`.claude/skills/speccy-decompose/SKILL.md`, `.claude/agents/speccy-decompose.md`, `.agents/skills/speccy-decompose/SKILL.md`, `.codex/agents/speccy-decompose.toml`) follow the project's existing shim conventions. Frontmatter `name:` / `name = ` fields, level-1 headings, and self-referential body lines all read `speccy-decompose`. The heading asymmetry between the two SKILL.md shims (`.agents/skills/` uses `# speccy-decompose` without a slash; `.claude/skills/` uses `# /speccy-decompose` with a slash) is a faithful carry-forward of the pre-existing originals, not a regression. Trailing newlines confirmed `0x0a` on all four files. No suppressions, no dead code, no parallel helpers invented, no out-of-scope edits.
</review>
