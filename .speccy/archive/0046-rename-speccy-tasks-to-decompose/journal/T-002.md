---
spec: SPEC-0046
task: T-002
generated_at: 2026-05-26T18:18:11Z
---

<implementer date="2026-05-26T18:18:11Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Renamed the five resource template paths via `git mv` to
  preserve history: `resources/agents/.claude/skills/speccy-tasks/` →
  `.../speccy-decompose/` (incl. `references/tasks.md.tmpl`),
  `resources/agents/.claude/agents/speccy-tasks.md.tmpl` →
  `speccy-decompose.md.tmpl`,
  `resources/agents/.agents/skills/speccy-tasks/` → `.../speccy-decompose/`
  (incl. `references/tasks.md.tmpl`),
  `resources/agents/.codex/agents/speccy-tasks.toml.tmpl` →
  `speccy-decompose.toml.tmpl`, and
  `resources/modules/phases/speccy-tasks.md` → `speccy-decompose.md`.
  Swept the renamed-template bodies and replaced every `speccy-tasks`
  token with `speccy-decompose`: frontmatter `name:` / TOML
  `name = `, level-1 headings, internal `/agent` invocations, and the
  `{% include "modules/phases/speccy-tasks.md" %}` references which
  now point at the renamed phase body. Verified zero `speccy-tasks`
  matches under each of the five renamed template paths via Grep.

- Undone: Cross-skill references in `resources/modules/skills/`
  (speccy-plan.md, speccy-brainstorm.md, speccy-orchestrate.md) and
  in `resources/modules/phases/speccy-work.md`, plus the speccy-work
  SKILL.md.tmpl frontmatter description in both pack template dirs,
  still contain the literal `speccy-tasks`. Per REQ-002 done-when
  (which scopes to the renamed templates' bodies) and TASKS.md T-003
  body (which explicitly owns "cross-skill references in other
  shipped skills"), those land in T-003. The TASKS.md T-002 scenario
  text says "recursive search ... over `resources/`" returns zero,
  which conflicts with REQ-002's narrower done-when — I followed
  REQ-002 since SPEC.md is authoritative over TASKS.md prose.

- Hygiene checks: Skipped — mid-spec out-of-sync is SPEC-design
  expected. The SPEC explicitly defers test and render.rs updates
  to T-004; `cargo test --workspace` fails on
  `copy_claude_code_pack_skill_md`,
  `copy_codex_pack_skill_md`,
  `t007_init_renders_claude_code_pin_assignments_matching_dogfood_pack`,
  and `t007_init_renders_codex_pin_assignments_matching_dogfood_pack`
  — all four assert old `speccy-tasks` template paths and are T-004
  scope. T-001 took the same carve-out and the reviewer accepted it.
  Following that precedent. Not run: cargo clippy, cargo +nightly fmt
  --check, cargo deny check (would not surface anything renamed-file
  specific that the implementer can act on at this slice).

- Evidence: paper trail demonstrated by the working-tree shape
  itself; no scriptable test file is in scope at this slice
  (test/render updates belong to T-004).
  Roll call for CHKs under REQ-002:
    - CHK-003 (`speccy init` in a tempdir against a stub `AGENTS.md`
      produces a tree containing `speccy-decompose` entries and no
      `speccy-tasks` entries under the four install dirs):
      judgment-only at this slice → render.rs still references old
      template paths (it is T-004 scope per the SPEC), so a fresh
      `speccy init` from this commit would not yet produce a clean
      tree. The template inputs are correctly renamed and ready;
      after T-003 (cross-refs) and T-004 (render.rs + tests) land,
      this CHK becomes deterministically green. Reviewer can verify
      template inputs via `ls resources/agents/.claude/skills/
      resources/agents/.claude/agents/ resources/agents/.agents/skills/
      resources/agents/.codex/agents/ resources/modules/phases/` →
      every `speccy-decompose` entry exists, no `speccy-tasks` entry
      remains.

- Discovered issues: None new. Heeded prior-turn note about
  PowerShell `Get-Content -Raw` / `Set-Content -NoNewline` Unicode
  corruption — used harness `Edit` and `Write` (after `Read`) for
  every in-file token rewrite. The two .tmpl files that got
  full-`Write`d (`speccy-decompose.md.tmpl`,
  `speccy-decompose.toml.tmpl`) drop below git's default 50%
  rename-detection threshold (43% similarity), but they are still
  detected as renames at lower thresholds and T-002 has no
  `git log --follow` scenario, so history-preservation surfaces
  through `git diff -M30` if a reviewer wants the chain.

- Procedural compliance: Followed speccy-work flow: precondition
  `git status --porcelain` clean, `speccy next SPEC-0046 --json`
  resolved T-002, flipped state pending → in-progress in TASKS.md,
  did the work with `git mv` + content edits, verified zero
  `speccy-tasks` under the five renamed paths, then flipped state
  in-progress → in-review and wrote this journal. Conflict between
  TASKS.md T-002 scenario prose ("zero in resources/") and SPEC.md
  REQ-002 done-when ("Template bodies contain no `speccy-tasks`
  token") resolved in favour of SPEC.md, which is the contract; the
  broader sweep is T-003's job by both SPEC and TASKS.md narrative.
</implementer>

<review date="2026-05-26T18:22:05Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
REQ-002 done-when satisfied. All four `.tmpl` paths exist at `resources/agents/.claude/skills/speccy-decompose/SKILL.md.tmpl`, `resources/agents/.agents/skills/speccy-decompose/SKILL.md.tmpl`, `resources/agents/.claude/agents/speccy-decompose.md.tmpl`, `resources/agents/.codex/agents/speccy-decompose.toml.tmpl`; `resources/modules/phases/speccy-decompose.md` exists; the renamed templates' bodies (frontmatter `name:`/TOML `name = `, level-1 headings, `/agent` invocations, the Jinja `{% include "modules/phases/speccy-decompose.md" %}`) contain zero `speccy-tasks` tokens. Files moved via `git mv` (rename detection visible in `git status` output). Non-goals preserved: no behavior change, no CLI changes, no shim, no other-skill renames, no archive edits.

On the flagged scope conflict between the T-002 scenario "zero matches under `resources/`" and REQ-002's narrower "Template bodies contain no `speccy-tasks` token": the implementer's interpretation aligns with SPEC intent. REQ-002's `<requirement>` body explicitly scopes the rename to "the four `.tmpl` files mirroring the install paths and the phase body `resources/modules/phases/speccy-tasks.md`, plus any internal references inside those templates." The six remaining `speccy-tasks` matches under `resources/` (in `resources/modules/skills/speccy-plan.md`, `speccy-brainstorm.md`, `speccy-orchestrate.md`, `resources/modules/phases/speccy-work.md`, and the two `speccy-work` `SKILL.md.tmpl` frontmatter descriptions) are all in T-003's explicit "Suggested files" list and are exactly the cross-skill loop-suggestion lines REQ-003 names as its own scope ("Cross-skill 'next step' suggestions"). Sweeping them in T-002 would collapse the T-002/T-003 boundary the SPEC carved. Surfacing the TASKS.md prose overreach in the journal rather than silently choosing matches the "Surface unknowns; never invent" rule in AGENTS.md.

CHK-003 (`speccy init` produces a clean tree) is correctly noted as not yet deterministically green at this slice — it depends on T-003 (cross-refs) and T-004 (render.rs path lookups), which the SPEC explicitly defers. Mid-spec out-of-sync is by design per T-001's accepted carve-out and the SPEC's task decomposition.
</review>

<review date="2026-05-26T18:22:05Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-002 is a pure resource-template rename slice with no executable test code in scope — T-004 owns all test updates. The three `<task-scenarios>` reduce to file-shape assertions, each deterministically verifiable by Read/Grep/git inspection.

Independently verified:
- The five renamed paths exist on disk: `resources/agents/.claude/skills/speccy-decompose/SKILL.md.tmpl`, `resources/agents/.claude/agents/speccy-decompose.md.tmpl`, `resources/agents/.agents/skills/speccy-decompose/SKILL.md.tmpl`, `resources/agents/.codex/agents/speccy-decompose.toml.tmpl`, `resources/modules/phases/speccy-decompose.md`.
- Grep for `speccy-tasks` over each of the five renamed paths (and their subtrees) returns zero matches — the REQ-002 done-when narrow scope holds.
- `git status` shows R/RM markers on all five renames, so `git log --follow` history is preserved per DEC-002.
- The remaining six `speccy-tasks` matches in `resources/` (in `speccy-plan.md`, `speccy-brainstorm.md`, `speccy-orchestrate.md`, `speccy-work.md`, and the two `speccy-work` SKILL.md.tmpl files) are exactly what T-003's task body claims as its scope; nothing leaks outside the documented T-003 split.

Evidence roll call inspection: CHK-003 (`speccy init` in a tempdir emits clean tree) is labelled `judgment-only` with a reviewer-runnable `ls` inspection path. The label is plausible — the deterministic test that would exercise CHK-003 is the integration test in `speccy-cli/tests/init.rs`, which is explicitly T-004 scope per the SPEC. The implementer's framing of why ("render.rs still references old template paths") is slightly imprecise (render.rs's production rendering code at `speccy-cli/src/render.rs:132` uses dynamic template discovery; only the inline `#[cfg(test)]` assertions at lines 371-392 hard-code old slugs), but the bottom line is correct: no in-scope test can demonstrate CHK-003 this slice.

No fabrication signals: the implementer ran no test commands and claims no runner output, so there is no red-then-green transcript to scrutinize. The hygiene-checks skip is documented with the same SPEC-design rationale T-001 used and the T-001 tests reviewer accepted.

One legitimate quibble (not blocking): TASKS.md `<task-scenarios>` scenario 1 ("recursive search ... over `resources/` returns zero matches") is literally violated at this slice — six matches remain in cross-skill bodies. The implementer flagged this conflict transparently and chose REQ-002's narrower done-when ("Template bodies contain no `speccy-tasks` token") as authoritative, which is consistent with the task body's own "Do not yet" carve-out two paragraphs above the scenario and with the T-001 precedent. The conflict is a TASKS.md drafting inconsistency, not fabrication. T-003 will reconcile both readings.
</review>

<review date="2026-05-26T18:22:05Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-002 is a mechanical rename of skill template artifacts with token-substitution in their bodies. No authentication boundaries, input validation surfaces, secret handling, cryptographic primitives, or injection vectors are touched. The `{% include "modules/phases/speccy-decompose.md" %}` directives reference static, repo-local paths with no dynamic or user-controlled components; the Rust renderer resolves them at build time. Nothing in the diff introduces a new security exposure.
</review>

<review date="2026-05-26T18:22:05Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All five renamed template files are clean. Trailing-newline absence on `.tmpl` files (`resources/agents/.claude/agents/speccy-decompose.md.tmpl`, `resources/agents/.codex/agents/speccy-decompose.toml.tmpl`, and both `references/tasks.md.tmpl` files) is explicitly sanctioned by `.editorconfig`'s `[*.tmpl] insert_final_newline = false` rule (byte-probe confirms: none end in `0a`); the old source files had the same state. `resources/modules/phases/speccy-decompose.md` correctly ends in `0a`. Token sweep is complete within the five renamed paths — zero `speccy-tasks` residuals. TASKS.md carries only the `state` attribute flip with no journal elements. No suppression annotations, no dead code, no naming deviations.
</review>
