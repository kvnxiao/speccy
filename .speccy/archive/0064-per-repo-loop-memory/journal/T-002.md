---
spec: SPEC-0064
task: T-002
generated_at: 2026-06-13T05:04:54Z
---

<implementer date="2026-06-13T05:04:54Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area-mapped the work-phase recipe and the CHK-004 test surface. Reuse-as-is — the `{% include "modules/references/memory-ledger.md" %}` directive (the T-001 reference module) and the existing `dogfood_outputs_match_committed_tree` test in `init.rs` (covers the reeject-sync half of CHK-004's first scenario, so no new dogfood test was written). Extend — none. Write-fresh — `speccy-cli/tests/memory_feedforward.rs` (the CHK-004 structural placement test); searched `speccy-cli/tests/` for an existing memory/feedforward test and found none, so the new file is warranted, modelled on `tests/persona_snippets.rs`'s include-directive assertions. The new step's prose mirrors the existing reuse-survey/journal step shape rather than inventing a new format.
- Completed: Inserted a new numbered step 6 ("Load the memory ledger slice") into `resources/modules/phases/speccy-work.md` before the bounded reuse survey, instructing the implementer to read `.speccy/MEMORY.md` when present, load the trigger-matched slice (mirroring the journal "load slice, drill in on demand" shape), and treat absence as a silent no-op; the step `{% include %}`s the T-001 memory-ledger reference rather than restating the entry shape. Renumbered steps 6-10 to 7-11 and updated both cross-references in step 3's first-attempt-branch prose ("steps 4-10" -> "4-11", journal-append "step 9" -> "step 10"). Ran `just reeject`; `.claude/agents/speccy-work.md` and `.codex/agents/speccy-work.toml` regenerated with the expanded reference inline (no `.agents` host ships a speccy-work subagent, so none changed). Added `speccy-cli/tests/memory_feedforward.rs` with the CHK-004 structural test.
- Undone: Nothing in scope. CHK-003 (the read actually changes the produced diff) is semantic and validated only by dogfooding, per the task body — not implementable as an automated test here.
- Hygiene checks: cargo test --workspace -> exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings -> exit 0; cargo +nightly fmt --all --check -> exit 0; cargo deny check -> exit 0.
- Evidence: REQ-003 covered. CHK-004 (structural) demonstrated by `tests/memory_feedforward.rs::work_phase_body_includes_memory_ledger_reference_once` (include exists exactly once in the canonical body) and `::no_host_wrapper_inlines_memory_ledger_include` (no wrapper under resources/agents/ inlines the directive); written red (failed: found 0, before edit) then green after the phase-body edit. The reeject-sync half is demonstrated by the pre-existing `tests/init.rs::dogfood_outputs_match_committed_tree` passing post-reeject. CHK-003 (dogfood, diff-changes-behaviour) is judgment-only here — semantic, deferred to dogfooding per the task body.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work first-attempt branch (clean tree, no journal). Edited the source module under `resources/` and ran `just reeject`; did not patch ejected files. No shipped-body real-identifier leak (test references SPEC-0064 in assertion messages, which is Speccy's own dogfood artifact, not shipped template text). Test avoids vacuous-test traps: it gates the include-directive structural surface (a real invariant), not curated prose, complying with DEC-009.
</implementer>
<review date="2026-06-13T05:06:28Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Verified the step-renumbering control flow in the prose edit is internally consistent: `resources/modules/phases/speccy-work.md` now runs steps 1–11, and every cross-reference was updated in lockstep — the "steps 4–10"→"4–11" first-attempt-branch range, the retry-branch journal-append "step 9"→"step 10", and the sequential headers 6→11 with no duplicate or skipped ordinal. Both ejected hosts (`.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`) carry the identical renumbering and the expanded memory-ledger reference inline, with no stale step pointer. The new step-6 logic is correct: read the slice when `.speccy/MEMORY.md` is present, silent no-op on absence. The CHK-004 test in `speccy-cli/tests/memory_feedforward.rs` is logically sound — exact-once via `body.matches(...).count() == 1`, and a recursive `collect_files` walk asserting no `resources/agents/` wrapper inlines the include; the `read_dir`-error early-return is benign because the checked-in `resources/agents/` directory exists. No logic, boundary, Option/Result, or resource-leak defects on any reachable path.
</review>
<review date="2026-06-13T05:06:30Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concern in scope. This task is a prose/template change (a new step 6 in `resources/modules/phases/speccy-work.md`) plus a structural placement test (`speccy-cli/tests/memory_feedforward.rs`). Reviewed against the untrusted-input-into-agent-context angle: the implementer now reads `.speccy/MEMORY.md`, but that file is user-owned and git-tracked, the same trust class as `AGENTS.md`/`BACKLOG.md`/`SPEC.md` the loop already loads — no new trust boundary is crossed and no externally-fetched/attacker-controlled feed is introduced. The diff correctly honors DEC-005's adversarial-review posture: no ledger read was added to the reviewer or vet bodies, so a poisoned "we decided X is fine" entry cannot blunt review. The included reference (`modules/references/memory-ledger.md:39-44`) explicitly requires real, non-fabricated provenance and abstract wording over fragile coordinates, which limits stale/phantom guidance feeding forward. No secret handling, auth boundary, crypto, or injection sink touched. The test reads files via `fs_err` with paths derived from `CARGO_MANIFEST_DIR` (no traversal), `.expect()` is test-only. Ejected `.claude/agents/speccy-work.md` and `.codex/agents/speccy-work.toml` are in sync with the source module (reference expanded inline at eject time; directive-only in source and wrappers).
</review>
<review date="2026-06-13T05:06:50Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-003 fully delivered. All three <done-when> bullets map to the diff:
(1) feed-forward step is in the canonical module
`resources/modules/phases/speccy-work.md` (new step 6, before the reuse
survey and any code write per the requirement's ordering) and absent as an
inline copy in any `resources/agents/` wrapper (grep clean; gated by
`memory_feedforward.rs::no_host_wrapper_inlines_memory_ledger_include`);
(2) the ejected `.claude/agents/speccy-work.md` and
`.codex/agents/speccy-work.toml` carry the expanded reference with no drift
(`init.rs::dogfood_outputs_match_committed_tree` passes post-reeject);
(3) absent-ledger is an explicit silent no-op in the step prose ("proceed
with no error or comment about memory"), matching the <behavior> identical-
to-today's-loop case. The matched-trigger <behavior> case reads the slice
before writing code via the step's placement (6 before reuse survey 7 and
implement 8) and the "load the relevant slice, drill in on demand" phrasing
mirrors the journal shape REQ-003 names. CHK-003 (diff-changes-behaviour) is
correctly deferred to dogfooding, as the SPEC marks it dogfood-only.
Non-goals respected: no `speccy memory` CLI verb, no reviewer/vet read added
(DEC-005), no new lint family — the changeset is prose + one structural test
only. Open questions are all resolved (none outstanding) and the single
changelog entry shows no intent shift. CHK-004 structural test passes.
</review>
<review date="2026-06-13T05:07:10Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-004 is covered by a genuine, non-vacuous structural test. `speccy-cli/tests/memory_feedforward.rs::work_phase_body_includes_memory_ledger_reference_once` asserts the `{% include "modules/references/memory-ledger.md" %}` directive appears exactly once in `resources/modules/phases/speccy-work.md` — it goes red (count 0) if the feed-forward step is dropped and red (count 2) if duplicated, so it catches both the missing-instruction and accidental-duplication failure modes. `::no_host_wrapper_inlines_memory_ledger_include` walks every file under `resources/agents/` and fails if any host wrapper inlines the directive, gating the no-duplicate-snippet invariant; I confirmed the vec is non-empty (40+ wrappers walked, dotted host dirs recursed correctly). Independently verified via grep that the include lives in exactly one source file (the canonical phase body) and that both `speccy-work` wrappers pull the body transitively via `{% include "modules/phases/speccy-work.md" %}`, so the reference reaches every host without a shadowing copy. Both tests pass; `tests/init.rs::dogfood_outputs_match_committed_tree` also passes, confirming the reeject'd `.claude`/`.codex` bodies are byte-in-sync with the renderer (the task-scenario's second half). Evidence roll call is complete: CHK-004 `demonstrated` (named tests, red-then-green claim "found 0 before edit" consistent with the assertion logic), CHK-003 `judgment-only` — correctly deferred, since the behavioural "read changes the diff" check requires running the implementer subagent during dogfooding and is not expressible as a deterministic command, and substring-matching the prose instruction would violate DEC-009 / the project's test-hygiene rule. Test naming and structure match the `tests/persona_snippets.rs` include-directive convention the implementer cited. No fabrication patterns matched.
</review>
<review date="2026-06-13T05:07:49Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No blocking style issues found.

`resources/modules/phases/speccy-work.md`: new step 6 ("Load the memory ledger
slice") matches the existing prose style, numbered correctly (4–11), and the
cross-reference in step 3's first-attempt branch ("steps 4–11") and retry branch
("step 10") were updated consistently. The `{% include
"modules/references/memory-ledger.md" %}` directive placement inside the step body
is idiomatic and matches the existing reuse-survey and convention-checklist include
pattern.

`speccy-cli/tests/memory_feedforward.rs`: file follows the `persona_snippets.rs`
model it was asked to mirror — same `#![allow(clippy::expect_used, reason = "...")]`
header, same `workspace_root()` helper, same `fs_err` usage, same `assert!` /
`assert_eq!` message style with file path + invariant description. The
`collect_files` helper is a near-duplicate of `walk_collect` in
`skill_body_discovery.rs`, but per-test-file independence makes this acceptable
(no common module exposes a walk helper). Const name `MEMORY_LEDGER_INCLUDE` is
consistent with the `EXPECTED_SNIPPET_FILES` naming convention in the sibling
file.

One non-blocking note: both the new file and the sibling `persona_snippets.rs`
use `#![allow(clippy::expect_used, reason = "...")]` rather than `#[expect]`
as AGENTS.md prescribes. This is pre-existing drift in the test tier; the lint
passes in both files under the workspace `clippy::expect_used = "deny"` setting,
so no new violation is introduced here. Grounding: the project's `clippy.toml`
sets `allow-expect-in-tests = true`, which suppresses `expect_used` in test
code automatically — the `#![allow]` annotation in both files is thus gratuitous
but harmless, consistent with the pre-existing sibling, and not a new failing
lint introduced by this diff.

Reuse survey present and honest: `{% include %}` convention reused from
`reuse-survey-implementer.md` include pattern; no symbol claimed to be reused
that does not exist; no reinvention of an available helper.
</review>
