---
spec: SPEC-0064
task: T-004
generated_at: 2026-06-13T05:19:23Z
---

<implementer date="2026-06-13T05:19:23Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the task area (init.rs, verify.rs, ARCHITECTURE.md File Layout). Reuse-as-is: `project_with_name`/`write_file`/`read_file` (init.rs fixtures) for the CHK-001 byte-identity test, modelled on the existing `force_preserves_user_files`; `Workspace`/`invoke`/`field`/`spec_md_template`/`write_spec` (verify.rs + common/mod.rs) for the CHK-008 softness test. Write-fresh: three new test fns — searched init.rs/verify.rs for any existing MEMORY.md coverage and found none (this SPEC introduces the file). No new top-level production symbols: DEC-002 means the invisibility property holds for free, so no production code changed.
- Completed: Added CHK-001 regression tests to speccy-cli/tests/init.rs (`force_preserves_speccy_memory_ledger` — seeds .speccy/MEMORY.md, runs `init --force` for claude-code then codex in one fixture, asserts byte-identity after each; `fresh_init_does_not_create_speccy_memory_ledger` — asserts a fresh init creates none). Added CHK-008 test to speccy-cli/tests/verify.rs (`malformed_speccy_memory_does_not_affect_verify_lint_output` — captures the no-MEMORY lint baseline, writes a malformed ledger, asserts exit code and the whole `lint` object are byte-equal to baseline). Documented MEMORY.md in docs/ARCHITECTURE.md File Layout tree at top level of .speccy/ (sibling of specs/), noting user-owned, git-tracked, never-enumerated/overwritten by init, never read by verify.
- Undone: Nothing in scope deferred. No production code changes by design (DEC-002). No new lint code referencing the ledger (REQ-007 forbids it).
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0 (one wrap fix applied); `cargo deny check` exit 0.
- Evidence: CHK-001 (REQ-001) — demonstrated by `force_preserves_speccy_memory_ledger` (byte-identity across both shipped hosts) and `fresh_init_does_not_create_speccy_memory_ledger` (absence on fresh init); the docs `done-when` is judgment-only (DEC-009 forbids substring-matching curated prose) and satisfied by the ARCHITECTURE.md File Layout edit. CHK-008 (REQ-007) — demonstrated by `malformed_speccy_memory_does_not_affect_verify_lint_output` (lint object byte-equal to the no-MEMORY baseline; the "no new lint code" half is judgment-only, satisfied by adding no lint code). All scenarios green under `cargo test --workspace`.
- Discovered issues: docs/ARCHITECTURE.md does not currently document `.speccy/BACKLOG.md` in the File Layout tree despite the SPEC citing it as the never-overwritten precedent; documented MEMORY.md standalone rather than expanding scope to add BACKLOG.md.
- Procedural compliance: No shipped skill/module friction encountered; no resources/ edits needed. Tests live in the test crates only; no production source touched, consistent with DEC-002.
</implementer>
<review date="2026-06-13T05:20:29Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in this test-and-docs-only slice; no production code changed (DEC-002), so no auth, secret-handling, injection, or crypto surface is touched.

Checked: (1) the new init.rs fixtures (`force_preserves_speccy_memory_ledger`, `fresh_init_does_not_create_speccy_memory_ledger`) and verify.rs test (`malformed_speccy_memory_does_not_affect_verify_lint_output`) all write to fixed relative paths joined onto per-test temp roots — no path-traversal or untrusted-input surface. (2) The CHK-008 test positively pins that `speccy verify` never parses `.speccy/MEMORY.md`, which is itself a sound security property: a freely-editable, git-tracked user file is never fed to the lint parser, so malformed/hostile ledger content cannot become an injection vector into lint output or a parser-driven failure. (3) No secrets, credentials, or sensitive data appear in the seed fixtures or docs; the seed content is benign placeholder prose. (4) No new dependency. (5) The ARCHITECTURE.md File Layout edit accurately documents the file as user-owned, git-tracked, and never read by verify — no security claim is overstated.
</review>
<review date="2026-06-13T05:21:17Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Both covered requirements map cleanly to the diff. REQ-001/CHK-001: `force_preserves_speccy_memory_ledger` (speccy-cli/tests/init.rs) seeds non-empty `.speccy/MEMORY.md`, runs `init --force` for claude-code then codex in one fixture, and asserts byte-identity after each — matching CHK-001's "both hosts" wording and proving the eject pipeline never enumerates the ledger; `fresh_init_does_not_create_speccy_memory_ledger` covers the absence half of the `<behavior>`. REQ-007/CHK-008: `malformed_speccy_memory_does_not_affect_verify_lint_output` (speccy-cli/tests/verify.rs) compares the full `lint` object and exit code between a no-MEMORY baseline and a deliberately-malformed ledger; the malformed body embeds dangling `REQ-999 CHK-999` and a fake `<report>` tag, so the test genuinely guards that verify does not read the file (it would trip RPT-001/foreign-tag lints if it did), and the byte-equality framing is rot-resistant against future unrelated lint families. The docs `done-when` is satisfied by the ARCHITECTURE.md File Layout edit placing `MEMORY.md` at the top level of `.speccy/` (sibling of `specs/`) with user-owned / git-tracked / never-overwritten / never-read-by-verify status, mirroring BACKLOG.md per DEC-002; per DEC-009 this is judgment-only, not a substring assertion. Non-goals respected: no `speccy memory` verb, no new lint family, no enforcement surface; `git diff` confirms zero production source under speccy-core/ or speccy-cli/src/ changed, consistent with DEC-002's "holds for free." All three tests pass under `cargo test`.
</review>
<review date="2026-06-13T05:21:29Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found. Test-and-doc-only change; no production code touched (DEC-002 makes the invisibility property hold for free).

Verified the three additions' control flow and assertion soundness:
- `force_preserves_speccy_memory_ledger` (init.rs): seeds `.speccy/MEMORY.md`, runs `init --force` for claude-code then codex in one fixture, string-compares read-back to seed after each. `write_file` creates the `.speccy/` parent; both host flags are valid (`HostChoice` -> "claude-code"/"codex"). Sound and would catch a regression where init began enumerating the ledger.
- `fresh_init_does_not_create_speccy_memory_ledger` (init.rs): negative existence assertion after a fresh init. Sound.
- `malformed_speccy_memory_does_not_affect_verify_lint_output` (verify.rs): captures the no-MEMORY lint baseline first, then writes a malformed ledger and re-invokes, asserting both exit code and the whole `lint` object are byte-equal. The equality is well-founded, not coincidentally green: `scan_with_archive` (speccy-core/src/workspace.rs) enumerates only `.speccy/specs/` and optionally `.speccy/archive/`, never top-level `.speccy/` files, so verify's output is provably independent of `.speccy/MEMORY.md`. `field` returns the real `lint` object (always present), not the `JSON_NULL` fallback.
- docs/ARCHITECTURE.md File Layout edit is documentation prose; no executable behavior.

Ran all three new tests: 3 passed, 0 failed. No off-by-one, inverted-condition, Option/Result-mishandling, or resource-leak issues at or above the reporting bar.
</review>
<review date="2026-06-13T05:21:56Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style criteria satisfied.

Reuse survey: `project_with_name`, `write_file`, `read_file` (init.rs), `Workspace`, `invoke`, `field`, `spec_md_template`, `write_spec` (verify.rs/common) all exist at the claimed locations and are the symbols the diff actually calls. Survey is honest.

Test shape: both new tests gate real invariants — byte-identity of `.speccy/MEMORY.md` after `init --force` (CHK-001) and lint-output equality with/without a malformed ledger (CHK-008). Neither is vacuous; the "lint output unchanged" framing prevents unrelated-family rot per AGENTS.md. No `unwrap()`/bare-`expect()` in production code. No `#[allow(...)]` suppressions.

Naming and pattern: `force_preserves_speccy_memory_ledger`, `fresh_init_does_not_create_speccy_memory_ledger`, `malformed_speccy_memory_does_not_affect_verify_lint_output` match existing underscore/verb-noun conventions. `fs_err::write(...).as_std_path()` pattern in verify.rs matches every other call site.

Comment style: init.rs adds a pre-`#[test]` block comment not present on other tests in that file; verify.rs uses the established three-line `// ---` separator block. Minor stylistic drift in init.rs (no prior test uses a pre-`#[test]` section comment), not blocking.

ARCHITECTURE.md change: `MEMORY.md` line added at top level of `.speccy/`, above `specs/`, describing user-owned / git-tracked / never-enumerated / never-read-by-verify status. Accurate and consistent with `BACKLOG.md` precedent cited in DEC-002.

No dead imports, unused variables, or orphaned symbols introduced. No new lint codes referencing the ledger (REQ-007 satisfied structurally — diff adds no lint code).
</review>
<review date="2026-06-13T05:22:16Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Both covered CHKs are exercised by real, non-vacuous tests in the diff that drive production code paths and catch a wrong implementation.

CHK-001 (REQ-001): `init.rs::force_preserves_speccy_memory_ledger` seeds `.speccy/MEMORY.md` with non-empty content and runs the real binary (`Command::cargo_bin("speccy")`) `init --force` for both shipped hosts (`claude-code` then `codex`, confirmed distinct eject targets in `host.rs::dest_dirs`), asserting byte-identity after each — matches the REQ-001 "every shipped host" behavior clause. `fresh_init_does_not_create_speccy_memory_ledger` asserts absence on fresh init. Falsified: an eject pass that enumerated/overwrote the ledger flips the `assert_eq!(after, seed)`; a scaffolding init flips the `!exists()`. Ran both: green.

CHK-008 (REQ-007): `verify.rs::malformed_speccy_memory_does_not_affect_verify_lint_output` captures the no-MEMORY lint baseline via the real `run(VerifyArgs)`, writes a malformed ledger, re-runs, and asserts the whole `lint` object plus exit code are byte-equal to baseline. Falsified: a verify that read/linted the ledger gains a `lint` entry and trips `assert_eq!(lint, baseline_lint)`. Baseline-equality framing means it won't rot when an unrelated lint family lands. Ran: green.

Docs scenario (judgment-only per DEC-009): `docs/ARCHITECTURE.md` File Layout now lists `MEMORY.md` as user-owned, git-tracked, never enumerated/overwritten by init, never read by verify — confirmed by inspection.

Non-blocking note: the Evidence roll call labels both CHKs `demonstrated`, which per `journal-implementer.md` requires a red-then-green `evidence/T-004.md` paper trail; none exists and none is needed — this is a no-production-change regression/structural slice (DEC-002), exactly the case sibling T-001 handled by labelling `hygiene`/`judgment-only` and stating "no evidence file." `hygiene` is the precise label here. Not blocking: the roll call accounts for every CHK under both covered REQs, the three cited test names all exist in the diff, and I re-ran them; no coverage gap, omission, or fabrication. Tighten the label to `hygiene` on any future round.
</review>
