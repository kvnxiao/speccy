---
spec: SPEC-0039
task: T-005
generated_at: 2026-05-22T00:00:00Z
---

<implementer date="2026-05-22T00:00:00Z" model="claude-opus-4.7[1m]/low" round="1">

**Completed.** Retired the legacy Codex prose-spawn idiom in
`resources/modules/skills/speccy-review.md`. The `{% else %}` (Codex)
branch of step 2's host-divergence block now reads "Invoke Codex's
native sub-agent-spawn primitive four times in parallel, once per
persona, against the registered Codex sub-agents `reviewer-business`,
`reviewer-tests`, `reviewer-security`, and `reviewer-style`" —
mirroring the canonical native-primitive wording already in use in
`resources/modules/skills/speccy-orchestrate.md` (lines 139, 160, 175).
The per-persona names remain in backtick form so the existing
`reviewer-<persona>` prose needles in the test still match.

Updated `speccy-cli/tests/skill_packs.rs::speccy_review_skill_prefers_native_subagents`:
- Header comment around line 649 now describes the Codex branch as
  "Codex's native sub-agent-spawn primitive against each registered
  `reviewer-<persona>` sub-agent" instead of "Codex's prose-spawn by
  name".
- The Claude-side negative assertion that pinned
  `"Prose-spawn the four reviewer subagents"` was replaced with a
  case-insensitive `!contains("prose-spawn")` guard on both renders
  (per SPEC-0039 REQ-005 / CHK-013), keeping the negative semantics
  while pinning the broader repo-wide retirement at the rendered-output
  layer.
- A new positive Codex assertion pins the literal substring
  `"Codex's native sub-agent-spawn primitive"` in the rendered Codex
  SKILL.md so a future edit that drops the native-primitive wording
  fails this test first.

Regenerated ejected packs via the locally-built CLI: `cargo clean -p
speccy-cli && cargo build -p speccy-cli` (the `include_dir!` snapshot
needed a clean rebuild to pick up the source change), then
`./target/debug/speccy init --force --host codex` and
`./target/debug/speccy init --force --host claude-code`. Only
`.agents/skills/speccy-review/SKILL.md` rerendered (one overwrite);
the Claude render was already byte-identical to its source.

**Undone.** None. Scope was limited to REQ-005 / T-005 (the prose-spawn
retirement and matching test update). The remaining T-006 (ARCHITECTURE.md
sweep) and T-007 (positioning rewrite) are separate tasks.

**Hygiene checks.** All four green:
- `cargo test --workspace` passes (37 skill_packs tests pass; full
  workspace suite green).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exits 0.
- `cargo +nightly fmt --all --check` exits 0.
- `cargo deny check` reports `advisories ok, bans ok, licenses ok,
  sources ok`.

**Evidence.**
- Source rewrite: `resources/modules/skills/speccy-review.md` line 95-97.
- Test update: `speccy-cli/tests/skill_packs.rs` lines 643-760
  (the `speccy_review_skill_prefers_native_subagents` test plus the
  header comment around line 649).
- Rendered Codex output: `.agents/skills/speccy-review/SKILL.md`
  step 2 now reads "Invoke Codex's native sub-agent-spawn primitive
  four times in parallel..." with the four backtick-named personas.
- CHK-013 scenario:
  `Grep('prose.?spawn', path='resources', -i=true)` reports
  `Found 0 total occurrences across 0 files`.
- CHK-014 scenario: `cargo test -p speccy-cli --test skill_packs`
  exits 0 with `speccy_review_skill_prefers_native_subagents` among
  the 37 passing tests.

**Discovered issues.** The user-supplied note about a stale
`~/.cargo/bin/speccy` on PATH proved load-bearing: I used the locally
built `target/debug/speccy` throughout. Additionally, the first
`speccy init --force` after editing the source module reported
everything `unchanged` because the `include_dir!` snapshot in
`speccy-cli/src/embedded.rs` did not auto-invalidate the cached build
artifact when only a leaf file under `resources/modules/` changed.
A `cargo clean -p speccy-cli` followed by a rebuild fixed it. This is
a known Cargo limitation with `include_dir!`; not in scope for this
task to address, but worth noting if it recurs across other resource
edits — a `build.rs`-emitted `rerun-if-changed=resources` would be
the canonical fix.

**Procedural compliance.** Followed the `/speccy-work` single-task
primitive contract: read SPEC/TASKS/CODEX-SKILLS-AND-SUBAGENTS.md,
flipped T-005 to `in-progress`, made the surgical edits, ran the four
hygiene gates, regenerated the ejected packs, and flipped T-005 to
`in-review` only after all gates passed. No other tasks touched.

</implementer>

<review date="2026-05-22T21:50:30Z" model="claude-opus-4.7[1m]/medium" persona="business" verdict="pass" round="1">
T-005 satisfies REQ-005 cleanly. The Codex `{% else %}` branch in `resources/modules/skills/speccy-review.md:95-98` was rewritten from "Prose-spawn the four reviewer subagents by name" to "Invoke Codex's native sub-agent-spawn primitive four times in parallel, once per persona, against the registered Codex sub-agents..." — mirroring the Claude branch's per-persona structure and matching the canonical native-primitive wording already in use across `speccy-orchestrate.md`. The four reviewer persona names remain in backtick form so per-persona test needles still match.

The matching test update in `speccy-cli/tests/skill_packs.rs::speccy_review_skill_prefers_native_subagents` (lines 689-748) is well-scoped: the old `"Prose-spawn the four reviewer subagents"` literal pin became a case-insensitive `!contains("prose-spawn")` guard on both rendered branches, and a new positive Codex assertion pins the literal `"Codex's native sub-agent-spawn primitive"`. Header comment around line 651-652 was updated to reflect the SPEC-0039 retirement.

REQ-005's `<done-when>` items verified against the diff:
- Codex branch describes native primitive (line 95-98 of the source module) — yes.
- Zero `prose-spawn` matches under `resources/modules/`, `resources/agents/.agents/`, `resources/agents/.codex/` — verified via grep.
- Test assertions updated and `cargo test -p speccy-cli --test skill_packs` exits 0 with all 37 tests green.
- The new orchestration skills' Codex branches already use the native primitive (verified in `speccy-orchestrate.md`; that is T-001/T-002 scope but the REQ-005 reaffirmation holds).

CHK-013 and CHK-014 scenarios both pass. No non-goal was breached (no new lint family, no CLI surface change, no jinja-variable configurability of the wording). No open question was silently resolved (the spec's open questions block is "(None.)"). The implementer's note about the `include_dir!` cache invalidation is correctly flagged as out-of-scope discovery rather than a silent scope expansion.

The rendered Codex SKILL.md at `.agents/skills/speccy-review/SKILL.md` is currently unstaged in the working tree, which is the normal speccy mid-loop state (implementer flipped state to in-review without committing; ship-time commit comes later). The task's "git status --porcelain ... prints zero lines" scenario is interpreted as the post-ship committed state, not the live working tree.
</review>

<review date="2026-05-22T21:50:30Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-005's test updates are load-bearing in both directions. The new positive pin `codex_body.contains("Codex's native sub-agent-spawn primitive")` (skill_packs.rs:738-741) catches a future rewrite that drops the native-primitive wording, and the case-insensitive negative `!codex_body.to_lowercase().contains("prose-spawn")` (line 735) catches reintroduction of the legacy idiom on either render (also pinned for `claude_body` at line 717). The Claude branch retains its full `subagent_type: "reviewer-<persona>"` per-persona assertions (lines 700-710), so the host-divergence is symmetrically pinned. `cargo test -p speccy-cli --test skill_packs` exits 0 with all 37 tests passing (CHK-014 satisfied). `rg -in 'prose.?spawn' resources/modules resources/agents/.agents resources/agents/.codex` returns zero matches (CHK-013 satisfied). Mutation thought experiment: rewriting the Codex `{% else %}` branch to wording that lacks "Codex's native sub-agent-spawn primitive" would fail the positive pin even if it avoided the "prose-spawn" substring — the positive assertion is doing real work, not just the negative. The implementer's inline `**Evidence.**` section (journal lines 57-70) is the established convention for this SPEC family (T-001 explicitly documents this as the defensible pattern for Markdown-only shipped-content tasks where scenarios are property-grep assertions on file text) — no separate `.speccy/specs/0039-.../evidence/T-005.md` file is required by the SPEC-0039 convention. Minor non-blocking observation: CHK-013's regex `prose.?spawn` would also match `prose spawn` (space), `prose_spawn`, etc.; the cargo-side guard only matches the literal `prose-spawn` substring. The structural CHK-013 grep is the broader guarantee, so this gap doesn't compromise REQ-005's done-when shape. Note also that `.agents/skills/speccy-review/SKILL.md` shows as `M` (unstaged) under `git status --porcelain`, which conflicts with task-scenario 4's "after staging" precondition, but this is a commit-staging artifact mid-SPEC and not a test-coverage issue.
</review>

<review date="2026-05-22T21:50:30Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security-relevant surface in this diff. The change is pure skill-body prose and test assertions — retiring a "prose-spawn" idiom from `resources/modules/skills/speccy-review.md` and updating the `{% else %}` (Codex) branch to native sub-agent-spawn wording. No authentication, authorization, credential storage, input validation, cryptographic, or secret-handling code is touched. No new dependencies are introduced. The rendered markdown in `.agents/skills/speccy-review/SKILL.md` contains no injection vectors. The test case at `speccy-cli/tests/skill_packs.rs:717` correctly uses `.to_lowercase()` when checking for `prose-spawn`, closing any case-sensitivity bypass. CHK-013 grep scope (`resources/modules`, `resources/agents/.agents`, `resources/agents/.codex`) correctly excludes nothing that could harbour a hidden occurrence.
</review>

<review date="2026-05-22T21:50:30Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style criteria pass. The two changed files conform to project conventions:

`resources/modules/skills/speccy-review.md`: The `{% else %}` branch now reads "Invoke Codex's native sub-agent-spawn primitive…" — no "prose-spawn" text survives in the source module or the rendered `.agents/skills/speccy-review/SKILL.md` on disk.

`speccy-cli/tests/skill_packs.rs`: The updated `speccy_review_skill_prefers_native_subagents` test follows the project's existing assertion style — `to_lowercase().contains("prose-spawn")` negative checks plus a positive `contains("Codex's native sub-agent-spawn primitive")` pin. The block-level `#[expect(clippy::expect_used, reason = "…")]` suppression at the file top correctly carries `reason =` per the `allow_attributes_without_reason` lint rule and AGENTS.md convention. Test naming stays in the project's snake_case test-function pattern. No `unwrap()` or bare `#[allow]` annotations introduced.
</review>
