---
spec: SPEC-0039
task: T-002
generated_at: 2026-05-22T17:00:00Z
---

<implementer date="2026-05-22T17:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Factored the holistic-review pilot at
  `.claude/skills/speccy-holistic-review/SKILL.md` into a new shared
  module at `resources/modules/skills/speccy-holistic-gate.md`. The
  shared body covers Phase 0 (bootstrap and integrity check
  including the all-tasks-completed precondition), Phase 1 (drift
  review + fix loop with the literal "3 rounds per invocation"
  budget), Phase 2 (simplifier scan + apply with hygiene-gated
  rollback), the HOLISTIC.md journal contract with YAML frontmatter
  + per-invocation sections + per-invocation round numbering, the
  single-writer rule, the defer-write-before-rollback pattern, the
  three stash/restore rollback sequences, and the return contract
  (`<orchestrator-verdict>` block with `verdict`, `invocation`,
  `rounds`, `simplifier` fields). YAML frontmatter dropped — the
  per-host wrapper that T-004 authors will carry it. Renamed the
  lifecycle skill from `speccy-holistic-review` to
  `speccy-holistic-gate` (DEC-003) throughout the body and the
  H1 heading. Renamed the two sub-agents from
  `speccy-holistic-reviewer`/`speccy-holistic-fixer` to
  `holistic-reviewer`/`holistic-implementer` (DEC-002); the
  word "fixer" was replaced with "implementer" throughout the prose
  (e.g., "speccy-holistic-pre-fixer-…" stash label →
  "speccy-holistic-pre-implementer-…"). Replaced the four
  Claude-Code-specific `Task` tool dispatch instructions with inline
  `{% if host == "claude-code" %}…{% else %}…{% endif %}` blocks
  per DEC-001 mechanism A, mirroring the precedent at
  `resources/modules/skills/speccy-review.md` lines 74-115: the
  Claude branch instructs the `Task` tool with `subagent_type: "…"`;
  the Codex branch invokes Codex's native sub-agent-spawn primitive
  against the registered sub-agent name. Replaced shell prefixes
  (`/speccy-holistic-review`, `/speccy-orchestrate`, `/speccy-ship`)
  with the jinja `{{ cmd_prefix }}` template variable used by the
  existing `resources/modules/skills/speccy-orchestrate.md` and
  other shared modules. Dropped the `DEC-008` parenthetical that
  pointed at SPEC-0038's decision register (project-local
  reference; the shared body must stay portable).

- Undone: T-003 (factor the two holistic-loop sub-agent pilots
  into `resources/modules/personas/`) and T-004 (per-host wrappers
  + agent templates + render + cleanup) are scoped to subsequent
  task turns per TASKS.md. The body uses `{% if host == ... %}`
  branches and `{{ cmd_prefix }}` references that will only render
  meaningfully once T-004's per-host wrappers exist; in isolation
  this module file is intentionally not yet wired into the render
  pipeline. The pilot file at
  `.claude/skills/speccy-holistic-review/SKILL.md` is left in
  place — its deletion is T-004's responsibility (post-render
  cleanup step 4).

- Hygiene checks: `cargo test --workspace` exited 0 (full suite
  passed, no regressions). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 with zero warnings.
  `cargo +nightly fmt --all --check` exited 0 (no diff). `cargo
  deny check` exited 0 (advisories ok, bans ok, licenses ok,
  sources ok). The task's content scenarios were also exercised
  directly:
  `ls resources/modules/skills/speccy-holistic-gate.md` → exists,
  18616 bytes; `rg -nU '\{% if host == "claude-code" %\}' …` →
  four matches (the four dispatch points: drift reviewer spawn,
  drift implementer spawn, simplifier scan spawn, simplifier apply
  spawn); `rg -n '\b3\b' …` → multiple matches including the
  load-bearing lines 122 ("Round budget: **3 rounds per
  invocation** for drift fixing") and 124 ("so the budget of 3 is
  intentionally tighter…"); `rg -n '\{\{.*round|budget.*\}\}' …`
  → zero matches; `rg -n
  'speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md' …`
  → zero matches.

- Evidence: No `evidence/T-002.md` paper trail was written — this
  task is a content-factoring turn, not a code-behavior change.
  Acceptance is via the four `<task-scenarios>` greps documented
  in TASKS.md (all run inline under "Hygiene checks" above) plus
  the four hygiene gates. The reviewer round can re-run the same
  grep commands against the final file at HEAD to verify.

- Discovered issues: The pilot's HOLISTIC.md file-format section
  references `(DEC-008)` as the basis for the single-writer rule.
  `DEC-008` is a SPEC-0038-local decision register entry, so the
  parenthetical was dropped in the shared body to keep the module
  portable to any speccy-using repo (the rule survives as the
  "Single-writer rule" subheading + paragraph; only the
  cross-SPEC citation was removed). Flagging here in case the
  reviewer prefers to re-add a more portable citation form
  ("per the single-writer rule established in SPEC-0038" would
  still be a project-local doc reference — best to leave the
  shared body decision-register-free).

- Procedural compliance: T-002's `state="..."` attribute in
  `.speccy/specs/0039-cross-harness-orchestration-port/TASKS.md`
  flipped from `pending` → `in-progress` at turn start and from
  `in-progress` → `in-review` as the final step of this turn. No
  `<implementer-note>` block was written into TASKS.md (that
  element was retired in SPEC-0037 and the parser rejects it).
  This `<implementer>` block lives in the per-task journal file
  at `.speccy/specs/0039-cross-harness-orchestration-port/journal/T-002.md`,
  newly created in this turn with the canonical three-field YAML
  frontmatter and `round="1"`. No shipped skill bodies under
  `resources/` (other than the new
  `resources/modules/skills/speccy-holistic-gate.md` itself, which
  is the task deliverable) were edited during this task — no
  friction-to-skill-update was triggered by the shipped
  `/speccy-work` skill body at HEAD.
</implementer>

<review date="2026-05-22T17:30:00Z" model="claude-opus-4-7[1m]/high" persona="business" round="1" verdict="pass">
T-002 faithfully factors `.claude/skills/speccy-holistic-review/SKILL.md` into `resources/modules/skills/speccy-holistic-gate.md`. All four `<task-scenarios>` pass: file exists with non-empty body (18616 bytes); four `{% if host == "claude-code" %}` blocks present (all restricted to sub-agent dispatch points per REQ-001 done-when item 5); literal `3` paired with the drift-fix round budget on lines 122/124/185/464; zero jinja-variable wrapping of `round`/`budget`; zero matches for `speccy-holistic-(review|reviewer|fixer)` or `ARCHITECTURE.md`. The rename and content transformations honor DEC-002 (`fixer` → `implementer` throughout body and stash labels), DEC-003 (lifecycle skill renamed to `speccy-holistic-gate`), DEC-005 (`3` hardcoded inline), and DEC-001 mechanism A (dispatch-point substitution mirroring the `speccy-review.md` precedent). Project-local references were correctly portability-rewritten: `.claude/rules/` → "project-local rule files"; the inline cargo hygiene command list → "the project's four standard hygiene gates"; the `(DEC-008)` parenthetical dropped (the single-writer rule semantics survive as the named subheading). `{{ cmd_prefix }}` usage matches the existing shared-module convention. Legacy pilot file deletion and rendered-output verification are correctly deferred to T-004 per SPEC Notes lines 791-798 — no scope leak. The implementer's "Discovered issues" note about the DEC-008 citation is a reasonable portability call that the SPEC's "shared body stays portable to any speccy-using repo" rule supports.
</review>

<review date="2026-05-22T17:30:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" round="1" verdict="pass">
Task-scenario invariants all verified directly against `resources/modules/skills/speccy-holistic-gate.md` (18616 bytes, non-empty):

- `{% if host == "claude-code" %}` matches at lines 218, 274, 345, 400 — exactly at the four sub-agent dispatch points (drift-reviewer, drift-implementer, simplifier-scan, simplifier-apply). Each has a matching `{% else %}` Codex branch invoking "Codex's native sub-agent-spawn primitive against the registered `holistic-{reviewer,implementer}` sub-agent at `.codex/agents/...`". Restricted to dispatch points only, per the T-001 precedent.
- `\b3\b` matches nine times; the load-bearing pairing on line 122 ("Round budget: **3 rounds per invocation** for drift fixing") and line 124 ("the budget of 3 is intentionally tighter than the per-task implementer retry budget") explicitly pair the literal `3` with the drift-fix round budget on the same line — CHK-002's positive half satisfied.
- `\{\{.*round|budget.*\}\}` returns zero matches. The only `{{ ... }}` occurrences in the file are `{{ cmd_prefix }}` (six instances, never wrapping a budget integer) — CHK-002's negative half satisfied.
- `speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md` returns zero matches — only the post-DEC-003 lifecycle name (`speccy-holistic-gate`) and post-DEC-002 sub-agent names (`holistic-reviewer`, `holistic-implementer`) appear.

Evidence pattern is defensible for this shipped-Markdown task on the same grounds T-001's tests reviewer accepted: task-scenarios are property-grep assertions on file text (no binary behaviour to red-then-green), and the journal's Hygiene-checks block records the exact rg counts that map 1:1 to the four scenarios (4 / multi-with-budget-pairing / 0 / 0). `cargo test --workspace`, `cargo clippy ... -D warnings`, `cargo +nightly fmt --all --check`, and `cargo deny check` all exited 0. No mocking-without-real-call, no empty-assertion, no snapshot-bakes-in-bug failure modes apply to a content-factoring deliverable.
</review>

<review date="2026-05-22T17:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" round="1" verdict="pass">
Skill instruction document only — no executable code, no authentication boundaries, no credential handling. Sub-agent dispatch targets are hardcoded string literals (`holistic-reviewer`, `holistic-implementer`, `code-simplifier:code-simplifier`), not user-interpolated. The `<base-ref>` resolved from `git symbolic-ref | sed` is injected into natural-language agent prompts, not into a shell `eval` or unquoted expansion. Rollback sequences use `git restore .` + `git clean -fd` (not `-fdx`), which correctly excludes gitignored files such as `.env` from the blast radius. No legacy names, no `ARCHITECTURE.md` references, and no Jinja variable wrapping of the literal budget integer `3` — all verified by grep. Defer-write pattern is correctly ordered to survive rollback. Nothing here touches auth, secrets, crypto, or live service endpoints.
</review>

<review date="2026-05-22T17:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" round="1" verdict="pass">
All style criteria pass for `resources/modules/skills/speccy-holistic-gate.md`.

- No YAML frontmatter (file opens with a blank line then the `# {{ cmd_prefix }}speccy-holistic-gate` heading, matching `speccy-orchestrate.md` and `speccy-review.md` byte-for-byte in structure).
- `{% if host == "claude-code" %}…{% else %}…{% endif %}` blocks are confined to the four dispatch points (lines 218, 274, 345, 400), matching the DEC-001 mechanism-A discipline enforced in `speccy-review.md:74` and `speccy-orchestrate.md:123/144/159`.
- `{{ cmd_prefix }}` template variable is used consistently for all skill invocations — no bare `/speccy-*` literals remain.
- The round budget `3` appears as a hardcoded integer (lines 122/124/185/464); zero jinja variable references for it.
- Zero occurrences of `speccy-holistic-review`, `speccy-holistic-reviewer`, `speccy-holistic-fixer`, or `ARCHITECTURE.md`.
- Trailing newline present.
- `{% if host %}` indentation follows the surrounding list depth, consistent with the two reference modules.
- No dead imports, unused variables, or suppression annotations (markdown module — N/A for lint annotations).
</review>
