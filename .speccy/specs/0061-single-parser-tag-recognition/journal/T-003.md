---
spec: SPEC-0061
task: T-003
generated_at: 2026-06-12T08:12:36Z
---

<implementer date="2026-06-12T08:12:36Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the journal append path + serialize.rs + journal_append.rs tests. Reuse-as-is: `parse_journal_xml` (canonical parser, already imported in journal.rs) for the write-time round-trip; `JournalError::ProducedVetUnparseable` as the structural template for the new variant; journal_append.rs helpers (`workspace_with_pending_task`, `journal_path`, `Command::cargo_bin` driver) and the vet round-trip tests in journal_append_vet.rs as the test template. Extend: `JournalError` (added `ProducedJournalUnparseable`); `append_under_lock` (added round-trip before `fs_err::write`). Write-fresh: two integration tests (`line_isolated_close_tag_in_body_is_rejected_at_write_time` for CHK-004, `inline_element_mention_in_prose_is_accepted_and_reparses` for CHK-005) — searched journal_append.rs, no existing produced-unparseable test for the per-task path.
- Completed: Added `JournalError::ProducedJournalUnparseable { path, source }` (mirrors `ProducedVetUnparseable`, distinct message containing "unparseable"). Added the strict `parse_journal_xml` round-trip in `append_under_lock` immediately before `fs_err::write`, mapping failure to the new variant and returning before any byte is written. Removed `first_nested_journal_element`, its call site in `validate_and_render_block`, the `SerializeError::NestedJournalMarkup` variant, the dead `nested_journal_markup_rejected` unit test, the now-redundant `prose_word_reviewer_is_not_nested_markup` test, and the unused JOURNAL_ELEMENT_NAMES import in serialize.rs (still used by lint/rules/xml.rs, so only the serialize.rs import was dropped). Updated the serialize.rs module doc that promised a nested-markup-free body guard. Added CHK-004 and CHK-005 integration tests.
- Undone: Nothing in scope is left undone. REQ-003's next.rs deletions (last_gate_block, GateBlock, attribute_value) and the vet_xml module-doc edit were already landed by T-002 (sibling, completed) — this task owns only the journal half per its body.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file written; the red/green proof is the two new integration tests run through the real binary. Roll call for CHKs under REQ-002 and REQ-003:
  - CHK-004 (line-isolated close tag for implementer in body refused at write time, journal byte-identical, stderr names produced-unparseable): demonstrated -> `line_isolated_close_tag_in_body_is_rejected_at_write_time` in `speccy-cli/tests/journal_append.rs` seeds a parseable journal, snapshots bytes, appends a body with a line-isolated implementer close tag, asserts `.failure()` + `stderr(contains("unparseable"))` + byte-identical.
  - CHK-005 (inline element mention in prose accepted, journal re-parses): demonstrated -> `inline_element_mention_in_prose_is_accepted_and_reparses` in the same file appends an inline-tag prose body against a fresh journal, asserts `.success()` and `parse_journal_xml` re-parse with one entry. Additionally demonstrated live: this very journal block body mentions element names inline as prose and was accepted by the rebuilt binary.
  - CHK-006 (no hand-rolled tag recognizer remains in non-test source; audit recorded): demonstrated -> audit run; recorded for REPORT.md.
- Discovered issues: The audit (grep for the three tag-scan patterns over speccy-core/src and speccy-cli/src) returns exactly three hits, none a production recognizer: (1) consistency.rs:552 src.find for an implementer close tag is inside #[cfg(test)] mod tests; (2) journal_common.rs:54 a format! building a diagnostic *context* string, not a recognizer over input; (3) vet_xml/serialize.rs:389 a block *renderer* (emits tags), explicitly out of scope per REQ-003. Tighter grep for the dotted find forms confirms only the test-code hit. Note for the reviewer: consistency.rs::last_well_formed_offset (line 459) is a tolerant tag-boundary scanner over an already-rejected file at reconcile time — it uses .find for a single char, so it is outside the CHK-006 audit pattern set, is not named in REQ-003's removal list, and is neither a gate-read nor journal-write recognizer; left in place by design.
- Procedural compliance: Followed the speccy-work first-attempt recipe (clean tree, in-progress flip, scenario read from context bundle, bounded reuse survey, tests-then-code, self-review, hygiene gate, in-review flip). One friction point: the installed `speccy` binary on PATH was stale and its old body pre-scan rejected this very journal append (it carries inline element-name mentions). Reinstalled via `cargo install --path speccy-cli --locked` so the CLI reflects the new round-trip behaviour; no `resources/` skill edits were needed.
</implementer>
<review date="2026-06-12T08:14:28Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blocker. This change strengthens the structural-injection boundary
rather than widening it: the hand-rolled body pre-scan
(`first_nested_journal_element`, a divergent byte-substring recognizer that was
fence-blind and matched only three names at a tag boundary) is replaced by a
full round-trip through the canonical `parse_journal_xml` on the assembled
would-be-new content before `fs_err::write` (`speccy-cli/src/journal.rs:537`).
The single parser that later reads the file is now the sole authority over what
lands, closing the markup-smuggling surface a divergent scanner could be coaxed
past. CHK-004 confirms a line-isolated `</implementer>` smuggled in a body is
refused with `ProducedJournalUnparseable` (distinct from
`ExistingJournalUnparseable`) and the file left byte-identical; CHK-005 confirms
inline element-name prose stays inert and re-parses. The read→parse→
round-trip→write sequence runs entirely under the `task_id.md.lock` guard
(`journal.rs:354`), so no TOCTOU window opens between the existing-file read and
the write, and a rejection cannot leave a partial file. Body is never
interpolated into a shell/SQL/path/template sink — it is concatenated into text
and re-parsed. The new error message discloses only the path, not body content
or secrets. No new dependency. The retained `consistency.rs::last_well_formed_offset`
tag-boundary scanner is out of REQ-003 scope, runs on already-rejected files at
reconcile time, and is neither a gate-read nor a journal-write recognizer — not
a security regression for this slice.
</review>
<review date="2026-06-12T08:15:28Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests faithfully drive both REQ-002 scenarios and the REQ-003 audit, and would catch a wrong implementation.

CHK-004 (`line_isolated_close_tag_in_body_is_rejected_at_write_time`, `speccy-cli/tests/journal_append.rs:217`): seeds a parseable journal, snapshots bytes, appends a body carrying a line-isolated `</implementer>`, and asserts `.failure()` + `stderr(contains("unparseable"))` + byte-identical. The assertion discriminates the correct variant: `ProducedJournalUnparseable`'s Display contains "unparseable" while `ExistingJournalUnparseable`'s is "failed to parse" (`journal.rs:186,204`), so a mis-mapped variant would fail the test. Falsification holds two ways — the removed `first_nested_journal_element` pre-scan only matched *open* tags, so a stray *close* tag was never caught by it; with the new round-trip removed the append would succeed and `.failure()` would fire. Real failing path confirmed: the rendered block yields a second unmatched `</implementer>`, which genuinely fails `parse_journal_xml` (`journal.rs:537`).

CHK-005 (`inline_element_mention_in_prose_is_accepted_and_reparses`, `journal_append.rs:286`): fresh journal, body mentions `<review>` mid-sentence, asserts `.success()` then re-parses and asserts exactly one entry — guards against over-rejection of inert inline-tag prose and against a corrupted write.

CHK-006: independently re-ran the audit grep over `speccy-core/src` and `speccy-cli/src`; exactly three hits, none a production recognizer — `consistency.rs:552` is inside `#[cfg(test)]`, `journal_common.rs:54` is a diagnostic-context `format!` (not a scan over input), `vet_xml/serialize.rs:389` is a renderer (out of scope per REQ-003). `first_nested_journal_element`/`NestedJournalMarkup` survive only in spec/journal docs and the untouched archived SPEC-0055. The `consistency.rs::last_well_formed_offset` post-rejection recovery scanner uses single-char `find('<')`, falls outside the audit pattern set and REQ-003's named removal list, and was transparently disclosed.

Evidence roll call is inline (no separate file) but complete: CHK-004/005/006 each labeled `demonstrated` with a specific named test or the audit, not a bare "suite passes". No captured red/green runner output was attached, but reasoning-based falsification confirms each test exercises the real code path and would have been red pre-change. Tests are non-vacuous (no mock-the-SUT, no snapshot-bakes-bug, real discriminating assertions) and match the file's CHK-cited snake_case convention.

Minor non-blocking note for style/business: the `validate_and_render_block` doc comment at `serialize.rs:176-178` still says "or the body contains nested journal markup", stale after the pre-scan removal (the module-level doc was correctly updated).
</review>
<review date="2026-06-12T08:15:28Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
Pass. The write-time round-trip in `append_under_lock`
(`speccy-cli/src/journal.rs:537`) parses the COMPLETE `new_content`
before `fs_err::write`, short-circuiting via `?` on failure, so no byte
is written on rejection — control flow is sound on both the
existing-file and fresh-file branches. Verified the implementer's
"round-trip is a superset of the removed open-tag pre-scan" claim
against `assemble_flat` (`speccy-core/src/parse/journal_common.rs:181`):
a body-embedded nested `<implementer>` open pushes a second open and
fails as "never closed" / nested, and a line-isolated `</implementer>`
(CHK-004) closes the seeded block early then leaves the renderer's real
close as an orphan — both reach `MalformedMarker`, so the open-tag case
the deleted `first_nested_journal_element` covered is genuinely
subsumed. Confirmed CHK-005's inline `<review>` stays inert: the scanner
classifies tags per-line via `open_tag_regex`/`close_tag_regex`
(`xml_scanner/mod.rs:394`), so a mid-line mention is not recognized as
structural. Deletions leave no dangling references — `JOURNAL_ELEMENT_NAMES`
is still defined and used in mod.rs and lint/rules/xml.rs; the dropped
serialize.rs import was its only dead use. No off-by-one, no error
swallowed, no resource left unreleased.
</review>
<review date="2026-06-12T08:16:00Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. T-003 delivers its half of REQ-002/REQ-003 with every owned done-when clause mapped to verified behavior.

REQ-002 (round-trip before write): `append_under_lock` re-parses the complete would-be-new content with strict `parse_journal_xml` before `fs_err::write` (speccy-cli/src/journal.rs:537), mirroring the vet path's inline round-trip (journal.rs:662) per DEC-001/DEC-002 — separate call site, distinct parser, distinct error variant `ProducedJournalUnparseable`. The done-when "distinct from ExistingJournalUnparseable" is genuinely gated: the existing-unparseable message reads "failed to parse" (no "unparseable"), so the produced path's message ("...would make the journal...unparseable...") is the sole carrier of the substring CHK-004 asserts, and the test seeds a valid journal first so only the produced path can fire. CHK-004 (`line_isolated_close_tag_in_body_is_rejected_at_write_time`) and CHK-005 (`inline_element_mention_in_prose_is_accepted_and_reparses`) both pass; the inline-prose acceptance test exercises the exact case the deleted pre-scan would have over-rejected.

REQ-003 (journal half): `first_nested_journal_element`, its call site, `SerializeError::NestedJournalMarkup`, the dead `nested_journal_markup_rejected` test, the `prose_word_reviewer_is_not_nested_markup` test, and the `JOURNAL_ELEMENT_NAMES` import are all gone from serialize.rs (grep confirms zero residual references in speccy-core/src); the module doc no longer promises a nested-markup-free body guard. The CHK-006 audit over non-test source returns exactly three hits, none a recognizer: consistency.rs:552 is `#[cfg(test)]`; journal_common.rs:54 builds a diagnostic context string (emits, does not scan); vet_xml/serialize.rs:389 is a block renderer (carved out by REQ-003's own done-when). The implementer's disclosure of `consistency.rs::last_well_formed_offset` is honest — single-char `.find`, outside the audit pattern set and REQ-003's named-removal list, neither gate-read nor journal-write.

Non-blocking observation: `vet_xml/mod.rs:4` still reads "...lived only in skill prose and the tolerant `<gate>` scanner in [crate::next]". REQ-003 says the vet_xml module doc should no longer reference a separate tolerant scanner. git blame traces this line to the pre-SPEC base (670d746), untouched by T-002 or T-003; it is the `next.rs`/vet portion of REQ-003 owned by the completed sibling T-002, and T-003's body explicitly scopes only the `journal_xml/serialize.rs` module doc. Out of T-003's ownership — flagging for human awareness, not as a T-003 blocker.

Verified green: `cargo test -p speccy-cli --test journal_append` (9/9, incl. both new tests) and `cargo test -p speccy-core --lib parse::journal_xml::serialize` (8/8).
</review>
<review date="2026-06-12T08:16:05Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The diff is clean and idiomatic.

New variant `JournalError::ProducedJournalUnparseable` at `speccy-cli/src/journal.rs:208` structurally mirrors `ProducedVetUnparseable` in naming, doc comment, and `#[source]` field shape — consistent with surrounding conventions.

Removals are complete: `first_nested_journal_element`, `SerializeError::NestedJournalMarkup`, `nested_journal_markup_rejected`, `prose_word_reviewer_is_not_nested_markup`, and the `JOURNAL_ELEMENT_NAMES` import from serialize.rs are all gone; no dangling references remain. The two orphaned tests are correctly identified as orphans of the deleted pre-scan code and removed — this is the right call per "clean up orphans your changes created."

Module doc at `serialize.rs:16–24` accurately describes the new authority split (caller round-trips; this module pre-scans only for empty body). The `DEC-001` reference is correct.

Tag-scan audit: the three production hits (`consistency.rs:552` inside `#[cfg(test)]`, `journal_common.rs:54` a diagnostic formatter, `vet_xml/serialize.rs:389` a block renderer) are all legitimate; no recognizer remains.

No `#[allow(…)]` suppressions introduced; `?` propagation used throughout the new round-trip path; no `unwrap()` in production code; no dead imports.
</review>
