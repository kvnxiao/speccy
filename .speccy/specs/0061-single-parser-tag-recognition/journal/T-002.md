---
spec: SPEC-0061
task: T-002
generated_at: 2026-06-12T07:53:54Z
---

<implementer date="2026-06-12T07:53:54Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area-mapped `speccy-core/src/next.rs` (gate-read path), the canonical parser `speccy-core/src/parse/vet_xml/mod.rs` (`parse_vet_in_flight`, `VetDoc`, `VetBlock`), and the test crate `speccy-core/tests/next_priority.rs`. Reuse-as-is: `parse_vet_in_flight` (re-exported at `crate::parse::parse_vet_in_flight`) and `VetBlock::Gate` — the typed parser is the single canonical recognizer the reroute collapses onto. Reuse-as-is in tests: the T-001 core helper `render_vet_md` and its production renderers `validate_and_render_vet_block` / `render_fresh_vet_frontmatter` / `render_vet_section_heading`, plus `sha256_hex_of_file` and `write_spec`. No new top-level production symbol introduced (the reroute is in-body on `vet_gate_is_fresh_pass`). New test-only symbol: `render_spoof_vet_md` — built on the same production renderers as the existing `render_vet_md` (extend-by-rhyme, not a fresh string), so the spoof fixture stays grammar-valid by construction (DEC-004/DEC-005).
- Completed: REQ-001 — rewrote `vet_gate_is_fresh_pass` in `next.rs` to derive the terminal gate from `parse_vet_in_flight`'s typed `VetDoc` (`doc.invocations.last().and_then(|inv| inv.blocks.last())` matched as `VetBlock::Gate { verdict, tasks_hash, .. }`); parse failure / empty doc / non-Gate terminal block all yield false; the `eq_ignore_ascii_case` hash comparison is unchanged. REQ-005 — added regression test `vet_when_terminal_gate_failed_despite_inline_passing_gate` (CHK-001/CHK-009): valid VET.md, `failed` terminal gate whose body quotes an inline `<gate verdict="passed">` with a tasks_hash matching the fixture TASKS.md, all tasks completed, REPORT.md absent. REQ-003 (gate-read half) — deleted `last_gate_block`, `GateBlock`, the `next.rs` `attribute_value` helper, their `#[cfg(test)] mod tests` cases, and the corresponding `use super::...` lines; updated the `vet_xml/mod.rs` module doc that claimed `crate::next` keeps an independent tolerant `<gate>` scanner.
- Undone: REQ-002 and the journal-write half of REQ-003 (`first_nested_journal_element`, `SerializeError::NestedJournalMarkup`, the `serialize.rs` pre-scan) are T-003's scope, untouched here. CHK-006's full production-wide audit and REPORT.md recording are deferred to the ship boundary; this task removed only the gate-read scanner.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok; the cpufeatures duplicate is a pre-existing non-error warning).
- Evidence: no separate evidence file authored; the red/green pair is reproducible from the test itself. Roll call for CHKs under T-002's covered REQs:
  - CHK-001 (inline-gate spoof resolves Vet): demonstrated -> `vet_when_terminal_gate_failed_despite_inline_passing_gate` in `speccy-core/tests/next_priority.rs` asserts `NextAction::Vet` post-fix.
  - CHK-002 (fresh passing terminal gate, REPORT.md absent -> Ship): hygiene -> `ship_when_vet_passes_fresh_and_no_report` in `next_priority.rs` passes under `cargo test`.
  - CHK-003 (unparseable VET.md missing frontmatter -> Vet): hygiene -> covered by the parser's reject path; `vet_when_all_done_no_vet_md` and the failed/stale tests in `next_priority.rs` exercise the not-fresh fallbacks. (A frontmatter-missing fixture is not added here; parse failure -> false is the same code path the existing tests cover and the parser's own suite asserts.)
  - CHK-009 (pre-fix Ship, post-fix Vet): demonstrated -> pre-reroute run of the new test panicked "expected Vet, got Ship" (recorded observation, proving the live bug); post-reroute run passes asserting Vet.
  - CHK-006 (no hand-rolled recognizer remains): judgment-only for the whole-tree audit (T-003 + ship), but the gate-read half is demonstrated -> grep of `next.rs` for `last_gate_block|GateBlock|attribute_value|cursor.find|find("<gate")` returns no matches.
- Discovered issues: CHK-003's "missing frontmatter" scenario has no dedicated fixture in this task; it is covered transitively by the parser-reject -> false path. If reviewer-tests wants an explicit unparseable-VET.md fixture, that is a cheap add — flag it and I will add one rather than leave it judgment-only.
- Procedural compliance: no shipped skill/module friction encountered; no edits under `resources/`. Followed the first-attempt branch (clean tree, no prior journal). Wrote the regression test first, recorded the pre-fix Ship observation, then applied the reroute.
</implementer>
<review date="2026-06-12T07:56:32Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. The gate-spoof vulnerability is closed at the only `Ship`-deciding
read path: `vet_gate_is_fresh_pass` in `speccy-core/src/next.rs:177` now
routes entirely through `parse_vet_in_flight`'s typed `VetDoc` and reads
only the terminal `VetBlock::Gate`, so a `<gate verdict="passed">` quoted
inside a block body is captured as body text and can no longer drive a
ship (REQ-001/REQ-005). The spoofable byte-scanner (`last_gate_block`,
`attribute_value`, `GateBlock`) is deleted — no residual
`find("<gate")` recognizer remains in `next.rs` (verified by grep,
REQ-003 gate-read half). Behavior is fail-safe by construction: parse
failure, empty document, non-Gate terminal block, failed verdict, or
stale hash all resolve to `Vet`, never `Ship` (re-vet over ship-on-
ambiguity is the correct default). The regression test
`vet_when_terminal_gate_failed_despite_inline_passing_gate`
(`next_priority.rs`) pins the vector with a *matching* `tasks_hash` on
the inline gate, so only the structural terminal-gate distinction
prevents the false `Ship`; full suite green. No secret/log/PII surface,
no new dependency, no auth or injection vector touched — the change only
narrows recognition onto the canonical parser. Note (non-blocking,
out of security scope): the module-level doc at `next.rs:25-27` still
describes the old "final non-whitespace `<gate>` block" byte-scan
semantics; the function-level doc was correctly updated.
</review>
<review date="2026-06-12T07:56:43Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Reroute of `vet_gate_is_fresh_pass` in speccy-core/src/next.rs:185-204 is correct on every branch: `parse_vet_in_flight` Err -> false (re-vet on malformed, per doc contract); `doc.invocations.last().and_then(|inv| inv.blocks.last())` None on empty doc or block-less last section -> false; non-`Gate` terminal block -> false; `verdict != "passed"` -> false; the `eq_ignore_ascii_case` hash compare against the on-disk TASKS.md SHA-256 is unchanged. The spoof defense holds: a line-isolated inline `<gate>` in a block body is recognized by xml_scanner as a tag (leading whitespace is trimmed at xml_scanner/mod.rs:387), so `assemble_flat` (journal_common.rs:210) rejects it as a nested block and the document fails to parse -> false -> Vet. (Note: the dies-by-construction path is parse-failure, not the journal's "captured as inert body text" framing -- both resolve Vet, so behavior is correct either way.) Deletions are complete: no `last_gate_block`, `GateBlock`, `attribute_value`, or `cursor.find("<gate")` remains in next.rs. All 15 next_priority tests pass, including the red/green spoof regression `vet_when_terminal_gate_failed_despite_inline_passing_gate` and the chk007 round-trip. No logic, boundary, Option/Result, or resource-handling defect found in scope.
</review>
<review date="2026-06-12T07:56:43Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Style passes cleanly.

Naming: `render_vet_md` and `render_spoof_vet_md` follow the `render_*` convention used by the production helpers they delegate to. The new `write_vet_md` signature (adding `spec_id`) aligns with the helper-split pattern.

Imports: the five new top-level `use` declarations in `next_priority.rs` are alphabetically ordered within their group and all consumed. The two inner `use` declarations inside `chk007_core_helper_output_round_trips_through_parser` are appropriately scoped to their single call site rather than hoisted to module scope, consistent with the pattern in sibling tests.

Suppressions: no new `#[allow]` / `#[expect]` added by the diff. The pre-existing `#![allow(clippy::expect_used, reason = ...)]` at the file top is unchanged.

Dead code: none introduced. `spec_id` in `render_spoof_vet_md` is consumed by `render_fresh_vet_frontmatter(spec_id, date)`.

Docs match code: `vet_xml/mod.rs` module doc updated from "independent tolerant `<gate>` scanner in `crate::next`" to the correct statement that freshness resolves through `parse_in_flight`'s typed `VetDoc` (SPEC-0061 REQ-001). The `vet_gate_is_fresh_pass` doc comment accurately describes the new typed-parser path including the parse-failure-as-not-fresh rationale.

Deletions verified: `last_gate_block`, `GateBlock`, `attribute_value`, and `cursor.find("<gate")` are absent from `next.rs`. The `#[cfg(test)] mod tests` block that exercised those helpers is gone; the remaining `mod tests` block covers only `default_personas_is_the_first_five_of_all`.

Reuse survey present and honest: `parse_vet_in_flight`, `VetBlock::Gate`, and the production renderers named in the survey are all called by the diff.
</review>
<review date="2026-06-12T07:56:53Z" model="claude-opus-4-8[1m]" persona="tests" verdict="blocking" round="1">
CHK-003's negative path (an all-completed spec whose VET.md is *present but
unparseable* — missing required frontmatter — resolves to `Vet`) has no
executable test, yet it is an explicit done-when bullet and `<behavior>` line of
REQ-001. The roll call labels CHK-003 `hygiene` citing
`vet_when_all_done_no_vet_md`, `vet_when_vet_md_ends_with_failed_verdict`, and
`vet_when_passed_but_tasks_hash_is_stale` in
`speccy-core/tests/next_priority.rs`. But those exercise different branches of
`vet_gate_is_fresh_pass` than the one CHK-003 targets: `vet_when_all_done_no_vet_md`
hits the file-absent early return (`fs_err::read` fails, `next.rs:181`), and the
failed/stale tests hit *successful* parses with a non-passing or stale terminal
gate. None drives `parse_vet_in_flight` to *fail to parse* a present file — the
distinct `let Ok(doc) = parse_vet_in_flight(...) else { return false; }` branch
at `next.rs:185`. A `hygiene` label must cite a test that covers *this* CHK; the
cited tests do not, so the parse-failure-yields-Vet branch is ungated. A wrong
implementation of that branch would not be caught. The implementer flagged this
himself ("a frontmatter-missing fixture is not added here... if reviewer-tests
wants an explicit unparseable-VET.md fixture, that is a cheap add"). Add a
test that writes a VET.md with missing/malformed frontmatter (the renderer-backed
`render_vet_md` cannot produce one by design per DEC-005, so hand-roll the
malformed fixture as the lint tests do) and asserts `compute_for_spec` resolves
`Vet`.

The rest of T-002 is strong and not blocked: the REQ-001 reroute derives the
terminal gate from the typed `VetDoc` (`next.rs:185-195`); the CHK-001/CHK-009
spoof test (`vet_when_terminal_gate_failed_despite_inline_passing_gate`) is built
via the production renderer `validate_and_render_vet_block` so the inline
`<gate verdict="passed">` is genuinely embedded in a real gate block's body, and
its falsification design is sound — the outer failed gate carries a non-matching
`tasks_hash="deadbeef"` while the inline passing gate carries the matching
TASKS.md hash, so only consulting the terminal (failed) gate yields `Vet`; the
old byte-scanner read the inline gate and yielded `Ship` (red recorded, green
confirmed by `cargo test -p speccy-core --test next_priority`, 15 passed). CHK-002
(`ship_when_vet_passes_fresh_and_no_report`) and the REQ-003 gate-read-half
deletions (`last_gate_block`/`GateBlock`/`attribute_value` and their `mod tests`
cases) are clean.
</review>
<review date="2026-06-12T07:56:55Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
Pass. The diff delivers the observable behavior T-002's covered requirements promise.

REQ-001: `vet_gate_is_fresh_pass` (`speccy-core/src/next.rs:165`) now derives the terminal gate from `parse_vet_in_flight`'s typed `VetDoc` (`doc.invocations.last().and_then(|inv| inv.blocks.last())` matched as `VetBlock::Gate`); parse failure / empty doc / non-Gate terminal all return false, and the `eq_ignore_ascii_case` hash comparison is unchanged. The `cursor.find("<gate")` scan is gone. CHK-002 (passing terminal gate + matching hash + REPORT.md absent -> Ship) is covered by `ship_when_vet_passes_fresh_and_no_report`.

REQ-005 / CHK-009: `vet_when_terminal_gate_failed_despite_inline_passing_gate` (`speccy-core/tests/next_priority.rs:283`) builds a valid VET.md from the production renderers whose terminal gate is `failed` while its body quotes an inline `<gate verdict="passed">` carrying a tasks_hash matching the on-disk TASKS.md, and asserts Vet. This directly serves the user story "a vet narrative that quotes a passing gate inline can never spoof a premature ship." The pre-fix Ship observation is recorded in the round-1 implementer handoff per CHK-009.

REQ-003 (gate-read half): `last_gate_block`, `GateBlock`, the `attribute_value` helper, their `#[cfg(test)]` cases, and the stale `vet_xml/mod.rs` module-doc claim are all removed.

No non-goals breached, no open question silently resolved, no scope sneak into T-003's journal-write half (`serialize.rs` untouched).

One minor non-blocking gap, disclosed by the implementer: CHK-003 (missing-frontmatter VET.md -> vet), named in REQ-001's done-when and the task's `<task-scenarios>`, has no dedicated test fixture in `next_priority.rs`. The behavior itself is delivered — a frontmatter-less VET.md fails `parse_vet_in_flight`, returning false via the same not-fresh path the existing failed/stale tests exercise — so the SPEC's behavioral commitment is met; only a dedicated regression fixture is absent. Test-coverage adequacy is the tests persona's call; from the business lens the promised observable behavior holds.
</review>
<blockers date="2026-06-12T07:57:36Z" round="1">
Add an executable test for CHK-003's negative path: a present-but-unparseable VET.md (missing/malformed frontmatter) on an all-completed spec must resolve to `Vet`.
- reviewer-tests (blocking): The parse-failure branch at `next.rs:185` (`let Ok(doc) = parse_vet_in_flight(...) else { return false; }`) is ungated. The cited roll-call tests cover only the file-absent early return (`vet_when_all_done_no_vet_md`) and successful-parse-with-non-fresh-gate branches (`vet_when_vet_md_ends_with_failed_verdict`, `vet_when_passed_but_tasks_hash_is_stale`); none drives `parse_vet_in_flight` to fail on a present file. Hand-roll the malformed fixture (the renderer-backed `render_vet_md` cannot produce one by design per DEC-005, so build it as the lint tests do) and assert `compute_for_spec` resolves `Vet`. The CHK-003 roll-call label must then cite this new test rather than the three branch-mismatched ones.
</blockers>
<implementer date="2026-06-12T08:01:55Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Retry round addressing a non-reuse (test-coverage) blocker, so the full area-map is not re-run. No new top-level production symbol introduced. The one new top-level test symbol is `vet_when_vet_md_present_but_unparseable` (a `#[test]` fn); it reuses the existing in-crate helpers `write_spec`, `utf8`, `sha256_hex_of_file`, `scan`, and `compute_for_spec` — searched `next_priority.rs` for an existing parse-failure fixture and found none (the file-absent and successful-parse-non-fresh branches were covered, the parse-failure branch was not), so the test is written fresh against those existing helpers. The malformed VET.md fixture is hand-rolled by design (DEC-005): the renderer-backed `render_vet_md` produces only valid gate-terminated documents and cannot emit a frontmatter-less file.
- Completed: Round-2 amend addressing the round-1 reviewer-tests blocker. Added `vet_when_vet_md_present_but_unparseable` to `speccy-core/tests/next_priority.rs`: an all-completed spec whose VET.md is present but missing required frontmatter (so `parse_vet_in_flight` rejects it, driving the `let Ok(doc) = ... else { return false; }` branch at `next.rs:185`), REPORT.md absent. The fixture's gate carries a `tasks_hash` matching the on-disk TASKS.md, so the parse failure is the only thing preventing a false Ship — the test genuinely gates that branch, not the hash-mismatch path. CHK-003's roll-call label is corrected below from `hygiene` (citing branch-mismatched tests) to `demonstrated` (citing this new test).
- Undone: Nothing newly undone. REQ-002 and the journal-write half of REQ-003 remain T-003's scope, untouched. CHK-006's full production-wide audit + REPORT.md recording remain deferred to the ship boundary.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok; the deny.toml unmatched-license-allowance notices are pre-existing warnings, not errors).
- Evidence: red/green reproducible from the test itself; no separate evidence file. Roll call for CHKs under T-002's covered REQs (REQ-001/REQ-005/REQ-003 gate-read half):
  - CHK-001 (inline-gate spoof resolves Vet): demonstrated -> `vet_when_terminal_gate_failed_despite_inline_passing_gate` in `speccy-core/tests/next_priority.rs` asserts `NextAction::Vet` post-fix.
  - CHK-002 (fresh passing terminal gate, REPORT.md absent -> Ship): demonstrated -> `ship_when_vet_passes_fresh_and_no_report` asserts `NextAction::Ship`.
  - CHK-003 (present-but-unparseable VET.md missing frontmatter -> Vet): demonstrated -> NEW `vet_when_vet_md_present_but_unparseable` drives the parse-failure branch at `next.rs:185` and asserts `NextAction::Vet`. Red-check confirmed: temporarily flipping that branch to `return true` makes the test fail with "got Ship" (the gate's tasks_hash matches the on-disk TASKS.md, so only the parse failure prevents a false Ship); reverted, `next.rs` byte-identical to the round-1 WIP. This replaces the round-1 `hygiene` label, which cited three branch-mismatched tests (the file-absent early return and successful-parse-non-fresh branches) per the reviewer-tests blocker.
  - CHK-009 (pre-fix Ship, post-fix Vet): demonstrated -> round-1 pre-reroute run of `vet_when_terminal_gate_failed_despite_inline_passing_gate` panicked "expected Vet, got Ship" (recorded in round-1 handoff); post-reroute run passes asserting Vet.
  - CHK-006 (no hand-rolled recognizer remains): judgment-only for the whole-tree audit (T-003 + ship boundary); the gate-read half is demonstrated -> grep of `next.rs` for `last_gate_block|GateBlock|attribute_value|cursor.find|find("<gate")` returns no matches.
- Discovered issues: none new. The round-1 self-flagged CHK-003 gap is now closed by `vet_when_vet_md_present_but_unparseable`.
- Procedural compliance: retry branch followed — read the round-1 implementer block and round-1 blockers block from the journal, amended the existing uncommitted WIP in place (no `git restore`/`clean`/`checkout`, no rewrite-from-scratch). Only the test file changed this round; `next.rs` and `vet_xml/mod.rs` are byte-identical to round 1. No shipped skill/module friction; no edits under `resources/`.
</implementer>
<review date="2026-06-12T08:03:53Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="2">
Pass. The round-2 amend closes the sole round-1 blocker (tests persona:
CHK-003's parse-failure branch was ungated) and the SPEC→diff mapping for
T-002's covered requirements holds.

REQ-001 done-when, all four bullets covered: inline-gate spoof → Vet
(`vet_when_terminal_gate_failed_despite_inline_passing_gate`, next_priority.rs:345);
fresh passing terminal gate + REPORT.md absent → Ship
(`ship_when_vet_passes_fresh_and_no_report`); the previously-missing
present-but-unparseable → Vet branch now has a dedicated executable test
(`vet_when_vet_md_present_but_unparseable`, next_priority.rs:410); and the
`cursor.find("<gate")` scan is gone — `vet_gate_is_fresh_pass` (next.rs:185-204)
reads the terminal `VetBlock::Gate` from `parse_vet_in_flight`'s typed VetDoc.

The new CHK-003 test is non-vacuous: the frontmatter-less fixture genuinely
fails `parse_vet_in_flight` (frontmatter is required — vet_xml/mod.rs:336
MissingField, mirrored by `missing_frontmatter_is_rejected` at mod.rs:1096),
and its gate carries a `tasks_hash` matching the on-disk TASKS.md, so only the
parse-failure branch prevents a false Ship — the `Vet` assertion truly gates
next.rs:185. The CHK-003 roll-call label was correctly corrected from the
round-1 branch-mismatched `hygiene` citation to `demonstrated` citing this test,
exactly as the round-1 blocker demanded.

REQ-005/CHK-009: pre-fix Ship recorded in the round-1 handoff, post-fix Vet
asserted; the spoof test pins a matching `tasks_hash` on the inline gate so only
the structural terminal-gate distinction averts the false ship — sound
falsification. REQ-003 gate-read half: `last_gate_block`, `GateBlock`,
`attribute_value`, and the stale `vet_xml/mod.rs` module-doc claim are removed;
CHK-006's whole-tree audit + REPORT.md recording are legitimately deferred to
T-003 and the ship boundary (serialize.rs is T-003 scope). No non-goal breached
(grammar, priority ordering, hash semantics all untouched); no open question
silently resolved.
</review>
<review date="2026-06-12T08:03:59Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Gate-spoof attack surface is closed and the ship-authorization decision is fail-closed. The reroute matches only the terminal `VetBlock::Gate` of the last invocation via `parse_vet_in_flight` (`speccy-core/src/next.rs:185-218`); an inline `<gate verdict="passed">` quoted in a block body is captured as body text and can never surface as terminal, so the spoof dies by construction. The tolerant byte-scanner that was the spoof vector (`last_gate_block`/`GateBlock`/`attribute_value`) is deleted, not patched, removing the divergent recognizer entirely (grep of `next.rs` for `find("<`/`last_gate_block`/`GateBlock`/`attribute_value`/`cursor` returns no matches). Parse failure, empty doc, and a non-Gate terminal block all return false → re-vet, the safe default. The round-2-added `vet_when_vet_md_present_but_unparseable` isolates this fail-closed branch adversarially: the gate carries a `tasks_hash` matching the on-disk TASKS.md, so only the parse rejection prevents a false Ship. The spoof test `vet_when_terminal_gate_failed_despite_inline_passing_gate` likewise gives the inline gate a *matching* fresh hash, proving the terminal-vs-inline distinction is the sole barrier. Hash freshness comparison (`eq_ignore_ascii_case` over the live TASKS.md SHA-256) is unchanged — no weakening of the integrity check, no new TOCTOU, no new dependency, no secret/log/data-exposure surface.
</review>
<review date="2026-06-12T08:04:28Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. Reroute of `vet_gate_is_fresh_pass` in `speccy-core/src/next.rs:165` is correct: `parse_vet_in_flight` Err -> false; the `let Some(VetBlock::Gate { verdict, tasks_hash, .. }) = doc.invocations.last().and_then(|inv| inv.blocks.last()) else { return false; }` chain correctly yields not-fresh on empty doc, empty terminal invocation, and non-Gate terminal block; `verdict != "passed"` and TASKS.md read-error paths preserve the prior false-returns; the `tasks_hash.eq_ignore_ascii_case(&actual_hash)` comparison is semantically unchanged. No inverted condition, off-by-one, swallowed-error, or resource-leak defect introduced. The `last_gate_block`/`GateBlock`/`attribute_value` deletions leave no dangling reference (workspace compiles; `next_priority.rs` 16/16 pass). Round-2 amend added only the `vet_when_vet_md_present_but_unparseable` test (drives the parse-failure branch, asserts Vet) and the chk007 round-trip; production logic is byte-identical to the round-1 WIP this lane already passed.
</review>
<review date="2026-06-12T08:04:46Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
No style violations found in the round-2 changes or the round-1 WIP.

Round-2 additions (`next_priority.rs`):
- `vet_when_vet_md_present_but_unparseable`: correctly follows the test file's existing pattern (tempdir setup → `write_spec` → manual fixture write → `scan`/`compute_for_spec` → assert). Hand-rolled fixture is explicitly justified via DEC-005 in the block comment, matching the project convention for carve-outs.
- `render_spoof_vet_md`: new private helper, well-documented, reuses the same production renderers as the sibling `render_vet_md`. Not a reinvention — it builds on the same `validate_and_render_vet_block`/`render_fresh_vet_frontmatter`/`render_vet_section_heading` calls already used in `render_vet_md`.
- Five new `use` imports are sorted alphabetically within their existing crate group, consistent with lines 20–24 of the pre-existing import block.
- The pre-existing `#![allow(clippy::expect_used, reason = ...)]` is not introduced by this diff; no new bare `#[allow]` added.

Round-1 WIP (`next.rs`, `vet_xml/mod.rs`):
- `vet_gate_is_fresh_pass` updated doc comment matches the new implementation accurately (no aspirational/stale prose).
- Deleted `last_gate_block`, `GateBlock`, `attribute_value` and their `#[cfg(test)] mod tests` cases cleanly; corresponding `use super::…` lines removed.
- Module doc in `vet_xml/mod.rs` updated to describe what the code actually does (sole recognizer via typed parser), replacing the now-incorrect claim about `crate::next`'s independent scanner.
- No `unwrap()` in production code; `tasks_hash.eq_ignore_ascii_case(&actual_hash)` follows existing style.
- No dead imports, no reinvented helpers, no lint suppressions added.
</review>
<review date="2026-06-12T08:05:21Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="2">
Round-1 blocker resolved: `vet_when_vet_md_present_but_unparseable` (`speccy-core/tests/next_priority.rs:410`) now drives the parse-failure branch at `next.rs:185`. The fixture is a frontmatter-less VET.md whose `<gate verdict="passed">` carries a `tasks_hash` matching the on-disk TASKS.md, so the only thing preventing a false Ship is `parse_vet_in_flight` rejecting the document — verified mechanically: `parse_with_mode` calls `split_required` first (`vet_xml/mod.rs:319`), which returns `MissingField{frontmatter}` on a source with no `---` delimiter (`frontmatter.rs:100`). Falsification holds: flipping the `let Ok(doc) = ... else { return false }` arm to `return true` short-circuits `vet_gate_is_fresh_pass` to fresh-pass → Ship, which the test catches (implementer's documented red-check). CHK-003 roll-call label correctly moved from round-1 `hygiene` (branch-mismatched cites) to `demonstrated`.
CHK-001/CHK-009 spoof test (`next_priority.rs:345`) genuinely gates the live bug: the inline `<gate verdict="passed">` (matching hash) sits in the `failed` terminal block's body, so the typed parser keeps the terminal gate `failed` → Vet; the old byte-scanner would `find("<gate")` the inline one → Ship (round-1 recorded red run "got Ship"). CHK-002 (`ship_when_vet_passes_fresh_and_no_report`) asserts Ship through the real `compute_for_spec`. All three exercise the actual code path, no mocks. Roll call complete for REQ-001/REQ-005/REQ-003 (CHK-006 judgment-only for the whole-tree audit is plausible — deferred to T-003/ship; gate-read-half deletion independently demonstrated by grep). Evidence is an inline prose red/green trail rather than captured runner output, but every named test appears in the diff and the red/green mechanism is independently verifiable from source — no fabrication pattern matches. Test naming/structure match the crate's existing `vet_when_*` conventions.
</review>
