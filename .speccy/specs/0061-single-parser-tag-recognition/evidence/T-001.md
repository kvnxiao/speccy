# Evidence for SPEC-0061 T-001

Paper trail for routing test VET.md through per-crate renderer-backed
helpers (REQ-004 / CHK-007). The task is a test-infrastructure
migration: it adds one renderer-backed helper per test crate and
re-routes the hand-rolled valid `## Invocation` / `<gate>` fixtures
onto them. The load-bearing behavioural claim — helper output parses
under the canonical parser with a terminal gate carrying the
requested `(verdict, tasks_hash)` — is proven by two new round-trip
tests, one per crate.

## Session 2026-06-12-T001-attempt-1

### Scenario 1 — Helper output round-trips through the canonical parser (CHK-007)

The new tests
`chk007_core_helper_output_round_trips_through_parser`
(`speccy-core/tests/next_priority.rs`) and
`chk007_cli_helper_output_round_trips_through_parser`
(`speccy-cli/tests/next_json.rs`) render a VET.md via each crate's
helper for `(verdict="passed", tasks_hash="feedface…")`, parse it
with `parse_vet_in_flight`, and assert the terminal block is a
`VetBlock::Gate` carrying that verdict and hash.

<red>
Pre-edit: neither test exists; the core crate's `write_vet_md`
emitted a frontmatter-less `## Invocation 1` string that
`parse_vet_in_flight` rejects (missing required frontmatter), so a
round-trip assertion against the old helper output would fail at the
parse step. Confirmed by inspection of the pre-edit
`speccy-core/tests/next_priority.rs:227` body — `format!("## Invocation 1\n\n<gate …>")`
with no `---` frontmatter.
</red>

<green>
Post-edit: both round-trip tests pass.

```
$ cargo test -p speccy-core --test next_priority chk007
test chk007_core_helper_output_round_trips_through_parser ... ok
test result: ok. 1 passed; 0 failed

$ cargo test -p speccy-cli --test next_json chk007
test chk007_cli_helper_output_round_trips_through_parser ... ok
test result: ok. 1 passed; 0 failed
```

The helpers compose the exported production renderers
(`render_fresh_vet_frontmatter`, `render_vet_section_heading`) plus a
gate rendered via `validate_and_render_vet_block`, so the produced
document is grammar-valid by construction and the parser accepts it.
</green>

### Migrated fixtures stay green (task scenario 3)

The migration re-routes the valid VET.md fixtures in
`next_priority.rs`, `next_text.rs`, `next_json.rs`, `common/mod.rs`,
and the valid `journal_show.rs` fixture onto the helpers. Renderer
output is a strict superset of the old byte-scan input (it adds valid
frontmatter), so the still-byte-scanning gate-read path keeps
accepting it.

```
$ cargo test --workspace
test result: ok.  (all crates green; 0 failed)
```

## Hygiene-gate evidence

```
$ cargo test --workspace            ; echo $?   # 0
$ cargo clippy --workspace --all-targets --all-features -- -D warnings ; echo $?   # 0
$ cargo +nightly fmt --all --check  ; echo $?   # 0
$ cargo deny check                  ; echo $?   # 0
```

All four standard hygiene gates exited 0 after the T-001 diff landed.
