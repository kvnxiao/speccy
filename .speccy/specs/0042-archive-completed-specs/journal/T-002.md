---
spec: SPEC-0042
task: T-002
generated_at: 2026-05-23T19:30:00Z
---

<implementer date="2026-05-23T19:30:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Added the `Archive` clap subcommand to
  `speccy-cli/src/main.rs` with positional `SPEC-NNNN`, `--reason`
  (with a newline-rejecting `value_parser`), `--force`, and `--json`.
  Created `speccy-cli/src/archive.rs` housing `ArchiveArgs`,
  `ArchiveOutcome`, `ArchiveError`, and `run()`. The run path:
  resolves the project root via `find_root`, validates the SPEC-NNNN
  shape, locates the spec directory under `.speccy/specs/` (flat and
  mission-folder layouts), parses SPEC.md to read frontmatter,
  refuses `in-progress` without `--force` via `StatusGate`, mutates
  the source SPEC.md to append `archived_at: <UTC>` and
  (optionally) `archived_reason: "..."` before the closing `---`
  fence, creates `.speccy/archive/` if absent, then shells out to
  `git mv` with `project_root` as cwd. On `git mv` failure the
  frontmatter mutation is rolled back so the source file is restored
  to its pre-archive bytes. Successful runs emit a human-readable
  confirmation line on stdout; `--json` emits a placeholder receipt
  (full shape deferred to T-003 per its task body). Added
  `parse_reason()` as the clap value parser rejecting `\n` / `\r`.
  Added end-to-end integration tests in
  `speccy-cli/tests/archive_text.rs` (10 cases covering CHK-001
  through CHK-009 plus the `git status --porcelain` rename
  invariant). Added unit tests in `archive.rs` for
  `insert_archive_fields` (LF and CRLF), `yaml_double_quoted`
  escaping, and `parse_reason`.
- Undone: T-003's full `--json` receipt shape (the
  `ArchiveReceipt`/`ArchivedSpec`/`ArchiveWarning` structs and the
  `archive_json.rs` integration suite). T-004's orphan-supersession
  warning detection -- the `warnings: []` field in the placeholder
  JSON output is always empty for now. T-005's `vacancy` archive
  scan, T-006's `status --include-archive`, T-007's regression
  suite, T-008's doc-only updates.
- Hygiene checks: `cargo test --workspace` (all suites green
  including the 10 new `archive_text` cases),
  `cargo clippy --workspace --all-targets --all-features -- -D
  warnings` (clean after addressing pedantic warnings around
  `map(...).unwrap_or_else(...)`, `let _` on `#[must_use]` types,
  `assert!(...is_ok())`, `manual_let_else`, and
  `format_push_string`), `cargo +nightly fmt --all --check`
  (auto-applied during the loop), `cargo deny check` (advisories
  ok, bans ok, licenses ok, sources ok; only the pre-existing
  unmatched-allowance warnings for `ISC` and `MPL-2.0`).
- Evidence: no separate paper-trail file -- the integration suite
  in `speccy-cli/tests/archive_text.rs` is the demonstrated
  evidence and runs under the hygiene gate. Roll call for CHKs
  under REQ-001 / REQ-002 / REQ-003:
  - CHK-001 (archive implemented spec moves dir, archived_at
    written, git rename recorded): demonstrated ->
    `archive_implemented_spec_moves_dir_and_writes_archived_at`
    and `archive_git_rename_is_recorded` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-002 (SPEC-9999 not found exits non-zero with stderr naming
    the ID): demonstrated ->
    `archive_missing_spec_id_exits_nonzero`.
  - CHK-003 (no positional -> clap exit 2): demonstrated ->
    `archive_without_positional_clap_exits_two`.
  - CHK-004 (`in-progress` without `--force` refuses with stderr
    naming current status and all three archivable statuses):
    demonstrated ->
    `archive_in_progress_spec_without_force_refuses`.
  - CHK-005 (`in-progress` with `--force` moves and keeps status):
    demonstrated ->
    `archive_in_progress_spec_with_force_moves_and_keeps_status`.
  - CHK-006 (`superseded` without `--force` succeeds):
    demonstrated ->
    `archive_superseded_spec_without_force_succeeds`.
  - CHK-007 (`--reason "ship cleanup"` writes both archive fields,
    in order): demonstrated ->
    `archive_with_reason_writes_reason_field` (asserts ordering of
    `archived_at` before `archived_reason`).
  - CHK-008 (no `--reason` -> zero `archived_reason:` matches,
    exactly one `archived_at:` match): demonstrated ->
    `archive_without_reason_omits_reason_field`.
  - CHK-009 (literal newline in `--reason` -> clap exit 2 naming
    `--reason`): demonstrated ->
    `archive_reason_with_newline_rejected_at_parse_time`.
- Discovered issues: clippy's pedantic ruleset flagged several
  patterns I had to rewrite (no behavioural changes). The `Workspace`
  test helper does not auto-create a tracked file, so the
  `SPEC-9999` not-found test needed a `.speccy/.gitkeep` so the
  initial `git commit` had something to record; documented in the
  test body. No SPEC contract issues surfaced.
- Procedural compliance: no shipped-skill friction encountered
  during this task; no `skills/` edits required.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-23T20:15:00Z" round="1">
REQ-001 / REQ-002 / REQ-003 done-when items satisfied by the diff.
The `Archive` subcommand lands with the positional `SPEC-NNNN`, the
three flags (`--reason`, `--force`, `--json`), the status-gate refusal
for `in-progress` (with stderr naming `in-progress` plus all three
archivable statuses via `ARCHIVABLE_STATUSES` in
`speccy-cli/src/archive.rs:27`), and the frontmatter mutation that
appends `archived_at` unconditionally and `archived_reason` only when
`--reason` was passed (insertion is keyed on the closing `---` fence
in `insert_archive_fields` at `archive.rs:348`). `git mv` runs with
the project root as cwd; on failure the source SPEC.md is rolled
back to original bytes (`archive.rs:199`, `:209`). The `--json` path
is an intentional placeholder per the task body's "the `--json` path
is a placeholder this task; T-003 fills it in"; the consequence is
that T-002 itself does not satisfy REQ-009 — but T-002 doesn't claim
to, since it covers REQ-001 / REQ-002 / REQ-003 only. No non-goal
violations: no bulk mode, no unarchive, no status mutation under
`--force` (verified by `archive_in_progress_spec_with_force_moves_and_keeps_status`).
The Changelog row in SPEC.md is the initial 2026-05-23 draft; nothing
shifted mid-loop.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-7[1m]/high" date="2026-05-23T20:18:00Z" round="1">
Evidence contract violation. The round-1 `<implementer>` entry's
`Evidence:` field reads "no separate paper-trail file -- the
integration suite in `speccy-cli/tests/archive_text.rs` is the
demonstrated evidence and runs under the hygiene gate." Per the
reviewer-tests evidence-loading contract (steps 3 and 5 in
`.claude/agents/reviewer-tests.md`), the absence of a per-task
evidence file at `.speccy/specs/0042-archive-completed-specs/evidence/T-002.md`
is treated as a blocking verdict, and SPEC-0031 explicitly mandates
the red-then-green paper trail for every task closure that flips to
`state="in-review"`. Confirmed via direct probe: no `evidence/`
directory exists under `.speccy/specs/0042-archive-completed-specs/`.

The CHK-by-CHK roll call uses the `demonstrated` label for CHK-001
through CHK-009 and cites integration-test names in
`speccy-cli/tests/archive_text.rs`. The label is wrong for the shape
delivered: `demonstrated` requires backing by a `### Scenario N`
block in `evidence/T-002.md` with pre-edit (red) and post-edit
(green) captured output. The cited tests are workspace-test-suite
cases that run under `cargo test --workspace`; the shape they fit is
`hygiene` (per the implementer reference at
`.claude/skills/speccy-work/references/journal-implementer.md` Six-field
handoff template). Each cited test is specific (named by function),
so the underlying coverage is real — but the label is mis-shaped and
the paper-trail file SPEC-0031 mandates is absent.

Fix path: either (a) author
`.speccy/specs/0042-archive-completed-specs/evidence/T-002.md` with
red-then-green captures for CHK-001 / CHK-004 / CHK-005 / CHK-007 /
CHK-009 (the behaviour-shifting CHKs where a pre-edit failure mode
exists to capture) and keep the `demonstrated` labels for those,
relabelling the remaining CHKs to `hygiene` with their existing test
names; or (b) write the evidence file as one consolidated session
covering the behaviour and relabel all nine CHKs to `hygiene` with
their test-function citations, dropping the `demonstrated` claim.
Either path resolves the blocker. Path (a) is the SPEC-0031 intent;
path (b) is the smaller delta if no genuine pre-edit failure-mode
output was captured during the implementer turn.

Note that the integration suite in `archive_text.rs` itself is
adversarially solid: each test asserts the contract claim (exit
codes, stderr substrings, frontmatter line presence/absence, ordering
of `archived_at` before `archived_reason`, git porcelain rename
entry). No mock-only assertions; the tests drive the real
`speccy archive` binary via `assert_cmd::Command::cargo_bin`. The
blocker is the missing paper-trail file plus the mislabel, not the
test coverage itself.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-7[1m]/high" date="2026-05-23T20:20:00Z" round="1">
Shell-out to `git mv` in `archive.rs:186-194` passes paths as
separate `Command::arg(...)` invocations, not shell-interpolated;
no command-injection vector. Source path is derived from a
filesystem scan keyed on a regex-validated `SPEC-\d{4,}` ID, not
from raw user input. The `--reason` clap value parser rejects `\n`
/ `\r` at parse time (`archive.rs:439-444`), eliminating the
YAML-line-break injection that would have let a crafted reason
inject arbitrary frontmatter keys. The `yaml_double_quoted` helper
escapes `"` and `\\` correctly; non-newline control chars pass
through unchanged but are bounded by the clap parser's newline
rejection (CR / LF are the only YAML-significant whitespace for
line breaks; tabs and other control bytes are valid inside a
double-quoted scalar). No new dependencies introduced. No secret
handling, no auth boundary, no logging of sensitive material.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-7[1m]/medium" date="2026-05-23T20:22:00Z" round="1">
Diff stays within suggested files. `unwrap()` appears twice in
`OnceLock` regex initializers (`spec_id_regex`, `spec_dir_name_regex`
at `archive.rs:319-331`) both guarded by `#[expect(clippy::unwrap_used,
reason = "compile-time literal regex; covered by unit tests")]` per
the AGENTS.md convention. No `#[allow(...)]` suppressions added.
The rollback pattern `let _write = fs_err::write(...)` (`archive.rs:179`,
`:199`, `:209`) is a deliberate best-effort cleanup on the failure
path; binding to a named variable rather than `_` keeps the
must-use lint satisfied without `#[allow]`. The placeholder JSON
emission in `main.rs` (`println!("{{\"schema_version\":...`) is
ugly but explicitly sanctioned by the task body ("the `--json`
path is a placeholder this task; T-003 fills it in"); it will be
replaced by `serde_json::to_writer` on the receipt struct in T-003.
Dependencies (`jiff` for time, `fs-err` for I/O, `camino` for
paths) align with `.claude/rules/rust/rust-dependencies.md`.
Test file `archive_text.rs` carries `#![allow(clippy::expect_used,
...)]` with `reason = "..."` per the test-file convention. No
duplicate helpers — the file picks up `Workspace`, `spec_md_template`,
and `write_spec` from the existing `tests/common` module rather than
inventing parallels. Module-level docstring on `archive.rs` cites
SPEC-0042 REQ-001 / REQ-002 / REQ-003.
</review>

<blockers date="2026-05-23T20:25:00Z" round="2">
One blocker landed in round 1 (tests). Business, security, and
style passed. Fix before respawning the review fan-out.

1. **Missing red-then-green paper-trail file plus mislabelled roll
   call (tests).** The round-1 `<implementer>` block's `Evidence:`
   field disclaims the paper-trail file (`evidence/T-002.md`) that
   SPEC-0031 mandates for every task closure, and labels CHK-001
   through CHK-009 as `demonstrated`. The reviewer-tests evidence
   contract treats absence of the file as blocking, and the
   `demonstrated` label requires backing by a `### Scenario N`
   block in the evidence file (per
   `.claude/speccy-references/evidence.md` "Coverage rule" and the
   handoff-template definition in
   `.claude/skills/speccy-work/references/journal-implementer.md`).
   The cited integration tests in
   `speccy-cli/tests/archive_text.rs` are the right shape for
   `hygiene` labels (workspace-test-suite cases with specific
   function-name citations), not `demonstrated`. Fix: write
   `.speccy/specs/0042-archive-completed-specs/evidence/T-002.md`
   with at least one consolidated session covering the round-1
   behaviour, and either (a) keep `demonstrated` labels for the
   CHKs whose pre-edit failure mode you can capture in `### Scenario`
   blocks (CHK-001 / CHK-004 / CHK-005 / CHK-007 / CHK-009 are
   strongest candidates), relabelling the rest to `hygiene` with
   their existing test cites; or (b) relabel all nine CHKs to
   `hygiene` with the existing test-function citations and let the
   evidence file carry the hygiene-gate output as the catch-all
   proof. Path (a) is the SPEC-0031 intent; path (b) is acceptable
   if the round-1 implementer did not capture genuine pre-edit
   failure output and the retry round refuses to fabricate it.

The mechanical floor — clap subcommand wiring, status-gate
refusal, frontmatter mutation with rollback, `git mv` shell-out
with separate-arg passing, the `archive_text.rs` integration suite,
clippy/fmt/deny cleanliness — is solid and reusable for the
retry. Do not regress the test suite or the rollback discipline.
The retry round only needs to (i) produce the evidence file and
(ii) rewrite the `Evidence:` roll call in a new round-2
`<implementer>` block per the journal append-only contract (do not
edit the round-1 block).
</blockers>

<implementer date="2026-05-23T21:00:00Z" model="claude-opus-4-7[1m]/low" round="2">

- Completed: Addressed the round-1 tests blocker by (a) creating
  `.speccy/specs/0042-archive-completed-specs/evidence/T-002.md`
  with the hygiene-gate paper trail (the new
  `archive_text` integration suite's run output plus the four
  workspace-wide hygiene commands) and (b) rewriting the Evidence
  roll call below to label every CHK under REQ-001 / REQ-002 /
  REQ-003 as `hygiene`, per path (b) of the blocker. No source
  changes in this round; the round-1 implementation
  (`speccy-cli/src/archive.rs`, the `Archive` subcommand wired
  into `speccy-cli/src/main.rs`, and the ten integration cases
  in `speccy-cli/tests/archive_text.rs`) is unchanged. Re-ran
  the integration suite to confirm the ten cases are still green
  on the round-2 checkout.
- Undone: T-003's full `--json` receipt shape, T-004's
  orphan-supersession warning detection, T-005's `vacancy`
  archive scan, T-006's `status --include-archive`, T-007's
  regression suite, T-008's doc-only updates. (Unchanged from
  round 1 -- T-002's scope is REQ-001 / REQ-002 / REQ-003 only.)
- Hygiene checks: `cargo test --workspace --test archive_text`
  (10 passed, 0 failed); the full workspace hygiene block
  (`cargo test --workspace`, clippy, fmt --check, deny) was green
  in round 1 and round-2 introduces no source changes that could
  regress it. Evidence-file content under
  `.speccy/specs/.../evidence/T-002.md` records the captured
  output.
- Evidence: paper trail at
  `.speccy/specs/0042-archive-completed-specs/evidence/T-002.md`.
  Per path (b) of the round-1 blocker, no `### Scenario N` blocks
  are claimed -- the round-1 implementer did not capture genuine
  pre-edit failure-mode output (the `archive` subcommand did not
  exist on the parent commit; the only "pre-edit" signal would
  have been a generic `clap` unknown-subcommand error carrying no
  contract-specific information). Roll call for CHKs under
  REQ-001 / REQ-002 / REQ-003:
  - CHK-001 (archive implemented spec moves dir, writes
    archived_at, git rename recorded): hygiene ->
    `archive_implemented_spec_moves_dir_and_writes_archived_at`
    and `archive_git_rename_is_recorded` in
    `speccy-cli/tests/archive_text.rs` cover it under
    `cargo test --workspace`.
  - CHK-002 (SPEC-9999 not found exits non-zero, stderr names
    the ID): hygiene ->
    `archive_missing_spec_id_exits_nonzero` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-003 (no positional -> clap exit 2): hygiene ->
    `archive_without_positional_clap_exits_two` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-004 (`in-progress` without `--force` refuses with stderr
    naming current status and all three archivable statuses):
    hygiene ->
    `archive_in_progress_spec_without_force_refuses` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-005 (`in-progress` with `--force` moves and keeps
    status): hygiene ->
    `archive_in_progress_spec_with_force_moves_and_keeps_status`
    in `speccy-cli/tests/archive_text.rs`.
  - CHK-006 (`superseded` without `--force` succeeds): hygiene
    -> `archive_superseded_spec_without_force_succeeds` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-007 (`--reason "ship cleanup"` writes both archive
    fields, ordered): hygiene ->
    `archive_with_reason_writes_reason_field` in
    `speccy-cli/tests/archive_text.rs` (asserts ordering of
    `archived_at` before `archived_reason`).
  - CHK-008 (no `--reason` -> zero `archived_reason:` matches,
    exactly one `archived_at:` match): hygiene ->
    `archive_without_reason_omits_reason_field` in
    `speccy-cli/tests/archive_text.rs`.
  - CHK-009 (literal newline in `--reason` -> clap exit 2
    naming `--reason`): hygiene ->
    `archive_reason_with_newline_rejected_at_parse_time` in
    `speccy-cli/tests/archive_text.rs`.
- Discovered issues: none in round 2. The round-1 friction
  (clippy pedantic rewrites, `Workspace` helper not
  auto-creating a tracked file) is documented in the round-1
  block.
- Procedural compliance: this round exposed friction worth
  recording but not a shipped-skill bug per se -- the round-1
  implementer wrote the `Evidence:` field in a shape the
  reviewer-tests contract rejects (claimed `demonstrated`
  without `### Scenario N` backing, and disclaimed the
  paper-trail file entirely). The shipped
  `speccy-work/references/journal-implementer.md` and
  `speccy-references/evidence.md` already document the correct
  shape; no edit to `skills/` was required. The lesson belongs in
  the per-task journal, not in the skill body: if no genuine red
  capture is available, label as `hygiene` from the start rather
  than claiming `demonstrated` and disclaiming the file.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:57:50Z" round="2">
REQ-001 / REQ-002 / REQ-003 `<done-when>` items remain satisfied
under the round-2 checkout. No source changes landed in round 2;
the round-1 mechanical floor (clap subcommand wiring, status-gate
refusal with `ARCHIVABLE_STATUSES` naming the three allowed
statuses, frontmatter mutation with rollback on `git mv` failure,
`archived_at` written unconditionally and `archived_reason` only
when `--reason` was passed, ordering invariant of `archived_at`
before `archived_reason`) is intact at
`speccy-cli/src/archive.rs:122-227`. The `--json` placeholder
remains intentional and explicitly deferred to T-003 per the task
body. Non-goals respected: no bulk archive, no unarchive, no
status mutation under `--force` (verified by
`archive_in_progress_spec_with_force_moves_and_keeps_status`).
SPEC.md's `## Open Questions` is `(None.)` — nothing silently
resolved. Scope (REQ-001 / REQ-002 / REQ-003) matches the task's
`covers="..."` attribute.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:57:50Z" round="2">
Round-1 blocker resolved. The paper-trail file at
`.speccy/specs/0042-archive-completed-specs/evidence/T-002.md`
exists and records the captured hygiene-gate output for the new
`archive_text` integration suite (10 cases, all green) plus the
four standard hygiene commands. Per path (b) of the round-1
blocker, the round-2 `<implementer>` Evidence roll call relabels
all nine CHKs (CHK-001 through CHK-009) as `hygiene` with their
specific test-function citations in
`speccy-cli/tests/archive_text.rs`; no `demonstrated` label
fabricates pre-edit failure-mode output that was not captured.
The deliberate refusal to fabricate red captures is the correct
read of `.claude/speccy-references/evidence.md`'s coverage rule.
The integration suite itself is adversarially solid: each test
drives the real `speccy archive` binary via
`assert_cmd::Command::cargo_bin`, asserts exit codes, stderr
substrings (status-gate refusal names `in-progress` plus all
three archivable statuses), frontmatter line presence/absence,
ordering of `archived_at` before `archived_reason`, and the
`git status --porcelain=v1` rename invariant. Unit tests for
`insert_archive_fields` cover LF and CRLF line endings,
`yaml_double_quoted` covers quote/backslash escaping, and
`parse_reason` covers both LF and CR rejection.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:57:50Z" round="2">
No source changes in round 2; round-1 security verdict carries.
Re-verified: `git mv` shell-out at `archive.rs:186-194` passes
paths as separate `Command::arg(...)` invocations (no shell
interpolation, no command-injection vector). `stdin(Stdio::null())`
on the git invocation eliminates TTY-driven hangs. The `--reason`
clap value parser rejects `\n` / `\r` at parse time
(`archive.rs:439-444`), closing the YAML-line-break-injection
vector that would otherwise let a crafted reason inject arbitrary
frontmatter keys. `yaml_double_quoted` escapes `"` and `\\`
correctly; non-newline control chars pass through but are bounded
by the parse-time newline rejection. Source path derived from a
filesystem scan keyed on a regex-validated `SPEC-\d{4,}` ID, not
raw user input. No new dependencies, no secret handling, no
logging of sensitive material.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T08:57:50Z" round="2">
No source changes in round 2; round-1 style verdict carries.
Re-verified: `unwrap()` appears only in the two `OnceLock` regex
initializers (`spec_id_regex`, `spec_dir_name_regex` at
`archive.rs:319-331`), both guarded by
`#[expect(clippy::unwrap_used, reason = "...")]` per AGENTS.md
convention. No `#[allow(...)]` suppressions. The rollback pattern
`let _write = fs_err::write(...)` (`archive.rs:179`, `:199`,
`:209`) binds to a named variable to satisfy the `#[must_use]`
contract on `fs_err::write` without an `#[allow]`. The placeholder
JSON emission in `speccy-cli/src/main.rs` is ugly but explicitly
sanctioned by the task body ("the `--json` path is a placeholder
this task; T-003 fills it in") and will be replaced by
`serde_json::to_writer` on the receipt struct in T-003.
Dependencies (`jiff`, `fs-err`, `camino`) align with
`.claude/rules/rust/rust-dependencies.md`. The new test file
`archive_text.rs` carries the standard test-file lint allowance
header. Module-level docstring on `archive.rs` cites SPEC-0042
REQ-001 / REQ-002 / REQ-003.
</review>

