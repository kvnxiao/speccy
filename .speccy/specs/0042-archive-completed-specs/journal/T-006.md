---
spec: SPEC-0042
task: T-006
generated_at: 2026-05-23T00:00:00Z
---

<implementer date="2026-05-23T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: added the `--include-archive` flag to `speccy status`
  (clap definition in `speccy-cli/src/main.rs`; `StatusArgs` field
  in `speccy-cli/src/status.rs`). Exposed
  `speccy_core::workspace::scan_archive_specs` (new) and renamed the
  internal `parse_one_spec_dir` to `parse_spec_dir` so it could be
  reused by both the active and archive scans. Extended `SpecView`
  with an `archived` boolean and threaded it through
  `assemble`/`assemble_with_archive`/`build_view`. The text renderer
  emits an `[archived YYYY-MM-DD]` marker on archived entries; the
  attention filter (`show_in_text_view`) returns `false` for
  archived specs so they never appear in the default view; the
  hidden-specs footer count is computed over active specs only.
  JSON output reuses the existing `archived_at` / `archived_reason`
  serialization path that T-001 wired into `JsonSpec` — no JSON
  schema change beyond making those fields populated for archive
  entries.
- Undone: nothing — the task scope is fully covered.
- Hygiene checks: `cargo test --workspace` green;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  green; `cargo +nightly fmt --all --check` green; `cargo deny check`
  green (`advisories ok, bans ok, licenses ok, sources ok`).
- Evidence: paper trail at `.speccy/specs/0042-archive-completed-specs/evidence/T-006.md`.
  Roll call for CHKs under REQ-007:
  - CHK-017 (status --include-archive --json populates archived_at):
    demonstrated → evidence scenario
    `status_json_include_archive_surfaces_archived_spec_with_archived_at`.
  - CHK-018 (default status --json omits archived spec):
    demonstrated → evidence scenario
    `status_json_default_omits_archived_spec`.
  - CHK-019 (`speccy next --include-archive` exits 2):
    demonstrated → evidence scenario `next_rejects_include_archive`
    (plus `check`/`verify`/`lock` for the REQ-007 invariant that
    `--include-archive` is `status`-only).
- Discovered issues: none. The pre-existing `JsonSpec.archived_at`
  field landed by T-001/T-003 plumbing was already wired through
  `status.rs::json_spec`, so the JSON shape required no further
  change beyond ensuring archived specs make it into the spec list.
- Procedural compliance: followed the `speccy-work` skill steps —
  flipped task to `in-progress` before editing, kept the change
  surgical (only `status` learned the new flag; the other hot-path
  commands inherit their rejection from clap's default unknown-flag
  handling), and stopped after one task per the single-task
  primitive contract.

</implementer>

<review date="2026-05-23T10:30:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-006 cleanly implements REQ-007. The `--include-archive` flag is added to `speccy status` only (verified by `next_rejects_include_archive`, `check_rejects_include_archive`, `verify_rejects_include_archive`, `lock_rejects_include_archive` — covering CHK-019's "no `--include-archive` on other commands" extension). Default JSON omits archived specs (CHK-018 via `status_json_default_omits_archived_spec`); opt-in surfaces them with populated `archived_at` (CHK-017 via `status_json_include_archive_surfaces_archived_spec_with_archived_at`). The `--all --include-archive` superset behavior holds. Text-mode `[archived YYYY-MM-DD]` marker renders. Archived specs bypass the attention filter via `show_in_text_view` early-return and are excluded from the hidden-specs footer count. JSON entry includes `archived_at`/`archived_reason` with `skip_serializing_if = "Option::is_none"`, preserving byte-identical output for non-archived specs. No silent open-question resolutions; no non-goal scope leak.
</review>

<review date="2026-05-23T10:30:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
Tests in `speccy-cli/tests/status_include_archive.rs` drive the real `speccy` binary end-to-end via `assert_cmd`. CHK-017 archives SPEC-0001 with the real `speccy archive` command and asserts `archived_at` is a 10-char `YYYY-MM-DD` string. CHK-018 asserts the archived spec is absent AND the active SPEC-0002 is still present. CHK-019 asserts exit code 2 AND stderr naming `--include-archive` for every non-status hot-path command — no over-broad "any error" trap. Supplementary `--all --include-archive` and text-marker scenarios are also covered. No mocking of the system under test; the `archived_at` value comes from the production `archive` flow. Evidence file at `.speccy/specs/0042-archive-completed-specs/evidence/T-006.md` has a CHK-by-CHK roll call matching the actual test names and shows a scoped per-binary runner invocation. 8 tests pass.
</review>

<review date="2026-05-23T10:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-006 adds `--include-archive` (boolean, no user string) to `speccy status`, a `scan_archive_specs` function that filters directory names with the same anchored regex (`^\d{4}-[a-z0-9-]+$`) used for active specs, and populates `archived_at`/`archived_reason` in JSON output from parsed YAML frontmatter via serde_json. No injection vectors, no auth boundaries crossed, no secrets, no new dependencies, no path traversal surface.
</review>

<review date="2026-05-23T10:30:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Implementation is style-clean. All lint suppressions in production code use `#[expect(..., reason = "...")]` (the two regex-init `unwrap` suppressions in `archive.rs`). The `#![allow(clippy::expect_used, ...)]` crate-level attribute in `status_include_archive.rs` matches the identical pattern used in every other integration test file in `speccy-cli/tests/`. `fs_err` is used throughout with no `std::fs` calls. No dead imports, no bare `unwrap()`/`expect()` in non-test code outside the annotated regex initializers. The `assemble` → `assemble_with_archive` factoring avoids duplication cleanly. Naming and field types are consistent with the surrounding code.
</review>
