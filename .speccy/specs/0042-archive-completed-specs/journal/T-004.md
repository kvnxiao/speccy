---
spec: SPEC-0042
task: T-004
generated_at: 2026-05-23T09:37:59Z
---

<implementer date="2026-05-23T09:37:59Z" model="claude-opus-4-7[1m]" round="1">

- Completed: Added the pure detector
  `speccy_core::parse::supersession::orphan_candidates_on_archive(active: &[&SpecMd], archiving: &str) -> Vec<String>`
  implementing the SPEC-0042 REQ-008 algorithm (target active +
  status: superseded + sole declarer). Wired it into
  `speccy-cli/src/archive.rs` so `run` calls `scan_workspace`,
  collects active `&SpecMd` refs, invokes the detector before the
  filesystem mutation/`git mv`, and threads the resulting orphan
  IDs through a new `ArchiveOutcome.orphan_warnings` field.
  `ArchiveReceipt::from_outcome` now turns each orphan ID into an
  `{"spec":"SPEC-NNNN","reason":"orphaned-supersession"}` entry
  in the receipt's `warnings` array (otherwise still `[]`).
  `speccy-cli/src/main.rs` emits one human-readable warning line
  to stderr per orphan in both text and JSON modes, before any
  stdout output is written. Archival proceeds regardless of
  warnings — the gate is informational.
- Undone: nothing in scope for T-004. The simplification of
  `to_forward_slash` (T-003 holdover) was left alone.
- Hygiene checks: `cargo test --workspace` green;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  clean; `cargo +nightly fmt --all --check` clean; `cargo deny check`
  green. The new integration binary `archive_warnings` runs 4
  tests, the unit tests in `parse::supersession` grow by 6
  (sole-declarer warn, natural-archive empty, multi-declarer
  empty, target-not-active empty, target-not-superseded empty,
  empty-supersedes empty).
- Evidence: paper trail at
  `.speccy/specs/0042-archive-completed-specs/evidence/T-004.md`.
  Roll call for CHKs under REQ-008:
  - CHK-020 (sole declarer → warn): demonstrated →
    `archive_emits_orphan_warning_when_sole_declarer` in
    `speccy-cli/tests/archive_warnings.rs` covers the
    pre-implementation red (no warning ever) and the green
    (stderr names both specs, JSON `warnings` has one entry).
  - CHK-021 (natural archive of older spec → no warn):
    demonstrated → `archive_older_spec_in_pair_emits_no_warning`
    asserts no warning line and empty `warnings`.
  - CHK-022 (multi-declarer → no warn): demonstrated →
    `archive_multi_declarer_emits_no_warning` covers the
    SPEC-0019 + SPEC-0021 + SPEC-0022 fixture and asserts the
    warnings array stays empty.
- Discovered issues: none. The existing `Workspace` scan from
  `speccy_core::workspace::scan` already tolerates per-spec parse
  failures (`ParsedSpec` carries `Result` fields), so the orphan
  detector is naturally robust to a malformed sibling spec.
- Procedural compliance: implemented one task, single round.
  Flipped state pending → in-progress at start, in-progress →
  in-review at exit. No skill-file friction worth a downstream
  edit on this turn.

</implementer>

<review date="2026-05-23T09:41:14Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-004 satisfies REQ-008 end-to-end. The pure detector `orphan_candidates_on_archive` in `speccy-core/src/parse/supersession.rs` implements the three-condition algorithm (active, status=superseded, sole declarer) faithfully; six unit tests cover sole-declarer warn, natural-archive empty, multi-declarer empty, target-not-active empty, target-not-superseded empty, and empty-supersedes empty. The wiring in `speccy-cli/src/archive.rs` computes the candidate list before any filesystem mutation against the `scan_workspace` active set (which globs `.speccy/specs/` only, correctly excluding archive — preserving condition (a) of the algorithm). `speccy-cli/src/main.rs` emits one `warning:` stderr line per orphan naming both the archiving spec and the orphaned spec, mentioning SPC-006, in both text and JSON modes; `ArchiveReceipt::from_outcome` populates `warnings` with `{"spec":"SPEC-NNNN","reason":"orphaned-supersession"}` entries (empty `[]` when none). Four e2e tests in `speccy-cli/tests/archive_warnings.rs` cover CHK-020, CHK-021, CHK-022, and the empty-supersedes case; all green. Archival proceeds regardless of warnings (CHK-020 asserts exit 0 with the warning present). No non-goals violated; no goals dropped; no open questions silently resolved.
</review>

<review date="2026-05-23T09:41:14Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-004's CHK-020/021/022 are demonstrated by real end-to-end tests in `speccy-cli/tests/archive_warnings.rs` that invoke the `speccy archive --json` binary against fixture workspaces and assert on both stderr warning text and exact JSON shape (`[{"spec":"SPEC-0019","reason":"orphaned-supersession"}]`). The pure detector `orphan_candidates_on_archive` in `speccy-core/src/parse/supersession.rs` has 6 unit tests covering every algorithm branch (sole-declarer warn, natural archive empty, multi-declarer empty, target-not-active empty, target-not-superseded empty, empty-supersedes empty). Negative-path coverage is explicit, not just absence-of-crash. Evidence roll call matches test names in the diff; hygiene invocation is scoped per-test, not workspace-wide. All 10 tests run green locally.
</review>

<review date="2026-05-23T09:41:14Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
T-004 adds a pure orphan-detection function and wires it into the archive command. No blocking security findings.

- `validate_spec_id` enforces `^SPEC-\d{4,}$` before any path operations; the digit portion is used only as a `starts_with` prefix for directory lookup, not for direct path construction — path traversal is blocked.
- `--reason` injection into YAML: `parse_reason` rejects `\n`/`\r` at clap parse time; `yaml_double_quoted` escapes `"` and `\`. Sufficient for double-quoted YAML scalar safety.
- `git mv` is invoked via `std::process::Command` with explicit args (no shell interpolation); stdin is `Stdio::null()`.
- Orphan IDs surfaced in the stderr warning come from parsed frontmatter in the user's own workspace; no external input reaches that path.
- No auth boundaries, no secrets, no crypto, no network in this changeset.
</review>

<review date="2026-05-23T09:41:14Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All conventions in AGENTS.md and the rule files are observed. No blocking style issues found.

- `#![allow(clippy::expect_used, reason = "...")]` in `archive_warnings.rs` is pre-existing project-wide test file convention (every test file under `speccy-cli/tests/` carries it); T-004 follows the pattern exactly.
- Both `#[expect(clippy::unwrap_used, reason = "...")]` suppressions in `archive.rs` (the two regex `OnceLock` initializers) carry `reason` text as required.
- `orphan_candidates_on_archive` in `supersession.rs` is not a duplicate of `supersession_index`; the two functions answer distinct questions (workspace-wide inverse index vs. archive-specific orphan check).
- Error propagation uses `?` or explicit `match`/`map_err` throughout — no inconsistent `unwrap()` in production paths.
- Naming (`orphan_candidates_on_archive`, `ArchiveOutcome`, `ArchiveWarning`, `orphan_warnings`) is consistent with existing identifier style in the file.
- Clippy (`--workspace --all-targets --all-features -D warnings`) is clean.
</review>
