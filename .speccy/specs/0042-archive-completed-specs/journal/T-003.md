---
spec: SPEC-0042
task: T-003
generated_at: 2026-05-23T20:00:00Z
---

<implementer date="2026-05-23T20:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed: Replaced the inline placeholder `--json` writer in
  `speccy-cli/src/main.rs` with a serde-derived `ArchiveReceipt`
  envelope (`schema_version`, `archived`, `warnings`) defined in
  `speccy-cli/src/archive.rs`. Added `ArchivedSpec` and a
  T-004-ready `ArchiveWarning` struct so the `warnings: []` field
  has a concrete element type today. `archived_reason` is
  `Option<String>` with no `skip_serializing_if`, so absent reasons
  serialize as JSON `null` per REQ-009. Path strings on the
  receipt go through a `to_forward_slash` helper so the contract
  holds on Windows (where `Utf8Path` preserves native backslashes).
  Added `speccy-cli/tests/archive_json.rs` with 4 end-to-end tests
  covering CHK-023, CHK-024, null-reason, and forward-slash paths.
- Undone: nothing in T-003 scope. T-004 will populate the
  `warnings` array; the struct field is already wired.
- Hygiene checks: `cargo test --workspace`, `cargo clippy
  --workspace --all-targets --all-features -- -D warnings`,
  `cargo +nightly fmt --all --check`, `cargo deny check` — all
  pass on Windows host.
- Evidence: paper trail at
  `.speccy/specs/0042-archive-completed-specs/evidence/T-003.md`.
  Roll call for CHKs under REQ-009:
  - CHK-023 (jq-style receipt assertions on --json --reason):
    demonstrated → `archive_json_with_reason_emits_full_receipt`
    in `speccy-cli/tests/archive_json.rs` parses stdout and
    asserts `schema_version == 1`, `archived.id == "SPEC-0001"`,
    `archived.archived_reason == "ship cleanup"`, and
    `archived.to == ".speccy/archive/0001-artifact-parsers"`.
  - CHK-024 (in-progress --json refused, stdout empty, non-zero):
    demonstrated →
    `archive_json_on_in_progress_without_force_fails_with_empty_stdout`
    asserts exit-failure, empty stdout, stderr contains
    `in-progress`.
- Discovered issues: SPEC commentary for T-003 claimed `Utf8Path`
  rendering already enforces forward slashes; in practice
  `Utf8Path` preserves the host separator. Solved here by
  normalizing `from`/`to` at receipt-construction time. No SPEC
  amendment needed — the user-visible contract (forward slashes)
  is met.
- Procedural compliance: none. No shipped-skill edits needed.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-23T20:30:00Z" round="1">
REQ-009's receipt shape is delivered as specified. `ArchiveReceipt`
in `speccy-cli/src/archive.rs:235` carries `schema_version: u32 = 1`,
the nested `archived: ArchivedSpec` with all six required fields
(`id`, `slug`, `from`, `to`, `archived_at`, `archived_reason`), and
`warnings: Vec<ArchiveWarning>`. `archived_reason` is `Option<String>`
with no `skip_serializing_if`, so absent values serialize as JSON
`null` (REQ-009 sub-bullet honored, confirmed by
`archive_json_without_reason_emits_null_reason`). The text-mode
confirmation line from T-002 is gated behind `if json { ... } else
{ println!("archived ...") }` in `main.rs:148` so `--json` emits
exactly one JSON object to stdout with no other output. Failure
paths in `dispatch` return exit 1 with stderr-only output, leaving
stdout empty as REQ-009 requires (verified by
`archive_json_on_in_progress_without_force_fails_with_empty_stdout`).
`ArchiveWarning` is declared with concrete fields (`spec`, `reason`)
ready for T-004 wiring without forcing a struct redesign. No scope
creep into T-004's orphan-detection territory; the receipt always
emits `warnings: []`. Non-goals respected.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/high" date="2026-05-23T20:30:00Z" round="1">
Evidence file at
`.speccy/specs/0042-archive-completed-specs/evidence/T-003.md`
carries a proper CHK roll call: CHK-023 → demonstrated by
`archive_json_with_reason_emits_full_receipt`; CHK-024 →
demonstrated by
`archive_json_on_in_progress_without_force_fails_with_empty_stdout`.
The evidence's framing of the "red" phase as the trivial absence of
the new keys is defensible — T-003 replaces a placeholder writer
with a serde-derived envelope, so the pre-edit form structurally
cannot satisfy the assertions. Tests are real end-to-end exercises
via `assert_cmd::Command::cargo_bin("speccy")`, parsing actual
`serde_json` output and asserting on typed values (not on byte
substrings alone). The null-reason test pairs a substring check
(`"archived_reason":null`) with a typed `Value::is_null` assertion,
which catches both "key omitted" and "string-typed null" failure
modes. The forward-slash test asserts `!from.contains('\\')` and
`!to.contains('\\')` directly, which exercises the
`to_forward_slash` normalizer on Windows (where I confirmed the
test suite passes via `cargo test -p speccy-cli --test
archive_json`, 4/4 green). No mock-the-system-under-test patterns,
no empty assertions, no snapshot-after-bug shape.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-7[1m]/high" date="2026-05-23T20:30:00Z" round="1">
T-003 introduces only a JSON serialization shape over data already
collected by T-002. No new external inputs: `--reason` is still
validated for newlines at clap parse time
(`speccy-cli/src/archive.rs:522` `parse_reason`), the spec ID is
still regex-checked, and the receipt fields derive from
workspace-relative paths already computed by T-002's archive logic.
No secrets in the receipt. The failure path emits zero bytes to
stdout under `--json`, eliminating the partial-JSON-leak vector that
would have existed had error text been mixed into the success-shape
buffer. The `serde_json::to_string` error case is caught and
surfaced via stderr + exit 1 rather than `unwrap`. `Utf8Path`-typed
paths preclude non-UTF-8 byte sequences from reaching the receipt.
No new dependencies, no new dynamic linking, no cryptographic
primitives in scope. Path strings remain relative to project root
(no absolute paths leaked into the receipt), which preserves
predictable output regardless of where the user's checkout lives.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-7[1m]/medium" date="2026-05-23T20:30:00Z" round="1">
Diff matches established conventions across the workspace.
`#[derive(Debug, Clone, Serialize)]` matches the project's other
serde-derived output structs (`status_output.rs`, `next_output.rs`).
`#[must_use = "the receipt is the JSON payload to emit on stdout"]`
on `ArchiveReceipt::from_outcome` follows the `must_use` rule from
`.claude/rules/rust/rust-code-quality.md`. Doc comments populate
every public item (module, struct, struct fields, `from_outcome`,
the new `to_forward_slash` helper). No `unwrap`/`expect`/`panic`
in production code; the new code path uses `?`-propagation and
match-on-Result patterns consistent with the rest of `archive.rs`.
The test file's `#![allow(clippy::expect_used, reason = "...")]`
header matches the project-wide convention used across all 27
existing integration test binaries under `speccy-cli/tests/`
(verified via grep). `to_forward_slash` short-circuits on
already-forward-slash hosts before touching `String::replace`,
avoiding unnecessary allocation. No `#[allow]` directives without
`reason`. Imports follow `camino` / `fs_err` / `jiff` substitutions
mandated by `.claude/rules/rust/rust-dependencies.md`. `cargo
clippy --workspace --all-targets --all-features -- -D warnings`
runs clean (verified locally during this review).
</review>
