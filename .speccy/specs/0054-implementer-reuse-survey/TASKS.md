---
spec: SPEC-0054
spec_hash_at_generation: a0d4c677fb2248d641c0cd93f4132e1e9d3ae2ffdee072fc877cecb9b227f161
generated_at: 2026-05-31T07:33:03Z
---
# Tasks: SPEC-0054 Implementer pre-implementation reuse survey, forked reuse guidance, and `speccy-work` opus/high repin

<task id="T-001" state="completed" covers="REQ-004">
## Repin the Claude Code `speccy-work` agent from `effort: low` to `effort: high`

Bump only the effort axis on the Claude Code implementer pin. The model
(`opus[1m]`) and every other frontmatter field stay unchanged, the Codex
template is untouched (its `model_reasoning_effort` axis is a separate
judgement per DEC-005), and no Rust or test change is needed — `high` is
already in the `pin_shape` Opus allow-set, so the existing `pin_shape`
suite passes as-is. This task is independent of the prose work and lands
first as the smallest self-contained slice.

Steps:

1. In `resources/agents/.claude/agents/speccy-work.md.tmpl`, change
   `effort: low` to `effort: high`. Leave `name`, `description`,
   `model: opus[1m]`, and the `{% include "modules/phases/speccy-work.md" %}`
   body line unchanged.
2. In `README.md`, the "Pin assignment" table row for `speccy-work`
   (`| `speccy-work` | `model: opus[1m]`, `effort: low` | ...`), change
   `effort: low` to `effort: high`. Leave the Codex column (`reasoning
   effort medium`) unchanged. The version-lock override example below the
   table names only `model: opus[1m]` (no effort value), so it stays
   consistent and needs no edit.
3. Leave `resources/agents/.codex/agents/speccy-work.toml.tmpl`
   unchanged.
4. Run `just reeject` to regenerate the ejected packs, then run the four
   standard hygiene gates.

CHK-008 (the next dogfood run recording a `/high` model suffix) is
dogfooding / judgment-only and not CI-provable; it is not a deliverable
of this task, only confirmed later by inspecting a post-change journal
entry.

<task-scenarios>
Given the template edited to `effort: high`,
when `just reeject` runs,
then `.claude/agents/speccy-work.md` declares `effort: high` with
`model: opus[1m]` unchanged.

Given the regenerated tree,
when `just reeject` runs a second time,
then `git status --porcelain` is empty — template and in-tree agent file
are aligned with no residual drift.

Given the edited tree,
when `cargo test --workspace` runs,
then it exits 0, including the `speccy-cli/tests/pin_shape.rs` suite
(`high` is already a valid Opus effort, so no allow-set change is
required).

Given the README after the edit and the Codex template,
when the "Pin assignment" table and the Codex `.toml.tmpl` are read,
then the `speccy-work` row shows `effort: high` in the Claude Code
column, the Codex column is unchanged, and
`resources/agents/.codex/agents/speccy-work.toml.tmpl` is absent from the
diff.

Suggested files: `resources/agents/.claude/agents/speccy-work.md.tmpl`,
`README.md`, `.claude/agents/speccy-work.md` (regenerated by reeject),
`.agents/agents/speccy-work.md` and `.codex/agents/speccy-work.toml`
(regenerated; Codex effort unchanged)
</task-scenarios>
</task>

<task id="T-002" state="completed" covers="REQ-001 REQ-003">
## Fork the reuse guidance into lifecycle variants and add the bounded pre-implementation survey step

This is the atomic reuse-fork-and-survey change. Today
`resources/modules/references/convention-checklist.md` carries one
"Reuse over reinvent" bullet that is `{% include %}`d by both the
implementer self-review step in the `speccy-work` phase and by
`reviewer-style`. Per DEC-001, fork *only* the reuse concern into two
lifecycle-specific modules so the producer's and reviewer's framings can
diverge, and add the bounded pre-implementation survey step to the
phase. Removing the shared bullet must be atomic with providing both
forks (otherwise a lifecycle loses reuse guidance for an interval), and
the implementer fork plugs into the new survey step — that coupling is
why REQ-001 and REQ-003 land in one task.

Steps:

1. Create `resources/modules/references/reuse-survey-implementer.md` —
   the implementer "survey-and-build" variant. It must: define the three
   tiers (reuse-as-is / extend / write-fresh); require naming the
   specific existing symbol for reuse-as-is and extend, or naming the
   search that came up empty for write-fresh; state the survey is bounded
   to the task's area (its covered REQs, the suggested-files hint, and
   the immediate module / neighbouring files) and is explicitly **not** a
   whole-repo scan; and state the round semantics — the full area-map is
   round-1 only (re-run on a retry round only when a reuse-related
   blocker was raised), while the per-symbol floor (for each new
   top-level symbol the implementation introduces, name the existing
   thing reused or extended, or the search that found nothing) is
   round-agnostic. Write it language- and project-agnostic; it ships
   downstream via `speccy init`, so use no Speccy-repo identifiers.
2. Create `resources/modules/references/reuse-hunt-reviewer.md` — the
   reviewer "adversarially verify-and-hunt" variant: verify the
   `Reuse survey` field is present in the implementer's journal block,
   confirm the existing symbols it names actually exist, and hunt
   reinvention or near-duplicate code that should have been extended.
   Adversarial framing, project-agnostic.
3. In `resources/modules/references/convention-checklist.md`, remove the
   "Reuse over reinvent" bullet (the first list item). Leave the other
   four items unchanged (match-local-conventions, docs-match-code,
   no-false-complexity, re-apply-the-project's-own-hard-rules).
4. In `resources/modules/phases/speccy-work.md`, insert a new numbered
   step between the current step 5 ("Read the task scenarios") and step 6
   ("Implement the task"), renumbering the downstream steps. The new step
   introduces the bounded reuse survey before implementation, scopes it
   to the task area, states it is not a whole-repo scan, and
   `{% include "modules/references/reuse-survey-implementer.md" %}`s the
   implementer variant for the tier definitions and round semantics.
   Update the phase's internal step-number cross-references so none go
   stale — in particular the first-attempt-branch action list at step 3
   ("Proceed with the recipe below (steps 4–9) ...") must reflect the
   inserted survey step in both its range and its enumerated actions.
5. In `resources/modules/personas/reviewer-style.md`, add
   `{% include "modules/references/reuse-hunt-reviewer.md" %}` under the
   "What to look for that's easy to miss" section, alongside the existing
   `convention-checklist.md` include.
6. Run `just reeject` — both new includes must resolve at render time at
   their callsites — then run the four standard hygiene gates.

<task-scenarios>
Given the ejected `speccy-work` phase body after `just reeject`,
when a reviewer reads it,
then a bounded reuse-survey step appears after the read-scenarios step
and before the implement step, scoped to the task area (explicitly not a
whole-repo scan), defining the three tiers and the round-agnostic
per-symbol floor versus the round-1 / reuse-blocker-triggered area-map.

Given the ejected phase body, the ejected `reviewer-style` persona, and
the shared `convention-checklist.md` after `just reeject`,
when a reviewer reads all three,
then the phase includes `reuse-survey-implementer.md`, `reviewer-style`
includes `reuse-hunt-reviewer.md`, and `convention-checklist.md` no
longer carries a reuse bullet while retaining its other four items.

Given the renumbered phase step list,
when the phase body is read end to end,
then no stale step-number cross-reference remains — the first-attempt
branch's action list and any "steps N–M" range reflect the inserted
survey step.

Given the edited `resources/`,
when `just reeject` and `cargo test --workspace` run,
then both exit 0 — both new includes resolve at their respective
callsites and no pack-structure test breaks.

Suggested files:
`resources/modules/references/reuse-survey-implementer.md` (new),
`resources/modules/references/reuse-hunt-reviewer.md` (new),
`resources/modules/references/convention-checklist.md`,
`resources/modules/phases/speccy-work.md`,
`resources/modules/personas/reviewer-style.md`, plus the regenerated
`.claude/`, `.agents/`, and `.codex/` copies
</task-scenarios>
</task>

<task id="T-003" state="pending" covers="REQ-002">
## Record the survey in a free-form `Reuse survey` journal field

Add a seventh free-form field, `Reuse survey`, to the implementer
handoff. Per DEC-003 the field is unenforced — make no `speccy-core`
parser or lint change; the `<implementer>` body stays verbatim, validated
only on its `date` / `model` / `round` attributes, and the existing body
fields remain unenforced convention alongside the new one. Two source
files name the handoff template and must move in lockstep or they will
contradict each other: the canonical reference
(`resources/modules/references/journal-implementer.md`) and the phase
body's inline restatement in
`resources/modules/phases/speccy-work.md` (the "six-field handoff
template (`Completed`, `Undone`, ...)" prose). Update both.

Steps:

1. In `resources/modules/references/journal-implementer.md`: add a
   `Reuse survey` field to the handoff-template section (currently the
   "Six-field handoff template" heading with six bullets — make it seven
   and rename the heading count accordingly), describing it as the
   recorded survey (the mapped areas and the per-tier decisions with
   named symbols). State the round semantics: round-1 records the full
   survey; a retry round that adds no new top-level symbol and addresses
   a non-reuse blocker records "unchanged — no new symbols, no reuse
   blocker" (or the delta), not a fresh re-survey. Add the field to the
   worked `<implementer>` example block so the example stays consistent,
   and update any other "six-field" phrasing in the file (e.g. the worked
   example's Procedural-compliance sentence) to "seven-field". Place the
   field coherently in the order — the survey is a pre-implementation
   design input, so near the top of the body reads naturally.
2. In `resources/modules/phases/speccy-work.md`: update the inline
   handoff-template restatement — the parenthetical field roll-call
   (`Completed`, `Undone`, `Hygiene checks`, `Evidence`,
   `Discovered issues`, `Procedural compliance`) — to include
   `Reuse survey`, and change the count word ("six" → "seven"). Keep the
   field ordering consistent with the reference.
3. Make no change to `speccy-core` journal parsing or to any lint rule.
   The diff must contain no file under `speccy-core/src/parse/` or
   `speccy-core/src/lint/`.
4. Run `just reeject`, then run the four standard hygiene gates; the
   unchanged journal-parsing tests must still pass.

<task-scenarios>
Given the ejected `journal-implementer` reference after `just reeject`,
when a reviewer reads it and the diff,
then the `Reuse survey` field is present in the handoff template with its
round-1-versus-retry semantics, the worked example block carries the
field, and no `speccy-core` parser or lint file appears in the diff.

Given the ejected `speccy-work` phase body,
when its inline handoff-template restatement is read,
then it names `Reuse survey` and agrees with the reference on the field
set — no "six-field" wording survives in either file.

Given the reference's round semantics,
when read,
then a round-1 turn records the full survey and a retry round that adds
no new top-level symbol records "unchanged" rather than a fresh survey.

Given the edited `resources/`,
when `just reeject` and `cargo test --workspace` run,
then both exit 0 — the journal reference renders at its callsite and the
unchanged journal-parsing tests still pass.

Suggested files:
`resources/modules/references/journal-implementer.md`,
`resources/modules/phases/speccy-work.md`, plus the regenerated
`.claude/`, `.agents/`, and `.codex/` copies
</task-scenarios>
</task>
