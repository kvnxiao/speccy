---
spec: SPEC-0054
task: T-002
generated_at: 2026-05-31T08:30:00Z
---

<implementer date="2026-05-31T08:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: round-1 area-map of the T-002 area — the four
  `resources/modules/` files named in the task plus the existing
  reference modules in `resources/modules/references/`. Tiers:
  - Reuse-as-is: the MiniJinja `{% include %}` directive mechanism and
    the existing `{% include "modules/references/convention-checklist.md" %}`
    callsites in both `phases/speccy-work.md` and
    `personas/reviewer-style.md` — wired the two new includes the same
    way, no new render mechanism. The `## ` / `- **Bold.**` heading and
    bullet style of `convention-checklist.md` was reused for the new
    `reuse-survey-implementer.md` and `reuse-hunt-reviewer.md` bodies so
    they read as siblings of the sibling reference modules.
  - Extend: none — the reuse concern was *forked out* of
    `convention-checklist.md` (one bullet removed) into two new files,
    not extended in place.
  - Write-fresh: two new top-level module files,
    `reuse-survey-implementer.md` and `reuse-hunt-reviewer.md`. Searched
    `resources/modules/references/` and `resources/modules/personas/`
    for an existing reuse-survey or reuse-hunt module before creating
    them — none existed (the only prior reuse text was the single
    `convention-checklist.md` bullet, which DEC-001 directs to fork, not
    reuse in place). Per-symbol floor: both new files are the fresh
    symbols this task introduces; each is justified by REQ-003's
    explicit fork-into-two-modules directive.
- Completed: REQ-001 — inserted a bounded reuse-survey step (new step 6)
  in `resources/modules/phases/speccy-work.md` between read-scenarios and
  implement, scoping it to the task area (covered REQs, suggested-files
  hint, immediate/neighbouring files), stating it is not a whole-repo
  scan, and `{% include %}`-ing `reuse-survey-implementer.md` for the
  three tiers and round semantics (round-1 area-map, reuse-blocker-only
  re-run, round-agnostic per-symbol floor). Renumbered downstream steps
  6→7, 7→8, 8→9, 9→10 and updated the step-3 first-attempt-branch
  cross-reference ("steps 4–9" → "steps 4–10", added the survey to its
  enumerated action list). REQ-003 — created
  `reuse-survey-implementer.md` (survey-and-build) and
  `reuse-hunt-reviewer.md` (adversarial verify-and-hunt), removed the
  "Reuse over reinvent" bullet from `convention-checklist.md` (its other
  four items unchanged), and added the reviewer include to
  `personas/reviewer-style.md` under "What to look for that's easy to
  miss". Ran `just reeject`.
- Undone: nothing in T-002's scope. REQ-002's `Reuse survey` journal
  field is T-003, deliberately untouched here.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exit 0;
  `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file — DEC-006 makes the only
  deterministic gate `just reeject` + `cargo test --workspace` green;
  the substantive criteria are judgment-only on the diff.
  Roll call for CHKs under REQ-001 and REQ-003:
  - CHK-001 (bounded survey step placed after read-scenarios, before
    implement, scoped, three tiers, round-agnostic per-symbol floor):
    judgment-only → new step 6 in the ejected `.claude/agents/speccy-work.md`
    plus the included `reuse-survey-implementer.md` body; confirmed on
    the diff.
  - CHK-002 (`just reeject` + `cargo test --workspace` exit 0, phase
    edit renders, no pack-structure break): hygiene → both ran exit 0;
    second `just reeject` reported 0 overwritten (idempotent).
  - CHK-005 (phase includes implementer variant, `reviewer-style`
    includes reviewer variant, checklist loses reuse bullet but keeps
    four items): judgment-only → confirmed on the diff; `grep "Reuse
    over reinvent"` over `.claude .agents .codex` returns nothing.
  - CHK-006 (both new includes resolve at render time, no pack break):
    hygiene → `just reeject` resolved both includes (ejected
    `speccy-work` carries the survey body, ejected `reviewer-style`
    carries the reuse-hunt body) and `cargo test --workspace` exit 0.
- Discovered issues: none.
- Procedural compliance: edited only source under `resources/` then ran
  `just reeject`; no ejected file under `.claude/`, `.agents/`, `.codex/`
  was hand-edited. No Rust, parser, or lint file touched (REQ-001/REQ-003
  are prose-only). New module bodies use no Speccy-repo identifiers, so
  they ship safely downstream via `speccy init`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:06:12Z" round="1">
T-002 (covers REQ-001, REQ-003) satisfies every `<done-when>` and `<behavior>` on the diff. REQ-001: the bounded survey is a new step 6 in `resources/modules/phases/speccy-work.md`, placed after read-scenarios (step 5) and before implement (step 7), scoped to the task area with an explicit "not a whole-repo scan", and `{% include %}`s `resources/modules/references/reuse-survey-implementer.md`, which defines all three tiers with per-tier symbol-naming and the round semantics (round-1 area-map, reuse-blocker-only re-run, round-agnostic per-symbol floor). The step-3 first-attempt cross-reference was correctly updated from "steps 4–9" to "steps 4–10" with the survey added to its enumerated action list; no stale step range survives. REQ-003: the reuse bullet is removed from `resources/modules/references/convention-checklist.md` (the other four items remain), and the two forked variants land in separate files — implementer `reuse-survey-implementer.md` wired into the phase, reviewer `reuse-hunt-reviewer.md` wired into `resources/modules/personas/reviewer-style.md` and covering all three adversarial duties. Both includes resolve in the ejected packs; `just reeject` is idempotent and `cargo test --workspace` exits 0. No non-goal breached. The six-field handoff template and the `Reuse survey` journal field are correctly left to T-003 (REQ-002).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:06:12Z" round="1">
All four covered CHKs (CHK-001, CHK-002, CHK-005, CHK-006) appear in the journal's Evidence roll call with plausible labels. The two `judgment-only` CHKs (prose presence/placement and fork structure) are correctly deferred — they are prose-on-the-diff facts, not deterministically testable without a vacuous substring-match test the project forbids. The two `hygiene` CHKs cite the genuine deterministic gate for this prose-only slice per SPEC-0054 DEC-006: I independently re-ran `just reeject` (idempotent, 0 overwritten, 36 unchanged — both new includes resolve at render time, no raw `{% include %}` leaks) and `cargo test --workspace` (0 failures, pack-structure suites green). The "no separate evidence file" posture is spec-sanctioned (DEC-006). Verified on the diff: the "Reuse over reinvent" bullet is gone from all ejected packs while the other four checklist items remain; the implementer survey body and reviewer reuse-hunt body render into both `.claude` and `.codex` packs; step renumbering is clean (1–10, first-attempt branch updated to "steps 4–10"). The residual "six-field" wording is out of T-002 scope (deferred to pending T-003/REQ-002).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:06:12Z" round="1">
No security-relevant surface in this slice. The change is prose-only (skill/persona/reference modules); no auth, input-validation, injection, crypto, secret-handling, logging, or race-condition code is touched, and the diff contains no `speccy-core/src/parse/` or `/lint/` files. The two new shipped module bodies (`reuse-survey-implementer.md`, `reuse-hunt-reviewer.md`) carry no real Speccy-repo identifiers, no absolute paths, and no secret-like material, so they ship safely downstream via `speccy init`. The reuse-hunt reviewer guidance is additive to `reviewer-style` and dilutes no existing security-review criteria; the implementer north-star map's Security bullet is intact. Ejected packs carry the resolved include bodies with no unresolved `{% include %}` markers and no orphaned reuse bullet.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T08:06:12Z" round="1">
`resources/modules/references/reuse-hunt-reviewer.md:1` uses `### Reuse hunt` (H3), but it is placed "alongside" `convention-checklist.md` in `reviewer-style.md` per the task's step 5. In the ejected output at `.claude/agents/reviewer-style.md:121`, this makes "Reuse hunt" render as a *sub-section of* "Convention-drift checklist" (H2 at line 83) — a subordinate, not a peer. The task says "alongside", so both should be at the same heading level. The sibling module `reuse-survey-implementer.md` uses `##` for its top-level heading, making the two new peer modules inconsistent with each other. To fix: raise the heading in `reuse-hunt-reviewer.md` from `###` to `##` so it renders as a peer alongside `## Convention-drift checklist`, then run `just reeject` to regenerate the ejected output.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:06:12Z" round="1">
No correctness defect found. Step renumbering in `resources/modules/phases/speccy-work.md` is internally consistent: new step 6 (reuse survey) shifts old 6→7/7→8/8→9/9→10, the only range cross-reference was updated (`steps 4–9` → `steps 4–10`) with the survey added to the enumerated action list, and the entry-precondition's step 1/step 2 references remain valid. Both new includes (`reuse-survey-implementer.md`, `reuse-hunt-reviewer.md`) resolve at render time in all four ejected wrappers with no leaked `{% include %}` directives; `just reeject` is idempotent (0 overwritten) and the "Reuse over reinvent" bullet is fully removed from the ejected packs. The fork-out and bullet removal land together in one working tree, so no lifecycle loses reuse guidance for an interval.
</review>

<review persona="docs" verdict="blocking" model="claude-sonnet-4-6[1m]" date="2026-05-31T08:06:12Z" round="1">
Within-file contradiction in `resources/modules/phases/speccy-work.md` (and its ejected copies): step 6 at line 148 tells the implementer to "record it in the `Reuse survey` journal field on handoff," but step 9 at line 236 says "Use the six-field handoff template the implementer prompt supplies (`Completed`, `Undone`, `Hygiene checks`, `Evidence`, `Discovered issues`, `Procedural compliance`)" — `Reuse survey` is not in that list. An implementer reading step 9's enumeration after T-002 lands but before T-003 lands has contradictory authoritative text within the same file. T-003 is scoped to add the field to the handoff template (its step 2), but until then the shipped phase carries the contradiction. Options: (1) collapse the step-9 parenthetical update (add `Reuse survey`, six→seven) into T-002; or (2) remove the forward reference to the `Reuse survey` journal field from step 6's prose so the contradiction does not land until T-003. Option 2 is the smaller change and keeps T-002 within its REQ-001/REQ-003 scope.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:06:12Z" round="1">
SPEC-0054 DEC-001's "fork only the reuse item into two lifecycle-specific modules" is implemented as designed. The shared "Reuse over reinvent" bullet is removed from `resources/modules/references/convention-checklist.md` (the other four items unchanged) atomically with both new forks — `reuse-survey-implementer.md` (producer) and `reuse-hunt-reviewer.md` (reviewer) — and both include callsites (`phases/speccy-work.md`, `personas/reviewer-style.md`). No lifecycle is left without reuse guidance; `grep "Reuse over reinvent"` across `.claude .agents .codex resources` is empty. Module-include layering follows the established `resources/modules/references/` source-of-truth pattern with the same `{% include %}` form as the existing `convention-checklist.md` callsite; `just reeject` is idempotent and both Claude Code and Codex packs carry the forks. The split is a justified divergence (two distinct concrete consumers, diverging build-vs-hunt framings) rather than premature abstraction, and respects DEC-002's "no new reviewer-reuse persona" and the AGENTS.md deduplication conventions.
</review>

<blockers date="2026-05-31T08:06:12Z" round="1">
Two blocking review findings to resolve before the next implementer pass; both are prose fixes under `resources/` followed by `just reeject`.
- style: `resources/modules/references/reuse-hunt-reviewer.md` opens with `### Reuse hunt` (H3) while its peer `reuse-survey-implementer.md` opens with `## ` (H2). When included into `reviewer-style.md` "alongside" the `## Convention-drift checklist` block, the H3 makes "Reuse hunt" render as a subsection of the convention checklist rather than a peer check. Raise the `reuse-hunt-reviewer.md` top heading from `###` to `##` so the two new modules are consistent and the reuse-hunt block renders as a peer, then `just reeject`.
- docs: within-file contradiction in `resources/modules/phases/speccy-work.md` — the new step 6 instructs the implementer to "record it in the `Reuse survey` journal field on handoff," but step 9's inline handoff-template restatement still lists only the six fields (`Completed`, `Undone`, `Hygiene checks`, `Evidence`, `Discovered issues`, `Procedural compliance`) and the `Reuse survey` field itself is not introduced until T-003 (REQ-002). Resolve the contradiction without pulling T-003's scope forward: remove the forward reference to the `Reuse survey` journal field from step 6's prose (describe performing the bounded survey, not where to record it). The journal field and the matching six→seven handoff-template update remain T-003's responsibility.
</blockers>

<implementer date="2026-05-31T09:15:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: round-2 retry — reuse-blocker-only re-run scope, no
  area re-survey. Neither blocker touched the tier classification from
  round 1; both are in-place edits to symbols round 1 already created
  (`reuse-hunt-reviewer.md` heading, `phases/speccy-work.md` step 6).
  No new symbols introduced this round, so the per-symbol floor is a
  no-op.
- Completed: addressed both round-1 blockers by amending the existing
  WIP in place (no restore/clean/rewrite).
  - style: raised the top heading of
    `resources/modules/references/reuse-hunt-reviewer.md` from `###` to
    `##` so it renders as a peer of `## Convention-drift checklist`
    (and is consistent with the sibling `reuse-survey-implementer.md`,
    which already used `##`). Ejected `reviewer-style` now carries
    `## Reuse hunt …` at H2 in both `.claude` and `.codex`.
  - docs: removed the forward reference to the `Reuse survey` journal
    field from step 6 in `resources/modules/phases/speccy-work.md`
    ("Let the survey inform what you write in the next step." — dropped
    "and record it in the `Reuse survey` journal field on handoff").
    Step 9's six-field handoff template no longer contradicts step 6;
    the journal field plus the six→seven template update remain T-003
    (REQ-002).
  Ran `just reeject` (2 overwritten, idempotent on re-run).
- Undone: nothing in T-002's scope. The `Reuse survey` field and the
  seven-field template are T-003.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exit 0;
  `cargo +nightly fmt --all --check` exit 0 (only pre-existing unknown
  config-option warnings, no formatting diff); `cargo deny check` exit 0.
- Evidence: no separate evidence file (DEC-006 — the only deterministic
  gate is `just reeject` + `cargo test --workspace` green; substantive
  criteria are judgment-only on the diff).
  Roll call for CHKs under REQ-001 and REQ-003:
  - CHK-001 (bounded survey step placed after read-scenarios, before
    implement, scoped, three tiers, round-agnostic per-symbol floor):
    judgment-only → step 6 unchanged in shape; only the trailing
    forward-reference clause was dropped. Confirmed on the diff.
  - CHK-002 (`just reeject` + `cargo test --workspace` exit 0, phase
    edit renders, no pack-structure break): hygiene → both exit 0;
    second `just reeject` idempotent.
  - CHK-005 (phase includes implementer variant, `reviewer-style`
    includes reviewer variant, checklist loses reuse bullet keeping
    four items): judgment-only → unchanged; confirmed on the diff.
  - CHK-006 (both new includes resolve at render time, no pack break):
    hygiene → `grep "{% include"` over `.claude .agents .codex` returns
    nothing; both bodies present in ejected packs.
- Discovered issues: none.
- Procedural compliance: edited only source under `resources/` then ran
  `just reeject`; no ejected file hand-edited. Both fixes are prose-only
  under `resources/modules/`; no Rust/parser/lint file touched.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:15:07Z" round="2">
The round-1 style blocker is resolved (ejected `reviewer-style.md:121` now renders `## Reuse hunt` as an H2 peer of `## Convention-drift checklist`). The round-1 docs blocker is NOT resolved — it was only relocated, not removed. The round-1 `<blockers>` docs directive required removing the forward reference to the `Reuse survey` journal field from step 6's prose, leaving the journal field and the six→seven handoff-template update to T-003 (REQ-002). The round-2 implementer dropped the clause from the *inline* phase prose, but the module `{% include %}`d into step 6, `resources/modules/references/reuse-survey-implementer.md:41`, still ends with "Record the result in the journal's `Reuse survey` field on handoff." Because that module renders directly inside step 6, the shipped phase body still carries the forward reference: `.claude/agents/speccy-work.md:195` says "Record the result in the journal's `Reuse survey` field on handoff," while step 9's handoff template at `.claude/agents/speccy-work.md:349` (source `resources/modules/phases/speccy-work.md:235`) still enumerates only the six fields with no `Reuse survey`. This is the exact within-file contradiction the round-1 docs persona blocked, now living in the included module rather than the inline prose, and it leaks REQ-002 scope deferred to T-003. To fix within T-002's scope: remove the trailing "Record the result in the journal's `Reuse survey` field on handoff." sentence from `resources/modules/references/reuse-survey-implementer.md`, then `just reeject`.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:15:07Z" round="2">
Round-2 evidence roll call is complete and correctly labeled for both covered REQs: CHK-001 + CHK-002 (REQ-001) and CHK-005 + CHK-006 (REQ-003) all appear, with the two prose-presence/fork-structure CHKs as `judgment-only` (rightly deferred — the project forbids substring-matching curated prose, and SPEC-0054 DEC-006 makes the substantive criteria judgment-only) and the two as `hygiene` citing the spec-sanctioned deterministic gate. I independently reproduced both hygiene claims: `just reeject` is idempotent, `cargo test --workspace` exits 0, no raw `{% include %}` leaks across `.claude/.agents/.codex/`, the `## Reuse hunt` block renders at H2 (style blocker fixed), and the "Reuse over reinvent" bullet is gone from every ejected pack while the other four checklist items remain. No fabrication pattern. (Out of my remit, flagging for the docs/style re-review only: the included `reuse-survey-implementer.md` still ends with "Record the result in the journal's `Reuse survey` field on handoff," which renders inside step 6 at `.claude/agents/speccy-work.md:195` while step 9 still lists six fields — the within-file contradiction the docs blocker named arguably still ships. No T-002 scenario asserts this, so it is not a tests-persona blocker.)
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T08:15:07Z" round="2">
No security-relevant surface in this slice. The round-2 changes are prose-only edits to shipped skill/persona/reference modules (`resources/modules/phases/speccy-work.md` step 6 dropped a forward-reference clause; `resources/modules/references/reuse-hunt-reviewer.md` heading raised `###`→`##`); no auth, input-validation, injection, crypto, secret-handling, logging, or race-condition code is touched, and no `speccy-core/src/parse/` or `/lint/` file appears. The two new shipped module bodies carry no real Speccy-repo identifiers, absolute paths, URLs, or secret-like material, so they ship safely downstream via `speccy init`. The additive `reuse-hunt-reviewer.md` guidance in `reviewer-style` dilutes no existing security-review criteria. Ejected `.claude`/`.codex` packs carry resolved include bodies with no unresolved `{% include %}` markers and no orphaned reuse bullet.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T08:15:07Z" round="2">
Round-1 blocker resolved. `resources/modules/references/reuse-hunt-reviewer.md:1` now opens with `## Reuse hunt` (H2), matching the H2 used by sibling `reuse-survey-implementer.md` and rendering as a peer of `## Convention-drift checklist` in the ejected `reviewer-style.md` (lines 83 and 121 respectively). No new style issues introduced in the round-2 amendments.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:15:07Z" round="2">
No correctness defect found in T-002's round-2 changes. The step renumbering in `resources/modules/phases/speccy-work.md` is internally consistent: steps run 1–10 with no gaps or duplicates, the inserted bounded-survey step shifts old 6→7/7→8/8→9/9→10 cleanly, the only step-range cross-reference (first-attempt branch) was updated `4–9`→`4–10` with the survey added to its enumerated action list, and the entry-precondition's `step 1`/`step 2` references remain valid. The two `{% include %}` directives resolve at render time with no leaked markers, and the round-1 style fix landed (`reuse-hunt-reviewer.md` now opens with `##`). Note for the docs owner, not a correctness blocker: the round-1 docs contradiction is only partially resolved — step 6's own prose dropped the journal-field forward reference, but the included `reuse-survey-implementer.md:41` still says "Record the result in the journal's `Reuse survey` field on handoff," which renders into `.claude/agents/speccy-work.md:195` and still contradicts the six-field handoff template at line 349.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T08:15:07Z" round="2">
Round-1 blocker resolved cleanly. The forward reference to the `Reuse survey` journal field has been removed from step 6 of `resources/modules/phases/speccy-work.md` (line 147 now reads "Let the survey inform what you write in the next step." with no mention of the journal field). Step 9's six-field handoff template at line 235-236 is no longer contradicted by step 6. The `Reuse survey` field and the six→seven template update remain correctly deferred to T-003/REQ-002. No new documentation drift introduced. (Consolidator note: this verdict checked only the phase source file; it did not account for the included module `reuse-survey-implementer.md` which still renders the forward reference into the ejected step 6 — see the round-2 business blocker, which is authoritative for this round.)
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:15:07Z" round="2">
SPEC-0054 DEC-001's "fork only the reuse item into two lifecycle-specific modules" is implemented as designed and the round-2 blocker fixes introduced no architectural drift. The shared "Reuse over reinvent" bullet is removed atomically from `convention-checklist.md` (the other four items unchanged) and forked into `reuse-survey-implementer.md` (producer survey-and-build) and `reuse-hunt-reviewer.md` (reviewer adversarial verify-and-hunt) — two distinct concrete consumers with genuinely diverging framings, so justified divergence, not premature abstraction. Both forks follow the established `resources/modules/references/` source-of-truth layering and the same `{% include %}` form as the existing checklist callsites; no new render mechanism, cross-layer call, or module cycle. `just reeject` is idempotent and Claude + Codex packs carry resolved bodies with identical step renumbering. DEC-002 (no new `reviewer-reuse` persona) respected. Round-2 style fix correctly makes `## Reuse hunt` an H2 peer; round-2 docs fix removes the step-6 inline forward reference. No cross-spec invariant violated, no dead-end shape — the fork cleanly enables T-003.
</review>

<blockers date="2026-05-31T08:15:07Z" round="2">
One blocking finding remains; it is a prose fix under `resources/` followed by `just reeject`. The round-1 style blocker is fully resolved and is NOT to be reopened.
- docs/business (relocated, still shipping): the round-1 docs contradiction was moved, not removed. The round-2 pass dropped the `Reuse survey` forward reference from step 6's *inline* phase prose, but the module included into step 6, `resources/modules/references/reuse-survey-implementer.md:41`, still ends with "Record the result in the journal's `Reuse survey` field on handoff." That module renders directly inside step 6, so the shipped phase body still carries the forward reference (`.claude/agents/speccy-work.md:195`) while step 9's handoff template (`.claude/agents/speccy-work.md:349`, source `resources/modules/phases/speccy-work.md:235`) still lists only the six fields and the `Reuse survey` field is not introduced until T-003 (REQ-002). Fix within T-002's REQ-001/REQ-003 scope: remove the trailing sentence "Record the result in the journal's `Reuse survey` field on handoff." from `resources/modules/references/reuse-survey-implementer.md` (describe performing the bounded survey, not where to record it), then `just reeject`. The `Reuse survey` journal field and the six→seven handoff-template update remain T-003's responsibility.
- Verification directive: the round-2 docs re-review passed on an incomplete check — it grepped only the phase source file (which correctly has no reference) and missed the included module body that renders into the ejected step 6. After the fix, verify against the *ejected* `.claude/agents/speccy-work.md`: no "Reuse survey" reference may remain anywhere in the step-6 region, and step 9's six-field template must be unchanged. `grep -rn "Reuse survey" resources/modules/phases/speccy-work.md resources/modules/references/reuse-survey-implementer.md .claude/agents/speccy-work.md` should surface only the step-6 heading "## Reuse survey (implementer: survey-and-build)", not any "Record the result … field on handoff" sentence.
</blockers>

<implementer date="2026-05-31T10:00:00Z" model="claude-opus-4-8[1m]/high" round="3">
- Reuse survey: round-3 retry — reuse-blocker-only re-run scope, no
  area re-survey. The sole round-2 blocker is a non-reuse (docs/scope)
  fix to a symbol round 1 already created
  (`reuse-survey-implementer.md`). No new top-level symbol introduced,
  so the per-symbol floor is a no-op this round.
- Completed: removed the trailing sentence "Record the result in the
  journal's `Reuse survey` field on handoff." from
  `resources/modules/references/reuse-survey-implementer.md` (the
  module `{% include %}`d into step 6). This was the relocated form of
  the round-1 docs contradiction — round 2 dropped the inline step-6
  clause but left the same forward reference in the included module,
  which still rendered into the ejected step 6. The body now ends on
  the per-symbol-floor paragraph; no journal-field forward reference
  remains anywhere in the step-6 region. Ran `just reeject`
  (1 overwritten: `.codex/agents/speccy-work.toml`; idempotent on
  re-run).
- Undone: nothing in T-002's scope. The `Reuse survey` journal field
  and the six→seven handoff-template update remain T-003 (REQ-002),
  deliberately untouched.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exit 0;
  `cargo +nightly fmt --all --check` exit 0 (only pre-existing unknown
  config-option warnings, no formatting diff); `cargo deny check`
  exit 0.
- Evidence: no separate evidence file (DEC-006 — the only
  deterministic gate is `just reeject` + `cargo test --workspace`
  green; substantive criteria are judgment-only on the diff).
  Roll call for CHKs under REQ-001 and REQ-003:
  - CHK-001 (bounded survey step placed after read-scenarios, before
    implement, scoped, three tiers, round-agnostic per-symbol floor):
    judgment-only → step 6 shape unchanged; only the trailing
    journal-field sentence was removed from the included module.
    Confirmed on the diff.
  - CHK-002 (`just reeject` + `cargo test --workspace` exit 0, phase
    edit renders, no pack-structure break): hygiene → both exit 0;
    second `just reeject` idempotent.
  - CHK-005 (phase includes implementer variant, `reviewer-style`
    includes reviewer variant, checklist loses reuse bullet keeping
    four items): judgment-only → unchanged; confirmed on the diff.
  - CHK-006 (both new includes resolve at render time, no pack break):
    hygiene → ejected verification:
    `grep -rn "Reuse survey" resources/modules/phases/speccy-work.md
    resources/modules/references/reuse-survey-implementer.md
    .claude/agents/speccy-work.md` surfaces only the step-6 heading
    "## Reuse survey (implementer: survey-and-build)" and no "Record
    the result … field on handoff" sentence; step 9's six-field
    template at `.claude/agents/speccy-work.md:347-348` unchanged.
- Discovered issues: none.
- Procedural compliance: edited only source under `resources/` then
  ran `just reeject`; no ejected file hand-edited. The fix is prose-only
  under `resources/modules/references/`; no Rust/parser/lint file
  touched. Verified against the ejected `.claude/agents/speccy-work.md`
  per the round-2 verification directive, not just the phase source.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:27:13Z" round="3">
Round-3 fix confirmed against the EJECTED output. The round-2 blocker — the `Reuse survey` forward reference relocated (not removed) into the included module `resources/modules/references/reuse-survey-implementer.md`, which still rendered "Record the result in the journal's `Reuse survey` field on handoff" into the ejected step 6 — is now resolved. `grep -rn "Reuse survey"` over the phase source, the module, and `.claude/agents/speccy-work.md` surfaces only the step-6 heading and no "Record the result … field on handoff" sentence anywhere. In the ejected pack, the rendered step-6 module body now ends on the per-symbol-floor paragraph with step 7 immediately following — no journal-field forward reference ships. Step 9's six-field handoff template is unchanged (`Completed`, `Undone`, `Hygiene checks`, `Evidence`, `Discovered issues`, `Procedural compliance`, no `Reuse survey`). The within-file contradiction is gone and the journal field plus the six→seven template update correctly remain T-003/REQ-002 scope. REQ-001 (bounded survey, scoped, three tiers, round semantics) and REQ-003 (fork into two lifecycle modules) both remain satisfied.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:27:13Z" round="3">
Round-3 evidence roll call is complete and honestly labeled for both covered REQs: CHK-001 + CHK-002 (REQ-001) and CHK-005 + CHK-006 (REQ-003) all appear, the two prose-presence/fork-structure CHKs correctly judgment-only and the two hygiene CHKs citing the spec-sanctioned deterministic gate. I independently reproduced both hygiene claims against the dirty tree: `just reeject` is idempotent (0 overwritten, 36 unchanged — both new includes resolve at render time with no leaked `{% include %}` markers), and `cargo test --workspace` exits 0 across every binary including the pack-structure suites. Both prior-round blockers are resolved on the diff: the H3→H2 heading fix landed and the relocated round-2 contradiction is gone (`grep -rn "Record the result"` is empty; the step-6 region carries only the heading; step 9's six-field template left to T-003). The "Reuse over reinvent" bullet is removed everywhere; the other four checklist items remain. No fabrication pattern.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:27:13Z" round="3">
No security-relevant surface in this round-3 slice. The single change is removal of one prose line ("Record the result in the journal's `Reuse survey` field on handoff.") from the shipped reference module `resources/modules/references/reuse-survey-implementer.md`, followed by `just reeject`. No auth, input-validation, injection, crypto, secret-handling, logging, or race-condition code is touched, and no `speccy-core/**` parser/lint surface appears in the branch diff. Shipped-template safety holds: the edited module and its sibling `reuse-hunt-reviewer.md` carry no Speccy-repo identifiers, absolute paths, URLs, or secret-like material, so they remain safe to distribute downstream via `speccy init`. Removing the line strictly reduces downstream-leaking surface rather than adding any. Ejected packs carry resolved include bodies with no unresolved `{% include %}` markers.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T08:27:13Z" round="3">
The round-3 edit is a single sentence removal from `resources/modules/references/reuse-survey-implementer.md` (dropping the trailing "Record the result in the journal's `Reuse survey` field on handoff." sentence). The file now ends cleanly with a proper trailing newline. Heading structure is intact (`##` at line 1, consistent with sibling `reuse-hunt-reviewer.md`); the three-tier bullet list and Round semantics section are unaffected. No dangling cross-references, no dead text, no broken bullets. The ejected `.claude/agents/speccy-work.md` correctly carries only the step-6 heading reference; no "Record the result" text survives in any ejected file. Round-1 blocker (H3→H2 heading) remains resolved and is not reopened.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:27:13Z" round="3">
No correctness defect in T-002 round-3. The trailing journal-field sentence was cleanly removed from `resources/modules/references/reuse-survey-implementer.md`; the module body now ends coherently on the per-symbol-floor paragraph with no dangling reference. Both `{% include %}` directives resolve at render time in all ejected packs with zero leaked markers, and no residual "Record the result" / journal-field forward reference survives anywhere (`grep "Record the result"` empty; `grep "Reuse survey"` surfaces only the legitimate step-6 heading). `just reeject` is idempotent across two consecutive runs (0 overwritten, 36 unchanged). Phase step numbering is internally consistent: ejected steps run 1–10 with no gaps or duplicates, step 6 (reuse survey) sits between read-scenarios (5) and implement (7), the step-3 first-attempt cross-reference reads "steps 4–10", and step 9's six-field handoff template no longer contradicts step 6.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-31T08:27:13Z" round="3">
Contradiction confirmed resolved in the rendered phase. The ejected `.claude/agents/speccy-work.md` contains "Reuse survey" only at the step-6 section heading (`## Reuse survey (implementer: survey-and-build)` from the included module), with no "Record the result … field on handoff" instruction anywhere in the step-6 region. The included module body (`resources/modules/references/reuse-survey-implementer.md`) ends after round-semantics prose with no journal-field directive. Step 9's six-field handoff template (`Completed`, `Undone`, `Hygiene checks`, `Evidence`, `Discovered issues`, `Procedural compliance`) is unchanged and does not list `Reuse survey`. The field remains correctly deferred to T-003. (Round-2 docs note: this round I verified against the ejected output and the included module body, not just the phase source.)
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T08:27:13Z" round="3">
SPEC-0054 DEC-001's "fork only the reuse item into two lifecycle-specific modules" still holds after the round-3 fix, and the change introduced no architectural drift. The round-3 edit was confined to removing the single trailing journal-field forward-reference sentence from `resources/modules/references/reuse-survey-implementer.md`; the module now ends on the per-symbol-floor paragraph. No new module, include, render mechanism, or cross-layer call was added. Atomic bullet removal intact (`grep "Reuse over reinvent"` empty across packs and resources; the other four checklist items unchanged); fork layering intact (both modules `{% include %}`d via the established `resources/modules/references/` pattern, no leaked markers); DEC-002 respected (reuse lens stays inside `reviewer-style`, no new persona); no T-003/REQ-002 scope pulled forward (only the step-6 heading occurrence of "Reuse survey"; step 9 still reads "six-field"). The fork remains a justified divergence that cleanly enables T-003.
</review>
