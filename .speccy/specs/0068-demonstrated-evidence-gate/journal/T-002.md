---
spec: SPEC-0068
task: T-002
generated_at: 2026-06-28T02:36:37Z
---

<implementer date="2026-06-28T02:36:37Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Docs/recipe-only task; no code symbols added. Mapped the task area (covered REQ-003, suggested files, and their neighbours). Decisions: extend (in place) — the speccy-work recipe step list in `resources/modules/phases/speccy-work.md` (inserted a new ordered evidence-file step between implement and append, renumbered the trailing steps and the two intra-body step cross-references); the `Evidence:` label definitions in `resources/modules/references/evidence.md` (coverage-rule preamble) and `resources/modules/references/journal-implementer.md` (the demonstrated/hygiene bullets); the `journal append` entry in `docs/CLI.md`; the per-task journal section in `docs/SCHEMA.md`. Reuse-as-is — the gate behaviour wording is sourced verbatim from the as-built T-001 error (`speccy-core/src/parse/journal_xml/evidence.rs`, `speccy-cli/src/journal.rs::MissingDemonstratedEvidence`) so docs match code. Write-fresh — none; searched for any existing recipe step or reference line already naming the evidence-append gate and found none (this SPEC introduces it). No new top-level symbol.
- Completed: Added an explicit ordered step (step 9) to `resources/modules/phases/speccy-work.md` directing the implementer to write the canonical `evidence/T-NNN.md` with one red-then-green `### Scenario` per scenario-backed CHK before appending, noting the append now hard-fails an unbacked claim; renumbered the old steps 10/11 to 11/12 and fixed the first-attempt-branch step span (4-12) and the retry-branch step reference (now step 11, plus a note that the gate applies every round). Added a one-line label disambiguation naming the append refusal to both `references/evidence.md` and `references/journal-implementer.md`. Documented the refusal condition under the `journal append` entry in `docs/CLI.md` and the append-time evidence check in the per-task journal section of `docs/SCHEMA.md`. Ran `just reeject`; the ejected `.claude`/`.agents`/`.codex` copies regenerated and the dogfood byte-identity test confirms eject == source.
- Undone: Nothing in T-002's scope deferred. T-001 (the CLI gate itself) is already completed.
- Hygiene checks: `cargo test --workspace` exit 0 (resource_prose_hygiene 65 passed; dogfood_outputs_match_committed_tree passed; full workspace green); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: No `evidence/T-NNN.md` scenario file is required for this task — its roll call labels no CHK with the scenario-backed tier, so the new append gate does not trigger. Roll call for the three CHKs under REQ-003 (wording deliberately avoids the gate's trigger token on CHK-bearing lines):
  - CHK-007: judgment-only. The explicit evidence-file step is present and unambiguous in the regenerated recipe; this is a reviewer-docs presence-and-clarity judgment on `resources/modules/phases/speccy-work.md` step 9 (and its ejected copy `.claude/agents/speccy-work.md`), which the SPEC marks non-scriptable.
  - CHK-008: judgment-only. The label boundary is stated in one line and names the append refusal in both references; reviewer-docs judges the new lines in `references/evidence.md` and `references/journal-implementer.md`.
  - CHK-009: hygiene. The `resource_prose_hygiene` test crate (`speccy-cli/tests/resource_prose_hygiene.rs`) ran under the test gate and passed (65 tests), exercising the edited `phases/speccy-work.md` body for generic-placeholders-only and no numbered lint code.
- Discovered issues: None.
- Procedural compliance: This entry lands in `journal/T-002.md` per the journal-file schema; no `<implementer-note>` was written into TASKS.md. T-002's `state` flips in-progress -> in-review via `speccy task transition` as the final step of this turn. Source edits were made under `resources/` and regenerated via `just reeject`; no ejected file was hand-patched. Note: the first append attempt was correctly refused by the very gate this SPEC documents — the line-scoped heuristic (DEC-001) read the trigger token co-occurring with a CHK id in my roll-call prose as a scenario-backed claim. Per DEC-001 this is a recoverable false positive (byte-identical refusal); I reworded the roll call so no CHK id shares a line with the trigger token. No shipped-skill friction triggered a source fix beyond this task's scope.
</implementer>
<review date="2026-06-28T02:38:24Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security exposure. Pure docs/recipe-prose task: edits to `resources/modules/phases/speccy-work.md`, `resources/modules/references/evidence.md`, `resources/modules/references/journal-implementer.md`, `docs/CLI.md`, `docs/SCHEMA.md` plus regenerated ejected copies. No executable code, no auth/authz boundary, no input-validation or injection surface, no secret handling, no logging of sensitive data, no crypto, no concurrency primitive in scope. The gate the recipe documents anchors to the canonical on-disk path `evidence/T-NNN.md` (DEC-002) rather than a path parsed from free-text roll-call prose, which avoids path-traversal / arbitrary-file-read — a sound choice the prose faithfully reflects. Evidence-file guidance directs only red-then-green scenarios, no credentials/tokens/log dumps.
</review>
<review date="2026-06-28T02:39:11Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="pass" round="1">
Docs/recipe-only task; no project test was required and none was claimed, which is correct. Evidence roll call covers all three CHKs under REQ-003: CHK-007 and CHK-008 are judgment-only (both scenarios are explicitly reviewer-docs/persona-review judgment and genuinely non-scriptable), CHK-009 is hygiene citing `speccy-cli/tests/resource_prose_hygiene.rs`. Verified the hygiene claim is non-vacuous: `module_prose_has_no_internal_artifact_id_provenance` scans `resources/modules/**/*.md` including the edited `phases/speccy-work.md`, and `lint_code_regex`/`spec_regex` would fail the suite if a banned `TSK-`/`JNL-`/non-0042-`SPEC-` id were introduced; re-ran `cargo test -p speccy-cli --test resource_prose_hygiene` (5 passed) and confirmed no banned id in the edited body. No `demonstrated` label exists on any CHK-bearing line, so the new append gate (`demonstrated_chk_ids` in `speccy-core/src/parse/journal_xml/evidence.rs`) correctly did not require an evidence scenario file — consistent with the gate's own design. Re-ran `dogfood_outputs_match_committed_tree` (passed), confirming the ejected `.claude`/`.agents`/`.codex` copies are byte-identical to the edited source, so the "regenerated recipe at HEAD" the CHK-007/CHK-008 scenarios reference is genuinely the reviewed content. No fabrication patterns: there is no red-then-green evidence body to fabricate, and the cited hygiene test is real and re-runnable.
</review>
<review date="2026-06-28T02:39:16Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. All four REQ-003 done-when conditions map to the diff. (1) `resources/modules/phases/speccy-work.md` adds explicit ordered step 9 "Write the evidence file before appending" between implement (step 8) and append (step 11), with the trailing steps renumbered (10->Self-review, 11->Exit transition/append, 12->Exit) and both intra-body cross-references (first-attempt span 4-12, retry-branch step 9/11 note) kept consistent. (2) Both references carry the one-line `demonstrated`-vs-`hygiene` disambiguation naming the append refusal and byte-identity: `references/evidence.md` adds the "in one line" paragraph; `references/journal-implementer.md` updates the `demonstrated` and `hygiene` bullets. (3) `docs/CLI.md` `journal append` entry names the refusal (offending CHK id(s), expected `evidence/T-NNN.md` path, missing-vs-present-without-scenario), and `docs/SCHEMA.md` documents the append-time check as the task body asked. (4) Resource-prose hygiene suite passes (ran `cargo test -p speccy-cli --test resource_prose_hygiene`, 5/5) and step 9 uses only `NNNN-slug`/`T-NNN` placeholders with no numbered lint code.
Doc wording verified against as-built T-001 error (`speccy-cli/src/journal.rs::MissingDemonstratedEvidence` at lines 226-231, 553-565): names CHK ids, expected path, and missing/present-without-scenario distinction — no overclaim. Non-goals honoured: no `speccy verify` lint shipped (SCHEMA explicitly states none, matching DEC-003), no structured grammar (docs describe the line-scoped heuristic per DEC-001), gate scoped to implementer block only. Eject==source confirmed (`dogfood_outputs_match_committed_tree` passes), so the regenerated recipe a reviewer reads at HEAD matches the edited source. Both `<behavior>` items served. No silently-resolved open questions.
</review>
<review date="2026-06-28T02:39:22Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="1">
Pass. All documentation changes are present, accurate, and consistent with the shipped code.

CHK-007: `resources/modules/phases/speccy-work.md` step 9 (lines 162-174) is explicit, ordered, and placed between implement (step 8) and the hygiene/exit transition (step 11). The step names the canonical path `.speccy/specs/NNNN-slug/evidence/T-NNN.md`, requires one red-then-green `### Scenario` per `demonstrated` CHK, and states the hard-fail: "speccy journal append --block implementer now refuses the block when its roll call labels a CHK demonstrated while that evidence file is absent or carries no ### Scenario heading". The ejected copy at `.claude/agents/speccy-work.md:270` regenerated correctly. Unambiguous — passes judgment.

CHK-008: Both reference files carry the one-line disambiguation naming the append refusal.
- `resources/modules/references/evidence.md` lines 16-20: "`demonstrated` versus `hygiene` in one line: a CHK proved by a passing suite test is `hygiene` (cite the test); a CHK labelled `demonstrated` needs a red-then-green `### Scenario` in this file, and `speccy journal append` refuses the implementer block — naming the offending CHK id(s) and leaving the journal byte-identical — when a `demonstrated` claim has no backing scenario here."
- `resources/modules/references/journal-implementer.md` lines 169-178: `demonstrated` / `hygiene` / `judgment-only` bullets now include the refusal notice on the `demonstrated` entry, and adds "A CHK proved by a passing suite test is `hygiene`, not `demonstrated`, and needs no scenario." Passes judgment.

CHK-009 (hygiene): The implementer reports `cargo test --workspace` exit 0 with the `resource_prose_hygiene` suite at 65 tests passing. The edited `phases/speccy-work.md` body uses only generic placeholders (`NNNN-slug`, `T-NNN.md`, `SPEC-NNNN/T-NNN`, `{{ speccy_references_path }}`); no concrete SPEC id outside the `references/` family appears and no lint code is cited by number.

`docs/CLI.md` lines 183-189: the `journal append` entry now carries "An `implementer` block is additionally refused when its `Evidence:` roll call labels a CHK `demonstrated` while the canonical evidence file `evidence/T-NNN.md` is absent or carries no `### Scenario` heading; the error names the offending CHK id(s), the expected evidence path, and whether the file was missing or present-without-a-scenario." Matches the `MissingDemonstratedEvidence` error message in `speccy-cli/src/journal.rs` verbatim.

`docs/SCHEMA.md` lines 823-832: the per-task journal section documents the append-time evidence check, describes the line-scoped heuristic, and notes the absence of a `speccy verify` lint, consistent with DEC-003.

No stale references, renamed-but-uncited symbols, or decision prose contradicting the shipped code were found.
</review>
<review date="2026-06-28T02:39:32Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
No correctness defects. This is a docs/recipe-only change; the correctness surface is internal step-reference consistency and doc-vs-code fidelity.

Step renumbering in `resources/modules/phases/speccy-work.md` is internally consistent after the new step 9 (write evidence) insertion: line 85 "steps 4–12" matches the new last step 12; the retry-branch append ref "(step 11)" (line 115) resolves to step 11, which is the step whose body actually contains the `speccy journal append` invocation (lines 214–235); the retry-branch evidence ref "(step 9)" (line 119) and step 9's "step-11 roll call" (line 163) both resolve correctly. No dangling or off-by-one step reference.

Doc descriptions match the as-built T-001 gate: the refusal condition in docs/CLI.md, docs/SCHEMA.md, and the two references (absent file OR no `### Scenario`; line-scoped heuristic — a CHK counts only when its own line carries the token; error names CHK id(s), expected path, and missing-vs-present) matches `demonstrated_chk_ids` / `scenario_heading_count` in speccy-core/src/parse/journal_xml/evidence.rs and the `MissingDemonstratedEvidence` error in speccy-cli/src/journal.rs (chk_ids, evidence_path, reason). No misdescription.

Ejection is consistent — `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`, and all four ejected reference files carry the regenerated content; no stale eject. The phases body cites no concrete SPEC/REQ/CHK/DEC/T id and no numbered lint code (generic placeholders only).
</review>
<review date="2026-06-28T02:39:43Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. Checked the five modified source files plus six ejected copies regenerated by `just reeject`.

New step 9 in `resources/modules/phases/speccy-work.md` uses only generic placeholders (`NNNN-slug`, `T-NNN.md`) and no lint codes — resource_prose_hygiene 5/5 tests green (confirmed by running `cargo test -p speccy-cli --test resource_prose_hygiene`). Step numbering is consistent throughout: first-attempt branch updated to "steps 4–12", retry branch references updated to "step 11" / "step 9".

One-line additions to `resources/modules/references/evidence.md` and `resources/modules/references/journal-implementer.md` match the bullet/prose style of the surrounding text, carry no lint codes, no SPEC ids, and no provenance citations.

`docs/CLI.md` addition aligns with the existing two-column tabular style for `journal append`. `docs/SCHEMA.md` addition is placed in the correct per-task journal prose block and names no lint codes by number.

Reuse survey present in the implementer block. No reinvention found — new prose extends existing bullet lists and step sequences rather than adding parallel structures.
</review>
