---
spec: SPEC-0068
task: T-001
generated_at: 2026-06-28T02:27:17Z
---

<implementer date="2026-06-28T02:27:17Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-001/REQ-002, the suggested
  files, and the journal-family parsers). Decisions: reuse-as-is — followed the
  existing `OnceLock<Regex>` lazy-compile pattern from
  `parse/journal_common.rs` for the `chk_id_regex` helper, and reused the
  established `JournalError` thiserror enum and the byte-identical-on-failure
  round-trip ordering in `append_under_lock`. Extend — added the
  `MissingDemonstratedEvidence` variant to the existing `JournalError` enum and
  threaded a `spec_dir` field into the existing `AppendInputs` struct rather
  than introducing a parallel carrier. Write-fresh — the new pure module
  `parse/journal_xml/evidence.rs` (two functions `demonstrated_chk_ids` and
  `scenario_heading_count`); searched `speccy-core/src/parse` for an existing
  CHK-roll-call or scenario-heading scanner and found none. New top-level
  symbols: `evidence` module, `demonstrated_chk_ids`, `scenario_heading_count`,
  `chk_id_regex` (private), and the `MissingDemonstratedEvidence` enum variant.
- Completed: Added the demonstrated-evidence append-time gate. New pure module
  `evidence.rs` exposes `demonstrated_chk_ids` (line-scoped, dedup+sorted) and
  `scenario_heading_count`, declared `pub mod evidence` and re-exported both
  from `parse/mod.rs`. Added the `MissingDemonstratedEvidence` JournalError
  variant carrying the offending CHK ids, the expected `evidence/T-NNN.md`
  path, and a missing-vs-present-without-scenario reason; its message ends
  "journal left unchanged". Threaded `spec_dir` from `location.spec_dir` into
  `AppendInputs`. In `append_under_lock`, for the implementer kind only, after
  `validate_and_render_block` and before the round-trip parse and write, the
  gate runs `demonstrated_chk_ids` and requires `spec_dir/evidence/{task_id}.md`
  to exist with at least one `### Scenario` heading when the result is
  non-empty. 8 core unit tests and 6 integration tests added.
- Undone: Nothing in T-001's scope is deferred. The `speccy-work` recipe
  evidence-file step and the references' demonstrated-vs-hygiene disambiguation
  (REQ-003) belong to sibling task T-002 and are out of this task's scope.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace
  --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all
  --check exit 0; cargo deny check exit 0. Note: the in-tree-spec lint
  initially failed on three `</scenario></requirement>` blank-line omissions in
  the SPEC-0068 SPEC.md authored during decompose (confirmed pre-existing via
  git stash); fixed with whitespace-only edits (no Requirement content changed,
  no spec-hash drift reported by `speccy next`).
- Evidence: CHK roll call for T-001's covered REQs. Backing paper trail in
  evidence/T-001.md.
  - CHK-001 (bullet-form unbacked claim refused): demonstrated -> evidence
    Scenario 1; integration test bullet_demonstrated_with_no_evidence_is_refused.
  - CHK-002 (prose-form refused identically): demonstrated -> evidence
    Scenario 2; integration prose_demonstrated_with_no_evidence_is_refused plus
    unit prose_form_roll_call_is_detected.
  - CHK-003 (present-but-no-scenario distinct message): demonstrated -> evidence
    Scenario 3; integration evidence_present_without_scenario_is_refused.
  - CHK-004 (claim backed by scenario succeeds): demonstrated -> evidence
    Scenario 4; integration demonstrated_backed_by_scenario_succeeds.
  - CHK-005 (non-demonstrated roll call succeeds): demonstrated -> evidence
    Scenario 5; integration no_demonstrated_label_succeeds_without_evidence.
  - CHK-006 (incidental token does not over-trigger): demonstrated -> evidence
    Scenario 6; integration
    demonstrated_token_on_chk_less_line_succeeds_without_evidence plus unit
    token_alone_on_chk_less_line_is_not_a_claim.
- Discovered issues: The SPEC-0068 SPEC.md carried three blank-line lint
  violations between `</scenario>` and `</requirement>` from the decompose
  phase; fixed in place (whitespace only).
- Procedural compliance: Followed the speccy-work recipe. No shipped skill or
  resource module needed editing. The first append of my own implementer block
  exercised the new gate end-to-end — it required evidence/T-001.md with a
  `### Scenario` heading to be present first, which it is.
</implementer>
<review date="2026-06-28T02:28:53Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security weakness in this slice. The gate is a local CLI check over a workspace-relative journal/evidence file with no auth boundary, network, secret, crypto, or untrusted-deserialization surface. `task_id` and `spec_dir` are sourced from the resolved `location` (the same canonical source the existing journal path uses), not from raw selector text, so the new `spec_dir/evidence/{task_id}.md` join introduces no path-traversal surface beyond what already shipped. The `CHK-[0-9]+` regex is a fixed literal with no ReDoS risk. The gate is fail-closed: in `append_under_lock` (`speccy-cli/src/journal.rs:543`) a non-`NotFound` read error returns `JournalError::Io` early rather than silently treating the evidence as unbacked or silently passing — only `NotFound` is the missing path, and the write is gated behind a satisfied scenario count. The `MissingDemonstratedEvidence` message discloses only the author's own CHK ids, the workspace-relative path, and a static reason string — no sensitive data leak.
</review>
<review date="2026-06-28T02:29:18Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
Pass. The diff satisfies every REQ-001/REQ-002 done-when item and respects all five non-goals.

REQ-001: `append_under_lock` gates the implementer block kind only, runs `demonstrated_chk_ids` before the round-trip/write (preserving byte-identical-on-failure), and returns `MissingDemonstratedEvidence` with distinct `reason` strings for the missing-file vs present-without-`### Scenario` branches. Bullet and prose forms both refused (`bullet_demonstrated_with_no_evidence_is_refused`, `prose_demonstrated_with_no_evidence_is_refused`), error names the CHK id and path (stderr asserts `CHK-001`/`CHK-002` + `T-001.md`), no journal created on refusal, and the present-but-no-scenario branch is distinctly messaged (`evidence_present_without_scenario_is_refused`).

REQ-002: backed-by-scenario succeeds, hygiene/judgment-only-only succeeds without evidence, and the incidental CHK-less-line token does not over-trigger — all three accept-paths covered by integration tests plus line-scoped unit tests.

Non-goals all honored: no `speccy verify` lint family, no roll-call grammar (line-scoped heuristic), no red-then-green internal validation (`### Scenario` heading count only), gate scoped to `TaskBlockKind::Implementer` so review/blockers/VET untouched. REQ-003 (recipe/docs) is correctly left to sibling T-002 per the task body — no scope sneak-in. The implementer's whitespace-only SPEC.md lint fix touches no requirement content. 15 integration + 8 core unit tests pass.
</review>
<review date="2026-06-28T02:29:51Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
Pass. Verified the demonstrated-evidence gate logic in
`speccy-cli/src/journal.rs:547-572` and the pure detectors in
`speccy-core/src/parse/journal_xml/evidence.rs`.

Control flow: the `match` over `fs_err::read_to_string` is exhaustive and
correct — present-with-scenario passes (None), present-without-scenario and
NotFound map to refusal reasons, and a non-NotFound IO error is propagated as
`JournalError::Io` rather than swallowed. No inverted condition or fall-through.

Ordering / byte-identical contract: the gate runs after `validate_and_render_block`
and strictly before the round-trip `parse_journal_xml` and the single
`fs_err::write` (journal.rs:591-601), so a refusal performs no write — journal
stays absent or byte-identical, satisfying REQ-001's no-write-on-failure clause.

Boundary cases: `scenario_heading_count` (evidence.rs:54) anchors on the
space-terminated prefix `### Scenario`, correctly excluding a 4-hash
`#### Scenario` line. `demonstrated_chk_ids` (evidence.rs:33) is line-scoped
(filter on lines containing the token, then extract CHK ids per line), and its
sort-then-dedup is correct since sort makes equal ids adjacent before dedup —
the CHK-less `demonstrated` line and the `demonstrated`-less CHK line both
contribute nothing. The `demonstrated` substring match could over-trigger on a
superstring word, but that is within DEC-001's documented bias toward catching
omissions and is recoverable (byte-identical refusal), not a defect.

`spec_dir` is threaded from `location.spec_dir` into `AppendInputs` and the
evidence path resolves to the canonical `spec_dir/evidence/{task_id}.md`.

Confirmed by build/test run: 8 core unit tests and 6 integration tests pass
under the SPEC-0042 worked-instance fixture.
</review>
<review date="2026-06-28T02:30:05Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No style violations found.

Checked: naming conventions (snake_case, verb-first), `OnceLock<Regex>` pattern
matches `journal_common.rs` exactly; `#[expect(clippy::unwrap_used, reason =
"...")]` suppression is consistent with sibling files. No dead imports or
variables. No provenance citations (no SPEC/REQ/DEC ids, no doc-path pointers,
no planning artifact references in production code or comments). `evidence_path`
helper naming is consistent with `journal_path` in the same file. `reason:
&'static str` in the error variant is idiomatic for the string-literal arms
used at the call site. `fs_err::read_to_string(evidence_path.as_std_path())`
matches the existing pattern at line 500 of `journal.rs`.

Reuse survey verified: `OnceLock<Regex>` pattern reused from
`journal_common.rs` (confirmed present at lines 16-33); `JournalError` enum
extended with the new variant (confirmed); `AppendInputs` struct extended with
`spec_dir` field (confirmed). Both new pub symbols (`demonstrated_chk_ids`,
`scenario_heading_count`) exist in the untracked `evidence.rs` and are called
from `journal.rs` via the re-export chain in `parse/mod.rs`. No reinvention
found.

Unit tests in `evidence.rs` gate real invariants: bullet form, prose form,
hygiene/judgment-only, mixed multiline, same-line guard, dedup+sort, scenario
count. None are vacuous. Integration tests in `journal_append.rs` follow the
byte-identical pattern established by the existing suite.
</review>
<review date="2026-06-28T02:30:08Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
All six covered scenarios (CHK-001..006) drive real code paths via the built `speccy` binary against scratch workspaces, not mocks; each would catch an obviously-wrong implementation.
CHK-001/002 (`bullet_demonstrated_with_no_evidence_is_refused`, `prose_demonstrated_with_no_evidence_is_refused`) assert `.failure()` plus `stderr` contains both the CHK id and `T-001.md`, and the journal is never created — a skipped gate exits zero and fails these. CHK-003 (`evidence_present_without_scenario_is_refused`) asserts the *specific* distinguishing phrase `present but carries no `### Scenario`` rather than catching any error, so a collapsed missing-vs-present message fails it. CHK-004 (`demonstrated_backed_by_scenario_succeeds`) writes a `### Scenario` heading first and asserts success plus exactly one parsed implementer block, catching an over-firing gate. CHK-005 (`no_demonstrated_label_succeeds_without_evidence`) asserts no evidence file exists then expects success, catching a gate that fires on hygiene/judgment-only. CHK-006 (`demonstrated_token_on_chk_less_line_succeeds_without_evidence`) puts `demonstrated` on a CHK-less line with a hygiene CHK line and expects success, directly falsifying a naive whole-body substring check — backed by core unit `token_alone_on_chk_less_line_is_not_a_claim`.
Negative paths carry explicit, specific assertions; the present-but-no-scenario case is distinguished by exact phrase. Core unit tests cover bullet, prose, mixed-multiline, dedup-sort, same-line guard, and `scenario_heading_count` (including the `#### Scenario` non-match and `### not a scenario` non-match). Ran `cargo test -p speccy-core parse::journal_xml::evidence` (8 passed) and `cargo test -p speccy-cli --test journal_append` (15 passed, 9 prior + 6 new, no regression).
Evidence roll call at `.speccy/specs/0068-demonstrated-evidence-gate/evidence/T-001.md` is complete: all six covered CHKs labelled `demonstrated`, each naming a test present in the diff. Green snippets match real cargo single-test runner output shape; red/green halves are materially different (not byte-identical); evidence commands are scoped per-test, not the workspace hygiene run. No fabrication pattern matched.
</review>
