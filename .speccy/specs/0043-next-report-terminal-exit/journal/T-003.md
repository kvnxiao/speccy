---
spec: SPEC-0043
task: T-003
generated_at: 2026-05-23T00:00:00Z
---

<implementer date="2026-05-23T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Swept canonical templates under `resources/modules/` so every
  SPEC-scoped `speccy next --json` callsite now uses
  `speccy next SPEC-NNNN --json`. Added the exit-code-stop contract one-liner
  to `speccy-orchestrate.md`, `speccy-review.md`, `vet-phases.md`, and
  `speccy-work.md`. Rewrote the readiness-semantics block in
  `speccy-ship.md`: `next_action.kind == "ship"` is the ship-readiness
  signal; `next_action: null` paired with non-zero exit is the
  terminal-already-shipped signal (REQ-005 doc-drift fix). Workspace-form
  `speccy next --json` calls were preserved only in the no-selector
  invocation paths of `speccy-work.md` and `speccy-review.md`, each with
  an inline comment naming the reason.
- Undone: T-004 (mirror canonical edits into the three shipped skill packs)
  remains pending.
- Hygiene checks: no Rust / source code changed in this task — only prose
  under `resources/modules/`. `cargo test`, `clippy`, `fmt`, `deny` were
  not run for this task and are intentionally deferred to T-004 once the
  shipped host packs are re-mirrored.
- Evidence: paper trail is the canonical templates at HEAD plus the
  task-scenarios grep contract.
  Roll call for CHKs under REQ-001 / REQ-004 / REQ-005:
  - CHK-001 (every `speccy next --json` in SPEC-scoped sections carries
    SPEC-NNNN): demonstrated → `grep -n 'speccy next --json'` over the
    five files now returns only four matches, all in no-selector
    workspace-form callsites with inline-comment reasons
    (`speccy-review.md:7,55`, `speccy-work.md:7,50`).
  - CHK-002 (orchestrator's first `speccy next` is per-spec): demonstrated
    → `speccy-orchestrate.md` outer dispatch loop step 1 now runs
    `speccy next SPEC-NNNN --json`; the startup integrity check
    immediately above also resolves the spec dir via the per-spec form.
  - CHK-010 (orchestrator states non-zero exit is the loop-stop signal):
    demonstrated → new "**Exit-code-stop contract.**" paragraph at lines
    ~98 of `speccy-orchestrate.md` plus the in-loop restatement at
    step 1, both naming "halt the loop and surface the stderr line".
  - CHK-011 (ship.md states `kind == "ship"` is ship-ready and
    `next_action: null` + non-zero exit is terminal): demonstrated →
    new readiness-semantics bulleted block under "When to use" in
    `speccy-ship.md`, mirrored in step 1.
- Discovered issues: none.
- Procedural compliance: followed the speccy-work skill; flipped task
  state pending → in-progress → in-review; appended this `<implementer>`
  block to the per-task journal. Did not touch shipped skill-pack mirrors
  (`.claude/`, `.codex/`, `.agents/`) — T-004 owns that propagation and
  the corresponding `cargo test -p speccy-cli skill_packs` gate.
</implementer>

<review date="2026-05-23T22:55:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-003 delivers the canonical-template sweep promised by REQ-001, REQ-004, and REQ-005. REQ-001: every SPEC-scoped `speccy next` callsite under `resources/modules/skills/{speccy-orchestrate,speccy-review}.md`, `resources/modules/skills/partials/vet-phases.md`, and `resources/modules/phases/{speccy-work,speccy-ship}.md` now carries the `SPEC-NNNN` positional. The only unfiltered `speccy next --json` matches are in the no-selector workspace-form sections of `speccy-review.md` (lines 7, 55) and `speccy-work.md` (lines 7, 50), each with an inline comment naming the reason — exactly the `<done-when>` carve-out REQ-001 specifies. CHK-001 and CHK-002 hold. REQ-004: `speccy-orchestrate.md` lines 98-101 carry the explicit "Exit-code-stop contract" paragraph, restated co-located with the in-loop `speccy next` invocation at lines 152-155. The exit-code-stop one-liner also lives in `speccy-review.md:65-69`, `speccy-work.md:59-63`, and `vet-phases.md:22-25`. CHK-010 holds. REQ-005: `speccy-ship.md` no longer carries the "all tasks completed, no REPORT.md yet" claim against `next_action: null`; lines 17-30 spell out the corrected readiness semantics (`next_action.kind == "ship"` with exit 0 is the ship-readiness signal; `next_action: null` paired with non-zero exit is terminal). CHK-011 holds. Non-goals respected, T-004 mirror correctly named out-of-scope.
</review>

<review date="2026-05-23T22:55:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="blocking" round="1">
T-003's substantive prose edits all match the task scenarios — grep contracts under CHK-001, CHK-002, CHK-010, and CHK-011 all read true at HEAD. The blocker is the evidence shape, not the diff content.

The `<implementer>` block says "Evidence: paper trail is the canonical templates at HEAD plus the task-scenarios grep contract." This is a description, not a path. The canonical evidence schema (`resources/modules/references/evidence.md`) and the implementer-shape reference both require `paper trail at <path>` — pointing to a `.speccy/specs/NNNN-slug/evidence/T-NNN.md` file with `### Scenario N` (or `## CHK-NNN`) blocks. No such file exists at `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`. Per the evidence-loading step in this reviewer's agent file: "Treat the absence of the `Evidence:` field, or the absence of the file at the referenced path, as a `verdict=\"blocking\"` review."

All four CHKs are labeled `demonstrated`, which per the canonical schema must be backed by exactly one scenario block in the evidence file. Precedent is the directly analogous SPEC-0042/T-008 doc-only task: it created `.speccy/specs/0042-archive-completed-specs/evidence/T-008.md` with one `## CHK-NNN` block per demonstrated CHK plus a hygiene roll-up, even though its CHKs were also pure grep contracts.

Two narrowly-scoped fixes resolve the block; round 2 can pick either:

1. Create `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md` with one section per demonstrated CHK (CHK-001 / CHK-002 / CHK-010 / CHK-011), each carrying the exact grep / read command and its observed output at HEAD. The journal Evidence field then becomes `paper trail at .speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`. Matches T-008 precedent.

2. Relabel the four CHKs from `demonstrated` to `judgment-only` in the journal roll call, on the basis that "prose appears in file" is a reviewer-readable contract rather than a red-then-green execution artifact. The journal Evidence field then drops the `paper trail at <path>` line entirely.
</review>

<review date="2026-05-23T22:55:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
All five changed files are static prose/prompt templates under `resources/modules/` with no Rust source changes, no authentication or authorization logic, no credential handling, no secret storage, and no injection vectors. Shell command examples reference only the `speccy` binary and standard tools (`git`, `grep`, `gh`, `sha256sum`) with no user-input interpolation. No security-relevant surface is touched by this task.
</review>

<review date="2026-05-23T22:55:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All five new `resources/modules/` files pass style review. Naming, structure, and conventions are consistent with the pre-existing module files (leading blank line before the `# heading` matches the established pattern; trailing newline confirmed on all five). Every SPEC-scoped `speccy next` callsite carries `SPEC-NNNN --json`; the two workspace-form callsites in `speccy-review.md:55` and `speccy-work.md:50` carry inline comments naming the reason. The exit-code-stop contract one-liners are present in all four required files. The `T-003` occurrences in `speccy-work.md` are illustrative example values, not literal task references — consistent with the existing template convention. No unused imports, dead code, suppression annotations, or duplicated helpers introduced.
</review>

<blockers date="2026-05-23T22:55:00Z" round="1">
Land a canonical evidence paper trail backing the four `demonstrated` CHKs (CHK-001 / CHK-002 / CHK-010 / CHK-011). Pick one:
- Preferred: create `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md` with one section per demonstrated CHK (each carrying the grep/read command and its observed output at HEAD) per the SPEC-0042/T-008 precedent, and update the journal's Evidence field to `paper trail at .speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`.
- Alternative: relabel the four CHKs from `demonstrated` to `judgment-only` in the journal roll call and drop the `paper trail at <path>` Evidence line, on the basis that prose-content scenarios are reviewer-readable judgment rather than a red-then-green execution artifact.
- tests: Evidence field is descriptive prose, not a path; no evidence file exists; canonical schema requires `paper trail at <path>` backing every `demonstrated` CHK.
</blockers>

<implementer date="2026-05-23T00:00:00Z" model="claude-opus-4-7[1m]/low" round="2">
- Completed: Created `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`
  per the SPEC-0042/T-008 precedent — one section per demonstrated CHK
  (CHK-001 / CHK-002 / CHK-010 / CHK-011), each carrying the exact
  grep command and its observed output at HEAD, plus a hygiene roll-up
  naming the T-004 deferral. No canonical-template prose changed in
  this round; the round-1 substantive edits all stand at HEAD and the
  tests-reviewer's grep contracts read true unchanged.
- Undone: T-004 (mirror canonical edits into the three shipped skill
  packs) remains pending.
- Hygiene checks: no Rust / source code changed. `cargo test`,
  `clippy`, `fmt`, `deny` remain deferred to T-004 once the shipped
  host packs are re-mirrored. The four grep commands captured in the
  evidence file were re-run against the working tree to produce the
  observed-output blocks; outputs match the file contents.
- Evidence: paper trail at `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`.
  Roll call for CHKs under REQ-001 / REQ-004 / REQ-005:
  - CHK-001 (every SPEC-scoped `speccy next --json` carries
    SPEC-NNNN): demonstrated → evidence section CHK-001 captures
    the five-file grep returning four matches, all in no-selector
    workspace-form callsites in `speccy-review.md` (lines 7, 55) and
    `speccy-work.md` (lines 7, 50) with inline-reason context.
  - CHK-002 (orchestrator's first `speccy next` is per-spec):
    demonstrated → evidence section CHK-002 captures the
    `speccy next SPEC` grep over `speccy-orchestrate.md` showing
    every callsite (lines 5, 95, 98, 108, 149, 176, 185, 210) uses
    the per-spec form.
  - CHK-010 (orchestrator states non-zero exit is loop-stop):
    demonstrated → evidence section CHK-010 captures the
    `exit|non-zero|loop-stop` grep showing the named "Exit-code-stop
    contract" paragraph at lines 98–101 and the in-loop restatement
    at lines 152–155.
  - CHK-011 (ship.md states `kind == "ship"` is ship-ready;
    `null` + non-zero exit is terminal): demonstrated → evidence
    section CHK-011 captures the `next_action` grep over
    `speccy-ship.md` showing the readiness-semantics block at
    lines 19–27.
- Discovered issues: none.
- Procedural compliance: followed the speccy-work skill; flipped
  T-003 state pending → in-progress → in-review; appended this
  round-2 `<implementer>` block to the per-task journal beneath the
  existing round-1 review/blocker artifacts. Did not modify the
  round-1 blocks. Resolved the tests-reviewer's blocker via the
  preferred path (evidence file at canonical `evidence/T-NNN.md`
  location matching the SPEC-0042/T-008 precedent), not the
  judgment-only relabel.
</implementer>

<review date="2026-05-23T23:15:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round-2 evidence paper trail at `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md` resolves the round-1 tests blocker without altering the substantive prose edits that round 1's business persona already approved. Re-running the four grep contracts against the working tree reproduces the evidence file's captured output exactly: CHK-001's grep over the five canonical files returns only the 4 expected unfiltered matches inside the no-selector workspace-form sections of `speccy-review.md:7,55` and `speccy-work.md:7,50`; CHK-002's grep over `speccy-orchestrate.md` confirms every `speccy next` callsite uses the per-spec form; CHK-010 confirms the named "Exit-code-stop contract" paragraph plus the in-loop restatement; CHK-011 confirms `speccy-ship.md` carries the corrected readiness semantics. REQ-001 / REQ-004 / REQ-005 done-when clauses all satisfied. Non-goals respected. The round-2 implementer picked the preferred resolution path (evidence file matching the SPEC-0042/T-008 precedent), not the judgment-only relabel, which is the right call because the four CHKs really are red-then-green-style grep contracts.
</review>

<review date="2026-05-23T23:15:00Z" model="claude-opus-4-7[1m]/low" persona="tests" verdict="pass" round="2">
Round-2 blocker resolved via the preferred path. Evidence file present at `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md` with one section per demonstrated CHK (CHK-001 / CHK-002 / CHK-010 / CHK-011). Each section carries the exact scoped grep command and its observed output at HEAD; all four contracts re-run byte-for-byte. Journal round-2 Evidence field now reads `paper trail at .speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md` — canonical shape. Hygiene roll-up correctly defers `cargo test`/`clippy`/`fmt`/`deny` to T-004 (no Rust source touched). Round-2 implementer block was appended below the round-1 blocks without modifying them. No fabrication patterns triggered.
</review>

<review date="2026-05-23T23:15:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="2">
Round 2 adds a single file: `.speccy/specs/0043-next-report-terminal-exit/evidence/T-003.md`. The file contains only markdown prose — four CHK sections with scoped `grep` commands against static template files and their observed outputs, plus a hygiene roll-up. No Rust source code, no authentication or authorization logic, no credential storage, no secret handling, no injection vectors, and no user-input interpolation are present. The security assessment from round 1 holds unchanged.
</review>

<review date="2026-05-23T23:15:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-2 evidence file conforms fully to style conventions. Heading hierarchy (`# Evidence: SPEC-NNNN / T-NNN`, `## CHK-NNN — <title>`, `## Hygiene roll-up`) is identical to the T-008 precedent. Em-dash separator (` — `) in section titles matches the precedent. Trailing newline confirmed. Journal `Evidence:` field updated to the canonical `paper trail at <path>` form. The hygiene roll-up correctly names the T-004 deferral without claiming passes for runs not executed. No naming drift, no suppression annotations, no structural deviations.
</review>
