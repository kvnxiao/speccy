---
spec: SPEC-0043
task: T-001
generated_at: 2026-05-23T21:30:00Z
---

<implementer date="2026-05-23T21:30:00Z" model="claude-opus-4-7[1m]" round="1">
- Completed: Reordered the all-tasks-completed branch in
  `speccy-core/src/next.rs::compute_for_spec` so REPORT.md presence
  beats `journal/VET.md` state. New ordering inside the branch:
  (1) `report_md_exists(spec)` → return `None` (terminal);
  (2) `vet_gate_is_fresh_pass(spec)` → `Some(NextAction::Ship)`;
  (3) else → `Some(NextAction::Vet)`. Updated both the module-level
  `//!` doc block (lines ~13–23) and the `compute_for_spec` rustdoc
  Priority enumeration (rules 4/5/6) to match the new ordering and
  cross-reference SPEC-0043 REQ-002. Added an inline comment in the
  function body explaining the priority rationale. Added three new
  unit tests in `speccy-core/tests/next_priority.rs`:
  `report_md_beats_missing_vet_md` (REPORT.md present, no VET.md →
  None), `report_md_beats_stale_vet_md` (REPORT.md present,
  stale-hash passing VET.md → None), and
  `report_md_beats_failed_vet_md` (REPORT.md present,
  failed-verdict VET.md → None). The existing
  `none_when_vet_passes_fresh_and_report_present`,
  `ship_when_vet_passes_fresh_and_no_report`,
  `vet_when_all_done_no_vet_md`,
  `vet_when_vet_md_ends_with_failed_verdict`, and
  `vet_when_passed_but_tasks_hash_is_stale` cases already cover the
  remaining combinations and continue to pass. Verified
  `speccy next SPEC-0001 --json` against the in-tree workspace now
  returns `"next_action":null` with `"reason":"completed"` (exit
  code 0 — T-002 wires the loud exit).

- Undone: T-002 (terminal-state exit code 2 + stderr message) and
  T-003 / T-004 (template sweep + shipped-pack mirror) are out of
  scope for this task. Nothing within T-001's covered REQ-002 is
  deferred.

- Hygiene checks: `cargo test --workspace` → exit 0, all tests pass
  including 13 in `next_priority` (10 prior + 3 new). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` → exit 0
  with zero new warnings. `cargo +nightly fmt --all --check` → exit
  0 (applied formatter after first iteration; final check clean).
  `cargo deny check` → exit 0 (`advisories ok, bans ok, licenses
  ok, sources ok`).

- Evidence: no separate `evidence/T-001.md` paper trail file —
  CHK-003 and CHK-004 are both covered by the test suite. Roll call
  for CHKs under REQ-002:
  - CHK-003 (`speccy next SPEC-0001 --json` returns `next_action:
    null` and `reason: completed` against the in-tree SPEC-0001):
    hygiene → verified by manual CLI invocation captured above
    (stdout: `{"schema_version":1,"spec_id":"SPEC-0001",
    "next_action":null,"reason":"completed",...}`). The behaviour
    is also covered by the broader `report_md_beats_missing_vet_md`
    unit test in `speccy-core/tests/next_priority.rs` which
    constructs the equivalent fixture state.
  - CHK-004 (`compute_for_spec` returns `Some(NextAction::Ship)`
    for a fixture with all tasks completed, REPORT.md absent, and
    a fresh-pass VET.md whose `tasks_hash` matches): hygiene →
    `ship_when_vet_passes_fresh_and_no_report` test in
    `speccy-core/tests/next_priority.rs` covers this exactly under
    `cargo test --workspace`.
  - CHK-005 (workspace-form `speccy next --json` omits the 39
    historical pre-vet shipped specs): the priority reorder
    accomplishes this — those specs all have REPORT.md present, so
    `compute_for_spec` now returns `None` for each, and
    `compute_workspace` omits `None` entries. The expected
    behaviour is hygiene-covered by the existing
    `chk005_ship_when_all_done_no_report` workspace assertion
    (which asserts REPORT.md-present specs are omitted from
    `compute_workspace`). Loud-exit verification for CHK-005 lands
    with T-002.

- Discovered issues: SPEC-0043's `SPEC.md` was authored without
  blank lines between the final `<scenario>` and `</requirement>`
  in each REQ block, violating the in-tree
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag` lint.
  Fixed in-flight (6 occurrences, mechanical `\n` insertions; no
  semantic change to requirements) and re-locked SPEC-0043's
  `spec_hash_at_generation` via `speccy lock SPEC-0043` so TASKS.md
  stays in sync. Flagged here because the fix is adjacent to T-001
  rather than required by REQ-002; it removes friction blocking
  the full hygiene gate.

- Procedural compliance: This implementer entry lands directly in
  `.speccy/specs/0043-next-report-terminal-exit/journal/T-001.md`
  per the journal-file schema. No `<implementer-note>` block was
  written into TASKS.md. The T-001 `state="..."` attribute flips
  from `in-progress` to `in-review` as the final step of this
  turn. No shipped skill bodies under `skills/`,
  `resources/modules/`, `.claude/`, `.codex/`, or `.agents/`
  required edits during this task — the friction-to-skill-update
  convention was not triggered.
</implementer>

<review date="2026-05-23T21:45:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-001 satisfies REQ-002 cleanly: priority reordered so REPORT.md presence beats VET.md state, both doc blocks (module-level `//!` enumeration and `compute_for_spec` rustdoc) updated and cross-reference SPEC-0043 REQ-002, three new unit tests cover the REPORT.md-present combinations (missing VET.md / stale-hash passing VET.md / failed-verdict VET.md → None), and the pre-existing tests cover the remaining no-REPORT.md combinations (`ship_when_vet_passes_fresh_and_no_report`, `vet_when_all_done_no_vet_md`, `vet_when_passed_but_tasks_hash_is_stale`, `vet_when_vet_md_ends_with_failed_verdict`). Manual verification confirms `speccy next SPEC-0001 --json` now returns `"next_action":null,"reason":"completed"` against the in-tree workspace — the user-story headline outcome is delivered. Deferring exit code 2 to T-002, template sweep to T-003, and shipped-pack mirror to T-004 is consistent with the SPEC's REQ→Task mapping (T-001 covers REQ-002 only). The in-flight SPEC.md blank-line lint fix + `speccy lock` re-record was properly disclosed in the journal and is within "surgical change" bounds. No non-goals violated, no open questions silently resolved, no edge cases dropped.
</review>

<review date="2026-05-23T21:45:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-001's tests faithfully exercise the priority reorder behaviour. The three new tests in `speccy-core/tests/next_priority.rs` (`report_md_beats_missing_vet_md`, `report_md_beats_stale_vet_md`, `report_md_beats_failed_vet_md`) each construct a real `ParsedSpec` via `scan(&root)` and call `compute_for_spec` against actual on-disk REPORT.md + VET.md combinations rather than mocking. Mentally reverting the priority swap (vet-check-first, the previous order) makes every one of the three new tests fail: with no VET.md or with a failed/stale VET.md, `vet_gate_is_fresh_pass` returns false and the old code would return `Some(Vet)` while the tests assert `is_none()`. Task-scenario 1 (REPORT.md present, no VET.md → None) is covered exactly by `report_md_beats_missing_vet_md`; task-scenario 2 (REPORT.md absent + fresh-pass VET.md → Some(Ship)) is covered by the pre-existing `ship_when_vet_passes_fresh_and_no_report`; task-scenario 3 (in-tree `speccy next SPEC-0001 --json` returns null + completed, exit-code semantics deferred to T-002) is verified by the manual CLI invocation cited in the evidence and the equivalent fixture state in `next_derived.rs::per_spec_json_null_when_all_done_and_report_present`. The journal's Evidence roll call covers CHK-003 / CHK-004 / CHK-005 under REQ-002, each labelled `hygiene` with a specific test name and file path as required. `cargo test -p speccy-core --test next_priority` runs 13 passing tests (10 prior + 3 new) at HEAD, matching the implementer's hygiene claim.
</review>

<review date="2026-05-23T21:45:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. The change is a pure state-machine reorder in a local CLI with no network, no auth, and no sensitive data. Path components joined in `report_md_exists` and `vet_gate_is_fresh_pass` are hardcoded filenames (`"REPORT.md"`, `"journal/VET.md"`, `"TASKS.md"`) appended to the pre-scanned `spec.dir`; no user-controlled string reaches `join`. The `tasks_hash` attribute extracted from VET.md is consumed only in a string equality comparison against a SHA-256 hex digest — it is never executed, deserialized as a path, or used as a lookup key. No new dependencies are introduced. The REPORT.md-first ordering is security-neutral or marginally better: it makes the terminal state depend on the most durable artifact rather than a pre-ship gate file, reducing any hypothetical loop-manipulation vector via crafted VET.md content.
</review>

<review date="2026-05-23T21:45:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style conventions satisfied. The `#![allow(clippy::expect_used, reason = "...")]` + `#![expect(clippy::panic_in_result_fn, reason = "...")]` pairing in `speccy-core/tests/next_priority.rs` exactly matches the boilerplate head found in every other test file in the workspace — this is the established project pattern, not a violation of the `#[allow]` prohibition (which targets production code suppressions without a reason string). The module-level `//!` doc and the `compute_for_spec` rustdoc Priority table both correctly reflect the new REPORT.md-first ordering and cite SPEC-0043 REQ-002. Production code in `speccy-core/src/next.rs` uses `fs-err`, `camino`, and `sha2` as mandated by `rust-dependencies.md`, carries `#[must_use]` annotations with descriptive messages, and contains no `unwrap()`, `expect()`, or `allow(...)` suppressions. No unused imports or dead code introduced.
</review>
