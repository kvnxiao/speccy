# Evidence: SPEC-0043 / T-003

Doc-only task. Four CHKs (CHK-001, CHK-002, CHK-010, CHK-011), all
demonstrated via scoped grep contracts against the canonical templates
at HEAD after the task's edits.

## CHK-001 — every SPEC-scoped `speccy next --json` carries SPEC-NNNN

Command:

```
grep -n 'speccy next --json' \
  resources/modules/skills/speccy-orchestrate.md \
  resources/modules/skills/speccy-review.md \
  resources/modules/skills/partials/vet-phases.md \
  resources/modules/phases/speccy-work.md \
  resources/modules/phases/speccy-ship.md
```

Output:

```
resources/modules/skills/speccy-review.md:7:resolves the next reviewable task via `speccy next --json` (workspace
resources/modules/skills/speccy-review.md:55:  speccy next --json
resources/modules/phases/speccy-work.md:7:implementable task via `speccy next --json` (workspace form — used here
resources/modules/phases/speccy-work.md:50:     speccy next --json
```

All four remaining unfiltered matches are inside the no-selector
workspace-form sections of `speccy-review.md` and `speccy-work.md`,
each accompanied by an inline reason naming the workspace-form intent.
No SPEC-scoped section in any of the five files emits an unfiltered
`speccy next --json` invocation. The carve-out in REQ-001's
`<done-when>` for workspace-form callsites is satisfied.

## CHK-002 — orchestrator's first `speccy next` is per-spec

Command:

```
grep -n 'speccy next SPEC' resources/modules/skills/speccy-orchestrate.md
```

Output:

```
5:Queries `speccy next SPEC-NNNN --json`, dispatches each step to one or more
95:orchestrator always re-queries `speccy next SPEC-NNNN --json` after a
98:**Exit-code-stop contract.** If `speccy next SPEC-NNNN --json` exits
108:1. Resolve the spec directory from `speccy next SPEC-NNNN --json`:
149:   speccy next SPEC-NNNN --json
176:   `speccy next SPEC-NNNN --json` and loop from step 1.
185:> short — the caller reads `speccy next SPEC-NNNN --json` for ground truth.
210:`speccy next SPEC-NNNN --json`.
```

Every `speccy next` callsite in `speccy-orchestrate.md` — including
the startup spec-dir resolution at line 108 and the in-loop query at
line 149 — uses the per-spec form. A reader following
`/speccy-orchestrate SPEC-0042` executes `speccy next SPEC-0042 --json`
as their first invocation.

## CHK-010 — orchestrator states non-zero exit is the loop-stop signal

Command:

```
grep -n -i 'exit\|non-zero\|loop-stop' resources/modules/skills/speccy-orchestrate.md
```

Output (relevant rows):

```
98:**Exit-code-stop contract.** If `speccy next SPEC-NNNN --json` exits
99:non-zero, the SPEC has reached a terminal state — halt the loop and
101:when exit code is 0.
110:   to get `<spec-dir>`. If the command exits non-zero, surface the
152:   If the command exits non-zero, the SPEC has reached a terminal
155:   envelope when exit code is 0.
```

Lines 98–101 carry the named "Exit-code-stop contract" paragraph; the
contract is restated co-located with the in-loop `speccy next` query
at lines 152–155, both naming "halt the loop and surface the stderr
line". The same one-liner is mirrored in `speccy-review.md`,
`speccy-work.md`, and `vet-phases.md` per the task scope.

## CHK-011 — ship.md states `kind == "ship"` is ship-ready; null+nonzero is terminal

Command:

```
grep -n 'next_action' resources/modules/phases/speccy-ship.md
```

Output:

```
19:- `next_action.kind == "ship"` (exit 0) is the ship-readiness
22:- `next_action: null` paired with **non-zero exit** is the
27:- Any other `next_action.kind` (`work`, `review`, `vet`,
42:   `"next_action": {"kind": "ship", ...}` (exit 0) — that is the
43:   ship-readiness signal. If instead it returns `next_action: null`
```

The readiness-semantics block at lines 19–27 declares
`next_action.kind == "ship"` as the ship-readiness signal and
`next_action: null` paired with non-zero exit as the terminal /
already-shipped signal. The stale "all tasks completed, no REPORT.md
yet" claim no longer appears in the file.

## Hygiene roll-up

No Rust source code changed in this task — only prose under
`resources/modules/`. `cargo test`, `cargo clippy`, `cargo fmt`, and
`cargo deny` are intentionally deferred to T-004, which mirrors the
canonical edits into the three shipped host packs and runs the
`cargo test -p speccy-cli skill_packs` gate against the byte-equivalence
invariant.
