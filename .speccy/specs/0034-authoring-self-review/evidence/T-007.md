# Evidence

## Session session-2026-05-20-t007-attempt-1

Red-then-green paper trail for T-007: pre-check section in `speccy-brainstorm.md`.

### Scenario 1 — Four pre-check property names (CHK-005)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-brainstorm.md`

```
$ git show HEAD:resources/modules/skills/speccy-brainstorm.md | grep -in "atomized restated requirements\|structurally distinct framings\|load-bearing assumptions\|shape-changing open questions"
(no output)
```
</red>

<green>
Post-edit: all four property names present in working tree

```
$ grep -in "atomized restated requirements\|structurally distinct framings\|load-bearing assumptions\|shape-changing open questions" resources/modules/skills/speccy-brainstorm.md
148:   - **Atomized restated requirements.** Each restated-ask
153:   - **Structurally distinct framings.** The alternative framings in
158:   - **Load-bearing assumptions.** Each silent assumption in artifact
163:   - **Shape-changing open questions.** Each open question in artifact
```
</green>

### Scenario 2 — Verbatim chat-preamble opening and closing lines (CHK-008)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-brainstorm.md`

```
$ git show HEAD:resources/modules/skills/speccy-brainstorm.md | grep -n "Self-review caught the following\|Proceeding with the four artifacts"
(no output)
```
</red>

<green>
Post-edit: both verbatim strings present, quoted in template body

```
$ grep -n "Self-review caught the following\|Proceeding with the four artifacts" resources/modules/skills/speccy-brainstorm.md
135:   `**Self-review caught the following before presenting artifacts:**`
141:   `Proceeding with the four artifacts below.`
```
</green>

### Scenario 3 — Tie-breaker line (CHK-006)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-brainstorm.md`

```
$ git show HEAD:resources/modules/skills/speccy-brainstorm.md | grep -in "judging requires reading semantics"
(no output)
```
</red>

<green>
Post-edit: tie-breaker present

```
$ grep -in "judging requires reading semantics" resources/modules/skills/speccy-brainstorm.md
129:   fix in chat. If judging requires reading semantics, it is semantic.
```
</green>

### Scenario 4 — No-loop instruction (CHK-009)

<red>
Pre-edit: zero matches in `git show HEAD:resources/modules/skills/speccy-brainstorm.md`

```
$ git show HEAD:resources/modules/skills/speccy-brainstorm.md | grep -in "do not re-check"
(no output)
```
</red>

<green>
Post-edit: no-loop instruction present

```
$ grep -in "do not re-check" resources/modules/skills/speccy-brainstorm.md
111:   user, run this internal review pass exactly once. Do not re-check
```
</green>

### Scenario 5 — Section position

Pre-check section (step 4, lines 110-167) appears after the agent's internal
artifact draft step (step 3, "Produce four artifacts", lines 62-109) and before
the stop-and-wait step (step 5, line 168: `**Stop and wait.**`).

### Commands run

```
$ cargo run -- check SPEC-0034
19 scenarios rendered across 1 specs
exit 0

$ cargo test --workspace
test result: FAILED. 34 passed; 1 failed
(only dogfood_outputs_match_committed_tree — pre-existing local model override)

$ cargo run -- next --json SPEC-0034
{"schema_version":1,"spec_id":"SPEC-0034","next_action":{"kind":"review","task_id":"T-007"},...}
exit 0
```
