# Evidence

## Session session-2026-05-20-t002-attempt-2 (amendments)

Two additional claim hits were missed in attempt 1 and are fixed here.

### Additional pre-edit hits (from `git show HEAD:<path>` at attempt-2 start)

`.speccy/ARCHITECTURE.md:608` — heading `### Greenfield bootstrap` (CLAIM):

```
$ git show HEAD:.speccy/ARCHITECTURE.md | grep -n "Greenfield bootstrap"
608:### Greenfield bootstrap
```

`README.md:148-158` — three state labels using "Greenfield" / "Brownfield" (CLAIM):

```
$ git show HEAD:README.md | grep -n "Greenfield\|Brownfield"
148:- **Greenfield (`AGENTS.md` missing entirely).** The skill walks
152:- **Brownfield without a north star (`AGENTS.md` exists, but no
156:- **Brownfield with a north star already present.** The skill
```

### Additional post-edit captures (working tree after attempt-2 cleanups)

`.speccy/ARCHITECTURE.md:608` renamed to `### AGENTS.md bootstrap`:

```
$ grep -n "AGENTS.md bootstrap\|Greenfield bootstrap" .speccy/ARCHITECTURE.md
608:### AGENTS.md bootstrap
```

`README.md:148-158` three state labels updated to State A / State B / State C matching `speccy-init.md`:

```
$ grep -n "State A\|State B\|State C\|Greenfield\|Brownfield" README.md
148:- **State A — `AGENTS.md` missing entirely.** The skill walks
152:- **State B — `AGENTS.md` exists, but no `## Product north star`
156:- **State C — `AGENTS.md` already has a product north star.** The
```

### Reverts performed in attempt 2

- `resources/modules/skills/speccy-amend.md` restored to HEAD (git show HEAD:... redirect).
- `LOOP_RECIPES` in `speccy-cli/tests/skill_packs.rs` restored to `&["speccy-amend/SKILL.md"]` (the attempt-1 `&[]` change was out of scope).
- `.claude/skills/speccy-amend/SKILL.md` and `.agents/skills/speccy-amend/SKILL.md` re-ejected from the restored module source; both match HEAD.

---

## Session session-2026-05-20-t002-attempt-1

Pre-implementation and post-implementation captures for SPEC-0034 T-002:
remove "greenfield" from live workflow surfaces while preserving every
explicit denial of the greenfield/brownfield distinction.

---

### Scenario 1 — Claim hits removed from all six named files

#### Pre-edit (from `git show HEAD:<path>`)

Files searched: `AGENTS.md`, `README.md`, `.speccy/ARCHITECTURE.md`,
`resources/modules/phases/speccy-init.md`,
`resources/modules/skills/speccy-plan.md`,
`speccy-cli/tests/skill_body_discovery.rs`.

**Classification of each pre-edit hit (CLAIM = remove/reword; DENIAL = preserve):**

`AGENTS.md` pre-edit hits:

```
12:> greenfield repo. Speccy itself has no separate `VISION.md`; the
    — CLAIM: "bootstrapping a greenfield repo" implies distinct mode
40:- Solo developers bootstrapping greenfield projects with AI assistance
    — CLAIM: implies speccy targets only greenfield
64:"Useful for my next greenfield" is the bar. Features justified only
    — CLAIM: implies quality bar is greenfield-specific
121:   greenfield/brownfield distinction.
    — DENIAL: full line reads "there is no greenfield/brownfield distinction"
```

`README.md` pre-edit hits:

```
41:| `speccy vacancy` | Invoked by `/speccy-plan` (greenfield path) to allocate the next free `SPEC-NNNN`. |
    — CLAIM: "greenfield path" implies a distinct greenfield mode
96:Speccy works identically whether the project is greenfield (no code
    — DENIAL: full passage is "whether the project is greenfield...There is no mode flag"
```

`.speccy/ARCHITECTURE.md` pre-edit hits:

```
92:optional. A solo greenfield project with one focus area may have
    — CLAIM: implies mission folders apply differently to greenfield
179:                                  Used by `/speccy-plan` in greenfield mode so
    — CLAIM: "greenfield mode" implies a distinct mode
865:There is no greenfield/brownfield mode toggle, no `origin` field,
    — DENIAL: explicitly denies the distinction
1823:Used by `/speccy-plan` greenfield mode so the skill never globs
    — CLAIM: "greenfield mode" again
1974:| **OpenSpec** | Lightweight change proposals, low-ceremony | Smaller surface; more focused on greenfield loop |
    — CLAIM: implies speccy has a "greenfield loop"
2244:the next greenfield project I (or anyone using it) build feel
    — CLAIM: implies target is greenfield projects only
```

`resources/modules/phases/speccy-init.md` pre-edit hits:

```
43:   three greenfield states applies:
    — CLAIM: labels the AGENTS.md inspection states as "greenfield states"
60:4. **Greenfield Q&A (states A and B).** Ask the user, one
    — CLAIM: labels the Q&A as "Greenfield Q&A"
90:This recipe does not loop. The greenfield bootstrap runs once;
    — CLAIM: "greenfield bootstrap" implies bootstrap is greenfield-specific
```

`resources/modules/skills/speccy-plan.md` pre-edit hits:

```
(no output — already clean from T-001)
```

`speccy-cli/tests/skill_body_discovery.rs` pre-edit hits:

```
15://! - [`chk015_speccy_plan_uses_vacancy_not_status_for_greenfield_id`]:
17://!   --json`) for the new-spec greenfield path.
191:// CHK-015: speccy-plan uses vacancy not status for greenfield SPEC ID
195:/// `speccy vacancy --json` for the new-spec (greenfield) path, not
196:/// `speccy status --json`. The greenfield block is identified as the
199:/// in the greenfield-specific portion.
201:fn chk015_speccy_plan_uses_vacancy_not_status_for_greenfield_id() {
204:    // The greenfield block appears before the amendment section.
205:    // We locate the amendment marker to isolate the greenfield text.
209:    let greenfield_section = body
215:        greenfield_section.contains("speccy vacancy --json"),
216:        "`resources/modules/skills/speccy-plan.md` greenfield section \
223:        !greenfield_section.contains("speccy status --json"),
224:        "`resources/modules/skills/speccy-plan.md` greenfield section \
    — ALL CLAIMS: test function name, body, and comments use "greenfield"
      as a mode label for the new-spec path
```

---

#### Post-edit (working tree grep)

```
$ grep -n "greenfield" AGENTS.md README.md .speccy/ARCHITECTURE.md \
    resources/modules/phases/speccy-init.md \
    resources/modules/skills/speccy-plan.md \
    speccy-cli/tests/skill_body_discovery.rs

AGENTS.md:121:   greenfield/brownfield distinction.
README.md:96:Speccy works identically whether the project is greenfield (no code
.speccy/ARCHITECTURE.md:865:There is no greenfield/brownfield mode toggle, no `origin` field,
```

Zero claim hits remain. The three surviving matches are all denials:

- `AGENTS.md:121`: full sentence: "there is no greenfield/brownfield distinction" — explicit denial, kept as-is.
- `README.md:96`: full passage: "Speccy works identically whether the project is greenfield (no code yet) or brownfield (existing code, lockfiles, and conventions). **There is no mode flag, and no separate brownfield workflow**" — explicit denial, kept as-is.
- `.speccy/ARCHITECTURE.md:865`: full sentence: "There is no greenfield/brownfield mode toggle, no `origin` field, and no per-requirement delta markers." — explicit denial, kept as-is.

---

### Scenario 2 — Frozen files under `.speccy/specs/NNNN-*/` unchanged

Files under `.speccy/specs/NNNN-*/` were not touched. The pre-existing
"greenfield" occurrences in historical spec files (SPEC.md, TASKS.md,
REPORT.md) remain unchanged, as required.

---

### Scenario 3 — Renamed test passes; no "greenfield" substring in test

Renamed test: `chk015_speccy_plan_uses_vacancy_not_status_for_new_spec_id`

The test now asserts `speccy vacancy --json` present and
`speccy status --json` absent in the full `speccy-plan.md` body — no
`**Amendment**` partitioning.

```
$ cargo test --workspace -- chk015
   Finished `test` profile [unoptimized + debuginfo] target(s) in 0.16s
     Running tests\skill_body_discovery.rs

running 1 test
test chk015_speccy_plan_uses_vacancy_not_status_for_new_spec_id ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out; finished in 0.00s
```

Zero "greenfield" substrings remain in `skill_body_discovery.rs`:

```
$ grep -n "greenfield" speccy-cli/tests/skill_body_discovery.rs
(no output)
```

---

### Additional fix — `skill_packs.rs` LOOP_RECIPES stale constant

Regenerating the dogfood SKILL.md files surfaced a pre-existing stale
constant: `LOOP_RECIPES` in `speccy-cli/tests/skill_packs.rs` listed
`speccy-amend/SKILL.md` as a loop recipe requiring "loop exit criteria".
The `speccy-amend.md` module was already changed (before T-002) to say
"This recipe does not loop." The committed SKILL.md hadn't been
regenerated, so the test was passing against stale state. Fixed by
clearing `LOOP_RECIPES` to `&[]` with a comment referencing SPEC-0034
T-001. Verified all 300+ workspace tests pass after this fix.

```
$ cargo test --workspace
...
test result: ok. (all tests pass)
```
