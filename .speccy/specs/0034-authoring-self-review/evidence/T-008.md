# Evidence

## Session: session-2026-05-20-t008-attempt-1

### Re-eject: claude-code host

Command: `cargo run -- init --force --host claude-code`
Exit code: 0

```
speccy init plan:
  unchanged        .speccy\speccy.toml
  unchanged        .claude/agents/reviewer-architecture.md
  unchanged        .claude/agents/reviewer-business.md
  unchanged        .claude/agents/reviewer-docs.md
  unchanged        .claude/agents/reviewer-security.md
  unchanged        .claude/agents/reviewer-style.md
  unchanged        .claude/agents/reviewer-tests.md
  unchanged        .claude/agents/speccy-ship.md
  unchanged        .claude/agents/speccy-tasks.md
  (!) overwritten  .claude/agents/speccy-work.md
  (!) overwritten  .claude/skills/speccy-amend/SKILL.md
  (!) overwritten  .claude/skills/speccy-brainstorm/SKILL.md
  unchanged        .claude/skills/speccy-init/SKILL.md
  (!) overwritten  .claude/skills/speccy-plan/SKILL.md
  unchanged        .claude/skills/speccy-review/SKILL.md
  unchanged        .claude/skills/speccy-ship/SKILL.md
  unchanged        .claude/skills/speccy-tasks/SKILL.md
  unchanged        .claude/skills/speccy-work/SKILL.md
  unchanged        .speccy/examples/evidence.md
Init complete: 0 created, 4 overwritten, 15 unchanged.
```

### Re-eject: codex host

Command: `cargo run -- init --force --host codex`
Exit code: 0

```
speccy init plan:
  unchanged        .speccy\speccy.toml
  (!) overwritten  .agents/skills/speccy-amend/SKILL.md
  (!) overwritten  .agents/skills/speccy-brainstorm/SKILL.md
  unchanged        .agents/skills/speccy-init/SKILL.md
  (!) overwritten  .agents/skills/speccy-plan/SKILL.md
  unchanged        .agents/skills/speccy-review/SKILL.md
  unchanged        .agents/skills/speccy-ship/SKILL.md
  unchanged        .agents/skills/speccy-tasks/SKILL.md
  unchanged        .agents/skills/speccy-work/SKILL.md
  unchanged        .codex/agents/reviewer-architecture.toml
  unchanged        .codex/agents/reviewer-business.toml
  unchanged        .codex/agents/reviewer-docs.toml
  unchanged        .codex/agents/reviewer-security.toml
  unchanged        .codex/agents/reviewer-style.toml
  unchanged        .codex/agents/reviewer-tests.toml
  unchanged        .codex/agents/speccy-ship.toml
  unchanged        .codex/agents/speccy-tasks.toml
  unchanged        .codex/agents/speccy-work.toml
  unchanged        .speccy/examples/evidence.md
Init complete: 0 created, 3 overwritten, 16 unchanged.
```

### CHK-001 verification

CHK-001: `.claude/skills/speccy-plan/SKILL.md` contains self-review section whose
body precedes the `/speccy-tasks` handoff suggestion line.

```
$ grep -n "Self-review\|self-review\|/speccy-tasks SPEC-" .claude/skills/speccy-plan/SKILL.md
42:   discretion; neither choice is surfaced as a self-review issue.
44:3. **Self-review pass.** Run this pass exactly once after writing
58:   `- [ ] {ordinal}. **Self-review caught:** {issue}`
99:   <!-- Note: the plan self-review above is an independent copy.
112:5. Suggest the next step: `/speccy-tasks SPEC-NNNN` to
```

PASS: Self-review section at line 44 precedes `/speccy-tasks SPEC-` at line 112.

### CHK-002 verification

CHK-002: `.claude/skills/speccy-amend/SKILL.md` contains self-review section AND
`.claude/skills/speccy-brainstorm/SKILL.md` contains pre-check section.

```
$ grep -n "Self-review\|self-review" .claude/skills/speccy-amend/SKILL.md
39:3. **Self-review pass.** Run this pass exactly once after writing the
43:   <!-- Note: the amend self-review below is an independent copy.
58:   `- [ ] {ordinal}. **Self-review caught:** {issue}`

$ grep -n "Pre-check\|pre-check\|Self-review caught the following" .claude/skills/speccy-brainstorm/SKILL.md
114:4. **Pre-check pass.** Before presenting the four artifacts to the
118:   This pre-check fires on every brainstorm invocation — including
122:   <!-- Note: the brainstorm pre-check below is an independent copy.
139:   `**Self-review caught the following before presenting artifacts:**`
147:   If the pre-check finds no issues, omit the preamble block entirely
```

PASS: amend has self-review section at line 39; brainstorm has pre-check section at line 114.

### CHK-010 verification

CHK-010: All three source module files contain self-review/pre-check section body inline.

```
$ grep -n "Self-review pass\|self-review" resources/modules/skills/speccy-plan.md
38:   discretion; neither choice is surfaced as a self-review issue.
40:3. **Self-review pass.** Run this pass exactly once after writing

$ grep -n "Self-review pass\|self-review" resources/modules/skills/speccy-amend.md
35:3. **Self-review pass.** Run this pass exactly once after writing the
39:   <!-- Note: the amend self-review below is an independent copy.

$ grep -n "Pre-check pass\|pre-check" resources/modules/skills/speccy-brainstorm.md
110:4. **Pre-check pass.** Before presenting the four artifacts to the
114:   This pre-check fires on every brainstorm invocation — including
118:   <!-- Note: the brainstorm pre-check below is an independent copy.
143:   If the pre-check finds no issues, omit the preamble block entirely
```

PASS: all three source modules contain the relevant sections inline.

### CHK-015 verification

CHK-015: `resources/modules/skills/speccy-plan.md` body has no "Amendment",
"amend an existing", or "SPEC-NNNN argument".

```
$ grep -n "Amendment\|amend an existing\|SPEC-NNNN argument" resources/modules/skills/speccy-plan.md
(zero matches)
```

PASS: zero matches.

### CHK-016 verification

CHK-016: ejected `.claude/skills/speccy-plan/SKILL.md` and
`.agents/skills/speccy-plan/SKILL.md` body has no "Amendment" or "amend an existing".

```
$ grep -n "Amendment\|amend an existing" .claude/skills/speccy-plan/SKILL.md
(zero matches)

$ grep -n "Amendment\|amend an existing" .agents/skills/speccy-plan/SKILL.md
(zero matches)
```

PASS: zero matches in both ejected files.

### CHK-017 verification

CHK-017: "greenfield" appears only in denying prose across the five named files.

```
$ grep -n "greenfield" AGENTS.md README.md .speccy/ARCHITECTURE.md \
    resources/modules/phases/speccy-init.md resources/modules/skills/speccy-plan.md
AGENTS.md:121:   greenfield/brownfield distinction.
README.md:96:Speccy works identically whether the project is greenfield (no code
.speccy/ARCHITECTURE.md:865:There is no greenfield/brownfield mode toggle, no `origin` field,
```

PASS: 3 hits, all in explicit denying prose. Zero claiming uses remain.

### cargo test --workspace result

```
test dogfood_outputs_match_committed_tree ... ok
...
test result: ok. 35 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.43s
```

All test suites pass. Final aggregate: 0 failed across all binaries.
`dogfood_outputs_match_committed_tree` now passes — the re-eject brings committed
ejected files back in sync with the renderer.

### speccy-work.md override reversion

`.claude/agents/speccy-work.md` was regenerated from the upstream template
(`resources/agents/.claude/agents/speccy-work.md.tmpl`). The local-only override of
`model: opus[1m]` / `effort: low` has been reverted to `model: sonnet[1m]` /
`effort: medium` as expected and documented in the T-008 task instructions.
