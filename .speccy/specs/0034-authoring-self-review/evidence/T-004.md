# Evidence

## Session session-2026-05-20-t004-attempt-1

### Scenario 1: CHK-013 — brainstorm collapse-parallels heuristic (MAY wording, cognitive load, concrete example)

**Pre-edit** (sourced from `git show HEAD:resources/modules/skills/speccy-brainstorm.md`):

<red>
$ git show HEAD:resources/modules/skills/speccy-brainstorm.md | grep -n "collapse\|parallel\|MAY\|cognitive"
(no matches)
</red>

No collapse-parallels guidance existed. The "Restated ask, atomized" step (step 3.1) ended at
"each bullet here typically becomes a `<requirement>` block in SPEC.md" with no heuristic paragraph.

**Post-edit** (working tree):

<green>
$ grep -n "MAY\|cognitive\|collapse" resources/modules/skills/speccy-brainstorm.md
75:      requirements differ only by one noun, you MAY express them as one
76:      requirement with an enumerated sub-list to reduce reader cognitive
78:      Y" (differing only in X), collapse to a single requirement with
80:      structure six times. This is agent discretion — there is no
</green>

The new "Collapse-parallels heuristic." paragraph uses "MAY" (not "MUST"), names reader cognitive
load as the goal, and gives the concrete example: "if R1-R6 all read 'the X self-review verifies
Y' (differing only in X), collapse to a single requirement with six sub-bullets a-f".

### Scenario 2: CHK-014 — plan symmetric expansion guidance (MAY expand or keep grouped, agent discretion)

**Pre-edit** (sourced from `git show HEAD:resources/modules/skills/speccy-plan.md`):

<red>
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -n "expand\|grouped\|atomic\|MAY\|discretion"
(no matches)
</red>

Step 2 was a single sentence: "Write SPEC.md following the PRD template." No symmetric expansion
guidance existed.

**Post-edit** (working tree):

<green>
$ grep -n "MAY\|expand\|grouped\|atomic\|discretion" resources/modules/skills/speccy-plan.md
34:   sub-list), you MAY expand each sub-bullet to its own atomic
35:   `<requirement>` block (when atomicity adds reviewer-fan-out value)
36:   or keep them grouped under one `<requirement>` with a `<done-when>`
38:   discretion; neither choice is surfaced as a self-review issue.
</green>

Step 2 now reads: "you MAY expand each sub-bullet to its own atomic `<requirement>` block ...
or keep them grouped under one `<requirement>` with a `<done-when>` bullet list ... Agent
discretion; neither choice is surfaced as a self-review issue."

### Scenario 3: Neither template's self-review section surfaces failure to collapse or expand

T-005 and T-006 add the self-review sections; T-004 does not add them (per task scope). The task
scenario states: "Given either template's self-review section (added in T-005 and T-006), when it
is searched for 'collapse' or 'expand', then no passage surfaces failure to collapse or expand as
a self-review issue." This is a constraint on T-005/T-006's future additions; T-004's obligation
is solely CHK-013 and CHK-014 (above).

### Verification commands

```
$ cargo run -- check SPEC-0034
19 scenarios rendered across 1 specs
# exit 0

$ cargo test --workspace
# exit 101 — 1 failure: dogfood_outputs_match_committed_tree (pre-existing local model override)
# all other tests pass (34 passed)
```
