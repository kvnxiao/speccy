---
spec: SPEC-0057
task: T-003
generated_at: 2026-06-11T01:51:21Z
---

<implementer date="2026-06-11T01:51:21Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: full area-map (round 1). Reuse-as-is: `balance_pass` in `xml.rs` (name-scoped per-name-stack balance over raw/path/whitelist — journals call it unchanged); `JOURNAL_ELEMENT_NAMES` (`crate::parse::journal_xml`) as the journal whitelist; `fs_err::read_to_string` for the on-demand read; the JNL path-derivation pattern (`spec.dir.join("journal")` + `T-NNN.md` per `tasks_md_ok().tasks`, per DEC-003). Write-fresh: one `journal_pass(spec, out)` helper — searched `xml.rs` for any journal handling, none existed; it is the T-003 addition. Test side reuses `make_workspace`/`run_lint`/`xml_001` and adds one `write_journal` helper mirroring `lint_jnl.rs`'s `journal/` subdir creation.
- Completed: extended `speccy-core/src/lint/rules/xml.rs` `lint()` with a `journal_pass` that derives `journal/T-NNN.md` per parsed task, skips non-existent files, reads existing ones on demand, and runs the existing `balance_pass` with `JOURNAL_ELEMENT_NAMES`. Added two integration tests to `speccy-core/tests/lint_xml.rs`: `xml_001_fires_for_dangling_tag_in_journal` (CHK-007 — dangling close fires one XML-001 naming the journal file + line + tag) and `xml_001_silent_on_balanced_journal` (balanced foreign pair in a journal is silent). No journal field added to `ParsedSpec`.
- Undone: nothing in scope for REQ-004's journal clause. T-004 (REQ-006) remains pending.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: roll call for CHKs under REQ-004:
  - CHK-006 (three parsed artifacts each fire one XML-001): hygiene → pre-existing `xml_001_covers_all_three_parsed_artifacts` in `speccy-core/tests/lint_xml.rs` covers it under `cargo test` (untouched by this task; T-002 owned it).
  - CHK-007 (existing journal/T-NNN.md with a dangling foreign tag fires exactly one XML-001 naming that journal file): demonstrated → `xml_001_fires_for_dangling_tag_in_journal` asserts hits.len()==1, file ends_with `T-001.md`, line is Some, message names `journalorphan`; paired with `xml_001_silent_on_balanced_journal` showing a balanced journal stays silent.
- Discovered issues: none.
- Procedural compliance: followed speccy-work first-attempt branch; clean tree at entry, journal absent, consistency ok. No skill/source-module edits needed.
</implementer>
<review date="2026-06-11T01:53:11Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in the T-003 journal-coverage slice.
The on-demand journal read in `speccy-core/src/lint/rules/xml.rs:72`
builds paths as `spec.dir.join("journal").join(format!("{}.md", task.id))`,
and `task.id` is regex-anchored to `^T-\d{3,}$` at parse time
(`speccy-core/src/parse/task_xml/mod.rs:332,889`), so there is no
path-traversal vector through the interpolated id and the read is
confined to the spec's own `journal/` subdir. Reads go through
`fs_err::read_to_string` with a graceful `else` skip on failure
(no `unwrap`/panic), matching the JNL-* access pattern. The emitted
`XML-001` diagnostic carries only the foreign tag name and a line
number (`orphan_diagnostic`, line 139) — no journal file contents,
secrets, or credentials leak into diagnostics or logs. No injection,
deserialization, auth-boundary, crypto, or race surface is introduced.
</review>
<review date="2026-06-11T01:53:27Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-004's journal clause is satisfied. journal_pass in speccy-core/src/lint/rules/xml.rs derives journal/T-NNN.md per tasks_md_ok().tasks via spec.dir.join("journal") (the JNL access pattern), skips missing/unreadable files, and runs the existing name-scoped balance_pass with JOURNAL_ELEMENT_NAMES — no ParsedSpec field added, honoring DEC-003. Done-when "dangling foreign tag in an existing journal/T-NNN.md produces an XML-001 whose file is that journal file" maps to xml_001_fires_for_dangling_tag_in_journal (asserts one hit, file ends_with T-001.md, line present, message names the orphan); CHK-007 covered. The second task-scenario (balanced/no journal stays silent) maps to xml_001_silent_on_balanced_journal, with the whitelisted journal-native element pair correctly unflagged. No non-goal scope creep: only task-derived journal paths are read, evidence/unparsed prose untouched. No open questions silently resolved; changelog intent is stable. All 8 lint_xml tests pass; clippy clean on speccy-core.
</review>
<review date="2026-06-11T01:53:29Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defect found in the T-003 journal-coverage slice.
The new `journal_pass` in `speccy-core/src/lint/rules/xml.rs:72` derives
`journal/T-NNN.md` paths exactly as the established JNL-* rules do
(`spec.dir.join("journal")` + `format!("{}.md", task.id)`, confirmed
against `speccy-core/src/lint/rules/jnl.rs:32`), guards on
`tasks_md_ok()`, skips non-existent files via `exists()`, and handles an
unreadable file with `let Ok(raw) ... else continue` — no panic and no
wrong-branch on the absent/unreadable paths. The reused `balance_pass`
runs unchanged with the correct `JOURNAL_ELEMENT_NAMES` whitelist
(`["implementer","review","blockers"]`), so whitelisted journal blocks
are not flagged while a foreign orphan (e.g. `</journalorphan>`) is. No
double-processing: the parsed-document passes cover only SPEC/TASKS/
REPORT, journals only via `journal_pass`. All 8 `lint_xml` tests pass.
</review>
<review date="2026-06-11T01:53:37Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
Two stale-doc mismatches (docs-match-code convention):

1. `speccy-core/src/lint/rules/xml.rs:35-36`: `pub fn lint` doc says "Append every `XML-001` diagnostic for one spec across its parsed document artifacts (SPEC.md / TASKS.md / REPORT.md)" but the function body also calls `journal_pass(spec, out)` (line 63). The journal artifact is now part of what `lint()` covers; the doc omits it. Expected: doc lists all four artifact classes or delegates to "including journal files."

2. `speccy-core/tests/lint_xml.rs:5-12`: Module-level doc says "SPEC-0057 T-002 integration tests … CHK-001 … CHK-006" but T-003 added `write_journal`, `JOURNAL_MD`, `xml_001_fires_for_dangling_tag_in_journal` (CHK-007), and `xml_001_silent_on_balanced_journal` to this file. The doc range and task attribution no longer match the file's content.

Both are concrete code-prose mismatches added by this task's changes. No other findings: naming, imports, no dead code, `#[expect]` suppression carries a justification, path-derivation pattern matches `jnl.rs` verbatim, reuse survey is present and all named symbols confirmed in-tree.
</review>
<review date="2026-06-11T01:55:31Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. The two new journal tests genuinely drive the real lint engine over an on-disk tempdir workspace (`speccy_core::lint::run`), no mocking of the system under test. I confirmed mutation-sensitivity by rewriting the implementation twice: (1) disabling the `journal_pass` call at `speccy-core/src/lint/rules/xml.rs:63` makes `xml_001_fires_for_dangling_tag_in_journal` fail (0 XML-001 hits vs expected 1), proving the test actually exercises the new on-demand journal path; (2) forcing the close-arm to never pop in `balance_pass` makes `xml_001_silent_on_balanced_journal` fire false positives on the balanced `details` pair, proving it gates real per-name balance rather than being a vacuous zero-assert. Both restored green afterward (full `lint_xml.rs` 8/8 pass). Fixtures are valid: the `implementer` element name is whitelisted via `JOURNAL_ELEMENT_NAMES` so the structural wrapper is skipped, leaving `journalorphan` as the lone dangling foreign close and the `details` open/close as a balanced foreign pair. CHK-007 is demonstrated (asserts exact count, journal file path ends_with `T-001.md`, line present, message names the offending tag); the task's balanced-journal scenario is the silent half. Evidence roll call is inline in the implementer block (the established convention for this spec, accepted on T-002) and complete for REQ-004: CHK-006 labeled hygiene citing the untouched pre-existing `xml_001_covers_all_three_parsed_artifacts` (accurate -- out of T-003's scope), CHK-007 labeled demonstrated citing a test present in the diff. No fabrication: the implementer reports hygiene exit codes rather than a fabricated red/green transcript, and every cited test name exists in the diff.
</review>
<blockers date="2026-06-11T01:56:18Z" round="1">
Fix two stale doc comments that this task's changes left out of sync with the code (docs-match-code convention).

- `speccy-core/src/lint/rules/xml.rs` (`pub fn lint` doc, ~line 35-36): the doc enumerates "SPEC.md / TASKS.md / REPORT.md" but `lint()` now also calls `journal_pass(spec, out)`. Update the doc to include the journal artifact class (e.g. list all four, or "...including per-task journal files").
- `speccy-core/tests/lint_xml.rs` (module doc, ~line 5-12): the doc still says "SPEC-0057 T-002 integration tests … CHK-001 … CHK-006", but T-003 added `write_journal`, `JOURNAL_MD`, `xml_001_fires_for_dangling_tag_in_journal` (CHK-007), and `xml_001_silent_on_balanced_journal`. Update the task attribution and CHK range to cover the journal tests added here.

Both are prose-only edits to comments introduced/affected by this task; no behavior change required. Business, tests, security, and correctness all passed.
</blockers>
<implementer date="2026-06-11T01:58:58Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: no full area-map this round — the round-1 blocker was a docs-match-code style finding, not a reuse finding, and this round adds no new top-level symbol (per-symbol floor n/a). Behavior code unchanged.
- Completed: addressed both round-1 style blockers (prose-only, docs-match-code). (1) `speccy-core/src/lint/rules/xml.rs` `pub fn lint` doc now enumerates all four artifact classes including the per-task `journal/T-NNN.md` files, matching the `journal_pass(spec, out)` call in the body. (2) `speccy-core/tests/lint_xml.rs` module doc now attributes the parsed-document tests to T-002 and the journal tests (CHK-007) to T-003, and the CHK range covers CHK-001 … CHK-007 rather than stopping at CHK-006.
- Undone: nothing in scope for REQ-004. T-004 (REQ-006) remains pending.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail unchanged from round 1 — this round is docs-only and adds no test. Roll call for CHKs under REQ-004:
  - CHK-006 (three parsed artifacts each fire one XML-001): hygiene → pre-existing `xml_001_covers_all_three_parsed_artifacts` in `speccy-core/tests/lint_xml.rs` covers it under `cargo test` (T-002-owned, untouched).
  - CHK-007 (existing journal/T-NNN.md with a dangling foreign tag fires exactly one XML-001 naming that journal file): demonstrated → `xml_001_fires_for_dangling_tag_in_journal` asserts hits.len()==1, file ends_with `T-001.md`, line present, message names the orphan; paired with `xml_001_silent_on_balanced_journal` (balanced journal stays silent). Both green this round.
- Discovered issues: none.
- Procedural compliance: followed speccy-work retry branch — amended the prior pass's uncommitted WIP in place (no restore/clean/checkout, no rewrite from scratch), addressed every listed blocker, re-ran the full hygiene gate. No skill/source-module edits needed.
</implementer>
<review date="2026-06-11T02:00:30Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns in the T-003 round-2 retry. The round-1 blocker
was a style/docs-match-code finding; the round-2 changes are prose-only
(the `pub fn lint` doc and module doc in `speccy-core/src/lint/rules/xml.rs`
now enumerate the journal artifact class, and the `lint_xml.rs` module doc
attributes the journal tests to T-003). The behavioral surface
(`journal_pass`, `balance_pass`, `orphan_diagnostic`) is byte-identical to
the round-1 pass and carries no new security implications.

Re-verified the unchanged surface: `journal_pass`
(`speccy-core/src/lint/rules/xml.rs:73`) builds paths as
`spec.dir.join("journal").join(format!("{}.md", task.id))`; `task.id` is
regex-anchored to `^T-\d{3,}$` at parse time, so the interpolated id is no
path-traversal vector and reads stay confined to the spec's own `journal/`
subdir. Reads go through `fs_err::read_to_string` with a graceful
`else continue` (no unwrap/panic). The emitted `XML-001` diagnostic
(`orphan_diagnostic`, line 140) carries only the foreign tag name and a
1-indexed line — no journal contents, secrets, or credentials leak into
diagnostics or logs. No injection, deserialization, auth-boundary, crypto,
or race surface is introduced.
</review>
<review date="2026-06-11T02:00:43Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-2 retry resolves the round-1 blocker and REQ-004's journal clause holds. The two stale-doc blockers are fixed: `speccy-core/src/lint/rules/xml.rs` `pub fn lint` doc now enumerates the journal artifact alongside SPEC/TASKS/REPORT and the module-level doc names the on-demand journal/T-NNN.md coverage; `speccy-core/tests/lint_xml.rs` module doc now attributes CHK-001..CHK-006 to T-002 and CHK-007 to T-003. REQ-004 done-when "dangling foreign tag in an existing journal/T-NNN.md produces an XML-001 whose file is that journal file" maps to `journal_pass` deriving paths via `spec.dir.join("journal").join("{task.id}.md")` (JNL access pattern), skipping missing/unreadable files, and running the existing name-scoped `balance_pass` with JOURNAL_ELEMENT_NAMES (the three whitelisted journal element names) — no ParsedSpec field added, honoring DEC-003. Test `xml_001_fires_for_dangling_tag_in_journal` asserts one hit, file ends_with T-001.md, line present, message names `journalorphan` (CHK-007); the balanced-journal scenario `xml_001_silent_on_balanced_journal` correctly leaves the whitelisted journal wrapper element and the balanced foreign pair unflagged. No non-goal creep: only task-derived journal paths are read, evidence/unparsed prose untouched, no autofix. No open question silently resolved; changelog intent stable. The SPEC.md/REPORT.md done-when bullets are T-002's scope (sibling completed) and stay covered by the untouched `xml_001_covers_all_three_parsed_artifacts`.
</review>
<review date="2026-06-11T02:00:48Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Both round-1 blocking items are resolved.

1. `speccy-core/src/lint/rules/xml.rs:35-37`: `pub fn lint` doc now reads "Append every `XML-001` diagnostic for one spec across its parsed document artifacts (SPEC.md / TASKS.md / REPORT.md) and its existing per-task `journal/T-NNN.md` files." — matches the function body which calls `journal_pass(spec, out)` at line 64.

2. `speccy-core/tests/lint_xml.rs:5-14`: module doc now separates T-002 attribution (CHK-001 … CHK-006, parsed-document artifacts) from T-003 attribution (CHK-007, journal files) — content range correctly covers the journal tests added by this task.

No new findings. Naming matches project patterns (`journal_pass`, `write_journal`). No dead imports or unused variables introduced. No suppression annotations added. Path-derivation in `journal_pass` matches `jnl.rs` verbatim. Reuse survey present in both implementer blocks; named symbols (`balance_pass`, `JOURNAL_ELEMENT_NAMES`, `fs_err::read_to_string`) confirmed in-tree and called by the diff.
</review>
<review date="2026-06-11T02:01:33Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
No correctness defect in the round-2 T-003 journal-coverage slice. The
round-1 blockers were style-lane docs-match-code findings; the round-2
changes are confined to doc comments plus the already-round-1-passed
`journal_pass` logic, introducing no new control flow. I re-reviewed the
behavior on its merits: `journal_pass` in `speccy-core/src/lint/rules/xml.rs:69`
guards on `tasks_md_ok()` with an early `else return`, derives
`journal/T-NNN.md` via `spec.dir.join("journal")` + `format!("{}.md", task.id)`
exactly as `speccy-core/src/lint/rules/jnl.rs:32-35`, skips non-existent files
via `exists()`, and handles an unreadable file with `let Ok(raw) ... else
continue` — no panic, no inverted condition, no wrong-branch on the
absent/unreadable paths (the exists-then-read TOCTOU window is benign because
the read failure falls through to `continue`). The reused `balance_pass`
runs unchanged with the correct `JOURNAL_ELEMENT_NAMES`
(`["implementer","review","blockers"]`) whitelist: a close pops its name's
stack via `get_mut(&name).and_then(Vec::pop).is_some()` (dangling close flagged
on empty stack), non-void opens push, and leftover stack lines flag dangling
opens — no off-by-one and no double-processing (parsed-document passes cover
only SPEC/TASKS/REPORT; journals only via `journal_pass`). All 8 `lint_xml`
tests pass.
</review>
<review date="2026-06-11T02:01:39Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Pass (round 2). Round 1's blocker was style-only (two stale doc comments); the tests substance already passed in round 1 and is unchanged here. Round 2 added no tests — docs-only — so no new test risk. I re-verified the two journal tests in `speccy-core/tests/lint_xml.rs` genuinely drive the real lint engine (`speccy_core::lint::run`) over an on-disk tempdir workspace with no mocking of the system under test, and confirmed mutation-sensitivity twice: (1) commenting out the `journal_pass(spec, out)` call at `speccy-core/src/lint/rules/xml.rs:64` makes `xml_001_fires_for_dangling_tag_in_journal` fail (0 XML-001 vs expected 1), proving it exercises the new on-demand journal path; (2) forcing the close-arm to never pop in `balance_pass` makes `xml_001_silent_on_balanced_journal` emit two false-positive XML-001 hits on the balanced details pair, proving it gates real per-name balance rather than a vacuous zero-assert. Both restored to green (8/8 lint_xml pass). Fixtures are valid: the journal implementer wrapper element is whitelisted via `JOURNAL_ELEMENT_NAMES` (implementer/review/blockers) so the structural wrapper is skipped, leaving the journalorphan close tag as the lone dangling foreign close and the details open/close as a balanced foreign pair. CHK-007 is demonstrated (asserts exact count==1, file ends_with `T-001.md`, line present, message names the orphan). Evidence roll call is inline in the implementer block (the established convention accepted on T-002 for this spec) and complete for REQ-004: CHK-006 labeled hygiene citing the untouched pre-existing `xml_001_covers_all_three_parsed_artifacts` (accurate — T-002-owned, out of T-003 scope), CHK-007 labeled demonstrated citing a test present in the working-tree diff. No fabrication: hygiene exit codes reported rather than a fabricated transcript, and every cited test name exists in the diff under review.
</review>
