---
spec: SPEC-0055
task: T-008
generated_at: 2026-06-10T07:08:18Z
---

<implementer date="2026-06-10T07:08:18Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Prose-only task editing skill-pack module sources under `resources/`; no new top-level code symbols. Area-map (round 1): the lifecycle-write CLI verbs `speccy task transition`, `speccy journal append`, and `speccy journal show` (landed in T-001/T-002/T-004/T-005) are the reuse targets — every state/journal mutation in the touched module sources now routes through these existing verbs rather than introducing new mechanism. Reuse-as-is: `task transition --to STATE` for all TASKS.md state flips (review-fanout consolidation, speccy-work step 4/9 + retry branch, amend completed-to-pending, every reconcile-policy state row); `journal append --block blockers` / `--block gate` for orchestrator-authored consolidated blockers and the vet terminal gate; `journal show --block review --round latest` / `--verdict blocking` for review completeness + blocker read-back, and `journal show ... --round latest` for retry-shape detection. Extend: none. Write-fresh: none (searched `resources/modules/` for any lifecycle mechanism not already covered by a CLI verb — none found; the only journal mutation with no append equivalent is the journal_xml_malformed corruption-recovery truncation, left as a documented byte-offset truncation).
- Completed: Routed every lifecycle write in the orchestrate/review/vet/amend/work module sources through the CLI verbs. (1) partials/review-fanout.md: replaced the serial review-block transcription + hand TASKS.md/journal edits with a three-step CLI flow — verify completeness via journal show --block review --round latest, flip state via task transition, consolidate blockers via journal append --block blockers (body stays orchestrator-authored per DEC-001); restated single-writer rule. (2) speccy-review.md + speccy-orchestrate.md: updated consolidation prose, lifecycle diagram, and dispatch-settle summary to name the CLI verbs. (3) phases/speccy-work.md: steps 4 and 9 and the retry branch now flip state via speccy task transition instead of editing TASKS.md. (4) speccy-amend.md step 4: split into structural task-element add/remove (direct edits, not state mutations), completed-to-pending via task transition, and blockers via journal append. (5) references/reconcile-policy.md: every state-flip row + idempotency property now name task transition. (6) references/retry-shape.md: read-only scope now reads the journal via journal show instead of hand-parsing XML. (7) partials/vet-phases.md: dropped Phase 0 hand-bootstrap of VET.md frontmatter/invocation headings (CLI owns sectioning), removed orchestrator transcription of sub-agent blocks (personas self-append per T-007), routed the terminal gate through journal append --block gate (CLI computes tasks_hash), and made every snapshot/revert journal-scoped (pathspec excluding .speccy/specs/*/journal/) so the audit trail survives rollback. (8) speccy-vet.md: rewrote single-writer rule and file-format sections to describe the CLI-mediated append contract. (9) references/journal-implementer.md: example handoff prose names task transition. Ran just reeject; all 8 ejected files regenerated to match.
- Undone: Nothing in REQ-009/T-008 scope. ARCHITECTURE.md updates are REQ-010/T-009, explicitly out of scope.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0. Prose-only change; no Rust source touched — gates confirm the re-eject did not disturb embedded-template tests.
- Evidence: No new automated test (CHK-012 is a reviewer-performed content grep over `resources/modules/`, judgment-only by the SPEC's own statement). Roll call for CHKs under REQ-009:
  - CHK-012 (grep over `resources/modules/` finds no instruction to hand-edit task state attributes or journal files that bypasses the CLI verbs): judgment-only -> reviewer-business/reviewer-style runs the grep on the diff. Self-verified: Grep over `resources/modules/` for edit-state / append-by-hand patterns returns only negations ("never by editing the state attribute directly", "do not transcribe", "no longer hand-bootstraps") plus the one legitimate structural task-element add/remove edit in speccy-amend (not a state mutation). REQ-009 behaviors: orchestrate review-consolidation section directs journal show --block review --round latest (completeness), --verdict blocking (read-back), journal append --block blockers (directive), task transition (flip); reconcile-policy auto-fix table names task transition for every state flip.
- Discovered issues: The T-007 self-append contract created a latent tension with the vet defer-write/rollback pattern: vet personas now append their blocks to VET.md during their own runs (before the orchestrator keep-vs-revert decision), so a git restore / git stash would erase the audit trail the old defer-write pattern protected by writing only after the revert. Resolved within T-008 "route gate through CLI / drop hand-bootstrap" scope by making every vet snapshot and revert journal-scoped via a pathspec excluding the journal directory, so appended blocks persist across any rollback. Flagged for the reviewer as the one non-mechanical design call in this task.
- Procedural compliance: Edited only sources under `resources/` and ran just reeject to regenerate the ejected packs (never edited .claude/.agents/.codex directly), per AGENTS.md "Skill pack source of truth". Dogfooded the contract this task lands: T-008 own state flips (pending-to-in-progress, in-progress-to-in-review) went through speccy task transition, and this handoff lands via speccy journal append. No shipped skill body required a friction-fix edit beyond the task scope.
</implementer>
<review date="2026-06-10T07:14:51Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-008 (REQ-009) faithfully routes every lifecycle mutation in the orchestrate/review/vet/amend/work module sources through the new CLI verbs, and the diff serves the orchestrator user story (never hand-edit markdown, never re-read to append, never relay verdict bodies). Verified against REQ-009's done-when and behavior:

- No file-editing-tool mutation paths. A content grep over `resources/modules/` (CHK-012) returns only negations ("never by editing the state attribute", "do not transcribe", "never edits VET.md with file-editing tools") plus the one legitimate structural-edit carve-out in `resources/modules/skills/speccy-amend.md` (adding/removing task elements is not a state mutation). No lifecycle state/journal-block write bypasses task transition / journal append.
- Completeness-then-flip ordering (behavior bullet 1). `review-fanout.md` Step 1 reads `journal show --json --block review --round latest` to confirm every spawned persona appended before any state flip; Step 2 flips via `task transition`; Step 3 reads `--verdict blocking --round latest` for read-back and appends the consolidated blockers via `journal append`. All four CLI operations the SPEC names are present and correctly ordered.
- Reconcile auto-fix rows (behavior bullet 2). `reconcile-policy.md` policy table now names `speccy task transition` for `commit_without_state`, `state_completed_no_commit` (clean), `state_in_progress_orphaned`, and `state_in_progress_clean`; the idempotency property is restated as a same-state no-op.
- Single-writer rule restated (done-when 4). `review-fanout.md` and `speccy-vet.md` both state the CLI append lock owns serialization while the orchestrator stays sole author of blocker bodies and git commits.
- Non-goals respected. The blockers body stays orchestrator-authored semantic judgment (DEC-001 / "No CLI-authored prose"). Git commit ownership unchanged. The vet journal-rollback handling (journal-scoped git restore/stash/clean pathspecs) is a necessary consequence of T-007's self-append contract, not smuggled scope.
- Ejected packs carry the contract. `just reeject` reports "0 overwritten, 36 unchanged".

One residual, acceptable: the `journal_xml_malformed` reconcile row still directs a byte-offset journal truncation. This is a corruption-recovery operation with no journal-append equivalent, now explicitly labeled as such and pairing its state reset with task transition. REQ-009 targets lifecycle mutations, not corruption recovery, and this SPEC defines no truncation verb — so it is out of REQ-009's contract scope, not a gap. No blocker.
</review>
<review date="2026-06-10T07:15:01Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-008 is a prose-only module-source change; its single covered CHK-012 is explicitly a reviewer-performed content check (SPEC: "content check performed by the reviewer ... the invariant is the absence of file-edit-tool mutation paths"), correctly labelled `judgment-only` in the journal's Evidence roll call and not a blockable test CHK. The `judgment-only` label is honest — the invariant cannot be demonstrated by any deterministic red-then-green command. I independently ran the CHK-012 grep over `resources/modules/`: every match is a negation, a CLI-behavior description, the legitimate structural task-element add/remove in `speccy-amend.md`, or orchestrator-role prose that now routes through `task transition` — no instruction bypasses the CLI verbs. Both named behavior elements are satisfied (orchestrate review consolidation names `journal show --block review --round latest`, `--verdict blocking`, `journal append --block blockers`, and `task transition`; `reconcile-policy.md` names `task transition` for every state-flip row). `just reeject` is idempotent against the working tree (ejected packs match sources), and `speccy verify --json` passes with 0 errors/0 warnings, so the source edits broke no journal/lint shape. No fabrication patterns apply and no executable-test surface exists for a prose-absence invariant, so there is nothing to drive red-then-green here. Evidence field present, file/roll-call honest.
</review>
<review date="2026-06-10T07:15:15Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
PASS. T-008 is a prose-only change to skill/reference module sources under `resources/` (re-ejected to `.claude/`/`.agents/`/`.codex/`). It introduces no authentication/authorization boundaries, no input-injection surface, no credential storage, no crypto parameter choices, and no sensitive-data logging. Lifecycle mutations now route through deterministic CLI verbs (`task transition`, `journal append`, `journal show`). I checked the append call sites: bodies are piped via stdin heredocs with a quoted delimiter, so there is no shell-interpolation/command-substitution injection into the journal append path (`review-fanout.md`, `speccy-amend.md`, `vet-phases.md`).

On the flagged journal-scoped snapshot/rollback pathspec change in `vet-phases.md` (the `git restore -- ':!.speccy/specs/*/journal/'` / `git clean -fd -e '.speccy/specs/*/journal/'` / `git stash push ... -- ':!.speccy/specs/*/journal/'` triad): I reproduced the semantics in a scratch repo and the rollback's core safety property is intact — every non-journal path is fully reverted and untracked non-journal files (including anything a sub-agent might write into source) are still cleaned. The exclusion shields only the journal directory, which is exactly the durable audit trail the change is meant to preserve.

Non-blocking hardening observations (not gating this verdict):
1. The exclusion is directory-wide, not file-pattern-scoped. A misbehaving vet sub-agent that drops an arbitrary untracked file into the journal dir would survive the journal-scoped `git clean`, whereas the same file dropped elsewhere is wiped. Realistic blast radius is small (sub-agents are prompted only to append structured XML to VET.md, and the journal dir is committed evidence that surfaces in `git status` before the commit). Defense-in-depth, not a vulnerability.
2. Pre-existing and out of T-008 scope: `reconcile-policy.md` `state_in_progress_orphaned` still uses unscoped `git restore .` / `git clean -fd`. That is the reconcile path (intentionally discarding orphaned partial work), distinct from the vet loop's journal-preservation contract; it predates this task. Flagging only for awareness.
</review>
<review date="2026-06-10T07:15:22Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Prose-only diff; all Rust hygiene gates remain passing. Every placeholder uses the fictional `SPEC-NNNN/T-NNN` form consistently with the project convention. The one "edit TASKS.md directly" survivor in `speccy-amend.md` is the deliberately scoped structural-task-element carve-out (add/remove task elements for new/dropped requirements — not a state mutation), clearly distinguished from the state-flip and journal-append cases that now route through the CLI verbs. Ejected pack files in `.claude/` and `.agents/` mirror the `resources/modules/` source changes. No naming drift, no dead imports, no vacuous constructs. Reuse survey present; named symbols (`task transition`, `journal show`, `journal append`) exist and are the actual call sites in the diff.
</review>
<review date="2026-06-10T07:15:37Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found at or above the 80% confidence bar.

The central rewired control flow — the journal-scoped snapshot/rollback in `vet-phases.md` — is mechanically sound. I empirically verified the pathspec round-trip (`git stash push --include-untracked -- ':!.speccy/specs/*/journal/'`, `git restore -- ':!...'`, `git clean -fd -e '.speccy/specs/*/journal/'`) for three cases: tracked-journal modification, untracked first-invocation VET.md, and the round-2 stuck revert. In every case the self-appended drift-review/holistic-fix blocks survive the rollback while code correctly reverts to the pre-implementer snapshot. The exclude-pathspec and clean-exclude both correctly partition journal from code.

The task transition / journal append / journal show invocations carry correct selectors, flags, and ordering, verified against the CLI `--help` contracts: `--verdict` is supplied for every vet block and review/blockers; `--model` accompanies the round-bearing synthesized vet appends (holistic-fix --verdict stuck --model ...); the gate append omits a hand-computed `tasks_hash` (CLI-owned); `journal show --block review --round latest` correctly reads back the parallel reviewer self-appends for the completeness gate; `--json` exposes `invocations[].number` so the Phase 0 invocation read-back is well-founded.

No orphaned old-flow instructions survive: every "edit TASKS.md state", "transcribe", "hold in memory", and "defer-write" reference is now a negation, except the one legitimate structural-task-element add/remove in `speccy-amend.md` (correctly left as a direct edit — not a state mutation, no CLI verb covers it). The orchestrate lifecycle diagram and dispatch-settle summary in `speccy-orchestrate.md` and the consolidation prose in `speccy-review.md` are rewired consistently with the CLI-verb flow.

Two items considered and intentionally not flagged: (1) the empty-stash edge case where `git stash apply`/`pop` errors when there is no uncommitted code to snapshot is pre-existing — the pre-T-008 flow used the same bare follow-ups; T-008 only narrows the pathspec and does not regress it. (2) The Phase 2 apply-step read-back `speccy journal show SPEC-NNNN --block simplifier-scan` omits `--round latest`; scan-then-apply runs back-to-back within one invocation so this is a minor read-precision nit, not a reachable wrong-result path — below the reporting bar.
</review>
<review date="2026-06-10T07:15:54Z" model="claude-opus-4-8[1m]/high" persona="architecture" verdict="pass" round="1">
The journal-scoped snapshot/rollback redesign is architecturally sound and is the correct structural response to the tension T-007 introduced. PASS.

Design call (the flagged non-mechanical decision in `vet-phases.md`): T-007 moved VET.md block authorship into the sub-agents, which append during their own runs — so a drift-review/holistic-fix/simplifier block is on disk before the running session reaches its keep-vs-revert decision. The old defer-write pattern guaranteed code-only rollback temporally (write VET.md only after the revert). T-008 replaces that with a structural guarantee: every snapshot (`git stash push ... -- ':!.speccy/specs/*/journal/'`), restore (`git restore -- ':!.speccy/specs/*/journal/'`), and clean (`git clean -fd -e '.speccy/specs/*/journal/'`) excludes the journal directory. This cleanly separates code-under-revert (discardable) from durable journal evidence (append-only audit trail).

No partial-rollback hazard: because the journal is excluded from both the push and the restore/clean/pop, the journal never enters any stash, so `git stash pop` can never conflict on or clobber journal bytes; the journal sits untouched in the tree throughout, accreting blocks via the CLI's locked appends. The push-pathspec and restore-pathspec are byte-identical (`':!.speccy/specs/*/journal/'`), and the `git clean -e` glob form is the correct cross-command translation (clean does not honor `:!` pathspec magic). Enforcement lives in a single locus — the running vet session — and `vet-implementer.md` (and the simplifier) explicitly forbid sub-agents from running git stash/reset/restore/clean, so there is exactly one actor applying the exclusion.

Consistent with the T-007 single-writer-to-self-append shift: vet personas append their own blocks and return thin verdict elements; the running session never transcribes a block. The vet skill's invocation-number return field is now correctly sourced by reading the CLI-assigned invocation number back via `speccy journal show SPEC-NNNN --json` (Phase 0), matching DEC-004/DEC-008 where the CLI owns invocation sectioning. The orchestrate/review/vet contracts are internally consistent after the rewire.

No ADR drift: the journal-scoping refines the existing "journal must survive rollback" property and is not a new decision needing its own DEC; it honors DEC-001 (gate tasks_hash/date/sectioning routed to the CLI — the hand sha256sum/Get-FileHash and hand invocation-heading are dropped), DEC-006, DEC-008, and the SPEC Non-goal preserving orchestrator-owned commits. No agent-behavior knob leaked into the CLI (principle 2 intact). No premature abstraction.

Minor non-blocking observation: the journal-exclusion glob is repeated inline across Phase 1 and Phase 2 rather than stated once as a named "journal-scoped revert" snippet; acceptable under the project's no-prose-size-gate guidance, but a future edit that touches one copy and misses the other is the only drift risk worth flagging socially — not a blocker.
</review>
<review date="2026-06-10T07:16:10Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="blocking" round="1">
The spawn prompt embedded in `resources/modules/skills/partials/review-fanout.md` (the per-reviewer dispatch prompt, ~lines 16-22 of the working-tree file) contradicts the self-append contract that REQ-008/T-007 established.

The prompt the orchestrator sends each reviewer still reads, in effect: "Return your verdict as your final message as a review element block; the model attribute is required ...; Do not edit TASKS.md and do not edit the journal file."

This is the old pre-T-007 instruction. Post-T-007, each reviewer persona is expected to (a) append its own review block via `speccy journal append --block review` and (b) return a thin self-closing verdict element (persona/verdict/model/rationale) — not a full review block body. The "do not edit the journal file" line actively prohibits the correct write path (the CLI-mediated append).

The rest of the partial is correct: the prose below the spawn prompt correctly describes that each reviewer self-appends, and "Step 1 — verify the round's reviews are complete" correctly uses `journal show`. Only the spawn prompt itself was not updated.

Concrete fix needed in `resources/modules/skills/partials/review-fanout.md` (and its ejected copies under `.claude/`, `.agents/`, `.codex/`):
1. Replace the "return your verdict as a review element block" sentence with an instruction to run `speccy journal append ... --block review --persona PERSONA --verdict V --model MODEL` with the review body on stdin, then return a thin self-closing verdict element as the final message.
2. Replace "Do not edit TASKS.md and do not edit the journal file." with "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal."

The verdict-return contract in `verdict_return_contract.md` (included in each persona's agent definition) already specifies the correct two-step behavior. The spawn prompt just needs to match so the instruction the orchestrator sends on spawn is consistent with the instruction the reviewer's own agent file gives.

No other blocking issues: `review-fanout.md` single-writer-rule restatement is correct; `vet-phases.md` correctly removes hand-bootstrap and serial-transcription language and adds journal-scoped rollback; `speccy-vet.md` single-writer rule correctly describes CLI-mediated writes; `reconcile-policy.md` names `speccy task transition` for every state flip; `retry-shape.md` routes detection through `speccy journal show`. AGENTS.md "read-only agents" language: the tools list includes Bash (which enables journal append), so the mechanical grant is sufficient; the label is imprecise but predates T-008 and is T-009 cleanup territory. The `reconcile-policy.md` header's stale "inlined verbatim ... re-sync all three copies" claim is pre-existing debt T-008 did not introduce.
</review>
<blockers date="2026-06-10T07:16:23Z" round="1">
Update the per-reviewer spawn prompt in review-fanout.md to match the self-append contract, then re-eject.
- docs (blocking): `resources/modules/skills/partials/review-fanout.md` — the per-reviewer dispatch/spawn prompt (~lines 16-22) still carries the old pre-T-007 contract: it tells each reviewer to "return your verdict as a review-element block" and "do not edit the journal file." That contradicts the self-append contract REQ-008/T-007 shipped (and that the rest of this same partial already assumes): each reviewer must append its own review block via `speccy journal append --block review --persona PERSONA --verdict V --model MODEL` (body on stdin), then return a thin self-closing verdict element. Required fix: (1) replace the "return a review-element block as your final message" sentence with the `speccy journal append --block review ...` invocation + thin-verdict-return instruction; (2) replace "Do not edit TASKS.md and do not edit the journal file." with "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal." Keep the downstream consolidation prose (Step 1 `journal show` completeness, Step 2 `task transition`, Step 3 `journal append --block blockers`) as-is — it is already correct. After editing the source under `resources/`, run `just reeject` so the ejected copies under `.claude`/`.agents`/`.codex` match. The `verdict_return_contract.md` already specifies the correct two-step behavior; align the spawn prompt to it.
</blockers>
<implementer date="2026-06-10T07:20:33Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Retry round addressing a single docs-persona blocker; no new top-level code symbols introduced. Full area-map not re-run (the blocker is not reuse-related — it is a prompt-contract correction). Per-symbol floor: zero new symbols. The fix reuses the existing self-append contract already specified in `resources/modules/personas/verdict_return_contract.md` (the `speccy journal append --block review` + thin verdict two-step) — the spawn prompt now points at that same contract rather than the superseded "return a review block" wording.
- Completed: Round-2 amend in place over the round-1 WIP (no clean-slate rewrite). (1) `resources/modules/skills/partials/review-fanout.md`: rewrote the per-reviewer spawn prompt (~lines 16-22) — replaced the old "Return your verdict as your final message as a review-element block ... Do not edit TASKS.md and do not edit the journal file." text with an instruction to append the reviewer's own review block via `speccy journal append SPEC-NNNN/T-NNN --block review --persona <persona> --verdict <pass|blocking> --model <your-model>` (body on stdin) and return a thin self-closing verdict element (persona/verdict/model/rationale), plus "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal." This aligns the spawn prompt with `verdict_return_contract.md` and the downstream Step 1/2/3 consolidation prose (left unchanged, already correct). (2) Ran `just reeject`: 2 ejected copies overwritten (the Claude Code + `.agents` renders of the partial flow into `speccy-review` and `speccy-orchestrate`); old "do not edit the journal file" spawn wording is gone from all of `resources/`, `.claude/`, `.agents/`, `.codex/`. (3) Updated the rendered-output test `speccy_review_skill_prefers_native_subagents` in `speccy-cli/tests/skill_packs.rs`: it pinned the old spawn-prompt contract (asserted the spawn prompt contains the review-element opening tag text); flipped it to assert the self-append contract (`speccy journal append ... --block review` and a thin verdict-element return) for both rendered hosts, and updated the surrounding comment that described the old "review directly without a CLI command" behavior.
- Undone: Nothing in the round-1 blocker scope. The docs-persona note also mentioned the AGENTS.md read-only-agents label imprecision and the reconcile-policy header stale "re-sync all three copies" claim as pre-existing T-009 territory — explicitly out of this blocker's required fix and untouched.
- Hygiene checks: cargo test --workspace exit 0 (skill_packs suite now green after the assertion update); cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: Roll call for CHKs under REQ-009:
  - CHK-012 (grep over `resources/modules/` finds no instruction to hand-edit task state or journal files bypassing the CLI verbs): demonstrated (regression-pinned) -> the rendered-output test `speccy_review_skill_prefers_native_subagents` now asserts the spawn prompt directs `speccy journal append ... --block review` and a thin verdict-element return for both hosts; it fails red on the old "return a review block / do not edit the journal file" wording and passes green on the self-append contract. Self-verified: grep for "do not edit the journal file" across `resources/` + ejected packs returns empty; the spawn prompt no longer prohibits the correct CLI-mediated write path.
- Discovered issues: The round-1 WIP left a rendered-output test (`speccy_review_skill_prefers_native_subagents`) pinning the pre-T-007 spawn-prompt contract, which is what surfaced red once the blocker fix landed. Updated the test to pin the self-append contract instead — without this, the test would have silently re-enforced the old wording on a future edit. No other latent old-contract assertions found in the skill_packs suite.
- Procedural compliance: Edited only the source under `resources/modules/skills/partials/review-fanout.md` and the Rust test, then ran `just reeject` to regenerate the ejected packs (never edited `.claude`/`.agents`/`.codex` directly), per AGENTS.md "Skill pack source of truth". Amended the round-1 WIP in place per the retry-shape contract (no git restore/clean/checkout against the dirty tree). State flips (pending->in-progress, in-progress->in-review) went through `speccy task transition`; this handoff lands via `speccy journal append`.
</implementer>
<review date="2026-06-10T07:24:00Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="2">
Round-2 fix fully closes the single round-1 docs blocker and the diff stays faithful to REQ-009 and SPEC-0055's user stories.

The round-1 blocker was precise: the per-reviewer spawn prompt in `resources/modules/skills/partials/review-fanout.md` still told each reviewer to "return your verdict as a review-element block" and "do not edit the journal file" — the pre-T-007 contract that contradicts the self-append model, and actively prohibited the correct CLI-mediated write path. Round-2 applied exactly the two named fixes:

- Spawn prompt (review-fanout.md lines 16-26) now directs each reviewer to `speccy journal append SPEC-NNNN/T-NNN --block review --persona <persona> --verdict <pass|blocking> --model <your-model>` with the body on stdin, then return a thin self-closing verdict element. The invocation shape matches the CLI contract verbatim (`journal append --help` confirms --block/--persona/--verdict/--model are the required review inputs; date/round are CLI-stamped and correctly omitted).
- The "do not edit the journal file" prohibition is replaced with "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal." A grep for the old wording across `resources/`, `.claude/`, `.agents/`, `.codex/` returns empty.

REQ-009 done-when / behavior all hold: the orchestrator-side Steps 1-3 (already correct in round-1) name `journal show --block review --round latest` (completeness), `--verdict blocking --round latest` (read-back), `journal append --block blockers` (directive), and `task transition` (flip). The consolidated blockers body stays orchestrator-authored semantic judgment — the "No CLI-authored prose" non-goal (DEC-001) is respected. No excluded scope smuggled in: round-2 touched only the partial, its re-ejected copies, and the one named test function `speccy_review_skill_prefers_native_subagents`.

CHK-012 holds. `just reeject` is idempotent (0 overwritten, 36 unchanged), and the regression-pinning test now asserts the self-append contract and passes green. No open questions silently resolved; no error/edge path dropped. No blocker.
</review>
<review date="2026-06-10T07:24:11Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 fix to `speccy_review_skill_prefers_native_subagents` (`speccy-cli/tests/skill_packs.rs:669-680`) is non-vacuous and correctly gates the self-append spawn-prompt contract.

- Red proven empirically: reverting only `resources/modules/skills/partials/review-fanout.md` to its T-007 (HEAD) content while keeping the new test makes the test FAIL (0 passed; 1 failed). The old spawn prompt contained neither the needle `speccy journal append SPEC-NNNN/T-NNN --block review` nor a verdict-element opening (grep count 0 for both) and instead carried the old review-element return-contract string — exactly the regression the round-1 docs blocker flagged.
- Green confirmed: passes against round-2 source; full 45-test skill_packs suite green.
- Specificity: both needles occur exactly once in the rendered body, in the spawn-prompt blockquote (review-fanout.md:18,20); the assertion loop iterates both `claude_body` and `codex_body`, so a host-divergent regression is caught. Mentally rewriting the spawn prompt back to the old "return a review block" contract would be caught.
- Evidence: round-2 roll call inlines CHK-012 as "demonstrated (regression-pinned)" with the test named — honest and re-runnable. Minor divergence from T-006/T-007 (which name a separate evidence/T-NNN.md paper-trail file) noted but not blocking: the roll call is present, names the single covered CHK, cites the specific test, and the red/green transition I reproduced matches the claim with genuine runner output (no fabrication pattern). CHK-012's broader grep invariant stays reviewer-judgment per the task note; the spawn-prompt sub-contract is now additionally test-pinned.
</review>
<review date="2026-06-10T07:24:27Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
PASS. The round-2 change is a prose-only spawn-prompt rewrite in `resources/modules/skills/partials/review-fanout.md` (lines 12-26), a re-eject, and one test-assertion update in `speccy-cli/tests/skill_packs.rs`. It introduces no authentication/authorization boundary, no injection surface, no secret handling, no crypto parameters, and no sensitive-data logging.

Security-relevant observations:
- The new spawn prompt directs each reviewer to run `speccy journal append ... --block review --persona <persona> --verdict <pass|blocking> --model <your-model>` with the review body on stdin. The body is piped, not interpolated into the command line, and the flag values are bounded-vocabulary/identity tokens — consistent with the round-1 stdin-heredoc posture (no shell-substitution path into the journal write). If anything this reduces surface: the running session no longer transcribes reviewer-supplied review-block / model attribute strings into its own file write, so there is no orchestrator-side verbatim copy of reviewer-influenced text into a markdown edit.
- `just reeject` is idempotent against the working tree ("0 overwritten, 36 unchanged"), so the ejected `.claude`/`.agents`/`.codex` copies carry the corrected contract with no stale rendered output. A grep for the prohibited old wording "do not edit the journal file" returns only the round-1 docs-blocker quote inside the journal itself and a pre-existing archived spec journal — neither is live instruction text, both out of T-008 scope.
- The updated test (`speccy_review_skill_prefers_native_subagents`) now pins the self-append contract for both hosts and passes.

The round-1 blocker was a docs-persona contract-consistency finding, not a security defect; the round-2 fix resolves it without regressing the journal-append injection posture validated in round 1. No blocker.
</review>
<review date="2026-06-10T07:24:37Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-2 diff is clean. The spawn-prompt rewrite in `resources/modules/skills/partials/review-fanout.md` is prose-only and introduces no naming drift, no dead constructs, and no prohibited patterns. All fictional placeholders (`SPEC-NNNN/T-NNN`, `<persona>`, `<pass|blocking>`) follow the project convention. The thin verdict-element example is consistent with the form the downstream Step 1 completeness check reads back.

The two test-assertion updates in `speccy-cli/tests/skill_packs.rs` are non-vacuous: each new `body.contains(...)` assertion gates a real behavioral property (the rendered skill carries the self-append CLI invocation string), passes only when the module source encodes the correct contract, and fails on any reversion to the old "return a review-element block" wording. No banned `[i]` indexing, no `unwrap()`, no `#[allow(...)]`, no fabricated constants.

The `(SPEC-0055 REQ-008)` reference in the test comment is in the test suite (not in a shipped module body under `resources/`), so it does not violate the "no real Speccy-repo identifiers in shipped skill bodies" rule.

Reuse survey present; named call sites (`speccy journal append`, `verdict_return_contract.md`) exist and are the actual references in the diff. No reinvention found.
</review>
<review date="2026-06-10T07:24:50Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
Round-2 fix is internally consistent; no correctness defects at or above the 80% confidence bar.

Verified against the round-1 docs blocker (the only blocking finding):

1. Spawn prompt rewired correctly. `resources/modules/skills/partials/review-fanout.md:12-26` now directs each reviewer to `speccy journal append SPEC-NNNN/T-NNN --block review --persona <persona> --verdict <pass|blocking> --model <your-model>` with the body on stdin, then return a thin self-closing verdict element (persona/verdict/model/rationale). Selector (`SPEC-NNNN/T-NNN`), block (`--block review`), and flags match the append contract and `verdict_return_contract.md`. The old contradiction is replaced with "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal."

2. No leftover contradiction anywhere. Greps for "do not edit the journal file" and the old "return a review element block" return empty across `resources/`, `.claude/`, `.agents/`, `.codex/`.

3. Ejected copies match. `just reeject` reports "0 created, 0 overwritten, 36 unchanged" — sources and ejected packs are byte-current. The corrected spawn prompt is present in the rendered `.claude/skills/speccy-review/SKILL.md` and `.agents` render, plus the orchestrate flows.

4. Test self-consistent with the new prompt. `speccy-cli/tests/skill_packs.rs::speccy_review_skill_prefers_native_subagents` now asserts, for both rendered hosts, that the spawn prompt contains `speccy journal append SPEC-NNNN/T-NNN --block review` and a verdict element opening — both needles map to the exact rendered spawn-prompt text (source lines 18 and 20). Assertions are mutually consistent with the prompt and not vacuous. Full skill_packs suite: 45 passed, 0 failed.

5. No round-1 WIP regressed. `speccy task transition` still present in `reconcile-policy.md` (5x) and `phases/speccy-work.md` (4x); the journal-scoped rollback pathspec `':!.speccy/specs/*/journal/'` in `vet-phases.md` (6x) is undisturbed; the Step 1/2/3 consolidation prose in `review-fanout.md` was left unchanged per the blocker directive.
</review>
<review date="2026-06-10T07:25:03Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="pass" round="2">
Round-2 fix verified. The per-reviewer spawn prompt in `resources/modules/skills/partials/review-fanout.md` (lines 12-26) now correctly instructs `speccy journal append SPEC-NNNN/T-NNN --block review --persona <persona> --verdict <pass|blocking> --model <your-model>` with body on stdin, then return a thin self-closing verdict element. The old "do not edit the journal file" prohibition is replaced with "Do not edit TASKS.md. The journal write goes through `speccy journal append` — do not use file-editing tools on the journal." The phrase "do not edit the journal file" is absent from all of `resources/`, `.claude/`, `.agents/`, and `.codex/`. The downstream Step 1/2/3 consolidation prose is unchanged. The ejected copies at `.claude/skills/speccy-review/SKILL.md` and `.agents/skills/speccy-review/SKILL.md` both carry the corrected spawn prompt. The `verdict_return_contract.md` two-step contract (append + thin verdict) is consistent with the spawn prompt. The `reviewer-docs.md` ejected agent definition correctly renders the self-append contract. The `speccy_review_skill_prefers_native_subagents` test was updated to assert the self-append path and now pins the correct contract.
</review>
<review date="2026-06-10T07:25:19Z" model="claude-opus-4-8[1m]/high" persona="architecture" verdict="pass" round="2">
Round-2 change resolves the round-1 docs blocker with no architectural regression. The orchestrate/review contract chain is internally consistent end-to-end.

The fix is surgically scoped to the one inconsistency round-1 flagged: the per-reviewer spawn prompt in `resources/modules/skills/partials/review-fanout.md` (lines 13-26) now directs each reviewer to `speccy journal append ... --block review --persona/--verdict/--model` with body on stdin, then return a thin self-closing verdict element — byte-aligned with the persona's own agent definition via `resources/modules/personas/verdict_return_contract.md` (which every `reviewer-*` body includes, and which itself pulls in `no_tasks_md_writes.md`). The pre-T-007 "return a review block / do not edit the journal file" wording, which actively prohibited the correct CLI-mediated write path, is gone from `resources/` and all four ejected render sites (verified: grep for "do not edit the journal file" returns empty across `resources/`, `.claude/`, `.agents/`, `.codex/`).

The spawn prompt to verdict_return_contract to Step 1/2/3 consolidation to persona-body loop is now closed with no phantom transcription step: Step 1 reads `journal show --block review --round latest` (the reviewers' appended blocks, not their thin returns), Step 2 flips via `task transition`, Step 3 consolidates via `journal append --block blockers`. Step 1/2/3 was already correct in round 1 and is untouched. No surviving positive "transcribe a reviewer block" instruction (all remaining "transcribe" hits are negations). No layering boundary moved — lifecycle writes still route through the CLI verbs; CLI remains the deterministic write authority; no agent-behavior knob leaked (principle 2 intact). The journal-scoped rollback design from round 1 is unchanged this round, as expected. The regression pin (`speccy_review_skill_prefers_native_subagents`) was correctly flipped to assert the self-append contract for both hosts. No new DEC required: this refines the existing T-007 self-append contract rather than revisiting a decision.
</review>
