---
spec: SPEC-0055
task: T-005
generated_at: 2026-06-09T00:00:00Z
---

<implementer date="2026-06-09T00:00:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — covered REQ-006, the suggested
  files (verified stale: there is no `speccy-cli/src/commands/` dir;
  the journal command lives at `speccy-cli/src/journal.rs`), and the
  neighbouring output modules. Decisions:
  reuse-as-is — `speccy_core::parse::parse_journal_xml` and
  `parse_vet_xml` already parse both journals into the typed
  `JournalDoc` / `VetDoc`; `speccy_core::task_lookup::{parse_ref,
  find}` resolve task selectors; `speccy_core::workspace::{find_root,
  scan}` discover the workspace; the `schema_version`-first serde
  envelope convention from `next_output.rs` is reused verbatim. The
  DEC-004 routing discriminant — a `^SPEC-\d{4,}$` bare-spec regex —
  is reused conceptually from `journal.rs` (it is a private fn there,
  not exported, so the show path carries its own copy rather than
  widening the append module's surface). Extend — none; show is a
  read-only command orthogonal to append. Write-fresh — the new
  top-level symbols, with the searches that came up empty noted in
  Completed.
- Completed: Added `speccy journal show <selector> [--json]
  [--round latest|N] [--verdict V] [--block TYPE]`. Two new modules:
  `speccy-cli/src/journal_show.rs` (resolve selector by DEC-004
  routing → read file → parse → apply the three conjunctive filters →
  render) and `speccy-cli/src/journal_show_output.rs` (the
  `FilteredJournal` view model, the `schema_version`-first JSON
  envelopes `JsonTaskJournal` / `JsonVetJournal` behind an untagged
  `JournalShowJson`, and a text renderer writing into an
  `io::Write` sink — matching the `check.rs` `writeln!(out, …)?`
  house style after clippy `format_push_string` flagged a
  `String`-building first draft). Wired `JournalCommand::Show` plus a
  `parse_round_filter` value-parser and a `run_journal_show`
  dispatcher into `speccy-cli/src/main.rs`, and registered both
  modules in `speccy-cli/src/lib.rs`. New top-level symbols and the
  empty searches behind write-fresh: `RoundFilter`, `ShowArgs`,
  `ShowError`, `FilteredJournal`, `FilteredInvocation`, the Json*
  envelope structs, `render_json` / `render_text`, `task_view` /
  `vet_view` — searched `speccy-cli/src` and the `speccy_core::parse`
  re-exports for any existing journal-show / round-filter / journal
  JSON envelope and found none (the only journal command was
  `append`, which neither reads nor renders). The round filter is
  scoped to the file as a whole for the per-task journal and to the
  **last** invocation section for VET.md (rounds reset per section,
  per REQ-006), with a round filter excluding earlier sections so
  "latest" never leaks a prior invocation's blocks.
- Undone: T-006 (`VET-*` lint family), T-007 (loop subagents call
  the CLI verbs), T-008 (orchestrator/reconcile consume the verbs),
  and T-009 (ARCHITECTURE.md) remain their own tasks. Nothing in
  T-005's scope is deferred. The loop does not yet route its journal
  reads through `journal show` — that is T-008's explicit scope — so
  this command ships unused by the skills until then, as the task
  body anticipates.
- Hygiene checks: `cargo test --workspace` exited 0 (0 failed),
  including the new `journal_show` integration binary's 5 tests.
  `cargo clippy --workspace --all-targets --all-features -- -D
  warnings` exited 0. `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (advisories / bans / licenses /
  sources ok; no new dependencies — `serde`, `serde_json`,
  `thiserror`, `regex`, `camino`, `fs-err` are all already in the
  crate).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0055-lifecycle-write-commands/evidence/T-005.md`.
  Roll call for the one CHK under REQ-006:
  - CHK-009 (`--round latest --verdict blocking` returns the single
    blocking latest-round block with `schema_version` 1):
    demonstrated → evidence Scenario 1 covers the
    `round_latest_verdict_blocking_returns_single_block` integration
    test, which asserts the one-element `blocks` array, the
    persona/verdict matching the fixture, and `schema_version` 1.
  The non-CHK done-when bullets are demonstrated for completeness:
  the `--block review --round N` completeness call site (Scenario 2),
  the VET.md spec-selector path (Scenario 3), the missing-file
  non-zero exit (Scenario 4), and the `--json`-toggles-representation
  invariant (Scenario 5).
- Discovered issues: the `^SPEC-\d{4,}$` bare-spec routing regex is
  now duplicated between `journal.rs` (append) and `journal_show.rs`
  (show) — both private. Not consolidated this turn to keep the
  change surgical and avoid widening either module's public surface;
  flagged for a future extraction into a shared selector-routing
  helper if a third journal subcommand lands.
- Procedural compliance: this implementer entry lands directly in
  `journal/T-005.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state` for T-005 flips `pending` → `in-progress` → `in-review`
  across this turn. No shipped skill bodies under `resources/`
  required edits — the only stale signal hit was the task body's
  "Suggested files" hint pointing at a non-existent
  `speccy-cli/src/commands/` directory, which is task-local advisory
  text (not a shipped skill instruction), so no
  friction-to-skill-update was triggered.
</implementer>
<review date="2026-06-10T06:07:50Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-006 is faithfully delivered. `speccy journal show <selector> [--json] [--round latest|N] [--verdict V] [--block TYPE]` lands at `speccy-cli/src/journal_show.rs` + `journal_show_output.rs`, wired in `main.rs`/`lib.rs`.

Done-when mapping verified:
- `--round latest` returns only the highest round's blocks and surfaces the resolved round number (`latest_round`) in the envelope; `--round N` (exact) correctly omits it (`journal_show.rs:226-230`, asserted `latest_round: 2` in CHK-009).
- `--verdict blocking --round latest` returns exactly the blocking latest-round blocks — CHK-009 passes with a length-1 `blocks` array, persona `security`, verdict `blocking`.
- `--block review --round N` lists the round's reviewer personas (the completeness call site); all five round-2 personas appear and the round-1 review plus implementer blocks are filtered out (`block_review_round_n_lists_round_personas`).
- Output is valid JSON with `schema_version: 1` as the first field via the untagged envelope (`journal_show_output.rs:217-224`); a missing journal exits non-zero with a `journal not found` diagnostic (`JournalNotFound`, `missing_journal_exits_nonzero`).

Both REQ-006 behavior Given/When/Then cases are covered: the rounds-1-3 latest+blocking case (CHK-009) and the spec-selector VET.md path (invocation sections and blocks appear in the JSON). The two task-body "semantics to pin down" are honored: `--json` toggles representation not content (text mode renders the identical filtered set, `text_mode_renders_same_filtered_blocks`), and VET.md rounds are scoped to the last invocation section (`journal_show.rs:277-309`, earlier sections excluded under a round filter so "latest" never leaks a prior invocation's blocks).

Persona fixtures (`business`, `tests`, `security`, `style`, `docs`) are all valid against `speccy-core/src/personas.rs` `ALL` — no fabricated personas. Filters serve exactly the three orchestrator call sites the SPEC named (persona-completeness, blocker read-back, retry-shape detection), with no scope creep: no `speccy context` bundle, no mutation/patch surface, no CLI-authored prose — all read-only, consistent with the non-goals. The 5 integration tests pass and the evidence at `evidence/T-005.md` is a genuine red-then-green trail mapped per-done-when. No open question was silently resolved.
</review>
<review date="2026-06-10T06:08:06Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-005's tests genuinely exercise the real `speccy journal show` behaviour and survive adversarial scrutiny.

Coverage and evidence: REQ-006's sole scenario CHK is CHK-009 (confirmed via `speccy check SPEC-0055/T-005`, one scenario rendered). The journal's Evidence field points at `.speccy/specs/0055-lifecycle-write-commands/evidence/T-005.md`, which exists and labels CHK-009 `demonstrated` against `round_latest_verdict_blocking_returns_single_block` plus four named tests for the non-CHK done-whens. Every named test exists verbatim in the under-review diff (`speccy-cli/tests/journal_show.rs`). The evidence command (`cargo test -p speccy-cli --test journal_show`) is correctly scoped per-test, not the workspace-wide hygiene run, and the red (verb rejected at argparse, no command existed) / green (named test passing) narratives differ materially.

Tests drive the real system under test, not mocks: all five tests invoke the built binary via `assert_cmd::Command::cargo_bin("speccy")` against isolated scratch workspaces and parse real stdout — no mock-of-SUT pattern.

Mutation probes confirm the assertions bite (mutants applied to local untracked copies, then restored; suite re-verified green):
- Disabling the `--verdict` filter: CHK-009 fails (6 blocks vs expected 1).
- Resolving `--round latest` to no-op: CHK-009 fails.
- Flipping `schema_version` 1→2 in the task envelope: CHK-009 fails.

Negative path is specific, not catch-any-error: `missing_journal_exits_nonzero` asserts `.failure()` and `stderr` contains the contractual `journal not found` diagnostic (`ShowError::JournalNotFound`). The conjunctive-filter completeness test (`block_review_round_n_lists_round_personas`) checks both that all five round-2 personas survive and that no `implementer` block leaks. The text-mode test pins the `--json`-toggles-representation-only invariant against the same filtered set. No flake risk: fresh tempdir per test, fixed-literal timestamps, persona comparison sorted before assert, no shared mutable state.

`speccy-cli/tests/journal_show.rs:93` (CHK-009), `:152`, `:202`, `:266`, `:282`. Implementation at `speccy-cli/src/journal_show.rs` and `speccy-cli/src/journal_show_output.rs`.
</review>
<review date="2026-06-10T06:08:17Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security issues. `speccy journal show` is a read-only local CLI that parses a workspace-local journal and renders it to stdout/JSON.

- No auth/authz boundary, no network, no privilege escalation surface — it exposes only local files the caller already has filesystem access to.
- No injection vector: selector resolves through the existing `parse_ref`/`find_task` seam and a `^SPEC-\d{4,}$` routing regex; journal paths are built from already-resolved `task.id` / workspace `spec.dir` plus literal filenames (`journal_show.rs:206-209`, `:260`), so no path-traversal via the selector. Block bodies are emitted via serde_json (escaped) and `writeln!` with the body as an argument, not a format literal — no format-string injection.
- No secret handling, no crypto introduced (`tasks_hash` is read-and-re-emitted from the parsed block, not computed). Error messages disclosing local journal paths are expected CLI behavior, not an information-leak to any untrusted caller.
- No new dependencies; `cargo deny check` reported clean per the implementer journal. The sole `unwrap()` (`journal_show.rs:58`) is a compile-time-literal regex behind `#[expect(..., reason=...)]`, consistent with AGENTS.md.
</review>
<review date="2026-06-10T06:08:27Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All new files (`journal_show.rs`, `journal_show_output.rs`, `tests/journal_show.rs`) follow the project's established conventions cleanly.

- The `#![allow(clippy::expect_used, reason = "...")]` header in the test file matches every sibling test file introduced earlier in this branch (`journal_append.rs`, `journal_append_vet.rs`, `transition.rs`) — consistent with the local pattern even though AGENTS.md prefers `#[expect]`; clippy passes clean with no new warnings.
- The two-module split (`journal_show` for logic, `journal_show_output` for rendering/types) mirrors the existing `next`/`next_output` and `status`/`status_output` pairs exactly.
- `vet_block_round` and `vet_block_verdict` private helpers are not reinvention: `VetBlock::round()` exists in core but is private (not `pub`), so the show-side copies are forced. The implementer survey correctly names this.
- `JournalEntry::round()` and `JournalEntry::element_name()` are public in core and called directly rather than re-implemented.
- `Option::is_none_or` is stable at Rust 1.82; MSRV is 1.95 — no issue.
- `#[must_use]` annotations on `render_json`, `task_view`, and `vet_view` carry descriptive messages, matching the rule files' guidance.
- `#[expect(clippy::unwrap_used, reason = "compile-time literal regex; covered by tests")]` on the `OnceLock` regex matches the identical pattern in `journal.rs` (`bare_spec_selector_regex`) and `vet_xml/mod.rs`.
- No unused imports, dead variables, or orphaned symbols introduced.
</review>
<review date="2026-06-10T06:08:38Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found in `speccy journal show` (T-005). Reviewed the round/verdict/block conjunctive filtering in `speccy-cli/src/journal_show.rs` (task path lines 225-241, VET path lines 277-317) and the JSON/text renderers in `speccy-cli/src/journal_show_output.rs`.

Checked specifically: `--round latest` resolves `target_round`/`latest_round` correctly per CHK-009 (whole-file max for task journals, last-invocation-section max for VET); `--round N` correctly scopes to the last VET section and excludes earlier sections (`target_round.is_none()` guard, line 303); `checked_sub` on `invocations.len()` avoids underflow on an empty doc (line 277); `parse_round_filter` rejects `0` and non-numeric input (1-based rounds, main.rs:250-251); verdict filtering correctly drops verdict-less `implementer`/`blockers` blocks via the `Option` match (lines 324-329); `is_none_or` composition is sound since every task entry carries a round. No off-by-one, inverted condition, swallowed error, or resource-leak path observed. The lone degenerate case (VET `--round latest` when the last section has only round-less blocks, making the round filter a no-op) is below my reporting threshold: every vet section opens with a `drift-review` per T-004, so the realistic path always has a round-bearing block, and the outcome is neither a crash nor wrong results on any reachable documented path.
</review>
