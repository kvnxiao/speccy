---
spec: SPEC-0055
task: T-001
generated_at: 2026-06-09T20:16:54Z
---

<implementer date="2026-06-09T20:16:54Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 / REQ-002, the
  suggested files, and the `task_xml` / `task_lookup` / CLI-command
  neighbours). Decisions:
  - Reuse-as-is — `task_lookup::parse_ref` / `find` + `TaskRef` /
    `TaskLocation` / `LookupError` for selector resolution (the same
    seam `speccy check` uses, so ambiguity / not-found wording
    matches); `workspace::find_root` / `scan`; `Task.span`
    (`ElementSpan` start/end into `TasksDoc.raw`); the existing
    `TaskState` enum + `TaskState::as_str`; `fs_err::write` for the
    file write (mirroring `tasks::commit_frontmatter`); the
    `OnceLock<Regex>` lazy-regex pattern already used throughout
    `task_xml`.
  - Extend — the existing `TaskState` enum, which gained a public
    `parse` wrapper over its private `from_str` (so `main.rs`'s clap
    value parser can validate `--to`); the `parse/mod.rs` re-export
    list gained the new public items rather than a new module.
  - Write-fresh — `classify_transition` + `TransitionKind` +
    `LEGAL_TRANSITION_EDGES` (the legal-graph logic); `splice_task_state`
    + `SpliceError` + the `state_attr_regex` helper (byte-surgical
    rewrite). Searched `task_xml` for an existing attribute-value
    rewriter: `render` reformats (forbidden by REQ-001) and
    `commit_frontmatter` rewrites only frontmatter, so neither fit.
    New CLI module `speccy-cli/src/transition.rs`; searched
    `speccy-cli/src/` for a `commands/` dir (the task's advisory hint
    named `commands/transition.rs`) and found none — the crate uses a
    flat module layout, so the module landed flat to match.
  - New top-level symbols and their accounting: `classify_transition`,
    `TransitionKind`, `LEGAL_TRANSITION_EDGES`, `splice_task_state`,
    `SpliceError`, `TaskState::parse` (extend), `state_attr_regex`
    (private helper) — all write-fresh per the searches above;
    `transition::run` / `TransitionArgs` / `TransitionError` mirror the
    `lock.rs` / `check.rs` command-module shape; `run_transition` /
    `parse_task_state` / `TaskCommand` in `main.rs` mirror the existing
    dispatch + value-parser conventions.
- Completed: Added `speccy task transition <selector> --to <state>`
  end-to-end. In `speccy-core/src/parse/task_xml/mod.rs`:
  `LEGAL_TRANSITION_EDGES` (the closed six-edge set), `classify_transition`
  returning `TransitionKind::{Legal,NoOp,Illegal}` (same-state is a NoOp
  per DEC-003), and `splice_task_state` which locates the `state="..."`
  value within the parsed `<task>` open-tag span (`Task.span`) and
  replaces only those bytes — never round-tripping through `render`.
  `TaskState::parse` exposed for the CLI value parser. Re-exports added
  to `parse/mod.rs`. New CLI module `speccy-cli/src/transition.rs` wires
  resolution → graph check → splice → `fs_err::write`, writing only on a
  legal edge (NoOp writes nothing; illegal / not-found / parse-reject
  leave the file untouched). `main.rs` gained the `task transition`
  subcommand with a clap `value_parser` that rejects any `--to` outside
  the four states at argument-parse time, plus a `run_transition` handler
  reusing `check`'s lookup-error message shapes.
- Undone: Nothing in T-001's scope is deferred. T-002..T-009 (journal
  append, VET grammar, show, lints, skill-pack rewrites, ARCHITECTURE.md)
  are separate tasks per their `<task>` elements and are untouched.
- Hygiene checks: `cargo test --workspace` exited 101 — the *only*
  failure is the pre-existing `speccy-core --test in_tree_specs`
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag`, which is
  red at HEAD (5412196) independent of this task and lives in the
  SPEC-0055/0056 SPEC.md planning artifacts this task never edited (see
  Discovered issues). All other suites pass: all 174 `speccy-core` lib
  unit tests (incl. the 3 new `task_xml::tests` cases) and all
  `speccy-cli` integration tests (incl. 6 new in `tests/transition.rs`).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 (no new dependencies — REQ-001/002 add no crates;
  the locking crate belongs to T-002).
- Evidence: paper trail at
  `.speccy/specs/0055-lifecycle-write-commands/evidence/T-001.md`.
  Roll call for the CHKs under REQ-001 / REQ-002:
  - CHK-001 (byte-surgical rewrite touches only the state value):
    demonstrated → evidence Scenario 1 shows the CRLF + multi-line-body
    + multi-space-attribute fixture rewriting to a single-value diff,
    with prose-embedded `state="completed"` and the sibling task
    untouched.
  - CHK-002 (not-found selector exits non-zero, bytes unchanged):
    demonstrated → evidence Scenario 2 drives the built binary against a
    scratch workspace; `SPEC-0042/T-099` exits non-zero with TASKS.md
    byte-identical.
  - CHK-003 (all 16 ordered state pairs; 6 legal + 4 no-ops succeed, 6
    illegal exit non-zero naming both states): demonstrated → evidence
    Scenario 3 (the 16-pair unit test plus the illegal-edge and
    same-state integration tests).
  No CHK in T-001's scope is `judgment-only`; the transition contract
  is fully scriptable.
- Discovered issues: The repo-wide `speccy-core` test
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag` fails on
  the committed `SPEC-0055` and `SPEC-0056` SPEC.md files, which place
  `</scenario>` immediately before `</requirement>` with no blank line.
  Verified pre-existing: with this task's working tree stashed, the test
  is still red at HEAD (5412196). The mechanical fix (insert the blank
  lines) was attempted and reverted because editing SPEC.md desyncs the
  recorded `spec_hash_at_generation` in TASKS.md (a `hash-drift` stale
  condition surfaced in `speccy status`); reconciling the hash is
  `/speccy-amend`'s job, not a CLI-implementation task's. Left untouched
  and surfaced here so it is fixed via the amend flow rather than
  silently inside T-001.
- Procedural compliance: This entry lands directly in
  `journal/T-001.md`; no `<implementer-note>` was written into TASKS.md.
  The `state` flip from `in-progress` to `in-review` was performed by
  dogfooding the very command this task implements
  (`speccy task transition SPEC-0055/T-001 --to in-review`, exit 0). The
  task's advisory suggested-files hint named
  `speccy-cli/src/commands/transition.rs`; the crate has no `commands/`
  subdirectory, so the module landed flat at
  `speccy-cli/src/transition.rs` — a stale advisory hint, not a shipped
  skill-prose defect, so no `resources/` edit or `just reeject` was
  warranted.
</implementer>

<review date="2026-06-09T20:20:45Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-001 delivers REQ-001 and REQ-002 exactly as specified, with no scope creep into REQ-003..REQ-010.

REQ-001 done-when fully mapped: byte-surgical rewrite touching only the state value (CHK-001 unit test asserts a body-embedded `state="completed"` and a sibling task stay untouched; splice scopes its regex to `[span.start, span.end)`, the documented `<task>` open-tag span at `speccy-core/src/parse/task_xml/mod.rs:267`); qualified + unqualified selectors resolve via the reused `task_lookup` seam with `#[error(transparent)]` `LookupError` so not-found/ambiguity wording matches `speccy check` by construction; not-found exits non-zero with bytes unchanged (CHK-002, `speccy-cli/tests/transition.rs:74`).

REQ-002 done-when fully mapped: the closed six-edge graph plus four same-state no-ops (DEC-003) and six illegal edges are exhaustively verified across all 16 ordered pairs (`classify_covers_all_sixteen_ordered_pairs`); illegal edge names both states and the illegality (`speccy-cli/src/transition.rs:45`); unknown `--to` rejected at argument-parse time via the clap value parser (exit 2). The dropped/error paths the SPEC named (illegal edge, not-found, no-op) are all covered, not just the happy path.

No non-goal violated: no journal append, no VET grammar, no locking crate, no `journal show`. No open question silently resolved — the Changelog shows a single initial entry with no intent shift, and the pre-existing unrelated `in_tree_specs` failure was surfaced for the amend flow rather than fixed inside this task. All 6 integration tests and the 3 new core unit cases pass.

Relevant files: `/Users/kevin/src/speccy/speccy-cli/src/transition.rs`, `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`, `/Users/kevin/src/speccy/speccy-cli/tests/transition.rs`, `/Users/kevin/src/speccy/speccy-cli/src/main.rs`.
</review>

<review date="2026-06-09T20:20:45Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Each of the three task scenarios maps to a real, behavior-exercising test. CHK-001's `splice_rewrites_only_the_state_value_crlf_multiline_unusual_spacing` asserts full-byte equality against a `replacen`-reconstructed expectation over a CRLF + multi-space-attribute + prose-embedded `state="completed"` fixture, so it would catch a render-roundtrip, a global string replace, or a body-touching regex — directly gating the REQ-001 byte-surgical invariant. CHK-003's `classify_covers_all_sixteen_ordered_pairs` exhausts all 16 ordered pairs with independent bucket cross-checks and exact 6/4/6 counts. CHK-002's `not_found_selector_exits_nonzero_and_leaves_bytes_unchanged` drives the built binary and asserts non-zero exit plus byte-identical TASKS.md; the illegal-edge test asserts the specific `not in the legal state graph` diagnostic naming both states, not a generic any-error catch. Integration tests run the full real path (no SUT mocking). Evidence field present, file exists, roll call covers CHK-001/002/003 all `demonstrated` with cites, and every named test appears in the diff; no fabrication pattern matched. I re-ran all 9 tests green. The lone `cargo test --workspace` failure (`in_tree_specs`) is a pre-existing SPEC.md planning-artifact lint unaffected by this task's diff — out of scope.
</review>

<review date="2026-06-09T20:20:45Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security issues in the `speccy task transition` slice. Write target (`spec_dir.join("TASKS.md")`) is workspace-derived, not user-controlled — no path-traversal vector through the selector. `--to` is enum-validated by the clap value parser before resolution; the spliced value is always a hardcoded `TaskState::as_str()`, never raw input, so no injection. The state-rewrite regex `state\s*=\s*"([^"]*)"` is linear (no ReDoS) and anchored to the parsed open-tag span (`speccy-core/src/parse/task_xml/mod.rs` `splice_task_state` / `state_value_range`), so body-embedded or sibling `state="..."` substrings cannot be matched — confirmed by the CHK-001 test. Slicing uses `.get(..).unwrap_or("")` not panicking index; the lone `unwrap()` is a compile-time-constant regex under `#[expect(...)]`. Error messages leak only selectors/spec-IDs/state names, no sensitive data. No new dependencies (cargo deny clean). The read-resolve-write lacks a file lock, but that is a local data-integrity concern (not an auth-boundary TOCTOU), and the per-file lock plus single-writer serialization are explicitly scoped to T-002 / the orchestrator — out of scope here.
Relevant files: `/Users/kevin/src/speccy/speccy-cli/src/transition.rs`, `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`, `/Users/kevin/src/speccy/speccy-cli/src/main.rs`.
</review>

<review date="2026-06-09T20:20:45Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`speccy-core/src/parse/task_xml/mod.rs:1150-1152` places the three new `use super::` imports mid-module, after the last pre-existing test (line 1148), rather than with the established import block at the top of `mod tests` (lines 948-955). Every other `use` in the test module is grouped at the top immediately after `mod tests {`; inserting new imports elsewhere breaks the file's own convention. The three lines should be moved up to join the existing `use super::` cluster at lines 948-951.
</review>

<review date="2026-06-09T20:20:45Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found in SPEC-0055/T-001. The six legal edges in `LEGAL_TRANSITION_EDGES` match the SPEC exactly; `classify_transition` routes NoOp/Legal/Illegal without inverted conditions (verified by the 16-pair test counting 6/4/6). `splice_task_state` (speccy-core/src/parse/task_xml/mod.rs:204) confines its regex match to the open-tag span `[span.start, span.end)`, so body-prose `state="..."` cannot match, and `id`/`covers` are pattern-validated so no parseable Task carries a spurious `state=`; offset arithmetic is byte-correct and degrades to a clean `SpliceError` on a bad span rather than panicking. All non-Legal paths (NoOp, Illegal, not-found, splice error) return before `fs_err::write`, leaving TASKS.md byte-identical. No resource leak or Option/Result mishandling. Core and CLI tests pass.
</review>

<blockers date="2026-06-09T20:20:45Z" round="1">
Move the three new `use super::` test imports to the established import cluster at the top of `mod tests`; all other personas passed.
- style: `speccy-core/src/parse/task_xml/mod.rs:1150-1152` places three new `use super::` imports mid-module (after the last pre-existing test at line 1148) instead of with the existing import block at the top of `mod tests` (lines 948-955). Move the three lines up to join the existing `use super::` cluster at lines 948-951.
</blockers>

<implementer date="2026-06-09T20:34:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Round-2 amend addressing a single non-reuse (style /
  import-ordering) blocker; no full area-map re-survey is owed (the
  round-1 map stands) and no new top-level symbol is introduced, so
  the per-symbol floor adds nothing this round. The three moved items
  (`TransitionKind`, `classify_transition`, `splice_task_state`) are
  the same round-1 write-fresh symbols, now imported from their
  canonical cluster rather than re-declared.
- Completed: Addressed the round-1 style blocker by relocating the
  three `use super::TransitionKind` / `use super::classify_transition`
  / `use super::splice_task_state` imports out of the mid-module
  position (they previously sat after the last pre-existing test) and
  into the established `use super::` cluster at the top of `mod tests`,
  interleaved in alphabetical order with the existing
  `ALLOWED_TASK_STATES` / `TASKS_ELEMENT_NAMES` / `TaskState` / `parse`
  imports. No production code, test bodies, or behaviour changed — this
  is a pure import-relocation amend to the prior pass's WIP in place.
- Undone: Nothing in T-001's scope is deferred. The blocker is fully
  resolved; no other blockers were raised.
- Hygiene checks: `cargo test --workspace` exited non-zero (101) — the
  *only* failure remains the pre-existing
  `speccy-core --test in_tree_specs`
  `every_in_tree_spec_md_has_blank_line_after_each_close_tag`, which is
  red at HEAD (5412196) and lives entirely in the committed SPEC-0055 /
  SPEC-0056 SPEC.md planning artifacts this task never edits
  (`git status --porcelain` shows both SPEC.md files unmodified by this
  task). Both the round-1 tests and correctness personas accepted this
  same failure as out-of-scope. This task's own suites are green:
  `cargo test -p speccy-core --lib task_xml::tests` (incl. the relocated
  `classify_covers_all_sixteen_ordered_pairs` and
  `splice_rewrites_only_the_state_value_crlf_multiline_unusual_spacing`)
  and `cargo test -p speccy-cli --test transition` (6/6) both pass.
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0 (the moved
  imports are formatter-clean in their new position). `cargo deny check`
  exited 0.
- Evidence: paper trail at
  `.speccy/specs/0055-lifecycle-write-commands/evidence/T-001.md`
  (unchanged this round — no behaviour changed). Roll call for the CHKs
  under REQ-001 / REQ-002 (all still demonstrated by the same tests,
  which pass after the relocation):
  - CHK-001 (byte-surgical rewrite touches only the state value):
    demonstrated → `splice_rewrites_only_the_state_value_crlf_multiline_unusual_spacing`
    still green after the import move.
  - CHK-002 (not-found selector exits non-zero, bytes unchanged):
    demonstrated → `not_found_selector_exits_nonzero_and_leaves_bytes_unchanged`
    in `speccy-cli/tests/transition.rs`, green.
  - CHK-003 (all 16 ordered state pairs; 6 legal + 4 no-ops succeed, 6
    illegal exit non-zero naming both states): demonstrated →
    `classify_covers_all_sixteen_ordered_pairs` plus the illegal-edge /
    same-state integration tests, green.
  No CHK in scope is judgment-only.
- Discovered issues: The pre-existing `in_tree_specs` SPEC.md
  blank-line-after-close-tag failure persists (surfaced in round 1).
  Still out of this task's scope: the mechanical fix desyncs the
  recorded `spec_hash_at_generation` and belongs to the `/speccy-amend`
  flow, not a CLI-implementation task. Left untouched.
- Procedural compliance: This entry lands directly in
  `journal/T-001.md`; no `<implementer-note>` was written into TASKS.md.
  Both state flips this round (`pending`→`in-progress`→`in-review`) were
  performed by dogfooding the command this task implements
  (`speccy task transition SPEC-0055/T-001 --to ...`, exit 0 each). No
  `resources/` edit or `just reeject` was warranted — the blocker was a
  local import-ordering nit, not a shipped-skill-prose defect.
</implementer>

<review date="2026-06-09T20:28:37Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
T-001 delivers REQ-001 and REQ-002 exactly as the SPEC promises, with no scope creep into REQ-003..REQ-010 and no dropped error paths. Round-2 import-relocation blocker confirmed resolved (the three new `use super::` imports now sit in the grouped cluster at `speccy-core/src/parse/task_xml/mod.rs:951-954`, no mid-module imports remain); that was a pure style fix with no behavior change.

REQ-001 done-when fully mapped: byte-surgical rewrite touching only the state value (CHK-001 asserts full-byte equality over a CRLF + multi-space-attribute + prose-embedded `state="completed"` fixture; `splice_task_state` scopes its regex to the `[span.start, span.end)` open-tag range so body and sibling `state="..."` can't match); qualified + unqualified selectors resolve via the reused `task_lookup` seam (`unqualified_selector_resolves` covers the bare `T-001`); not-found exits non-zero with bytes unchanged (CHK-002 drives the built binary). The "same errors as `speccy check`" promise holds in substance — `run_transition` (`speccy-cli/src/main.rs:229-253`) is a faithful mirror of `run_check`'s `LookupError` rendering, differing only in the correct command-name prefix and the transition-appropriate disambiguation example line.

REQ-002 done-when fully mapped: the closed six-edge graph + four same-state no-ops (DEC-003) + six illegal edges are exhaustively verified across all 16 ordered pairs (`classify_covers_all_sixteen_ordered_pairs`, counts 6/4/6); illegal edge names both states and the illegality and leaves bytes unchanged; unknown `--to` rejected at argument-parse time via the clap value parser (exit 2).

No non-goal violated: no journal/VET/show work, no locking crate (Cargo.toml/deny.toml untouched), no CLI-authored prose (spliced value is always a hardcoded `TaskState::as_str()`), no commit-ownership change. No open question silently resolved. I re-ran the suite: core and CLI integration tests green.

Relevant files: `/Users/kevin/src/speccy/speccy-cli/src/transition.rs`, `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`, `/Users/kevin/src/speccy/speccy-cli/tests/transition.rs`, `/Users/kevin/src/speccy/speccy-cli/src/main.rs`.
</review>

<review date="2026-06-09T20:28:37Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
All three task scenarios map to real, behavior-exercising tests that would catch an obviously-wrong implementation, and the round-2 style blocker (mid-module test imports) is resolved with the three `use super::` imports now in the top cluster of `mod tests` at `speccy-core/src/parse/task_xml/mod.rs:951-954`.

CHK-001 (`splice_rewrites_only_the_state_value_crlf_multiline_unusual_spacing`) asserts full-byte equality against a `replacen`-reconstructed expectation anchored on the full open-tag prefix `id="T-001"   state="pending"`, over a CRLF + multi-space-attribute fixture that embeds `state="completed"` in prose and a sibling `state="pending"` task. A render-roundtrip or a global string replace would fail it. CHK-003 (`classify_covers_all_sixteen_ordered_pairs`) cross-checks every pair against an independent in-test oracle with exact 6/4/6 counts — not a bare equality against the production constant, so it is not vacuous. CHK-002 (`not_found_selector_exits_nonzero_and_leaves_bytes_unchanged`) drives the built binary, asserts `.failure()` + `not found` + byte-identical TASKS.md; the illegal-edge test asserts the specific `not in the legal state graph` diagnostic naming both states.

Evidence field present, roll call covers CHK-001/002/003 all `demonstrated` with specific test cites; every named test appears in the diff and I re-ran all tests green. No fabrication pattern matched. The lone `cargo test --workspace` failure (`in_tree_specs` blank-line lint) is a pre-existing SPEC.md planning-artifact failure red at HEAD with this work stashed and untouched by this diff — out of scope.
</review>

<review date="2026-06-09T20:28:37Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security issues in the `speccy task transition` slice; round-2 change is a pure test-import relocation that introduces no new attack surface.

Re-verified against the current tree:
- No path traversal: the write target is `location.spec_dir.join("TASKS.md")` (workspace-derived), not selector-derived (`speccy-cli/src/transition.rs:120`).
- No injection: `--to` is enum-validated by the clap value parser before resolution, and the spliced bytes are always a hardcoded `TaskState::as_str()`, never raw input.
- No ReDoS / no over-broad match: the regex `state\s*=\s*"([^"]*)"` is linear and confined to the parsed open-tag span via `raw.get(task.span.start..task.span.end)`, so body-embedded or sibling `state="..."` substrings cannot match.
- No panics on attacker-influenceable paths: slicing uses `.get(..).unwrap_or("")` plus `saturating_add`; the lone `unwrap()` is on a compile-time-constant regex under `#[expect(...)]`.
- No sensitive-data exposure: error messages leak only selectors, spec-IDs, and state names.
- No new dependencies (cargo deny clean).
- Missing file lock on the read-resolve-write is a local data-integrity concern, not an auth-boundary TOCTOU; per-file locking is scoped to T-002 / the orchestrator and out of scope here.

Relevant files: `/Users/kevin/src/speccy/speccy-cli/src/transition.rs`, `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`, `/Users/kevin/src/speccy/speccy-cli/src/main.rs`.
</review>

<review date="2026-06-09T20:28:37Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. The three new `use super::` imports (`TransitionKind`, `classify_transition`, `splice_task_state`) are now correctly positioned within the established import cluster at the top of `mod tests` (lines 951-954 in `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`), interleaved alphabetically between the pre-existing `TaskState` and `parse` imports. No other style issues observed in the diff.
</review>

<review date="2026-06-09T20:28:37Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
No correctness defects found in SPEC-0055/T-001 (round 2).

The round-2 change is a pure test-import relocation (the three `use super::` imports now sit in the top `mod tests` cluster at `speccy-core/src/parse/task_xml/mod.rs:951-954`); no production logic, test body, or behavior changed, and all core and CLI tests pass.

Re-verified the core logic that round 1 cleared, still sound:
- `classify_transition` routes NoOp/Legal/Illegal with no inverted condition; the 16-pair test counts exactly 6/4/6.
- `splice_task_state` confines its regex match to the open-tag span `[span.start, span.end)`. `ElementSpan.end` is the open-tag-line end and the scanner is line-oriented, so the single-line `<task ...>` open tag always contains the `state="..."` attribute, while body-prose `state="..."` cannot match. `id`/`covers` are pattern-validated, so no parseable Task carries a spurious `state=` earlier in the line. Offset arithmetic uses `saturating_add` and `.get(..)` with `unwrap_or("")`, degrading a corrupt span to a clean `SpliceError::NoStateAttribute` rather than panicking.
- In `speccy-cli/src/transition.rs`, every non-Legal arm (NoOp, Illegal, lookup error, splice error) returns before `fs_err::write`, so TASKS.md stays byte-identical on all error/no-op paths. No resource leak, no `Option`/`Result` mishandling.

Relevant files: `/Users/kevin/src/speccy/speccy-core/src/parse/task_xml/mod.rs`, `/Users/kevin/src/speccy/speccy-cli/src/transition.rs`, `/Users/kevin/src/speccy/speccy-cli/src/main.rs`.
</review>
