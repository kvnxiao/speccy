# Evidence for SPEC-0055 T-003

Red-then-green paper trail for the `vet_xml` parser (REQ-004). The
proof lives in the in-module `#[cfg(test)]` suite at
`speccy-core/src/parse/vet_xml/mod.rs`. Each scenario below cites a
test by name; "red" is the pre-implementation state (the module and
its `parse` entry point did not exist, so every fixture failed to
compile / had no parser to call), "green" is the committed test
passing under `cargo test -p speccy-core --lib vet_xml`.

## Session 2026-06-09-T003-attempt-1

### Scenario 1 — Full-grammar VET.md parses with structure intact (REQ-004 done-when: acceptance)

Drives the REQ-004 done-when "parser accepts the VET.md files the
current vet skill produces" against a fixture carrying frontmatter,
one `## Invocation 1` section, all five block types in order
(`drift-review`, `holistic-fix`, `simplifier-scan`,
`simplifier-apply`), and a terminal `gate`.

<red>
Before this task, `speccy_core::parse::vet_xml` did not exist; the
fixture had no `parse` to call and the test could not compile.
</red>
<green>
`cargo test -p speccy-core --lib vet_xml`
test parse::vet_xml::tests::full_grammar_parses_with_structure_intact ... ok
Asserts spec, one invocation (number=1, date intact), block count 5,
the block-name sequence, and the typed attributes on the first
`drift-review` and the terminal `gate`.
</green>

### Scenario 2 — Each grammar violation is rejected with a diagnostic (REQ-004 done-when: rejection)

Drives the REQ-004 done-when "rejects unknown blocks, unknown
attributes, out-of-domain verdict values, a non-terminal `gate`, and
a second `gate` in one invocation section." One fixture per rule.

<red>
No parser existed; the rejection contract was unenforced.
</red>
<green>
`cargo test -p speccy-core --lib vet_xml`
- unknown_block_is_rejected ... ok          (`UnknownMarkerName` "drift-revue")
- unknown_attribute_is_rejected ... ok       (`UnknownMarkerAttribute` "extra")
- out_of_domain_verdict_is_rejected ... ok   (`InvalidJournalAttribute` verdict=maybe)
- non_terminal_gate_is_rejected ... ok       (`MalformedMarker` "not the last block")
- two_gates_in_one_section_is_rejected ... ok(`MalformedMarker` "exactly one is allowed")
- missing_gate_in_section_is_rejected ... ok (`MalformedMarker` "no terminal `<gate>`")
</green>

### Scenario 3 — Per-section round sequence and partitioning (REQ-004 behavior)

Drives the REQ-004 behavior bullets that rounds reset per invocation
section and blocks partition by `## Invocation` heading.

<red>
No parser existed.
</red>
<green>
`cargo test -p speccy-core --lib vet_xml`
- round_reset_per_section_is_legal ... ok        (two sections each opening at round 1)
- two_invocation_sections_partition_by_heading ... ok
- first_round_not_one_is_rejected ... ok         (`InvalidJournalRoundSequence`)
- round_skip_is_rejected ... ok                  (`InvalidJournalRoundSequence` "skipped")
- foreign_html_in_body_is_tolerated ... ok       (foreign `<details>` flows through as body)
</green>

## Hygiene gate (catch-all for `hygiene` CHKs)

All four standard gates run green at the exit transition:

- `cargo test --workspace` — exit 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0
- `cargo +nightly fmt --all --check` — exit 0
- `cargo deny check` — exit 0
