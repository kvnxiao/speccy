# Evidence for SPEC-0055 T-006

Red-then-green paper trail for the `VET-*` lint family (REQ-007). The
proof lives in the CLI integration suite at
`speccy-cli/tests/lint_vet.rs`, which drives `speccy verify --json`
against scratch workspaces, plus the registry snapshot at
`speccy-core/tests/lint_registry.rs`. "red" is the pre-implementation
state — no `VET-*` code existed in the registry and `speccy verify`
never parsed `journal/VET.md`; "green" is the committed test passing
under `cargo test --workspace`.

## Coverage rule

REQ-007's only scenario CHK is CHK-010. It is `demonstrated` below.
The done-when bullets that are not scenario CHKs (the VET-002
missing-gate-in-non-final-section path, the block-after-gate path, the
"VET.md produced by append passes clean" path, and the
"no VET.md → no diagnostic" path) are also each demonstrated by a
named integration test and recorded here for completeness.

## Session 2026-06-09-T006-attempt-1

### Scenario 1 — `verdict="maybe"` drift-review fires VET-001 and gates verify (CHK-010)

Drives CHK-010: a fixture `journal/VET.md` carrying a
`<drift-review verdict="maybe" ...>` block on an `implemented` spec,
linted by `speccy verify --json`.

<red>
Before this task, `speccy verify` never opened `journal/VET.md`, and
the lint REGISTRY carried no `VET-*` code. A grammar-violating VET.md
produced zero diagnostics and `verify` exited 0.
</red>
<green>
`cargo test -p speccy-cli --test lint_vet`
test out_of_domain_verdict_fires_vet_001_and_gates ... ok
Asserts `verify --json` exits 1, the `lint.errors` array carries a
`VET-001` diagnostic, and that diagnostic's `file` ends with `VET.md`.
(The `implemented` spec status keeps the error gating; an in-progress
spec would demote it to info per `partition_lint`, matching the RPT-*
posture.)
</green>

### Scenario 2 — non-final section without a terminal gate fires VET-002 (done-when / behavior)

Drives the done-when "an invocation section other than the last lacks
a terminal `gate`" and the REQ-007 behavior scenario.

<red>
No VET.md parse path existed in verify; the gate-structure rule was
unreachable from lint.
</red>
<green>
`cargo test -p speccy-cli --test lint_vet`
test non_final_section_without_gate_fires_vet_002_and_gates ... ok
Asserts a two-section VET.md whose first section lacks a gate exits 1,
`lint.errors` carries `VET-002`, and — critically — does NOT also
carry `VET-001`, proving the gate-structure violation routes to the
dedicated `ParseError::VetGateStructure` variant rather than the
generic grammar bucket.
</green>

### Scenario 3 — a block after the gate fires VET-002 (done-when)

Drives the done-when "any block follows a `gate` within its section."

<red>
Unreachable from lint before this task.
</red>
<green>
`cargo test -p speccy-cli --test lint_vet`
test block_after_gate_fires_vet_002 ... ok
Asserts a section with a `<simplifier-scan>` after the terminal
`<gate>` exits 1 with a `VET-002` error.
</green>

### Scenario 4 — a well-formed VET.md emits no VET-* diagnostic (done-when)

Drives the done-when "a workspace whose VET.md was produced solely by
`journal append` passes with no `VET-*` diagnostics."

<red>
No clean-path assertion existed.
</red>
<green>
`cargo test -p speccy-cli --test lint_vet`
test well_formed_vet_md_fires_no_vet_diagnostic ... ok
Asserts a single-section, gate-terminated VET.md produces no `VET-*`
code in any of the `errors` / `warnings` / `info` buckets.
</green>

### Scenario 5 — a spec without VET.md emits no VET-* diagnostic (done-when / behavior)

Drives the done-when "a spec directory without VET.md produces no
`VET-*` diagnostics" and the REQ-007 behavior "no VET.md anywhere."

<red>
No existence gate existed because the lint did not exist.
</red>
<green>
`cargo test -p speccy-cli --test lint_vet`
test absent_vet_md_emits_no_vet_diagnostic ... ok
Asserts a spec with no `journal/VET.md` produces no `VET-*` code in
any bucket — the existence gate inside `rules::vet::lint` short-
circuits before any parse.
</green>

### Scenario 6 — registry pins VET-001 and VET-002 as errors (done-when: envelope counts VET-* as errors)

Backs the done-when "the JSON envelope counts it as an error." The
gating exit in Scenarios 1–3 follows from both codes being registered
`Level::Error`, which the snapshot pins.

<red>
The REGISTRY snapshot at `tests/snapshots/lint_registry.snap` had no
`VET-*` rows.
</red>
<green>
`cargo test -p speccy-core --test lint_registry`
test registry_matches_snapshot ... ok
The snapshot now carries `VET-001\terror` and `VET-002\terror`; the
stability test fails if either code is removed, renamed, or its
severity changes.
</green>

## Hygiene gate (catch-all for hygiene CHKs)

All four standard gates pass on the working tree:

- `cargo test --workspace` — exit 0 (0 failed)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0
- `cargo +nightly fmt --all --check` — exit 0
- `cargo deny check` — exit 0 (advisories / bans / licenses / sources ok)
