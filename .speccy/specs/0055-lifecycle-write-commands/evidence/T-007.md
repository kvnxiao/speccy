# Evidence for SPEC-0055 T-007

Paper trail for REQ-008: the loop's block-author subagents (the
`speccy-work` implementer phase, the seven reviewer personas, and the
three vet personas) now write their own journal blocks via
`speccy journal append` and return a thin `<verdict>`, instead of
returning a full block body for an orchestrator to transcribe. The
date/round/invocation attributes become CLI-stamped. This is a
source-module + ejection change under `resources/`, `.claude/`,
`.agents/`, `.codex/`; no Rust production code changed except one
brittle prose-substring assertion in the skill-pack render test that
referenced wording the contract rewrite removed.

## Coverage rule

REQ-008's only scenario CHK is **CHK-011** (clean tree after
`just reeject`). It is `demonstrated` below. The done-when bullets that
are not scenario CHKs (no module instructs a full-block return; no
module instructs date/round/tasks_hash/invocation computation; the
ejected packs regenerate with the contract; the Codex pack carries it
in prose) are each demonstrated below for completeness via greps over
the regenerated packs and a re-run idempotency check.

## Session 2026-06-09-T007-attempt-1

### Scenario 1 — `just reeject` leaves a clean tree once sources and ejections are committed (CHK-011)

Drives CHK-011: the ejected packs must be byte-identical to a fresh
render of the (edited) sources, so a re-run of `just reeject` after
committing produces zero overwrites.

<red>
Before this task, the source modules (`verdict_return_contract.md`,
`inline_note_format.md`, the reviewer/vet persona bodies, the
`speccy-work` implementer phase, the three journal references) taught
agents to return full block bodies and author `date`/`round`. The
ejected packs matched those old sources. A `just reeject` against the
old sources was a clean no-op only because nothing had changed.
</red>
<green>
After editing the sources, the first `just reeject` overwrote the 14
affected ejected files across the two Claude Code/Codex packs:

```
$ just reeject
...
Init complete: 0 created, 14 overwritten, 22 unchanged.
$ echo $?
0
```

A second, immediate `just reeject` proves idempotency — sources and
ejections are in sync, so re-rendering changes nothing:

```
$ just reeject
...
Init complete: 0 created, 0 overwritten, 36 unchanged.
$ echo $?
0
```

Once this task's source + ejection edits are committed together, a
fresh checkout's `just reeject` + `git status --porcelain` is the
clean-tree state CHK-011 asserts (the second run above is the same
render that a clean checkout reproduces).
</green>

### Scenario 2 — regenerated reviewer pack directs `journal append` + thin verdict, no date/round computation

Drives the REQ-008 done-when "each instructs a `journal append`
invocation plus a thin verdict return" and "no module instructs the
agent to compute date/round". Checked against the regenerated Claude
Code pack and the prose-enforced Codex pack.

<green>
```
$ grep -c "speccy journal append SPEC-NNNN/T-NNN --block review" .claude/agents/reviewer-business.md
(matches: command present)
$ grep "<verdict persona=\"business\"" .claude/agents/reviewer-business.md
<verdict persona="business" verdict="pass|blocking" model="<your-model>" rationale="<one line>" />
$ grep -n "CLI is the sole authority for the block's `date` and `round`" .claude/agents/reviewer-business.md
(present — instructs the agent NOT to supply date/round)
$ grep -n "speccy journal append" .codex/agents/reviewer-tests.toml
162:speccy journal append SPEC-NNNN/T-NNN --block review \
229:<verdict persona="tests" ...
```

The expanded include carries the persona name correctly
(`persona="business"` in the business pack, `persona="tests"` in the
tests pack), proving the shared `verdict_return_contract.md` partial
is inherited by every wrapper.
</green>

### Scenario 3 — regenerated implementer phase lands the handoff via `journal append`

Drives the REQ-008 behavior "the seven-field handoff lands via
`journal append` rather than inside the final return message".

<green>
```
$ grep -n "journal append SPEC-NNNN/T-NNN --block implementer" .claude/agents/speccy-work.md
280:   speccy journal append SPEC-NNNN/T-NNN --block implementer \
$ grep -n "CLI is the sole authority for the block's `date` and `round`" .claude/agents/speccy-work.md
292:   The CLI is the sole authority for the block's `date` and `round`
```

The implementer phase no longer carries a "Required attributes …
author date/model/round" section; it pipes the seven-field body on
stdin and lets the CLI stamp date/round and create the file.
</green>

### Scenario 4 — vet personas append their own blocks

Drives REQ-008 (c): the vet personas append `<drift-review>`,
`<holistic-fix>`, and `<simplifier-scan>`/`<simplifier-apply>` rather
than returning them for transcription.

<green>
```
$ grep -n "speccy journal append SPEC-NNNN --block drift-review" .claude/agents/vet-reviewer.md
(present)
$ grep -n "speccy journal append SPEC-NNNN --block holistic-fix" .claude/agents/vet-implementer.md
(present)
$ grep -n "speccy journal append SPEC-NNNN --block simplifier-scan" .claude/agents/vet-simplifier.md
(present)
$ grep -rn "orchestrator transcribes\|the orchestrator owns the VET.md write" resources/modules/personas/
(no matches — every "transcribe" claim removed; the persona is now the writer via the CLI lock)
```

The simplifier blocks omit `--model` (the CLI requires `--model` only
for implementer/review and the round-bearing vet blocks
drift-review/holistic-fix), matching `journal append --help`.
</green>

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all suites pass; the updated
reviewer_correctness_renders_with_includes_expanded_both_hosts asserts
the new `speccy journal append ... --block review` contract marker)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
Finished `dev` profile
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
advisories ok, bans ok, licenses ok, sources ok
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-007 diff landed.
