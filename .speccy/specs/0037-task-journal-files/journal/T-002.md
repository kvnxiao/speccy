---
spec: SPEC-0037
task: T-002
generated_at: 2026-05-21T23:00:00Z
---

<implementer date="2026-05-21T10:00:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Documentation sweep for the new per-task journal artifact
  and the shrunken closed XML element set in `docs/ARCHITECTURE.md`
  and `AGENTS.md`. Added a new "TASKS.md per-task journal" section
  covering directory location, frontmatter shape, full worked journal
  example, binding rules, element grammar table, attribute schemas
  (including slash-suffix `model` convention documented as
  skill-layer-only), round monotonicity, the JNL-001/002/003 family,
  and the TSK-006 rule. Updated the TASKS.md format section to show
  bare `<task>` under the heading. Updated Phase 3 / Phase 4 bullets
  to describe journal writes. Updated File Layout to list `journal/`
  as a sibling of TASKS.md. Updated Lint Codes section with TSK-006
  and JNL-* entries. Updated Threat Model with two new "loud" items.
  Updated `AGENTS.md` skill-friction loop bullet and appended a new
  "Implementer / reviewer activity records" section.

- Undone: T-003 (verdict_return_contract.md), T-004/T-005/T-006 (skill
  bodies), T-007 (historical migration) still pending as designed.

- Commands run: `cargo run --bin speccy -- init --force --host
  claude-code` (0 created / 0 overwritten / 19 unchanged), `--host
  codex` (same), `git diff --exit-code .claude .codex .agents` (exit
  0), `cargo +nightly fmt --all --check` (0), `cargo clippy
  --workspace --all-targets --all-features --locked -- -D warnings`
  (0), `cargo test --workspace --all-features --locked` (0, all suites
  green; T-007-gated `#[ignore]`d tests stay ignored), `cargo deny
  check` (0).

- Exit codes: 0 across all four hygiene gates plus host-pack drift.

- Discovered issues: Mid-edit, introduced a nested fenced code block
  inside the reviewer-security persona's worked-example `markdown`
  fence. Caught before commit and rewrote the inner example as
  indented prose so the outer fence stays paired. The one remaining
  mention of `<implementer-note>` left in ARCHITECTURE.md is the prose
  sentence that explicitly retires it from the allow-list —
  intentional documentation of the cutover.

- Procedural compliance: Touched only `docs/ARCHITECTURE.md`,
  `AGENTS.md`, and SPEC-0037's TASKS.md state-flip. Did not modify
  any Rust source, the resources/modules/ skill bodies, or any spec
  under `.speccy/specs/0001..0036`. Materialized host packs regenerated
  cleanly with zero drift.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T11:00:00Z" round="1">
T-002 delivers REQ-011's done-when bullets faithfully against the
SPEC. Journal artifact described in `docs/ARCHITECTURE.md` with
directory location, three-field frontmatter, bare-element body, both
binding rules. Journal element grammar table covers required
attributes and the skill-layer slash-suffix `model` convention. Round
monotonicity documented. Closed XML set updated to five with legacy
names explicitly named as retired (historical-context, not
live-workflow). TASKS.md bare-`<task>` shape documented. JNL-001/002/003
registry and TSK-006 carry state-gating semantics. AGENTS.md updates
point activity prose at `journal/T-NNN.md`. Non-goals respected. CHK
greps clean. Host-pack drift exit 0.

Forward observation, not blocking T-002 (T-005 will pick this up):
`docs/ARCHITECTURE.md`'s `# Review` section still describes the
legacy reviewer behaviour in `## CLI invocation` / `## State
transitions` — contradicts Phase 4 prose but does not trip the
literal grep budget.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T11:00:00Z" round="1">
All six T-002 task scenarios verified clean. `docs/ARCHITECTURE.md`
carries `journal/T-NNN.md` × 21 and `JNL-001` × 2; only
`<implementer-note session=...>` / `<retry>` / `<tasks spec=...>`
mentions live in the retirement-cutover paragraph at lines 1124-1127
— historical-context prose. `AGENTS.md` adds the "Implementer /
reviewer activity records" section referencing `journal/T-NNN.md`;
no legacy element names remain. `git diff --exit-code .claude .codex
.agents` exits 0. All four hygiene gates exit 0. The two T-007-gated
`#[ignore]`s stay ignored per the documented red-CI window. Diff
scope narrow: ~286/82 ins/del across `docs/ARCHITECTURE.md` +
`AGENTS.md` only; no Rust source changes attributable to T-002.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T11:00:00Z" round="1">
All four hygiene gates confirmed clean. Host-pack drift check exits 0.
No secrets, credentials, tokens, API keys, or internal hostnames in
the ~2400 lines of added prose. The one email address pattern
(`alice@example.com`) uses the RFC 2606 reserved domain throughout.
The Threat Model additions correctly describe journal lints as
shape/binding/round-sequence validators — no overclaims about
catching malicious journal files or semantic correctness. The
JNL-001/002/003 descriptions consistently qualify them as structural
proof-shape checks, matching the "feedback, not enforcement" stance
throughout the document.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T11:00:00Z" round="1">
Stale prose in `# Review` / `## CLI invocation` and `## State
transitions` contradicts the new journal design. `docs/ARCHITECTURE.md:1582-1595`
still says the reviewer sub-agent "appends an inline note to the task
in TASKS.md" with old-style Markdown bullet examples. Lines 1604-1606
still say a failing review produces "a `Retry:` note summarizing the
blockers." These are the old TASKS.md inline-note patterns that
T-001's TSK-006 lint now rejects. This contradicts three places in
the same file that T-002 did update correctly: Phase 4 description
(lines 440-450), the reviewer persona skeleton (line 1853), and the
TSK-006 entry in the lint table (line 2073). The subsections need to
be rewritten to describe the return-to-orchestrator pattern and
journal write.
</review>

<review persona="docs" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T11:00:00Z" round="1">
`docs/ARCHITECTURE.md:1565-1609` — the "CLI invocation" and "State
transitions" subsections under the Review section — were not updated
by T-002 and still describe the pre-journal inline-TASKS.md workflow.
Line 1568: `speccy review T-003 --persona security` is a ghost CLI
verb. Lines 1582-1595: "The reviewer sub-agent reads the prompt,
performs the review, and appends an inline note to the task in
TASKS.md" with two markdown-bullet worked examples. Lines 1604-1606:
"plus a `Retry:` note summarizing the blockers" — the `Retry:` inline
bullet is the retired format. Line 1609: "keeps persona sub-agents to
a single inline append per review". Line 1574: "the task line from
TASKS.md (with all prior notes)" — post-T-001 prior notes live in
`journal/T-NNN.md`. Line 1579: `.speccy/skills/personas/reviewer-{persona}.md`
project-local override path — retired in SPEC-0027. Fix: rewrite the
"CLI invocation" and "State transitions" subsections to match the
journal-based model already described in Phase 4.
</review>

<blockers date="2026-05-21T12:00:00Z" round="2">
Style and docs both flag the same miss: `docs/ARCHITECTURE.md:1565-1609`
(the "CLI invocation" and "State transitions" subsections under the
`# Review` section) survived T-002's sweep unchanged and still describe
the legacy pre-journal reviewer workflow. Business flagged as forward
observation; only style + docs block.

Fixes required:

1. Rewrite `docs/ARCHITECTURE.md:1565-1596` ("## CLI invocation"
   under `# Review`):
   - Drop the ghost `speccy review T-003 --persona security` verb.
   - Replace "the task line from TASKS.md (with all prior notes)" with
     the journal-based path.
   - Drop the `.speccy/skills/personas/reviewer-{persona}.md` override
     path (retired in SPEC-0027).
   - Replace inline-note-to-TASKS.md prose + bullet examples with the
     journal-based model: reviewer subagent returns `<review>` to
     orchestrator; orchestrator writes serially to `journal/T-NNN.md`.
   - Drop "keeps persona sub-agents to a single inline append per
     review" phrasing.

2. Rewrite `docs/ARCHITECTURE.md:1597-1609` ("## State transitions"):
   - Drop the "`Retry:` note summarizing the blockers" phrasing.
   - Replace with the `<blockers>` block written to the journal on a
     blocking verdict (matching TSK-006 / the new schema).

3. Re-run host-pack regen + four hygiene gates after the rewrite;
   confirm zero drift.

Business / tests / security all returned pass — only style + docs
block, and only on this single section.
</blockers>

<implementer date="2026-05-21T13:00:00Z" model="claude-opus-4.7[1m]/low" round="2">
- Completed: Rewrote `# Review` → `## Invocation` (renamed from
  `## CLI invocation`) and `## State transitions` subsections to match
  the journal-based model. Dropped the ghost `speccy review` CLI verb,
  the retired project-local persona override path, the "inline note
  to TASKS.md" prose + two markdown-bullet worked examples, the
  "single inline append per review" phrasing, and the "`Retry:` note"
  phrasing. Replaced with: invocation goes through `/speccy-review`;
  reviewer returns a `<review>` element to the orchestrator;
  orchestrator writes serially to `journal/T-NNN.md`; blocking
  verdicts append a `<blockers>` block to the journal. Also fixed an
  incidental `/speccy:review` → `/speccy-review` straggler.

- Undone: Nothing in T-002 scope remains.

- Commands run: `cargo test`, `cargo clippy`, `cargo +nightly fmt
  --all --check`, `cargo deny check`, `cargo run --bin speccy -- init
  --force --host claude-code`, `--host codex`, `git diff --exit-code
  .claude .codex .agents`.

- Exit codes: All exit 0. Second `init --force` run idempotent
  (0/0/19 unchanged for both hosts).

- Discovered issues: One stale `/speccy:review` (colon-form) skill-name
  typo in `## State transitions` was renamed to `/speccy-review`
  while rewriting.

- Procedural compliance: Touched only `docs/ARCHITECTURE.md` and
  this task's TASKS.md state-flip. Did not touch other files. T-007
  will migrate this `<implementer-note>` to `journal/T-002.md`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T14:00:00Z" round="2">
Round 2 cleanly addresses both the round-1 forward observation and
the round-1 style + docs blockers. All three round-2 blocker items
land at `docs/ARCHITECTURE.md:1565-1615`. Coherence with Phase 4,
the persona skeleton, TSK-006, and JNL-001/002/003 holds. REQ-011's
done-when bullets are all satisfied. No business-scope regressions.
Pre-existing stale phrasings at `docs/ARCHITECTURE.md:1848` and
`:2348` are pre-round-2 and outside T-002's task-scenarios' literal
grep contract — forward signal for a follow-up sweep, not blocking.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.7[1m]/high" date="2026-05-21T14:00:00Z" round="2">
Round-2 edits hold up cleanly against T-002's `<task-scenarios>`.
Substring grep budget verified: `docs/ARCHITECTURE.md` carries
`journal/T-NNN.md` × 24 and `JNL-001` × 2; `AGENTS.md:184` still
references `journal/T-NNN.md`. Legacy element names only in the
retirement-cutover paragraph. Round-2 fixes landed cleanly. Host-pack
drift = 0 confirmed. Round-2 diff scope is docs-only per `git diff
--stat`, so reported exit-0 across the four hygiene gates is plausible
by construction. No new ungated assertion would warrant a test
scaffold; T-002's task-scenarios are intentionally substring-oriented
per REQ-011's docs-only contract. Forward observations at
`docs/ARCHITECTURE.md:1848` "format of the inline note to append"
in `## Persona definitions` is internally inconsistent with the worked
example at lines 1853-1861; line 2348 still names the retired
`.speccy/skills/personas/reviewer-X.md` override path. Not blocking
T-002.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T14:00:00Z" round="2">
Round-2 changes are docs-only prose in `docs/ARCHITECTURE.md:1565-1615`.
No Rust source changes, no new dependencies, no credentials or tokens
introduced. The orchestrator trust boundary description accurately
states that reviewer sub-agents return a `<review>` element to the
orchestrator and never write to TASKS.md or the journal directly —
the journal-writer concurrency contract is correctly attributed to
the orchestrator session. The TSK-006 claim is accurate. The Threat
Model additions describe journal lints as shape/binding/round-sequence
validators only. No new credentials, tokens, internal hostnames, or
API keys.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T14:00:00Z" round="2">
All three round-1 blocking findings confirmed resolved. Fix 1 — ghost
`speccy review` CLI verb is absent; the section was renamed from `##
CLI invocation` to `## Invocation`; opening sentence reads "The
seven-command CLI has no `review` verb". Fix 2 —
`.speccy/skills/personas/reviewer-{persona}.md` override path is
absent from `## Invocation` (a pre-existing reference at line 2348
predates T-002 round 2 and is outside the three-fix scope). Fix 3 —
inline-note-to-TASKS.md prose and worked examples are absent; replaced
with journal-based model. Fix 4 — `Retry:` note phrasing is absent;
replaced with the `<blockers>` block journal write. Cross-coherence
with Phase 4 and the persona skeleton is intact. Hygiene gates reported
exit 0 across all four gates plus host-pack drift check.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-21T14:00:00Z" round="2">
All six round-1 findings are resolved in
`docs/ARCHITECTURE.md:1565-1615`. Ghost `speccy review` verb gone.
"task line from TASKS.md (with all prior notes)" replaced with
journal-path bullet. `.speccy/skills/personas/reviewer-{persona}.md`
override path gone from `## Invocation`. Inline-note-to-TASKS.md prose
and worked examples gone. `Retry:` note phrasing gone — replaced with
the `<blockers>` block written to `journal/T-NNN.md`. "single inline
append per review" phrasing replaced with "keeps the journal the
single source of truth for review history". Cross-coherence with
Phase 4, persona skeleton, TSK-006 lint table, and File Layout all
hold. Pre-existing inconsistency at line 2348 under `# Operational
Details > ## Persona file resolution` is outside round-2 scope; not a
T-002 regression.
</review>
</content>
</invoke>
