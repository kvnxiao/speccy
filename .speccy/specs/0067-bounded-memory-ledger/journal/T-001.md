---
spec: SPEC-0067
task: T-001
generated_at: 2026-06-21T19:42:34Z
---

<implementer date="2026-06-21T19:42:34Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Prose-only edit to one reference file; introduces no new top-level symbols. Reused the established one-line ledger format already live in `.speccy/MEMORY.md` (`TRIGGER → CORRECTIVE RULE. [SPEC-NNNN/T-NNN]`) and matching REQ-002's `<behavior>` illustration, rather than inventing a new shape — the worked example mirrors that exact form. No code searched; no fresh symbol added.
- Completed: Rewrote `resources/modules/references/memory-ledger.md` from the four-part shape to the one-line shape — trigger, corrective rule, bracketed `[SPEC-NNNN/T-NNN]` provenance tag, no convention/mistake or history field. Added a "What earns an entry" bar (durable across specs + not already gate-enforced). Rewrote the worked example to one line using only the `SPEC-0042/T-001` carve-out ids. Updated the provenance bullet to describe the bracketed tag and the drop-task-segment-only-for-spec-wide-lesson rule, while keeping the abstract-wording discipline and provenance-resolvability requirement. Ran `just reeject` (1 ship wrapper pair regenerated via its `{% include %}` of the reference).
- Undone: Nothing in REQ-002 scope. Deliberately did not touch the ship-phase body's own capture prose (still says "four-part shape" / "both feeds" / "mistake-flavoured") — that is retro-behavior prose under REQ-001/003/004, owned by sibling T-002.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: REQ-002 CHK roll-call — CHK-002: judgment-only. It is an explicit dogfood/manual-inspection check (DEC-005: no lint enforces the one-line shape; SPEC-0064 boundary keeps the ledger outside every `speccy verify` lint). The reference now documents the one-line shape and its example is a single line of that shape (`resources/modules/references/memory-ledger.md`), and the ejected ship bodies carry that included shape; conformance/tag-resolvability remain semantic properties no prose-substring test may gate.
- Discovered issues: The ejected ship-phase body (`.claude/agents/speccy-ship.md` lines ~142-147 and the `.codex` mirror) still describes the four-part shape, "both feeds", and "mistake-flavoured" capture in its own prose, which now conflicts with the included one-line reference. That prose is retro-behavior under REQ-001/003/004 and is T-002's reconciliation; flagging it so T-002 closes the loop.
- Procedural compliance: Followed speccy-work first-attempt recipe — context bundle, clean-tree gate (clean), in-progress flip via transition, reuse survey, edit, `just reeject`, four hygiene gates, in-review flip. No source-module friction encountered; no `resources/` instruction needed fixing.
</implementer>
<review date="2026-06-21T19:43:54Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in this slice. Prose-only rewrite of `resources/modules/references/memory-ledger.md` (and its faithful reeject into `.claude/agents/speccy-ship.md` / `.codex/agents/speccy-ship.toml`) describing the one-line ledger entry shape — no code, endpoints, auth boundary, input parsing, secret handling, or crypto touched, so no new attack surface. Reviewed the sensitive-data-exposure angle that applies to a git-tracked, agent-loaded memory file: the documented shape keeps the abstract-wording discipline and a bracketed provenance tag, and never instructs authors to capture verbatim command output, env values, tokens, or absolute paths; dropping the mistake/history narrative strictly narrows captured content, lowering incidental secret/PII leakage. Ejected host bodies match the source module byte-for-byte, so the reeject injected nothing. No new dependency.
</review>
<review date="2026-06-21T19:44:14Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Prose-only reference rewrite with no executable logic to falsify; correctness lane checks for internal consistency and faithful reeject propagation, both hold. `resources/modules/references/memory-ledger.md` documents the one-line shape (trigger, corrective rule, bracketed provenance tag, no narrative field) and the worked example at line 55 is a single line of exactly that shape using only the `SPEC-0042/T-001` carve-out ids — internally consistent with itself and with REQ-002's `<behavior>` illustration. The provenance bullet (lines 42-48) correctly describes the bracketed tag and the drop-task-segment-only-for-a-spec-wide-lesson rule. `just reeject` propagated faithfully: the included region in `.claude/agents/speccy-ship.md` (lines 88+) is byte-consistent with the rendered module, with no stale divergent copy of the include. The one residual inconsistency — the ship body's own capture prose at `.claude/agents/speccy-ship.md:142-147` still naming the four-part shape / "both feeds" / "mistake-flavoured" — is retro-capture behavior under REQ-001/003/004, explicitly out of T-001's REQ-002 scope and correctly handed to sibling T-002; that is a scope/coverage matter, not a correctness defect in this slice.
</review>
<review date="2026-06-21T19:44:28Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. T-001 is a prose-only rewrite of `resources/modules/references/memory-ledger.md` covering REQ-002; its sole CHK-002 is labelled `judgment-only` in the journal Evidence roll call, and that label is correct and not mine to block on — CHK-002 is an explicit dogfood/manual-inspection check (one-line conformance and tag resolvability are semantic properties; per DEC-005 no lint gates the shape, and a prose-substring test would be the banned vacuous-test pattern). No executable project test is owed, so there is no red/green transition to scrutinise. Verified the reference now documents the one-line shape (`memory-ledger.md:19-32`: trigger, corrective rule, bracketed `[SPEC-NNNN/T-NNN]` tag, explicit no mistake/history field), the "What earns an entry" bar (`:10-17`), the provenance bullet describing the bracketed tag and the drop-task-segment-only-for-spec-wide rule while keeping abstract-wording and resolvability discipline (`:42-48`), and the worked example as a single line using only the `SPEC-0042/T-001` carve-out ids matching the REQ-002 `<behavior>` illustration form (`:55`). `just reeject` propagated the identical block into both ejected mirrors (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`). The residual four-part / "both feeds" / "mistake-flavoured" capture prose at `.claude/agents/speccy-ship.md:142-147` is retro-behavior under REQ-001/003/004 (sibling T-002), correctly out of T-001's REQ-002 scope and flagged by the implementer for T-002.
</review>
<review date="2026-06-21T19:44:50Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-002's reference-editing scope is fully satisfied and nothing out-of-scope sneaked in.

Done-when mapping (`resources/modules/references/memory-ledger.md`):
- "reference documents the one-line shape and worked example is a single line of that shape" — met: the "one-line entry shape" section (lines 19-32) names the three parts (trigger, corrective rule, bracketed provenance tag) and states "no mistake or history field"; the worked example (line 55) is one line of the form `TRIGGER → CORRECTIVE RULE. [SPEC-0042/T-001]`, matching REQ-002's `<behavior>` illustration verbatim.
- "provenance tag resolves to a real SPEC (and task when task-specific)" — met: authoring-discipline bullet (lines 42-48) documents the bracketed `[SPEC-NNNN/T-NNN]` form and the rule that the task segment drops to `[SPEC-NNNN]` only for a spec-wide lesson, covering the second `<behavior>` case; the resolvability-not-fabricated requirement is preserved.

Goal/non-goal alignment: the added "What earns an entry" bar (durable-across-specs + not-already-gate-enforced, lines 10-17) makes the reference agree with the ship retro as the task asked, and serves the SPEC goal of a higher capture bar. The abstract-wording discipline (lines 36-40) is retained. Only the `SPEC-0042/T-001` carve-out ids appear — grep confirms no other real Speccy id leaked. No CLI change, no lint, no summary-file or ship-capture-prose edit — consistent with the non-goals and the SPEC's scope boundary.

Cross-file conflict correctly deferred: the ship-phase body's own capture prose still says "four-part"/"both feeds"/"mistake-flavoured" (`resources/modules/phases/speccy-ship.md:80-85`); the SPEC Notes assign that file to the retro step (REQ-001/003/004), which is sibling T-002's scope. The implementer flagged it rather than touching out-of-scope prose — the right boundary.

Reeject verified: both `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml` carry the regenerated one-line shape matching the module (included at `speccy-ship.md:76`). resource_prose_hygiene tests (id-ban + references carve-out) pass.
</review>
<review date="2026-06-21T19:45:01Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. `resources/modules/references/memory-ledger.md` rewritten cleanly to the one-line shape.

Checked:
- No real SPEC/REQ/DEC/CHK/task IDs appear in the resource body; the worked example uses only the permitted `SPEC-0042/T-001` carve-out. No doc-path or rule-file citations in the body.
- The new "What earns an entry" section, "The one-line entry shape" section, and updated provenance bullet all use `SPEC-NNNN/T-NNN` placeholder form, matching AGENTS.md conventions.
- Worked example (`resources/modules/references/memory-ledger.md:55`) is a single line in `TRIGGER → CORRECTIVE RULE. [SPEC-0042/T-001]` form with no multi-part shape.
- Section headings follow the file's existing style.
- Ejected bodies (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`) match the source module after `just reeject` — no shadowing inline copies.
- `.speccy/MEMORY.md` reformatted entries all conform to the one-line shape with `[SPEC-NNNN/T-NNN]` provenance tags; no fabricated identifiers.
- Reuse survey present and honest: no new symbols introduced; survey correctly identifies the existing one-line format already live in `.speccy/MEMORY.md` as the model for the worked example.
- The implementer's "Discovered issues" note (conflicting four-part language in the ship-phase retro body) is correctly scoped to T-002; the reference file itself is clean.
- No suppression annotations, dead code, or false complexity added.
</review>
