---
spec: SPEC-0067
task: T-002
generated_at: 2026-06-21T19:49:16Z
---

<implementer date="2026-06-21T19:49:16Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Prose-only rewrite of one phase body; no new top-level symbols. Reused the one-line entry-shape language and the durable-and-not-already-enforced capture bar already authored in `resources/modules/references/memory-ledger.md` (T-001) rather than inventing fresh phrasing, and the REQ-001/003/004 done-when wording from the bundle. Searched `resources/` for residual "four-part"/"mistake-flavoured"/sentinel phrasing (Grep) to scope the rewrite; the one remaining hit, `memory-ledger-summary.md`, is T-003's named file, left untouched to avoid sibling-task scope creep.
- Completed: Rewrote step 3 of `resources/modules/phases/speccy-ship.md`. Replaced the capture bullet's per-friction mandate and "no durable lesson this loop" sentinel with the two-part bar (durable across specs AND not already enforced by a gate/persona/AGENTS.md-rule), recording-nothing-as-default. Split the old human-gated consolidate-and-dedupe bullet into a "Compact (autonomous)" bullet (refuse-to-append + near-duplicate merge, no human step, never deletes a non-redundant entry, boundedness rests here) and a "Promote (human-gated)" bullet (single gated mutation, removes promoted entry on approval, boundedness independent of promotion firing). Reworded "four-part shape" to one-line; updated intro "three things" → "these things". Left phantom-reference GC bullet intact. Placeholder ids only. Ran `just reeject` (1 overwritten: `.codex/agents/speccy-ship.toml`; `.claude/agents/speccy-ship.md` regenerated, both carry new prose, no Conflict markers).
- Undone: Nothing in scope deferred. `memory-ledger-summary.md` four-part wording is REQ-005/T-003's named deliverable, intentionally not touched here.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: CHK-001 (REQ-001) judgment-only — dogfood check that the capture bar suppresses gate-redundant lessons; the scenario states this is a semantic judgment no prose-substring test can gate. The rewritten capture bullet states the durable-and-not-already-enforced bar, drops the per-friction mandate and the sentinel, and names recording-nothing as default including for gate-already-enforced friction. CHK-003 (REQ-003) judgment-only — same dogfood/semantic-judgment shape; the new Compact bullet describes refuse-to-append + near-duplicate merge as autonomous with no human-approval step and states it never deletes a non-redundant entry. CHK-004 (REQ-004) judgment-only — same shape; the new Promote bullet makes promotion the single human-gated mutation that removes the promoted entry on approval, and states boundedness rests on autonomous compaction not on promotion firing. The second task-scenario (reeject Conflict-free + green hygiene) is demonstrated: reeject regenerated both wrappers with no Conflict against the rendered module and all four hygiene gates are green.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work first-attempt branch. No source-module friction encountered; no `resources/` fix needed beyond the task itself. Edited source under `resources/` and ran `just reeject`, never patched ejected files.
</implementer>
<review date="2026-06-21T19:50:28Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. Prose-only rewrite of the ship-retro step 3 and the memory-ledger reference; no auth boundary, input-validation, injection, secret-handling, or crypto surface is touched, and no dependency changes. Reviewed sensitive-data exposure specifically: the new one-line entry shape drops the mistake/history narrative field, which narrows (not widens) what the ledger records and reduces the chance of incident detail leaking into `.speccy/MEMORY.md`. The provenance tag is a bracketed `[SPEC-NNNN/T-NNN]` reference with no injection vector. SPEC-0064 invariants the goals require are preserved in the body: the ledger stays eject-safe, implementer-only on read, and outside every `speccy verify` lint — no telemetry/log sink added. The human gate is retained exactly where it has security weight: promotion into the durable tier (`AGENTS.md`/rules) stays the single human-approved mutation, while only the can-only-shrink operations (refuse-to-append, dedupe) run autonomously — no privilege is granted to the unattended path to mutate the always-loaded north-star docs. See `resources/modules/phases/speccy-ship.md:78-114`.
</review>
<review date="2026-06-21T19:50:39Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Prose-only rewrite of step 3 in `resources/modules/phases/speccy-ship.md` (lines 78-114); no executable logic. Checked the rewritten control flow for defects in my lane: the four bullets (Capture / Compact / Promote / Phantom-reference GC) describe disjoint, non-contradictory responsibilities. The Compact bullet's "Before appending" clause correctly sequences dedupe ahead of the append despite the bullet appearing after Capture — the ordering is explicit, not ambiguous. The capture predicate (durable across specs AND not-already-enforced) is not inverted and recording-nothing-as-default is internally consistent; "Compact never deletes a non-redundant entry" and "boundedness rests on compaction, not on promotion firing" are stated consistently across the Compact and Promote bullets with no contradiction. The pre-existing single-arg `git diff <base-ref>` guidance is untouched and out of scope. Verified reeject consistency: both ejected wrappers (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`) carry the rewritten "Compact (autonomous)" prose and no stale four-part/mistake-flavoured/sentinel phrasing survives in source or ejected output. No correctness defect at or above the reporting bar.
</review>
<review date="2026-06-21T19:50:59Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass — the rewritten step 3 of resources/modules/phases/speccy-ship.md satisfies every done-when for the three covered requirements with no non-goal creep.

REQ-001: the Capture bullet (speccy-ship.md:80-93) states the durable-and-not-already-enforced two-part bar, explicitly drops the per-friction mandate and the "no durable lesson this loop" sentinel, names the gate-already-enforced-friction case as recording-nothing-by-default, and still records a genuinely new durable lesson — covering all three done-when clauses and both behavior Given/When/Then cases.

REQ-003: the Compact (autonomous) bullet (:95-104) describes refuse-to-append and near-duplicate merge as unattended in orchestrate → ship with no human-approval step, and states it never deletes a non-redundant entry — matching the merge and never-removes-non-redundant behavior clauses.

REQ-004: the Promote (human-gated) bullet (:106-114) makes promotion the single gated mutation, never auto/silent including in an autonomous run, removes the promoted entry from .speccy/MEMORY.md on approval, and asserts boundedness rests on autonomous compaction not on promotion firing — covering all three done-when clauses.

Non-goals honored: prose-only, no CLI verb/lint/enforcement, no hard cap or eviction (never-deletes-non-redundant preserved), no bulk migration pass added. CHK-001/003/004 are judgment-only dogfood checks the SPEC declares unscriptable; the prose embodies each. just reeject propagated the new prose to both .claude/agents/speccy-ship.md and .codex/agents/speccy-ship.toml with no Conflict markers. The lone residual "four-part" hit (memory-ledger-summary.md:7) is REQ-005/T-003's named deliverable, correctly left untouched. Changelog shows a single stable intent entry matching the diff.
</review>
<review date="2026-06-21T19:51:05Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. `resources/modules/phases/speccy-ship.md` rewritten cleanly; both ejected mirrors updated via `just reeject`.

Checked:
- No real SPEC/REQ/DEC/CHK/task IDs in the phase body; placeholder ids `SPEC-NNNN`/`T-NNN` only throughout. `SPEC-0042/T-001` does not appear (correctly — this is a phase body, not the references carve-out).
- No doc-pointer, rule-file, or governance-doc citations in any changed prose (no `per AGENTS.md`, `Core principle N`, `see docs/…` shapes).
- Step 3 bullets correctly renamed: "Capture" replaces "Capture (both feeds)", "Compact (autonomous)" is a new bullet split from the old "Consolidate and dedupe (human-gated)", and "Promote (human-gated)" carries the single-gate mutation. "three things" → "these things" is a minor but accurate update.
- The Phantom-reference GC bullet is untouched, as required.
- Ejected `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml` carry the same rewritten step 3 via the `{% include %}` of `memory-ledger.md` plus the directly-written retro bullets; no shadowing inline copies.
- `.speccy/MEMORY.md` compaction is also in the working tree diff: 4 verbose four-part entries replaced with 3 one-line entries conforming to the `TRIGGER → CORRECTIVE RULE. [SPEC-NNNN/T-NNN]` shape. This is the autonomous compaction dogfood expected for this loop.
- Reuse survey present and honest: implementer correctly reused phrasing from the already-authored `memory-ledger.md` (T-001) and the REQ done-when wording.
- No suppression annotations, dead code, or false complexity introduced.
- Hygiene gates all reported green by implementer; no Rust code changed, so lints are not at risk from this prose-only edit.
</review>
<review date="2026-06-21T19:51:08Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Roll call complete and honest; the one demonstrated claim verified, judgment-only labels are correct, prose matches all three covered scenarios.

Covered CHKs all present: CHK-001 (REQ-001), CHK-003 (REQ-003), CHK-004 (REQ-004), each labelled judgment-only. The label is correct, not a dodge: every scenario describes runtime behaviour of a future ship-retro agent following prose guidance, and DEC-005 explicitly bans any CLI lint over the ledger shape (it would re-import the substring-matching-curated-prose anti-pattern AGENTS.md forbids), so no deterministic test can gate these. No separate `evidence/T-NNN.md` file is required because judgment-only CHKs carry no execution artifact per `.claude/speccy-references/evidence.md`.

Did the persona-review judgment the labels defer to me. The rewritten step 3 of `resources/modules/phases/speccy-ship.md:78-114` satisfies each requirement: Capture (l.80) states the durable-and-not-already-enforced bar, drops the per-friction mandate and the "no durable lesson this loop" sentinel, names recording-nothing as default including for gate-already-enforced friction (REQ-001 / CHK-001); Compact (l.95) describes refuse-to-append and near-duplicate merge as autonomous with no human step and "never deletes a non-redundant entry" (REQ-003 / CHK-003); Promote (l.106) is the single human-gated mutation, removes the promoted entry on approval, and states boundedness rests on compaction not promotion (REQ-004 / CHK-004). Phantom-reference GC left intact as required; placeholder ids only.

The second task-scenario (reeject Conflict-free + green hygiene) is the one `demonstrated` claim, independently verified: no conflict markers in `.claude/agents/speccy-ship.md` or `.codex/agents/speccy-ship.toml`, the rewritten prose propagated to the ejected wrapper, and `cargo test -p speccy-cli --test resource_prose_hygiene` passes 5/5 — including the ID-ban lint over `resources/modules/**` that gates the placeholder-only requirement. No residual "four-part"/"mistake-flavoured"/sentinel phrasing remains in the rewritten module. No executable test is added, which is correct: the requirements are soft-guidance prose the CLI never reads, so there is no real code path a test could drive.
</review>
