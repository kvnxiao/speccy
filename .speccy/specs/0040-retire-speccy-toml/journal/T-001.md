---
spec: SPEC-0040
task: T-001
generated_at: 2026-05-22T23:30:00Z
---

<implementer date="2026-05-22T23:30:00Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- Deleted `speccy-core/src/parse/toml_files.rs` in full (parser
  `speccy_toml`, types `SpeccyConfig` / `ProjectConfig` plus their
  `RawSpeccyConfig` / `RawProject` mirrors, `SUPPORTED_SCHEMA_VERSION`
  const, `guard_schema_version` helper, and the in-module test suite).
- Relocated the shared `read_to_string` helper to a new
  `speccy-core/src/parse/fs.rs` module as `pub(crate) fn read_to_string`.
  Behavior preserved: still wraps `fs_err::read_to_string` errors as
  `ParseError::Io { path, source }`.
- Updated `speccy-core/src/parse/mod.rs`: dropped `pub mod toml_files`,
  added `pub(crate) mod fs`, removed the three re-exports
  (`SpeccyConfig`, `ProjectConfig`, `speccy_toml`).
- Updated import in `speccy-core/src/parse/spec_md.rs` from
  `parse::toml_files::read_to_string` to `parse::fs::read_to_string`.
- Updated three call sites in `speccy-core/src/workspace.rs`
  (`parse_spec_doc`, `parse_one_tasks_xml`, `parse_one_report_xml`)
  from `crate::parse::toml_files::read_to_string` to
  `crate::parse::fs::read_to_string`.
- Removed `ParseError::UnsupportedSchemaVersion` variant from
  `speccy-core/src/error.rs` (DEC-003 — sole emitter is gone).
- Deleted `workspace_speccy_toml_still_parses` test from
  `speccy-core/tests/workspace_loader.rs` (the only external consumer
  of the deleted parser).
- Renamed pre-existing test `no_spec_toml_files_remain_under_speccy_specs`
  to `no_stray_spec_toml_remains_under_speccy_specs` in
  `speccy-core/tests/in_tree_specs.rs` so the test fn name no longer
  contains the literal substring `toml_files` (which would otherwise
  trip CHK-004's case-sensitive `*.rs` ripgrep). The assertion
  semantics are unchanged — still scans for stray `spec.toml` files
  under `.speccy/specs/`.

## Undone

- None within T-001's REQ-002 / REQ-003 / REQ-005 scope. REQ-005's
  CLI-side test rewrites (`scaffold_speccy_toml`,
  `refuse_without_force`, `force_overwrites_shipped_files`,
  `exit-one-conflict`, plus the new `scaffold_gitkeep`) belong to
  T-003 per the TASKS.md split.

## Hygiene checks

- `cargo check --workspace --all-targets --all-features` — exit 0.
- `cargo test -p speccy-core --lib` — 149 passed.
- `cargo test -p speccy-core --test workspace_loader` — 3 passed
  (the relocated helper supports SPEC.md / TASKS.md / REPORT.md parse
  paths end-to-end against real files).
- `cargo test --workspace --exclude speccy-core` — passes.
- `cargo test --workspace` — fails on **two pre-existing** tests in
  `speccy-core/tests/in_tree_specs.rs`:
  `every_in_tree_spec_md_parses_with_xml_parser` (SPEC-0041's SPEC.md
  is missing the required `<changelog>` element) and
  `every_migrated_spec_md_has_blank_line_after_each_close_tag`
  (SPEC-0040 and SPEC-0041 SPEC.md files were authored by the planner
  without a blank line after each `</scenario>` close tag immediately
  before `</requirement>`). Both failures are planner-artifact
  drift — they predate T-001's code surgery, they affect SPEC.md
  source files not Rust source, and they are not within T-001's
  declared `covers` set (REQ-002 / REQ-003 / REQ-005). Flagged for
  the reviewer to route: either to a SPEC-0040 SPEC.md hygiene fix
  outside the implementer loop or as a separate follow-up.

## Evidence

- `speccy-core/src/parse/toml_files.rs` — deleted.
- `speccy-core/src/parse/fs.rs` — new file, ~22 lines.
- `speccy-core/src/parse/mod.rs` — module declaration + re-exports
  updated.
- `speccy-core/src/parse/spec_md.rs` — single `use` line updated.
- `speccy-core/src/workspace.rs` — three call sites updated via
  `replace_all`.
- `speccy-core/src/error.rs` — `UnsupportedSchemaVersion` variant
  removed.
- `speccy-core/tests/workspace_loader.rs` —
  `workspace_speccy_toml_still_parses` deleted.
- `speccy-core/tests/in_tree_specs.rs` — test renamed (substring
  collision fix).
- Workspace-wide ripgrep for `toml_files | SpeccyConfig | ProjectConfig
  | speccy_toml | SUPPORTED_SCHEMA_VERSION | guard_schema_version
  | UnsupportedSchemaVersion` over `*.rs` after the deletion shows
  remaining hits **only** in `speccy-cli/src/init.rs` and
  `speccy-cli/tests/init.rs` — those are T-003's scope (REQ-004 /
  REQ-005 CLI-side).

## Discovered issues

- Pre-existing `cargo test --workspace` failures on SPEC-0040 and
  SPEC-0041 SPEC.md hygiene (see Hygiene checks above). Not in T-001
  scope; surfaced for routing.

## Procedural compliance

- TASKS.md state transitions: `pending` → `in-progress` → `in-review`
  via XML attribute edits, no `<implementer-note>` inlined.
- Journal file created at
  `.speccy/specs/0040-retire-speccy-toml/journal/T-001.md` with the
  required three-field YAML frontmatter (`spec`, `task`,
  `generated_at`) and a `round="1"` `<implementer>` block carrying
  `date`, `model`, `round` attributes.
- No skill-file edits needed this round — the skill body and the
  implementer prompt matched the work done.

</implementer>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="business" verdict="pass" round="1">
T-001's `covers` set (REQ-002 / REQ-003 / REQ-005) is satisfied end-
to-end. REQ-002: `speccy-core/src/parse/toml_files.rs` is deleted in
full; `pub mod toml_files` and the three re-exports
(`speccy_toml`, `SpeccyConfig`, `ProjectConfig`) are gone from
`parse/mod.rs`; `ParseError::UnsupportedSchemaVersion` is gone from
`error.rs` per DEC-003. REQ-003: the helper survives at
`speccy-core/src/parse/fs.rs` as `pub(crate) fn read_to_string`,
preserves the `fs_err::read_to_string` → `ParseError::Io { path,
source }` wrapping contract, and both call sites
(`parse::spec_md::spec_md` and the three `workspace.rs` call sites
in `parse_spec_doc` / `parse_one_tasks_xml` / `parse_one_report_xml`)
are updated. REQ-005: `workspace_speccy_toml_still_parses` is
deleted from `speccy-core/tests/workspace_loader.rs`; the CLI-side
test rewrites belong to T-003 per the TASKS.md split, which the
implementer correctly respected. The CHK-005 surface failure
(`cargo test --workspace` not exit 0) is pre-existing planner-
artifact drift in SPEC-0040 and SPEC-0041 SPEC.md hygiene (blank-
line-after-`</scenario>` and missing `<changelog>`), confirmed by
running the same test against the pre-T-001 tree — it predates this
task's surgery, affects SPEC.md source not Rust source, and is not
within T-001's `covers` set. Implementer correctly flagged it under
`## Discovered issues` for routing rather than silently fixing
adjacent drift. Not a T-001 blocker.
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="tests" verdict="pass" round="1">
Test coverage holds. `cargo check --workspace --all-targets
--all-features` exits 0, proving the helper relocation kept
`parse::spec_md` and `workspace` compiling. `cargo test -p
speccy-core --lib` reports 149 passing (per implementer transcript),
exercising the new `fs::read_to_string` indirectly through SPEC.md /
TASKS.md / REPORT.md parse paths. `workspace_loader.rs` retains its
remaining three tests with `parse::speccy_toml`'s sole external
consumer removed; the deletion is mechanical and the surviving
tests still cover loader paths. The `no_spec_toml_files_remain_…`
→ `no_stray_spec_toml_remains_…` rename is a targeted fix for the
CHK-004 case-sensitive substring collision (the old name contained
the literal `toml_files`); assertion semantics are byte-identical.
The `cargo test --workspace` failures in `in_tree_specs.rs`
(`every_in_tree_spec_md_parses_with_xml_parser`,
`every_migrated_spec_md_has_blank_line_after_each_close_tag`) are
SPEC.md hygiene drift in SPEC-0040 and SPEC-0041 planner output —
pre-existing at base, not caused by T-001's edits, and outside
T-001's REQ scope (REQ-002 / REQ-003 / REQ-005 all target Rust
source, not SPEC.md formatting). Implementer routed the issue
correctly. No test gaps introduced.
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
No new attack surface. Net deletion of a parser plus a narrowed-
visibility relocation of an existing helper. The new
`parse::fs::read_to_string` is `pub(crate)`, tightening visibility
versus the pre-state where `pub mod toml_files` made the function
reachable as `crate::parse::toml_files::read_to_string` from the
crate's own public surface (still pub-in-crate, but the module
itself was pub). The helper preserves `fs_err`-based error
wrapping with path attached, so I/O failure modes remain
diagnosable rather than panicking. No `unwrap` / `expect` /
`panic` / `unreachable` / `todo` / `unimplemented` introduced; no
`#[allow]` introduced; no `unsafe` introduced. No new dependency
declarations. Schema-version guard removal is a deliberate dead-
code deletion (DEC-003) — no security-relevant invariant was
hanging off `SUPPORTED_SCHEMA_VERSION` (it gated only
`speccy_toml` parsing, which is itself gone).
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="style" verdict="pass" round="1">
Style conforms to the project's Rust rules. The new
`parse/fs.rs` carries a module-level `//!` doc explaining purpose
and SPEC-0040 provenance, the function carries a `///` doc
describing the error-wrapping contract, and the body is a single
`map_err` chain — minimal, boring, obvious. Types are `Utf8Path`
+ `ParseResult` per the patina path/error conventions documented
in the rule files. `fs_err::read_to_string` is used instead of
`std::fs::read_to_string` per the preferred-crates table. No
`unwrap` / `expect` / `panic` / indexing in production code; no
`#[allow]`; no `unsafe`. The visibility narrowing from
module-public to `pub(crate)` matches the "internal helper" framing
in REQ-003. Import ordering in `spec_md.rs` (replacing one `use`
line with the new path) preserves the file's pre-existing style.
The `replace_all` rename of `no_spec_toml_files_…` →
`no_stray_spec_toml_…` is a single-call surgical edit. No
unrelated cleanups bundled in. Procedural compliance: journal
file created with the required frontmatter and a round-1
`<implementer>` block with all required attributes.
</review>
