---
spec: SPEC-0040
task: T-004
generated_at: 2026-05-22T23:30:00Z
---

<implementer date="2026-05-22T23:30:00Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- `README.md`: rewrote the "Scaffolds `.speccy/speccy.toml` and the
  `.speccy/specs/` skeleton" bullet to "Scaffolds the `.speccy/`
  directory and the `.speccy/specs/` skeleton"; removed the
  `speccy.toml` line from the "Repo layout after `speccy init`"
  diagram; removed the trailing sentence "The only TOML left in the
  layout is the workspace-level `.speccy/speccy.toml`." from the
  closing paragraph (preserved the requirement-to-scenario in-band
  claim).
- `.speccy/ARCHITECTURE.md`: removed the `speccy.toml` line from the
  `.speccy/` file-layout diagram; deleted the entire `## speccy.toml`
  section (heading, fenced TOML block, surrounding prose); rewrote the
  `SPC-001` row in the lint catalogue to describe the rule's actual
  current behaviour — SPEC.md read/parse catch-all per
  `speccy-core/src/lint/rules/spc.rs::spc_001_spec_doc_parse`;
  deleted the entire `## Schema version` section; rewrote the
  implementation-sequence bullet to drop the `speccy.toml` mention
  while preserving the SPEC.md/TASKS.md/REPORT.md description.

## Undone

None — all REQ-007 deliverables landed.

## Hygiene checks

- `cargo test -p speccy-cli --test init architecture_doc_pins`: 3
  passed (covers the architecture-doc pins called out in CHK-013's
  task scenarios).
- `cargo test -p speccy-cli --test init reviewer_tests_persona_pins`:
  1 passed.
- `cargo test --workspace`: 2 failures, **both pre-existing and out
  of T-004 scope** —
  `speccy-core/tests/in_tree_specs::every_in_tree_spec_md_parses_with_xml_parser`
  and `every_migrated_spec_md_has_blank_line_after_each_close_tag`
  flag formatting issues in SPEC-0040 / SPEC-0041 SPEC.md files
  (missing `<changelog>` and blank-line-after-close convention).
  Neither file was touched by this task; both failures predate it
  and belong to the SPEC.md authoring side, not REQ-007's
  README+ARCHITECTURE strip.

## Evidence

- CHK-012 (case-sensitive ripgrep for `speccy.toml` across
  `README.md` and `.speccy/ARCHITECTURE.md` returns zero matches):
  verified via Grep — "No matches found".
- CHK-013 (case-sensitive ripgrep for `## speccy.toml` /
  `## Schema version` in `.speccy/ARCHITECTURE.md` returns zero
  matches): verified via Grep — "No matches found".

## Discovered issues

The in-tree spec-format tests
(`every_in_tree_spec_md_parses_with_xml_parser`,
`every_migrated_spec_md_has_blank_line_after_each_close_tag`) are
red at HEAD on SPEC-0040 itself and on the untracked SPEC-0041 draft.
These pre-date T-004 and are out of scope per the SPEC's Non-goals
(broader drift cleanup deferred to a follow-up SPEC). Flagging here
so the reviewer is not surprised when running `cargo test --workspace`.

## Procedural compliance

No speccy skill bodies needed edits during this turn. The
speccy-work skill steps were followed: flipped task to
`in-progress`, read scenarios via `speccy check`, made surgical
edits, verified scenarios pass, flipped to `in-review`, wrote this
journal entry.

</implementer>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="business" verdict="pass" round="1">
REQ-007's done-when items are all met: case-sensitive ripgrep for
`speccy.toml` over README.md and `.speccy/ARCHITECTURE.md` returns
zero matches, both `## speccy.toml` and `## Schema version` headings
are gone, and the SPC-001 row now describes the rule's actual
SPEC.md parse-failure catch-all surface rather than a stray TOML
file. The README rewrite preserves the in-band requirement-to-
scenario claim called out in the task body. Non-goals are respected
— `spc.rs` is untouched and broader ARCHITECTURE.md drift is left
for the follow-up SPEC.
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="tests" verdict="pass" round="1">
CHK-012 and CHK-013 verified by direct ripgrep — both return zero
matches. The load-bearing pins flagged in the task scenarios
(`architecture_doc_pins_*` x3 and `reviewer_tests_persona_pins_*`)
all pass at HEAD. The two pre-existing workspace-test failures the
implementer flagged (`every_in_tree_spec_md_parses_with_xml_parser`,
`every_migrated_spec_md_has_blank_line_after_each_close_tag`) are
red against SPEC-0040 / SPEC-0041 SPEC.md authoring conventions,
not against any file touched by T-004, and are out of scope for
this REQ-007 docs-only slice.
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
Docs-only edits to README.md and `.speccy/ARCHITECTURE.md`. No
code paths, dependencies, secrets, or trust boundaries touched.
No security surface.
</review>

<review date="2026-05-22T23:55:00Z" model="claude-opus-4-7[1m]/low" persona="style" verdict="pass" round="1">
Edits are surgical and match surrounding prose style. The SPC-001
row rewrite is concise, matches the rule's actual behavior in
`speccy-core/src/lint/rules/spc.rs`, and preserves the
code-block column-aligned format of the lint catalogue. The
README closing sentence trim leaves a clean "there is no per-spec
`spec.toml`." terminator without a dangling clause. No stylistic
drift introduced.
</review>
