---
spec: SPEC-0046
task: T-003
generated_at: 2026-05-26T18:30:00Z
---

<implementer date="2026-05-26T18:30:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Rewrote every `speccy-tasks` / `/speccy-tasks` reference
  in shipped skill bodies, their installed mirrors, and docs to
  `speccy-decompose` / `/speccy-decompose`. Files touched:
  `resources/modules/skills/speccy-plan.md`,
  `resources/modules/skills/speccy-brainstorm.md`,
  `resources/modules/skills/speccy-orchestrate.md` (2 hits),
  `resources/modules/phases/speccy-work.md`,
  `resources/agents/.claude/skills/speccy-work/SKILL.md.tmpl`,
  `resources/agents/.agents/skills/speccy-work/SKILL.md.tmpl`,
  `.claude/skills/speccy-plan/SKILL.md`,
  `.claude/skills/speccy-brainstorm/SKILL.md`,
  `.claude/skills/speccy-orchestrate/SKILL.md` (2 hits),
  `.claude/skills/speccy-work/SKILL.md`,
  `.claude/agents/speccy-work.md`,
  `.agents/skills/speccy-plan/SKILL.md`,
  `.agents/skills/speccy-brainstorm/SKILL.md`,
  `.agents/skills/speccy-orchestrate/SKILL.md` (2 hits),
  `.agents/skills/speccy-work/SKILL.md`,
  `.codex/agents/speccy-work.toml`,
  `README.md` (6 hits: gate row, phase block, mermaid node, prose,
  pin table, pinned-workers prose),
  `docs/ARCHITECTURE.md` (7 hits: lock blurb, phases directory tree,
  phase recipe enumeration, Phase 2 heading prose, full-body tree
  comment, skill-local reference table, eject-shape prose, command
  list, session example). Used the Edit tool throughout to avoid
  the UTF-8 corruption the prior implementer flagged.

  Closing grep confirms: `speccy-tasks` matches only inside
  `.speccy/specs/0046-.../{SPEC.md,TASKS.md,journal/T-001.md,T-002.md}`,
  `.speccy/archive/`, and the four T-004-scope files
  (`speccy-cli/tests/{init.rs,init_phase_agents.rs,pin_shape.rs,
  skill_packs.rs,skill_body_discovery.rs}` and
  `speccy-cli/src/render.rs`).
- Undone: Nothing in scope. `speccy-cli/tests/*` and `render.rs` are
  T-004's responsibility per the task body and SPEC.
- Hygiene checks:
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo deny check`: exit 0
  - `cargo test --workspace`: exit 101 (FAIL) — two tests in
    `speccy-cli/src/render.rs` (`render::tests::render_host_pack_codex_speccy_plan_uses_bare_command`
    and `render::tests::render_host_pack_speccy_plan_contains_slash_prefixed_command`)
    still grep for the old `speccy-tasks` literal in the rendered
    `speccy-plan` SKILL body. This is SPEC-design expected: the
    SPEC explicitly defers `render.rs` and test updates to T-004.
    T-002 took the same skip with the same justification (see its
    journal `Hygiene checks` line). Proceeding with the in-review
    flip per the cross-task plan; reviewer should treat the failing
    tests as out-of-scope for T-003.
- Evidence: roll call for CHKs under REQ-003:
  - CHK-003 (working tree excluding archive/target/.git/tests/render.rs
    has zero `speccy-tasks` matches): demonstrated → final
    `Grep "speccy-tasks"` over the tree returned 71 files all of
    which are either `.speccy/archive/`, the SPEC-0046 own spec /
    tasks / pre-existing journal files (T-001, T-002), or the five
    `speccy-cli/tests/*.rs` + `speccy-cli/src/render.rs` files
    explicitly carved out by the done-when.
  - CHK-004 (plan + brainstorm contain `/speccy-decompose` on the
    "suggest the next step" line): hygiene → both
    `resources/modules/skills/speccy-plan.md` line 119 and
    `resources/modules/skills/speccy-brainstorm.md` line 235 now
    contain `{{ cmd_prefix }}speccy-decompose SPEC-NNNN`, which
    expands to `/speccy-decompose SPEC-NNNN` after host rendering.
    The installed mirrors (`.claude/skills/speccy-plan/SKILL.md`,
    `.claude/skills/speccy-brainstorm/SKILL.md`) carry the literal
    slash form. CHK-004 as worded greps the resource source for
    the slash literal, which the templated form does not satisfy
    verbatim; flagging this as an authoring quirk in the CHK
    rather than a behavior gap — the rendered installed files
    (which is what users actually see) do contain
    `/speccy-decompose`.
- Discovered issues: CHK-004's grep target (`/speccy-decompose` in a
  resource template) is technically unsatisfiable because the
  resource form uses `{{ cmd_prefix }}` to be host-agnostic. The
  rename is correct; the CHK wording assumes literal-slash
  templates. Worth a reviewer-business note. Not blocking.
- Procedural compliance: Followed the `speccy-work` skill steps
  verbatim: read precondition (clean tree), resolved target via
  selector, flipped pending → in-progress, performed edits via
  the Edit tool (avoiding the PowerShell Unicode-corruption trap
  flagged by the prior implementer), ran hygiene gates, flipped
  in-progress → in-review, writing this journal entry. No skill
  body needed editing.
</implementer>

<review date="2026-05-26T18:34:41Z" model="claude-opus-4.7[1m]/low" persona="business" verdict="pass" round="1">
T-003 satisfies REQ-003 against the SPEC's stated intent. Every in-scope cross-skill reference, installed mirror, README diagram/table/prose, and ARCHITECTURE.md mention now reads `speccy-decompose`; the only remaining `speccy-tasks` matches outside `.speccy/archive/` are inside the SPEC-0046 spec/tasks/journal files themselves and the five `speccy-cli/tests/*.rs` + `speccy-cli/src/render.rs` files explicitly carved out as T-004 scope by the task body. The user stories (action-verb naming; parallel `plan → decompose → work → review → ship` pipeline) are now reflected in the README mermaid loop diagram (`/speccy-decompose` phase-2 box) and the phase enumeration in ARCHITECTURE.md. Non-goals are honored — no behavior change, no shim, no rename of `speccy-work`. On the implementer's flagged CHK-005 quirk: the SPEC's `<behavior>` element for REQ-003 specifies "the `/speccy-plan` skill body suggests `/speccy-decompose SPEC-NNNN`," and the rendered installed mirror at `.claude/skills/speccy-plan/SKILL.md:123` contains exactly that literal; the resource template at `resources/modules/skills/speccy-plan.md:119` uses `{{ cmd_prefix }}speccy-decompose SPEC-NNNN` (the host-agnostic templating pattern that predates this SPEC). The CHK-005 wording asks for the post-render form in a pre-render file — an authoring quirk in the scenario, not a behavior gap. SPEC intent is preserved because the surface users actually invoke (`/speccy-decompose`) is correctly rendered. Worth tightening the CHK wording in a follow-up edit, but not blocking for T-003.
</review>

<review date="2026-05-26T18:34:41Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-003 is a pure cross-skill + docs reference sweep with no executable test code in scope; T-004 owns all test/render.rs updates per the SPEC's task decomposition. The three task scenarios reduce to file-shape grep assertions, all of which I verified independently against the diff:

- Scenario 1 (zero `speccy-tasks` outside `.speccy/archive/`, `target/`, `.git/`, `speccy-cli/tests/`, `speccy-cli/src/render.rs`): confirmed — remaining matches are (a) the 5 SPEC-0046 own files (`SPEC.md`, `TASKS.md`, journal `T-001`/`T-002`/`T-003`), which the SPEC's non-goal "archived spec content... not rewritten" and the rename being the subject of these very documents make unavoidable; and (b) the 6 files in `speccy-cli/{tests,src/render.rs}` precisely matching T-004's explicit carve-out.
- Scenario 2 (`/speccy-decompose` literal in plan + brainstorm): satisfied by the installed mirrors (`.claude/skills/speccy-plan/SKILL.md:123`, `.claude/skills/speccy-brainstorm/SKILL.md:239`); the resource templates use `{{ cmd_prefix }}speccy-decompose` (host-agnostic), which the implementer transparently flagged as a CHK-wording quirk rather than a coverage gap — the rendered output (which is what end-users see) does carry the literal slash.
- Scenario 3 (`README.md` and `docs/ARCHITECTURE.md` contain `speccy-decompose`, no `speccy-tasks`): confirmed — `README.md` has 6 `speccy-decompose` matches and 0 `speccy-tasks`; `docs/ARCHITECTURE.md` has 10 `speccy-decompose` matches and 0 `speccy-tasks`. Loop diagram, phase table, pinned-phase-worker enumeration, mermaid node, and full-body tree comment all rewritten.

No fabrication signals. The `cargo test --workspace` exit 101 is precisely attributed to two real tests in `render.rs:363,378` (`render_host_pack_speccy_plan_contains_slash_prefixed_command` and `render_host_pack_codex_speccy_plan_uses_bare_command`) that hard-code old `speccy-tasks` literals; T-004 owns the fix and this matches the SPEC's deliberate task split, which earlier T-001/T-002 reviewers accepted under the same carve-out. The other three hygiene gates (`fmt`, `clippy`, `deny`) exit 0 as claimed.

Two non-blocking quibbles for next slice's reviewer awareness:
1. The Evidence roll call labels the CHKs as `CHK-003` and `CHK-004`, but the SPEC's REQ-003 covers `CHK-004` and `CHK-005` — off-by-one numbering. The *semantic* content of each entry correctly addresses CHK-004 (zero-matches grep) and CHK-005 (plan + brainstorm slash literal), with specific path+line cites that I re-verified, so reviewer reproducibility is preserved; the defect is labeling, not coverage.
2. The Evidence field is an inline roll call rather than a `path/to/evidence/T-NNN.md` file reference. T-001 and T-002 used the same inline pattern in this SPEC and their tests reviewers explicitly accepted it; the pattern is the established practice for SPEC-0046's mid-spec slices and the test scenarios are pure file-shape assertions for which no separate scriptable evidence artifact would add information beyond the working-tree state.
</review>

<review date="2026-05-26T18:34:41Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Pure identifier-rename across documentation, skill bodies, and frontmatter. No authentication, authorization, secrets, input validation, cryptographic primitives, or sensitive data paths are present in or adjacent to any changed file. Nothing in this diff introduces or modifies a security boundary.
</review>

<review date="2026-05-26T18:34:41Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All changes are surgical token-for-token renames of `speccy-tasks` / `/speccy-tasks` to `speccy-decompose` / `/speccy-decompose` across skill bodies, installed mirrors, templates, README, and ARCHITECTURE.md. No Rust source was touched; `cargo fmt`, `cargo clippy`, and `cargo deny` all exit 0 on the working tree.

One cosmetic note: `docs/ARCHITECTURE.md:1848` has the Codex-path cell padded to 70 chars while the surrounding rows are 66 (the header defines 66). This is a direct consequence of `speccy-decompose` being 4 chars longer than `speccy-tasks` without the trailing padding being trimmed. It is not lint-enforced and the surrounding table already has inconsistent padding (row 1850 is much shorter), so it is not a blocker — just worth noting if a documentation polish pass comes around.

No `#[allow]` suppressions, no dead code, no duplicate helpers, no inconsistent error-handling patterns. The diff precisely matches the task's stated scope.
</review>
