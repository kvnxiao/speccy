---
spec: SPEC-0039
task: T-006
generated_at: 2026-05-22T21:53:34Z
---

<implementer date="2026-05-22T21:53:34Z" model="claude-opus-4.7[1m]/low" round="1">

- Completed: Rewrote the module-doc comment at
  `speccy-core/src/prompt/id_alloc.rs:3-5` to cite SPEC-0005 DEC-005
  alone, dropping the `.speccy/ARCHITECTURE.md "Spec ID allocation"`
  reference. The replacement reads:

  ```
  //! Per SPEC-0005 DEC-005 ("Spec ID allocation"): gaps left by dropped
  //! specs are not recycled so historical SPEC IDs retain unambiguous
  //! meaning in commits and PR descriptions.
  ```

  This is the only edit in scope; the file's allocator logic, walking
  semantics, tests, and surrounding docstrings are unchanged.

- Undone: None. Scope was strictly the one-line doc-comment sweep at
  `speccy-core/src/prompt/id_alloc.rs:3`. Per the task body, the
  project-local `AGENTS.md`, per-crate `README.md` files, the
  `.speccy/specs/` SPEC history, and the
  `speccy-core/tests/docs_sweep.rs` / `speccy-cli/tests/init.rs` test
  fixtures remain exempt and continue to reference `ARCHITECTURE.md`
  as before (these are project-local docs/tests, not shipped library
  code). T-007 (the README/ARCHITECTURE/AGENTS positioning rewrite)
  is a separate task.

- Hygiene checks. All four green:
  - `cargo test --workspace` exits 0; the full workspace suite passes
    (no test references the removed string).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    exits 0 with no warnings.
  - `cargo +nightly fmt --all --check` exits 0 (zero diff).
  - `cargo deny check` reports `advisories ok, bans ok, licenses ok,
    sources ok`.

- Evidence: This task is a one-line doc-comment edit; the scenarios
  in TASKS.md verify the change directly via `rg` against the
  post-edit source tree, so no red/green binary-behaviour paper trail
  applies. Verification commands run and observed:
  - `rg -n 'ARCHITECTURE\.md' speccy-core/src speccy-cli/src resources`
    → zero matches (CHK scenario 1).
  - `rg -c 'ARCHITECTURE\.md' AGENTS.md` → 4 matches (project-local
    `AGENTS.md` still references `ARCHITECTURE.md`, as required).
  - `rg -l 'ARCHITECTURE\.md' .speccy/specs/` → at least 5 paths
    (existing SPEC history preserved).
  - `rg -l 'ARCHITECTURE\.md' speccy-core/tests speccy-cli/tests`
    → `speccy-core/tests/docs_sweep.rs` and
    `speccy-cli/tests/init.rs` (docs-sweep + init tests still
    reference the project-local file).
  - `cargo test --workspace` → exits 0.

- Discovered issues: None. The edit is surgical: only the literal
  substring `` and `.speccy/ARCHITECTURE.md` `` was removed from one
  comment line; no neighbouring prose, identifier, or test fixture
  needed adjusting.

- Procedural compliance: TASKS.md's `state="..."` attribute for T-006
  flips from `in-progress` to `in-review` as the final step of this
  turn. The handoff lives in this `journal/T-006.md` file per the
  post-SPEC-0037 schema; no `<implementer-note>` element was written
  into TASKS.md (retired in SPEC-0037 — parser rejects it). No
  shipped skill body under `resources/modules/skills/` or any other
  ejected pack file required edits during this task; the implementer
  prompt at HEAD already documents the post-SPEC-0034 six-field
  handoff template, so no friction-to-skill-update was triggered.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T21:58:20Z" round="1">
T-006 delivers exactly the one-line doc-comment edit REQ-004's done-when names for `speccy-core/src/prompt/id_alloc.rs:3`. The replacement cites SPEC-0005 DEC-005 ("Spec ID allocation") as the SPEC promised; the `ARCHITECTURE.md` reference is gone from production Rust source. All three scope-bounded scenarios verify: zero matches under `speccy-core/src speccy-cli/src resources`, while `AGENTS.md` (4 matches), `.speccy/specs/` (existing SPEC history), `speccy-core/tests/docs_sweep.rs`, and `speccy-cli/tests/init.rs` remain exempt as REQ-004 requires. Scope is surgical — no adjacent prose, identifier, or fixture drift. This task does not encroach on T-007 (the README/ARCHITECTURE/AGENTS positioning rewrite covering REQ-006), which remains correctly pending.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-22T21:58:20Z" round="1">
T-006 is a one-line doc-comment cite-swap at `speccy-core/src/prompt/id_alloc.rs:3-5`; the diff matches the task body verbatim and changes no executable code. All three `<task-scenarios>` independently verified true against the working tree: scenario 1 (`rg -n 'ARCHITECTURE\.md' speccy-core/src speccy-cli/src resources` → 0 matches), scenario 2 (AGENTS.md still has 4 matches; SPEC history preserved across 91 files; `speccy-core/tests/docs_sweep.rs` and `speccy-cli/tests/init.rs` still reference the project-local doc), scenario 3 (`cargo test --workspace` exits 0 with zero failures across ~40 test binaries). The 10 existing id_alloc unit tests (absent dir, empty dir, gap-no-recycling, high-id-max-plus-one, non-matching-ignored, malformed-prefix, 5-digit IDs, nested mission folders, flat+nested shared id space, no-double-count) continue to exercise the allocator's behaviour, and a hypothetical accidental removal of a code line would surface as a `cargo test --workspace` failure — that signal was exercised. The journal's `Evidence:` field is inline-prose rather than a separate evidence file path, which is appropriate for a one-line doc-comment cite-swap where no red-then-green binary-behaviour transition is observable; the implementer named the rg and `cargo test` commands they ran and the match counts they reported reproduce exactly under independent verification. No fabrication smell.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T21:58:20Z" round="1">
Pure doc-comment edit in `speccy-core/src/prompt/id_alloc.rs:3-5` — three lines of `//!` comment text reworded to drop a repo-local path reference. No logic, no inputs, no secrets, no API surface, no dependencies touched. No security findings.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T21:58:20Z" round="1">
The sole diff in scope is the module-level doc comment at `speccy-core/src/prompt/id_alloc.rs:3-5`. The `ARCHITECTURE.md` reference was removed and the text now reads `//! Per SPEC-0005 DEC-005 ("Spec ID allocation"):` as the task required. No other lines were touched. Style checks all pass: the existing `#[expect(clippy::unwrap_used, reason = "...")]` suppression at line 67 carries a `reason` attribute per AGENTS.md convention; no bare `#[allow]` was added; no new identifiers, imports, or dead code were introduced; trailing newline confirmed present (`0x0a`).
</review>
