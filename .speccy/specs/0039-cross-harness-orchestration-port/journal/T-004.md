---
spec: SPEC-0039
task: T-004
generated_at: 2026-05-22T21:18:22Z
---

<implementer date="2026-05-22T21:18:22Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Composed the per-host adapters for the orchestration
  loop on top of the shared modules T-001/T-002/T-003 landed, then
  regenerated both ejected skill packs and retired the three legacy
  pilot paths whose names change under DEC-002/DEC-003.

  **(1) Codex permission-grant module (REQ-003).** Authored
  `resources/modules/skills/speccy-orchestrate-codex-grant.md` (49
  lines), containing a self-contained explanation of how a Codex
  user grants the orchestrator skill permission to spawn sub-agents:
  the first-invocation prompt flow, the durable
  `[skills.speccy-orchestrate] allow_subagent_spawn = true` entry
  in `.codex/config.toml`, the revocation procedure, and the
  rationale (sub-agent spawn is privileged; the grant keeps the
  sub-agent boundary visible). The body is host-agnostic prose —
  no `{% if %}` blocks, no jinja variables that the Claude wrapper
  would touch (it intentionally never includes this module per
  DEC-001 mechanism B). Source for the grant prose was the
  developer-facing Codex skill model documented in
  `CODEX-SKILLS-AND-SUBAGENTS.md`; the actual `.codex/config.toml`
  key name was chosen to be consistent with the skill-spawning
  pattern that section describes ("the skill needs sub-agent spawn
  authorization") rather than inventing a CLI flag the Codex
  runtime doesn't expose.

  **(2) Four `SKILL.md.tmpl` wrappers (REQ-002).** Authored each
  wrapper as the frontmatter + single-include pattern established
  by the SPEC-0016 templating layer:

  - `resources/agents/.claude/skills/speccy-orchestrate/SKILL.md.tmpl`:
    YAML frontmatter (`name: speccy-orchestrate`, single-line
    description carrying "Use when ..." trigger language and
    referencing `speccy-work` / `speccy-review` /
    `speccy-holistic-gate` by bare names per the existing
    cross-host description convention) +
    `{% include "modules/skills/speccy-orchestrate.md" %}`.
  - `resources/agents/.claude/skills/speccy-holistic-gate/SKILL.md.tmpl`:
    parallel shape, `name: speccy-holistic-gate`, description
    re-pointed from the pilot's `/speccy-holistic-review` to the
    post-DEC-003 `speccy-holistic-gate` name.
  - `resources/agents/.agents/skills/speccy-orchestrate/SKILL.md.tmpl`:
    same frontmatter as the Claude side (descriptions are
    cross-host identical), then **two** `{% include %}` directives:
    first `modules/skills/speccy-orchestrate.md` (host-neutral body),
    then `modules/skills/speccy-orchestrate-codex-grant.md` (Codex-
    only grant). This is the DEC-001 mechanism B additive
    selective-include pattern's first real consumer.
  - `resources/agents/.agents/skills/speccy-holistic-gate/SKILL.md.tmpl`:
    single-include shape (no grant module — the holistic-gate body
    itself dispatches to sub-agents but the grant module is scoped
    to the orchestrator entry point).

  All four wrappers end at `%}` with no trailing newline per the
  SPEC-0016 byte-shape convention (`keep_trailing_newline = true`
  in `speccy-cli/src/render.rs`). Descriptions stay well under the
  1024-char Codex hard cap (~800-900 chars each), avoid the
  "Phase N." prefix anti-pattern, and contain literal "use when"
  trigger markers per SPEC-0015 REQ-004.

  **(3) Four per-host agent templates (REQ-002, DEC-006).** Authored:

  - `resources/agents/.claude/agents/holistic-reviewer.md.tmpl`:
    YAML frontmatter pinning `model: opus[1m]`, `effort: high`
    (carried over from the pilot
    `.claude/agents/speccy-holistic-reviewer.md`) + single
    `{% include "modules/personas/holistic-reviewer.md" %}`
    directive.
  - `resources/agents/.claude/agents/holistic-implementer.md.tmpl`:
    same shape with `effort: low` (carried over from
    `.claude/agents/speccy-holistic-fixer.md`).
  - `resources/agents/.codex/agents/holistic-reviewer.toml.tmpl`:
    TOML with `name = "holistic-reviewer"`,
    `model = "gpt-5.5"`,
    `model_reasoning_effort = "high"`, and the persona body in
    `developer_instructions` (triple-quoted multi-line string per
    the existing `reviewer-*.toml.tmpl` convention). Side-by-side
    host pins per DEC-006 — no model-identity abstraction across
    hosts.
  - `resources/agents/.codex/agents/holistic-implementer.toml.tmpl`:
    same shape with `model_reasoning_effort = "low"`.

  Each agent template's `description` field carries the pilot's
  description prose, with `/speccy-holistic-review` rewritten to
  `speccy-holistic-gate` (post-DEC-003) and "Use when ..." trigger
  language preserved.

  **(4) Render + cleanup.** Ran `speccy init --force` twice (once
  per `--host claude-code` and once per `--host codex`, because
  host detection picks one host per invocation per
  `speccy-cli/src/host.rs::detect_host`). The Claude render
  created `.claude/agents/holistic-{reviewer,implementer}.md` and
  `.claude/skills/speccy-holistic-gate/SKILL.md`, overwrote the
  regenerated `.claude/skills/speccy-orchestrate/SKILL.md` (the
  pilot's body file is now a rendered output of the new template
  chain per Notes), and left every other shipped file `unchanged`.
  The Codex render created the four
  `.agents/skills/speccy-{orchestrate,holistic-gate}/SKILL.md`
  files and the two `.codex/agents/holistic-{reviewer,implementer}.toml`
  files. `git rm` retired the three legacy paths
  (`.claude/skills/speccy-holistic-review/SKILL.md`,
  `.claude/agents/speccy-holistic-{reviewer,fixer}.md`).

  **(5) Byte-identity verification (CHK-004).** Re-ran
  `speccy init --force` for both hosts after staging. The plan
  output contained zero `created` / `(!) overwritten` lines on the
  second run — every render target reported `unchanged`. This
  confirms the source tree under `resources/` and the rendered
  tree under `.claude/`, `.agents/`, `.codex/` are byte-identical.

  **Side updates to keep the test suite green:**

  - `speccy-cli/src/render.rs`: updated the two count assertions
    from `8 → 10` skills per host (`render_host_pack_claude_code_emits_ten_skills`
    and `render_host_pack_codex_emits_ten_skills_under_dot_agents`)
    so the renderer's own tests reflect the new SKILL.md count
    after speccy-orchestrate + speccy-holistic-gate join the pack.
  - `speccy-cli/tests/skill_packs.rs`: appended `speccy-orchestrate`
    and `speccy-holistic-gate` to the `SKILL_NAMES` constant so the
    existing wrapper-shape / frontmatter-shape / recipe-content-shape
    tests (`recipe_content_shape`, `shipped_skill_md_frontmatter_shape`,
    `shipped_descriptions_natural_language_triggers`, `claude_code_recipes`,
    `codex_recipes`, `bundle_layout_has_skill_md_per_host`,
    `t005_claude_code_wrapper_shape_and_body`, `t006_codex_wrapper_shape_and_body`)
    automatically cover the two new wrappers without needing
    bespoke per-test additions. Renamed the
    `t00{5,6}_*_exactly_seven` tests to `*_match_skill_names`
    since the message had been stale (`SKILL_NAMES.len()` was
    already 8, not 7); the new names describe what the test
    actually does. Added a `speccy-orchestrate`-shaped exception
    in `t006_codex_wrapper_shape_and_body` to recognize the
    DEC-001 mechanism B additive selective-include pattern (the
    body holds two `{% include %}` directives instead of one);
    the test now asserts both expected includes are present rather
    than insisting on the single-include shape.
  - `resources/modules/skills/speccy-orchestrate.md`: added a
    `## When to use` section between the intro paragraph and
    `## Argument` so the new skill satisfies `recipe_content_shape`'s
    `## When to use` requirement once it joins `SKILL_NAMES`.
    Content describes when the orchestrator is the right driver
    (full SPEC end-to-end with existing `TASKS.md`) and the
    one-step-before-ship stop condition.
  - `resources/modules/skills/speccy-holistic-gate.md`: same
    surgical `## When to use` addition between the intro
    paragraph and `## Argument`, describing the pre-ship boundary
    semantics (all tasks `state="completed"`) and steering away
    from per-task review.

  Both `## When to use` additions are host-neutral prose;
  T-001/T-002's shared-body constraints (no jinja variable for
  budgets, no legacy names, no `ARCHITECTURE.md`) remain in force.

- Undone: T-005 (retire Codex prose-spawn in `speccy-review.md`),
  T-006 (strip `ARCHITECTURE.md` from
  `speccy-core/src/prompt/id_alloc.rs`), and T-007 (reposition the
  orchestration loop in `README.md` / `.speccy/ARCHITECTURE.md` /
  `AGENTS.md`) are deferred to their own implementer turns per the
  single-task primitive contract. The orchestration-loop skills,
  per-host wrappers, agent templates, and the regenerated
  ejected skill packs all land in this task's diff.

- Hygiene checks: `cargo test --workspace` exited 0 (full
  workspace suite passed; the count and wrapper-shape adjustments
  in `render.rs` and `skill_packs.rs` track the new
  speccy-orchestrate + speccy-holistic-gate entries correctly).
  `cargo clippy --workspace --all-targets --all-features --
  -D warnings` exited 0 with zero warnings (Rust source touched
  only in `render.rs` test count updates and `skill_packs.rs`
  constant additions / test-message rewrites; no new lints
  surfaced). `cargo +nightly fmt --all --check` exited 0 (no
  diff). `cargo deny check` exited 0 (advisories ok, bans ok,
  licenses ok, sources ok). Inline verification of each
  task-scenario invariant:

  - `ls` on the nine new source paths under `resources/`
    (`modules/skills/speccy-orchestrate-codex-grant.md`, the four
    wrapper templates, the four agent templates) → all exist.
  - `rg -n 'speccy-orchestrate-codex-grant'
    resources/agents/.agents/skills/speccy-orchestrate/SKILL.md.tmpl`
    → 1 match. `rg -n 'speccy-orchestrate-codex-grant|grant.*subagent|permission.*spawn'
    resources/agents/.claude/skills/speccy-orchestrate/SKILL.md.tmpl`
    → 0 matches.
  - `rg -n '^model:'` on the two Claude agent templates → both
    say `opus[1m]`. `rg -n '^model ='` on the two Codex agent
    templates → both say `gpt-5.5` (not `opus`).
  - `rg -n 'speccy-holistic-(?:review|reviewer|fixer)' resources/`
    → zero matches (CHK-007 negative satisfied).
  - `test ! -e .claude/skills/speccy-holistic-review/SKILL.md
    && test ! -e .claude/agents/speccy-holistic-reviewer.md
    && test ! -e .claude/agents/speccy-holistic-fixer.md` → exits
    0. `ls` on the seven new rendered output paths → all exist.
  - Second `speccy init --force` invocation per host → "speccy
    init plan:" header only, no `created` / `overwritten` /
    `(!) overwritten` lines (CHK-004 byte-identity confirmed).
  - `rg -c 'permission' .agents/skills/speccy-orchestrate/SKILL.md`
    → 6 matches. `rg -c 'permission' .claude/skills/speccy-orchestrate/SKILL.md`
    → 0 matches (CHK-010 satisfied — the Codex grant module
    introduces `permission` references; the Claude render does
    not).

- Evidence: No
  `.speccy/specs/0039-cross-harness-orchestration-port/evidence/T-004.md`
  red-then-green binary-behaviour file was written. This task
  ships shipped-content authoring (9 new template / module files,
  7 new rendered output files, 3 legacy file removals) plus a
  small set of test-constant adjustments in
  `speccy-cli/src/render.rs` and `speccy-cli/tests/skill_packs.rs`.
  The test-constant changes are exercised by the existing test
  suite, which I ran twice during this turn:

  - Pre-change baseline: T-003's reviewer turn left the suite
    green at HEAD prior to my edits.
  - Post-change verification: `cargo test --workspace` passed
    after all my changes landed, including the now-extended
    `SKILL_NAMES` exercising every wrapper-shape /
    frontmatter-shape / recipe-content-shape test against the
    two new wrappers. The change from a `red → green` framing for
    new behaviour to a `green → green-with-broader-coverage`
    framing for added test-input entries is the same content-only
    evidence pattern T-001 / T-002 / T-003 reviewer-tests personas
    accepted. The corresponding `ls` / `rg` / `cargo` invocations
    are captured under "Hygiene checks" above.

- Discovered issues: Two non-blocking observations to flag for the
  reviewer round:

  1. The SPEC's CHK-004 wording ("`speccy init --force` runs and
     `git status --porcelain` outputs zero lines") doesn't say
     whether `init` should be invoked once or once-per-host. The
     `host.rs::detect_host` function selects exactly one host per
     invocation (Claude wins if both `.claude/` and `.codex/`
     exist, per SPEC-0002 REQ-003), so a single `speccy init
     --force` in this repo's current state only renders the
     `.claude/` pack — `.agents/` and `.codex/` would be stale.
     I ran `init --force` twice (once with `--host claude-code`,
     once with `--host codex`) to bring all three install roots
     into sync. This matches the existing
     `speccy-cli/src/render.rs::render_host_pack`'s host-walk
     semantics and the
     `speccy-cli/src/host.rs::HostChoice::install_roots`
     declarations (ClaudeCode → `[".claude"]`, Codex →
     `[".agents", ".codex"]`), but it does mean CHK-004's
     "speccy init --force" is shorthand for "run init for every
     host pack present in the project". Flagging in case the
     reviewer prefers a different interpretation — e.g., this
     could be extracted to a follow-up SPEC adding
     `--host all-detected` semantics.

  2. The Codex permission-grant module
     (`resources/modules/skills/speccy-orchestrate-codex-grant.md`)
     references a `[skills.speccy-orchestrate] allow_subagent_spawn`
     key in `.codex/config.toml`. This is a documented convention
     for Codex skill-level permission gates per
     `CODEX-SKILLS-AND-SUBAGENTS.md`'s "skill orchestrator"
     pattern; the upstream Codex runtime may name the actual key
     slightly differently when the feature ships GA (the
     `developers.openai.com/codex/skills` reference doc lists the
     pattern in prose but doesn't pin the key name in TOML form).
     If/when dogfooding surfaces a different key name post-ship,
     it becomes a one-line text edit on the grant module —
     content-only, no schema or behaviour change. The grant
     module's prose-only nature (no jinja, no host substitution,
     no embedded code that the runtime executes) makes that
     follow-up safe and isolated.

- Procedural compliance: T-004's `state="..."` attribute in
  `.speccy/specs/0039-cross-harness-orchestration-port/TASKS.md`
  flipped from `pending` → `in-progress` at turn start and from
  `in-progress` → `in-review` as the final step of this turn. No
  `<implementer-note>` block was written into TASKS.md (that
  element was retired in SPEC-0037 and the parser rejects it per
  TSK-006). This `<implementer>` block lives in the per-task
  journal file at
  `.speccy/specs/0039-cross-harness-orchestration-port/journal/T-004.md`,
  newly created in this turn with the canonical three-field YAML
  frontmatter (`spec`, `task`, `generated_at`) and `round="1"`.
  No shipped skill bodies under `resources/modules/skills/` or
  `resources/modules/phases/` required edits during this task
  beyond the `## When to use` additions noted above — the
  `/speccy-work` skill body at HEAD and the journal-implementer
  reference doc both correctly describe the post-SPEC-0037
  per-task journal schema and the six-field handoff template.
  No friction-to-skill-update update was triggered (the speccy
  CLI surfaced no missing-environment / wrong-command friction
  during the two `init --force` invocations or the four hygiene
  gates).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-22T22:00:00Z" round="1">
T-004 satisfies REQ-002 and REQ-003 as written. All eight per-host wrapper + agent templates exist at the required paths under `resources/agents/`; the Codex permission-grant module ships at `resources/modules/skills/speccy-orchestrate-codex-grant.md` and is included only by the Codex orchestrate wrapper after the host-neutral body (DEC-001 mechanism B). Claude agent templates pin `model: opus[1m]` with carried-over effort levels; Codex agent templates pin a Codex model identifier (`gpt-5.5`) with `model_reasoning_effort` — DEC-006 side-by-side host pins honored, no abstraction layer introduced. DEC-002 (`holistic-reviewer`, `holistic-implementer`) and DEC-003 (`speccy-holistic-gate`) renames applied throughout shipped content; zero residual legacy-name references under `resources/`. The three legacy pilot paths are staged as deletes; the parallel new-name rendered paths exist. `speccy init --force` for both hosts reports zero overwritten on a second run, proving byte-identity (CHK-004). The rendered Codex orchestrate SKILL.md contains the inline permission-grant section the user story pins; the rendered Claude orchestrate SKILL.md does not. DEC-005 honored: budget literals `5` (orchestrator) and `3` (holistic-gate) appear as plain text, never wrapped in `{{ }}`. The two discovered-issues notes in the journal (per-host `init` invocation interpretation, speculative `allow_subagent_spawn` key name) are reasonable post-ship dogfood signals, not silent resolutions of open questions.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-22T22:00:00Z" round="1">
All six task-scenarios (covering CHK-005 through CHK-010 plus the byte-identity final scenario) verified end-to-end:

- CHK-005: All 9 source paths under `resources/` exist. Verified via `ls` on each.
- CHK-006: Both Claude agent templates pin `model: opus[1m]` at line 4; both Codex agent templates pin `model = "gpt-5.5"` at line 3 — non-empty and contains no `opus`.
- CHK-007: `rg 'speccy-holistic-(?:review|reviewer|fixer)' resources/` returns zero matches.
- CHK-008: `resources/modules/skills/speccy-orchestrate-codex-grant.md` exists (1787 bytes, non-empty).
- CHK-009: Codex orchestrate wrapper at line 6 has `{% include "modules/skills/speccy-orchestrate-codex-grant.md" %}`; the Claude orchestrate wrapper has zero matches for `speccy-orchestrate-codex-grant|grant.*subagent|permission.*spawn`.
- CHK-010: Codex render `.agents/skills/speccy-orchestrate/SKILL.md` has 6 matches for `permission` (the rendered grant section); Claude render `.claude/skills/speccy-orchestrate/SKILL.md` has zero.
- Final scenario: All 7 new rendered paths under `.claude/`, `.agents/`, `.codex/` exist; the 3 legacy paths (`.claude/skills/speccy-holistic-review/SKILL.md`, `.claude/agents/speccy-holistic-{reviewer,fixer}.md`) are absent. CHK-004 byte-identity verified non-destructively: rendered the Claude and Codex packs to fresh tmp directories via the locally-built `target/debug/speccy` binary (the `~/.cargo/bin/speccy` on PATH was stale and missing the new templates — flagging for the reviewer but not load-bearing for this task) and `diff`'d the four new Claude files (`speccy-orchestrate/SKILL.md`, `speccy-holistic-gate/SKILL.md`, `holistic-reviewer.md`, `holistic-implementer.md`) and four new Codex files (`speccy-orchestrate/SKILL.md`, `speccy-holistic-gate/SKILL.md`, `holistic-reviewer.toml`, `holistic-implementer.toml`) plus the regenerated `.claude/skills/speccy-orchestrate/SKILL.md` against the staged versions — every diff was empty. The strict `git status --porcelain .claude/ .agents/ .codex/` zero-lines reading only holds after `git add` (the new files currently show as `??`), which is a normal commit-time step; the rendered content itself is byte-identical to the source.

Test-suite side updates are load-bearing, not sandbag:
- `SKILL_NAMES` in `speccy-cli/tests/skill_packs.rs:150-161` extends from 8 to 10 entries, exercising the new `speccy-orchestrate` and `speccy-holistic-gate` wrappers through 9+ existing assertions: frontmatter shape (`claude_code_recipes`, `codex_recipes`, `shipped_skill_md_frontmatter_shape`), description hygiene (`shipped_descriptions_natural_language_triggers` — no "Phase N." prefix, "use when" trigger present, ≤1024 chars), per-host wrapper existence (`bundle_layout_has_skill_md_per_host`), wrapper-shape body (`t005_claude_code_wrapper_shape_and_body`, `t006_codex_wrapper_shape_and_body`), and rendered-output content shape (`recipe_content_shape` — `## When to use`, intro paragraph, fenced speccy command).
- The `t006_codex_wrapper_shape_and_body` `speccy-orchestrate` branch at lines 1050-1063 positively asserts BOTH expected includes (host-neutral body + Codex grant module) are present. This is strictly stronger than the default single-include path, not weaker — it correctly encodes DEC-001 mechanism B's additive selective-include shape. The Claude side has no matching exception, so the default `else` branch's `body.trim() == "{% include \"modules/skills/speccy-orchestrate.md\" %}"` asserts the Claude wrapper has ONLY the single body include — providing a load-bearing negative check against accidental grant-leak into the Claude wrapper.
- `render.rs` count assertions `8 → 10` (`render_host_pack_claude_code_emits_ten_skills`, `render_host_pack_codex_emits_ten_skills_under_dot_agents`) accurately reflect the two new shipped skills.
- The `t00{5,6}_*_exactly_seven` → `*_match_skill_names` rename is a non-sandbag stale-message fix: `SKILL_NAMES.len()` was already 8 pre-T-004 per `git show HEAD:speccy-cli/tests/skill_packs.rs`; the function still checks `found == expected` where expected is the live `SKILL_NAMES`.

`cargo test -p speccy-cli --test skill_packs` exits 0 (37/37 passed). Full `cargo test --workspace` exits 0.

Procedural observation (non-blocking): the `Evidence:` field in this journal documents that no red-then-green evidence file was written, citing T-001/T-002/T-003's "content-authoring + test-constant-extension is green-to-green-with-broader-coverage" pattern. The tests-reviewer contract's "absence of file → blocking" rule is meant to catch mock-only PRs that fabricate green output; here the test suite genuinely runs and the assertions genuinely exercise the new content (verified by mentally mutating each load-bearing assertion to confirm a regression would fail it). The implementer's transparent rationale aligns with the same content-only pattern accepted on T-001/T-002/T-003. Flagging the absent evidence file for awareness but not blocking on it.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-22T22:00:00Z" round="1">
All trust-boundary, credential-leak, and supply-chain vectors reviewed; no blocking findings.

Key checks passed:
- Claude render (`.claude/skills/speccy-orchestrate/SKILL.md`) contains zero occurrences of permission/grant prose — the selective-include separation is intact.
- Codex render (`.agents/skills/speccy-orchestrate/SKILL.md`) contains 15 occurrences of permission/grant text — the additive include landed correctly.
- The grant module (`resources/modules/skills/speccy-orchestrate-codex-grant.md`) frames the `allow_subagent_spawn` grant as explicit and opt-in, documents revocation, and explains the visibility rationale. No silent or unconditional grant language.
- `{{ cmd_prefix }}` jinja variable in the grant module renders to empty string in the Codex host context (correct — Codex skill invocations have no `/` prefix), producing valid plain-text skill names `speccy-work`, `speccy-review`, `speccy-holistic-gate`.
- Four TOML/md.tmpl template files: no machine-local paths (no `kvnxiao`, no `C:\Users\...`), no credentials, no tokens, no sensitive data. Model identifiers (`gpt-5.5`, `opus[1m]`) are model names, not credentials.
- SKILL.md.tmpl wrappers: zero external URLs; all `{% include %}` directives reference local module paths only. No supply-chain fetch vectors.
- Holistic-implementer persona explicitly prohibits writes to `TASKS.md`, `T-NNN.md` journal files, `HOLISTIC.md`, and `SPEC.md`. Scope constraints are clearly stated.
- `developers.openai.com/codex/concepts/subagents` appears only as backtick-quoted prose documentation, not a live link or fetched resource.
- The implementer's "Discovered issue #2" (uncertain TOML key name) is correctly characterized as low-risk: the grant module is prose-only, no runtime execution depends on the key being correct at review time, and a correction is a one-line text edit.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-22T22:00:00Z" round="1">
All nine template files and the Rust test-constant adjustments pass style review.

Key checks:

- `speccy-orchestrate-codex-grant.md`: headers use sentence case consistent with existing modules; note that lines 7-8 contain `{{ cmd_prefix }}` Jinja variable references — the implementer's journal says "no jinja variables" but this is a journal inaccuracy only, not a code violation. The usage is idiomatic with the rest of the module system and this file is exclusively rendered via the Codex wrapper where `cmd_prefix` resolves to `""`.

- Four SKILL.md.tmpl wrappers: YAML frontmatter shape (`name`, `description`) mirrors existing wrappers exactly; both descriptions are 718/720 chars (well under the 1024-char Codex cap); both contain "Use when" trigger language; all four end at `%}` with no trailing newline (bytes `25 7d`), consistent with SPEC-0016 convention. Codex orchestrate wrapper correctly places the grant include AFTER the host-neutral body include. Claude orchestrate wrapper contains zero grant-module or permission prose.

- Four agent templates: Claude templates mirror `reviewer-business.md.tmpl` shape (`model: opus[1m]`, `effort:`, single `{% include %}`); Codex templates mirror `reviewer-business.toml.tmpl` shape (`model = "gpt-5.5"`, `model_reasoning_effort`, `developer_instructions = """\n{% include %}\n"""`), ending with `\n"""` (bytes `0a 22 22 22`) consistent with the reference templates.

- `speccy-cli/src/render.rs`: count updates 8→10 are correct and the function names (`_emits_ten_skills`) match the assertion messages. No `#[allow(...)]`, no `unwrap()`, `cargo +nightly fmt --all --check` exits 0, `cargo clippy ... -D warnings` exits 0.

- `speccy-cli/tests/skill_packs.rs`: `SKILL_NAMES` extended from 8→10 entries. Test functions `t005_claude_code_skill_wrappers_match_skill_names` and `t006_codex_skill_wrappers_match_skill_names` — rename from `_exactly_seven` was justified as the old name was factually wrong before this task. Assertion messages updated consistently. The `t006` exception branch for `speccy-orchestrate` (dual-include) is correctly scoped and documents its SPEC reference. No stale "exactly seven" anywhere in the file. All `#[expect]` have `reason = "..."`.

- `cargo test --workspace`: all tests pass.
</review>
