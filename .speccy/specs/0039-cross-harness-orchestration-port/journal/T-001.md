---
spec: SPEC-0039
task: T-001
generated_at: 2026-05-22T15:00:00Z
---

<implementer date="2026-05-22T15:00:00Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Authored the host-neutral orchestrator module at
  `resources/modules/skills/speccy-orchestrate.md` by factoring the
  hand-written pilot at `.claude/skills/speccy-orchestrate/SKILL.md`.
  Frontmatter dropped (lives in per-host wrappers, authored by
  T-004). Three `{% if host == "claude-code" %}…{% else %}…{% endif %}`
  blocks were added at the three dispatch points only (work, review,
  ship); all non-dispatch prose (Lifecycle, Context discipline,
  Startup integrity check, outer loop scaffolding, Stop conditions,
  Status reporting, Non-goals) was rewritten to be host-neutral. The
  Context-discipline section now talks about "the host's
  sub-agent-spawn primitive" rather than naming Claude's `Task` /
  `Skill` tools by name. Sub-agent dispatch targets use the
  post-DEC-002 / post-DEC-003 names throughout: the holistic
  lifecycle skill is `speccy-holistic-gate`, never the pilot-era
  `speccy-holistic-review`; the legacy `speccy-holistic-reviewer` /
  `speccy-holistic-fixer` sub-agent names are absent. The per-task
  retry budget appears as the literal integer `5` in four places
  (lifecycle pseudo-code, stop-conditions paragraph including the
  "budget of 5 is the orchestrator's only per-task retry bound"
  line per DEC-005, and the worked status-line example `(retry 2/5
  after blocking review)`); no jinja variable, template default, or
  env-lookup wraps the integer (CHK-002's
  `\{\{.*retry|budget.*\}\}` negative grep is satisfied). The body
  contains zero references to `ARCHITECTURE.md`, `.speccy/ARCHITECTURE.md`,
  or any other project-local doc — the only cross-file pointers are
  to `.speccy/specs/NNNN-slug/journal/T-NNN.md` (universal Speccy
  path) and to the rendered skill bodies under `.claude/skills/` /
  `.agents/skills/` inside the host-divergent dispatch blocks. The
  pilot's final non-goal bullet ("This skill is **local-only** for
  now…") was dropped, since SPEC-0039 is itself the act that moves
  this skill out of local-only state.

- Undone: T-002 (factor the holistic-review pilot into
  `speccy-holistic-gate.md` shared module), T-003 (factor the two
  holistic-loop persona pilots into `resources/modules/personas/`),
  T-004 (Codex grant module + per-host wrappers + per-host agent
  templates + render + delete legacy pilots), T-005 (retire the
  Codex prose-spawn idiom in `speccy-review.md`), T-006 (strip
  `ARCHITECTURE.md` reference from `speccy-core/src/prompt/id_alloc.rs`),
  T-007 (reposition the orchestration loop in `README.md` /
  `.speccy/ARCHITECTURE.md` / `AGENTS.md`). Each lands in its own
  implementer turn per the single-task primitive contract. The
  rendered Claude pack at `.claude/skills/speccy-orchestrate/SKILL.md`
  is deliberately NOT regenerated in this turn — `speccy init
  --force` is T-004's responsibility because byte-identity
  verification (CHK-004) only makes sense once all wrapper
  templates, persona modules, and the Codex grant module exist.

- Hygiene checks: `cargo test --workspace` exited 0 with all
  workspace test crates passing (counts visible in the run summary;
  the previously-failing
  `every_migrated_spec_md_has_blank_line_after_each_close_tag`
  test in `speccy-core/tests/in_tree_specs.rs` now passes after
  the SPEC.md formatting fix described under "Discovered issues").
  `cargo clippy --workspace --all-targets --all-features -- -D
  warnings` exited 0. `cargo +nightly fmt --all --check` exited 0
  (no Rust source changed). `cargo deny check` exited 0 (no
  dependency changes). `speccy verify` exited 0 with `verified 39
  specs, 223 requirements, 334 scenarios; 0 errors` (2 warnings,
  59 info notices — unchanged from pre-turn baseline). The
  task-scenario greps were also verified directly against the new
  module: `\{% if host == "claude-code" %\}` matches 3 times; the
  literal `\b5\b` matches 4 times; the
  `speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md`
  negative grep matches 0 times.

- Evidence: This task ships Markdown-only shipped-content (a new
  shared module under `resources/modules/skills/`). No
  `.speccy/specs/0039-cross-harness-orchestration-port/evidence/T-001.md`
  red-then-green binary-behaviour file was written — the
  task-scenario invariants are property-tests on the module's text
  content (file existence, `{% if host %}` block presence, literal
  `5` paired with the retry budget, absence of legacy names and
  `ARCHITECTURE.md` references), and the corresponding `rg` /
  `ls` invocations are recorded under "Hygiene checks" above as
  the observable proof. This follows the same content-only task
  evidence pattern that SPEC-0038 T-001 used and that its
  reviewer-tests persona accepted as a defensible pattern for
  shipped-Markdown tasks.

- Discovered issues: The pre-existing
  `every_migrated_spec_md_has_blank_line_after_each_close_tag`
  test in `speccy-core/tests/in_tree_specs.rs` was failing against
  `.speccy/specs/0039-cross-harness-orchestration-port/SPEC.md` at
  six positions (lines 328, 409, 475, 528, 584, 664 — every
  `</scenario>` close tag that was immediately followed by
  `</requirement>` without an intervening blank line). The failure
  was confirmed pre-existing via `git stash` + re-run before any
  T-001 edits landed. Strictly speaking this is a SPEC artifact
  bug, not T-001's scope; however, `cargo test --workspace` is a
  mandatory hygiene gate per `AGENTS.md` "Standard hygiene" and
  leaving the gate red would block every subsequent T-NNN turn on
  this SPEC. The fix was a single mechanical `Edit` with
  `replace_all=true` that turned the literal six-line `</scenario>\n</requirement>`
  occurrences into `</scenario>\n\n</requirement>`. No other SPEC
  content changed. The reviewer should confirm the fix is bounded
  to SPEC-0039 and did not bleed into other specs.

- Procedural compliance: This implementer entry lands directly in
  `.speccy/specs/0039-cross-harness-orchestration-port/journal/T-001.md`
  per the post-SPEC-0037 schema (sibling of `SPEC.md` and
  `TASKS.md`). No `<implementer-note>` block was written into
  TASKS.md (that element was retired in SPEC-0037 and the parser
  rejects it per TSK-006). The TASKS.md `state="..."` attribute
  for T-001 flips from `in-progress` to `in-review` as the final
  step of this turn. No shipped skill bodies under
  `resources/modules/skills/` or `resources/modules/phases/`
  required edits during this task — the implementer prompt and
  the speccy-work skill body at HEAD already documented the
  factoring contract correctly. The blank-line-after-close
  SPEC.md fix under "Discovered issues" was applied to a
  project-local SPEC artifact, not to a shipped skill body, so
  no friction-to-skill-update update was warranted there either.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-22T15:00:00Z" round="1">
T-001 delivers what REQ-001 promised for the orchestrator-module slice. The new `resources/modules/skills/speccy-orchestrate.md` carries every behavior the SPEC's `<done-when>` named for this skill: startup integrity check, outer dispatch loop, per-task retry counter with budget literal `5` (paired with the retry semantics on L32/L194/L200/L215 per DEC-005), status-line writes, and stop conditions. Host-variance is confined to three `{% if host == "claude-code" %}` blocks at sub-agent-spawn dispatch points only (L123, L144, L159) — non-dispatch prose like the Context-discipline section was generalized to "the host's sub-agent-spawn primitive" rather than naming Claude's `Task`/`Skill` tools, matching the SPEC's REQ-001 host-neutrality discipline. Sub-agent dispatch targets use the post-DEC-002/DEC-003 names throughout (`speccy-holistic-gate`, never `speccy-holistic-review`/`speccy-holistic-reviewer`/`speccy-holistic-fixer`). The four task-scenario greps were verified directly — all pass. No `ARCHITECTURE.md` references survive, no jinja variable wraps the retry budget, no legacy names remain. The implementer correctly deferred render + legacy-path deletion to T-004 (per `## Notes` in SPEC.md, byte-identity CHK-004 only makes sense once all wrappers exist) and correctly dropped the pilot's "local-only" non-goal because SPEC-0039 is the act that moves the skill out of that state. Goals, non-goals, user stories, and open questions all line up with the diff. No business-level drift detected.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-22T15:00:00Z" round="1">
Task-scenario invariants all verified directly via grep against `resources/modules/skills/speccy-orchestrate.md` (238 lines, non-empty):
- `{% if host == "claude-code" %}` matches at lines 123, 144, 159 — exactly at the three sub-agent dispatch points (work, review, ship), mirroring the `speccy-review.md` precedent the task body cites.
- `\b5\b` matches at lines 32, 194, 200, 215 — each contextually pairs `5` with the per-task retry budget ("bounded here in the orchestrator: 5 rounds", "5 rounds in a row", "the budget of 5 is the orchestrator's only per-task retry bound", "retry 2/5 after blocking review"). The negative grep `\{\{.*retry|budget.*\}\}` returns zero matches; the only `{{ ... }}` occurrences in the file are `{{ cmd_prefix }}`, never wrapping the integer.
- `speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md` returns zero matches — only the post-DEC-003 lifecycle name `speccy-holistic-gate` is used.

The pre-existing `every_migrated_spec_md_has_blank_line_after_each_close_tag` fix is correctly scoped: `git diff` shows six blank-line insertions between `</scenario>` and `</requirement>` at SPEC.md lines 326, 408, 475, 529, 586, 667 — all confined to `.speccy/specs/0039-cross-harness-orchestration-port/SPEC.md`; no other SPEC.md was touched. The test now passes (`cargo test -p speccy-core --test in_tree_specs every_migrated_spec_md_has_blank_line_after_each_close_tag`: `1 passed; 0 failed`).

Evidence pattern is defensible for this shipped-Markdown task: the task-scenarios are property-grep assertions on file text, and the journal's Hygiene-checks block records the exact rg counts (3 / 4 / 0 / 0) that map 1:1 to the scenarios, plus `cargo test --workspace` green. The `Evidence:` field's content-only justification cites SPEC-0038 T-001 as precedent, which is consistent — there is no binary behaviour here to red-then-green.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T15:00:00Z" round="1">
No security concerns. The deliverable is a Markdown skill body (natural-language LLM instructions) with no credentials, auth material, injection vectors, or sensitive data exposure. The three `{% if host == "claude-code" %}` dispatch blocks and `{{ cmd_prefix }}` template variable are the only template expressions; no budget integer is wrapped in a jinja variable (CHK-002 satisfied). The user-confirmation gate before ship is unconditional and hardcoded in prose. The startup integrity check's only bash snippet (`grep -n 'state="in-progress"'`) operates on a path derived from the CLI's own JSON output, not from unsanitized user input. No new dependencies.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T15:00:00Z" round="1">
`resources/modules/skills/speccy-orchestrate.md` conforms to all project style conventions. Leading and trailing bytes match the structural template (`speccy-review.md`): both start with `0x0a` (blank line before the `# {{ cmd_prefix }}…` heading) and end with `0x0a` (trailing newline). `{{ cmd_prefix }}` is the only jinja variable used; no spurious variables or template defaults. The three `{% if host == "claude-code" %}…{% else %}…{% endif %}` dispatch blocks follow the same inline format as the template — tag flush against prose, `{% else %}` and `{% endif %}` immediately after their branch's final sentence, no extra blank lines inside blocks. The literal integer `5` appears four times and is never wrapped in a jinja expression. Section headings use sentence-case throughout, matching project conventions. No legacy names, no `ARCHITECTURE.md` references, no dead code.
</review>
