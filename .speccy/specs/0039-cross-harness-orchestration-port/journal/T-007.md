---
spec: SPEC-0039
task: T-007
generated_at: 2026-05-22T22:30:00Z
---

<implementer date="2026-05-22T22:30:00Z" model="claude-opus-4.7[1m]/low" round="1">

- Completed:
  - **`README.md`** — added a second slash-command block introducing
    `/speccy-orchestrate` and `/speccy-holistic-gate` alongside the
    existing five-recipe golden path, framing the orchestrator as the
    opinionated end-to-end driver that chains the five recipes and
    delegates to `/speccy-holistic-gate` at the ship boundary. Updated
    the "Repo layout after `speccy init`" tree to list the orchestrate
    and holistic-gate skill directories under both `.claude/skills/`
    and `.agents/skills/`, bumped the skill count from "Eight" to
    "Ten" in the comment, and added entries for the new
    `agents/holistic-{reviewer,implementer}.md` / `.toml` sub-agent
    files in both host blocks.
  - **`docs/ARCHITECTURE.md`** — rewrote the "Long-Term Vision"
    section so the lead sentence acknowledges that the in-pack
    implementation + review orchestration loop now ships in the skill
    layer (`/speccy-orchestrate` chained with
    `/speccy-holistic-gate`), and removed any "multi-agent
    orchestration" bullet from the "Future layers (not v1)" list. The
    six remaining future-layer bullets (concurrent task pickup,
    worktree orchestration per task, cross-spec dependency reasoning,
    dashboard/kanban UI, production-telemetry feedback,
    cross-repository orchestration) stay verbatim. Also audited the
    "Skills / Harness Layer" tree (around line 1722) and added the two
    new shared-module entries (`speccy-orchestrate.md`,
    `speccy-holistic-gate.md`), and added two bullets to the
    workflow-recipes list at the end of that section naming
    `/speccy-orchestrate` and `/speccy-holistic-gate`.
  - **`AGENTS.md`** — rewrote line 34 (the "Long-term, speccy is the
    substrate underneath multi-agent harnesses…" sentence) so the
    long-term framing now applies only to cross-host and
    cross-repository harnesses, with the in-pack orchestration loop
    called out as already shipping via `/speccy-orchestrate` +
    `/speccy-holistic-gate`. Rewrote the `(Future) multi-agent
    harnesses…` users-bullet (line 44) to drop the `(Future)`
    parenthetical and call out that the in-pack orchestration loop
    ships in v1 while cross-host / cross-repository harnesses remain
    future. Added a new "V1.0 outcome" bullet describing the shipped
    orchestration loop alongside the existing seven-command CLI and
    shipped-skill-packs bullets.

- Undone: Nothing in scope. The task is prose-only across three
  files; no CLI surface change, no schema change, no
  `<requirement>`/`<task>` edits elsewhere, no test or rule edits.

- Hygiene checks:
  - `cargo test --workspace` → green (all suites pass; the largest
    suite reports 53 passed / 0 failed).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    → green (no warnings).
  - `cargo +nightly fmt --all --check` and `cargo deny check` not run
    locally for a prose-only edit; CI is the gate. No Rust source was
    touched, so fmt has nothing to check; no `Cargo.toml`/dependency
    changes were made, so deny is unaffected.

- Evidence:
  - `rg -n 'speccy-orchestrate' README.md` →
    `181:/speccy-orchestrate   Chain /speccy-work and /speccy-review across every`
    (CHK-015 satisfied).
  - `rg -nU 'Long-term, speccy is the substrate underneath multi-agent harnesses' AGENTS.md`
    → zero matches (CHK-017 satisfied).
  - `rg -n '\(Future\) multi-agent' AGENTS.md` → zero matches (CHK-016
    second half satisfied).
  - `rg -n 'multi-agent' docs/ARCHITECTURE.md` →
    `2524:multi-agent harnesses can build on. The in-pack implementation +`
    — the only remaining occurrence is in the intro framing sentence,
    not in any "Future layers (not v1)" bullet (CHK-016 first half
    satisfied).
  - `rg -n '\`init\`, \`status\`, \`next\`, \`check\`, \`verify\`, \`lock\`, \`vacancy\`' AGENTS.md`
    →
    `54:  \`docs/ARCHITECTURE.md\`: \`init\`, \`status\`, \`next\`, \`check\`, \`verify\`, \`lock\`, \`vacancy\`.`
    (seven-command CLI surface preserved on a single line — see
    "Procedural compliance" below for the small reflow that made this
    a stable single-line match).
  - `rg -n 'Feedback, not enforcement' AGENTS.md` → line 106 (durable
    principle #1 survived); `rg -n 'Stay small' AGENTS.md` → line 126
    (durable principle #5 survived).

- Discovered issues: One scenario in T-007 greps for the seven-command
  CLI surface as a single contiguous line (`rg -c` is per-line). In
  the pre-edit `AGENTS.md`, that string was already wrapped across
  two lines (lines 50-51 of the original), so the scenario would have
  been failing at HEAD too. I reflowed the surrounding bullet to put
  the seven `` `cmd` `` names on one line; the prose is otherwise
  unchanged. This is a documentation-shape fix, not a behavioural
  change, and it brings the AGENTS.md surface in line with what
  T-007's CHK-018 (the seven-command verbatim-survival check)
  expects.

- Procedural compliance: No shipped skill body required edits as a
  result of friction encountered during this task. The pre-existing
  multi-line wrap of the seven-command CLI surface in AGENTS.md is a
  documentation issue local to this repo (not a property of any
  skill body), so the fix landed in `AGENTS.md` directly rather than
  in a shipped skill template.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.7[1m]/medium" date="2026-05-22T22:13:20Z" round="1">
T-007 satisfies REQ-006: README.md adds a `/speccy-orchestrate` + `/speccy-holistic-gate` block alongside the five-recipe golden path and updates the repo layout tree; `docs/ARCHITECTURE.md` "Long-Term Vision" no longer lists multi-agent orchestration among future layers and adds the in-pack loop as current skill-layer artifact; `AGENTS.md` rewrites lines 34 and 44 to drop the `(Future)` framing for the in-pack loop and adds a new v1.0-outcome bullet for the shipped orchestration loop. CHK-015 (README has `speccy-orchestrate` at line 181), CHK-016 (no `(Future) multi-agent` in AGENTS.md; no multi-agent bullet in "Future layers (not v1)"), and CHK-017 (no "Long-term, speccy is the substrate underneath multi-agent harnesses" in AGENTS.md) all pass. Six durable principles preserved verbatim (lines 106-133), seven-command CLI surface preserved (line 54, reflowed onto one line as transparently disclosed in journal). Non-goals respected: prose-only diff (51 insertions, 17 deletions across exactly the three suggested files), no CLI surface change, no schema change, no `<requirement>`/`<task>` element changes, stay-small framing intact (orchestrator framed as skill-layer artifact, not CLI verb). The task scenario at TASKS.md L423 (`rg -n '^- \*\*Feedback, not enforcement\.\*\*'`) is a malformed authoring miss — the principle is and always was in numbered form (`1. **...**`) — but that does not reflect on T-007's implementation; the underlying REQ-006 done-when condition ("No existing positioning principle is reversed") is satisfied.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-22T22:13:20Z" round="1">
T-007 satisfies the substantive scenarios. The three SPEC-level checks pass against the diff: CHK-015 finds `/speccy-orchestrate` introduced at `README.md:181` in a new block framing the orchestrator as the end-to-end driver; CHK-016 finds zero multi-agent bullets under "Future layers (not v1)" in `docs/ARCHITECTURE.md:2528-2535` (the six remaining bullets are unchanged) and zero `(Future) multi-agent` matches in `AGENTS.md`; CHK-017 finds zero matches for the legacy substrate sentence in `AGENTS.md`. The task-local seven-command-CLI scenario also passes (`AGENTS.md:54` now has the seven backticked names on one line), and the implementer correctly disclosed under "Discovered issues" that this required a one-line reflow of pre-existing wrap. Durable principles survive verbatim at `AGENTS.md:106` (Feedback, not enforcement) and `AGENTS.md:126` (Stay small). Hygiene-gate: `cargo test --workspace` green (no Rust source touched). Evidence pattern is the same inline form prior turns (T-001..T-006) used and the tests persona already accepted as defensible for shipped-Markdown / prose-only tasks — there is no binary behaviour here to red-then-green; the grep counts in the journal map 1:1 to the scenarios and were independently re-verified against the working tree. Non-blocking scenario-quality observation for the orchestrator (not a T-007 defect): the durable-principles task-scenario in `TASKS.md:423` uses regex `^- \*\*Feedback, not enforcement\.\*\*` but `AGENTS.md`'s Core principles is a numbered list (`1. **Feedback…**`), so that regex returns zero matches against both the pre-edit and post-edit file. The underlying invariant (principle survives verbatim) is satisfied, and the implementer's evidence used a looser `'Feedback, not enforcement'` regex that demonstrates the survival; the buggy `^- ` anchor is a SPEC scenario-defect, not an implementation gap, and fixing it would require a SPEC amend rather than a T-007 edit (T-007 is prose-only across README/ARCHITECTURE/AGENTS). Surfacing here so an amend turn can pick it up if desired.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T22:13:20Z" round="1">
T-007 is a prose-only documentation task — README.md, AGENTS.md, and docs/ARCHITECTURE.md. No code, endpoints, credentials, secrets, or access-control logic were modified. All "password", "bcrypt", and "token" occurrences in the diff are illustrative example content inside the architecture doc (showing sample reviewer verdicts and SPEC examples), not real credentials or live security configuration. No security-relevant surface was introduced or altered.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-22T22:13:20Z" round="1">
All three files follow the project's existing style conventions. The AGENTS.md, README.md, and `docs/ARCHITECTURE.md` changes are surgical prose edits that match surrounding formatting in each file. The only borderline change is the reflow of the seven-command CLI line in AGENTS.md (lines 50-51 of the pre-edit file, collapsed to line 54 in the post-edit file). The implementer explicitly documents this in the journal under "Discovered issues": the CHK scenario requires a single-line grep, and the pre-edit form was already split across two lines, so the scenario would have failed without this reflow. The change is minimal and the justification is sound. No new `#[allow]` / `#[expect]` suppressions, no dead imports, no parallel helpers, no formatting drift relative to surrounding text. Naming in the two added ARCHITECTURE.md recipe bullets and tree entries matches the existing kebab-case skill-name pattern used throughout that file. The new README.md recipe block sits after the existing five-recipe block in the identical code-fence style, consistent with the surrounding blocks.
</review>
