---
spec: SPEC-0039
task: T-003
generated_at: 2026-05-22T18:30:00Z
---

<implementer date="2026-05-22T18:30:00Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Authored two new host-neutral persona modules at
  `resources/modules/personas/holistic-reviewer.md` (177 lines) and
  `resources/modules/personas/holistic-implementer.md` (195 lines)
  by factoring the host-neutral bodies of the two holistic-loop
  sub-agent pilots at `.claude/agents/speccy-holistic-reviewer.md`
  and `.claude/agents/speccy-holistic-fixer.md`. YAML frontmatter
  was dropped from both — per-host frontmatter (model pin, effort
  level, allowed tools) will be carried by T-004's per-host agent
  templates under `resources/agents/.claude/agents/*.md.tmpl` and
  `resources/agents/.codex/agents/*.toml.tmpl`. Claude-Code-specific
  tool names (`Edit`, `Write`, `NotebookEdit`, `Task`, `Read`,
  `Grep`, `Glob`) were generalized to host-neutral wording: the
  "Read-only role" section now reads "edit/write/notebook-edit
  primitives, or destructive `Bash` invocations such as `git stash`,
  `git reset`, `git restore`, or anything else that mutates state",
  and the read-only allow-list now reads "Read-only operations
  (reading files, searching for content, listing directories, and
  non-destructive `Bash` invocations like `git diff`, `git log`,
  `cat`, `ls`)". Universal `Bash` references remain because the
  shell tool is host-agnostic. The lifecycle-skill name was updated
  from `/speccy-holistic-review` to
  `{{ cmd_prefix }}speccy-holistic-gate` per DEC-003 (using the
  shared-module jinja variable rather than hardcoding the slash
  prefix), matching the precedent at
  `resources/modules/skills/speccy-holistic-gate.md`. The sister
  sub-agent name was updated from `speccy-holistic-fixer` to
  `holistic-implementer` per DEC-002 (and the surrounding prose
  switched from "fixer" to "implementer" wherever the role was
  named). The reviewer's verdict-return contract dropped the
  Claude-specific cross-reference (the pilot's pointer at
  `.claude/agents/reviewer-business.md`) in favor of an inline
  description of the slash-suffix-on-`model` convention. The
  implementer's hygiene-gate section dropped the speccy-the-project
  cargo command list (`cargo test --workspace`, `cargo clippy ...`,
  `cargo +nightly fmt --all --check`, `cargo deny check`) in favor
  of "the project's standard hygiene suite as documented in
  `AGENTS.md`" — matching T-002's precedent in the shared
  holistic-gate skill body. Cross-skill references to
  `speccy-work`, `speccy-review`, and `speccy-amend` all use
  `{{ cmd_prefix }}` for portability. The `DEC-008` reference in
  the reviewer pilot's "HOLISTIC.md write" parenthetical was
  generalized to "single-writer per the holistic-gate skill body"
  to keep the shared module decision-register-free (the same
  portability decision T-002 made for the parallel reference in
  `speccy-holistic-gate.md`). All four task-scenario greps were
  verified directly against the new files: file existence with
  non-empty bodies (177 + 195 lines); zero matches for
  `speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md` in
  either file; neither file begins with `---` (frontmatter
  absent — first non-blank line of each is the H1 heading).

- Undone: T-004 (Codex grant module + per-host wrappers + per-host
  agent templates + render + delete legacy pilots) is the next
  task. T-004 is the consumer of the persona module files this
  task creates — its per-host agent templates will `{% include %}`
  these shared bodies under
  `resources/agents/.claude/agents/holistic-{reviewer,implementer}.md.tmpl`
  and `resources/agents/.codex/agents/holistic-{reviewer,implementer}.toml.tmpl`,
  attach host-native frontmatter (model pin + effort), and the
  subsequent `speccy init --force` render will produce the
  rendered Claude/Codex agent files. T-005 (retire Codex
  prose-spawn in `speccy-review.md`), T-006 (strip
  `ARCHITECTURE.md` from `speccy-core/src/prompt/id_alloc.rs`),
  and T-007 (reposition the orchestration loop in `README.md` /
  `.speccy/ARCHITECTURE.md` / `AGENTS.md`) are deferred to their
  own implementer turns per the single-task primitive contract.
  The legacy pilot files at `.claude/agents/speccy-holistic-reviewer.md`
  and `.claude/agents/speccy-holistic-fixer.md` are left in place;
  their deletion is T-004's responsibility (post-render cleanup
  step 4, alongside the legacy `speccy-holistic-review` skill
  directory).

- Hygiene checks: `cargo test --workspace` exited 0 (full
  workspace suite passed; no regressions from the SPEC.md text
  changes — none happened here, only Markdown additions under
  `resources/modules/personas/`). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0 with zero
  warnings (the deliverable is Markdown only; no Rust source
  changed). `cargo +nightly fmt --all --check` exited 0 (no diff).
  `cargo deny check` exited 0 (advisories ok, bans ok, licenses
  ok, sources ok — no dependency changes). The four task-scenario
  invariants were also exercised inline:
  `ls resources/modules/personas/holistic-reviewer.md resources/modules/personas/holistic-implementer.md`
  → both exist; `wc -l ...` → 177 / 195 lines (non-empty);
  `rg -n 'speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md' ...`
  → zero matches across both files; `head -3 ...` → both files'
  first non-blank line is the H1 heading (no leading `---` YAML
  fence on either).

- Evidence: No `.speccy/specs/0039-cross-harness-orchestration-port/evidence/T-003.md`
  red-then-green binary-behaviour file was written. This task
  ships Markdown-only shipped-content (two new shared persona
  modules under `resources/modules/personas/`), and the
  task-scenario invariants are property-tests on file text
  (existence, content-grep negative matches, frontmatter absence).
  The corresponding `ls`/`rg`/`head` invocations are recorded
  under "Hygiene checks" above as the observable proof. This
  follows the same content-only task evidence pattern that
  T-001 and T-002 used and that the SPEC-0038 T-001 reviewer-tests
  persona accepted as a defensible pattern for shipped-Markdown
  tasks.

- Discovered issues: The pilot reviewer's "model attribute"
  bullet ("required, copy your model identity per the convention
  in `.claude/agents/reviewer-business.md`") pointed at a
  Claude-specific rendered path. Inlining the slash-suffix
  convention directly into the persona body (rather than referring
  to a sister agent file that the consuming project may not have
  rendered yet) felt cleaner than the alternative
  `{% include "modules/personas/verdict_return_contract.md" %}`
  approach the per-task reviewer personas use — because the
  holistic personas have a fundamentally different verdict shape
  (`<drift-review>` / `<holistic-fix>` with `round` and
  `verdict="pass|blocking|stuck"` attributes), the per-task
  shared verdict-return contract doesn't apply to them. Flagging
  here in case the reviewer prefers a different generalization
  strategy.

- Procedural compliance: T-003's `state="..."` attribute in
  `.speccy/specs/0039-cross-harness-orchestration-port/TASKS.md`
  flipped from `pending` → `in-progress` at turn start and from
  `in-progress` → `in-review` as the final step of this turn. No
  `<implementer-note>` block was written into TASKS.md (that
  element was retired in SPEC-0037 and the parser rejects it per
  TSK-006). This `<implementer>` block lives in the per-task
  journal file at
  `.speccy/specs/0039-cross-harness-orchestration-port/journal/T-003.md`,
  newly created in this turn with the canonical three-field YAML
  frontmatter (`spec`, `task`, `generated_at`) and `round="1"`.
  No shipped skill bodies under `resources/modules/skills/` or
  `resources/modules/phases/` required edits during this task —
  the `/speccy-work` skill body at HEAD and the journal-implementer
  reference doc both correctly describe the post-SPEC-0037
  per-task journal schema and the six-field handoff template.
  No friction-to-skill-update update was triggered.
</implementer>

<review date="2026-05-22T19:00:00Z" model="claude-opus-4-7[1m]" persona="business" verdict="pass" round="1">
T-003 faithfully factors the two holistic-loop persona pilots into `resources/modules/personas/holistic-reviewer.md` and `resources/modules/personas/holistic-implementer.md`, satisfying REQ-001's persona-body bullets and serving user-story #1 (single source of truth). All four task-scenario invariants pass: both files exist (177 / 195 lines), zero matches for `speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md`, and neither begins with YAML frontmatter. The Claude-Code-specific tool names (`Edit`, `Write`, `NotebookEdit`, `Task`, `Read`, `Grep`, `Glob`) are correctly generalized to host-neutral wording; the DEC-008 reference is generalized to "single-writer per the holistic-gate skill body" consistent with T-002's precedent in `resources/modules/skills/speccy-holistic-gate.md`; cross-skill references use `{{ cmd_prefix }}` matching the parallel skill modules; sub-agent names use the post-DEC-002 forms (`holistic-implementer`, `holistic-reviewer`); the lifecycle skill name uses post-DEC-003 `speccy-holistic-gate`. The implementer's call to inline the slash-suffix-on-`model` convention directly (rather than cross-referencing the Claude-specific `reviewer-business.md` agent path or using `{% include "verdict_return_contract.md" %}`) is well-justified — the holistic personas emit `<drift-review>` and `<holistic-fix>` blocks with `round` and `verdict="pass|blocking|stuck"` attributes, a different verdict shape from the per-task reviewer contract, so include-reuse would be wrong. No non-goal violation (no CLI surface change, no jinja-variable budgets, no model abstraction across hosts). REQ-001's "pilot paths no longer exist" item appropriately defers to T-004's render-and-cleanup step per the SPEC's Notes section and the task body. No drift between SPEC intent and shipped content.
</review>

<review date="2026-05-22T19:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-003's three task-scenarios are property-grep assertions on file text and all pass when re-run directly: `ls resources/modules/personas/holistic-{reviewer,implementer}.md` returns both paths (177 + 195 lines, non-empty); `rg 'speccy-holistic-(?:review|reviewer|fixer)|ARCHITECTURE\.md' resources/modules/personas/holistic-{reviewer,implementer}.md` returns zero matches; `head -3` on each file shows the H1 heading as the first non-blank line (no leading `---` YAML fence). Diffing each new persona module against its source pilot (`.claude/agents/speccy-holistic-{reviewer,fixer}.md` minus the 7-line frontmatter) confirms the documented transformations: Claude-tool names (`Edit`/`Write`/`NotebookEdit`/`Read`/`Grep`/`Glob`/`Task`) generalized to host-neutral primitive language; `/speccy-holistic-review` → `{{ cmd_prefix }}speccy-holistic-gate`; `holistic-fixer`/`speccy-holistic-fixer` → `holistic-implementer`; `DEC-008` → "single-writer per the holistic-gate skill body"; the model-attribute cross-reference at `.claude/agents/reviewer-business.md` inlined into the persona body. The Evidence field is present and the content-only justification mirrors the pattern this SPEC's T-001 and T-002 reviewer-tests personas already accepted — there is no binary behaviour to red-then-green when the deliverable is two host-neutral Markdown modules whose scenarios are property assertions on file text. The corresponding `ls`/`rg`/`wc -l`/`head` invocations are recorded under Hygiene checks and map 1:1 to the scenarios; `cargo test --workspace`/`clippy`/`fmt`/`deny check` all exited 0 (Rust source unchanged). No tests-persona drift detected.
</review>

<review date="2026-05-22T19:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="security" verdict="pass" round="1">
Both new persona modules (`resources/modules/personas/holistic-reviewer.md` and `resources/modules/personas/holistic-implementer.md`) are Markdown prompt bodies with no code execution, credential handling, or external service references. The security surface is limited to what the personas instruct agents to do. Reviewer persona: enforces a strict read-only boundary (lines 4-13 of holistic-reviewer.md), explicitly names destructive git operations that must be refused, and introduces no new filesystem access paths beyond the pilot. Implementer persona: write-capable but constrained; the "What you must NOT modify" section (holistic-implementer.md:65-83) preserves the same access boundary as the pilot. Single-writer rule for HOLISTIC.md is clearly stated, correctly preventing parallel-write races. The generalization of the hygiene gate from a hardcoded cargo command list to `AGENTS.md`-delegated gates is appropriate for portability and within the SPEC's stated scope — not a security regression. No credential storage, secret references, token lifecycle issues, injection vectors, or privilege escalations introduced. No `ARCHITECTURE.md` references, no YAML frontmatter baked in that could hard-pin a model or tool allowlist in the shared body.
</review>

<review date="2026-05-22T19:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Both new persona modules (`resources/modules/personas/holistic-reviewer.md` and `resources/modules/personas/holistic-implementer.md`) are clean against all style criteria. No leading YAML frontmatter (both start with `# Holistic Drift ...`). No banned references (`speccy-holistic-review|reviewer|fixer`, `ARCHITECTURE.md`): zero matches. Trailing newlines present on both files (byte probe: `0a`). No `{% set persona_name %}` header — correctly omitted because these personas don't include `verdict_return_contract.md` (which uses `{{ persona_name }}` and targets `<review persona="...">` elements that don't apply to `<drift-review>` / `<holistic-fix>` verdicts). The omission is idiomatic, not dead code. `{{ cmd_prefix }}` usage throughout `holistic-implementer.md` and once in `holistic-reviewer.md` mirrors the sibling skill modules (`speccy-orchestrate.md`, `speccy-holistic-gate.md`) and is appropriate: these personas describe invoking other skills, unlike the per-task reviewer personas that contain only review logic. Verdict contracts and "do not write" rules are inlined rather than `{% include %}`'d — correct, because the shared `verdict_return_contract.md` and `no_tasks_md_writes.md` modules are tailored to the `<review>` element shape, not `<drift-review>` / `<holistic-fix>`. File names match the post-DEC-002 naming contract exactly. No Rust source changed; all four CI gates confirmed passing per the implementer record.
</review>
