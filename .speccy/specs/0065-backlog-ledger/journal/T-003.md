---
spec: SPEC-0065
task: T-003
generated_at: 2026-06-14T03:16:43Z
---

<implementer date="2026-06-14T03:16:43Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Extended the existing skill body `resources/modules/skills/speccy-brainstorm.md` in place — no new top-level symbols introduced. Reused the canonical `modules/references/backlog-ledger.md` reference (authored in T-001) via `{% include %}`, and mirrored the already-shipped wording/structure of the sibling `speccy-plan.md` backlog steps (T-002) so brainstorm reads as the same author wrote it. Searched `resources/modules/` for any existing brainstorm-side backlog wiring — none found, this task is the brainstorm producer wiring.
- Completed: Wired `speccy-brainstorm.md` for all three covered REQs. REQ-003: added a backlog-read instruction at step 1 "Explore project context" — reads `.speccy/BACKLOG.md` when present, folds entries into the step-3 candidate framings, absence silent/non-fatal. REQ-004: added new step 7 "Record a future-spec candidate" that appends a four-field entry with `SPEC-NNNN, brainstorm` provenance, self-creating the file/header when absent, distinguishing a future-spec candidate (backlog) from a spec-local Non-goal (SPEC `## Non-goals` via the routing list); explicitly no strike since brainstorm never promotes. REQ-001: added `{% include "modules/references/backlog-ledger.md" %}` at that authoring step. Renumbered the old step 7 to 8 and reconciled three now-stale "nothing written to disk" claims (intro, step 8 close, Exit) so docs match the one backlog write. Ran `just reeject`; both `.claude` and `.agents` brainstorm packs regenerated with parity.
- Undone: Nothing in scope deferred. T-004 (ship producer) and T-005 (bootstrap conventions line) remain separate pending tasks per the decomposition.
- Hygiene checks: cargo test --workspace -> exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings -> exit 0; cargo +nightly fmt --all --check -> exit 0; cargo deny check -> exit 0.
- Evidence: CHK roll-call across covered REQs. REQ-001/CHK-001 (include resolves + parity, no orphan): demonstrated — `{% include %}` directive added; reeject regenerated both host packs; `grep` confirms 7 backlog-shape matches in each ejected SKILL.md. REQ-001/CHK-002 (reference clarity/parallelism vs memory-ledger): judgment-only — persona-review judgment, no change to the reference itself this task. REQ-003/CHK-005 (read placement + silent absence): demonstrated — read instruction added at step 1 with explicit "absence is normal and silent, proceed without comment"; placement correctness is persona judgment. REQ-004/CHK-006 (four-field append + future-vs-Non-goal routing): demonstrated — step 7 appends four-field entry with brainstorm provenance and routes spec-local cuts to `## Non-goals`; the future-vs-local distinction is persona judgment. No CHK is hygiene-only here; resource-prose ID-ban + wrapper-description lints (resource_prose_hygiene.rs) gate the body and passed under cargo test.
- Discovered issues: None. The existing brainstorm body had three "nothing written to disk / ephemeral" claims that the new backlog write would have falsified; I corrected all three in the same diff rather than leaving doc drift.
- Procedural compliance: Followed speccy-work first-attempt recipe. Clean-tree gate passed before dispatch. Memory ledger (`.speccy/MEMORY.md`) read — header only, no entries matching this area, silent no-op. Edited only the source module under `resources/`, never the ejected files directly; ran `just reeject` to regenerate. Generic placeholders only (`SPEC-NNNN, brainstorm`); no real artifact IDs in the body.
</implementer>
<review date="2026-06-14T03:17:54Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface in scope. T-003 wires `speccy-brainstorm` skill prose to read and append a user-owned, git-tracked `.speccy/BACKLOG.md` convention file; the CLI never reads or parses it (DEC-001), so no new endpoint, auth boundary, parser, injection sink, secret, crypto, or dependency is introduced. The backlog path is a hardcoded literal (`.speccy/BACKLOG.md`), not derived from user input, so no path-traversal vector. The four-field entry shape carries intent metadata (Title / What & why / Deferred-because / Provenance), not credentials, and authoring discipline mandates terse one-phrase fields with the skill instructed not to dump session context back at the user — no sensitive-data-into-tracked-file leak path. Ejected `.claude` / `.agents` packs match the source include verbatim.
</review>
<review date="2026-06-14T03:18:03Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. T-003 is prose-only skill wiring in `resources/modules/skills/speccy-brainstorm.md` plus its regenerated ejected packs; there is no executable code, branching logic, Option/Result handling, indexing, concurrency, or resource acquisition in the diff, so the correctness lane has no defect surface. The instruction-level control flow it does encode is internally consistent: the step-1 backlog read states absence as silent/non-fatal in both the read step and the Exit/routing recap; step 7's append correctly distinguishes a future-spec candidate (backlog) from a spec-local Non-goal (SPEC `## Non-goals`) and states no-strike for brainstorm (which never promotes); and the three "nothing written to disk" intro/close/Exit claims were reconciled to the single backlog write with no contradictory residue. Include directive resolves and the four-field shape + header appear with parity in both `.claude` and `.agents` ejected packs (no orphan, no half-eject). No correctness finding at or above the reporting threshold.
</review>
<review date="2026-06-14T03:18:10Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. Diff satisfies all three covered requirements' observable behaviour for the brainstorm producer.

REQ-001 done-when: `{% include "modules/references/backlog-ledger.md" %}` added at the step-7 authoring point in `resources/modules/skills/speccy-brainstorm.md`; both ejected packs (`.claude` and `.agents` SKILL.md) carry the resolved four-field shape — parity holds, reference not orphaned.

REQ-003 done-when: step 1 "Explore project context" reads `.speccy/BACKLOG.md` when present and folds entries into the step-3 candidate framings, with explicit "Absence is normal and silent ... proceed without comment" — both the populated-backlog and missing-file behavior branches are covered.

REQ-004 done-when: new step 7 appends a four-field entry on deliberate future-spec deferral, self-creates the header verbatim when absent, carries `SPEC-NNNN, brainstorm` provenance (originating spec + phase), and routes a merely-spec-local cut to the SPEC's `## Non-goals` instead — the future-vs-local distinction the requirement names is present and parallels the sibling speccy-plan wiring.

Non-goals respected: pure skill-body prose, no `speccy backlog` subcommand, no grammar/lint/verify participation, no status/priority/ordering field, and no review/vet producer wiring (this task is brainstorm-only). Correctly omits the promotion strike — brainstorm writes no SPEC and never promotes, matching the SPEC's plan-only strike (REQ-006). The three reconciled "nothing written to disk" claims (intro, step-8 close, Exit) are in-scope: the new backlog write would otherwise have left those claims false; no scope creep. No real Speccy SPEC ids leaked into the body (SPEC-0042 lives only in the included worked-example reference).
</review>
<review date="2026-06-14T03:19:38Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No style violations found in the diff.

Checked `resources/modules/skills/speccy-brainstorm.md` (uncommitted), both ejected packs (`.claude/skills/speccy-brainstorm/SKILL.md`, `.agents/skills/speccy-brainstorm/SKILL.md`), and the previously-committed `resources/modules/references/backlog-ledger.md`.

- Placeholder discipline: all provenance cites `SPEC-NNNN, brainstorm` (never a real ID); `SPEC-NNNN` used as CLI arg placeholder. Clean.
- Artifact-ID ban: no real SPEC/REQ/CHK/DEC/T ids outside the `modules/references/` carve-out. The worked example in `backlog-ledger.md` uses `SPEC-0042, ship` — the one whitelisted concrete id family. Clean.
- Meta-annotation ban: no `// per REQ-NNN`, `// per AGENTS.md`, or governance-doc citations in the skill body. Clean.
- No-provenance in production code: not applicable (skill bodies only).
- Include placement: `{% include "modules/references/backlog-ledger.md" %}` at step 7, matching the precedent in `speccy-plan.md`. Consistent.
- Ejected parity: both `.claude` and `.agents` packs carry identical backlog count (12 matches for BACKLOG/backlog-ledger/four-field/Provenance terms) — parity holds.
- Wrapper frontmatter: `description` is 678 chars (within 1024-char limit), angle-bracket-free, and carries both "Use when" and "Do NOT trigger" clauses. Clean.
- Reuse survey: present in the implementer block; the named symbols (`backlog-ledger.md` include, speccy-plan.md wording mirroring) exist and are the things the diff actually calls. Honest.
- Docs match code: three "nothing written to disk" / "ephemeral" claims corrected in the intro, step 8 close, and Exit to reflect the one new backlog write. No stale prose introduced.
- Dead code / unused imports: not applicable (Markdown skill body, no Rust diff).
</review>
<review date="2026-06-14T03:19:42Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. Convention-only skill-wiring task (DEC-001: CLI never touches the backlog), no Rust changed, so the sole deterministic gate is resource-to-ejected parity — CHK-001's `demonstrated` label. I falsified it by re-execution rather than trusting the grep claim: built `speccy` from the working-tree `resources/` (embedded via include_dir! at compile time), ejected both hosts into a scratch dir outside the repo, and diffed against the working-tree ejected packs — `.claude/skills/speccy-brainstorm/SKILL.md` and `.agents/skills/speccy-brainstorm/SKILL.md` are byte-identical (CLAUDE_PARITY_OK / AGENTS_PARITY_OK). The `{% include "modules/references/backlog-ledger.md" %}` (source line 206) resolves into the ejected output — the file header "Speccy backlog — future-spec candidates" and the four fields (Title / What & why / Deferred-because / Provenance) appear in the ejected SKILL.md — so the reference is non-orphaned and a stale or broken-include pack would fail the diff. No working-tree mutation: build targets the gitignored `target/`, eject went to a /tmp scratch dir, scratch removed.

Evidence roll-call is complete and plausibly labelled. All four CHKs under the covered REQs appear: CHK-001 (REQ-001) demonstrated and re-runnable (named ejected paths), CHK-002 (REQ-001) judgment-only — correct, the reference itself was authored in T-001 and is unchanged here; CHK-005 (REQ-003) and CHK-006 (REQ-004) demonstrated as prose with the placement being persona judgment. Each scenario explicitly states it is a persona-review judgment, not a scriptable assertion, so none can be driven by a deterministic command on prose.

Prose checked against the `<behavior>` boundary arms. REQ-003 step-1 read covers both arms (populated backlog folded into step-3 candidate framings; "Absence is normal and silent ... proceed without comment"). REQ-004 step-7 append covers the negative path explicitly ("a cut that is merely out of the forthcoming spec's scope is a spec-local Non-goal — it rides into the SPEC's `## Non-goals` ... not the backlog"), with provenance `SPEC-NNNN, brainstorm` and self-creating header. The three pre-existing "nothing written to disk / ephemeral" claims that the new backlog write would have falsified (intro, step-8 close, Exit) were reconciled in the same diff, so no doc drift. No vacuous-mock, snapshot-baked-bug, or any-error-catch concerns — there is no test code to falsify and the one deterministic invariant (parity) catches a stale/un-reejected pack.
</review>
