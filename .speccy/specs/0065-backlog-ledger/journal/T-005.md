---
spec: SPEC-0065
task: T-005
generated_at: 2026-06-14T03:29:49Z
---

<implementer date="2026-06-14T03:29:49Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Full area-map (round 1). Reuse-as-is — the conventions reference `resources/modules/references/agents-md-speccy-conventions.md` already holds the canonical block; extended its existing prose with one line, no new symbol. The `.speccy/BACKLOG.md` path, its four-field shape, and the plan/ship producer wiring already exist from siblings T-002/T-003/T-004 (`backlog-ledger.md`, `speccy-plan.md`, `speccy-brainstorm.md`, `speccy-ship.md`) — reused those as the authoritative phrasing source rather than inventing new role language. No new top-level symbol introduced (prose-only change).
- Completed: Added one terse line to the conventions reference naming `.speccy/BACKLOG.md` and its read/append role (planning reads; plan and ship append), placed directly after the journal-location line. Ran `just reeject`; the line reached the ejected bootstrap conventions block at `.claude/skills/speccy-bootstrap/SKILL.md` (and `.agents/` parity).
- Undone: Nothing outstanding for REQ-007.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: REQ-007 covered. CHK-009 (ejected bootstrap conventions-block source at HEAD references `.speccy/BACKLOG.md` path + one-line role): demonstrated — `.claude/skills/speccy-bootstrap/SKILL.md:182-183` and source `resources/modules/references/agents-md-speccy-conventions.md` both carry the line; reejection parity confirmed via `just reeject` (1 overwritten = the bootstrap SKILL). Wording adequacy is a persona-review judgment per the scenario.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work recipe. Edited the source reference under resources/, not the ejected file; ran just reeject. No source-module fixes needed.
</implementer>
<review date="2026-06-14T03:31:02Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security surface in this slice. The change is a one-line prose addition to the bootstrap conventions reference (`resources/modules/references/agents-md-speccy-conventions.md:26-27`) naming `.speccy/BACKLOG.md` and its read/append role, with ejected parity in `.claude/` and `.agents/` bootstrap SKILLs. Checked: no auth/authz boundary, no input parsing or injection vector (DEC-001: the CLI never reads the backlog, so no parse surface is added), no secret handling, no sensitive-data exposure (backlog is user-authored future-spec intent, git-tracked, no credentials), no crypto, no rate-limiting concern. The producer-boundary wording ("plan and ship append") matches DEC-002 and does not imply an over-broad write authorization for review/vet.
</review>
<review date="2026-06-14T03:31:02Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defect. T-005 is a prose-only edit adding one conventions line to `resources/modules/references/agents-md-speccy-conventions.md:26-27` naming `.speccy/BACKLOG.md` and its read/append role; there is no control flow, Option/Result handling, concurrency, or resource lifecycle in scope. Verified reejection parity: the identical line reaches both ejected bootstrap blocks (`.claude/skills/speccy-bootstrap/SKILL.md` and `.agents/skills/speccy-bootstrap/SKILL.md`); `.codex/` ships no bootstrap skill, so no parity gap. The stated producer roles ("planning reads; plan and ship append") are internally consistent with the spec's DEC-002 producer set.
</review>
<review date="2026-06-14T03:31:07Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
REQ-007 satisfied. The conventions-block source `resources/modules/references/agents-md-speccy-conventions.md:26-27` names `.speccy/BACKLOG.md` plus its read/append role ("planning reads it as candidate input; plan and ship append to it"), placed directly after the journal-location line as the task directed. Both `<done-when>` clauses hold: the source carries the line, and reejection parity reached both ejected trees — `.claude/skills/speccy-bootstrap/SKILL.md` and `.agents/skills/speccy-bootstrap/SKILL.md` carry the byte-identical line. The `<behavior>` Given/When/Then (block names the path and its read/append roles) is met. Goal "an agent reading the conventions block learns the backlog exists and what reads/writes it" is served: an agent not running plan or ship now discovers the artifact. Noted but not blocking: the producer set per DEC-002 is plan/brainstorm/ship/manual, and the terse line names only "plan and ship append" (brainstorm rides under the block's existing "Plan" grouping; manual is omitted) — this is verbatim what the T-005 body prescribed ("plan/ship append") and consistent with the stated terseness bar, so it is an acceptable faithful reduction, not drift. No non-goal violated: no CLI subcommand, grammar, or lint surface introduced; change is prose-only and surgical.
</review>
<review date="2026-06-14T03:31:54Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The diff is a pure prose addition to one source file with two ejected outputs.

Checked:
- `resources/modules/references/agents-md-speccy-conventions.md`: the added two-line block (`Future-spec candidates live in \`.speccy/BACKLOG.md\`: planning reads it as / candidate input; plan and ship append to it.`) is placed correctly after the journal-location line and before the CI line. No artifact-ID provenance annotations. Wording is terse and consistent with the surrounding block's register.
- Ejected parity: `.claude/skills/speccy-bootstrap/SKILL.md:182-183` and `.agents/skills/speccy-bootstrap/SKILL.md` both carry the identical line, matching the source — parity holds.
- No dead code, no unused imports, no naming drift (prose-only change).
- No suppression annotations added.
- Reuse survey present in implementer block; named symbol (`agents-md-speccy-conventions.md`) exists and is the file actually modified — survey is honest.
- No parallel reinvention found: the added line uniquely names a path that no other conventions-block line covers.
- TASKS.md flip from `pending` to `in-review` is the only other working-tree change (plus the untracked journal file) — both are correct orchestration artifacts, not style concerns.
</review>
<review date="2026-06-14T03:32:13Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
REQ-007 / CHK-009 satisfied; prose-only conventions-block edit, no project test owed. The single covered CHK-009 is a structural path-reference scenario whose wording adequacy is explicitly a persona-review judgment, so no red-then-green test applies — and a substring assertion over this curated prose would be vacuous per AGENTS.md. Verified the deterministic demonstration the Evidence roll-call cites: the backlog line and its read/append role are byte-identical across the source reference `resources/modules/references/agents-md-speccy-conventions.md:26-27` and both ejected bootstrap packs (`.claude/skills/speccy-bootstrap/SKILL.md:182-183`, `.agents/skills/speccy-bootstrap/SKILL.md:182-183`), placed directly after the journal-location line as the task required; reejection parity holds (the `<done-when>` second bullet). Falsification check: a wrong path, an omitted role, or a source-only landing would be caught by the same file scan. Evidence roll-call present and complete — CHK-009 labeled `demonstrated` with a file:line citation, hygiene gates exit 0; inline-in-journal roll-call matches this spec's established convention across its four passed siblings, and no fabrication pattern applies to a doc-only change with no runner output to invent.
</review>
