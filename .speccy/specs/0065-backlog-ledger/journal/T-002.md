---
spec: SPEC-0065
task: T-002
generated_at: 2026-06-14T03:08:34Z
---

<implementer date="2026-06-14T03:08:34Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area = resources/modules/skills/speccy-plan.md plus the already-shipped reference resources/modules/references/backlog-ledger.md (landed by sibling T-001). Reuse-as-is: the backlog-ledger.md reference (file header + four-field shape + worked example) — pulled in via {% include %} rather than restated. Reuse-as-is: existing include convention `modules/references/...` (matches spec-self-review-core, branch-guard, commit-recipe includes in this same body) and the numbered-step prose shape. Extend: none. Write-fresh: the three prose blocks (read at step 1, append/distinction at step 2, strike at step 5) — searched speccy-plan.md and sibling brainstorm/ship bodies for existing backlog wiring (grep "backlog"), found none, so authored fresh; brainstorm/ship wiring is sibling tasks T-003/T-004, out of this task's scope.
- Completed: Wired resources/modules/skills/speccy-plan.md for all four covered REQs — REQ-003 read (.speccy/BACKLOG.md surfaced as candidate slices at step 1 intake, absence silent/non-fatal); REQ-004 append (four-field entry on deliberate future-spec scope-cut at step 2, self-create with verbatim header, provenance `SPEC-NNNN, plan`, with explicit route of spec-local exclusions to `## Non-goals`); REQ-006 strike (delete promoted entry at new step 5, no tombstone/residue, git+SPEC provenance as trail); REQ-001 include ({% include "modules/references/backlog-ledger.md" %} at the authoring step). Ran `just reeject` and committed-ready regenerated packs (.claude + .agents plan SKILL.md overwritten; 1 overwritten, 35 unchanged).
- Undone: Nothing in scope. Brainstorm read/append wiring (T-003) and ship append wiring (T-004) are separate sibling tasks and intentionally untouched; the reference is now {% include %}d by at least one authoring skill (plan), satisfying CHK-001's non-orphan gate from this task's side.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: No Rust code changed (DEC-001: convention-only, CLI never touches the backlog), so no new scriptable tests; behavior is skill-body prose verified by reejection parity. CHK-by-CHK roll call for this task's covered REQs — CHK-001 (REQ-001, resource-to-ejected parity, no orphan): demonstrated — grep confirms backlog-ledger.md content (file header "Speccy backlog — future-spec candidates", four fields Title/What & why/Deferred-because/Provenance) appears in ejected .claude/skills/speccy-plan/SKILL.md and .agents parity copy after reeject; the include is now resolved by an authoring skill so the reference is non-orphaned. CHK-002 (REQ-001, reference clarity/parallelism vs memory-ledger): judgment-only — persona-review of the reference prose; reference unchanged by this task (shipped in T-001). CHK-005 (REQ-003, read placement/wording): judgment-only — persona-review of the step-1 read instruction. CHK-006 (REQ-004, future-vs-local distinction): judgment-only — persona-review of the step-2 append + Non-goals routing. CHK-008 (REQ-006, silent-delete adequacy): judgment-only — persona-review of the step-5 strike instruction.
- Discovered issues: None. The backlog-ledger.md reference's file-header block already states "Promotion retires an entry by deletion," which the new step-5 strike prose aligns with.
- Procedural compliance: Edited source under resources/ only (never the ejected .claude/.codex/.agents files directly), ran `just reeject` to regenerate, ran all four hygiene gates. State flips via `speccy task transition` only — never hand-edited the TASKS.md state attribute. No source module instruction was stale; no fix-the-source-then-reeject correction needed.
</implementer>
<review date="2026-06-14T03:10:06Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no logic or control-flow defect in this prose-only skill-body change. Checked: step cross-references are consistent (step 1 says "step 5 can strike it"; the strike lives at step 5 and back-references step 1; old step 5 cleanly renumbered to step 6 with no stale references in body or Exit section). The append (step 2, self-creates file) and strike (step 5, deletes a promoted entry) target distinct entries and are order-independent, so no lost-update/conflicting-edit hazard. Absence handling is sound on both paths: step 1 read treats a missing `.speccy/BACKLOG.md` as silent/non-fatal, and step 5 has an explicit "if no candidate was promoted, skip" guard, so no path forces a read/delete of an absent file. Reejection parity holds: the `{% include "modules/references/backlog-ledger.md" %}` directive resolved into both `.claude/` and `.agents/` plan SKILL.md, and the renumbered steps propagated to both ejected packs. No executable code paths in scope.
</review>
<review date="2026-06-14T03:10:15Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. Convention-only skill-body wiring (DEC-001): no Rust, endpoints, auth, input parsing, secrets, crypto, or new dependencies. The CLI never reads/parses `.speccy/BACKLOG.md`, so the appended prose introduces no injection, deserialization, or path-traversal surface; a malformed/absent file is silent by design. The four-field entry shape (`resources/modules/skills/speccy-plan.md:68`, ejected at `.claude/skills/speccy-plan/SKILL.md`) records planning intent and `SPEC-NNNN, plan` provenance only — no credential/secret capture path, no log/telemetry leak (the binary never touches the file). Ejected parity confirmed by reading the resolved `backlog-ledger.md` include in the ejected SKILL.md diff.
</review>
<review date="2026-06-14T03:10:19Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. All four covered REQs map cleanly to the diff in resources/modules/skills/speccy-plan.md and the ejected packs.

REQ-003 (read as candidate slices): step 1 (lines 41-46) reads `.speccy/BACKLOG.md` when present, surfaces entries as candidate slices, treats absence as silent/non-fatal — matches both done-when bullets and the Given/When/Then absence path.

REQ-004 (append on deliberate future-spec cut): step 2 (lines 68-80) appends the four-field entry, self-creates the file with the verbatim header, sets provenance `SPEC-NNNN, plan` (spec + phase, per done-when), and — the load-bearing business judgment — explicitly routes a spec-local exclusion to the SPEC's `## Non-goals` rather than the backlog. The future-spec-candidate vs spec-local-Non-goal boundary the SPEC non-goals section calls out is drawn correctly.

REQ-006 (promotion strikes by deletion): step 5 (lines 95-101) instructs an outright delete, explicitly forbids struck-through / "promoted to" residue, and names git + the new SPEC's provenance as the trail — aligns with DEC-003 and both done-when bullets.

REQ-001 (include reaches plan context, non-orphan + parity): the `{% include "modules/references/backlog-ledger.md" %}` at the authoring step resolves; the four fields and file header appear verbatim in both `.claude/skills/speccy-plan/SKILL.md` and the `.agents` parity copy after reeject. CHK-001's parity/non-orphan gate is satisfied from this task's side.

Scope is clean: brainstorm/ship bodies and bootstrap/init-immunity (sibling tasks T-003/T-004/T-005/T-006) are untouched, so no excluded scope leaked in. Step cross-references are internally consistent (step 1 points to step 5 for the strike; branch-guard renumbered 5→6 cleanly). Changelog shows a single initial entry whose intent matches the diff; no Open Questions section exists, so nothing was silently resolved.

Note (non-blocking, out of this task's done-when): the commit recipe at step 6 stages `SPEC.md` alone, so a BACKLOG.md append/delete from steps 2/5 is left uncommitted by the planner. No covered REQ imposes a staging obligation, and this mirrors MEMORY.md's user-owned, hand-committed lifecycle (DEC-001/DEC-004), so it is convention-consistent rather than a contract gap.
</review>
<review date="2026-06-14T03:11:04Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Convention-only task (DEC-001: CLI never touches the backlog), no Rust code changed, so the sole scriptable gate is resource-to-ejected parity — which is exactly what CHK-001 (REQ-001) covers and labels `demonstrated`. I re-ran that scope independently: built `speccy` from the working-tree `resources/`, ran `init` for both hosts into a scratch dir, and diffed against the working-tree ejected packs — `.claude/skills/speccy-plan/SKILL.md` and `.agents/skills/speccy-plan/SKILL.md` are byte-identical (CLAUDE_PARITY_OK / AGENTS_PARITY_OK). The `{% include "modules/references/backlog-ledger.md" %}` resolves to the shipped reference (landed in T-001), so the reference is non-orphaned and its four-field shape (Title / What & why / Deferred-because / Provenance) plus the file header appear in the ejected output. No fabrication: parity verified by re-execution, not by trusting the grep claim.

Evidence roll call is complete and plausibly labelled: all five CHKs under the covered REQs (CHK-001/002 REQ-001, CHK-005 REQ-003, CHK-006 REQ-004, CHK-008 REQ-006) appear. CHK-001 is `demonstrated` with named ejected file paths I could re-run; CHK-002/005/006/008 are `judgment-only`, which is correct — each `<scenario>` explicitly states it is a persona-review judgment, not a scriptable assertion, and none can be driven by a deterministic command on prose.

Reviewed the prose against the `<behavior>` boundary conditions the requirements name. REQ-003 step-1 read covers both arms (populated backlog surfaced as candidate slices; "Absence is normal and silent ... proceed without comment"). REQ-004 step-2 append covers the negative path explicitly ("a cut that is merely out of THIS spec's scope is a spec-local Non-goal — record it in `## Non-goals`, not the backlog"), with provenance `SPEC-NNNN, plan`. REQ-006 step-5 strike covers the silent-delete positive path and the no-residue boundary ("No struck-through line, no 'promoted to SPEC-NNNN' residue") plus the skip arm. No vacuous-mock, snapshot-baked-bug, or any-error-catch concerns apply — there is no test code to falsify, and the one deterministic invariant (parity) catches a stale/un-reejected pack.
</review>
<review date="2026-06-14T03:11:44Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. Style, naming, and conventions are clean.

Checked:
- `resources/modules/skills/speccy-plan.md` — three new prose blocks (read at step 1, append at step 2, strike at new step 5) plus the `{% include "modules/references/backlog-ledger.md" %}` directive. No forbidden artifact-IDs (real SPEC/REQ/DEC/T/CHK digit-ids) outside the references carve-out. No provenance meta-annotation citing spec or doc pointers.
- Reuse survey present and honest: `backlog-ledger.md` exists at the claimed path; the include is correctly resolved; both ejected packs (`.claude/skills/speccy-plan/SKILL.md` and `.agents/skills/speccy-plan/SKILL.md`) updated by `just reeject`.
- Step numbering consistent: old step 5 (branch-guard) renumbered to step 6; new step 5 is the strike step; "step 5" forward-reference in step 1 and "step 1" back-reference in step 5 are both accurate.
- Include placement (zero indentation, line 80) matches the pre-existing zero-indent pattern of `branch-guard.md` (line 116) and `commit-recipe.md` (line 137) — consistent with the majority convention in this file.
- Heading level (`## Backlog ledger entry shape`) in the included content matches the pre-existing `branch-guard.md`/`commit-recipe.md` pattern; no new inconsistency introduced.
- No dead code, no unnecessary abstraction, no parallel helpers reinventing existing ones.
- `SPEC-0042` appears only in `resources/modules/references/backlog-ledger.md`, within the allowed worked-instance references carve-out.
</review>
