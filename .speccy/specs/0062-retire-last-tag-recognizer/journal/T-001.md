---
spec: SPEC-0062
task: T-001
generated_at: 2026-06-12T18:52:42Z
---

<implementer date="2026-06-12T18:52:42Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: REUSE-AS-IS `xml_scanner::scan_tags`, `RawTag` (read `span.end`,
  `is_close`), `ScanConfig`, `JOURNAL_ELEMENT_NAMES`,
  `collect_code_fence_byte_ranges`, `frontmatter::split_required` — all already
  composed by `journal_xml::parse` at the same call site (DEC-001). WRITE-FRESH
  one new top-level symbol: `pub fn last_well_formed_offset(source, path) ->
  usize` in `journal_xml/mod.rs`. Searched `journal_xml`/`consistency.rs` for an
  existing scanner-backed recovery helper to extend; none existed (the only prior
  offset code was the hand-rolled `consistency.rs::last_well_formed_offset`, which
  this task deletes). No new `xml_scanner` primitive (DEC-003).
- Completed: Added `journal_xml::last_well_formed_offset`, reusing the exact
  `split_required → collect_code_fence_byte_ranges → ScanConfig → scan_tags`
  preamble, then a depth counter over the `RawTag` stream recording each depth-0
  close's `span.end`. Routed `consistency.rs::detect_journal_drift` to call it;
  deleted the private `last_well_formed_offset` fn and its
  `#[expect(clippy::similar_names)]`. Relocated the two offset unit tests into the
  `journal_xml` tests module (each now carries valid frontmatter) and added a
  third asserting frontmatter-missing → 0 (CHK-003). Updated the two preserved
  integration fixtures (`consistency_detect.rs`, `speccy-cli/tests/consistency.rs`)
  to carry frontmatter + a recoverable trailing corruption; their expected offset
  values are unchanged.
- Undone: Nothing in scope deferred. `xml_scanner`, the
  `DriftDetails::JournalXmlMalformed` shape, the `last_well_formed_byte_offset`
  field, and the reconcile truncation policy are untouched (Non-goals).
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all
  --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at
  `.speccy/specs/0062-retire-last-tag-recognizer/evidence/T-001.md`.
  Roll call for CHKs under REQ-001:
  - CHK-001 (audit: only tag emitters remain, no input recognizer; the
    `find('<')` loop is gone): demonstrated → the audit `grep -rn 'find("<|find('\''<'\''|format!("<{'`
    over `speccy-core/src` + `speccy-cli/src` returns exactly two non-test hits,
    both tag *emitters/diagnostics*: `vet_xml/serialize.rs:389` (block renderer
    emitting `<{element} verdict=...>`, the SPEC-0061 CHK-006 exclusion) and
    `journal_common.rs:54` (`format!("<{}>...")` building an error-context string
    from an already-parsed `RawTag.name`, not scanning input). The
    `consistency.rs` `find('<')` loop no longer exists. Result recorded here for
    `/speccy-ship` to lift into REPORT.md.
  - CHK-002 (well-formed-then-corrupt journal → byte past `</implementer>`):
    demonstrated → evidence Scenario 1 shows the red (offset 0 under the new
    frontmatter-gated helper with the old frontmatter-less fixtures) → green
    (frontmatter + recoverable corruption, offset = `find("</implementer>") +
    len`) across the relocated unit test and both preserved integration fixtures.
  - CHK-003 (frontmatter-missing journal → 0): demonstrated → evidence Scenario 2;
    `last_well_formed_offset_zero_when_frontmatter_missing` asserts 0.
- Discovered issues: The task body's step-by-step said "on a close tag, if depth
  was 1, record `tag.body_end_after_tag`". That is wrong for the offset contract:
  for a close tag `scan_tags` sets `body_end_after_tag = abs_tag_offset` (the
  *start* of the close tag), whereas the preserved tests and the SPEC done-when
  require the byte *just past* the close (`find("</implementer>") + len`). The
  byte-past-`>` offset is `RawTag.span.end`, so the helper records `span.end`, not
  `body_end_after_tag`. Implemented per the SPEC's done-when/CHK expected values
  (the binding contract), not the advisory hint.
- Procedural compliance: Followed speccy-work first-attempt recipe (clean tree,
  transition pending→in-progress, implement, self-review against the four persona
  north-stars + convention-drift checklist, four hygiene gates, transition
  →in-review). No shipped-skill friction; no `resources/` edits needed.
</implementer>
<review date="2026-06-12T18:55:12Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blockers. The recovery offset drives downstream file truncation, so its safety is the load-bearing concern; the implementation is sound.

- Char-boundary / bounds safety: the offset is `RawTag.span.end` from the canonical `scan_tags` (close tag `span.end == abs_line_end_excl`, a line-end byte), always a UTF-8 boundary bounded by source length. No mid-multibyte or out-of-bounds truncation point can be produced. The implementer correctly recorded `span.end` rather than the task hint's `body_end_after_tag` (which for a close tag is the tag *start* and would truncate mid-tag, leaving a partial tag on disk) — `journal_xml/mod.rs:248`.
- DEC-002 honored: the offset derives from the token stream, never from `ParseError.offset` (which can fall mid-element and is an unsafe recovery point). Confirmed the helper never touches a ParseError.
- Conservative failure: both error paths (missing/empty frontmatter via `split_required`, overflow via `scan_tags`) collapse to `0` (`journal_xml/mod.rs:235,243`). Adversarial/malformed journal input cannot drive an unbounded or attacker-chosen large offset.
- Net security improvement: routing recovery through fence-aware `scan_tags` closes the fence-blindness divergence — a `</implementer>` quoted inside a fenced code block can no longer steer truncation to a wrong byte and discard/retain the wrong content (REQ-002 class).
- Audit (CHK-001) independently reproduced: `grep` for `find("<` / `find('<'` / `format!("<{` over non-test `speccy-core/src` + `speccy-cli/src` yields only `journal_common.rs:54` (diagnostic over already-parsed `RawTag.name`) and `vet_xml/serialize.rs:389` (block renderer emitting a tag, the SPEC-0061 CHK-006 exclusion). No input-scanning recognizer survives; the `consistency.rs` `find('<')` loop is deleted.
- No new attack surface, no secret handling, no logging of sensitive data, no injection vector (regex over in-memory text), no auth/authz boundary, no new dependency.
</review>
<review date="2026-06-12T18:55:25Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
Pass. Checked the new `journal_xml::last_well_formed_offset` depth walk and its
two consumers.
- Offset source: the helper records `tag.span.end` (byte just past the close
  tag's `>`), not the `body_end_after_tag = abs_tag_offset` the task hint named.
  `body_end_after_tag` for a close points at the tag *start*, which would leave a
  partial tag on disk; `span.end` is the correct truncation point and matches the
  preserved fixtures' `find("</implementer>") + len`. The implementer's documented
  deviation is correct, not a defect.
- Depth counter (`journal_xml/mod.rs`): open `saturating_add(1)`, close records
  only at `depth == 1` then `saturating_sub(1)`. Stray-close-before-open (depth 0)
  records nothing and cannot underflow; nested whitelisted open inside a body
  yields 0, which is REQ-001's specified depth-0-pairing behavior, not wrong
  results. No off-by-one against the contract.
- Both error paths (`split_required` Err on missing/empty frontmatter, `scan_tags`
  Err) collapse to `0` — the legitimate "nothing closed cleanly" value (CHK-003);
  verified `last_well_formed_offset_zero_when_frontmatter_missing` green.
- Audit (CHK-001): grep over speccy-core/src + speccy-cli/src for the hand-rolled
  patterns yields only journal_common.rs:54 (error-context from an already-parsed
  `RawTag.name`) and vet_xml/serialize.rs:389 (block renderer emitting a tag — the
  SPEC-0061 CHK-006 exclusion); the consistency.rs `find('<')` loop and the private
  fn are gone, only the new call site at consistency.rs:426 remains.
- Ran journal_xml unit tests and the preserved consistency_detect fixture: green.
</review>
<review date="2026-06-12T18:55:27Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. All four task-scenarios are driven by tests that exercise the real code path, with assertions independent of the production logic — no mock-the-SUT, no self-confirming snapshot.

CHK-002: the preserved fixtures in `consistency_detect.rs:271-273` and `speccy-cli/tests/consistency.rs:293-300` compute `expected = malformed.find("</implementer>") + "</implementer>".len()` independently and assert it against the offset the production `detect_journal_drift` derives from `scan_tags`. Falsification holds: the task body advised recording `tag.body_end_after_tag`, but for a close tag `xml_scanner` sets `body_end_after_tag = abs_tag_offset` (the close-tag *start* = `find(...)` without `+len`), so that wrong variant would fail both integration assertions. The implementer correctly recorded `tag.span.end` (`ElementSpan` doc: "exclusive byte offset just past the trailing `>`", mod.rs:50-51), and flagged the divergence under Discovered issues. Verified green: `cargo test -p speccy-core --test consistency_detect detect_journal_xml_malformed` and `-p speccy-cli --test consistency journal_xml_malformed` both pass.

CHK-003: `last_well_formed_offset_zero_when_frontmatter_missing` (journal_xml/mod.rs:642-647) asserts 0 for a frontmatter-less journal that still carries a complete `<implementer>…</implementer>` — an explicit negative-path assertion gating the `split_required` short-circuit, not just absence of crash. Green.

CHK-001 audit reproduced independently: the grep for hand-rolled tag-scan patterns over `speccy-core/src` + `speccy-cli/src` yields exactly two non-test hits, both confirmed non-recognizers — `vet_xml/serialize.rs:389` (block renderer emitting tags, SPEC-0061 CHK-006 exclusion) and `journal_common.rs:54` (format over an already-parsed `RawTag.name`). The third match (`journal_xml/mod.rs:629`) is inside `#[cfg(test)]`. The `consistency.rs` `find('<')` loop is deleted in the diff.

Relocated unit tests carry valid frontmatter via the `make()` helper (mod.rs:384) so they reach the scan rather than short-circuiting; the trailing open-without-close makes strict parse fail in the integration path. Evidence red/green output is materially distinct (left 0 / right 153 → ok), scoped per-test (not the workspace hygiene invocation), and every test name it cites appears in the diff — no fabrication pattern matched.
</review>
<review date="2026-06-12T18:55:37Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
REQ-001 done-when maps cleanly to the diff. `consistency.rs` no longer holds a
`find('<')` scan; `detect_journal_drift` now calls
`journal_xml::last_well_formed_offset` which reuses `scan_tags` (DEC-001). Audit
grep over non-test `speccy-core`/`speccy-cli` returns only emitters/diagnostics
(`vet_xml/serialize.rs:389`, `journal_common.rs:54`), no input recognizer; the
deleted `find('<')` loop is gone (CHK-001, recorded in the journal for REPORT.md
lift). CHK-002 fixture (`consistency_detect.rs`) and the CLI
`details.last_well_formed_byte_offset` assertion stay green at the unchanged
contract formula `find("</implementer>") + len`; CHK-003 frontmatter-missing maps
to 0 via the relocated/added unit tests. Goals/non-goals respected: no
`xml_scanner` edit, no `DriftDetails` shape or field change, no new parser
primitive, truncation policy untouched, scope limited to the four named files.
The implementer's documented deviation (recording `span.end`, not the advisory
`body_end_after_tag`) is correct — for a close tag `body_end_after_tag` is the
tag start (`xml_scanner/mod.rs:431`) while `span.end` is just-past-`>` (`:428`),
matching the contract value. REQ-002 fence regression is correctly left to T-002.
</review>
<review date="2026-06-12T18:55:48Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
REQ-001 done-when maps cleanly to the diff. `consistency.rs` no longer holds a
`find('<')` scan; `detect_journal_drift` now calls
`journal_xml::last_well_formed_offset` which reuses `scan_tags` (DEC-001). Audit
grep over non-test `speccy-core`/`speccy-cli` returns only emitters/diagnostics
(`vet_xml/serialize.rs:389`, `journal_common.rs:54`), no input recognizer; the
deleted `find('<')` loop is gone (CHK-001, recorded in the journal for REPORT.md
lift). CHK-002 fixture (`consistency_detect.rs`) and the CLI
`details.last_well_formed_byte_offset` assertion stay green at the unchanged
contract formula `find("</implementer>") + len`; CHK-003 frontmatter-missing maps
to 0 via the relocated/added unit tests. Goals/non-goals respected: no
`xml_scanner` edit, no `DriftDetails` shape or field change, no new parser
primitive, truncation policy untouched, scope limited to the four named files.
The implementer's documented deviation (recording `span.end`, not the advisory
`body_end_after_tag`) is correct — for a close tag `body_end_after_tag` is the
tag start (`xml_scanner/mod.rs:431`) while `span.end` is just-past-`>` (`:428`),
matching the contract value. REQ-002 fence regression is correctly left to T-002.
</review>
<review date="2026-06-12T18:55:56Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. Naming, import ordering, and error-handling patterns match the surrounding
file. The new `last_well_formed_offset` function follows the same conventions as
`parse` and `latest_round` — `#[must_use]` with a custom message, `# Arguments`
doc section, no `# Examples` (consistent with existing public functions in this
module). The deleted `#[expect(clippy::similar_names, ...)]` annotation is fully
gone with its associated function; no unfulfilled-expectation lint remains.
Reuse survey: all six named symbols (`scan_tags`, `RawTag`, `ScanConfig`,
`JOURNAL_ELEMENT_NAMES`, `collect_code_fence_byte_ranges`, `split_required`)
exist in the tree and are called by the new helper. No reinvention found.
Tests are non-vacuous: the finder test derives expected offset from
`src.find("</implementer>") + len` over the same fixture; the zero tests gate
distinct code paths (missing close-tag vs missing frontmatter). The audit of
hand-rolled tag-scan patterns confirms only two non-test matches remain
(`vet_xml/serialize.rs:389` and `journal_common.rs:54`), both tag emitters, not
input recognizers; the `consistency.rs` `find('<')` loop is gone. No `unwrap`,
no bare `#[allow]`, no dead imports.
</review>
