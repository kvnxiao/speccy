---
spec: SPEC-0062
task: T-002
generated_at: 2026-06-12T19:07:32Z
---

<implementer date="2026-06-12T19:07:32Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: test-only task, scoped to `speccy-core/tests/consistency_detect.rs`. Reused the existing `make_spec_dir` / `detect` / `FakeProbe` / `one_task` harness and the `DriftDetails::JournalXmlMalformed { last_well_formed_byte_offset, .. }` match shape from the sibling `detect_journal_xml_malformed_is_blocking_with_forward_slash_path` test. Reuse-as-is: all of the above. Extend: none. Write-fresh: one new `#[test] fn detect_journal_xml_malformed_recovery_offset_ignores_fenced_close` — searched the file for any existing fence-aware recovery-offset test and found none (T-001's fence unit tests live in `journal_xml/mod.rs`, not the end-to-end `detect` path). No new non-test top-level symbols.
- Completed: Added the REQ-002 / CHK-004 end-to-end regression test asserting that a line-isolated `</implementer>` inside a fenced code block is excluded from the recovery offset. Fixture: valid frontmatter + well-formed round-1 `<implementer>` (structural close ends at byte X=153) + round-2 `<implementer>` open whose only following close is fenced (ends at byte Y=235) + no real close, so `journal_xml::parse` fails and the malformed branch runs. Asserts `last_well_formed_byte_offset == X` (153), driven through the full `detect_journal_drift` path (T-001 moved this onto the fence-aware `journal_xml::last_well_formed_offset`).
- Undone: nothing in scope. Task is test-only and relies on T-001 (completed) for the read-path move.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0 (after applying `cargo +nightly fmt --all`); `cargo deny check` exit 0.
- Evidence: no separate paper-trail file; the recorded pre-fix measurement lives in the test's doc comment. Roll call for CHKs under REQ-002:
  - CHK-004 (fenced journal close excluded from recovery offset; pre-fix run reflects Y): demonstrated → the new test asserts the post-fix offset is X=153 (the real structural close), not Y=235 (the fenced occurrence). The pre-fix value Y=235 was measured once by resurrecting the pre-SPEC hand-rolled `find('<')` scan (from the merged SPEC-0061 revision, `git show HEAD~2:speccy-core/src/consistency.rs`) and running it against this exact fixture; it counted the fenced close and yielded 235 — the fence-blindness bug. The resurrected code was not committed (one-shot measurement). Both X=153 and pre-fix Y=235 are recorded in the test's doc comment.
- Discovered issues: the fixture needs a blank line before the ``` opening fence — otherwise comrak folds the fence into the preceding raw-HTML block (`<implementer ...>`) and `collect_code_fence_byte_ranges` returns no range, so the fenced close is not excluded. Documented inline in the test comment as load-bearing. No code defect found; T-001's fence-aware path is correct.
- Procedural compliance: followed the speccy-work first-attempt recipe (clean tree confirmed, state transitions via `speccy task transition`, journal via `speccy journal append`). No shipped-skill friction encountered; no `resources/` edits needed.
</implementer>
<review date="2026-06-12T19:08:58Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Test-only slice; no security-sensitive surface introduced. The
recovery-offset derivation this test guards is itself a safety
boundary — a fence-blind offset would let attacker-influenced fenced
content (a line-isolated `</implementer>` inside a code block) inflate
the truncation point and leave a partial structural tag on disk; the
test pins the offset to the real structural close X, not the fenced
occurrence Y. Not vacuous: X and Y are derived independently from the
fixture, `assert!(y > x)` guards the premise, and the assertion drives
the full `detect_journal_drift` path (`consistency_detect.rs:281-359`),
so deleting it would let the wrong (fence-blind) offset pass. No
secrets, logging/error-disclosure, crypto, auth, or injection vectors;
no new dependencies; placeholder `SPEC-0099`/`T-001`/zero-SHA live only
in test fixtures. Test compiles and passes.
</review>
<review date="2026-06-12T19:09:44Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. T-002 adds a single test-only change (`speccy-core/tests/consistency_detect.rs:280` `detect_journal_xml_malformed_recovery_offset_ignores_fenced_close`); the sibling test's fixture edit is mechanical. Reviewed the test's own control-flow logic: X is computed via a unique `</implementer>\n<implementer date="2026-05-21T19` marker (the only round-1-close→round-2-open boundary in the fixture) and Y via `rfind`, both ASCII so char-index == byte-offset (X=153, Y=235, matching the recorded comment); the `y > x` precondition is asserted before use. The assertion `last_well_formed_byte_offset == X` correctly discriminates the fix from the fence-blind bug: a fence-blind depth-walk would count the fenced `</implementer>` (depth 1->0 at Y) and record Y=235, failing the `==153` assert. Confirmed against the production read path (`consistency.rs:426` -> `journal_xml::last_well_formed_offset`, mod.rs:234), whose depth counter records the round-1 close at X and never returns to depth 0 after the round-2 open because the fenced close is excluded by `collect_code_fence_byte_ranges`. Test runs green and the malformed-drift branch is genuinely exercised (the `.ok_or` find succeeds). No off-by-one, marker-ambiguity, or Option-mishandling defect in the test logic.
</review>
<review date="2026-06-12T19:09:51Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Style passes. The new test `detect_journal_xml_malformed_recovery_offset_ignores_fenced_close` follows the `detect_*` naming convention established by all sibling tests in `consistency_detect.rs`. No new imports are added. The suppression at line 1 uses `#[expect(..., reason = "...")]` with a justification, matching project convention. Both X=153 and Y=235 are verified against the fixture bytes; the comment matches the computed values exactly. The `expected` offset in the modified `detect_journal_xml_malformed_is_blocking_with_forward_slash_path` is derived dynamically via `malformed.find(...)`, not hard-coded — not a constant-copy test. Reuse survey: `make_spec_dir`, `detect`, `FakeProbe`, `one_task`, and the `DriftDetails::JournalXmlMalformed` match shape are all confirmed to exist in the file and are actually called by the new test. No dead imports, no bare `#[allow]`, no `.unwrap()`, no slice-index access introduced.
</review>
<review date="2026-06-12T19:09:53Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-004 demonstrated. `consistency_detect.rs:281 detect_journal_xml_malformed_recovery_offset_ignores_fenced_close` drives the real `detect_journal_drift` → `last_well_formed_offset` path end-to-end (only the git probe is faked, which is out of scope), not a mock of the system under test. The fixture's round-1 `</implementer>` structural close ends at X=153 and the fenced `</implementer>` ends at Y=235; I recomputed both independently and they match the recorded measurement exactly. The assertion `last_well_formed_byte_offset == x` is tight: a fence-blind scan records Y=235 (≠153) and the test fails. I confirmed the falsification directly against `git show HEAD~1:speccy-core/src/consistency.rs` — the deleted `find('<')` scan has zero fence awareness and overwrites `last_close_end` for the fenced close, yielding Y=235, the exact pre-fix value the comment records. Deterministic static-string fixture, no ordering/time/parallel-run hazard. Naming and harness reuse (`make_spec_dir`/`detect`/`FakeProbe`/`one_task`) match the sibling `detect_journal_xml_malformed_*` convention. Evidence roll call covers REQ-002's only CHK (CHK-004=demonstrated) with a verifiable test cite; the recorded pre-fix Y and post-fix X live inline in the test doc comment per the task's explicit instruction, and the one-shot cross-revision measurement is the correct red/green shape for this test-only task.
</review>
<review date="2026-06-12T19:10:03Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
Pass. The diff delivers exactly what REQ-002 / CHK-004 promised and nothing more. The new `detect_journal_xml_malformed_recovery_offset_ignores_fenced_close` test (`speccy-core/tests/consistency_detect.rs:280`) drives the full `detect_journal_drift` path on a fixture with a real `</implementer>` structural close at X=153 and a line-isolated `</implementer>` inside a fenced code block at Y=235, asserting `last_well_formed_byte_offset == X` (computed dynamically from the fixture, not hardcoded; `y > x` is itself asserted). I independently resurrected the pre-SPEC hand-rolled `find('<')` scan from `origin/main:speccy-core/src/consistency.rs` and ran it against the byte-identical fixture: it yields 235, confirming the recorded pre-fix Y in the test comment/journal and proving the test fails against the old implementation and passes after the fence-aware `scan_tags` read path (both REQ-002 done-when bullets satisfied). The task body designs CHK-004's pre-fix clause as a one-shot recorded measurement (not a committed dual-run), and the implementer recorded X=153/Y=235 in the test comment and handoff per that contract. Non-goals respected: test-only diff, no production surface, no change to reconcile policy, drift kind, DriftDetails shape, field name, grammar, or parser primitives. No Open-questions section exists; the single Changelog entry shows no intent shift the diff could lag. Full `consistency_detect` suite (8 tests) green, including the third task scenario.
</review>
