# Evidence for SPEC-0053 T-005

Red-then-green paper trail for wiring the plan-time subagents
(`plan-explorer`, `plan-architect`) into their host skill bodies.
Both covered CHKs are structural assertions over the *ejected*
skill bodies, so the red/green pairs are run by toggling the source
wiring (stash the `resources/` edits + re-eject for RED; restore +
re-eject for GREEN).

## Session 2026-05-29-T005-attempt-1

### Scenario 1 — brainstorm/plan invoke plan-explorer, no new artifact (CHK-008)

<red>
Pre-wiring: with the `resources/` edits stashed and `just reeject`
re-run so the ejected packs reflect the unwired source, the new
test fails because the rendered `speccy-brainstorm` / `speccy-plan`
SKILL.md bodies reference neither `plan-explorer` nor the
no-new-artifact routing language.

```
$ git stash push -- resources/... .claude .codex .agents
$ just reeject
$ cargo test -p speccy-cli --test skill_packs -- \
    brainstorm_and_plan_skills_invoke_plan_explorer
test brainstorm_and_plan_skills_invoke_plan_explorer_without_new_artifact ... FAILED
panicked at speccy-cli/tests/skill_packs.rs:1430:13:
  rendered `.claude/skills/speccy-brainstorm/SKILL.md` must reference
  invoking the `plan-explorer` subagent
```
</red>

<green>
Post-wiring: with the `resources/` edits restored and `just reeject`
re-run, both `speccy-brainstorm` and `speccy-plan` render across both
hosts with the `plan-explorer` invocation and the
"explorer report is not persisted to a new artifact file" routing
prose.

```
$ git stash pop
$ just reeject
$ cargo test -p speccy-cli --test skill_packs -- \
    brainstorm_and_plan_skills_invoke_plan_explorer
test result: ok. 1 passed; 0 failed
```
</green>

### Scenario 2 — decompose invokes plan-architect, candidate tasks, Decisions (CHK-009)

<red>
Pre-wiring (same stash + re-eject as Scenario 1): the rendered
`speccy-decompose` agent body references none of `plan-architect`,
the candidate-task framing, the build-sequence checklist, or
`### Decisions`.

```
$ cargo test -p speccy-cli --test skill_packs -- \
    decompose_skill_invokes_plan_architect
test decompose_skill_invokes_plan_architect_with_candidate_tasks_and_decisions ... FAILED
panicked at speccy-cli/tests/skill_packs.rs:1460:9:
  rendered `.claude/agents/speccy-decompose.md` must reference
  invoking the `plan-architect` subagent
```
</red>

<green>
Post-wiring: the rendered `speccy-decompose` body (Claude
`.claude/agents/speccy-decompose.md` and Codex
`.codex/agents/speccy-decompose.toml`) references `plan-architect`,
names the build-sequence checklist as the candidate task list, and
directs promoting load-bearing decisions into `### Decisions`.

```
$ cargo test -p speccy-cli --test skill_packs -- \
    decompose_skill_invokes_plan_architect
test result: ok. 1 passed; 0 failed
```
</green>

## Hygiene-gate evidence

```
$ cargo test --workspace
(all suites pass)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-005 diff
landed. (A pre-existing rustfmt drift on HEAD in
`speccy-cli/tests/skill_packs.rs` unrelated to this task was
normalised by `cargo +nightly fmt --all` so the fmt gate could
pass — see the journal `Procedural compliance` field.)
