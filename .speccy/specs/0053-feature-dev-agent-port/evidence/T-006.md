# Evidence: SPEC-0053 T-006 — Read-only tool grants + Codex parity note

## Scenario 1 (CHK-010): read-only agents declare a read-only `tools:` grant

Test: `read_only_agents_declare_read_only_tool_grant`
(`speccy-cli/tests/skill_packs.rs`). Renders the Claude host pack via
`render_host_pack(HostChoice::ClaudeCode)` and, for each of the ten
read-only agents (`plan-explorer`, `plan-architect`,
`reviewer-correctness`, `reviewer-business`, `reviewer-tests`,
`reviewer-security`, `reviewer-style`, `reviewer-architecture`,
`reviewer-docs`, `vet-reviewer`), parses the YAML frontmatter and
asserts the `tools:` field is present, includes `Read`, and excludes
`Edit`/`Write`/`NotebookEdit`.

- GREEN (wiring intact): passes — all ten wrappers carry
  `tools: Read, Grep, Glob, LS, Bash, WebFetch`.
- RED proof: removing the `tools:` line from a read-only wrapper source
  (e.g. `resources/agents/.claude/agents/reviewer-business.md.tmpl`) and
  re-ejecting flips the test RED at the "must declare a `tools:` grant"
  panic. Equally, a `tools:` value that still listed `Edit` would flip
  the exclusion assertion RED.

## Scenario 2 (CHK-011): writer agents are NOT narrowed

Test: `writer_agents_are_not_narrowed_to_read_only`
(`speccy-cli/tests/skill_packs.rs`). For the five writer agents
(`speccy-work`, `speccy-decompose`, `speccy-ship`, `vet-implementer`,
`vet-simplifier`), asserts each either declares no `tools:` field
(inherits full access) or, if it ever declares one, retains both `Edit`
and `Write`.

- GREEN: passes — writers carry no `tools:` field (verified
  `grep -c "tools:"` returns 0 for all five wrappers).
- RED proof: adding `tools: Read, Grep, Glob, LS, Bash, WebFetch` to a
  writer wrapper (the over-broad-application regression CHK-011 gates)
  flips the test RED on the "lost `Edit`/`Write`" assertion.

## Scenario 3 (CHK-012): Codex parity note is recorded

Durable note in `AGENTS.md`, "Skill pack source of truth" section,
under the "Read-only agent tool grants — Claude Code vs Codex parity"
heading. States explicitly that Codex does NOT honor a per-subagent
tool restriction in its `.toml` agent definition (verified against the
Codex config reference at developers.openai.com/codex/config-reference:
agent definitions expose only `config_file`, `description`,
`nickname_candidates`; tool gating lives at MCP-server / app / global
scope), and that the Codex read-only posture therefore remains
prose-enforced. Judgment-only: reviewer-business / reviewer-docs judge
the note's presence and explicitness on the diff.
