# Evidence — SPEC-0053 / T-007

REQ-010: `can we brainstorm` added to the `speccy-brainstorm` trigger list.

## Scenario 1 (CHK-013): rendered description contains `can we brainstorm` for both hosts

Red→green pair on `brainstorm_description_lists_can_we_brainstorm_trigger`
in `speccy-cli/tests/skill_packs.rs`. The test renders the host pack for
both `HostChoice::ClaudeCode` and `HostChoice::Codex` via
`render_host_pack`, parses the `speccy-brainstorm` SKILL.md frontmatter,
and asserts the `description` field contains the substring
`can we brainstorm`.

### Red

Temporarily reverted the `"can we brainstorm"` insertion in both wrapper
sources (`resources/agents/.claude/...` and `resources/agents/.agents/...`),
forced a re-embed (`touch speccy-cli/src/embedded.rs`), and ran the scoped
test:

```
$ cargo test -p speccy-cli --test skill_packs brainstorm_description_lists_can_we_brainstorm_trigger

running 1 test
test brainstorm_description_lists_can_we_brainstorm_trigger ... FAILED

failures:

---- brainstorm_description_lists_can_we_brainstorm_trigger stdout ----

thread 'brainstorm_description_lists_can_we_brainstorm_trigger' (3415083) panicked at speccy-cli/tests/skill_packs.rs:502:9:
rendered `.claude/skills/speccy-brainstorm/SKILL.md` description must list the `can we brainstorm` trigger phrase (SPEC-0053 REQ-010); got: "Atomize a fuzzy ask into first-principle requirements before any SPEC.md is written. Walks the user through a Socratic exchange (one question at a time, 2-3 alternative framings with trade-offs, silent assumptions, open questions) and stops at a hard gate until the user explicitly approves the framing. Use when the user has a fuzzy idea, says \"help me brainstorm\", \"help me think about\", \"let's brainstorm\", \"I want to spec out X but I'm not sure where to start\", or before invoking speccy-plan on an unclear ask. Requires no preconditions. Do NOT trigger when the user has named the slice and the scope is clear - go straight to speccy-plan."
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace

failures:
    brainstorm_description_lists_can_we_brainstorm_trigger

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 44 filtered out; finished in 0.02s
```

### Green

Restored the `"can we brainstorm"` insertion in both wrapper sources,
forced a re-embed (`touch speccy-cli/src/embedded.rs`), and re-ran the
same scoped test:

```
$ cargo test -p speccy-cli --test skill_packs brainstorm_description_lists_can_we_brainstorm_trigger

running 1 test
test brainstorm_description_lists_can_we_brainstorm_trigger ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 44 filtered out; finished in 0.03s
```

Source-of-truth edit (not the ejected files):
- `resources/agents/.claude/skills/speccy-brainstorm/SKILL.md.tmpl`
- `resources/agents/.agents/skills/speccy-brainstorm/SKILL.md.tmpl`

Ejected output regenerated by `just reeject`:
- `.claude/skills/speccy-brainstorm/SKILL.md`
- `.agents/skills/speccy-brainstorm/SKILL.md`
