# Evidence: SPEC-0053 / T-002 — plan-explorer persona + wrappers

## CHK-003 — plan-explorer renders for both hosts (includes expanded), advisory non-verdict contract (no `<review` marker)

Tests in `speccy-cli/tests/skill_packs.rs`:

- `plan_explorer_renders_with_includes_expanded_both_hosts` — asserts
  the rendered `.claude/agents/plan-explorer.md` and
  `.codex/agents/plan-explorer.toml` exist and contain no `{%`
  (every `{% include %}` directive expanded).
- `plan_explorer_body_has_no_review_verdict_marker_both_hosts` —
  asserts neither rendered body contains the `<review` verdict-contract
  marker, gating the advisory, report-only contract against a future
  edit that inlines a verdict snippet.

Green run:

```
test plan_explorer_renders_with_includes_expanded_both_hosts ... ok
test plan_explorer_body_has_no_review_verdict_marker_both_hosts ... ok
```

Red check (sanity): before the binary embedded the new wrappers,
both tests failed with `rendered host pack must include
.claude/agents/plan-explorer.md after T-002` — proving the tests
gate the wrappers' existence, not a tautology.
