---
spec: SPEC-0053
task: T-005
generated_at: 2026-05-29T20:05:00Z
---

<implementer date="2026-05-29T20:05:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Wired the plan-time subagents into their host skills and
  added structural tests gating the wiring.
  - `resources/modules/skills/speccy-brainstorm.md` step 1
    ("Explore project context") now invokes the `plan-explorer`
    subagent when the ask touches existing code, states the report is
    ephemeral, and routes it into SPEC.md sections (Summary +
    `<requirement>` grounding) — explicitly NOT into a new artifact
    file.
  - `resources/modules/skills/speccy-plan.md` step 2 ("Write SPEC.md")
    now invokes `plan-explorer` before/while drafting and folds the
    grounding into `## Summary` + `<requirement>` blocks, again with
    the no-new-artifact constraint.
  - `resources/modules/phases/speccy-decompose.md` step 1 now invokes
    `plan-architect` before authoring `TASKS.md`, names its
    build-sequence checklist as the CANDIDATE task list (decompose
    retains final `<task>` authorship — merge/split/reorder/renumber),
    and directs promoting load-bearing blueprint decisions into SPEC.md
    `### Decisions` (DEC-NNN) blocks.
  - Ran `just reeject`; the six ejected files
    (`.claude`/`.agents` brainstorm+plan SKILL.md, and the Claude
    `.claude/agents/speccy-decompose.md` + Codex
    `.codex/agents/speccy-decompose.toml` agent bodies) now carry the
    wiring. Re-running `just reeject` produced no further diff
    (idempotent).
  - Added two tests to `speccy-cli/tests/skill_packs.rs`:
    `brainstorm_and_plan_skills_invoke_plan_explorer_without_new_artifact`
    (CHK-008) and
    `decompose_skill_invokes_plan_architect_with_candidate_tasks_and_decisions`
    (CHK-009), each rendering both host packs via `render_host_pack`
    and asserting on the rendered bodies. The decompose body is a
    pinned phase-worker stub, so the new `rendered_decompose_body`
    helper reads the rendered agent wrapper
    (`<root>/agents/speccy-decompose.{md,toml}`) rather than the
    SKILL.md stub.
- Undone: nothing in scope. REQ-006/REQ-007 are skill-wiring
  requirements fully satisfied by the three body edits + reeject +
  the two structural tests.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories/bans/licenses/sources ok).
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-005.md`.
  Roll call for CHKs under REQ-006 / REQ-007:
  - CHK-008 (brainstorm/plan reference invoking `plan-explorer`;
    neither references a new `*.md` report artifact outside SPEC.md
    routing targets): demonstrated → evidence Scenario 1; the red
    pair (wiring stashed + re-eject) fails on the missing
    `plan-explorer` reference, the green pair (restored + re-eject)
    passes across both hosts.
  - CHK-009 (decompose references invoking `plan-architect`, names
    the build-sequence checklist as candidate tasks, references
    promoting decisions into `### Decisions`): demonstrated →
    evidence Scenario 2; same stash/restore red→green pair over the
    rendered decompose agent body for both hosts.
- Discovered issues: none in the task surface.
- Procedural compliance: followed the speccy-work first-attempt recipe
  on a clean tree; edited only the source modules under `resources/`
  and re-ejected (never hand-edited the `.claude`/`.codex`/`.agents`
  output). One out-of-scope pre-existing snag: `cargo +nightly fmt
  --all --check` failed on HEAD in `speccy-cli/tests/skill_packs.rs`
  (a T-004-era rustfmt drift around a `toml::from_str` closure,
  confirmed present with my edits stashed). I normalised it with
  `cargo +nightly fmt --all` so the hygiene fmt gate could pass; the
  reformat is mechanical and touches only the pre-existing block plus
  whitespace, not the wiring logic. The new tests read rendered output
  via `render_host_pack` rather than re-asserting hard-coded source
  copies, and are proven non-vacuous by the stash/re-eject red phase.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:36:51Z" round="1">
T-005 faithfully delivers REQ-006/REQ-007. `speccy-brainstorm.md` and `speccy-plan.md` invoke `plan-explorer` and route its ephemeral report into existing SPEC.md sections (Summary + `<requirement>`), explicitly forbidding a new `*.md` artifact. `speccy-decompose` invokes `plan-architect`, names the build-sequence checklist as the candidate task list, retains final `<task>` authorship, and promotes load-bearing decisions into `### Decisions` (DEC-NNN). All `<done-when>` bullets for both requirements are met; non-goals honored (no new artifact file types, no new commands, DEC-002/DEC-003 respected). CHK-008/CHK-009 tests assert rendered output for both hosts and pass. Two non-blocking notes: (1) the REQ-006/REQ-007 narrative word "unconditionally" is implemented as "when the ask/slice touches existing code" — the more faithful reading, matching the REQ-006 `<behavior>` Given and the SPEC's own bootstrap Note; the contracted done-when bullets do not require unconditional invocation. (2) The suggested-files list named `resources/modules/skills/speccy-decompose.md`, but the decompose body correctly lives in `resources/modules/phases/speccy-decompose.md` and renders into both host agent wrappers; the wiring landed in the right rendered output and the implementer documented the location.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:36:51Z" round="1">
CHK-008's negative-artifact assertion in `speccy-cli/tests/skill_packs.rs:1418-1426` (`brainstorm_and_plan_skills_invoke_plan_explorer_without_new_artifact`) does not gate the invariant the SPEC scenario requires ("neither references creating a new `*.md` report artifact outside the SPEC.md routing targets"). It is a global `body.contains("NOT write") && body.contains("artifact")` (with case/verb variants) over the *entire* rendered body, but both target bodies already contain those tokens in unrelated pre-existing prose: brainstorm's hard-gate line 36 (`do NOT write SPEC.md`) plus `four artifacts` elsewhere; plan's line 73 (`do not write anything to ## Open Questions`) plus `artifact` on line 87. The assertion is therefore satisfied even when the no-artifact routing clause is deleted from the wiring. Proven empirically: stripping the entire `**ephemeral**: do NOT write it to a new *.md artifact file ... never into a standalone report file` clause from `resources/modules/skills/speccy-brainstorm.md` (keeping only the `plan-explorer` invocation), re-ejecting, and running the test → it still passed green. So a future edit that removes the no-artifact constraint — re-introducing exactly the standalone-report-file drift REQ-006 exists to prevent — would land without the suite catching it. Per AGENTS.md ("If I delete this, what real regression could land that the suite no longer catches?"), the answer is a named regression class, so the half-assertion is vacuous. Fix: scope the negative-routing assertion to the wiring sentence rather than the whole body — assert on a distinctive contiguous phrase from the routing clause that does not co-occur in unrelated prose (e.g. the explorer-report-specific `*.md artifact file` qualifier), or extract the wiring paragraph and assert within it. The token must flip RED when the no-artifact clause is removed but the `plan-explorer` invocation stays. Not blocking, for the record: the `plan-explorer` invocation assertion (primary CHK-008 invariant) is sound — deleting the invocation flips RED, both hosts covered. CHK-009 (`decompose_skill_invokes_plan_architect_with_candidate_tasks_and_decisions`) is sound and non-vacuous: `plan-architect`, `candidate`, `build-sequence`, and `### Decisions` each appear only in the T-005 wiring block, so each assertion independently gates a real clause. Helpers read the rendered/ejected bodies via `render_host_pack`, not source copies. Evidence file present, roll call covers both CHKs as `demonstrated`, red/green materially distinct, scoped per-test invocation — no fabrication concerns.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:36:51Z" round="1">
Prose-only skill-wiring change with no security-relevant attack surface: no code, endpoints, auth, parsers, or secret handling touched. Added instructions contain no injection/exfil/credential/env-read patterns, and explicitly mark the explorer grounding report as ephemeral (forbidding stray-file persistence). No real Speccy-repo identifiers leaked into the shipped skill bodies (only generic `T-NNN`/`REQ-NNN`/`DEC-NNN` placeholders and subagent names); `just reeject` is idempotent and ejected output matches source.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T19:36:51Z" round="1">
All changed lines trace cleanly to the task. Rustfmt churn on `skill_packs.rs`: the five reformatted lines on the `toml::from_str` closure (around line 2152) are pre-existing T-004-era code that nightly rustfmt disagrees with (`git show 7c320bc:speccy-cli/tests/skill_packs.rs` confirms the original form was present at the T-004 commit). The reformat is mechanical (whitespace only, no semantic change), explicitly disclosed in the journal, and required to pass the mandatory `cargo +nightly fmt --all --check` hygiene gate — justified collateral under AGENTS.md, not unexplained churn. Source-of-truth compliance clean: edits went only through the three source modules under `resources/` + `just reeject`; the six changed ejected files match the resource prose exactly (only template expansion like `{{ cmd_prefix }}speccy-plan` → `/speccy-plan`), no ejected file hand-edited. `rendered_decompose_body` reuses existing helpers; test names follow the existing pattern; no dead code introduced.
</review>

<blockers date="2026-05-29T19:36:51Z" round="1">
One blocker landed in round 1 (tests). Business, security, and style passed. Fix it before respawning the review fan-out.

1. **Vacuous no-artifact assertion in CHK-008 test (tests).** `brainstorm_and_plan_skills_invoke_plan_explorer_without_new_artifact` (`speccy-cli/tests/skill_packs.rs:1418-1426`) asserts `body.contains("NOT write")` + `body.contains("artifact")` (with verb/case variants) over the *entire* rendered body. Both bodies already carry those tokens in unrelated pre-existing prose (brainstorm: `do NOT write SPEC.md` at line 36, `four artifacts`; plan: `do not write anything to ## Open Questions` at line 73, `artifact` at line 87), so the assertion passes even when the no-artifact routing clause is deleted from the wiring. Adversarially confirmed: stripping the entire `**ephemeral**: do NOT write it to a new *.md artifact file … never into a standalone report file` clause from `resources/modules/skills/speccy-brainstorm.md` (keeping the `plan-explorer` invocation) + re-eject + rerun leaves the test GREEN — the standalone-report-file drift REQ-006 exists to prevent would ship uncaught. Fix: scope the negative-routing assertion to the wiring clause, not the whole body — assert a distinctive contiguous phrase unique to the explorer-report routing sentence (e.g. tie it to the explorer report's `*.md artifact file` qualifier), or extract the wiring paragraph and assert within it. Verify the assertion flips RED when the no-artifact clause is removed while the `plan-explorer` invocation stays, for both brainstorm and plan, across both hosts.

The mechanical floor is correct and must not regress: the `plan-explorer`/`plan-architect` invocations are wired into all three skill bodies with the right routing prose, CHK-009 is sound and non-vacuous, and the CHK-008 invocation assertion itself is sound. Only the one vacuous no-artifact half-assertion must be tightened for the retry.
</blockers>

<implementer date="2026-05-29T21:30:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the single round-1 tests blocker by tightening
  the vacuous no-artifact half-assertion in
  `brainstorm_and_plan_skills_invoke_plan_explorer_without_new_artifact`
  (`speccy-cli/tests/skill_packs.rs`). Replaced the global
  `body.contains("NOT write"/"not persist") && body.contains("artifact")`
  disjunction — which matched unrelated pre-existing prose (brainstorm
  `four artifacts` / hard-gate `do NOT write SPEC.md`; plan
  `## Open Questions` line / `artifact` at line 87) — with a single
  assertion on the distinctive contiguous phrase `new `*.md` artifact
  file`. That phrase occurs exactly once in each rendered body, only
  inside the no-artifact routing clause of the wiring block (brainstorm
  source line 55, plan source line 48), so the assertion is now scoped
  to the wiring sentence. Amended the prior WIP in place; did not
  rewrite the test from scratch or reset state.
- Undone: nothing in scope. The round-1 mechanical floor (the
  `plan-explorer` invocation assertion, CHK-009) was left untouched and
  still passes.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-005.md` (round-1
  scenarios remain valid). Round-2 adversarial proof for the tightened
  CHK-008 no-artifact assertion:
  - CHK-008 (brainstorm/plan reference invoking `plan-explorer`; neither
    routes the report into a new `*.md` artifact file): demonstrated →
    GREEN with the wiring intact. RED proof: stripped the
    `do NOT write/persist it to a new `*.md` artifact file` clause from
    the brainstorm source body (keeping the `plan-explorer` invocation),
    re-ejected, ran → FAILED on
    `.claude/skills/speccy-brainstorm/SKILL.md must state the explorer
    report is not persisted to a new `*.md` artifact file`. Restored
    brainstorm, stripped the same clause from the plan source body
    (invocation kept), re-ejected, ran → FAILED on
    `.claude/skills/speccy-plan/SKILL.md`. So the assertion flips RED
    for BOTH brainstorm and plan when the no-artifact clause is deleted
    while the invocation stays. Both hosts (ClaudeCode `.claude`, Codex
    `.agents`) are exercised by the per-host `find_rendered_skill` loop
    over the shared host-neutral source body. The
    `plan-explorer`-invocation assertion and CHK-009 were not regressed
    (full suite exit 0).
- Discovered issues: none.
- Procedural compliance: followed the speccy-work retry branch — read
  the round-1 `<implementer>` and the matching round-1 `<blockers>`,
  amended the existing dirty WIP in place (no `git restore`/`clean`/
  `checkout`, no state reset), edited only the test source plus a
  source-module RED experiment that was reverted via backup +
  `just reeject` before handoff. Never hand-edited ejected output. The
  tightened assertion gates on rendered output via `render_host_pack`,
  not a hard-coded source copy, and is proven non-vacuous by the
  per-host stash/re-eject RED phase above.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-29T19:45:14Z" round="2">
Round-2 change is test-only and introduces no business-axis regression; REQ-006/REQ-007 wiring intent is intact. The round-2 edit tightened the CHK-008 no-artifact assertion in `speccy-cli/tests/skill_packs.rs` from a body-wide `contains("NOT write") && contains("artifact")` to the distinctive contiguous phrase `new *.md artifact file`. No skill-wiring source body changed, so the round-1 business pass stands. Wiring confirmed: REQ-006 — `speccy-brainstorm.md` (step 1) and `speccy-plan.md` (step 2) invoke `plan-explorer`, mark the report ephemeral, forbid a new `*.md` artifact, route into SPEC.md Summary + `<requirement>`; renders to both hosts. REQ-007 — `speccy-decompose.md` (step 1) invokes `plan-architect` before authoring TASKS.md, names its build-sequence checklist the candidate task list while decompose retains final `<task>` authorship, promotes load-bearing decisions into `### Decisions`; renders into both host decompose wrappers. Non-goals honored (no new artifact file types per DEC-002, no new commands, advisory contract intact). Non-blocking (carried from round 1): the task title says "unconditionally" while the wiring gates on "when the ask/slice touches existing code" — the faithful reading, matching REQ-006's `<behavior>` Given and the contracted done-when bullets.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:45:14Z" round="2">
Round-1 blocker closed. CHK-008's no-artifact assertion (`speccy-cli/tests/skill_packs.rs:1446`) now gates the distinctive phrase `new *.md artifact file`, scoped to the routing clause. Independently confirmed RED: stripping the no-artifact clause from each of `resources/modules/skills/speccy-brainstorm.md` and `speccy-plan.md` (keeping the `plan-explorer` invocation) + re-eject flips the test RED at line 1446 (the no-artifact assertion, not the invocation), for both skills across both hosts via the per-host loop over the shared source body. Invocation assertion and CHK-009 unregressed; full skill_packs suite green (42 passed). Evidence file present, roll call complete (CHK-008/CHK-009 demonstrated), red/green materially distinct, scoped per-test invocation — no fabrication concerns.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:45:14Z" round="2">
Round-2 delta is test-only (tightened `body.contains` assertion in `speccy-cli/tests/skill_packs.rs` plus a whitespace-only rustfmt normalization) with zero security-relevant surface: no code paths, auth, parsers, deserialization sinks, or secret/credential handling touched. The unchanged round-1 wiring prose carries no injection/exfil/env-read patterns and explicitly forbids persisting the explorer report to a stray `*.md` file — the safer posture. No real Speccy-repo identifiers leaked into shipped bodies (only generic subagent names and `DEC-NNN`/`T-NNN` placeholders); the new assertion string is a literal, not a path/command construction.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T19:45:14Z" round="2">
Round-2 change is a single assertion replacement in `speccy-cli/tests/skill_packs.rs:1446-1449`. The broad disjunction is gone; the distinctive phrase `new *.md artifact file` is now the sole assertion token, with an inline comment explaining why the scoping matters and why the old form was vacuous. Comment style, indentation, and trailing format strings all match the surrounding test file. No new helpers, no dead imports, no lint suppressions introduced. No style issues.
</review>
