---
spec: SPEC-0053
task: T-004
generated_at: 2026-05-29T19:30:00Z
---

<implementer date="2026-05-29T19:30:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added the aggregate packaging-invariant test for the three
  feature-dev ports (`reviewer-correctness`, `plan-explorer`,
  `plan-architect`) and confirmed reeject idempotency.
  - New test `feature_dev_personas_declare_speccy_model_conventions_and_attribution`
    in `speccy-cli/tests/skill_packs.rs`. It renders both host packs via
    `render_host_pack`, finds each persona's rendered wrapper by
    `rel_path` (`.claude/agents/<name>.md`,
    `.codex/agents/<name>.toml`), parses the Claude YAML frontmatter
    (`serde-saphyr`) and the Codex TOML (`toml`), and asserts each
    Claude `model == "opus[1m]"`, each Codex `model == "gpt-5.5"`,
    neither wrapper contains `sonnet`, and each persona body carries a
    `feature-dev` attribution line.
  - Added two small helpers (`find_rendered_agent`,
    `AgentModelFrontmatter`) and a `FEATURE_DEV_PERSONAS` constant,
    mirroring the existing `find_rendered_skill` /
    `RecipeFrontmatter` patterns in the same file.
  - Verified CHK-005 behaviorally: `just reeject && just reeject &&
    git diff --exit-code` produces a clean tree (no second-run diff).
- Undone: nothing in scope. T-004 is a verification task; the personas
  themselves were authored in T-001/T-002/T-003 and were already
  correct, so no wrapper/body edits were needed.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites pass, incl. the new test).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0 (pre-existing
    `float_literal_trailing_zero` unknown-option warnings only).
  - `cargo deny check`: exit 0 (advisories/bans/licenses/sources ok).
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-004.md`.
  Roll call for CHKs under REQ-004:
  - CHK-005 (reeject idempotency → clean tree on second run):
    demonstrated → executed `just reeject && just reeject &&
    git diff --exit-code` from HEAD, clean tree; recorded as paper
    trail. Not a Rust unit test because the eject pipeline runs via
    `just`/MiniJinja, outside the cargo test harness.
  - CHK-006 (each Claude `model: opus[1m]`, each Codex
    `model = "gpt-5.5"`, no `sonnet`, each body carries `feature-dev`):
    demonstrated → `feature_dev_personas_declare_speccy_model_conventions_and_attribution`
    asserts all four sub-claims over the rendered wrappers and bodies;
    red/green pair shown (sonnet mutation → FAILED, restored → ok).
- Discovered issues: none.
- Procedural compliance: followed the speccy-work first-attempt recipe
  on a clean tree. No source-module/skill friction encountered, so no
  `resources/` edits or `just reeject` of skill bodies were needed
  beyond the idempotency verification. The new test reads rendered
  output via `render_host_pack` rather than re-asserting hard-coded
  source copies — it derives the model strings from the actual
  rendered frontmatter and is proven non-vacuous by the mutation check.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:24:28Z" round="1">
T-004 satisfies REQ-004's observable contract. CHK-005: reeject is idempotent — running `just reeject` twice leaves all rendered output clean (only out-of-scope TASKS.md and the uncommitted test file differ). CHK-006: the new test at `speccy-cli/tests/skill_packs.rs:2052` parses frontmatter for all three feature-dev personas and asserts Claude `model: opus[1m]`, Codex `model = "gpt-5.5"`, no `sonnet`, and a body-level `feature-dev` attribution — verified against the actual rendered wrappers. Every `<done-when>` and the idempotency `<behavior>` map to the diff with no scope creep and no stale-intent drift. Attribution wording ("Ported from the `feature-dev` ... agent, narrowed to ...") is attribution, consistent with the non-goal against verbatim copying.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T19:24:28Z" round="1">
T-004's aggregate test `feature_dev_personas_declare_speccy_model_conventions_and_attribution` (speccy-cli/tests/skill_packs.rs:2032) genuinely gates all four sub-claims of CHK-006: reproduced green and independently ran four red-phase mutations (wrong Codex model, wrong Claude model, stray `sonnet`, removed `feature-dev` attribution) — each fails the test at the expected assert line. Model checks parse real YAML/TOML frontmatter and assert exact equality, not loose substrings. CHK-005 (reeject idempotency) independently confirmed: `just reeject` produced "0 overwritten, 36 unchanged" with an empty ejected-pack diff. Evidence roll call covers both CHKs under REQ-004 with accurate `demonstrated` labels; named test and helpers all present in the code under review. Non-blocking nit: the evidence file's captured runner output for CHK-006 is thin (single `ok` line + prose red phase), but the red/green transition is real — verified directly.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:24:28Z" round="1">
No security-relevant surface in SPEC-0053/T-004. The change is test-only: it adds an aggregate packaging-invariant test (`feature_dev_personas_declare_speccy_model_conventions_and_attribution`) plus helpers/constants in `speccy-cli/tests/skill_packs.rs`, and bumps existing persona-count constants/assertions to include `correctness`. No auth/authz boundaries, no input-validation or injection vectors (frontmatter parsed with the already-present `serde_saphyr`/`toml` crates — no new dependency, no new CVE surface), no secret/credential/token handling, no sensitive-data exposure in logs or error messages (panic messages echo only shipped template bodies, never secrets), and no crypto or race-condition concerns. The asserted model strings (`opus[1m]`, `gpt-5.5`, no `sonnet`) are routing config with no security implication.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T19:24:28Z" round="1">
All T-004 additions in `speccy-cli/tests/skill_packs.rs` match the file's established conventions: `find_rendered_agent` mirrors `find_rendered_skill`, `AgentModelFrontmatter` mirrors `RecipeFrontmatter`, and `FEATURE_DEV_PERSONAS` mirrors the existing `REVIEWER_PERSONAS`/`DEFAULT_REVIEWER_PERSONAS` constants. The `clippy::panic` concern is handled by routing through the pre-existing `panic_with_test_message` centralisation. No dead imports, unused variables, or lint suppressions introduced. Naming and comment style are consistent with adjacent code.
</review>
