---
spec: SPEC-0053
task: T-002
generated_at: 2026-05-29T19:30:00Z
---

<implementer date="2026-05-29T19:30:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Added the `plan-explorer` persona end-to-end.
  Authored `resources/modules/personas/plan-explorer.md` — a
  read-only codebase-grounding agent that traces a feature from
  entry points through abstraction layers to dependencies and
  returns a four-section report (entry points / core files,
  execution+call flows with data transformations, architecture
  layers and patterns, dependency map), each section required to
  carry `file:line` references. The body states an explicit
  advisory contract: it returns a report, never a `pass`/`blocking`
  verdict, never writes `TASKS.md` / edits `SPEC.md` / flips task
  state, and it does NOT include the `<review>` verdict-contract
  snippets. Carries a one-line `feature-dev` attribution mirroring
  the `reviewer-correctness` port note. Added host wrappers
  `resources/agents/.claude/agents/plan-explorer.md.tmpl`
  (`model: opus[1m]`, `effort: high`) and
  `resources/agents/.codex/agents/plan-explorer.toml.tmpl`
  (`model = "gpt-5.5"`, `model_reasoning_effort = "high"`),
  mirroring the existing `reviewer-correctness` wrappers — never
  `sonnet`. Added the CHK-003 structural test pair to
  `speccy-cli/tests/skill_packs.rs`. `plan-explorer` is not a
  reviewer persona, so it is NOT added to `personas::ALL`. Ran
  `just reeject`; both ejected files
  (`.claude/agents/plan-explorer.md`, `.codex/agents/plan-explorer.toml`)
  were created.

- Undone: Nothing in T-002's scope is deferred. The
  `init_phase_agents.rs` enumeration test was inspected and does NOT
  assert an exhaustive reviewer/plan-agent wrapper set (it only
  enumerates the three phase-worker agents), so no extension was
  needed there — the new plan-explorer wrappers do not break it.
  The wider read-only `tools:` grant and skill wiring are T-006 /
  T-005 respectively, per their own `<task>` elements.

- Hygiene checks: `cargo test --workspace` exited 0 (all tests pass,
  including the two new CHK-003 tests). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0. `cargo
  +nightly fmt --all --check` exited 0 (only pre-existing
  unknown-config-option warnings, unrelated to this change). `cargo
  deny check` was NOT run to a verdict: the `cargo-deny` subcommand
  is not installed in this environment (exit 101, "no such command:
  deny"). This change adds zero dependencies — only Markdown/TOML
  template content and a Rust test — so the dependency-advisory
  surface is unchanged.

- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-002.md`.
  Roll call for the single CHK under REQ-002:
  - CHK-003 (plan-explorer renders for both hosts with includes
    expanded; body carries no `<review` verdict marker — advisory,
    non-verdict contract): demonstrated → the test pair
    `plan_explorer_renders_with_includes_expanded_both_hosts` and
    `plan_explorer_body_has_no_review_verdict_marker_both_hosts` in
    `speccy-cli/tests/skill_packs.rs` render the host pack for both
    hosts, assert the wrappers exist with `{%` fully expanded, and
    assert the rendered body contains no `<review` marker. Sanity
    red shown in the evidence file (both failed before the binary
    embedded the new wrappers).

- Discovered issues: none. The `cargo-deny` absence is an
  environment gap, not a defect in this change.

- Procedural compliance: edited only source under `resources/` and
  the test crate, then ran `just reeject` to regenerate the ejected
  packs — no ejected file under `.claude/` / `.codex/` was
  hand-edited. `include_dir!` snapshots `resources/` at compile time;
  a fresh `touch speccy-cli/src/embedded.rs` was needed to force the
  binary to re-embed the newly added wrapper files before the
  CHK-003 tests went green (build-cache behaviour, no source change).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:47:19Z" round="1">
T-002 (REQ-002, `plan-explorer`) satisfies its contract. Each `<done-when>` maps to the diff: the report covers all four named sections (entry points/core files, execution/call flows with data transformations, architecture layers/patterns, dependency map) at `resources/modules/personas/plan-explorer.md:59-67` with a mandatory `file:line` grounding rule at lines 52-57; the advisory non-verdict, no-state-mutation contract is stated explicitly at lines 20-31; and the body includes none of the `<review>` verdict snippets (CHK-003's `<review` marker is absent — the test pair `plan_explorer_renders_with_includes_expanded_both_hosts` / `plan_explorer_body_has_no_review_verdict_marker_both_hosts` passes). The `<behavior>` Given/When/Then (report names entry points + dependencies, returns no verdict) is covered. Serves the brownfield-planning user story ("anchored in real code rather than guesses") — line 70-72 mandates surfacing unknowns over fabricating a `file:line`. No scope creep against the non-goals: no new command, ephemeral output (no `EXPLORE.md`), no gating power, correctly NOT added to `personas::ALL`, and tool-hardening (T-006) / skill-wiring (T-005) correctly deferred to their own tasks. Wrappers use `opus[1m]`/`gpt-5.5`, never `sonnet`; `feature-dev` attribution present. Changelog's recent intent shift was REQ-005-only and does not touch REQ-002.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:47:19Z" round="1">
CHK-003 is faithfully and non-vacuously tested. The two tests in `speccy-cli/tests/skill_packs.rs:1261` and `:1277` drive the real MiniJinja render pipeline via `render_host_pack` (walks the embedded `RESOURCES` bundle, the same engine `just reeject` uses) for both hosts — not a mock. Each Given/When/Then clause maps to a concrete assertion: existence (`.unwrap_or_else(panic)` lookup), includes expanded (`!body.contains("{%")`), and no verdict marker (`!body.contains("<review")`). The `<review` token is the genuine verdict-contract marker (verified against `reviewer-correctness.md:90`), so a leaked verdict snippet or unexpanded include would fail the suite — both tests gate real structural invariants. Both tests pass on a clean run; the `init_phase_agents.rs` enumeration suite (10 tests) still passes with the new wrappers, confirming no exhaustive-set assertion was missed. Evidence field present, file exists, CHK-003 roll call is `demonstrated` with specific test-name cites; red/green are materially distinct and the red-phase panic string matches `skill_packs.rs:1253`. Captured output is thinner than a full `cargo test` block, but the lines present are accurate and independently reproducible — not a blocking fabrication pattern for a prose+structural-test slice.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T18:47:19Z" round="1">
T-002 adds the read-only advisory `plan-explorer` persona (Markdown body + Claude/Codex wrapper templates) and one Rust structural test. No security-relevant surface: zero new dependencies (no Cargo.toml/Cargo.lock churn), no auth/authz boundaries, no input parsing, no secret/credential handling, no crypto, no logging or error-disclosure paths. The persona body declares an explicit least-privilege contract — read-only, no state mutation, no implement/refactor/stage (`resources/modules/personas/plan-explorer.md:20-31`) — which is the correct posture for a code-tracing agent; harness-level `tools:` enforcement is correctly deferred to T-006 and not in this slice. No plaintext/secret leakage, no privilege grant introduced.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T18:47:19Z" round="1">
`speccy-cli/tests/skill_packs.rs:1790` and `:1889` carry stale "other five" comments that contradict reality after `correctness` was added as the sixth non-tests reviewer persona. The diff updated only the doc comment on the `NON_TESTS_REVIEWER_PERSONAS` const (line 1796, changed to "other six") but left the two sibling comments behind: line 1790 (section-header `//`): "the other five built-in reviewer personas carry no evidence-related instruction."; line 1889 (inline `//` in test body): "evidence loading. The other five anchor on diff + SPEC +". Both should read "other six" to match the const's own updated doc comment and the actual array length `[&str; 6]`. Per the AGENTS.md "Docs match code" convention, comments must describe what the code actually does — these now describe a prior state.
</review>

<blockers date="2026-05-29T18:47:19Z" round="1">
One blocker landed in round 1 (style). Business, tests, and security passed. Fix it before respawning the review fan-out.

1. **Stale "other five" comments in `speccy-cli/tests/skill_packs.rs` (style).** Two comments still describe the pre-`correctness` state where there were five non-tests reviewer personas; `NON_TESTS_REVIEWER_PERSONAS` is now `[&str; 6]` (and its own doc comment at line 1796 already reads "other six"). Update both to "other six": (a) line 1790, the SPEC-0031 section-header comment — "...the other five built-in reviewer personas carry no evidence-related instruction." → "other six"; (b) line 1889-1890, the inline comment in `non_tests_reviewer_files_carry_no_evidence_instruction` — "The other five anchor on diff + SPEC + `<task-scenarios>` alone." → "other six". Per the AGENTS.md "Docs match code" convention, both must describe the current six-element array. (These comments became stale when T-001 added `correctness`; T-001 updated the const doc comment and array size but missed the two siblings. The fix lands here since T-002 owns the current `skill_packs.rs` changes.)

The mechanical floor is correct and must not regress: the `plan-explorer` persona body, both host wrappers, the advisory non-verdict contract, and the CHK-003 structural test pair all passed business/tests/security review. Only the two stale-comment edits must land for the retry.
</blockers>

<implementer date="2026-05-29T20:05:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Addressed the single round-1 style blocker — the two
  stale "other five" comments in `speccy-cli/tests/skill_packs.rs`.
  Changed line 1790 (the SPEC-0031 section-header `//` comment) and
  line 1889 (the inline `//` comment in
  `non_tests_reviewer_files_carry_no_evidence_instruction`) from
  "the other five" to "the other six", matching the
  `NON_TESTS_REVIEWER_PERSONAS` array's actual `[&str; 6]` length and
  its own doc comment (line 1796, already "other six"). No other
  files touched this round; the prior pass's WIP (plan-explorer
  persona body, both host wrappers, ejected packs, CHK-003 test pair)
  is unchanged.

- Undone: Nothing in T-002's scope is deferred.

- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0 (only pre-existing
  unknown-config-option warnings, unrelated). `cargo deny check`
  exited 101 — the `cargo-deny` subcommand is not installed in this
  environment, the same environment gap as round 1. This round
  changes only two comment words in a test file and adds zero
  dependencies, so the dependency-advisory surface is unchanged.

- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-002.md`.
  Roll call for the single CHK under REQ-002:
  - CHK-003 (plan-explorer renders for both hosts with includes
    expanded; body carries no `<review` verdict marker): hygiene →
    the unchanged test pair
    `plan_explorer_renders_with_includes_expanded_both_hosts` and
    `plan_explorer_body_has_no_review_verdict_marker_both_hosts` in
    `speccy-cli/tests/skill_packs.rs` still pass under `cargo test
    --workspace`. This round's edit is comment-only and does not
    touch the CHK-003 assertions.

- Discovered issues: none.

- Procedural compliance: retry-mode amend in place — edited only the
  two flagged comments in the existing dirty WIP; no `git restore` /
  `git clean` / `git checkout`, no rewrite from scratch, no reset.
  The `cargo-deny` absence is an environment gap, not a defect.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:53:54Z" round="2">
T-002 (REQ-002, `plan-explorer`) still satisfies its business contract after the round-2 comment-only change. The round-2 diff is exactly the two stale-comment edits the round-1 style blocker named — `speccy-cli/tests/skill_packs.rs` lines ~1790 ("the other five built-in reviewer personas" → "other six") and ~1889 ("The other five anchor on diff + SPEC +" → "other six") — and nothing else; the plan-explorer persona body, both host wrappers, and the CHK-003 test pair are unchanged round-1 WIP. On the business axis: every REQ-002 `<done-when>` maps to the diff (four-section grounding report with mandatory `file:line` references; explicit advisory non-verdict + no-state-mutation contract; no `<review>` verdict snippets, gated by the passing test pair). The `<behavior>` (returns a report, no verdict element) is covered, and the brownfield-planner user story is served (the body mandates surfacing unknowns over fabricating a `file:line`). No non-goal breach: no new command, ephemeral output (no `EXPLORE.md`), no gating power, correctly absent from `personas::ALL`. The Changelog's recent intent shift was REQ-005-only and does not touch REQ-002. The comment edits resolve no open question and drop no error path.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T18:53:54Z" round="2">
CHK-003 remains faithfully and non-vacuously tested after the round-2 comment-only edit. The pair `plan_explorer_renders_with_includes_expanded_both_hosts` (`skill_packs.rs:1260`) and `plan_explorer_body_has_no_review_verdict_marker_both_hosts` (`:1276`) drive the real `render_host_pack` MiniJinja pipeline for both hosts — not a mock. Each Given/When/Then clause maps to a concrete assertion: existence (`unwrap_or_else` panic lookup), includes expanded (`!body.contains("{%")` — verified zero `{%` in both rendered bodies), and no verdict marker (`!body.contains("<review")`). The `<review` token genuinely discriminates: all 7 reviewer wrappers carry it while the advisory plan-explorer carries zero, so a leaked verdict snippet would fail the suite. Both tests pass; the round-2 edit (two "other five"→"other six" comment words at `:1790` and `:1889`) is comment-only, does not touch any CHK-003 assertion, and the SPEC-0031 test it annotates (`non_tests_reviewer_files_carry_no_evidence_instruction`) still passes against the now-correct `[&str; 6]` array. Evidence field present on both `<implementer>` blocks, file exists, CHK-003 roll call present with a valid `hygiene` label and specific test-name cites; round-1 red/green output is materially distinct and the red-phase panic string matches `skill_packs.rs:1252-1254`. No fabrication pattern matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T18:53:54Z" round="2">
Round-2 change is a two-word comment edit ("five"→"six") in `speccy-cli/tests/skill_packs.rs` — no security surface. The full T-002 slice (read-only `plan-explorer` persona body + two host wrappers + one Rust structural test) adds zero dependencies and introduces no auth/authz boundary, input parsing, secret handling, crypto, or logging/error-disclosure path. The persona declares an explicit least-privilege, read-only, no-state-mutation contract (`resources/modules/personas/plan-explorer.md:20-25`), and the wrappers carry no `tools:` grant (harness-level enforcement correctly deferred to T-006). No privilege grant or plaintext/secret leakage introduced.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T18:53:54Z" round="2">
Both stale "other five" comments are now "other six": line 1790 (the SPEC-0031 section-header `//` comment) and line 1889 (the inline `//` comment in `non_tests_reviewer_files_carry_no_evidence_instruction`) both read "the other six" in the on-disk file, matching the `[&str; 6]` array and the const's own doc comment at line 1796. The round-2 diff touches only those two comment words — no other files changed, no new dead code, no naming drift, no suppression annotations. Style blocker from round 1 is resolved.
</review>
