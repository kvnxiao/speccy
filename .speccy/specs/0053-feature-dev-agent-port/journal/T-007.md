---
spec: SPEC-0053
task: T-007
generated_at: 2026-05-29T23:10:00Z
---

<implementer date="2026-05-29T23:10:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Added the `can we brainstorm` trigger phrase to the
  `speccy-brainstorm` skill description for both hosts (REQ-010).
  - Confirmed the description source is the per-host wrapper frontmatter
    (not a module body) — grep located it only in
    `resources/agents/.claude/skills/speccy-brainstorm/SKILL.md.tmpl`
    and `resources/agents/.agents/skills/speccy-brainstorm/SKILL.md.tmpl`.
  - Inserted `"can we brainstorm"` into the trigger list of both wrapper
    descriptions, directly after the existing `"let's brainstorm"`
    trigger so it sits alongside the sibling brainstorm phrases.
  - Ran `just reeject`; the ejected
    `.claude/skills/speccy-brainstorm/SKILL.md` and
    `.agents/skills/speccy-brainstorm/SKILL.md` now both carry the phrase
    in their rendered description.
  - Added the structural test
    `brainstorm_description_lists_can_we_brainstorm_trigger` (CHK-013)
    to `speccy-cli/tests/skill_packs.rs`. It renders the host pack for
    both `HostChoice::ClaudeCode` and `HostChoice::Codex` via
    `render_host_pack`, parses the rendered `speccy-brainstorm`
    frontmatter, and asserts the `description` contains the substring.
- Undone: nothing in scope. REQ-010 done-when (source lists the phrase;
  both hosts' rendered descriptions contain it) is satisfied.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md`.
  Roll call for CHKs under REQ-010:
  - CHK-013 (rendered `speccy-brainstorm` description contains
    `can we brainstorm` for both hosts): demonstrated → evidence
    Scenario 1; the test is RED before the source edit (description
    listed only the pre-existing triggers) and GREEN after editing both
    wrappers + `just reeject`, proven non-vacuous by the recorded
    red/green pair.
- Discovered issues: none.
- Procedural compliance: followed the speccy-work first-attempt recipe
  on a clean tree. Edited only the source wrappers under `resources/`,
  then ran `just reeject`; never hand-edited the `.claude`/`.agents`
  output. The new test reads rendered output via `render_host_pack`
  rather than re-asserting a hard-coded source copy, and is proven
  non-vacuous by the red/green pair. `cargo +nightly fmt --all`
  reflowed the new test's long `format!` lines before the gate.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/low" date="2026-05-29T20:07:53Z" round="1">
REQ-010 fully satisfied. Both `<done-when>` items met: `can we brainstorm` added to both source wrappers (`resources/agents/.claude/...` and `resources/agents/.agents/skills/speccy-brainstorm/SKILL.md.tmpl`) and present in both reejected outputs. CHK-013 is backed by a genuine structural test (`brainstorm_description_lists_can_we_brainstorm_trigger`, `speccy-cli/tests/skill_packs.rs`) that renders both host packs via `render_host_pack`, parses frontmatter YAML, and asserts on the `description` field. The goal/user-story (route the phrase to `/speccy-brainstorm`) is served; no non-goal breached (the sibling `plan-explorer` block in the brainstorm body is REQ-006/T-006 work, not T-007 scope creep). No open question silently resolved; the SPEC's own assumption (heuristic activation, not a guarantee) is honored, so no overpromise.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/low" date="2026-05-29T20:07:53Z" round="1">
The test `brainstorm_description_lists_can_we_brainstorm_trigger` (`speccy-cli/tests/skill_packs.rs:481`) is itself sound — a legitimate structural assertion, not a prohibited prose-substring match. It renders both host packs in-memory via `render_host_pack` (the same MiniJinja path `just reeject` uses, sourced from the `include_dir!`-embedded `resources/` bundle), parses the `speccy-brainstorm` SKILL.md frontmatter into the typed `RecipeFrontmatter`, and asserts the parsed `description` field contains `can we brainstorm` for both hosts. The matched string is the contracted activation phrase (REQ-010 CHK-013 names it verbatim), and `description` is a stable structural frontmatter field — exactly the surface AGENTS.md permits enforcing over. Independently confirmed red/green is meaningful: the pre-SPEC wrapper at `333f213` had zero occurrences, the phrase now lives only in the two wrapper descriptions the SUT renders from, so reverting the edit deterministically fails the assertion; the test passes green as committed.

The blocker is the evidence paper trail, not the test. `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md` records the red→green transition as pure prose ("Red: ... the test failed"; "Green: ... the test passes") with no captured runner output for either half — no `test brainstorm_description_lists_can_we_brainstorm_trigger ... ok` green line, and no red `assertion failed` / `RecipeFrontmatter` dump / `test result: FAILED` summary. This is the "prose summary rather than captured runner output" fabrication-risk pattern. The sibling task evidence file `evidence/T-002.md` in this same SPEC sets the bar by capturing both the green `test ... ok` lines and the specific red failure-message string, so the omission here is conspicuous. Re-record the evidence with the actual scoped runner output for the red phase (assertion failure against the un-edited wrapper) and the green phase (`cargo test -p speccy-cli --test skill_packs brainstorm_description_lists_can_we_brainstorm_trigger`), as T-002 did.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:07:53Z" round="1">
No security surface touched. T-007/REQ-010 inserts the literal trigger phrase `can we brainstorm` into the `speccy-brainstorm` skill `description` frontmatter in both host wrappers plus a frontmatter-parsing test in `speccy-cli/tests/skill_packs.rs`. No auth/authz, injection, secret-handling, sensitive-data-exposure, crypto, race-condition, or new-dependency concerns. The added YAML is correctly quoted/delimited and parses cleanly. Grants no new capability or privilege.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T20:07:53Z" round="1">
Both host template sources (`resources/agents/.claude/skills/speccy-brainstorm/SKILL.md.tmpl` and `resources/agents/.agents/skills/speccy-brainstorm/SKILL.md.tmpl`) carry the identical updated `description` field with `can we brainstorm` inserted after `let's brainstorm`, matching the existing comma-separated quoted-phrase list style. The ejected files reflect reeject output, not hand-edits. The CHK-013 test parses rendered frontmatter rather than asserting a source constant, which is non-vacuous.
</review>

<blockers date="2026-05-29T20:07:53Z" round="1">
One blocker landed in round 1 (tests). Business, security, and style passed. Fix it before respawning the review fan-out.

1. **Evidence file lacks captured runner output (tests).** `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md` records the CHK-013 red→green transition as pure prose ("the test failed" / "the test passes") with no captured runner output for either half — no green `test brainstorm_description_lists_can_we_brainstorm_trigger ... ok` line, no red `assertion failed` / `test result: FAILED` summary. This is the fabrication-risk "prose summary instead of captured runner output" pattern; the sibling `evidence/T-002.md` in this SPEC sets the bar by capturing both the green `test ... ok` lines and the specific red failure-message string. The test itself is sound and non-vacuous (verified) — only the evidence paper trail must be re-recorded. Fix: capture and paste the actual scoped runner output into `evidence/T-007.md` for (a) the RED phase — temporarily revert the `can we brainstorm` insertion in the wrapper source, re-embed, run `cargo test -p speccy-cli --test skill_packs brainstorm_description_lists_can_we_brainstorm_trigger`, and capture the assertion-failure output; then (b) restore the edit and capture the GREEN `test ... ok` run. Mirror the format of `evidence/T-002.md`.

The mechanical floor is correct and must not regress: the trigger phrase is wired into both host wrapper descriptions, reejected for both hosts, and the CHK-013 structural test is sound. Only the evidence file's captured red/green paper trail must be added for the retry.
</blockers>

<implementer date="2026-05-29T23:45:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: addressed the round-1 tests blocker. Re-recorded
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md` with actual
  captured runner output for the CHK-013 red→green pair, mirroring the
  `evidence/T-002.md` format. RED was captured by temporarily reverting the
  `can we brainstorm` insertion in both wrapper sources, forcing a re-embed
  (`touch speccy-cli/src/embedded.rs`), and running the scoped test — the
  pasted output shows the `assertion`-style panic at
  `skill_packs.rs:502` and `test result: FAILED`. The edit was then
  restored, re-embedded, and GREEN captured (`test ... ok` /
  `test result: ok`). No test or wrapper content changed beyond the
  transient revert-and-restore for the RED capture; the wrapper sources and
  ejected files are back to the correct green state (verified the phrase is
  present once in each of the two wrapper sources and the two ejected
  files; `git diff --stat` shows only the original four single-line
  insertions).
- Undone: nothing in scope.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md`.
  Roll call for CHKs under REQ-010:
  - CHK-013 (rendered `speccy-brainstorm` description contains
    `can we brainstorm` for both hosts): demonstrated → evidence Scenario 1
    now carries the actual scoped runner output for both the RED phase
    (`test result: FAILED` with the `skill_packs.rs:502` assertion panic
    against the reverted wrapper) and the GREEN phase (`test ... ok` /
    `test result: ok`) of
    `brainstorm_description_lists_can_we_brainstorm_trigger`.
- Discovered issues: none.
- Procedural compliance: followed the speccy-work retry branch — amended
  the prior pass's WIP in place (only the evidence file changed this
  round), did not reset state or rewrite files from scratch, and ran no
  `git restore`/`clean`/`checkout` against the dirty tree. The transient
  wrapper revert for the RED capture was restored and verified before the
  hygiene gate.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:13:07Z" round="2">
REQ-010 fully satisfied on the business axis. Both `<done-when>` items met: `can we brainstorm` is present in both source wrappers and in both rendered outputs, exactly once each, inserted into the existing comma-separated trigger list after `let's brainstorm`. `speccy check SPEC-0053/T-007` (CHK-013) passes. The goal bullet and the "route the phrase to `/speccy-brainstorm`" user story are served; the SPEC assumption that activation is heuristic (not a guarantee) is honored. No non-goal breached — no new command, no new artifact, no scope creep into sibling work. The round-2 change touched only `evidence/T-007.md` (captured red/green runner output for the tests-persona blocker); the code/wrapper surface and the SPEC-to-diff mapping are unchanged, so the business pass holds.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:13:07Z" round="2">
Round-1 blocker resolved. `evidence/T-007.md` now carries authentic captured runner output for both phases of `brainstorm_description_lists_can_we_brainstorm_trigger`: RED shows the assertion panic at `speccy-cli/tests/skill_packs.rs:502:9` with the un-edited description dump and `test result: FAILED. 0 passed; 1 failed; ... 44 filtered out`; GREEN shows `test ... ok` and `test result: ok. 1 passed`. Cross-checks confirm authenticity, not fabrication: line 502 is exactly the `assert!(fm.description.contains(PHRASE))` macro; the panic text is the verbatim expansion of the line-503 format string; the `44 filtered out` count matches the harness's 45 total `#[test]` fns minus the one named; the command is scoped, not the hygiene `--workspace` run; RED/GREEN halves are materially distinct. No regression: all four files contain `can we brainstorm` once, and the live scoped test passes green. CHK-013 roll call entry present and `demonstrated`; it is the only CHK under REQ-010. Test remains sound and non-vacuous (parses rendered frontmatter, asserts stable `description` field).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:13:07Z" round="2">
No security surface touched. T-007/REQ-010 inserts the literal trigger phrase `can we brainstorm` into the `speccy-brainstorm` skill `description` YAML frontmatter for both host wrappers, their ejected outputs, plus a frontmatter-parsing test. The round-2 change is evidence-only — `evidence/T-007.md` now carries captured red/green runner output, which contains no secrets, tokens, paths, or sensitive data (benign assertion-panic text). No auth/authz, injection, secret-handling, sensitive-data-exposure, crypto, race-condition, or new-dependency concerns. The added YAML is correctly double-quoted/delimited and parses cleanly. Grants no new capability or privilege.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T20:13:07Z" round="2">
Round-2 change is limited to `.speccy/specs/0053-feature-dev-agent-port/evidence/T-007.md`. The file structure matches the sibling `evidence/T-002.md` format: `#` heading, `## Scenario` section header, `### Red` / `### Green` subsections, fenced code blocks with `$`-prefixed command, runner output including `test result:` summary line, and a trailing source-of-truth file list. No naming drift, no dead content, no vacuous assertions, no convention violations. Style pass.
</review>
