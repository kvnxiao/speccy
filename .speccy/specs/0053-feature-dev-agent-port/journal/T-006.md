---
spec: SPEC-0053
task: T-006
generated_at: 2026-05-29T22:10:00Z
---

<implementer date="2026-05-29T22:10:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Hardened the ten read-only agents' tool grants and recorded
  the Codex per-subagent tool-restriction finding as a durable note.
  - Added `tools: Read, Grep, Glob, LS, Bash, WebFetch` to the Claude
    wrapper frontmatter of all ten read-only agents
    (`resources/agents/.claude/agents/{plan-explorer,plan-architect,
    reviewer-correctness,reviewer-business,reviewer-tests,
    reviewer-security,reviewer-style,reviewer-architecture,
    reviewer-docs,vet-reviewer}.md.tmpl`), inserted directly after the
    existing `effort:` line. The grant excludes `Edit`/`Write`/
    `NotebookEdit` by omission.
  - Left the five writer wrappers (`speccy-work`, `speccy-decompose`,
    `speccy-ship`, `vet-implementer`, `vet-simplifier`) untouched — no
    `tools:` field, so they keep full (unrestricted) access.
  - Codex side: did NOT add a `tools` field to the `.codex/*.toml.tmpl`
    wrappers. Verified against the Codex config reference
    (developers.openai.com/codex/config-reference) that Codex agent
    definitions expose only `config_file`, `description`, and
    `nickname_candidates` per agent; tool gating lives at MCP-server,
    app/connector, and global scope, not per-agent. Recorded this as a
    durable note in `AGENTS.md` ("Skill pack source of truth" section):
    Codex does not honor per-subagent tool restriction, so the Codex
    read-only posture remains prose-enforced — parity is stated as
    absent, not assumed.
  - Ran `just reeject`; the ten Claude `.claude/agents/*.md` files now
    carry the grant and the `.codex`/`.agents` outputs are unchanged.
    Re-running `just reeject` produced no further diff (idempotent).
  - Added two structural tests to `speccy-cli/tests/skill_packs.rs`:
    `read_only_agents_declare_read_only_tool_grant` (CHK-010) and
    `writer_agents_are_not_narrowed_to_read_only` (CHK-011), each
    rendering the Claude pack via `render_host_pack` and parsing the
    rendered wrapper frontmatter with a new `AgentToolsFrontmatter`
    (optional `tools` field) struct.
- Undone: nothing in scope. REQ-008 (grants + writers retained +
  idempotent reeject) and REQ-009 (durable Codex-parity note) are both
  satisfied.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-006.md`.
  Roll call for CHKs under REQ-008 / REQ-009:
  - CHK-010 (ten read-only Claude wrappers each declare `tools:`
    including `Read` and excluding `Edit`/`Write`): demonstrated →
    evidence Scenario 1; removing the `tools:` line from a read-only
    wrapper source + re-eject flips the test RED on the missing-grant
    panic, the intact tree is GREEN.
  - CHK-011 (the five writer wrappers are not narrowed to the read-only
    set; writers retain `Edit`/`Write`): demonstrated → evidence
    Scenario 2; adding a read-only `tools:` grant to a writer wrapper
    flips the test RED on the lost-`Edit`/`Write` assertion, the intact
    tree is GREEN.
  - CHK-012 (durable note states explicitly whether Codex honors
    per-subagent tool restriction): judgment-only → recorded in
    `AGENTS.md` "Skill pack source of truth"; no scriptable proof,
    reviewer-business / reviewer-docs judge the note's presence and
    explicitness on the diff (evidence Scenario 3).
- Discovered issues: none.
- Procedural compliance: followed the speccy-work first-attempt recipe
  on a clean tree. Edited only the source wrappers under `resources/`
  and `AGENTS.md`, then ran `just reeject`; never hand-edited the
  `.claude`/`.codex`/`.agents` output. The two new tests read rendered
  output via `render_host_pack` rather than re-asserting hard-coded
  source copies, and are proven non-vacuous by the per-wrapper RED
  experiments above. The Codex finding was verified against the live
  Codex config reference rather than assumed.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:00:32Z" round="1">
T-006 (REQ-008/REQ-009) delivers exactly what the SPEC promised. All ten named read-only wrappers (`plan-explorer`, `plan-architect`, `reviewer-correctness`, six `reviewer-*`, `vet-reviewer`) declare `tools: Read, Grep, Glob, LS, Bash, WebFetch` in both source (`resources/agents/.claude/agents/*.md.tmpl`) and rendered (`.claude/agents/*.md`) output, excluding Edit/Write/NotebookEdit — matching REQ-008's enumeration verbatim. The five writers (`speccy-work`, `speccy-decompose`, `speccy-ship`, `vet-implementer`, `vet-simplifier`) retain unrestricted access (no `tools:` field). The reader/writer split matches the requirement's named partition exactly. REQ-009's durable Codex-parity note lives in `AGENTS.md` ("Skill pack source of truth"), explicitly states Codex does NOT honor per-subagent tool restriction and that the Codex posture remains prose-enforced — and correctly no `tools` field was added to any `.codex/*.toml.tmpl`. The AGENTS.md edit is squarely within REQ-009 intent (the requirement names AGENTS.md as a valid durable location), not scope creep. CHK-010/CHK-011 are gated by genuine structural tests in `speccy-cli/tests/skill_packs.rs` that parse rendered frontmatter. No non-goal violated; no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:00:32Z" round="1">
T-006 tests genuinely gate the reader/writer split for the read-only `tools:` grant. `read_only_agents_declare_read_only_tool_grant` and `writer_agents_are_not_narrowed_to_read_only` (`speccy-cli/tests/skill_packs.rs`) parse rendered wrapper frontmatter via `render_host_pack` (not a hard-coded source copy) and assert the specific tool set, not mere presence. Reproduced three RED experiments against the source templates: (1) dropping `tools:` from a read-only wrapper flips the reader test RED, (2) adding a read-only grant to writer `speccy-work` flips the writer test RED on the lost-`Edit`/`Write` assertion, (3) a read-only grant that still lists `Edit` flips the exclusion assertion RED — proving the exclusion is load-bearing and the check is non-vacuous. Both tests green on the intact tree (full skill_packs suite: 44 passed). Evidence roll call covers CHK-010 (demonstrated), CHK-011 (demonstrated), CHK-012 (judgment-only, plausibly deferred — testing prose explicitness mechanically would be a forbidden editorial substring assertion). No fabrication patterns. (Note: this persona's RED experiments mutated and restored source templates; see the orchestrator note on the round-1 concurrent-mutation artifact that briefly affected sibling reads.)
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T20:00:32Z" round="1">
Read-only `tools:` grant hardening is sound. All ten read-only agents declare `tools: Read, Grep, Glob, LS, Bash, WebFetch` excluding Edit/Write/NotebookEdit by omission — verified in source and rendered output after `just reeject` (idempotent). The five writers carry no `tools:` field and retain full access. The Codex parity note in `AGENTS.md` accurately states Codex does NOT honor per-subagent tool restriction and that its read-only posture stays prose-enforced — it does not overstate enforcement. Both structural tests render real output and assert the right invariants; pass in the settled tree. Non-blocking note on Bash: `Bash` in this grant is unscoped in Claude Code's `tools:` allowlist and can mutate state (rm, git commit, redirection, network POSTs), so this grant is a write/edit-tool removal, not a sandbox — the "read-only" label overstates the mechanical guarantee for Bash-mediated actions. Inherent: the personas need Bash to fetch the diff (`git diff <merge-base>...HEAD`) and re-run checks, so dropping Bash is non-viable; the residual risk matches the advisory-only posture acknowledged for Codex. Acceptable as-is; flag only so a future host supporting Bash command-allowlisting tightens it.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T20:00:32Z" round="1">
All style checks pass (clean solo re-read on the settled tree; the round-1 parallel style read had blocked on a spurious `tools:` line in `speccy-work.md.tmpl` that was a transient artifact of a concurrent reviewer's RED experiment, not a real defect — confirmed absent on the settled tree). The ten read-only wrappers uniformly declare `tools: Read, Grep, Glob, LS, Bash, WebFetch` on line 6 immediately after `effort:` — consistent list, consistent placement — in both source templates and ejected files. The five writer wrappers carry no `tools:` line in source or ejected. The AGENTS.md Codex-parity note uses the existing bold-leader paragraph style (`**Title.**`) matching the sibling paragraphs in `## Skill pack source of truth`. All changes are source-only under `resources/`; ejected files in `.claude/agents/` are reeject output, not hand-edited. The `*.tmpl` trailing-newline state is intentional per the documented `.editorconfig` carve-out (`insert_final_newline = false`).
</review>
