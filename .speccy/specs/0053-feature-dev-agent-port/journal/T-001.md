---
spec: SPEC-0053
task: T-001
generated_at: 2026-05-29T18:09:21Z
---

<implementer date="2026-05-29T18:09:21Z" model="claude-opus-4-8[1m]" round="1">
- Completed: Landed the `reviewer-correctness` persona end-to-end as one
  atomic slice.
  - Authored `resources/modules/personas/reviewer-correctness.md`: an
    adversarial correctness reviewer scoped to logic/control-flow
    errors, `Option`/`Result` mishandling, off-by-one/boundary
    conditions, non-security races/deadlocks, and resource leaks. It
    `{% include %}`s the four shared review-contract snippets
    (`diff_fetch_command`, `verdict_return_contract` →
    `inline_note_format`/`no_tasks_md_writes`), names the four deferral
    targets (security, style, business, tests) as out-of-lane, states
    the confidence-≥80 reporting threshold with Critical/Important
    severity grouping, and carries a one-line `feature-dev` attribution.
  - Added host wrappers
    `resources/agents/.claude/agents/reviewer-correctness.md.tmpl`
    (`model: opus[1m]`, `effort: high`) and
    `resources/agents/.codex/agents/reviewer-correctness.toml.tmpl`
    (`model = "gpt-5.5"`, `model_reasoning_effort = "high"`), mirroring
    `reviewer-security`. Neither uses `sonnet`.
  - Inserted `"correctness"` into `speccy-core/src/personas.rs` `ALL` at
    index 4 (after `style`, before `architecture`) and widened
    `next.rs::default_personas()` from `ALL.get(..4)` to `ALL.get(..5)`.
    Updated the module/fn doc comments from the four-prefix / six-total
    shape to five-default / seven-total.
  - Added `reviewer-correctness` to both host branches of
    `resources/modules/skills/partials/review-fanout.md` (four→five
    spawns) and ran `just reeject`; the rendered `speccy-review` and
    `speccy-orchestrate` SKILL.md files now dispatch it.
  - CHK-014 needed no parser change: `parse/journal_xml` already
    validates `<review persona="…">` against `personas::ALL`, so adding
    `"correctness"` to `ALL` makes a `correctness` verdict block
    registry-valid.
- Undone: nothing in scope. The read-only `tools:` grant for
  `reviewer-correctness` is deliberately deferred to T-006 (its own
  task). The plan-time persona wiring is T-002/T-003/T-005.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: not runnable on this host (`cargo-deny` not
    installed). No dependency-graph change in this task — only persona
    prose, host wrappers, and `&[&str]` registry literals — so the deny
    surface is unchanged. Flagged rather than silently skipped.
- Evidence: no separate evidence paper trail file (this task's CHKs are
  structural-render and registry assertions proven by the test suite,
  not red/green behavioural scenarios). Roll call for CHKs under
  REQ-001 / REQ-005:
  - CHK-001 (both rendered wrappers exist with includes expanded, no
    placeholders): hygiene →
    `reviewer_correctness_renders_with_includes_expanded_both_hosts` in
    `speccy-cli/tests/skill_packs.rs` asserts no `{%` remains and the
    expanded verdict-contract text is present, for both hosts.
  - CHK-002 (four deferral targets present + literal `80` threshold):
    hygiene → `reviewer_correctness_body_names_deferrals_and_threshold`
    in `speccy-cli/tests/skill_packs.rs`.
  - CHK-007 (`default_personas()` equals the five-name list; rendered
    SKILL.md dispatches `reviewer-correctness`): hygiene →
    `default_personas_is_the_first_five_of_all` in `next.rs`,
    `default_personas_is_prefix_of_all` /
    `all_contains_exactly_seven_names_in_declared_order` in
    `personas.rs`, `registry_default_personas_is_first_five_prefix` in
    `speccy-core/tests/personas.rs`, and the
    `DEFAULT_REVIEWER_PERSONAS` loop in
    `speccy_review_skill_prefers_native_subagents`
    (`skill_packs.rs`) which now includes `correctness`.
  - CHK-014 (journal parser accepts `persona="correctness"`): hygiene →
    existing `parse/journal_xml` persona-validation tests pass against
    the widened `ALL`; validation is `PERSONAS_ALL.contains(...)` at
    `journal_xml/mod.rs:318`.
- Discovered issues: none new in the persona surface.
- Procedural compliance: Followed the source-of-truth rule — edited
  only `resources/…` and re-ran `just reeject`; never hand-edited
  `.claude/`/`.codex/`/`.agents/`. `just reeject` is idempotent (no
  diff on a second run).
  - One out-of-scope blocker had to be cleared to make the
    `cargo test --workspace` hygiene gate green: the
    `every_in_tree_spec_md_has_blank_line_after_each_close_tag` test in
    `speccy-core/tests/in_tree_specs.rs` was already RED at HEAD before
    this task, failing on ten `</scenario>`-immediately-before-
    `</requirement>` sites in this SPEC's own
    `.speccy/specs/0053-feature-dev-agent-port/SPEC.md` (an authoring
    defect from the prior decompose commit `3b4e2f5`). I inserted the
    missing blank line at each site — a purely cosmetic whitespace fix
    that changes no requirement text — then re-recorded the spec hash
    with `speccy lock SPEC-0053`. Reviewer note: this SPEC.md +
    TASKS.md-hash change is orthogonal to the persona work and is the
    only reason the working tree touches SPEC.md / TASKS.md frontmatter.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:13:17Z" round="1">
T-001 satisfies both covered requirements. REQ-001: `resources/modules/personas/reviewer-correctness.md` includes all four shared review-contract snippets (`diff_fetch_command` L14, `verdict_return_contract` L82 which transitively pulls `inline_note_format`/`no_tasks_md_writes`, `inline_note_format` L86), names all four deferral targets out-of-scope (L51-60), states the confidence-≥80 threshold and Critical/Important grouping (L66-78), and returns the existing `<review>` verdict contract — matching every `<done-when>` and serving the null-deref-blocking / naming-defer-to-style `<behavior>` cases. REQ-005: `personas::ALL` gains `correctness` at index 4 (seven total), `next.rs::default_personas()` returns the five-name list (CHK-007), `review-fanout.md` and both rendered `speccy-review` SKILL.md files dispatch `reviewer-correctness` (CHK-007 second clause), and `journal_xml/mod.rs:318` validates `persona="correctness"` against the widened `PERSONAS_ALL` (CHK-014). No non-goal violated: single-registry widen, no new commands/artifacts, confidence gate scoped to correctness only (DEC-001). The Changelog's registry-driven correction is reflected, not the old prose intent. The deferred read-only `tools:` grant correctly belongs to T-006. `cargo test --workspace` is green. The orthogonal cosmetic blank-line whitespace fix in this SPEC.md is out of per-task review scope and mutates no requirement text.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:13:17Z" round="1">
CHK-014's behavioral scenario is not exercised by any executable test. The scenario (REQ-005 `<behavior>`, SPEC.md:341) is concrete: "Given a `<review persona="correctness" verdict="pass" model="x">` block, when the journal parser reads it, then it accepts the persona name as registry-valid rather than emitting an unknown-persona error." No test in the diff drives a `correctness` persona block through `parse/journal_xml`. The string `correctness` appears in exactly four Rust files — `speccy-core/src/personas.rs`, `speccy-core/src/next.rs`, `speccy-core/tests/personas.rs`, `speccy-cli/tests/skill_packs.rs` — and in none of the journal parser source or its tests. The existing `journal_xml` review-parsing tests use only `persona="tests"` (mod.rs:595,683) and `persona="business"` (mod.rs:687) as fixtures; they pass regardless of whether `correctness` is parser-valid. The journal roll call labels CHK-014 "existing parse/journal_xml persona-validation tests pass against the widened ALL", but those cited tests do not exercise `correctness` — so the hygiene cite does not cover this CHK's actual scenario (the bare "suite passes" anti-pattern: a named test that passes independent of the behavior under review). Mental-rewrite test confirms the gap: if a future edit reverts the parser validation at `journal_xml/mod.rs:318` from `PERSONAS_ALL.contains(&persona)` to a hardcoded `["business","tests","security","style"]` list, every test landed for this task still passes — the registry tests still see `correctness` in `ALL`, and the parser tests only ever use `tests`/`business`. CHK-014's named regression (a `correctness` verdict block rejected as unknown-persona) would ship uncaught. Required fix: add a parser-level test in `speccy-core/src/parse/journal_xml/mod.rs` that parses a `correctness` review block and asserts it parses successfully as a `JournalEntry::Review` with `persona == "correctness"` (i.e. is NOT rejected with unknown-persona); a paired negative test asserting a genuinely-unknown persona name IS rejected would harden the registry-coupling, since there is currently no unknown-persona rejection test at all. Secondary (non-blocking): CHK-002's `reviewer_correctness_body_names_deferrals_and_threshold` matches `security`/`style`/`business` which also appear in the persona body's Focus section, so only the `tests` target actually gates deletion of the "Out of scope" section; consider scoping the assertion to the deferral section to make all four targets load-bearing.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:13:17Z" round="1">
No security findings. T-001 adds a correctness-reviewer persona (prose + two host wrappers + a one-line `personas::ALL` registry entry) — no executable/auth/parsing code paths introduced. Wrappers mirror `reviewer-security.md.tmpl` with no `tools:` grant (no privilege escalation; read-only grant correctly deferred to T-006) and no `sonnet`. The CHK-014 parser change is allowlist-based (`PERSONAS_ALL.contains(...)` at `speccy-core/src/parse/journal_xml/mod.rs:318`) — adding `correctness` preserves the closed-set trust boundary and rejects unknown personas, no injection vector. The persona body (`resources/modules/personas/reviewer-correctness.md:32-33,55-56,62-63`) explicitly routes auth boundaries, injection, secrets, and authorization-affecting races to the security lane, and `reviewer-security` remains in the default fan-out (`review-fanout.md`, `next.rs::default_personas()`), so no security-lane erosion. No Speccy-repo identifiers or secrets leaked into shipped resources; zero `Cargo.toml`/`Cargo.lock` delta, so the un-runnable `cargo deny` note is not a supply-chain concern for this slice.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T18:13:17Z" round="1">
All style invariants pass. The Rust changes (`personas.rs`, `next.rs`, `speccy-core/tests/personas.rs`, `speccy-cli/tests/skill_packs.rs`) follow the project's naming and doc-comment conventions, stale "four"/"six" identifiers are renamed throughout to "five"/"seven" as required, no dead imports are introduced, and `cargo clippy` exits clean. The new `reviewer-correctness.md` persona body and host wrappers mirror the `reviewer-security` sibling in structure and frontmatter fields (`model: opus[1m]`, `effort: high` for Claude; `model = "gpt-5.5"`, `model_reasoning_effort = "high"` for Codex). The attribution blockquote differs in form from the inline prose credit in `speccy-brainstorm` but satisfies REQ-004's "one-line attribution" requirement and is not a violation of any documented convention. The `no_tasks_md_writes` include is satisfied transitively via `verdict_return_contract.md`. No suppressions, no parallel helpers, no dead code in the diff.
</review>

<blockers date="2026-05-29T18:13:17Z" round="1">
One blocker landed in round 1 (tests). Business, security, and style passed. Fix it before respawning the review fan-out.

1. **CHK-014's behavioral scenario is not exercised by any executable test (tests).** REQ-005's `<behavior>` (SPEC.md:341) requires that a `correctness` review block parse as registry-valid rather than emitting an unknown-persona error. No test in the diff drives a `correctness` persona block through `parse/journal_xml`: the string `correctness` appears only in `speccy-core/src/personas.rs`, `speccy-core/src/next.rs`, `speccy-core/tests/personas.rs`, and `speccy-cli/tests/skill_packs.rs` — never in the journal parser source or its tests, whose review fixtures use only `persona="tests"` / `persona="business"`. Mental-rewrite confirms the gap: reverting the parser validation at `journal_xml/mod.rs:318` from `PERSONAS_ALL.contains(...)` to a hardcoded four-name list leaves every landed test green, so CHK-014's named regression ships uncaught. The journal roll call's CHK-014 cite ("existing parse/journal_xml persona-validation tests pass against the widened ALL") is the bare "suite passes" anti-pattern — those tests pass independent of whether `correctness` is parser-valid. Required fix: add a parser-level test in `speccy-core/src/parse/journal_xml/mod.rs` that parses a `correctness` review block and asserts it yields a `JournalEntry::Review` with `persona == "correctness"` (NOT rejected as unknown-persona); add a paired negative test asserting a genuinely-unknown persona name IS rejected, since no unknown-persona rejection test exists today.

Secondary (non-blocking): tighten CHK-002's `reviewer_correctness_body_names_deferrals_and_threshold` so the deferral-target assertion is scoped to the "Out of scope — defer" section. Today three of its four targets (`security`/`style`/`business`) also appear in the Focus section, so only the `tests` target gates deletion of the deferral section.

The mechanical floor is correct and must not regress: the persona body, both host wrappers, the `personas::ALL` widen at index 4, the `default_personas()` derivation, the `review-fanout.md` dispatch in both host branches, and the renamed registry tests all passed business/security/style review. Only the missing CHK-014 parser test (and optionally the CHK-002 tightening) must land for the retry.
</blockers>

<implementer date="2026-05-29T18:40:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Completed: Addressed the round-1 tests blocker (and the non-blocking
  secondary) by amending the prior pass's WIP in place — no rewrite of
  the round-1 mechanical floor.
  - Added the missing CHK-014 parser-level coverage in
    `speccy-core/src/parse/journal_xml/mod.rs`:
    - `correctness_review_persona_parses_as_registry_valid` drives a
      `<review persona="correctness" verdict="pass" …>` block through
      `parse(...)` and asserts it yields a `JournalEntry::Review` with
      `persona == "correctness"` (i.e. NOT rejected as unknown-persona).
    - `unknown_review_persona_is_rejected` (paired negative) drives a
      `persona="nonsense"` block through `parse(...)` and asserts it
      fails with `ParseError::InvalidJournalAttribute { attribute ==
      "persona", value == "nonsense" }`. There was no unknown-persona
      rejection test before this; the pair now hardens the
      registry-coupling at `journal_xml/mod.rs:318`
      (`PERSONAS_ALL.contains(...)`).
    - Mental-rewrite gate now holds: reverting that validation to a
      hardcoded four-name list makes the positive test RED (correctness
      rejected), so the named CHK-014 regression can no longer ship
      uncaught.
  - Tightened the non-blocking CHK-002 secondary in
    `speccy-cli/tests/skill_packs.rs`: the deferral-target assertion now
    extracts the "## Out of scope" section (line-based scan, stops at the
    next `## ` heading) and asserts all four targets
    (`security`/`style`/`business`/`tests`) appear *within that section*
    rather than anywhere in the body — so deleting the deferral section
    fails all four targets, not just `tests`. Used a line scan rather
    than string slicing to satisfy `clippy::string_slice`.
- Undone: nothing in scope. T-006 (read-only `tools:` grant) and the
  plan-time wiring (T-002/T-003/T-005) remain their own tasks.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (the two new parser tests and the
    tightened CHK-002 test all green).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0 (after replacing the initial string-slice section extraction
    with a line scan).
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: not runnable on this host (`cargo-deny` not
    installed). This round adds only test code; zero `Cargo.toml` /
    `Cargo.lock` delta, so the dependency-graph surface is unchanged.
    Flagged rather than silently skipped, same as round 1.
- Evidence: no separate evidence paper-trail file. CHK roll call for the
  blocker-relevant CHK:
  - CHK-014 (journal parser accepts `persona="correctness"`):
    demonstrated → `correctness_review_persona_parses_as_registry_valid`
    in `speccy-core/src/parse/journal_xml/mod.rs` parses a `correctness`
    verdict block and asserts registry-validity; paired
    `unknown_review_persona_is_rejected` asserts a non-registry persona
    is rejected. The positive test is RED under a hardcoded persona list
    and GREEN against the widened `personas::ALL`.
  - CHK-002 (deferral targets + threshold): hygiene →
    `reviewer_correctness_body_names_deferrals_and_threshold` in
    `speccy-cli/tests/skill_packs.rs`, now section-scoped so all four
    deferral targets are load-bearing.
- Discovered issues: none new.
- Procedural compliance: retry-shape amend — edited the dirty WIP in
  place; no `git restore`/`clean`/`checkout` against the prior pass's
  changes. No `resources/…` template change was needed this round
  (the blocker was a missing Rust test, not a skill-body defect), so no
  `just reeject` run. The orthogonal round-1 SPEC.md whitespace fix and
  re-locked hash are untouched.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:28:15Z" round="2">
T-001 (covers REQ-001, REQ-005) satisfies every `<done-when>` after the round-2 retry. REQ-001: `resources/modules/personas/reviewer-correctness.md` renders to both hosts with all four shared review-contract snippets expanded (`diff_fetch_command`, `verdict_return_contract`→`inline_note_format`/`no_tasks_md_writes`); the rendered `.claude/agents/reviewer-correctness.md` shows no unresolved placeholders (CHK-001 green). The "Out of scope" section (L55-60) names all four deferral targets (security/style/business/tests) and the body states the confidence-≥80 threshold with Critical/Important grouping (L66-78). The null-deref-blocking / naming-defer-to-style `<behavior>` cases are persona-prose runtime behaviors not statically gated by any CHK — consistent with the SPEC's scenario design, not a dropped requirement. REQ-005: `personas::ALL` gains `correctness` at index 4 (seven total); `next.rs::default_personas()` returns `["business","tests","security","style","correctness"]` (CHK-007 green); `review-fanout.md` and the rendered `.claude/skills/speccy-review/SKILL.md` dispatch `reviewer-correctness` as a fifth subagent (CHK-007 second clause). The journal parser validates `persona="correctness"` via the live `PERSONAS_ALL.contains(...)` at `speccy-core/src/parse/journal_xml/mod.rs:318` (registry-coupled, not hardcoded). Round-1 blocker resolution (tests/CHK-014): the round-2 implementer added exactly the requested parser-level coverage — `correctness_review_persona_parses_as_registry_valid` (positive) plus the paired `unknown_review_persona_is_rejected` (negative); both green. The CHK-002 secondary was tightened to scope the four-deferral-target assertion to the "Out of scope" section, making all four load-bearing. No retry-round scope creep: changes are confined to test code, round-1 mechanical floor untouched. No non-goal violated: single-registry widen (no parallel config), no new commands, no new artifact files, confidence gate scoped to correctness only (DEC-001). The Changelog's registry-driven correction is reflected in the implementation, not the superseded prose-only intent.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-29T18:28:15Z" round="2">
Round-1 blocker (CHK-014) is closed. The round-2 implementer added two parser-level tests in `speccy-core/src/parse/journal_xml/mod.rs`: `correctness_review_persona_parses_as_registry_valid` drives a real `correctness` review block through `parse(...)` and asserts it yields `JournalEntry::Review` with `persona == "correctness"`, and the paired `unknown_review_persona_is_rejected` asserts a `persona="nonsense"` block fails with the specific `ParseError::InvalidJournalAttribute { attribute == "persona", value == "nonsense" }` (not "any error"). Both tests exercise the real validation at `journal_xml/mod.rs:318` (`PERSONAS_ALL.contains(...)`), not a mock. I empirically confirmed the RED gate: temporarily reverting line 318 to a hardcoded `["business","tests","security","style"]` list makes `correctness_review_persona_parses_as_registry_valid` FAIL (rejected as unknown persona), and it returns GREEN against the real `personas::ALL` (all 13 `journal_xml::tests` pass) — so CHK-014's named regression can no longer ship uncaught. The non-blocking CHK-002 secondary is also addressed: `reviewer_correctness_body_names_deferrals_and_threshold` in `speccy-cli/tests/skill_packs.rs` now scopes the deferral-target assertion to the "Out of scope" section via a line scan, making all four targets load-bearing rather than only `tests`. Evidence roll call is intact: CHK-014 labelled demonstrated with test names present in the diff, CHK-002 labelled hygiene with a specific test cite; no fabrication pattern matched (the RED/GREEN transition I reproduced myself). Round-1 mechanical floor (business/security/style passes) is untouched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T18:28:15Z" round="2">
No security findings. T-001 adds a correctness-reviewer persona (prose body + two host wrappers + a one-line `personas::ALL` registry widen) and round-2 added test-only coverage. The journal parser validates `persona` against the widened allowlist at `speccy-core/src/parse/journal_xml/mod.rs:318` (`PERSONAS_ALL.contains(...)`) — verified live by running `correctness_review_persona_parses_as_registry_valid` (accepts `correctness`) and `unknown_review_persona_is_rejected` (rejects `nonsense` with `InvalidJournalAttribute`); the closed-set trust boundary holds, no injection vector. Host wrappers (`resources/agents/.claude/agents/reviewer-correctness.md.tmpl`, `resources/agents/.codex/agents/reviewer-correctness.toml.tmpl`) mirror `reviewer-security` with no `tools:` grant (no privilege escalation; read-only grant correctly deferred to T-006) and no `sonnet`. `reviewer-security` remains in the default fan-out (`next.rs::default_personas()`, rendered `.claude/skills/speccy-review/SKILL.md`), so no security-lane erosion; the persona body (`resources/modules/personas/reviewer-correctness.md:55-56`) explicitly routes auth/injection/secrets/authorization-races to the security lane. No secrets or Speccy-repo identifiers leaked into shipped resources, includes fully expanded with no placeholders, and zero `Cargo.toml`/`Cargo.lock` delta so the un-runnable `cargo deny` note is not a supply-chain concern for this slice.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T18:28:15Z" round="2">
The round-2 changes are test-only and style-clean throughout. `speccy-core/src/parse/journal_xml/mod.rs`: both new tests (`correctness_review_persona_parses_as_registry_valid`, `unknown_review_persona_is_rejected`) follow the module's established snake_case descriptive naming (matching `date_only_is_rejected`, `invalid_verdict_is_rejected`, etc.), use the existing `make()` / `parse()` / `path()` helpers and `indoc!` macro, and deploy `matches!` for error pattern assertions consistent with sibling tests. No dead imports or variables introduced. `speccy-cli/tests/skill_packs.rs`: the new `rendered_correctness_body` helper uses `render_host_pack` and `panic_with_test_message` — the file's own canonical helpers. The section-scoped deferral-target assertion uses a line-scan rather than string slicing, consistent with the clippy constraint noted in the implementer block. No parallel implementations invented. One pre-existing drift noted outside the verdict (not a blocker, untouched by this diff): the T-009 and T-010 section header block comments still say "Six wrappers, one per shipped reviewer persona" after the count literals moved to seven — a natural follow-up cleanup for the next task touching that section.
</review>
