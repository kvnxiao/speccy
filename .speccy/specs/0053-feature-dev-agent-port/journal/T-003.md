---
spec: SPEC-0053
task: T-003
generated_at: 2026-05-29T20:30:00Z
---

<implementer date="2026-05-29T20:30:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Added the `plan-architect` persona end-to-end. Authored
  `resources/modules/personas/plan-architect.md` — a read-only
  architecture-design agent that analyzes existing codebase patterns
  and returns a four-section implementation blueprint: component
  design, file map (files to create / modify), data flow, and a
  build sequence rendered as an ordered checklist. The body
  dedicates a "Build sequence — an agent-sized ordered checklist"
  section stating each item is agent-sized (one item ≈ one plausible
  single Speccy task), dependency-ordered top to bottom, and
  directly consumable as candidate tasks — while the decomposing
  skill retains final authorship (merge/split/reorder/renumber). It
  states the same advisory contract as `plan-explorer`: returns a
  blueprint, never a `pass`/`blocking` verdict, never writes
  `TASKS.md` / edits `SPEC.md` / flips task state, does NOT include
  the `<review>` verdict-contract snippets, and carries a `file:line`
  grounding requirement with a surface-unknowns-never-fabricate rule.
  Carries a one-line `feature-dev` attribution mirroring the
  `plan-explorer` port note. Added host wrappers
  `resources/agents/.claude/agents/plan-architect.md.tmpl`
  (`model: opus[1m]`, `effort: high`) and
  `resources/agents/.codex/agents/plan-architect.toml.tmpl`
  (`model = "gpt-5.5"`, `model_reasoning_effort = "high"`),
  mirroring the `plan-explorer` wrappers — never `sonnet`. Added the
  CHK-004 structural test trio to `speccy-cli/tests/skill_packs.rs`
  (includes-expanded, no-`<review`-marker, agent-sized-build-sequence,
  each across both hosts). `plan-architect` is not a reviewer
  persona, so it is NOT added to `personas::ALL`. Ran `just reeject`;
  both ejected files (`.claude/agents/plan-architect.md`,
  `.codex/agents/plan-architect.toml`) were created.

- Undone: Nothing in T-003's scope is deferred. The
  `init_phase_agents.rs` enumeration test was inspected and does NOT
  assert an exhaustive reviewer/plan-agent wrapper set (it enumerates
  only the three phase-worker agents), so no extension was needed —
  the new plan-architect wrappers do not break it. Skill wiring of
  `plan-architect` into `speccy-decompose` is T-005; the wider
  read-only `tools:` grant is T-006; aggregate packaging invariants
  (idempotency / model / attribution across all three new personas)
  are T-004. Each is scoped to its own `<task>` element.

- Hygiene checks: `cargo test --workspace` exited 0 (all tests pass,
  including the three new CHK-004 tests). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0. `cargo
  +nightly fmt --all --check` exited 0 (only pre-existing
  unknown-config-option warnings, unrelated to this change). `cargo
  deny check` exited 0 (advisories ok, bans ok, licenses ok, sources
  ok) — installed `cargo-deny v0.19.8` into this environment to run
  the gate to a verdict.

- Evidence: paper trail at
  `.speccy/specs/0053-feature-dev-agent-port/evidence/T-003.md`.
  Roll call for the single CHK under REQ-003:
  - CHK-004 (plan-architect renders for both hosts with includes
    expanded; body carries no `<review` verdict marker; body
    specifies build-sequence items are agent-sized): demonstrated →
    the test trio `plan_architect_renders_with_includes_expanded_both_hosts`,
    `plan_architect_body_has_no_review_verdict_marker_both_hosts`, and
    `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts`
    in `speccy-cli/tests/skill_packs.rs` render the host pack for both
    hosts, assert the wrappers exist with `{%` fully expanded, assert
    the rendered body contains no `<review` marker, and assert the
    body contains `agent-sized` plus a build-sequence reference.
    Sanity red shown in the evidence file (lookup panics before the
    binary embedded the new wrappers).

- Discovered issues: none specific to this change. Same
  build-cache behaviour as T-002: `include_dir!` snapshots
  `resources/` at compile time, so a fresh
  `touch speccy-cli/src/embedded.rs` was needed to force the binary
  to re-embed the newly added wrapper files before `just reeject`
  ejected them (the first reeject reported `0 created`). No source
  change; build-cache only.

- Procedural compliance: edited only source under `resources/` and
  the test crate, then ran `just reeject` to regenerate the ejected
  packs — no ejected file under `.claude/` / `.codex/` was
  hand-edited. Confirmed `just reeject` is idempotent (a second run
  left no ejected-file diff; only the source edits and TASKS.md state
  flip remain in `git status`).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T19:05:35Z" round="1">
T-003 satisfies REQ-003 end-to-end. All three `<done-when>` items are observable in `resources/modules/personas/plan-architect.md`: the blueprint covers component design / file map / data flow / build-sequence (`:36-53`, `:81-84`); the build-sequence section states items are agent-sized = one Speccy task and directly consumable as candidate tasks while decompose retains authorship (`:55-73`); and the advisory contract explicitly disclaims verdicts and state mutation (`:16-32`) with no `<review` marker present. CHK-004's three structural tests pass for both hosts. Scope is tight — no `tools:` grant (deferred to T-006), no skill wiring (T-005), correctly absent from `personas::ALL` (advisory, not a reviewer); wrappers use `opus[1m]`/`gpt-5.5`, never `sonnet`. Honors the non-goals (no new artifact file, no new command, no gating power) and the `feature-dev` attribution is present. No open question was silently decided.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/high" date="2026-05-29T19:05:35Z" round="1">
The `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts` test in `speccy-cli/tests/skill_packs.rs:1322` does not gate the behavior CHK-004 / REQ-003 `<done-when>` requires ("the body specifies that build-sequence items are agent-sized"). It asserts `body.contains("agent-sized")` and `body.contains("build sequence"|"build-sequence")` against the *full rendered file*, but those strings also appear in the wrapper frontmatter `description` line (`resources/agents/.claude/agents/plan-architect.md.tmpl:3` and `.codex/...toml.tmpl:2`: "...a build-sequence checklist whose items are agent-sized..."). Verified adversarially: stripping every `agent-sized` and `build sequence`/`build-sequence` occurrence from the persona body while leaving the wrappers intact, then forcing a re-embed (`touch speccy-cli/src/embedded.rs`) and rerunning, leaves the test green. An implementer could ship a `plan-architect` whose body never instructs agent-sized build-sequence items and this test would not catch it — the load-bearing requirement of REQ-003 goes ungated. The evidence file labels CHK-004 `demonstrated`, but this half of the scenario is not actually demonstrated. Fix: scope the assertion to the rendered body below the frontmatter (split off the frontmatter and assert on the body portion only), or assert on a phrase that exists only in the persona body's build-sequence section, not in the description. Re-run with a forced re-embed to confirm the test now fails when the body's "agent-sized" build-sequence language is removed. (The other two tests are sound — `plan_architect_renders_with_includes_expanded_both_hosts` exercises the real `render_host_pack` pipeline, and `plan_architect_body_has_no_review_verdict_marker_both_hosts` genuinely fails when a `<review` marker leaks in.)
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:05:35Z" round="1">
No security concerns. T-003 adds only static skill-pack prose: the `plan-architect` persona body, two host wrappers, the ejected packs, and a structural test trio. No endpoints, auth boundaries, data access, secrets, env-var handling, or crypto are touched. The persona contract is explicitly read-only ("never write TASKS.md, edit SPEC.md, flip task state, or mutate any project state"), and the wrappers grant no `tools:` capability (the wider read-only grant is deferred to T-006) — so this slice introduces no privilege expansion. The MiniJinja `{% include %}` pulls only repo-controlled static prose, so no template-injection surface is added. No new dependencies; `cargo deny check` passed per the journal. Borderline item flagged for the style/convention lane (not a security defect): `speccy-core/src/next.rs:42` is used as an illustrative `file:line` in the shipped body — a real Speccy-repo identifier that AGENTS.md says must not appear in shipped template bodies (they land in downstream repos via `speccy init`).
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T19:05:35Z" round="1">
Both new wrapper templates end with a trailing newline (`0x0a`) where the project convention requires no trailing newline. `resources/agents/.claude/agents/plan-architect.md.tmpl` ends with `0x0a`; all 7 sibling Claude Code reviewer wrappers end with `0x7d` (`}`), matching the comment at `speccy-cli/tests/skill_packs.rs:944` ("ends at `%}` with no trailing newline..."), and the `plan-explorer.md.tmpl` peer also ends with `0x7d`. `resources/agents/.codex/agents/plan-architect.toml.tmpl` ends with `0x0a`; all 7 sibling Codex reviewer wrappers end with `0x22` (`"`), machine-tested at `speccy-cli/tests/skill_packs.rs:1480` (`contents.ends_with("\"\"\"")`), and `plan-explorer.toml.tmpl` also ends with `0x22`. (The T-010 test filters on the `reviewer-` prefix so it does not catch `plan-architect.toml.tmpl` automatically, but the convention is the same.) Fix: remove the trailing newline from the final line of each wrapper so `plan-architect.md.tmpl` ends with `%}` and `plan-architect.toml.tmpl` ends with `"""`.
</review>

<blockers date="2026-05-29T19:05:35Z" round="1">
Two blockers landed in round 1 (tests, style), plus one convention fix surfaced by security. Business passed. Fix all three before respawning the review fan-out.

1. **Vacuous build-sequence test (tests).** `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts` (`speccy-cli/tests/skill_packs.rs:1322`) asserts `agent-sized` / `build sequence` against the *full rendered file*, but both phrases also appear in the wrapper frontmatter `description` (`.claude/agents/plan-architect.md.tmpl:3`, `.codex/agents/plan-architect.toml.tmpl:2`). Adversarially confirmed: stripping the language from the persona body (leaving wrappers intact) + `touch speccy-cli/src/embedded.rs` + rerun leaves the test GREEN, so REQ-003's "body specifies agent-sized build-sequence items" goes ungated. Fix: scope the assertion to the rendered body below the frontmatter (split off the YAML frontmatter and assert on the body only), or assert a phrase that exists only in the body's build-sequence section. Re-verify the test goes RED when the body language is removed (forced re-embed).

2. **Trailing newline in both wrapper templates (style).** `resources/agents/.claude/agents/plan-architect.md.tmpl` ends with `0x0a` but must end with `%}` (`0x7d`), matching all 7 sibling Claude wrappers and the `plan-explorer.md.tmpl` peer. `resources/agents/.codex/agents/plan-architect.toml.tmpl` ends with `0x0a` but must end with `"""` (final byte `0x22`), matching all 7 sibling Codex wrappers (machine-tested at `skill_packs.rs:1480`) and the `plan-explorer.toml.tmpl` peer. Remove the trailing newline from the final line of each wrapper, then `just reeject`.

3. **Real Speccy-repo identifier in shipped bodies (convention / AGENTS.md, surfaced by security).** `resources/modules/personas/plan-architect.md:72` uses the real path `speccy-core/src/next.rs:42` as an illustrative `file:line`. AGENTS.md forbids real Speccy-repo identifiers in shipped template / reference / skill bodies — they land in downstream repos via `speccy init`, where the path is meaningless. Replace with an obviously fictional placeholder (e.g. `src/widget/render.rs:42`), matching how `reviewer-correctness.md` already uses a fictional `src/poll.rs:42`. **Also apply the identical fix to the sibling `resources/modules/personas/plan-explorer.md:49`** (committed in T-002), which leaks the same `speccy-core/src/next.rs:42` path — both plan-* persona bodies were authored in this SPEC and carry the identical violation, so fix them together for consistency. (Reviewer note for round 2: the cross-file touch of plan-explorer.md is this documented convention fix, not unexplained scope bleed.) Re-run `just reeject` so both ejected bodies pick up the placeholder.

The mechanical floor is correct and must not regress: the `plan-architect` persona body's blueprint sections (component design / file map / data flow / agent-sized build sequence) and the advisory non-verdict contract passed business/security review, and two of the three CHK-004 tests are sound. Only the one vacuous test, the two trailing newlines, and the two real-path placeholders must change for the retry.
</blockers>

<implementer date="2026-05-29T21:30:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Amended round-1 WIP in place to clear all three round-1
  blockers. (1) Tests: rewrote the two assertions in
  `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts`
  (`speccy-cli/tests/skill_packs.rs`) to match on language that lives
  only in the included persona body, never the wrapper frontmatter
  `description` — the dedicated section heading
  `## Build sequence — an agent-sized ordered checklist` and the
  sizing sentence `one item is a plausible single Speccy task`. The
  prior loose `agent-sized` / `build-sequence` substrings also matched
  the frontmatter description (which paraphrases with `one item ≈ one
  Speccy task`), so they could not catch a body that omitted the
  language. (2) Style: removed the trailing `0x0a` from both wrappers
  so `resources/agents/.claude/agents/plan-architect.md.tmpl` now ends
  `%}` (`0x7d`) and `resources/agents/.codex/agents/plan-architect.toml.tmpl`
  ends `"""` (final byte `0x22`), matching every sibling wrapper and
  the `plan-explorer.*` peers. (3) Convention: replaced the leaked real
  path `speccy-core/src/next.rs:42` with the fictional placeholder
  `src/widget/render.rs:42` in `resources/modules/personas/plan-architect.md`
  and applied the identical fix to the sibling
  `resources/modules/personas/plan-explorer.md` (round-1 blockers
  documented this cross-file touch as the intended convention fix, not
  scope bleed). Ran `just reeject` (after `touch
  speccy-cli/src/embedded.rs` to force re-embed); both ejected `.codex`
  plan-* bodies picked up the placeholder. No leaked path remains in
  any source or ejected plan-* file.

- Undone: nothing in T-003's retry scope is deferred. The ejected
  files still end in `0x0a` by design — the eject pipeline appends its
  own trailing newline to all ejected outputs (verified against the
  `reviewer-*` ejected siblings); the no-trailing-newline convention
  is a property of the `.tmpl` wrapper sources, which are now correct.

- Hygiene checks: `cargo test --workspace` exited 0 (59 + suites all
  green; the three plan_architect tests pass). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0 (only the pre-existing
  `float_literal_trailing_zero` unknown-config warnings, no diff).
  `cargo deny check` exited 0 (advisories ok, bans ok, licenses ok,
  sources ok).

- Evidence: CHK-004 roll call for REQ-003 (re-verified this round):
  - CHK-004 (plan-architect renders both hosts, includes expanded; no
    `<review` verdict marker; body specifies agent-sized build
    sequence): demonstrated → adversarial RED confirmed this round —
    stripping the section heading and sizing sentence from the body
    (leaving the frontmatter description intact) + `touch
    speccy-cli/src/embedded.rs` + rerun makes
    `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts`
    FAIL, then restoring the body returns it GREEN. The other two CHK-004
    tests were already sound and remain green.

- Discovered issues: none new. Same `include_dir!` build-cache
  behaviour noted in round 1: a `touch speccy-cli/src/embedded.rs` is
  needed before `just reeject` re-embeds edited `resources/` files.

- Procedural compliance: edited only `resources/` source and the test
  crate, then ran `just reeject`; no ejected file under `.claude/` /
  `.codex/` was hand-edited. Followed the retry-shape contract —
  amended the existing uncommitted WIP in place, no `git restore` /
  `clean` / `checkout` against the dirty paths, no reset of task state.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:15:56Z" round="2">
T-003 round 2 satisfies REQ-003 end-to-end and clears all three round-1 blockers. All `<done-when>` items are observable in `resources/modules/personas/plan-architect.md` (blueprint sections `:36-47`/`:81-84`, agent-sized build-sequence `:49-67`, advisory non-verdict contract `:16-29`, no `<review` marker). The round-2 test rewrite (`plan_architect_body_specifies_agent_sized_build_sequence_both_hosts`) now gates the body's agent-sized language, not the frontmatter description — confirmed via adversarial RED (stripping the body heading + sizing sentence makes the test fail). Both wrappers end at `%}`/`"""` with no trailing newline, matching the `plan-explorer` peers. The leaked `speccy-core/src/next.rs:42` is replaced with the fictional `src/widget/render.rs:42` in both `plan-architect.md:72` and the documented cross-touch `plan-explorer.md:49`, with no real path remaining in any shipped body. Scope is tight and honors all non-goals: no new command, no new artifact file, no gating power, `feature-dev` attribution present, `opus[1m]`/`gpt-5.5` never `sonnet`, correctly absent from `personas::ALL`. No open question silently decided. (Non-blocking note: the three CHK-004 tests need a forced `touch speccy-cli/src/embedded.rs` re-embed locally due to `include_dir!` incremental-build caching — a clean build re-reads `resources/`, so not a CI risk.)
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-29T19:15:56Z" round="2">
Round-1 vacuity closed and independently verified. `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts` (`speccy-cli/tests/skill_packs.rs:1375,1379`) now asserts body-only language — the markdown heading `## Build sequence — an agent-sized ordered checklist` and the sizing sentence `one item is a plausible single Speccy task` — neither of which appears in the wrapper frontmatter `description` (which paraphrases as `agent-sized (one item ≈ one Speccy task)`). Reproduced the adversarial RED: stripped both phrases from `resources/modules/personas/plan-architect.md` body only, left both wrappers intact, `touch speccy-cli/src/embedded.rs`, reran → the test FAILED at `skill_packs.rs:1375` (failure dump still shows the wrapper `description` carrying `agent-sized`, confirming the old body-wide contains would have falsely passed) while the other two CHK-004 tests stayed GREEN; restored the body → all three GREEN. Round-2 collateral confirmed clean: wrapper byte-endings now `0x7d`/`0x22`, the includes-expanded and no-review-marker tests pass, and the `plan-explorer.md:49` → `src/widget/render.rs:42` placeholder swap broke no sibling test. Full `cargo test --workspace` green. Evidence roll call labels CHK-004 `demonstrated`, names the three real test functions present in the diff, and the round-2 adversarial RED claim holds under independent reproduction. No fabrication pattern matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T19:15:56Z" round="2">
No security concerns; the round-1 observation is resolved. The leaked real path `speccy-core/src/next.rs:42` is gone from both shipped bodies — `resources/modules/personas/plan-architect.md:72` and `resources/modules/personas/plan-explorer.md:49` now use the fictional placeholder `src/widget/render.rs:42`, and all four ejected files carry the placeholder. A scan for `speccy-core`/`speccy-cli`/`next.rs`/spec IDs/slugs/repo/branch identifiers across both shipped bodies, both wrappers, and all ejected outputs returned no matches, so nothing meaningless-in-downstream-repos ships via `speccy init`. The slice remains static prose only: explicit read-only no-state-mutation persona contract, no `tools:` grant in either wrapper (no privilege expansion; wider grant deferred to T-006), no auth/secret/crypto surface, no new dependencies, and the MiniJinja `{% include %}` pulls only repo-controlled static content (no template-injection surface).
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T19:15:56Z" round="2">
All three round-1 blockers are resolved and verified on-disk. Trailing newlines: `tail -c 1 ... | od -An -tx1` confirms `0x7d` for `plan-architect.md.tmpl` and `0x22` for `plan-architect.toml.tmpl`, matching every sibling wrapper. Real-path placeholder: both source files (`plan-architect.md:72`, `plan-explorer.md:49`) and both ejected packs use `src/widget/render.rs:42`; no `speccy-core/src/next.rs` remains anywhere in the plan-* bodies. Vacuous test: `plan_architect_body_specifies_agent_sized_build_sequence_both_hosts` now asserts on `## Build sequence — an agent-sized ordered checklist` and `one item is a plausible single Speccy task` — phrases that live only in the persona body section, not the frontmatter description. No dead code, no new suppressions, naming matches sibling wrappers. Round-2 diff is style-clean.
</review>
