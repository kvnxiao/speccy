# Evidence: T-006

<evidence task="T-006" spec="SPEC-0031">

## Session 2026-05-18-T006-rev1 (attempt 1)

Command: `grep -c '^F-9:' .speccy/BACKLOG.md`

<red exit="1">
0
</red>

`grep -c` prints `0` and exits 1 (no matches) against the pre-edit
`.speccy/BACKLOG.md`, where Tier 2 carries only `F-8` and `F-6` and
no `F-9` header exists yet. This is the failing-verification baseline:
the file does not yet satisfy REQ-006's `^F-9:` presence scenario.

<green exit="0">
1
</green>

After appending the F-9 entry under Tier 2 (between `F-6` and the
`Tier 3 — reject` boundary), `grep -c '^F-9:'` prints `1` and exits 0:
exactly one `F-9:` header is now present. The entry carries the four
field-equivalents the task-scenarios block names (what / why / where /
heuristic-risk), references SPEC-0031 as the pattern's origin, names
the eject-vs-inline heuristic (≥ ~8 lines OR ≥ 2 consuming prompts),
and the over-ejection risk. The pre-existing `F-3` entry under Tier 1
is unchanged and carries no `closed by SPEC-0031` annotation, which is
correct: `speccy-ship` writes the F-3 closure at ship time, not this
task.

</evidence>
