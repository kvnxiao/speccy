---
spec: SPEC-0063
task: T-003
generated_at: 2026-06-13T03:14:02Z
---

<implementer date="2026-06-13T03:14:02Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: bounded to the test file and three persona sources named in the task. Test `feature_dev_personas_declare_speccy_model_conventions_and_attribution` in `skill_packs.rs` — extended (renamed, attribution assertion block deleted). Shared `read_persona` helper reused-as-is and retained (other callers: `persona_content_shape`, the `reviewer_correctness_*` tests); only the local use inside the edited test was removed. Three persona bodies edited (blockquote deletion). No new top-level symbol introduced — pure deletion/rename, so the per-symbol floor has nothing to account for.
- Completed: REQ-008 — removed the `persona_body.contains("feature-dev")` assertion plus its feeding `read_persona(...)` binding and `// The persona body carries a feature-dev attribution line.` comment; renamed the test to `feature_dev_personas_declare_speccy_model_conventions` (dropped `_and_attribution`); updated the section comment to drop the attribution-line clause. Model-convention assertions (`opus[1m]`, `gpt-5.5`, both `!contains("sonnet")`) retained. Residual REQ-001 (A6/C6) — deleted the two-line `> Ported from the feature-dev …` blockquote plus orphaned blank line from `reviewer-correctness.md`, `plan-architect.md`, `plan-explorer.md`. Ran `just reeject`; 6 ejected files updated (3 personas x 2 hosts), each dropping 3 lines mirroring source.
- Undone: nothing in scope. REPORT.md guardrail-retirement + A6/C6 records are deferred to ship time per the task body.
- Hygiene checks: `cargo test --workspace` exit 0 (renamed test ran and passed); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: CHK-010 (REQ-008) demonstrated — renamed test `feature_dev_personas_declare_speccy_model_conventions` passes; `grep "feature-dev"` of the assertion is absent; model-convention assertions remain; provenance gone from source + eject. CHK-008 (REQ-006) demonstrated/hygiene — all four gates exit 0. CHK-009 (REQ-007) demonstrated — `grep -rl "{%"` over `.claude/.agents/.codex/` returns nothing; eject diff is 6 files x -3 lines mirroring the 3 source edits. CHK-001/CHK-002 (REQ-001) judgment-only here — the full Track-1 redundant-passage audit is REPORT.md ship-time work; this task only lands the A6/C6 residual, verified by the empty `grep -rn "Ported from the .feature-dev"` across source + eject and the green `persona_snippets.rs`/`authoring_commit.rs` in the passing suite.
- Discovered issues: none.
- Procedural compliance: edited source under `resources/` and `speccy-cli/tests/`, never the ejected packs directly; ran `just reeject` to regenerate. Second reeject confirmed idempotent (0 overwritten). No shipped-skill friction encountered.
</implementer>
<review date="2026-06-13T03:15:28Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security-relevant surface in this slice. The task retires a brittle
`feature-dev` substring assertion in `speccy-cli/tests/skill_packs.rs`
and deletes three editorial provenance blockquotes from
`resources/modules/personas/{reviewer-correctness,plan-architect,plan-explorer}.md`
plus their eject mirror. Checked: no auth/authz boundary, input-validation,
injection, crypto, or secret-handling code is touched; the deleted blockquotes
carry no credentials or sensitive data and removing them exposes nothing. The
read-only `tools:` grants on the three Claude wrappers (`Read, Grep, Glob, LS,
Bash, WebFetch`) are unchanged, so the read-only review/plan agents keep their
posture. The dropped test assertion gated editorial prose, not a security
invariant, so its retirement weakens no security guardrail; the retained
frontmatter model-convention checks are not security-relevant. `grep -rn
'Ported from the .feature-dev'` over source + eject returns nothing and no
ejected file carries a `{%` marker.
</review>
<review date="2026-06-13T03:15:46Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. Reviewed the diff for T-003 against the suggested files and the full branch diff.

- `speccy-cli/tests/skill_packs.rs`: test renamed correctly to `feature_dev_personas_declare_speccy_model_conventions` (drops `_and_attribution`); attribution assertion block (comment + `read_persona` local binding + `assert!`) deleted cleanly; section comment updated to match; structural model-convention assertions retained intact; shared `read_persona` helper retained (still called at lines 188, 1650, 1690). No dead code left.
- Three persona sources (`resources/modules/personas/reviewer-correctness.md`, `plan-architect.md`, `plan-explorer.md`): two-line blockquote plus orphaned blank line deleted from each; heading/body spacing is clean post-deletion.
- `grep -rn 'Ported from the .feature-dev'` returns nothing across `resources/`, `.claude/`, `.agents/`, `.codex/` -- source and eject both clean.
- `grep -rn '{%'` over ejected packs returns nothing -- no MiniJinja markers survived eject.
- Ejected `.claude/agents/reviewer-correctness.md` confirmed mirroring the source deletion (3 lines removed, 1:1 with source).
- No new symbols introduced; no parallel helpers or reinvented utilities; `read_persona` reuse survey accurate.
- No suppressions, no `#[allow]`/`#[expect]` added; no naming drift.
</review>
<review date="2026-06-13T03:16:19Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
CHK-010 (REQ-008) genuinely demonstrated: `skill_packs.rs:1738` renamed test `feature_dev_personas_declare_speccy_model_conventions` drops the `persona_body.contains("feature-dev")` assertion plus its feeding `read_persona` binding and comment, while retaining the real structural assertions over rendered packs — `assert_eq!(claude_fm.model, "opus[1m]")`, `assert_eq!(codex_fm.model, "gpt-5.5")`, and both `!contains("sonnet")` checks. These parse frontmatter (a stable surface) and would red on a wrong model or a sonnet regression, so the test is not left vacuous by the deletion. Re-ran it in isolation: passes. The three `> Ported from the feature-dev …` blockquotes are gone from source and eject (`grep -rn 'Ported from the .feature-dev' resources/ .claude/ .agents/ .codex/` returns nothing), so the old assertion — had it survived — would now red; implementer correctly removed assertion and lines together. CHK-008 (REQ-006): full `cargo test --workspace` re-run passes with zero failures. CHK-009 (REQ-007): no `{%` marker in eject; eject diff is exactly 6 files (3 personas x 2 hosts) each dropping the 3-line blockquote, a clean 1:1 mirror of the 3 source edits; `rendered_personas_contain_no_minijinja_markup` passes. CHK-002 (REQ-001) corroborated: `persona_snippets.rs` and `authoring_commit.rs` re-run green — every asserted heading, include line, and command string in the edited persona bodies survives. CHK-001 judgment-only label is plausible (the Track-1 redundant-passage reviewer audit is a ship-time REPORT.md artifact, not deterministically demonstrable by this slice). Roll call covers all CHKs under the four covered REQs; no fabrication patterns matched — the renamed test symbol appears in the diff and the scoped per-test claim reproduces.
</review>
<review date="2026-06-13T03:16:28Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff delivers what T-003's covered requirements promise. REQ-008: the
`persona_body.contains("feature-dev")` assertion plus its feeding
`read_persona(...)` binding and comment are gone from
`speccy-cli/tests/skill_packs.rs`; the test is renamed
`feature_dev_personas_declare_speccy_model_conventions` (dropped
`_and_attribution`); the model-convention assertions (`opus[1m]`,
`gpt-5.5`, both `!contains("sonnet")`) are retained, and the shared
`read_persona` helper stays (still used at lines 188/1650/1690). The
renamed test passes (CHK-010). REQ-001 residual A6/C6: the three
`> Ported from the feature-dev …` blockquotes are deleted from
`reviewer-correctness.md`, `plan-architect.md`, `plan-explorer.md` with
clean heading/body spacing; `grep -rn 'Ported from the .feature-dev'`
over resources + .claude/.agents/.codex returns nothing (source +
eject). REQ-006: all four hygiene gates pass at this commit (test,
clippy, nightly fmt --check, deny). REQ-007: no `{%` marker survives in
any ejected pack and the eject diff is 6 files x -3 lines mirroring the
3 source deletions (CHK-009). No non-goal breached — pure
deletion/rename, no new CLI surface, no bare-pointer reduction, no edit
to archived specs. The guardrail retirement executes the authorized
2026-06-12 amendment (DEC-002), not a silent open-question resolution.
The REPORT.md records for REQ-001/REQ-008 are explicitly deferred to
ship time per the task body and are correctly out of this per-task
slice's scope.
</review>
<review date="2026-06-13T03:16:35Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Pure deletion/rename task with no control-flow surface. Verified
the test edit in `speccy-cli/tests/skill_packs.rs:1738`: the
`persona_body.contains("feature-dev")` assertion and its feeding
`read_persona(...)` binding are removed from inside the
`for persona in FEATURE_DEV_PERSONAS` loop, while the four
model-convention assertions (Claude `opus[1m]`, Codex `gpt-5.5`, both
`!contains("sonnet")`) remain and still gate a real frontmatter
invariant — the test is not rendered vacuous. The shared `read_persona`
helper stays referenced at lines 188, 1650, 1690, so no dangling-unused
compile break; the rename to `feature_dev_personas_declare_speccy_model_conventions`
is consistent at the `#[test]` definition with no stale call sites. The
three `> Ported from the feature-dev …` blockquote deletions in
`reviewer-correctness.md`, `plan-architect.md`, `plan-explorer.md` are
prose-only with no rendered-behavior change. Eject mirrors source 1:1
(6 files, -3 lines each); `just reeject` is idempotent (0 overwritten,
working tree byte-identical); `grep` for residual `feature-dev`
provenance and for `{%` markers across source + eject both return
nothing. `cargo test --workspace` is green. No off-by-one, Option/Result,
concurrency, or resource-leak surface in scope.
</review>
