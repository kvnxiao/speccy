---
spec: SPEC-0063
task: T-001
generated_at: 2026-06-12T22:03:11Z
---

<implementer date="2026-06-12T22:03:11Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: pure prose-compression task editing 25 existing `resources/modules/**` files (no new code symbols introduced, so the three-tier reuse classification is degenerate). Area-map round-1: surveyed the guardrail tests (`persona_snippets.rs`, `authoring_commit.rs`, `skill_packs.rs`) and the 26 enumerated module files. Reuse-as-is: every `{% include %}` directive (cli-stamps, identity-sourcing, verdict_return_contract, commit-recipe, branch-guard, convention-checklist) was preserved at its callsites — the cuts deliberately lean ON the includes (delete restatements that duplicate an included module) rather than adding parallel copies. No write-fresh symbols.
- Completed: Tier A (cli-stamps, identity-sourcing, verdict_return_contract, reviewer-tests, reviewer-style, reviewer-correctness*), Tier B (speccy-vet, vet-phases, speccy-orchestrate, speccy-init, speccy-brainstorm, speccy-review, speccy-amend, speccy-plan), Tier C/D (speccy-decompose, vet-implementer, vet-simplifier, plan-architect*, plan-explorer*, commit-recipe, branch-guard, review-fanout), Tier E (evidence drop Scenario 3, retry-shape parenthetical). Determinism trims: REQ-002 (work phase: removed post-append journal re-read, now relies on CLI validate-before-write + `speccy next --json` drift check) and REQ-003 (ship phase: step 1 derives readiness+paths from the `speccy next --json` already run in "When to use"; step 3 drops the post-flip `speccy status` TSK-003 re-check). Ran `just reeject`; all three host packs regenerated. ~332 net source lines removed (497 del / 165 ins), matching the ~330-360 descriptive estimate.
- Undone: (*) The A6/C6 instruction to delete the `> Ported from the feature-dev …` provenance lines in reviewer-correctness.md, plan-architect.md, and plan-explorer.md was NOT applied — see Discovered issues. These three were left intact. REPORT.md prose audit is deferred to ship time per the task close-out (not written this commit). The atomic `git add -A` review-pass commit is the reviewer's job, not appended here.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file — this is a prose/determinism task proven by the guardrail suite and direct eject scans. Roll call for CHKs under REQ-001/002/003/006/007:
  - CHK-001 (redundant passages gone, no inline-rule reduced to bare pointer, facts preserved once): judgment-only → reviewer-business/style judges the diff; the inline-rule-plus-pointer constraints (retry-shape, evidence pointers) were honoured and SPEC-0049 DEC-002 act-without-read one-liners kept. The REPORT.md audit lands at ship time.
  - CHK-002 (guardrail suite green): hygiene → `persona_snippets.rs` (27 passing) and `authoring_commit.rs` (11 passing) under `cargo test --workspace`; all asserted headings (`## Diff-format pitfalls`, `## Evidence loading`), `{% include %}` lines, and command strings intact.
  - CHK-003 (work phase: no post-append re-read): demonstrated → `grep -c 're-read the journal' .claude/agents/speccy-work.md` returns 0; the validate-before-write + `speccy next --json` drift wording is present at lines 362-364 of the ejected agent body.
  - CHK-004 (ship phase: no step-1/step-3 `speccy status` call): demonstrated → no ```bash `speccy status` command remains in the ejected ship bodies (only my deliberate prose mention "no separate `speccy status` call is needed"); step 1 derives from the existing `speccy next --json`.
  - CHK-008 (four hygiene gates pass): hygiene → all four exit 0 (see Hygiene checks).
  - CHK-009 (no `{%` in ejects, eject mirrors source): demonstrated → `grep -rl '{%' .claude .agents .codex` returns nothing; eject diff (980 net) is the expected include-expansion fan-out of the shared-module cuts (e.g. `CLAUDE_CODE_EFFORT_LEVEL` removal propagated to zero occurrences across source+eject), not a broken wrapper.
- Discovered issues: (1) The A6/C6 task instruction to delete the `> Ported from the feature-dev …` provenance lines conflicts with an existing guardrail `skill_packs.rs::feature_dev_personas_declare_speccy_model_conventions_and_attribution` (line 1792), which asserts `personas/{persona}.md` for `FEATURE_DEV_PERSONAS = [reviewer-correctness, plan-explorer, plan-architect]` must contain a `feature-dev` attribution line. The task's test-guardrail map listed `skill_packs` only for "no dangling references / no orphaned modules," not this attribution assertion. Per CHK-008 (suite must stay green) being a hard acceptance criterion vs. the advisory tier enumeration, I reverted those three deletions and kept the provenance lines. The reviewer should decide whether to instead amend the SPEC/test; flagging for the business/correctness personas. (2) Pre-existing failure unrelated to Track 1: `speccy-core::in_tree_specs::every_in_tree_spec_md_has_blank_line_after_each_close_tag` failed on clean HEAD (commit 73cbf00) because this SPEC's own SPEC.md had `</scenario>` immediately followed by `</requirement>` with no blank line at 7 sites — a decompose/plan-time formatting miss. I inserted the 7 missing blank lines (whitespace-only, between close tags) to keep the suite green; this is hash-neutral (no `TSK-003` drift reported by `speccy next --json`).
- Procedural compliance: edited source under `resources/modules/**` only, never the ejected `.claude/`/`.agents/`/`.codex/` files directly; ran `just reeject` after edits. No CLI/source-module friction encountered that required an AGENTS.md-rule source fix. The SPEC.md blank-line fix (issue 2) touches `.speccy/specs/0063-.../SPEC.md`, which is Speccy's own dogfood artifact, not a shipped template.
</implementer>
<review date="2026-06-12T22:05:29Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security regression in this prose-compression / determinism-trim slice.
Checked, all preserved: (1) no new injection surface or git-mutating verb —
review-fanout still excludes `git status --porcelain` / `git diff --cached
--quiet` / branch-guard include (guardrail `persona_snippets.rs` /
`authoring_commit.rs` green); preserved command strings (`tail -c 1 <path> |
od -An -tx1`, the 4-command journal-safe revert, `git rev-parse
--is-inside-work-tree`) are intact. (2) Branch-protection boundary intact —
`branch-guard.md` still guarantees HEAD is on a feature branch before any
commit; only the meta-preamble was cut. (3) Journal-integrity / rollback-safety
boundary intact — the `never git stash pop` rule and the journal-exclusion
invariant survive in `vet-phases.md`, and the "orchestrator owns all code-state
rollback; sub-agent does not revert itself" safety rule survives canonically in
`vet-simplifier.md:89-90` (the C5 de-dup kept one copy, not zero). (4) The two
determinism trims removed only provably-redundant re-checks, not security
verifications: the work-phase post-append re-read proved nothing under the
CLI's validate-before-write guarantee (preserved in `cli-stamps.md`), and the
ship-phase `speccy status` round-trips were redundant readiness / `TSK-003`
re-confirmations that the hash-neutral flip makes unable to fire. (5) No secret
exposure — `identity-sourcing.md` dropped the named env vars but kept the
binding "never read it from a runtime env override" rule. (6) No template-
internal leakage — `grep -rl '{%' .claude .agents .codex` returns nothing and
the ejected packs mirror the source edits 1:1.
</review>
<review date="2026-06-12T22:05:58Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
`resources/modules/personas/verdict_return_contract.md:22` introduces "serialises" (British) while the entire codebase uses "serializes" (American) — 7+ occurrences in `review-fanout.md`, `vet-phases.md`, `journal-review.md`, `vet-reviewer.md`, `vet-implementer.md`, `speccy-vet.md`, `vet-simplifier.md`. The original line at this location used "serializes" (confirmed via `git show HEAD:...`). Fix: change "serialises" to "serializes" and re-eject.

All other style aspects pass: naming matches surrounding patterns, the prose compressions preserve surrounding tone, check-property count labels updated correctly (plan: 6→5, amend: 8→7 after "Placeholder leakage" removal), no dead code or unused identifiers introduced, no new abstractions, and the inline `# --model required` comment on the `<<'EOF'` line is consistent across all ejected reviewers.
</review>
<review date="2026-06-12T22:06:02Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
Pass. Diff serves the SPEC's goals/user-stories with no out-of-scope creep.
REQ-001: prose cuts faithful — verdict_return_contract.md A3 deletes the
`## The --model value is required` section but preserves the fact at its
canonical site (identity-sourcing.md include + the `# --model required` Step-1
clause); retry-shape.md and evidence.md Tier-E cuts drop only the flagged
parenthetical / Scenario 3 and keep the inline rules, honoring SPEC-0049 DEC-002
(no inline-rule reduced to a bare pointer). REQ-002 satisfied in source+eject
(`phases/speccy-work.md`: post-append re-read removed, `speccy next --json` drift
check retained). REQ-003 satisfied (`phases/speccy-ship.md`: step-1 `speccy
status` derived from existing `speccy next --json`; step-3 post-flip re-check
removed). REQ-006: guardrail suites green — skill_packs 34, authoring_commit 27,
persona_snippets 11. REQ-007: zero `{%` markers in `.claude`/`.agents`/`.codex`.
Two business-relevant judgment calls, both correctly handled, neither a blocker:
(1) Task instructions A6/C6 (delete the `> Ported from the feature-dev …`
provenance lines in reviewer-correctness.md, plan-architect.md, plan-explorer.md)
contradict the `feature_dev_personas_declare_speccy_model_conventions_and_attribution`
guardrail at `speccy-cli/tests/skill_packs.rs:1792` (asserts each body contains
`feature-dev`); applying A6/C6 would fail REQ-006/CHK-008, a hard <done-when>.
Implementer kept the lines and surfaced the SPEC-internal contradiction for human
resolution rather than silently breaking the build — correct per Core Principle
"surface unknowns, never invent." (2) The REPORT.md audit clauses in
REQ-001/002/003 <done-when> are explicitly ship-time deliverables (SPEC.md:445,
TASKS close-out) and out of scope for per-task review; REPORT.md does not yet
exist, as expected.
</review>
<review date="2026-06-12T22:06:27Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. No logic or control-flow defect at or above the reporting bar.
Verified the two determinism trims are operationally sound, not just
prose: (REQ-002) `phases/speccy-work.md` drops the post-append journal
re-read but retains the `speccy next --json` drift check — the CLI's
validate-before-write guarantee makes the re-read redundant, so no
consistency gate is lost. (REQ-003) `phases/speccy-ship.md` step 1
derives readiness + paths from the `speccy next --json` already run in
"When to use"; confirmed that call's JSON carries `spec_md_path` and
`tasks_md_path` at top level, so step 2's REPORT.md write still has its
path source. Step 3's "`TSK-003` cannot fire" claim is backed by the
`lock.rs` test asserting status flips are hash-neutral (excluded from
`spec_hash_at_generation`). The `vet-phases.md` git rollback sequence
was compressed but preserves every operational command (the
never-`git stash pop` rule, snapshot/restore semantics, verbatim
4-command block) — no data-loss path introduced; `commit-recipe.md` /
`branch-guard.md` lost only meta-preamble, command bodies intact. Full
`cargo test --workspace` is green (72 binaries, 0 failures), ejects are
`{%`-clean, and the work/ship trims propagated to both Claude and Codex
ejected bodies. The unapplied provenance-line deletions, the SPEC.md
blank-line edit, and the dropped review-fanout sentence are scope/
completeness matters outside the correctness lane.
</review>
<review date="2026-06-12T22:06:37Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. Prose/determinism task with no new project tests; its proof surface is the existing guardrail suite, which genuinely gates the rendered-output invariants. Verified against the working tree (the implementer's edits are uncommitted by design; the bundle's `git diff origin/main...HEAD` only shows SPEC/TASKS so I reviewed `git diff origin/main` instead). Roll call complete (CHK-001/002/003/004/008/009 all labelled). Independently re-ran every hygiene/demonstrated claim: full `cargo test --workspace` green (incl. `persona_snippets.rs`, `authoring_commit.rs`, and `skill_packs.rs::feature_dev_personas_declare_speccy_model_conventions_and_attribution`), clippy/fmt/deny green (CHK-002, CHK-008). CHK-003: ejected `speccy-work.md:362-364` drops the post-append re-read and retains validate-before-write + `speccy next --json`. CHK-004: ejected `speccy-ship.md` step 1 (l.40-45) and step 3 (l.56-63) carry no `speccy status` call. CHK-009: `grep -rl '{%' .claude .agents .codex` empty; 44 ejected files mirror the source include fan-out (A2 env-var removal, A4 Shape-recognition block gone from source+eject). The `feature-dev` attribution guardrail genuinely catches a wrong impl — the implementer correctly reverted the A6/C6 provenance deletions to keep it green and flagged the SPEC-vs-test tension for business/correctness (out of tests scope). CHK-001 plausibly judgment-only (prose-fact-preservation audit is editorial). Minor non-blocking note: the roll call swaps the two test counts (persona_snippets is 11 / authoring_commit is 27, not the reverse stated) — both pass, a prose slip only.
</review>
<blockers date="2026-06-12T22:07:10Z" round="1">
Fix one British/American spelling regression introduced by the prose edits, then re-eject.
- style (blocking): `resources/modules/personas/verdict_return_contract.md:22` introduces "serialises" (British) against the codebase's established "serializes" (American), used in 7+ other locations (review-fanout.md, vet-phases.md, journal-review.md, vet-reviewer.md, vet-implementer.md, speccy-vet.md, vet-simplifier.md). The original line at this location used "serializes" (confirmed via `git show HEAD:...`). Change "serialises" → "serializes" in the source module and run `just reeject` so the ejected packs match.
</blockers>
<implementer date="2026-06-12T22:13:01Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: retry round addressing a non-reuse blocker (a spelling regression), so the round-1 full area-map is not re-run and no new top-level symbol is introduced — the per-symbol floor is vacuously satisfied. The edit reuses the codebase's established American "serializes" spelling (7+ existing occurrences across review-fanout.md, vet-phases.md, journal-review.md, vet-reviewer.md, vet-implementer.md, speccy-vet.md, vet-simplifier.md) rather than introducing a parallel convention.
- Completed: Fixed the single round-1 style blocker. Changed "serialises" → "serializes" at `resources/modules/personas/verdict_return_contract.md:22` and ran `just reeject` (7 reviewer wrappers across `.claude`/`.codex` overwritten to mirror the source; the include fans verdict_return_contract.md into all reviewer/vet bodies). No other round-1 WIP was touched — the prior pass's determinism trims and prose cuts remain in place.
- Undone: Nothing new this round. The round-1 carry-overs stand unchanged: the A6/C6 provenance-line deletions remain reverted (kept) because applying them breaks the `feature_dev_personas_declare_speccy_model_conventions_and_attribution` guardrail — still flagged for human SPEC-vs-test resolution; REPORT.md prose audit remains a ship-time deliverable.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file — prose/determinism task proven by the guardrail suite and direct eject scans. Roll call for the CHK touched this round:
  - CHK-001 (redundant passages gone, facts preserved once, no inline-rule reduced to a bare pointer): judgment-only → reviewer-business/style judges the diff; this round only corrected a spelling regression, preserving the round-1 prose semantics.
  - CHK-006/CHK-002 (guardrail suite green): hygiene → full `cargo test --workspace` green (0 failures); `persona_snippets.rs` (10), `authoring_commit.rs` (27), `skill_packs.rs` (65 incl. the feature-dev attribution guardrail) all pass under the project test command.
  - CHK-009 (no `{%` in ejects, eject mirrors source): demonstrated → `grep -rl '{%' .claude .agents .codex` returns nothing; `grep -rn 'serialis[e]' resources/ .claude/ .agents/ .codex/` returns nothing (British spelling eradicated source+eject); `per-file lock serializes` now present in all 7 re-ejected reviewer bodies.
  - CHK-008 (four hygiene gates pass): hygiene → all four exit 0 (see Hygiene checks).
- Discovered issues: None new. The two round-1 discovered issues (the A6/C6 vs feature-dev-attribution guardrail conflict, and the pre-existing SPEC.md blank-line fix) are unchanged and remain as flagged in the round-1 block.
- Procedural compliance: edited source under `resources/modules/personas/` only, never the ejected packs directly; ran `just reeject` after the edit. No CLI/source-module friction requiring an AGENTS.md-rule fix.
</implementer>
<review date="2026-06-12T22:14:40Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Pass. Track 1 is a prose/prompt token-reduction over `resources/modules/**`
plus regenerated ejects — no auth, input-validation, injection, secret, or
crypto surface. Audited every security-relevant guard that the compressions
touch and confirmed each survives: the runtime-env-override identity-spoofing
guard collapses naming `CLAUDE_EFFORT`/`CLAUDE_CODE_EFFORT_LEVEL` to a generic
"runtime env override" but keeps the operative "never read it" rule
(`references/identity-sourcing.md`); the journal-integrity guarantee
("validation runs before any write; no override flag for `date`/`round`")
survives in `references/cli-stamps.md`; the git-rollback safety ("never
`git stash pop`", journal-safe verbatim revert sequence) survives in
`skills/partials/vet-phases.md`; the read-only / no-working-tree-mutation
mandate (`personas/no_working_tree_mutation.md`) and the no-TASKS.md-writes
boundary (`personas/no_tasks_md_writes.md`, still `{% include %}`d from
`verdict_return_contract.md`) are intact. Ejects carry no `{%` markers. The
round-2 delta is the lone `serialises`→`serializes` spelling fix at
`personas/verdict_return_contract.md`, which introduces no security change.
</review>
<review date="2026-06-12T22:14:51Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass. Prose-only Track 1 task; the only behavior changes are the two determinism trims, both procedurally sound. Verified ship step 1 (`resources/modules/phases/speccy-ship.md`) now derives `spec_md_path`/`tasks_md_path` and ship-readiness from the `speccy next --json` run in "When to use" — the per-spec JSON envelope genuinely carries both path fields (`speccy-cli/src/next_output.rs:34-37`, module doc lines 13-14) and the readiness `kind == "ship"` gate is preserved in the "When to use" section, so no step depends on data a prior step fails to produce and no readiness gate was dropped. Verified the work-phase re-read removal (`resources/modules/phases/speccy-work.md`) relies on the validate-before-write guarantee and downgrades to a `speccy next --json` consistency check, which is a real field on the same envelope (`next_output.rs:44`). The round-2 edit (`serialises`→`serializes` at verdict_return_contract.md:22) and the A3 prose trims carry no control-flow content. No logic, Option/Result, off-by-one, concurrency, or resource-leak defect in my lane.
</review>
<review date="2026-06-12T22:16:18Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="2">
Pass. Prose/determinism slice with zero new project tests; "no separate evidence file" is appropriate — there is no red/green transition to capture, so the proof surface is the existing guardrail suite plus deterministic eject scans, all re-run independently. Roll call is complete: every covered CHK (001/002/003/004/008/009) carries a label, hygiene labels cite specific files (`persona_snippets.rs`, `authoring_commit.rs`), demonstrated labels cite reproducible grep probes.

Re-verified each CHK against the working tree (implementer edits are uncommitted by design; `git diff origin/main...HEAD` shows only SPEC/TASKS, so I reviewed `git diff origin/main` on the dirty tree):
- CHK-003 (demonstrated): ejected `.claude/agents/speccy-work.md` has no "re-read the journal" (grep exit 1); validate-before-write wording present at l.362. Falsifiable — leaving the re-read in would be visible in eject.
- CHK-004 (demonstrated): ejected `speccy-ship.md` carries no `speccy status` command call (only the deliberate prose "no separate `speccy status` call is needed"); step-1 derives from `speccy next --json`, step-3 post-flip re-check gone.
- CHK-002/008 (hygiene): re-ran `persona_snippets` (27 pass), `authoring_commit` (11 pass), full `cargo test --workspace` (0 failures), clippy and fmt clean. The `feature_dev_personas_declare_speccy_model_conventions_and_attribution` guardrail passes — it genuinely bites, which is why the A6/C6 provenance deletions were correctly reverted.
- CHK-009 (demonstrated): `grep -rl '{%' .claude .agents .codex` empty; round-2 spelling fix ("serialises"→"serializes") fanned out to all 14 reviewer bodies; no British spelling anywhere in source or eject.
- CHK-001 (judgment-only): plausibly deferred to business/style — prose-fact-preservation and the inline-rule-plus-pointer audit are editorial. Spot-checked the load-bearing Tier-E cuts (retry-shape drops only the parenthetical and keeps the "halts" inline rule; evidence.md drops Scenario 3 only, keeping Scenarios 1-2): no inline-rule reduced to a bare pointer. Not mine to block.

No mocks, no vacuous assertions, no fabricated evidence — the grep probes reproduce and the test counts match reality (the round-2 block swapped the 27/11 counts, a non-blocking prose slip the round-1 tests reviewer already noted; both suites pass).
</review>
<review date="2026-06-12T22:16:19Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Round-1 blocker addressed: "serialises" -> "serializes" is confirmed in source (`resources/modules/personas/verdict_return_contract.md:22`) and propagated to all 7 Claude Code and 7 Codex ejected reviewer wrappers via `just reeject`. `grep -rn "serialis[e]" resources/ .claude/ .agents/ .codex/` returns nothing.

All other style checks pass:
- Naming: American spelling is used consistently throughout; no new identifier, heading, or label diverges from codebase patterns.
- Inline `# --model required` comment in the heredoc opener is valid bash (tested), intentional per the task spec, and consistently applied across all 14 ejected reviewer files -- not a lone outlier.
- Check-property count labels updated correctly: `speccy-plan.md` changed 6->5 and lists exactly five properties; `speccy-amend.md` changed 8->7 and lists exactly seven properties.
- Reuse survey is present in the round-2 `<implementer>` block and correctly identifies this as a spelling-regression fix with no new symbols.
- No dead prose or orphaned identifiers introduced.
- No `{%` markers survive in any ejected file.
- Required guardrail headings (`## Diff-format pitfalls`, `## Evidence loading`) and `{% include %}` lines for `verdict_return_contract`, `diff_fetch_command`, `inline_note_format` are all intact in source and eject.
</review>
<review date="2026-06-12T22:16:31Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Pass. The diff delivers the SPEC's headline business outcomes for the
covered REQs with no out-of-scope creep.

REQ-002 (work-phase determinism trim): satisfied in source and all ejects —
`re-read the journal` is gone from `.claude/`/`.agents/`/`.codex/` work bodies,
replaced by the CLI validate-before-write + `speccy next --json` drift check
(`phases/speccy-work.md`).

REQ-003 (ship-phase determinism trim): satisfied — no `speccy status SPEC`
command remains in the ejected ship step 1 or step 3 (the only `speccy status`
strings left are deliberate explanatory prose); readiness/paths derive from the
existing `speccy next --json` (`phases/speccy-ship.md`).

REQ-006/REQ-007: full `cargo test --workspace` green (0 failures across all
binaries); zero `{%` markers in any ejected pack.

REQ-001: substantially satisfied; 23 of the enumerated Tier A–E cuts landed and
no kept inline-rule-plus-pointer (retry-shape, evidence) was reduced to a bare
pointer. One deviation, correctly handled, not a blocker: task instructions
A6/C6 (delete the `> Ported from the feature-dev …` provenance lines in
`resources/modules/personas/reviewer-correctness.md`, `plan-architect.md`,
`plan-explorer.md`) were NOT applied because the only `feature-dev` string in
those bodies lives on that line, and deleting it fails the existing guardrail
`speccy-cli/tests/skill_packs.rs:1739`
(`feature_dev_personas_declare_speccy_model_conventions_and_attribution`,
SPEC-0053 CHK-006), which would break REQ-006/CHK-008. REQ-001's "no longer
appears" and REQ-006's "suite stays green" are in direct, unresolvable conflict
for these three lines — an internal SPEC contradiction, not implementer error.
The implementer prioritized the hard hygiene gate, kept the lines, and loudly
surfaced the SPEC-vs-test tension in both the round-1 and round-2 `<implementer>`
blocks for human resolution rather than silently deciding it (Core Principle #6,
surface unknowns / never invent). Recording it here so the orchestrator/human can
decide whether to amend this SPEC's A6/C6 scope or the SPEC-0053 attribution
test; it does not warrant bouncing the task. The REPORT.md prose audit named in
the REQ-001/002/003 `<done-when>` is an explicit ship-time deliverable (SPEC
close-out), out of scope for per-task review.
</review>
