---
spec: SPEC-0063
task: T-002
generated_at: 2026-06-12T22:29:46Z
---

<implementer date="2026-06-12T22:29:46Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: bounded to the six enumerated passages and their callsites. Reuse-as-is: mirrored existing include-only exemplar `references/reconcile-summary.md` (inline one-liner + `{{ speccy_references_path }}` pointer) as the shape for `retry-shape-summary.md`; reused the established indented-`{% include %}` pattern from `phases/speccy-work.md` line 142 (reuse-survey-implementer) for indented callsites. Extend: none. Write-fresh: six new include-only modules — `references/retry-shape-summary.md`, `personas/review-role-tail.md`, `personas/vet-input-resolution.md`, `personas/vet-no-rollback.md`, `skills/partials/inline-fanout-rationale.md`, `references/spec-self-review-core.md`. Searched `resources/modules/**` for any pre-existing factoring of these six passages; none existed (T2-A–F are by definition the un-factored copies), so each module is genuinely new.
- Completed: All six T2 sub-tasks. T2-A: retry-shape-summary module replaces 3 verbatim copies (orchestrate, phases/work, skills/work). T2-B: review-role-tail module included in `## Role` of all 7 reviewers; each persona-specific lead-in kept inline, shared tail unified to the imperative "Append one..." wording. T2-C: vet-input-resolution module included in vet-reviewer + vet-implementer; each persona's specific nuance kept inline (reviewer's "re-derive drift" sentence; implementer's "leave your own changes uncommitted too"). T2-D: vet-no-rollback module included in vet-implementer + vet-simplifier. T2-E: inline-fanout-rationale partial included in orchestrate + vet + review with one-line site-specific lead-ins; orchestrate's "Why fan-outs run inline" heading and all 5 back-references preserved. T2-F: spec-self-review-core module (carrying the REQ-005 supersession comment) included in plan + amend; amend keeps its 2 deltas (Changelog row presence, Surgical-diff shape) inline after the include; brainstorm self-review left independent; all 3 stale DEC-001/OQ-b comments removed. SPEC.md already carried DEC-001 (verified in context bundle) — no SPEC.md edit needed. Ran `just reeject` (17 overwritten, 19 unchanged).
- Undone: Nothing in T-002 scope. REPORT.md SSOT-factoring audit is explicitly a ship-time obligation (speccy-ship), not this task.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: no separate evidence file — this task's CHKs are verified by the workspace test suite plus direct grep audits of source and ejected packs. Roll call for CHKs under REQ-004/005/006/007:
  - CHK-005 (six passages each one module, every callsite includes it, no inline copy at non-canonical callsites): demonstrated → grep audit confirms each consolidated string appears only in its module (`grep -rln "A task is in retry shape iff" resources/modules/` → only retry-shape-summary.md; role-tail / no-rollback / fanout / vet-input likewise show zero non-canonical inline copies). `persona_snippets.rs` (6 passed) gates the reviewer-persona includes.
  - CHK-006 (just reeject diff is include-expansion; one-liner-plus-pointer expands to full sentence not bare pointer; no `{%` marker): demonstrated → `grep -rn "{%" .claude .agents .codex` returns 0 matches; retry-shape-summary expands to the full inline sentence + pointer at all 3 callsites (verified in ejected `.claude/skills/speccy-work/SKILL.md`).
  - CHK-007 (SPEC carries DEC-001 superseding SPEC-0034 DEC-001; module carries supersession comment; brainstorm retains independent self-review; no DEC-001/OQ-b comment in plan/amend/brainstorm): demonstrated → context bundle shows DEC-001 in SPEC.md; ejected plan carries the supersession comment (1 match); brainstorm retains "Self-review caught the following..." (1 match); `grep -rn "OQ-b\|independent copy"` across the three ejected skills returns nothing.
  - CHK-008 (four hygiene gates pass): demonstrated → all four exit 0 (see Hygiene checks).
  - CHK-009 (no `{%` in ejected packs; eject diff mirrors source edits as include-expansion): demonstrated → 0 `{%` matches; `git diff --stat` on the packs touches only the files whose source modules changed.
- Discovered issues: T2-C and T2-D's source passages are NOT byte-identical across their callsites (the task body and DEC-001 describe them as "identical"/"verbatim"). Post-Track-1 (and pre-existing — T-001 did not touch them) the vet-reviewer vs vet-implementer `git diff <base-ref>` rationale and fallback blocks are paraphrases with persona-specific second halves, and the two no-rollback notes are paraphrases. Forcing one module means the rendered text at these callsites is unified rather than byte-equivalent to each prior copy. I resolved this per the task's explicit consolidation directive ("Factor the consumer-side shared block ... include it from both") and REQ-004's `<behavior>` byte-equivalence clause which binds only *verbatim* passages: picked one canonical wording, kept each persona's genuinely-specific sentence inline, lost no operational fact. Similarly, T2-F: plan had "five check properties", amend "seven" (5 shared + 2 deltas) — not the "6 common" the SPEC prose states. I factored the 5 verbatim-shared bullets + preamble into the module with a count-neutral "The check properties:" header, keeping amend's 2 deltas inline. Flagging for reviewer judgment on whether the unified (non-byte-equivalent) wording at these callsites is acceptable under the SSOT intent.
- Procedural compliance: All edits were to `resources/modules/**` source; `just reeject` regenerated the ejected packs; no ejected file was hand-edited. No shipped-body real-identifier violations introduced (modules use `{{ speccy_references_path }}` / `{{ speccy_personas_path }}` and generic placeholders). No source-module friction encountered that required an AGENTS.md-style fix.
</implementer>
<review date="2026-06-12T22:31:35Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security exposure in this Track-2 SSOT-factoring slice. The change is
markdown prompt/persona text only — no production code, auth boundary,
input-handling, crypto, or secret-storage surface. Verified the new
include-only modules carry no leaked credentials, tokens, or real
identifiers (`resources/modules/personas/review-role-tail.md`,
`vet-input-resolution.md`, `vet-no-rollback.md`,
`references/retry-shape-summary.md`, `spec-self-review-core.md`,
`skills/partials/inline-fanout-rationale.md`). The safety guards in the
factored text are preserved verbatim: `vet-no-rollback.md` keeps the
"do not git stash/reset/restore/clean" destructive-op guard, and
`vet-input-resolution.md` correctly preserves the `git diff <base-ref>`
(no `...HEAD`) guidance that prevents reviewers from silently missing
uncommitted WIP. `{% include %}` callsites use fixed literal paths with
no user-controlled interpolation — no template-injection vector. Ejected
packs (`.claude/`, `.agents/`, `.codex/`) contain zero stray `{%`
markers and the role-tail expands to text equivalent to the prior inline
copy (`.claude/agents/reviewer-security.md`).
</review>
<review date="2026-06-12T22:33:19Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
Pass — no logic or expansion defect found. Verified the six new include-only modules resolve and expand faithfully: `{% include %}` directives at all callsites (retry-shape ×3, review-role-tail ×7, vet-input-resolution ×2, vet-no-rollback ×2, inline-fanout ×3, spec-self-review-core ×2) render with zero `{%` markers leaking into `.claude`/`.agents`/`.codex`. Indented includes in `skills/speccy-plan.md:64`, `skills/speccy-amend.md:37`, and `phases/speccy-work.md:68` re-indent the full multi-line body correctly — no broken markdown nesting. Consolidated text is byte-faithful to the pre-Track-2 copies: plan and amend self-review property bodies + preamble were verbatim-identical at HEAD and match `references/spec-self-review-core.md` (only trailing-newline differs, matching the include-only exemplar); amend's two deltas survive inline after the include; retry-shape module is byte-identical to the committed orchestrate inline copy and expands to the full inline sentence plus pointer (not a bare pointer); the Role tail reads naturally after each of the 7 persona-specific lead-ins. `{{ speccy_references_path }}` resolves at all retry-shape callsites. `cargo test --workspace` is green (persona_snippets and authoring_commit included). The implementer's flagged items (the "five"→"" header word, the T2-C/T2-D paraphrase unification, the 5-vs-6 property-count discrepancy) are SSOT-faithfulness/scope matters owned by business and architecture, not control-flow defects, so they are out of the correctness lane.
</review>
<review date="2026-06-12T22:33:53Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
Pass. Track 2 delivers the SSOT-factoring outcome the SPEC committed to: each of the six enumerated passages now lives in exactly one new include-only module under `resources/modules/**`, every former callsite pulls it via `{% include %}`, and no non-canonical inline copy remains (grep audit: retry-shape, fanout, no-rollback, review-role-tail, vet-input-resolution each appear only in their canonical module). REQ-004 done-when met — retry-shape-summary expands to the full inline sentence + resolved pointer (`.claude/speccy-references/retry-shape.md`), not a bare pointer (act-without-read preserved, honoring the non-goal against bare-pointer reduction). REQ-005 met: SPEC.md carries DEC-001 superseding SPEC-0034 DEC-001, `spec-self-review-core.md` line 1 carries the supersession comment, brainstorm retains its independent four-property self-review, and no DEC-001/OQ-b comments remain in plan/amend/brainstorm source. REQ-006: `cargo test --workspace` green (0 failed; authoring_commit.rs 27 passed, persona_snippets.rs 11 passed). REQ-007: zero `{%` markers in `.claude`/`.agents`/`.codex`; orchestrate "Why fan-outs run inline" heading + all back-references (lines 82/216/242) still resolve after the include. Non-goals respected — no new CLI/git-mutating surface, archived specs untouched.

One deviation, transparently flagged in the implementer block and judged acceptable: the SPEC prose described T2-C/T2-D/T2-F passages as "identical"/"verbatim"/"6 common", but the actual pre-Track-2 copies were paraphrases (vet input-resolution, no-rollback, the 4 paraphrased reviewer Role tails) and plan/amend shared 5 properties, not 6. The implementer unified each to one canonical wording, kept each persona's genuinely-specific sentence inline, and used a count-neutral "The check properties:" header with amend's two deltas (Changelog-row, surgical-diff) retained inline. Rendered output at those callsites is therefore unified rather than byte-equivalent, but REQ-004's byte-equivalence `<behavior>` clause binds only *verbatim* passages, and the binding done-when constraint ("no operational fact changed") holds — every operational fact survives. This executes the SPEC's SSOT intent rather than drifting from it.
</review>
<review date="2026-06-12T22:34:16Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Pass. All six new include-only modules follow existing kebab-case naming and `{% include "modules/..." %}` conventions. Reuse survey is present in the implementer block. No inline copies of consolidated passages remain at non-canonical callsites (grep confirms each passage exists only in its module). No `{%` markers survive into any ejected pack. The DEC-001/OQ-b stale comments are absent from plan, amend, and brainstorm. `authoring_commit.rs` ordering assertion ("Self-review pass" precedes commit-recipe include) holds in the current speccy-plan.md. Back-references to "Why fan-outs run inline" in speccy-orchestrate.md still resolve. The vet-reviewer.md persona-specific sentence after the vet-input-resolution include is coherent in context. Nothing blocking.
</review>
<review date="2026-06-12T22:34:45Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. T-002 is a prose/module-factoring task: no new project test, CHK-005–009 are structural/audit CHKs verified by reproducible grep audits + the existing render-based guardrail suite. Evidence is the inline CHK-by-CHK roll call (all five labeled `demonstrated`, each citing a concrete grep command and/or named test); no fabrication pattern applies since no runner-output transcript is claimed. Independently reproduced every audit: CHK-005 each of the six consolidated strings appears only in its canonical module (retry-shape-summary, review-role-tail in 7 reviewers, vet-input-resolution in vet-reviewer+vet-implementer, vet-no-rollback in vet-implementer+vet-simplifier, inline-fanout-rationale in orchestrate/vet/review, spec-self-review-core in plan+amend), every callsite pulls it via `{% include %}`, no non-canonical inline copy remains; CHK-006/009 `grep -rn "{%" .claude .agents .codex` = 0 and on-disk ejected packs carry the expanded text (eject is fresh, not stale) with retry-shape expanding to the full inline sentence not a bare pointer; CHK-007 brainstorm retains its independent self-review, no stale DEC-001/OQ-b comment in plan/amend/brainstorm, supersession comment present in `spec-self-review-core.md`; CHK-008 `persona_snippets.rs` (27), `authoring_commit.rs` (11), `skill_packs.rs` (34) all pass against the current tree. Falsification holds: `render_host_pack` uses `UndefinedBehavior::Strict` (render.rs:173) with a loader that returns `TemplateNotFound` for a missing include (render.rs:187-191), so a broken/missing include in any of the six modules errors render workspace-wide and fails the suite; `plan_stages_spec_md_alone_with_create_spec_title_after_self_review` (authoring_commit.rs:595) gates T2-F's self-review-heading-before-commit-recipe ordering, and `rendered_personas_contain_no_minijinja_markup` (persona_snippets.rs:195) gates T2-B's role-tail expansion. The implementer's flagged byte-equivalence concern for the paraphrased T2-C/D/F passages is a semantic/SSOT judgment (REQ-004 byte-equivalence binds only verbatim passages) deferred to business/correctness review, not a tests-persona blocker; I confirmed no operational fact was dropped (vet-no-rollback's stash/reset/restore/clean set matches both prior source copies; the T2-C producer/consumer boundary leaves vet-phases Phase-0 in place).
</review>
