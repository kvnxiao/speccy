---
spec: SPEC-0066
task: T-003
generated_at: 2026-06-21T09:49:16Z
---

<implementer date="2026-06-21T09:49:16Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the vet flow. Reuse-as-is — the journal-safe
  revert sequence and the "Protect the journal from rollback" rationale block
  in vet-phases.md (referenced, not duplicated); the `{% if host ==
  "claude-code" %}…{% else %}…{% endif %}` sub-agent dispatch idiom (copied
  from the Phase 2 simplifier dispatch); the `vet-provenance` persona body and
  Claude/Codex wrappers built by sibling T-002 (the new phase only dispatches
  the already-existing sub-agent). Extend — none. Write-fresh — the new "Phase
  3 — provenance-cleanup pass" prose itself (no existing phase fit a
  prose-only, append-no-block, behaviour-preserving pass), modelled
  structurally on the Phase 2 simplifier apply step. No new top-level code
  symbols introduced (resource-prose task).
- Completed: Added "Phase 3 — provenance-cleanup pass" to
  resources/modules/skills/partials/vet-phases.md immediately after the Phase 2
  simplifier pass and before the gate phase; renumbered the gate phase Phase 3
  → Phase 4 and updated every internal "Phase N" cross-reference (gate
  exit-path list, gate body example). The new phase dispatches the
  `vet-provenance` sub-agent once over the bundle's `diff_command`, reuses the
  simplifier snapshot/apply/journal-safe-rollback sequence, and records its
  outcome only through a clause in the Phase 4 `<gate>` summary plus the
  returned verdict — no `speccy journal append --block <new-type>`, no new
  VetBlockKind, no new field on the orchestrator return block. Updated
  resources/modules/skills/speccy-vet.md narrative (autonomy sentence,
  single-writer rule, "what this skill writes") to name the provenance pass and
  state it appends no block. Ran `just reeject` (1 file overwritten: both
  ejected vet SKILL.md mirrors).
- Undone: Nothing in scope deferred. DEC-002's first-class provenance block
  remains a BACKLOG candidate by design, not this task's work.
- Hygiene checks: `cargo test --workspace` exit 0 (all suites green);
  `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit
  0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0
  (advisories/bans/licenses/sources ok).
- Evidence: REQ-004 CHK roll-call — CHK-007 (judgment-only): the ejected vet
  packs (.claude/skills/speccy-vet/SKILL.md, .agents/skills/speccy-vet/SKILL.md)
  now contain a provenance-cleanup phase that spawns the `vet-provenance`
  sub-agent over the cumulative diff before the gate and reference no new
  `--block` type; the CLI's closed journal/VET block set is unchanged (no Rust
  touched). CHK-008 (judgment-only): the provenance phase runs once over the
  cumulative diff at the pre-ship polish stage and records via the gate summary
  plus verdict; speccy-review.md gained no provenance persona (grep: 0
  occurrences). Both CHKs are persona-review judgments over prose, not
  scriptable assertions — verified by direct read of the rendered packs.
  `speccy verify` reports 0 errors / 0 warnings.
- Discovered issues: SPEC-0066/SPEC.md (committed at spec-creation, not part of
  this task's diff) violated the blank-line-after-close-tag convention at four
  `</scenario></requirement>` boundaries, failing the
  `in_tree_specs` hygiene test and blocking the gate. See Procedural
  compliance.
- Procedural compliance: Followed speccy-work first-attempt recipe. Two friction
  repairs outside the resources/** scope, both mechanical and contract-
  preserving: (1) inserted the four missing blank lines in SPEC-0066/SPEC.md to
  clear the pre-existing blank-line-after-close-tag hygiene failure — whitespace
  only, no Requirement/scenario content changed; (2) the whitespace edit
  desynced the recorded spec hash (spec-hash-mismatch warning), so ran `speccy
  lock SPEC-0066` to re-record it — zero-semantic change, so a re-lock rather
  than a `/speccy-amend` round is the proportionate fix. After both, `speccy
  verify` is clean and the only remaining drift is the expected
  state_in_progress_orphaned, which clears on the in-review flip. No shipped
  resources/** instruction was found stale, so no source-module fix + reeject
  was needed there.
</implementer>
<review date="2026-06-21T09:51:15Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security defects in this slice. Resource-prose only; no CLI/Rust, auth, crypto, or injection surface touched. The provenance sub-agent's write authority is correctly bounded to comment/doc prose (persona body section 4) and rollback authority is retained by the orchestrator -- the spawn prompt's "If any hygiene step fails, do NOT attempt to revert yourself ... the caller owns the rollback" instruction prevents the sub-agent from clobbering working-tree state, and the `git stash` snapshot/journal-safe-revert idiom mirrors the already-shipped Phase 2 simplifier pattern with the "Protect the journal from rollback" guard referenced (vet-phases.md:308-340). No new secret-handling or data-exposure surface: the diff handed to the sub-agent via `speccy context --json` `diff_command` is the same cumulative-diff exposure the simplifier already has, run locally. Verdict vocabulary (clean/applied/blocking) is consistent between the phase wiring and the persona return contract. Per-task review fan-out gained no provenance persona (speccy-review.md: 0 occurrences), so no review-coverage surface changed. Out-of-scope for security and left to the implementation reviewer: the snapshot is taken after the sub-agent has already applied its edits (vet-phases.md:354-360), so the rollback-target stash contains the very edits a `blocking` revert would aim to undo -- a rollback-correctness concern, not a security vulnerability.
</review>
<review date="2026-06-21T09:51:33Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="blocking" round="1">
Snapshot/rollback ordering is inverted in the new Phase 3 provenance pass, so the "revert on hygiene failure" path cannot actually revert. resources/modules/skills/partials/vet-phases.md:298-351.

The simplifier (Phase 2) is correct because it separates a report-only scan from the apply: it snapshots the working tree (lines 232-235) BEFORE spawning the apply sub-agent (lines 242-263), so `stash@{0}` holds the pre-apply state and the journal-safe revert sequence's `git checkout 'stash@{0}'` (line 68) restores files to the pre-simplifier state — undoing the edits.

Phase 3 inverts this. The `vet-provenance` sub-agent is apply-mode (persona body lines 14-15): step 1 (lines 298-320) spawns it and it applies its rewrites immediately. Only afterward, in step 3a (lines 327-340), does the session run `git stash push --include-untracked` + `git stash apply`. That snapshot therefore captures the POST-rewrite working tree — the prose at lines 337-339 even states the restored tree is "with the sub-agent's prose rewrites." There is no pre-pass snapshot anywhere: Phase 2's stash was already resolved, and Phase 3 takes only this single post-edit snapshot.

Consequence on the reachable `blocking`/hygiene-failure branch (step 3b second bullet, lines 348-351): the journal-safe revert sequence restores files from `stash@{0}`, which already contains the rewrites, so the edits are NOT undone. The gate summary will report `"provenance: reverted on hygiene failure"` (line 355) while the hygiene-failing edits remain in the working tree and get committed by the orchestrator at ship. The stated intent (a behaviour-preserving pass that reverts on failure) is unachievable as written.

Critical: wrong result on a reachable path (any hygiene failure after a rewrite) plus a dishonest gate summary. The "mirroring the simplifier apply step" claim (line 330) is the root error — the simplifier snapshots before the mutating sub-agent runs; an apply-mode provenance sub-agent must likewise be snapshotted before its dispatch, not after.
</review>
<review date="2026-06-21T09:52:00Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="pass" round="1">
Pass. T-003 is a resource-prose wiring task; both covered CHKs are honestly labelled judgment-only and that label is correct, not a dodge. CHK-008's spec body states verbatim "This is a persona-review judgment, not a scriptable assertion", and CHK-007 gates skill-body prose wiring plus a no-Rust no-new-block contract that no deterministic project test exercises. The evidence roll-call names both CHKs (neither omitted) and labels neither `demonstrated`/`hygiene` against a non-existent test, so no fabrication pattern applies.

Verified the behaviour directly against the working-tree diff (changes uncommitted per the dirty-tree contract; `git diff origin/main...HEAD` is empty only because the WIP is uncommitted, `git diff HEAD` carries it): `resources/modules/skills/partials/vet-phases.md` now has "Phase 3 — provenance-cleanup pass" slotted after the Phase 2 simplifier and before the renumbered Phase 4 gate; it dispatches `vet-provenance` once over the cumulative `diff_command` (not per task), appends no journal block, adds no `VetBlockKind`, and records via the Phase 4 `<gate>` summary plus returned verdict. Gate phase renumber is consistent (exit-path list updated to "Phase 2 and Phase 3 completion"). `speccy-vet.md` narrative updated to name the pass and state it appends no block; both ejected mirrors (.claude + .agents SKILL.md) contain the new phase, so reeject is in sync.

No-new-block / fan-out-unchanged contract holds at the source of truth, not just in prose: `speccy-cli/src` is untouched in the diff and `VetBlock` remains the five existing kinds (DriftReview/HolisticFix/SimplifierScan/SimplifierApply/Gate) with no Provenance variant; `resources/modules/skills/speccy-review.md` has zero diff and zero `provenance` occurrences, so the per-task review fan-out gained no provenance persona. `vet-provenance` subagent wrappers exist (sibling T-002, completed), so the new dispatch is not dangling.
</review>
<review date="2026-06-21T09:52:22Z" model="claude-opus-4-8[1m]/xhigh" persona="architecture" verdict="blocking" round="1">
The new Phase 3 inverts the simplifier's snapshot/rollback ordering, which silently breaks the journal-safe rollback invariant the task explicitly required reusing. In Phase 2 (resources/modules/skills/partials/vet-phases.md:225-284) the snapshot is taken BEFORE the apply sub-agent runs: `git stash push --include-untracked` captures the pre-edit state and `git stash apply` restores it for the agent to work on, so the blocking-path `git checkout 'stash@{0}'` (the journal-safe revert sequence, lines 64-70) restores the pre-edit snapshot and genuinely undoes the agent's edits.

Phase 3 (lines 298-356) reverses this: step 1 spawns `vet-provenance`, which applies its prose rewrites FIRST; only afterward does step 3a snapshot (lines 332-335). So `git stash push` captures the POST-edit tree and `git stash apply` restores the POST-edit tree — the prose rewrites are inside the snapshot. The text admits this at line 338-339 ("the second restores the working tree (with the sub-agent's prose rewrites)"). Consequently the `blocking` revert path at lines 348-351 ("run the journal-safe revert sequence ... The verdict is unaffected") cannot undo anything: `git checkout 'stash@{0}'` restores the very edited state it claims to discard. There is no pre-edit snapshot anywhere in Phase 3 (Phase 2 drops its stash on both the `applied` and `clean` exits, so none survives into Phase 3), so a hygiene-failed provenance pass leaves broken prose rewrites in the tree that ship.

Expected (per task body: "reuses the same journal-safe snapshot/rollback sequence the simplifier phase uses" and DEC-003: "The orchestrator owns the rollback snapshot, as it does for the simplifier"): snapshot pre-edit, then dispatch the apply agent, then keep-or-revert against the pre-edit snapshot — the exact Phase 2 shape. Observed: snapshot is taken post-edit, making the `blocking` revert a no-op. The line-338 "so a keep is a no-op" framing reveals the inversion was deliberate but only reasoned through the keep path, not the revert path. Reorder Phase 3 to snapshot before spawning the sub-agent (mirroring Phase 2 steps 3a→3b→3c), or the no-rollback contract the persona body promises (vet-no-rollback.md: "the caller owns all of those") is unfulfillable on the blocking path.

Non-goal adherence is otherwise clean: no Rust touched (no new VetBlockKind / schema change), no new journal block, the orchestrator return block at speccy-vet.md:128 gains no new attribute, and the per-task review fan-out (speccy-review.md) gains no provenance persona. Verdict vocabulary (clean/applied/blocking) matches the T-002 persona body. The single defect is the rollback ordering.
</review>
<review date="2026-06-21T09:53:03Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
Stale cross-reference: `vet-phases.md` line 37 still reads "Phase 3 `<gate>`" after the gate was renumbered Phase 3 → Phase 4.

The implementer states: "renumbered the gate phase Phase 3 → Phase 4 and updated every internal 'Phase N' cross-reference (gate exit-path list, gate body example)." The working-tree diff shows the section heading and the gate-body exit-path list were updated correctly, but the Phase 0 bootstrap prose at line 37 was missed:

```
`<drift-review>` the vet-reviewer appends, or the Phase 3 `<gate>` on
a Phase 0 early-exit
```

This should read "Phase 4 `<gate>`". The same stale reference propagates into the ejected `.claude/skills/speccy-vet/SKILL.md` (line 161) and `.agents/skills/speccy-vet/SKILL.md` mirrors, since they are rendered from the same source.

All other checked items pass:
- The `vet-phases.md` Phase 3 provenance section and Phase 4 gate section are correctly structured and numbered.
- The Phase 2 simplifier "go to Phase 3" reference (line 221) is now correct: after the simplifier, the flow goes to Phase 3 (provenance), then Phase 4 (gate).
- `speccy-vet.md` narrative updates are consistent and scoped correctly.
- `vet-provenance.md` persona body uses `{% include %}` for the convention checklist; no inline copy.
- The `persona_snippets.rs` test is non-vacuous and gates the delegation property.
- Description fields are angle-bracket-free and ≤ 1024 chars.
- The subagent wrapper carries "Use when" and correctly omits "Do NOT trigger" (subagent, not user-routed skill).
- No provenance meta-annotations introduced; no vacuous tests; no `#[allow]` suppressions.
- Reuse survey is present and honest.
</review>
<review date="2026-06-21T09:53:11Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="blocking" round="1">
Stale phase cross-reference contradicts the renumbering the task required and
the implementer claimed. REQ-004 wires a new Phase 3 (provenance-cleanup) and
renumbers the gate phase to Phase 4 "so it remains the last phase". The phase
headers, the exit-path list, and the gate body example were renumbered
correctly, but one internal cross-reference was missed:
`resources/modules/skills/partials/vet-phases.md:37` still reads "or the Phase
3 `<gate>` on a Phase 0 early-exit" — the gate is now Phase 4, and "Phase 3" is
now the provenance-cleanup pass. The rendered skill is therefore internally
contradictory about which phase appends the gate. `just reeject` faithfully
propagated the defect into both agent-facing mirrors that reload on every vet
run: `.claude/skills/speccy-vet/SKILL.md:161` and
`.agents/skills/speccy-vet/SKILL.md:161`.

The implementer's `<implementer>` block asserts they "updated every internal
'Phase N' cross-reference (gate exit-path list, gate body example)"; that claim
is inaccurate — this reference was not updated.

Expected: every "Phase N `<gate>`" reference points to the renumbered Phase 4.
Observed: line 37 (source) / line 161 (both ejected mirrors) still says "Phase
3 `<gate>`". Fix at the source module, then `just reeject`.

Substantively the rest of REQ-004's done-when is met and no non-goal is
breached: the dedicated provenance-cleanup phase dispatches the
`vet-provenance` sub-agent once over the cumulative `diff_command` before the
gate; it records via the Phase 4 `<gate>` summary clause plus the returned
verdict; no `speccy journal append --block <new-type>`, no new VetBlockKind
(the CLI's accepted vet-block set in `speccy-cli/src/main.rs:286-290` is
unchanged — drift-review/holistic-fix/simplifier-scan/simplifier-apply/gate),
and no new orchestrator return-block field. The per-task review skill gained no
provenance persona (0 occurrences in speccy-review.md). The verdict vocabulary
(clean|applied|blocking) matches the T-002 persona body. The carved-out
whitespace-only blank-line repair to SPEC.md is genuinely whitespace-only
(git diff --ignore-blank-lines is empty — no Requirement/goal/non-goal text
changed) and the accompanying spec-hash re-record is its benign consequence;
both belong in T-003's scope. Open questions are "None" and nothing was
silently resolved; the changelog shows a single initial entry the diff matches.
</review>
<blockers date="2026-06-21T09:54:21Z" round="1">
Two defects in the new Phase 3 provenance pass: a real rollback-ordering bug (must fix) and a stale phase cross-reference (must fix). Substance of REQ-004 is otherwise met and no non-goal is breached; the SPEC.md whitespace repair is confirmed in-scope and zero-semantic.
- correctness + architecture (blocking) — snapshot/rollback ordering inverted (`resources/modules/skills/partials/vet-phases.md:298-356`). Phase 3 spawns the apply-mode `vet-provenance` sub-agent FIRST, then snapshots in step 3a, so `git stash push --include-untracked` captures the POST-rewrite tree. The blocking/hygiene-failure revert path (`git checkout 'stash@{0}'`) then restores the already-edited state — a no-op — while the gate summary falsely reports "provenance: reverted on hygiene failure" and the broken prose ships. Reorder to mirror Phase 2 exactly: snapshot the working tree BEFORE dispatching the sub-agent, then keep-or-revert against that pre-edit snapshot. Note Phase 2 drops its stash on both `applied` and `clean` exits, so Phase 3 must take its own pre-edit snapshot.
- business + style (blocking) — stale phase cross-reference. `resources/modules/skills/partials/vet-phases.md:37` still reads "the Phase 3 `<gate>` on a Phase 0 early-exit"; the gate was renumbered to Phase 4 (Phase 3 is now the provenance pass), so this must read "Phase 4 `<gate>`". The defect propagated to both ejected mirrors (`.claude/skills/speccy-vet/SKILL.md:161`, `.agents/skills/speccy-vet/SKILL.md:161`). Fix at the source module, then run `just reeject`.
</blockers>
<implementer date="2026-06-21T09:57:21Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: No new top-level symbols (resource-prose retry; reuse-related
  blocker raised, so re-surveyed the vet flow). Reuse-as-is — Phase 2's
  snapshot-before-dispatch shape (vet-phases.md steps 3a→3b→3c) is now mirrored
  exactly by the reordered Phase 3; the journal-safe revert sequence and "Protect
  the journal from rollback" rationale block (referenced, not duplicated). Extend
  — none. Write-fresh — none; this round only reorders existing Phase 3 prose and
  fixes one cross-reference token.
- Completed: Addressed both round-1 blockers. (1) Rollback ordering: reordered
  Phase 3 to snapshot the pre-pass working tree BEFORE dispatching the apply-mode
  vet-provenance sub-agent (new step 1 = snapshot, step 2 = spawn, step 3 =
  keep-or-revert), mirroring Phase 2 exactly. The stash now holds the pre-rewrite
  state, so the blocking/hygiene-failure journal-safe revert genuinely undoes the
  rewrites. Folded the clean verdict into the snapshot-drop branch since the
  snapshot is now taken unconditionally before the spawn, and added a note that
  Phase 2 drops its stash on both applied and clean exits so Phase 3 must take its
  own. Removed the stale "so a keep is a no-op" framing that only reasoned through
  the keep path. (2) Stale cross-reference: vet-phases.md line 37 "Phase 3 <gate>"
  → "Phase 4 <gate>". Ran just reeject (1 file overwritten; both ejected vet
  SKILL.md mirrors now carry both fixes).
- Undone: Nothing in scope deferred.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace
  --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all
  --check exit 0; cargo deny check exit 0 (advisories/bans/licenses/sources ok).
- Evidence: REQ-004 CHK roll-call — CHK-007 (judgment-only): both ejected vet
  packs still spawn vet-provenance over the cumulative diff before the gate and
  reference no new --block type; CLI/Rust untouched this round (git status shows
  no speccy-cli changes), so the closed journal/VET block set is unchanged.
  CHK-008 (judgment-only): the provenance phase still runs once over the
  cumulative diff at the pre-ship polish stage and records via the gate summary
  plus verdict; the rollback path is now genuinely effective (pre-edit snapshot),
  so the gate summary is honest on the blocking branch. Both verified by direct
  read of vet-phases.md and the ejected mirrors; no scriptable assertion exists
  for prose wiring. No stale "Phase 3 <gate>" remains in any shipped file (grep:
  only the T-003 journal's quoted blocker text matches).
- Discovered issues: None this round.
- Procedural compliance: Followed speccy-work retry recipe — amended the existing
  WIP in place (no git restore/clean/checkout against the dirty tree, no rewrite
  from scratch), fixed both blockers at the source module, re-ejected. No shipped
  resources/** instruction found stale.
</implementer>
<review date="2026-06-21T09:58:44Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="2">
No security concerns. Prose-only wiring of the pre-ship provenance phase
into the vet flow (resources/modules/skills/partials/vet-phases.md,
resources/modules/skills/speccy-vet.md, plus ejected mirrors). The spawned
vet-provenance sub-agent prompt scopes the agent to prose-only edits and
the runtime-artifact carve-out, echoes no secrets/credentials/env vars, and
discloses nothing sensitive. The git stash snapshot/rollback
(`git stash push --include-untracked` → `git stash apply`) stays local and
is dropped or restored in place — no secret reaches logs, telemetry, or an
external surface. The `<gate>` summary records only outcome counts
("provenance: 2 comments rewritten"), not file contents. No new endpoint,
auth boundary, crypto choice, injection sink, or deserialization path is
introduced. Round-2 changes (snapshot-before-dispatch reorder, Phase 3→4
cross-reference fix) introduce no security regression over the round-1 pass.
</review>
<review date="2026-06-21T09:59:24Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Round-1 blocker (snapshot-after-apply made the Phase 3 revert a no-op) is fixed. Phase 3 in `resources/modules/skills/partials/vet-phases.md` now orders step 1 = snapshot (`git stash push --include-untracked` + `git stash apply` at line 305, leaving `stash@{0}` = pre-rewrite state), step 2 = spawn the apply-mode `vet-provenance` sub-agent (line 321), step 3 = keep-or-revert (line 345). Traced both paths: keep (`applied`/`clean`) drops the stash and retains the rewrites; revert (`blocking`/error) runs the journal-safe sequence, whose `git checkout 'stash@{0}'` restores the pre-rewrite snapshot — the rewrites are genuinely undone. The `stash@{0}` reference is unambiguous: Phase 1 (lines 167, 175) and Phase 2 (lines 272, 278) drop their stash on every exit, and Phase 2's `clean` path takes no snapshot, so no prior stash survives into Phase 3; the lines 300-303 note documents this and instructs Phase 3 to take its own. Stale "Phase 3 <gate>" cross-reference at line 37 corrected to "Phase 4 <gate>"; gate phase renumbered to Phase 4 and remains last. Both ejected mirrors (`.claude/skills/speccy-vet/SKILL.md`, `.agents/skills/speccy-vet/SKILL.md`) carry the reordered Phase 3 and no stale reference. No new correctness defects.
</review>
<review date="2026-06-21T09:59:34Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Pass. Cross-reference verified: `resources/modules/skills/partials/vet-phases.md:37` now reads "Phase 4 `<gate>`" on disk. Both ejected mirrors (`.claude/skills/speccy-vet/SKILL.md:161` and `.agents/skills/speccy-vet/SKILL.md:161`) read "Phase 4 `<gate>`". Grep over all three files plus `resources/` finds no residual "Phase 3 `<gate>`" token. Phase-transition prose is internally consistent: Phase 2's exit says "go to Phase 3", Phase 3's body references "Phase 4 `<gate>`", and Phase 4's heading and exit-path list include "Phase 3" completion — the numbering is coherent end-to-end. No provenance annotations, no concrete Speccy IDs, and no vacuous tests introduced by the round-2 edits. Reuse survey is present and the named `vet-provenance` symbols exist at the referenced paths.
</review>
<review date="2026-06-21T09:59:50Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="pass" round="2">
Pass. T-003 is a resource-prose wiring task; both covered CHKs carry the `judgment-only` label and that label remains correct, not a dodge. CHK-008's SPEC body states verbatim "This is a persona-review judgment, not a scriptable assertion"; CHK-007 gates skill-body prose wiring plus a no-Rust/no-new-block contract that no deterministic project test exercises (substring-matching curated skill prose is forbidden by AGENTS.md, so the prose-wiring half is genuinely non-scriptable). Per the verdict contract a `judgment-only` CHK is not mine to block on — I confirm both labels are plausible and defer them to reviewer-business / reviewer-style, which both reviewed this slice.

Evidence roll-call is honest: both CHK-007 and CHK-008 appear (neither omitted) and neither is mislabelled `demonstrated`/`hygiene` against a non-existent test. The hygiene-checks line (`cargo test`/`clippy`/`fmt`/`deny`, all exit 0) is correctly attributed to the workspace gate, not misrepresented as gating a CHK property. No fabrication pattern matched.

Round-2 changes verified against the working-tree diff (uncommitted per the dirty-tree contract): the round-1 blockers were raised by correctness/architecture/business/style, not tests, and neither round-2 fix alters the testability posture — both are pure prose edits. (1) `resources/modules/skills/partials/vet-phases.md:305-356` now snapshots the working tree (step 1) BEFORE spawning the apply-mode `vet-provenance` sub-agent (step 2), then keeps-or-reverts (step 3) against that pre-edit snapshot — mirroring Phase 2's 3a→3b→3c shape exactly, so the blocking-path revert genuinely undoes the rewrites. (2) the stale "Phase 3 `<gate>`" cross-reference at line 37 now reads "Phase 4 `<gate>`"; grep confirms no stale "Phase 3 <gate>" remains in any shipped file. Both ejected mirrors (`.claude` + `.agents` SKILL.md) carry the reordered Phase 3 and the cross-ref fix, so reeject is in sync.

No project test files are in the T-003 working-tree slice (resource prose + spec/tasks metadata + ejected mirrors only); `persona_snippets.rs` is in the committed T-002 diff, not this slice. `speccy-cli/` is untouched this round and `VetBlock` remains the five existing kinds with no `Provenance` variant, so the no-new-block contract holds at the source of truth. `speccy-review.md` has zero `provenance` occurrences, so the per-task review fan-out gained no provenance persona.
</review>
<review date="2026-06-21T10:00:11Z" model="claude-opus-4-8[1m]/xhigh" persona="architecture" verdict="pass" round="2">
Round-1 blocker resolved: the inverted snapshot/rollback ordering is fixed. Phase 3 (vet-phases.md:305-356) now runs step 1 snapshot pre-edit → step 2 dispatch the apply-mode vet-provenance sub-agent → step 3 keep-or-revert against the pre-edit snapshot, mirroring Phase 2's 3a→3b→3c shape (vet-phases.md:225-284). The stash now holds the pre-rewrite state, so the blocking/hygiene-failure path's journal-safe revert genuinely undoes the rewrites. DEC-003's orchestrator-owns-rollback contract and the vet-no-rollback.md invariant (delegated by the persona body at vet-provenance.md:100) hold on the blocking path. The clean-verdict folds into the snapshot-drop branch correctly since the snapshot is now unconditional before the spawn.

Non-goals confirmed intact: no new VetBlockKind — the CLI block set in speccy-cli/src/main.rs:286-290 is unchanged (drift-review/holistic-fix/simplifier-scan/simplifier-apply/gate) and the only speccy-cli diff is a test addition; no new journal block — Phase 3 records solely via the Phase 4 <gate> summary clause and the returned verdict; no new orchestrator return-block field (vet-phases.md:295-296 states this explicitly); per-task review fan-out untouched — speccy-review.md has zero diff and no provenance persona. Reejection genuine: both ejected mirrors (.claude/ and .agents/ speccy-vet SKILL.md) carry the reordered Phase 3 and renumbered Phase 4 verbatim. The "Phase 4 <gate>" cross-reference at line 37 is fixed.

Non-blocking observation: the shared "Protect the journal from rollback" preamble (vet-phases.md:61-62, mirror line 186) still enumerates "Every rollback in Phases 1 and 2 runs these four commands verbatim" — Phase 3 now also relies on that sequence (vet-phases.md:353-354) but is not added to the enumeration. The reuse itself is correct (Phase 3 points to and runs the canonical sequence), so the invariant holds; the enumeration is stale descriptive prose, not a broken reference. Worth tightening to "Phases 1, 2, and 3" but does not gate the architectural contract.
</review>
<review date="2026-06-21T10:00:13Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="2">
Both round-1 blockers resolved; REQ-004 fully met, no non-goal breached. (1) Phase 3 snapshot/rollback ordering now snapshots BEFORE dispatching the apply-mode vet-provenance sub-agent — step 1 snapshot, step 2 spawn, step 3 keep-or-revert (`resources/modules/skills/partials/vet-phases.md:305-356`), mirroring Phase 2; the stash holds the pre-rewrite state so the hygiene-failure revert genuinely undoes the rewrites, and the note that Phase 2 drops its stash on both exits is present (lines 301-303). (2) The stale "Phase 3 <gate>" cross-reference now reads "Phase 4 <gate>" at the source (`vet-phases.md:37`) and in both ejected mirrors (`.claude/skills/speccy-vet/SKILL.md:161`, `.agents/skills/speccy-vet/SKILL.md:161`); no stale "Phase 3 <gate>" remains in any shipped file. All three REQ-004 done-when bullets verified: the phase dispatches once over the cumulative diff before the Phase 4 gate; records via the gate summary clause plus verdict with no `--block <new-type>` and no new VetBlockKind (speccy-cli/src untouched, CLI block set unchanged); per-task review skill (`resources/modules/skills/speccy-review.md`) gained no provenance persona. speccy-vet.md narrative updated only as far as needed to name the new pass. Ships as resources/** plus reejection per DEC-002 — no CLI command, no journal block type, no schema change.
</review>
