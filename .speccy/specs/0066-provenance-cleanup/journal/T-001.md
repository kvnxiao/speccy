---
spec: SPEC-0066
task: T-001
generated_at: 2026-06-21T09:19:02Z
---

<implementer date="2026-06-21T09:19:02Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: This task is a pure prose edit to one reference file plus mechanical reejection — no new top-level code symbols introduced, so the per-symbol floor is vacuously met. Area-map (round 1): the convention SSOT is `resources/modules/references/convention-checklist.md`; its two consumers were confirmed by grep to be `resources/modules/phases/speccy-work.md:179` and `resources/modules/personas/reviewer-style.md:66`, both via `{% include "modules/references/convention-checklist.md" %}`. Reuse-as-is: the existing single bullet's opening clause, carve-out wording, and journal-traceability sentence were preserved and extended in place rather than rewritten. Extend: the bullet itself — promoted from one inline list of examples to a four-shape nested sub-list. Write-fresh: none; no new file, symbol, or test (the scenarios are persona-review judgment + existing resource-parity/ID-ban lints, which already exist).
- Completed: Broadened the "No provenance or doc-pointer meta-annotation" bullet to name four leaked shapes as an explicit sub-list — Speccy-id citation, descriptive prose pointing at a planning artifact (the no-`// per`-framing class, flagged as the most common leak), numbered project-rule citation, and doc-path citation — each with concrete negative examples drawn from the eight leaked forms (`// every failure mode the spec defines`, `// later specs populate this`, `// cardinal rule #4`, `(docs/implementation)`). Preserved the runtime-artifact carve-out (`SPEC.md` / `.speccy/…` path = data the code reads or writes, not provenance) and kept all ids as generic placeholders. Ran `just reeject` (2 overwritten: reviewer-style + speccy-work across Claude Code and Codex; 35 unchanged).
- Undone: Nothing within T-001's scope is left undone. Siblings T-002 (vet cleanup pass + wrappers) and T-003 remain pending by design — this is a single-task primitive.
- Hygiene checks: cargo clippy --workspace --all-targets --all-features -- -D warnings → exit 0. cargo +nightly fmt --all --check → exit 0. cargo deny check → exit 0. cargo test --workspace → exit 1, attributable solely to a PRE-EXISTING failure in `every_in_tree_spec_md_has_blank_line_after_each_close_tag` over `.speccy/specs/0066-provenance-cleanup/SPEC.md` (4 `</scenario>` close tags not followed by a blank line). Verified via `git stash` that this failure is present at HEAD independent of this diff — it is a plan-time SPEC.md authoring defect outside T-001's scope, not introduced by this change. Every other suite (incl. resource_prose_hygiene's 5 tests and all resource/ejected-parity tests) passes. The targeted lint `cargo test -p speccy-cli --test resource_prose_hygiene` → all 5 pass.
- Evidence: CHK-001 (resource-to-ejected parity + include-wiring): demonstrated — grep confirms all three new reference-class markers ("Descriptive prose pointing at a planning artifact", "cardinal rule #4", "docs/implementation") and the carve-out clause appear in both ejected consumers across both hosts (`.claude/agents/{reviewer-style,speccy-work}.md`, `.codex/agents/{reviewer-style,speccy-work}.toml`) after `just reeject`; the include-wiring is the existing `{% include %}` directives, exercised by reejection producing changed output in exactly those two consumers. CHK-002 (reviewer reads bullet against the eight leaked forms, carve-out still permits operated-on paths): judgment-only — explicitly a persona-review judgment, not a scriptable assertion per the scenario text; the bullet now names each of the leaked classes with a matching negative example and the carve-out sentence is retained verbatim-in-intent, leaving the final judgment to the style reviewer.
- Discovered issues: `.speccy/specs/0066-provenance-cleanup/SPEC.md` violates the blank-line-after-close-tag convention at lines 150/197/244/292 (`</scenario>` immediately followed by `</requirement>` with no blank line). This is a pre-existing plan-time authoring defect, present at HEAD, outside T-001's diff. I did NOT fix it here: editing SPEC.md is a cross-cutting planning-artifact change owned by /speccy-amend, and touching it mid-loop risks staling the committed spec hash. Recommend a follow-up amend (or a whitespace-only SPEC.md correction) to clear the in_tree_specs gate before ship.
- Procedural compliance: Followed speccy-work first-attempt recipe — clean-tree gate passed (empty porcelain), memory ledger read (one entry, not area-relevant), bounded reuse survey scoped to the SSOT + two consumers, edited only `resources/` then ran `just reeject` (never hand-edited ejected files). No source-module friction encountered, so no resource fix was needed. Consistency `next_action=reconcile` observed mid-task was the expected `state_in_progress_orphaned` (in-progress + dirty WIP), which the in-review flip resolves — not a SPEC-hash drift.
</implementer>
<review date="2026-06-21T09:20:29Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface in scope. The change is a pure prose broadening of one convention-checklist bullet (`resources/modules/references/convention-checklist.md`) plus mechanical reejection to the four host-pack mirrors; no executable code, CLI behavior, dependency, or schema change is touched. Reviewed for auth/authz boundaries, input-validation/injection vectors, secret handling, sensitive-data exposure in logs/errors, crypto parameters, and TOCTOU races — none apply to a documentation convention edit. Source-to-ejected parity confirmed byte-identical in the changed region across `.claude/agents/{reviewer-style,speccy-work}.md`; no secret, credential, or sensitive token introduced in the new examples (the `// per REQ-NNN`, `(docs/implementation)` illustrations are inert placeholders).
</review>
<review date="2026-06-21T09:20:59Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defect. T-001 is a pure-prose reference edit to the "No provenance or doc-pointer meta-annotation" bullet in `resources/modules/references/convention-checklist.md` plus mechanical reejection; there is no logic, control flow, Option/Result handling, off-by-one, concurrency, or resource management in scope for the correctness lane. The one mechanical correctness concern — whether reejection regenerated consistent state, with no stale or partial mirror — holds: all four ejected consumers (`.claude/agents/{reviewer-style,speccy-work}.md`, `.codex/agents/{reviewer-style,speccy-work}.toml`) carry every new reference-class marker (descriptive-prose, "cardinal rule #4", "docs/implementation") and the retained carve-out clause, and `git diff origin/main --stat` shows the change set is scoped to exactly the SSOT plus those four mirrors (no unrelated or omitted file). The bullet appears in no other ejected location, so no `.agents/` copy is left stale.
</review>
<review date="2026-06-21T09:21:32Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The diff touches exactly the files the task names.

`resources/modules/references/convention-checklist.md`: the "No provenance or doc-pointer meta-annotation" bullet is broadened to a four-shape nested sub-list (Speccy-id citation, descriptive prose pointing at a planning artifact, numbered project-rule citation, doc-path citation), each with concrete negative examples using generic placeholder IDs (`REQ-NNN`, `SPEC-NNNN T-NNN`). No concrete Speccy IDs appear in the file. The runtime-artifact carve-out (`SPEC.md`, `.speccy/…` path = data) is retained verbatim-in-intent. Wording is idiomatic with the surrounding bullet style.

Reuse survey: confirmed. The implementer named `resources/modules/phases/speccy-work.md:179` and `resources/modules/personas/reviewer-style.md:66` as the include callsites. Both confirmed by grep — the `{% include "modules/references/convention-checklist.md" %}` directives exist at those exact lines. No reinvention: the existing bullet was extended in-place, no parallel copy introduced.

Ejected parity: all four ejected consumers received the same broadened bullet — `.claude/agents/reviewer-style.md`, `.claude/agents/speccy-work.md`, `.codex/agents/reviewer-style.toml`, `.codex/agents/speccy-work.toml`. The working-tree diffs confirm the inline include content was updated correctly in each.

No dead code, no suppression annotations, no naming drift, no provenance citations introduced by the change itself.
</review>
<review date="2026-06-21T09:22:11Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff satisfies REQ-001's observable contract. The broadened provenance bullet in `resources/modules/references/convention-checklist.md:20-44` names all three required reference classes beyond `// per X`, each with a concrete negative example: descriptive prose pointing at a planning artifact ("every failure mode the spec defines", "later specs populate this", "a later spec can ask for X"); numbered project-rule citation ("(Core principle 2)", "cardinal rule #4"); doc-path citation ("see docs/ARCHITECTURE.md", "(docs/implementation)"). The runtime-artifact carve-out is retained at lines 42-44 with both `SPEC.md` and `.speccy/…` examples intact (substance preserved, wording sharpened to "data the code reads or writes, not provenance"). CHK-002 judgment holds: every one of the eight leaked forms named in the SPEC summary is covered as a negative example, so none can be rationalised as acceptable, and the carve-out still clearly permits operated-on paths.

CHK-001 parity verified: both consumers `{% include %}` the SSOT (`resources/modules/phases/speccy-work.md:179`, `resources/modules/personas/reviewer-style.md:66`), and all four ejected mirrors (`.claude/agents/{reviewer-style,speccy-work}.md`, `.codex/agents/{reviewer-style,speccy-work}.toml`) carry the broadened bullet after `just reeject`; no stale "is data, not meta-annotation" copies remain in any host pack.

Goals/non-goals respected: change is `resources/**` edits plus reejection only — no CLI command, no journal block type, no schema change; siblings T-002/T-003 untouched. No scope sneaked in and no open question silently resolved (SPEC declares none open). The pre-existing SPEC.md blank-line authoring defect the implementer flagged is plan-time and correctly scoped out of T-001.
</review>
<review date="2026-06-21T09:22:44Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Scenario-to-test coverage verified for the sole covered REQ-001. CHK-001 (resource-to-ejected parity + include-wiring) is genuinely demonstrated, not vacuous: the existing `speccy-cli/tests/init_three_way.rs` parity suite (chk019/chk020/chk021, 11 passed) gates byte-identical resource→ejected render, and I independently confirmed a fresh `init --host {claude-code,codex} --force` render is byte-identical to all four working-tree consumers (`.claude/agents/{reviewer-style,speccy-work}.md`, `.codex/agents/{reviewer-style,speccy-work}.toml`) — the "edited source, forgot to reeject" failure mode would surface as an `overwritten` line and fail chk019. `resource_prose_hygiene.rs` (5 passed) gates that the broadened bullet's example ids stay placeholders under the references carve-out. CHK-002 is correctly labelled judgment-only: the scenario text itself states it is a persona-review judgment, not a scriptable assertion; the bullet names all three new classes (descriptive-prose, numbered-project-rule, doc-path) with concrete negative examples and retains the runtime-artifact carve-out verbatim-in-intent (`convention-checklist.md:42-44`). Roll call complete — both CHKs under REQ-001 appear with valid labels and the demonstrated CHK cites the specific gating tests. The `cargo test --workspace` non-zero exit is attributable solely to a pre-existing SPEC.md blank-line lint in `speccy-core/tests/in_tree_specs.rs` over the plan-time-authored `.speccy/specs/0066-provenance-cleanup/SPEC.md` (committed at base, untouched by T-001's uncommitted resources edit) — orthogonal to this task's behaviour and out of scope. No mock-under-test, snapshot, or empty-body smells apply to this prose-only slice.
</review>
