---
spec: SPEC-0066
task: T-002
generated_at: 2026-06-21T09:28:12Z
---

<implementer date="2026-06-21T09:28:12Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Modeled on the existing `vet-simplifier` triple (body + two wrappers). Reuse-as-is: `{% include %}` of `modules/references/convention-checklist.md` (T-001's broadened provenance definition) — no inline copy; `{% include %}` of `modules/personas/vet-input-resolution.md` (bundle/diff_command resolution), `modules/personas/vet-no-rollback.md` (caller-owns-rollback), `modules/references/identity-sourcing.md` (identity), all reused verbatim as vet-simplifier does. Extend: none. Write-fresh: the persona body's five-section provenance-specific logic (scan / carve-out / intent-preserving rewrite / prose-only scope / thin-verdict contract) — searched personas/ for an existing provenance pass, none existed. The two wrappers are fresh files cloned from the vet-simplifier wrapper frontmatter shape (no `tools:`, medium pin, subagent description).
- Completed: Added `resources/modules/personas/vet-provenance.md` (single-concern provenance cleanup, apply-mode, include of the convention reference, carve-out respect, intent-preserving rewrite, prose-only scope, thin `<verdict role="provenance">` return appending no journal block per DEC-002). Added Claude wrapper `resources/agents/.claude/agents/vet-provenance.md.tmpl` (no read-only tools grant, `model: opus[1m]`/`effort: medium`) and Codex wrapper `resources/agents/.codex/agents/vet-provenance.toml.tmpl` (medium analog). Ran `just reeject`; subagent ejected to `.claude/agents/vet-provenance.md` and `.codex/agents/vet-provenance.toml`.
- Undone: Orchestration wiring (speccy-vet dispatching this pass, the `<gate>` summary field that records its outcome) is REQ-004 / T-003, deliberately out of scope here.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: REQ-002 — CHK-003 (include resolves, no inline definition copy): demonstrated; body's only definition source is the `{% include %}` of convention-checklist.md, verified by the resource_prose_hygiene scan over modules/. CHK-004 (single-concern, intent-preserving rewrite, carve-out, prose-only): judgment-only (persona-review judgment per the scenario text); body sections 1-4 instruct exactly this. REQ-003 — CHK-005 (Claude omits tools, both declare pin, description passes angle-bracket + "Use when" checks): demonstrated by `wrapper_descriptions_are_upload_safe_and_well_routed` (green) plus the absence of any `tools:` line in the Claude wrapper. CHK-006 (resource-to-ejected parity in both .claude/agents and .codex/agents): demonstrated by the dogfood byte-identity test `dogfood_outputs_match_committed_tree` (green after reeject).
- Discovered issues: First reeject+test run failed the provenance lint because the rewrite example used a concrete `REQ-007`; fixed to the generic `REQ-NNN` placeholder (the lint carves out only modules/references/, and personas/ is not exempt). The persona thus eats its own dogfood.
- Procedural compliance: Followed speccy-work first-attempt recipe. No shipped-skill friction encountered; no source-module fixes needed. Descriptions use em-dashes, consistent with the surrounding vet-simplifier wrapper prose and within the hygiene lint's allowances.
</implementer>
<review date="2026-06-21T09:30:05Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security-relevant defects in this slice. The change is resources/** prose
only (new `vet-provenance` subagent body + two host wrappers); no product
code, no auth/crypto/injection/secret/logging surface.

Security-relevant surface reviewed — the write-capable tool grant for an
apply-mode pre-ship subagent — is the minimum the apply-mode design requires
and matches the accepted `vet-simplifier` precedent. The Claude wrapper's
omission of a read-only `tools:` grant (`resources/agents/.claude/agents/vet-provenance.md.tmpl`)
is intentional and adequately fenced by the body's guardrails: prose-only /
behaviour-preserving / bounded-to-the-diff scope (sections 3-4), the
carve-out "bias toward under-editing" rule (section 2), an explicit
no-fabricated-rationale rule (section 3), the `vet-no-rollback.md` ban on
destructive git, and an explicit ban on writing TASKS.md / journal / VET.md
(`resources/modules/personas/vet-provenance.md:104-105`). Audit-trail
integrity is preserved — the pass appends no journal block and records via
the orchestrator's gate summary (DEC-002). The Codex `gpt-5.5`/low pin
mirrors the existing simplifier convention. No new blast radius vs. the
accepted baseline.
</review>
<review date="2026-06-21T09:30:54Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All five created files reviewed: `resources/modules/personas/vet-provenance.md`, `resources/agents/.claude/agents/vet-provenance.md.tmpl`, `resources/agents/.codex/agents/vet-provenance.toml.tmpl`, and their ejected mirrors.

**Reuse survey**: Present and honest. All named includes confirmed on-disk (`vet-input-resolution.md`, `vet-no-rollback.md`, `identity-sourcing.md`, `convention-checklist.md`). No reinvention found — the body reuses the exact include chain vet-simplifier does.

**No inline definition copy**: The provenance definition is sourced solely via `{% include "modules/references/convention-checklist.md" %}` at body:27; the definition text does not appear inline again.

**Single-concern remit**: Body opens with an explicit single-concern declaration ("Your sole review dimension is provenance leakage … no competing remit"); numbered phases 1–4 stay within that remit.

**Wrapper conformance**: Claude wrapper carries `model: opus[1m]` / `effort: medium` / no `tools:` field — matching the apply-mode requirement and the simplifier's frontmatter shape. Codex wrapper carries `model_reasoning_effort = "low"` — mirrors vet-simplifier.toml.tmpl's Codex analog exactly. Description is 592 chars (under 1024), angle-bracket-free, carries a "Use when …" clause, no "Do NOT trigger" clause (correct for a subagent wrapper).

**No provenance annotations in the body**: All placeholder ids are generic (`SPEC-NNNN`, `REQ-NNN`). The one `AGENTS.md` reference at body:83 ("suite per AGENTS.md") is instructional data — directing the agent to read the file for hygiene commands — not a bare id cited as the reason a line exists.

**Ejected parity**: Both `.claude/agents/vet-provenance.md` and `.codex/agents/vet-provenance.toml` present and correctly inline the resolved body.

No dead code, no naming drift, no suppression annotations, no parallel helpers reinventing existing ones.
</review>
<review date="2026-06-21T09:31:38Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
Diff satisfies REQ-002 and REQ-003 against their done-when contracts. REQ-002: `resources/modules/personas/vet-provenance.md` is single-concern (Role section names provenance as the sole remit; line 29 narrows the included checklist to "only the provenance bullet"), pulls the broadened definition via `{% include "modules/references/convention-checklist.md" %}` with no inline duplication (CHK-003, gated green by `module_prose_has_no_internal_artifact_id_provenance`), and instructs intent-preserving rewrite (sec 3), carve-out respect (sec 2), and prose-only/behaviour-preserving scope (sec 4) — CHK-004 judgment reads as intended. REQ-003: both wrappers include the body; Claude `.md.tmpl` carries no read-only `tools:` grant and pins `model: opus[1m]`/`effort: medium`, Codex `.toml.tmpl` mirrors vet-simplifier's `gpt-5.5`/`low` analog; descriptions are angle-bracket-free with a "Use when …" clause and no "Do NOT trigger" (CHK-005, green via wrapper-description lint); ejected mirrors present in both `.claude/agents/` and `.codex/agents/` with body inlined (CHK-006, green via `dogfood_outputs_match_committed_tree`). Non-goals honoured: no CLI command, no new journal block (DEC-002 — body line 83 "append no journal block", thin `<verdict role="provenance">` only), no schema change; T-003 orchestration wiring correctly deferred. Open questions: none outstanding, none silently resolved. Out-of-scope note for the orchestrator/vet, not a blocker for this task: `every_in_tree_spec_md_has_blank_line_after_each_close_tag` fails on `.speccy/specs/0066-provenance-cleanup/SPEC.md:150,197,244,292` — a pre-existing SPEC-authoring defect from commit 00fa9d8, untouched by this task's diff.
</review>
<review date="2026-06-21T09:31:49Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects in scope. This task ships markdown (persona body + two wrappers); the in-scope "logic" surface is the body's verdict-return contract and control-flow guards, which I checked for internal consistency.

- Verdict enum `clean|applied|blocking` (resources/modules/personas/vet-provenance.md:90-98) partitions cleanly: no leak/no edit → clean; ≥1 rewrite + green hygiene → applied; rewrite-impossible OR hygiene-failed → blocking. The hygiene-failed-after-edit case correctly routes to blocking, not applied, so a broken edit cannot surface as success.
- Carve-out branch (lines 44-51) defaults ambiguous mentions to "data, leave it" — biases toward under-editing, the safe direction; no inverted condition.
- Prose-only guards (lines 72-78): "would require changing code to stay green → skip" and "leak in a file the diff does not modify → skip" are correct early-exits; no path bypasses them into logic edits.
- Blind-delete edge (lines 60-66) covers both the citation-only line (delete) and the unrecoverable-intent case (drop, never fabricate) — no fall-through.
- All four `{% include %}` targets resolve (vet-input-resolution, convention-checklist, vet-no-rollback, identity-sourcing) and eject inlined with `cmd_prefix` substituted; dogfood parity, wrapper-description hygiene, and resource-prose hygiene tests pass.

Out of scope / not a T-002 defect: `every_in_tree_spec_md_has_blank_line_after_each_close_tag` fails on SPEC.md:150/197/244/292 (missing blank line after `</scenario>`), but that file was committed at the prior "create spec" commit (00fa9d8) and is the spec-authoring deliverable, not this task's persona/wrapper files.
</review>
<review date="2026-06-21T09:32:13Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="blocking" round="1">
CHK-003 is mislabeled `demonstrated` against a test that does not gate the scenario. The roll call cites "the resource_prose_hygiene scan over modules/" as proof that the convention reference is included and the definition text is not duplicated inline. That scan (`module_prose_has_no_internal_artifact_id_provenance` in `speccy-cli/tests/resource_prose_hygiene.rs:172`) only matches banned artifact-ID tokens; it never asserts the `{% include "modules/references/convention-checklist.md" %}` is present, and never asserts the checklist definition text is absent inline. A `vet-provenance.md` that inlined the full convention definition with no include would pass that scan byte-identically, so the cited test cannot catch the wrong implementation CHK-003 describes (share-via-include vs copy). This is a false coverage claim of exactly the kind this review gates: a `demonstrated` label backed by a test that does not exercise the behavior.

The implementation itself is correct — `resources/modules/personas/vet-provenance.md:27` sources the definition purely via the include, and grep confirms zero inline copy of the checklist text (no "Convention-drift checklist" / "No false complexity" / "Suppressions carry a justification" strings in the body). So the fix is to the evidence, not the code. Two acceptable remedies: (a) relabel CHK-003 `judgment-only` — the include-present + no-inline-copy property is verifiable by a reviewer reading the body, and the scenario is structurally analogous to other persona-body reads; or (b) add a delegation test mirroring the existing precedent `commit_recipe_delegates_trailer_via_include` (`speccy-cli/tests/authoring_commit.rs:183`) that asserts the `{% include "modules/references/convention-checklist.md" %}` line is present in `vet-provenance.md` and that the checklist definition prose does not appear inline, then keep the `demonstrated` label citing that new test.

The other three CHKs check out. CHK-005 (description hygiene) is genuinely gated by `wrapper_descriptions_are_upload_safe_and_well_routed` (verified green with the new wrappers in scope); the medium model/effort pin is additionally gated by the pin_shape suite (`claude_pinned_model_matches_alias_with_1m_suffix`, `opus_pinned_effort_is_valid`, `codex_pinned_model_equals_gpt55`, `codex_pinned_reasoning_effort_is_valid`, all green); the "no read-only tools:" property is a frontmatter absence verifiable by inspection. CHK-006 (resource-to-ejected parity) is genuinely demonstrated by `dogfood_outputs_match_committed_tree` (verified green; the new wrappers render and byte-match the committed `.claude/agents/vet-provenance.md` and `.codex/agents/vet-provenance.toml`). CHK-004 is correctly `judgment-only` per its own scenario text. No CHK is silently omitted from the roll call.
</review>
<blockers date="2026-06-21T09:33:06Z" round="1">
Fix CHK-003 evidence, not the code: the implementation is correct (the convention definition is sourced purely via `{% include "modules/references/convention-checklist.md" %}` with zero inline copy), but the roll-call label is unsupported by its cited test.
- tests (blocking): CHK-003 is labeled `demonstrated` citing the `resource_prose_hygiene` scan (`module_prose_has_no_internal_artifact_id_provenance`), which only matches banned artifact-ID tokens — it never asserts the include is present nor that the checklist definition text is absent inline, so a fully-inlined copy would pass it byte-identically. Apply one of: (a) relabel CHK-003 `judgment-only` (the include-present + no-inline-copy property is reviewer-verifiable by reading the body), or (b) add a delegation test mirroring `commit_recipe_delegates_trailer_via_include` (`speccy-cli/tests/authoring_commit.rs:183`) asserting the `{% include %}` line is present in `vet-provenance.md` and the checklist definition prose does not appear inline, then keep the `demonstrated` label citing that new test.
- Out of scope (do not fix here): `every_in_tree_spec_md_has_blank_line_after_each_close_tag` fails on `.speccy/specs/0066-provenance-cleanup/SPEC.md:150/197/244/292` — a pre-existing plan-time SPEC-authoring defect from commit 00fa9d8, untouched by this task; needs a separate `/speccy-amend` or whitespace correction before ship.
</blockers>
<implementer date="2026-06-21T09:37:35Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: No full area re-map — this retry round addresses a non-reuse (tests-evidence) blocker, so the round-1 area map stands. Per-symbol floor for the one new top-level symbol: `vet_provenance_delegates_convention_via_include` (test fn in `speccy-cli/tests/persona_snippets.rs`) is written-fresh, modeled directly on the existing precedent `commit_recipe_delegates_trailer_via_include` (`speccy-cli/tests/authoring_commit.rs:183`) cited by the blocker; reuses the existing in-file helper `require_module_file`. No production code touched.
- Completed: Addressed the round-1 tests blocker on CHK-003. Adopted remedy (b): added a delegation test `vet_provenance_delegates_convention_via_include` in `speccy-cli/tests/persona_snippets.rs` asserting (1) `vet-provenance.md` contains the `{% include "modules/references/convention-checklist.md" %}` line and (2) the checklist's H2 opener `## Convention-drift checklist` does not appear inline in the body. This gates the share-via-include property the round-1 cited test (`module_prose_has_no_internal_artifact_id_provenance`) did not — a fully-inlined copy now fails. Added the corresponding doc-comment bullet to the module header. CHK-003 is now genuinely `demonstrated`, backed by this new test rather than the mislabeled token scan.
- Undone: Orchestration wiring (speccy-vet dispatch + `<gate>` summary) remains REQ-004 / T-003, out of scope. The persona body and both wrappers from round 1 are unchanged — the blocker was evidence-only, the implementation was already correct.
- Hygiene checks: cargo test --workspace exit 1 — sole failure is the pre-existing `every_in_tree_spec_md_has_blank_line_after_each_close_tag` on committed `.speccy/specs/0066-provenance-cleanup/SPEC.md:150/197/244/292`, a plan-time SPEC-authoring defect from commit 00fa9d8 that this task's diff does not touch (git diff/status confirm SPEC.md is unmodified) and that the round-1 blockers block explicitly carved out as "do not fix here". The new delegation test and every other workspace test pass. cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: REQ-002 — CHK-003 (include resolves, no inline definition copy): demonstrated by the new `vet_provenance_delegates_convention_via_include` test, which asserts both the include line presence and the inline-absence of the checklist's H2 opener (a fully-inlined copy fails it). CHK-004 (single-concern, intent-preserving rewrite, carve-out, prose-only): judgment-only per the scenario text; body sections 1-4 instruct exactly this, unchanged from round 1. REQ-003 — CHK-005 (Claude omits tools, both declare pin, description passes angle-bracket + "Use when" checks): demonstrated by `wrapper_descriptions_are_upload_safe_and_well_routed` plus the pin_shape suite, all green. CHK-006 (resource-to-ejected parity in both .claude/agents and .codex/agents): demonstrated by `dogfood_outputs_match_committed_tree`, green.
- Discovered issues: clippy `doc_nested_refdefs` fired on the new doc bullet; fixed by adding the `[]` intra-doc-link suffix (`[`name`][]:`) and letting nightly fmt reflow the comment. The pre-existing SPEC.md blank-line-after-close-tag failure (flagged round 1, out of scope) still needs a separate `/speccy-amend` or whitespace fix before ship.
- Procedural compliance: Followed speccy-work retry recipe — amended the round-1 WIP in place, no git restore/clean/checkout, no rewrite-from-scratch. Chose remedy (b) over (a) because adding a real gating test strengthens CHK-003 to genuine `demonstrated` rather than downgrading to judgment-only. No shipped-skill friction; no source-module fixes needed.
</implementer>
<review date="2026-06-21T09:39:08Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security surface in this slice. The diff is `resources/**` prose (new `vet-provenance.md` persona body + Claude/Codex wrappers) plus a test-only addition in `speccy-cli/tests/persona_snippets.rs`; no production code, no auth/authz boundary, no input-validation, secret-handling, crypto, or data-exposure path. Reviewed the apply-mode capability grant as the one security-adjacent dimension: the Claude wrapper intentionally omits the read-only `tools:` grant (REQ-003/DEC-003), but the body scopes that write capability tightly — prose-only edits with logic/control-flow/signatures/test-assertions explicitly off-limits (`vet-provenance.md:68-78`), bounded to files the diff already touches, and `git stash/reset/restore/clean` forbidden via the `vet-no-rollback` include with rollback ownership reserved to the orchestrator (`vet-provenance.md:100-105`). Carve-out guidance biases toward under-editing (`vet-provenance.md:44-51`), limiting blast radius of an automated rewrite pass. Round-2 test addition introduces no security surface.
</review>
<review date="2026-06-21T09:39:33Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="2">
Diff satisfies REQ-002 and REQ-003 against their done-when contracts; no scope sneaked past the non-goals. REQ-002: `resources/modules/personas/vet-provenance.md` declares provenance as the sole review dimension (lines 5-12, naming divided attention as the leak root cause), sources the broadened definition purely via `{% include "modules/references/convention-checklist.md" %}` (line 27) with the in-scope narrowing to the provenance bullet (lines 29-32) and no inline copy, and instructs intent-preserving rewrite never blind-delete (section 3), carve-out respect (section 2), and prose-only/behaviour-preserving scope (section 4) — CHK-004 judgment reads as intended. REQ-003: both wrappers `{% include %}` the body; the Claude `.md.tmpl` carries no read-only `tools:` grant and pins `model: opus[1m]`/`effort: medium`, the Codex `.toml.tmpl` mirrors vet-simplifier's `gpt-5.5`/`low` analog; both descriptions are angle-bracket-free with a "Use when …" clause and no "Do NOT trigger" (subagent shape).
Round-2 delta is evidence-only and does not regress any business mapping: the new `vet_provenance_delegates_convention_via_include` test (`speccy-cli/tests/persona_snippets.rs`) now genuinely gates the share-via-include property REQ-002's done-when names (include line present + checklist H2 `## Convention-drift checklist` absent inline), strengthening the maintainer user story ("share one definition with the early gates, cannot drift apart").
Non-goals honoured: no new CLI command, no new journal block type (body line 83 — "append no journal block", thin `<verdict role="provenance">` only, per DEC-002), no schema change; orchestration wiring (REQ-004) correctly deferred to sibling T-003. Goals served: shared-via-include definition, carve-out survives (section 2), single-concern dedicated pass, apply-mode resources + reejection. Open questions: SPEC records "None"; nothing silently resolved — the audit-record fork is DEC-002 and the body reflects it. Changelog shows one initial entry; diff reflects current intent, no stale intent. The pre-existing SPEC.md blank-line lint failure (00fa9d8) is out of scope for T-002 per the round-1 carve-out and is not a business concern.
</review>
<review date="2026-06-21T09:40:04Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
No correctness defects found. Reviewed the round-2 remedy (the new delegation test `vet_provenance_delegates_convention_via_include` in `speccy-cli/tests/persona_snippets.rs:182-201`) plus the persona body and convention reference.

Assertion logic is sound and falsifies a wrong implementation on both arms: (1) `body.contains(expected_include)` fails if the `{% include "modules/references/convention-checklist.md" %}` line is removed or altered; (2) `!body.contains("## Convention-drift checklist")` fails if the checklist is inlined verbatim. Verified the anchor string `## Convention-drift checklist` exactly matches line 1 of `resources/modules/references/convention-checklist.md`, and the persona body `resources/modules/personas/vet-provenance.md` carries the include at line 27 with no inline H2 copy.

The test reads the raw (unexpanded) persona body via `require_module_file`, sourced from the `include_dir!`-embedded `RESOURCES` bundle (`speccy-cli/src/embedded.rs:33`) which is compiled from the live `resources/` tree — no source/embedded staleness path. The unexpanded include is correct here: the H2 from the reference appears in the raw body only if someone inlined a copy, which is exactly the gated defect.

Full `cargo test -p speccy-cli` green (65 unit + integration/dogfood parity tests), including the wrapper render/parity tests that exercise `vet-provenance` template expansion with no MiniJinja-markup leakage. The new delegation test passes in isolation.
</review>
<review date="2026-06-21T09:40:15Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-1 CHK-003 blocker remedied. The new delegation test `vet_provenance_delegates_convention_via_include` (`speccy-cli/tests/persona_snippets.rs:182-201`) genuinely gates the share-via-include property: it asserts the `{% include "modules/references/convention-checklist.md" %}` line is present AND that `## Convention-drift checklist` — the verbatim opening line of `resources/modules/references/convention-checklist.md` — does not appear inline. Falsification holds: a fully-inlined copy carries that H2 and fails the inline-absence assertion; a dropped include fails the presence assertion. It faithfully mirrors the cited precedent `commit_recipe_delegates_trailer_via_include` (`speccy-cli/tests/authoring_commit.rs:182`). Ran it green against the live (uncommitted) tree; confirmed the persona body contains no `Convention-drift checklist` string. CHK-003's `demonstrated` label is now legitimate.
Roll call complete and valid for both covered REQs. CHK-004 `judgment-only` is plausible per its own scenario text (persona-review judgment, not scriptable). CHK-005 `demonstrated` — re-ran `wrapper_descriptions_are_upload_safe_and_well_routed` green; Claude wrapper carries no `tools:` grant and pins `model: opus[1m]`/`effort: medium`. CHK-006 `demonstrated` — re-ran `dogfood_outputs_match_committed_tree` green, confirming resource-to-ejected parity for the new wrappers in both `.claude/agents/` and `.codex/agents/`.
No fabrication: the round-2 `cargo test --workspace exit 1` is an honest red disclosure whose sole failure (`every_in_tree_spec_md_has_blank_line_after_each_close_tag` on committed SPEC.md:150/197/244/292) I reproduced — it is a pre-existing plan-time SPEC-authoring defect from commit 00fa9d8, untouched by this task's diff (the slice touches only the persona body, two wrappers, ejected mirrors, and the new test), and was explicitly carved out as out-of-scope in the round-1 blocker.
</review>
<review date="2026-06-21T09:41:36Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Pass. Round-2 diff is narrow: one new module-level doc bullet and one new test function in `speccy-cli/tests/persona_snippets.rs`; plus the new resource files (persona body, two wrappers, ejected mirrors) carried from round 1 and unchanged by round 2.

**New test `vet_provenance_delegates_convention_via_include`** (persona_snippets.rs:177-201):
- Follows the exact pattern of the named round-1 precedent `commit_recipe_delegates_trailer_via_include` (authoring_commit.rs:183): same two-assertion shape (include-line presence + heading-absence), same `require_module_file` helper, same "delegation property" wording in assertion messages.
- Doc comment is accurate to what the test gates.
- Module-level doc bullet uses `[][]:` suffix (line 19), the correct intra-doc-link form when the referent lives in an external crate scope; consistent with how rustdoc resolves integration-test module items.
- No dead imports. No new suppression annotations. `#[expect(..., reason = "...")]` (pre-existing) is the only suppression in file.

**Persona body `resources/modules/personas/vet-provenance.md`**: Contains the `{% include "modules/references/convention-checklist.md" %}` line and does not contain `## Convention-drift checklist` inline — the test's two assertions both hold on the source file. No real Speccy IDs (only generic `SPEC-NNNN`, `REQ-NNN` placeholders). The single `AGENTS.md` mention (line 83) is a runtime-artifact reference ("run the project's standard hygiene suite per `AGENTS.md`"), which the carve-out explicitly permits.

**Wrappers**: Claude wrapper omits `tools:` (apply-mode, correct). Both declare `effort: medium` / `model_reasoning_effort = "low"` mirroring vet-simplifier's exact pin. Descriptions are angle-bracket-free, under 1024 chars (592 measured), carry "Use when …" clause with no "Do NOT trigger" clause (correct for a subagent wrapper).

**Reuse survey**: Present. Named symbol (`vet_provenance_delegates_convention_via_include`) exists at the stated location. `require_module_file` helper reused rather than re-implemented.

No convention drift, naming drift, dead code, or suppression hygiene issues in the round-2 addition.
</review>
