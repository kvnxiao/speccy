# Evidence for SPEC-0056 T-001

Paper trail for extracting the `covers → requirements → scenarios`
resolution walk into the shared `speccy-core::context` function and
rewriting `check::run_task` to consume it.

## Coverage rule

T-001 covers REQ-003. Of its two scenarios, CHK-005 (existing
`check` integration tests pass unchanged) is demonstrated here.
CHK-004 asserts the covered requirements appear *in the bundle
payload* — the `speccy context` command does not exist until T-003,
so CHK-004's payload assertion is judgment-only at this task and
will be demonstrated in T-003. The resolution semantics CHK-004
depends on (exactly the covered requirements, deduplicated, others
absent) are proven here by the `context` module unit tests under the
hygiene gate.

## Session 2026-06-10-T001-attempt-1

### Scenario 1 — Existing `check` integration tests pass unchanged (CHK-005)

<red>
Pre-edit baseline: at HEAD before this task, `check::run_task`
carried its own inlined `covers → requirements → scenarios` walk
(`speccy-cli/src/check.rs` lines 296-315 of the pre-edit file).
There was no shared `speccy-core::context` function — `cargo build`
referencing `speccy_core::context::resolve_covering_requirements`
would fail to compile (unresolved path), so the shared-function
contract did not yet exist.
</red>

<green>
Post-edit: the walk is lifted into
`speccy-core::context::resolve_covering_requirements` and
`check::run_task` consumes it. The 22 existing `check` integration
tests run unmodified (no fixture or expectation edits) and pass,
proving `speccy check`'s text output is byte-identical across the
refactor.

```
$ cargo test -p speccy-cli --test check
test result: ok. 22 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
$ echo $?
0
```

No file under `speccy-cli/tests/` was touched; `git diff --stat`
shows changes only in `speccy-cli/src/check.rs`,
`speccy-core/src/lib.rs`, plus the new untracked
`speccy-core/src/context.rs`.
</green>

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all suites passed; 0 failed across the workspace)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
Finished `dev` profile
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
advisories ok, bans ok, licenses ok, sources ok
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-001 diff
landed.
