---
spec: SPEC-0056
task: T-008
generated_at: 2026-06-10T23:12:25Z
---

<implementer date="2026-06-10T23:12:25Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the two source modules under `resources/modules/` named in the task. Reuse-as-is: the existing `speccy context` bundle field vocabulary (task, requirements/scenarios, journal, siblings, paths, diff_command, consistency) — I reference those existing field names, inventing no new ones. Extend: the existing fan-out spawn prompt in `review-fanout.md` and the `speccy-work.md` entry-precondition / step-2 / step-3 / step-5 / overview prose — edited the entry-read step in place rather than adding a parallel section. Write-fresh: none; no new top-level symbols. This is a prose migration of existing entry-read instructions.
- Completed: Migrated the per-task entry-read contract to a single `speccy context SPEC-NNNN/T-NNN --json` call in two source modules. (1) `resources/modules/skills/partials/review-fanout.md`: rewrote the reviewer spawn prompt (was `speccy check` + bare `<task>` body in TASKS.md + journal-file read) to dispatch one `speccy context` call and reference its bundle fields (task, requirements, scenarios, journal, paths, diff command), preserving the follow-up-reads carve-out. (2) `resources/modules/phases/speccy-work.md`: entry precondition now opens with `speccy context` after task resolution (`speccy next` still precedes on the no-selector path); the retry-shape rule (step 2), the retry branch's implementer/blockers reads (step 3), the scenario entry read (step 5), and the overview retry-shape sentence all now source from the bundle's journal/requirements sections instead of separate file reads. Ran `just reeject`; both host packs regenerated. Carve-outs honoured: reviewer-tests `speccy check` exit-code caveat untouched, vet personas byte-identical, `speccy check` re-read during implementation (step 7) and post-write journal read-back (step 9) left intact as non-entry reads.
- Undone: Nothing in REQ-008 scope. The six reviewer persona bodies were deliberately not touched (REQ-008: they carry only abstract framing and need no per-file-read removal).
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: No evidence file authored — this is a prose-only migration with no red/green test pairs (CHK-011/CHK-012 are reviewer content checks, not scriptable assertions, per the task's "content check by reviewer, not substring assertion" instruction; AGENTS.md forbids substring-matching curated prose). Roll call for CHKs under REQ-008:
  - CHK-011 (clean tree after `just reeject`; vet bodies unchanged): demonstrated → ran `just reeject` twice; second run reported `0 overwritten` (idempotent — committed ejections match sources). `git status --porcelain | grep vet` empty (vet persona modules and ejected vet files untouched). Reviewer re-runs `just reeject` on a clean checkout to confirm.
  - CHK-012 (fan-out prompt + implementer phase open with `speccy context`; no full-file / `speccy check` entry call; reviewer-tests caveat intact): demonstrated → `review-fanout.md` spawn prompt and `speccy-work.md` entry precondition both open with `speccy context SPEC-NNNN/T-NNN --json`; source greps show no `speccy check`/bare-`<task>`/journal-file entry-read instruction in the spawn prompt; `reviewer-tests.md:37` exit-code caveat present in source and ejected pack. Judgment-only confirmation that the prose reads correctly belongs to reviewer-business/reviewer-style on the diff.
- Discovered issues: None.
- Procedural compliance: Edited only source modules under `resources/modules/`; never touched the ejected `.claude/`/`.agents/`/`.codex/` copies directly — regenerated them via `just reeject` per AGENTS.md "Skill pack source of truth". State flips driven through `speccy task transition`. No SPEC.md or TASKS.md hand-edits. No skill source friction encountered.
</implementer>
<review date="2026-06-10T23:14:19Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. Prose-template migration only; no security boundary altered. The reviewer journal-write authority is unchanged — `review-fanout.md` still routes verdicts through `speccy journal append --persona/--verdict`, the edit touches only the entry-read step. The new `speccy context` bundle the prompt points subagents at aggregates already-in-repo data (task entry, covering requirements, full per-task journal, CLI-computed merge-base diff command) — no new data-exposure surface or privilege escalation, no secret material injected into the prompt. The suggested diff command is CLI-computed in merge-base form with no shell-interpolated user input, so the "read the diff with that command" prose introduces no injection vector. Shipped templates use fictional `SPEC-NNNN/T-NNN` placeholders — no real repo identifiers leaked. Verified carve-outs: reviewer-tests `speccy check` exit-code caveat intact (`resources/modules/personas/reviewer-tests.md:37`); vet persona modules byte-identical (empty diff under `resources/modules/personas/`); `just reeject` reports 0 overwritten, clean tree (CHK-011); ejected packs match sources exactly.
</review>
<review date="2026-06-10T23:14:30Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. REQ-008's done-when items all map to the diff. (1) Both task-scoped entry reads now open with `speccy context SPEC-NNNN/T-NNN --json`: the reviewer spawn prompt in `resources/modules/skills/partials/review-fanout.md:12` and the implementer entry precondition + step 2/3/5 in `resources/modules/phases/speccy-work.md`; the old `speccy check SPEC-NNNN/T-003` entry read (former step 5) and the separate journal-file read are gone. (2) `speccy next` still precedes on the no-selector path (work-phase reconcile/step-1 branch preserved), matching the carve-out. (3) `reviewer-tests`'s `speccy check` exit-code caveat is intact (`resources/modules/personas/reviewer-tests.md:37`). (4) Follow-up targeted reads (evidence file) remain explicitly permitted in both modules. (5) Vet modules byte-identical (no source or ejected vet diff vs merge-base). (6) `just reeject` re-run reports `0 overwritten` — committed ejections match sources; both Claude Code and Codex packs regenerated (the orchestrate/review SKILL.md changes are the propagated `review-fanout.md` partial, no scope creep). Non-goals honoured: no vet migration, no new selector form, no inlined diff/evidence. SPEC-0055's append/verdict/commit contract in review-fanout.md (steps 1-3 + atomic-commit section) left untouched per the Notes surgical-edit mandate. Implementer correctly reflected the amended (Changelog 2026-06-10 item C) scope — reviewer recipe in the shared spawn prompt, the six persona bodies untouched — rather than the stale "six reviewer personas" title.
</review>
<review date="2026-06-10T23:14:50Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All AGENTS.md "Skill pack source of truth" invariants are satisfied.

Source-only edits: both changed modules are under `resources/modules/` (review-fanout.md and speccy-work.md). No direct edits to ejected `.claude/`, `.agents/`, or `.codex/` files — all five ejected-file diffs are byte-consistent with the source changes, confirming `just reeject` drove regeneration.

No inlined canonical rule text: the diff adds no verbatim copies of content that should be `{% include %}`-d. Existing `{% include %}` directives in speccy-work.md (reuse-survey-implementer.md, convention-checklist.md, identity-sourcing.md) are pre-existing and untouched.

CHK-012 content check: (1) `review-fanout.md` spawn prompt now opens with `speccy context SPEC-NNNN/T-NNN --json` — the old `speccy check` + bare TASKS.md read + journal-file read entry-read is gone. (2) `speccy-work.md` entry precondition opens with `speccy context` after task resolution; retry-shape rule (step 2), retry branch reads (step 3), and scenario read (step 5) all source from the bundle rather than separate file reads. (3) The `speccy check` reference in step 7 (in-progress scenario re-read during implementation, not an entry read) is correctly left intact. (4) `reviewer-tests.md:37` exit-code caveat ("Do not treat `speccy check` exit codes...") is byte-identical and untouched.

Vet persona bodies: no diff touching them — confirmed by absence of vet-related files in `git status`.

Naming, dead code, reuse: prose-only migration; no new identifiers, no orphaned imports, no reinvented helpers.
</review>
<review date="2026-06-10T23:15:14Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defect found. This is a prose-only migration of the per-task entry-read contract; I reviewed it for logic/control-flow loss, not editorial intent. Verified against the merge-base diff (dff9f33) of `resources/modules/phases/speccy-work.md` and `resources/modules/skills/partials/review-fanout.md`:

- No data the personas previously obtained is dropped. The reviewer spawn prompt's old three reads (`speccy check` scenarios, bare task body in TASKS.md, journal file) all map onto present bundle fields (task entry, covering requirements plus scenarios, full per-task journal); the diff is read via the bundle's suggested merge-base command, consistent with `diff_fetch_command.md`.
- Implementer entry-precondition control flow is intact: step 1 still resolves the task via `speccy next --json` first on the no-selector path before the `speccy context` call, and the `git status --porcelain` clean-tree gate (clause iii) still fires with the same first-attempt/retry branching.
- Retry-shape round comparison remains computable: step 2 now sources the journal from the bundle's journal section, and the implementer/blockers `round` attributes the rule compares are projected into the bundle per T-004's `JsonJournalBlock` shape, so no detection input is lost.
- Carve-outs preserved: reviewer-tests' `speccy check` exit-code-not-evidence caveat (`reviewer-tests.md:37`) untouched; no persona module changed in the diff (`git diff --stat resources/modules/personas/` empty); step-7 `speccy check` re-read (`speccy-work.md:168`) and step-9 post-write journal read-back (`speccy-work.md:284`) left intact as non-entry reads; follow-up-reads carve-out retained in both files.

The shift of the retry-shape source of truth from `speccy journal show` (canonical `retry-shape.md:10-35`) to the bundle's journal section is a consistency/intent question for reviewer-business, not a correctness defect — the required round data is present in the bundle, so the mechanical comparison still resolves correctly.
</review>
<review date="2026-06-10T23:15:16Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. Prose-template migration with no scriptable red/green pairs is correct here: CHK-012 is a reviewer content check (the scenario itself says "content check by reviewer, not substring assertion"), and AGENTS.md forbids substring-asserting curated prose — adding such a test would be vacuous. The inline Evidence roll call covers both CHKs under REQ-008 with defensible labels; I re-verified both independently rather than trusting the claims.

CHK-011 (reeject idempotency + vet untouched): independently reproduced. `just reeject` reported `0 created, 0 overwritten, 36 unchanged`; `git status --porcelain` byte-identical before and after; all six ejected-file hashes (`.claude/agents/speccy-work.md`, the two `.claude`/`.agents` orchestrate+review SKILL.md pairs, `.codex/agents/speccy-work.toml`) unchanged across the run. This invariant is non-vacuous and falsifiable — an un-re-ejected source edit would surface as an `overwritten` count. No vet persona module appears in the changed-source set (`git diff --stat` over `resources/modules/` shows only `phases/speccy-work.md` and `skills/partials/review-fanout.md`).

CHK-012 (entry-read migrated; no stale `speccy check`/bare-`<task>`/journal entry read; reviewer-tests caveat intact): verified by reading the actual files. `review-fanout.md` spawn prompt (lines 12-25) and `speccy-work.md` entry precondition (line 51) + steps 2/3/5 now open the per-task read with a single `speccy context SPEC-NNNN/T-NNN --json` call. The two surviving references are correctly out of entry scope: `speccy-work.md:168` `speccy check` is the in-implementation re-read in step 7, and `review-fanout.md:66` `journal/T-NNN.md` is the `speccy journal append` write target (SPEC-0055 contract, left intact). `reviewer-tests.md:37-40` exit-code caveat present verbatim.
</review>
<review date="2026-06-10T23:17:59Z" model="claude-sonnet-4-6[1m]" persona="docs" verdict="pass" round="1">
Pass. All three review criteria satisfied for T-008's scope (REQ-008).

(1) Migrated skill bodies read coherently and instruct agents correctly.
`resources/modules/skills/partials/review-fanout.md:12-20` — spawn prompt now dispatches a single `speccy context SPEC-NNNN/T-NNN --json` call, names bundle fields (task, requirements/scenarios, journal, sibling index, file paths, suggested diff command), and keeps the follow-up-reads carve-out. `resources/modules/phases/speccy-work.md` — entry precondition (long entry paragraph), step 2 (retry-shape rule), step 3 (retry branch journal reads), and step 5 (scenario read) all correctly source from the bundle rather than separate file reads. Step 7's remaining `speccy check SPEC-NNNN/T-NNN` reference is an explicitly labelled mid-implementation re-read ("Use `speccy check …` to re-read the scenarios being satisfied") — not an entry call as entry context — consistent with CHK-012's qualifier and the task's carve-out that "`speccy check` still exists." `resources/modules/personas/reviewer-tests.md:37` `speccy check` exit-code caveat is intact and untouched.

(2) Ejected outputs are consistent with the updated sources; `just reeject` was run. All six modified ejected files (`.claude/agents/speccy-work.md`, `.claude/skills/speccy-orchestrate/SKILL.md`, `.claude/skills/speccy-review/SKILL.md`, `.agents/skills/speccy-orchestrate/SKILL.md`, `.agents/skills/speccy-review/SKILL.md`, `.codex/agents/speccy-work.toml`) contain the new `speccy context SPEC-NNNN/T-NNN --json` call and the updated prose matching the source diff exactly. Grep confirms each file contains exactly one occurrence of the updated call. Vet persona sources and ejected vet files are byte-identical per the diff (no changes under `resources/modules/personas/reviewer-architecture.md`, `reviewer-correctness.md`, `reviewer-business.md`, `reviewer-style.md`, `reviewer-security.md`, `reviewer-docs.md`).

(3) No reference doc in T-008's scope still describes the old `speccy check`-based entry-read contract in a contradictory way. Two potentially stale references noted as observations:
- `docs/ARCHITECTURE.md:476` still says "uses `speccy check SPEC-NNNN/T-NNN` only to render the scenarios it is satisfying" and is silent about the new `speccy context` entry read. This is explicitly out of T-008's scope — REQ-009/T-009 (state `pending`) owns ARCHITECTURE.md updates including removing stale full-file / `speccy check` entry-read contract prose per its done-when items and CHK-013.
- `resources/modules/skills/speccy-work.md:26` (the skill trigger/redirect, distinct from the phase module at `resources/modules/phases/speccy-work.md`) still carries the old "read `<spec-dir>/journal/T-NNN.md` (if it exists)" entry precondition summary. This file was not in T-008's stated scope (suggested files were the phase module and review-fanout partial); it redirects agents to read the full phase, where the instructions were correctly updated. T-009 may clean this up as part of its "no stale full-file entry-read contract remains" mandate, but it is not a blocking issue for T-008 given the trigger's redirect semantics.

CHK-011 (clean tree after reeject; vet bodies unchanged): confirmed — ejected outputs match sources, zero vet persona diffs.
CHK-012 (fan-out prompt + implementer phase open with `speccy context`; no full-SPEC.md/TASKS.md or `speccy check` entry call; reviewer-tests caveat intact): confirmed — spawn prompt updated correctly, phase module updated correctly, reviewer-tests exit-code caveat at line 37 untouched.
</review>
