---
spec: SPEC-0056
task: T-004
generated_at: 2026-06-10T22:23:56Z
---

<implementer date="2026-06-10T22:23:56Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Full area-map (round 1) over the task's area — `speccy-cli/src/context.rs`, `context_output.rs`, `journal_show_output.rs`, and `speccy-core/src/parse/journal_xml/mod.rs`. Reuse-as-is: `parse_journal_xml` (re-exported `speccy_core::parse::parse_journal_xml`) for journal parsing; `JsonJournalBlock` struct + `to_json_journal_block` mapping from `journal_show_output.rs` for per-block projection — the SPEC-0055 anti-drift seam REQ-004 mandates. Extend: `ContextBundle` (new `journal` field); module doc comments on context.rs / context_output.rs. Write-fresh: `BundleJournal` output struct in context_output.rs (searched journal_show_output.rs — `JsonTaskJournal` is the only journal envelope and is deliberately not reused, since its `schema_version` would collide per the SPEC caveat; nothing carried `exists`/empty-marker semantics), `build_journal` in context.rs, and the journal text-render branch. Per-symbol floor: `BundleJournal` (new struct — no existing equivalent, `JsonTaskJournal` rejected per SPEC); `build_journal` (new fn — `journal_show::resolve_task` does similar path resolution but also applies filters and errors on absence, the opposite of REQ-004's empty-marker contract, so not reusable); `ContextError::JournalParse` (new variant — modelled on `ShowError::Parse`). Made `to_json_journal_block` `pub` (was private) to enable the cross-command reuse.
- Completed: Extended the `speccy context` envelope with a `journal` section (REQ-004). `BundleJournal` carries an `exists` marker, the frontmatter (`spec`/`task`/`generated_at`, skipped when absent), and a `blocks: Vec<JsonJournalBlock>`. `build_journal` resolves `<spec-dir>/journal/<task-id>.md` (same path `journal show` uses), and on presence parses via `parse_journal_xml` and projects every entry in file order through the shared `to_json_journal_block`; on `NotFound` returns `exists: false` with zero blocks and success. Text renderer surfaces the journal section too (`--json` toggles representation, not content). Reused SPEC-0055's `JsonJournalBlock`/`to_json_journal_block` (made `pub`) rather than re-deriving, so `context` and `journal show` cannot drift; did NOT nest `JsonTaskJournal` (schema_version collision, per SPEC caveat).
- Undone: Nothing in scope. T-005 (sibling index/paths/diff) and T-006 (consistency) extend the same envelope and are intentionally untouched.
- Hygiene checks: `cargo test --workspace` exit 0 (all suites green; context suite 10 tests). `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 (fixed an initial `format_push_string` lint by replacing string-push with a single format!). `cargo +nightly fmt --all --check` exit 0 (after `cargo +nightly fmt --all` applied one wrapping change in build_journal). `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-004.md`. Roll call for CHKs under REQ-004:
  - CHK-006 (journal with rounds 1–2, 5 reviews, 1 blockers → all 8 blocks with matching rounds): demonstrated → `bundle_inlines_full_journal_with_all_blocks_and_rounds` in `speccy-cli/tests/context.rs` asserts 8 blocks, kind counts 2/5/1, round order `[1,1,1,1,1,2,2,2]`, body markers present, and the blocking review's persona. Red = no `journal` field on the envelope before this task.
  - CHK-007 (no journal file → exit 0 + explicit absence marker, zero blocks): demonstrated → `bundle_journal_absent_yields_explicit_empty_marker_and_success` (library: `exists:false`, empty blocks, successful emission) plus `binary_journal_absent_exits_zero` (`assert_cmd` `.success()` + stdout `"exists":false`, proving process exit 0). Red = same missing field.
- Discovered issues: None. The `to_json_journal_block` visibility bump is the only cross-module change; it is additive and now documented as the shared projection seam.
- Procedural compliance: Followed speccy-work first-attempt branch end to end (clean tree at entry, no journal for T-004 so first-attempt shape). No friction with the shipped skill or CLI; no `resources/modules/` edits needed. No production `unwrap`/`expect`/`panic`. Did not edit any `.claude/`/`.agents/`/`.codex/` ejected file.
</implementer>
<review date="2026-06-10T22:25:52Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-004 delivered as specified; all three done-when items map to the diff. (1) Full-journal inlining: `build_journal` (`speccy-cli/src/context.rs`) resolves `<spec-dir>/journal/<task-id>.md` and projects every block in file order through the shared `to_json_journal_block` — the CHK-006 test asserts all 8 blocks, kind counts 2/5/1, the exact round sequence `[1,1,1,1,1,2,2,2]` matching the file, and implementer/review/blockers body markers plus the blocking review's persona/verdict, so retry context (handoffs, verdicts, blockers directives) is genuinely present, not just claimed. (2) Absence = empty + exit 0: CHK-007 is covered at both the library boundary (`exists:false`, zero blocks) and the binary boundary (`assert_cmd .success()` proving process exit 0) — the stronger evidence. (3) Anti-drift seam honored: reuses SPEC-0055's `JsonJournalBlock`/`to_json_journal_block` (correctly bumped to `pub`) and deliberately does NOT nest `JsonTaskJournal` wholesale, matching the SPEC's schema_version-collision caveat and the 2026-06-10 changelog intent. Non-goals respected: `--json` toggles representation only (text renderer added in parallel), `speccy check` and `journal show` behavior unchanged (only additive visibility bump). The corrupt-journal-vs-absent distinction (new `JournalParse` variant errors only on parse failure, NotFound stays normal) is a correct resolution, not silent scope creep — DEC-004 carves out absence specifically. No open-questions block exists to be silently decided. Serves all three user stories: reviewer/implementer/solo-dev obtain journal history in the one bundle read.
</review>
<review date="2026-06-10T22:26:06Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security issues in the T-004 journal-inlining slice. Path safety: `build_journal` (`speccy-cli/src/context.rs:197-232`) builds `<spec-dir>/journal/<task-id>.md` from the resolved `task.id` (parsed from TASKS.md, constrained to `^T-\d{3,}$` per `speccy-core/src/task_lookup.rs:290-301`) and an absolute `spec_dir` from the workspace scan — byte-identical to `journal show`'s existing construction (`speccy-cli/src/journal_show.rs:194-197`); no attacker-controlled path component, so no path-traversal surface beyond what already shipped. Sensitive-data exposure: journal blocks are inlined verbatim, which is the intended REQ-004 contract; the journal is a repo-local plaintext file `journal show` already emits, so no new disclosure and no secrets handled. Error path: `ContextError::JournalParse` (`context.rs:73-80`) reveals only a local filesystem path in a dev CLI — no unauthenticated-endpoint disclosure, no file-content leak. The `NotFound` branch correctly degrades to `exists:false`/empty rather than erroring (DEC-004), and other `io::Error` kinds propagate without leaking contents. Read-only command: no auth boundary, network, crypto, or untrusted deserialization. No new dependencies (`Cargo.toml`/`Cargo.lock` unchanged); the `to_json_journal_block` visibility bump (`journal_show_output.rs`) is additive and behavior-neutral.
</review>
<review date="2026-06-10T22:26:15Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found in the T-004 journal-inlining slice (≥80 confidence threshold; nothing reportable).
Traced `build_journal` (`speccy-cli/src/context.rs:177-219`): the `NotFound` arm is the only benign path — it returns the `exists:false` empty marker with zero blocks and `Ok`, while every other IO error propagates as `ContextError::Io` and parse failure as `ContextError::JournalParse`, so absence-vs-failure is partitioned correctly (DEC-004). The `parse_journal_xml` error is `Box<ParseError>`, matching the `JournalParse.source: Box<ParseError>` field; the `read_to_string` handle is closed before mapping. The `doc.entries.iter().map(to_json_journal_block).collect()` is a straight file-order projection with no off-by-one or boundary case (empty `entries` yields an empty `blocks` vec, exercised by the absent-file test). `to_json_journal_block` (`journal_show_output.rs:232`) maps all three per-task variants (implementer/review/blockers) with correct round/model/persona/verdict population. Adding `journal` as the last `ContextBundle` field preserves `schema_version` as the first serialized field. `round` is `u32` end to end — no narrowing cast or overflow. The `?` on `build_journal(location)?` short-circuits before the bundle is constructed, so no partial-state emission. `cargo test -p speccy-cli --test context` green (10/10), including the round-order `[1,1,1,1,1,2,2,2]` and absence-marker assertions.
</review>
<review date="2026-06-10T22:26:17Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Both CHK-006 and CHK-007 are driven by non-vacuous tests in `speccy-cli/tests/context.rs` that exercise the real `build_journal` plus the shared `to_json_journal_block` path, not mocks.

CHK-006 (`bundle_inlines_full_journal_with_all_blocks_and_rounds`, context.rs:680): writes a real two-round fixture journal, emits the bundle through `invoke_json`, and asserts `exists==true`, exactly 8 blocks, kind counts 2 implementer / 5 review / 1 blockers, and the order-sensitive `rounds == [1,1,1,1,1,2,2,2]` (vec equality — a file-order or round-projection regression fails it), plus the round-1 security review's `persona=="security"`. Body markers from implementer, review, and blockers entries across both rounds are asserted present, covering REQ-004's "sufficient for retry context" done-when. Mentally setting the projection to drop a block, miscount rounds, or lose the persona attribute makes the test fail.

CHK-007 split correctly across the library/process boundary. The library test (`bundle_journal_absent_yields_explicit_empty_marker_and_success`, context.rs:790) asserts `exists==false` plus empty blocks. The scenario's "exit code is 0" is proven specifically by `binary_journal_absent_exits_zero` (context.rs:830) via `assert_cmd` `.success()` plus stdout `"exists":false` — a real process exit assertion, not a loose `is_ok()`. This is the right discipline: the Rust-Ok path alone would not have demonstrated process exit 0.

Evidence roll call complete: Evidence field present on the round-1 implementer entry, file exists at `.speccy/specs/0056-task-context-bundle/evidence/T-004.md`, both CHKs labelled demonstrated with named tests. Red phase (panic at `.expect("bundle has journal section")` before the journal field existed) is consistent with the diff adding that field; red and green descriptions differ materially; evidence command is scoped (`cargo test -p speccy-cli --test context`), not the workspace hygiene run. All 10 context tests pass on the dirty tree. No fabrication pattern matched.
</review>
<review date="2026-06-10T22:26:53Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No style violations found.

Naming, imports, and error-handling patterns match the surrounding codebase throughout. `ContextError::JournalParse` mirrors the shape of `ShowError::Parse` in `journal_show.rs`. `fs_err::read_to_string` is used consistently with `archive.rs` and `journal.rs`. The `#[must_use]` annotation added to `to_json_journal_block` follows the project's established pattern (all other non-trivial helpers in `journal_show_output.rs` carry it with a descriptive message). Import ordering is consistent with what `cargo +nightly fmt --all` produces (the implementer recorded a fmt run). No bare `#[allow]` suppressions — none present. No dead code introduced by the diff.

Reuse survey is present and honest: `parse_journal_xml`, `JsonJournalBlock`, and `to_json_journal_block` are confirmed to exist at the claimed paths and are the symbols the diff actually calls. `to_json_journal_block` was correctly promoted from `fn` to `pub fn` (additive, no parallel re-derivation). The decision not to nest `JsonTaskJournal` (schema_version collision) is documented in `context_output.rs` docstring, matching the SPEC caveat.

Tests gate real invariants: block count, kind counts, round attribute ordering, body marker presence, `exists: false` with empty blocks, and process exit-0. No vacuous tests.
</review>
