---
spec: SPEC-0056
task: T-006
generated_at: 2026-06-10T22:46:43Z
---

<implementer date="2026-06-10T22:46:43Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Round-1 area map (covers=REQ-006; suggested files context.rs/context_output.rs/tests/context.rs; neighbouring next.rs + consistency.rs). Reuse-as-is: `speccy_core::consistency::detect`, `ShellGitProbe`, `ConsistencyBlock`, `DriftEntry`, `ConsistencyStatus` — `next.rs:224` already calls exactly this path; the `resolve_spec` helper already in context.rs; the existing `ContextBundle`/`assemble_bundle` scaffolding. Extend: `ContextBundle` (new `consistency` field), `assemble_bundle` (compute+filter), module/file doc comments, `render_text`. Write-fresh: per-symbol floor — `build_consistency` (new top-level fn): searched context.rs for any existing consistency assembly → none (T-006 is the first to add it); it reuses `detect`. `render_consistency` (new top-level fn): extracted from `render_text` purely to keep that function under the file's 100-line clippy ceiling, matching how `render_text` already delegates per-section concerns. Test helpers `tasks_md_states` / `init_repo_no_task_commits` / `consistency_of`: searched tests/context.rs — `tasks_md_six`/`tasks_md_single` exist but none take per-task states or stand up a no-task-commit repo; modelled the git helper on the existing `diff_command_uses_resolved_default_branch_and_runs_from_repo_root` setup. Deliberately did NOT write a parallel `BundleConsistency` output struct: `ConsistencyBlock` already serializes the exact `{status, drifts[]}` shape REQ-006 wants, so reusing it verbatim honours DEC-002 anti-drift.
- Completed: Added the consistency section to the `speccy context` envelope (REQ-006). `context_output.rs` gains a `consistency: ConsistencyBlock` field on `ContextBundle` (reusing `speccy next`'s block verbatim). `context.rs` `build_consistency` runs the shared `detect` via a `ShellGitProbe` rooted at the project root (same as `next.rs`), preserves the workspace-level `status`, and filters `drifts` to the selected task's `task_id`. Text render gains a `## Consistency` section via the extracted `render_consistency` helper. Two integration tests added.
- Undone: Nothing in T-006 scope. T-007 (size-invariant property test), T-008 (skill-module migration), T-009 (ARCHITECTURE.md) remain as separate pending tasks per the decomposition.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 (initial run flagged `render_text` at 104/100 lines and a `doc_markdown` nit on "snake_case" — fixed by extracting `render_consistency` and backticking the term, then clean); `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-006.md`. Roll call for CHKs under REQ-006:
  - CHK-009 (drift affecting two tasks: both exit 0, both non-ok status, drifted task's bundle has only its own drift, undrifted task's drift list empty): demonstrated → evidence Scenario 1 covers the red (no `consistency` field) / green (`drifted_and_undrifted_bundles_share_status_but_scope_drifts_to_self`: T-001 drift list == [T-001] while T-002 also drifts; T-003 same non-ok status + empty drift list; both binary invocations exit 0). The REQ-006 behavior pair's "exactly the selected task's entries" half is additionally pinned by `three_task_drift_surfaces_only_the_selected_tasks_entries` (Scenario 2).
- Discovered issues: None. The "hash drift" phrasing in the scenarios maps onto the existing `state_completed_no_commit` drift kind (a task `completed` in TASKS.md with no matching `[SPEC/T-NNN]:` commit) — no new drift machinery was needed; `detect` already produces task-scoped `DriftEntry` items keyed by `task_id`, so REQ-006 reduces to filtering an existing list.
- Procedural compliance: Followed speccy-work first-attempt branch (clean tree, no journal for T-006). Tests written and run before the hygiene gate; state flipped pending → in-progress → in-review via `speccy task transition` only (never hand-edited TASKS.md). No shipped skill/reference friction encountered, so no `resources/` edit was needed. No reuse-blocker on a retry (this is round 1).
</implementer>
<review date="2026-06-10T22:48:31Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blockers. T-006 adds a read-only consistency section; the security-relevant surface is the task-scoping boundary and the reused git shell-out.
- Information scoping holds: `build_consistency` filters `drifts` by exact `task_id` match (`speccy-cli/src/context.rs:237-241`), and `drifted_and_undrifted_bundles_share_status_but_scope_drifts_to_self` (`speccy-cli/tests/context.rs`) proves a sibling task's drift entry never appears in the selected task's bundle. The workspace-wide `status` shared across bundles is an explicit spec decision (DEC-005) and carries no per-task identifiers, so it is not an unintended leak.
- No injection surface: the consistency path reuses `speccy next`'s `ShellGitProbe` verbatim — `Command::new("git")` with discrete `.arg()` calls (no shell), `regex_escape` on the `--grep` pattern (`speccy-core/src/consistency.rs:226,278-290`), `Stdio::null()` on stdin, and only read-only subcommands (`rev-parse`, `log`, `status`). T-006 introduces no new git machinery.
- No writes anywhere; `detect` is documented and implemented read-only, and `speccy context` never refuses on drift.
- `DriftDetails` fields surfaced for the selected task (commit SHA, this task's expected trailer/journal path, counts, booleans) contain no secrets and no foreign-task identifiers.
</review>
<review date="2026-06-10T22:49:13Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defects found in the T-006 consistency section. Reviewed `build_consistency` (speccy-cli/src/context.rs:227-243): the workspace-level `status` from `detect_consistency` is preserved verbatim while `drifts` is filtered with `d.task_id == task_id`. The filter condition is correct (not inverted), so the undrifted-task path correctly surfaces the non-ok aggregate status with an empty drift list, and the drifted-task path carries only its own entries — matching REQ-006 and DEC-005. Verified the read-command contract: `run` (context.rs:131-161) has no consistency-based exit branch, so drift never changes the exit code. The `spec_id` passed to `detect` (`location.spec_id`, context.rs:202) is the canonical SPEC-NNNN form sourced from `task_lookup` (task_lookup.rs:71,250), identical to the prefix-building input `next.rs` uses, so the per-task commit-title correlation cannot diverge between the two commands. Status aggregation in `consistency::detect` (consistency.rs:405-411) computes Blocked/Drift/Ok across all tasks before the bundle filters by selected task — correct ordering. The text render `render_consistency` (context.rs:505-526) handles the empty-drift branch explicitly and uses defensive `unwrap_or_default()` on serde-to-value with no panic path. Tests `drifted_and_undrifted_bundles_share_status_but_scope_drifts_to_self` and `three_task_drift_surfaces_only_the_selected_tasks_entries` exercise the scope-to-self and shared-status invariants correctly via real-git fixtures with a clean git-unavailable skip.
</review>
<review date="2026-06-10T22:49:27Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. T-006 (REQ-006) maps cleanly to the SPEC. `build_consistency`
(`speccy-cli/src/context.rs:215-228`) runs the shared
`consistency::detect` through a `ShellGitProbe` rooted at the
project root exactly as `next.rs:132,223`, preserves the
workspace-level `status` verbatim, and filters `drifts` to the
selected `task_id` — DEC-002/DEC-005 anti-drift via verbatim
`ConsistencyBlock` reuse, no full-catalogue field (non-goal "No full
consistency report" honored). Done-when coverage: drift-never-changes-
exit-code is asserted at both layers (`.assert().success()` on the
T-001 and T-003 binary invocations in
`drifted_and_undrifted_bundles_share_status_but_scope_drifts_to_self`,
plus the `?` on `invoke_json`); workspace-status-stays-non-ok-while-
undrifted-task-list-is-empty is asserted for T-003; selected-task-gets-
exactly-its-own-entry-amid-other-drifts is asserted in
`three_task_drift_surfaces_only_the_selected_tasks_entries` (T-002
only, T-001/T-004 excluded). Both tests pass (`cargo test -p
speccy-cli --test context drift`). No content-mode flag, no writes
(DEC-001 / non-goals honored). One disclosed-and-acceptable mapping:
the scenarios' "hash drift" Given-clause is realized via
`state_completed_no_commit` drift — there is no hash-specific
`DriftKind` in `consistency.rs` (hash-staleness is a verify-time lint,
not a `detect` output), and REQ-006 binds the contract to
`consistency::detect`'s task-scoped output, not to any named kind, so
the substitution exercises the contracted machinery and asserts the
load-bearing observable behaviour the scenarios name.
</review>
<review date="2026-06-10T22:49:32Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Pass. The T-006 additions (context.rs `build_consistency` + `render_consistency`, context_output.rs `consistency` field, tests/context.rs CHK-009 + three-task-drift tests + three helper functions) conform to project conventions throughout.

Naming: `build_consistency`, `render_consistency`, `detect_consistency` (aliased import), `tasks_md_states`, `init_repo_no_task_commits`, `consistency_of` — all follow the snake_case helpers already in these files.

Imports: `speccy_core::consistency::{ConsistencyBlock, ShellGitProbe, detect as detect_consistency}` added between the existing `speccy_core::context` and `speccy_core::lint` uses, maintaining alphabetical order within the `speccy_core` group.

No-unwrap rule: `unwrap_or_default()` at context.rs:512,521 is not a panicking call — it is a safe fallback on `Option` in the text renderer. Not a violation.

Suppressions: the `#[allow(clippy::expect_used)]` on the test file predates T-006 and covers the whole file; no new bare silencers introduced.

Reuse survey confirmed: `detect_consistency(spec_id, spec, &probe)` call shape matches next.rs:149,224 exactly; `ShellGitProbe::new(project_root)` matches next.rs:132; `ConsistencyBlock` reused verbatim from speccy-core, no parallel struct invented. `tasks_md_states` is a genuinely new fixture shape (per-task state slice) distinct from the three existing `tasks_md_*` helpers.

Doc comments: all new functions carry doc comments; module-level comments in both context.rs and context_output.rs are updated to reference T-006.

No dead code, no unused imports, no vacuous tests — both new tests gate real behavior (scoped drift list, workspace-level status shared across tasks).
</review>
<review date="2026-06-10T22:50:05Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-009 (REQ-006) is genuinely exercised against the real code path, not a mock. `drifted_and_undrifted_bundles_share_status_but_scope_drifts_to_self` and `three_task_drift_surfaces_only_the_selected_tasks_entries` (speccy-cli/tests/context.rs:1190-1296) stand up a real git repo via `init_repo_no_task_commits` (one base commit, no `[SPEC/T-NNN]:` commits), mark tasks `completed` so `consistency::detect` emits genuine `state_completed_no_commit` blocking drift, then drive both the library `run` (via `invoke_json`) and the actual `speccy` binary (`Command::cargo_bin(...).assert().success()`). Every Given/When/Then clause of CHK-009 has an explicit assertion: both selectors exit 0, both carry the same non-ok workspace status, the drifted T-001 bundle scopes to exactly ["T-001"] while T-002 also drifts (proving the filter, not an all-absent check), and undrifted T-003 carries an empty drift list with the same workspace status. The REQ-006 behavior pair is additionally pinned by the three-task test scoping to ["T-002"]. Evidence file present at the named path with a credible red (no `consistency` field on the envelope) / green transition; roll call names CHK-009 demonstrated with the covering test. I confirmed non-vacuity by mutation: deleting `.filter(|d| d.task_id == task_id)` in `build_consistency` (context.rs) makes both tests fail with the self-scoping assertions (left ["T-001","T-002"] vs right ["T-001"]); restoring it returns them green. Tests do not depend on ordering or time and self-skip cleanly when git is unavailable.
</review>
