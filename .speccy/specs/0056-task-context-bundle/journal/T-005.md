---
spec: SPEC-0056
task: T-005
generated_at: 2026-06-10T22:36:45Z
---

<implementer date="2026-06-10T22:36:45Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: full area-map (round 1). Reuse-as-is: `to_repo_relative` (`speccy-cli/src/paths.rs:12`, made reachable cross-module — already `pub(crate)`) for the three path fields; the `repo_sha` non-fatal shell-out shape (`git.rs`) as the convention for the new git helpers; `TaskLocation.tasks_md` + `Task{id,state,covers}` (`speccy-core/src/parse/task_xml/mod.rs:252`) for the sibling index; `TaskLocation.spec_dir` + `find_root` project root for path anchoring. Extend: `git.rs` (added `suggested_diff_command` + private `default_branch`, mirroring `repo_sha`'s `Command`/`Stdio` pattern); `context::assemble_bundle` + `ContextBundle` (+3 fields, +3 build fns); `context_output.rs` (+`SiblingEntry`, +`BundlePaths`). Write-fresh: default-branch + merge-base machinery — searched `git.rs` (only `repo_sha`) and `consistency.rs` (`first_commit_sha_with_title_prefix`, title-prefix only); nothing computes default branch or merge-base, so net-new per REQ-005. New top-level symbols and their accounting: `suggested_diff_command`/`default_branch`/`DEFAULT_BRANCH_FALLBACK` (write-fresh, justified above); `SiblingEntry`/`BundlePaths` (write-fresh — no existing struct carries id/state/covers-only or the path triple; `next_output.rs::SpecPaths` is a private next-only shape and includes a mission path the bundle does not want); `build_siblings`/`build_paths`/`journal_path` (write-fresh helpers local to context assembly).
- Completed: extended the `speccy context` envelope with REQ-005's three navigation aids. (1) `siblings`: every task in the spec except the selected one, projected to id/state/covers only (no body), in TASKS.md declared order. (2) `paths`: repo-relative forward-slash paths to SPEC.md, TASKS.md, and the task's journal file (surfaced even when the journal does not exist). (3) `diff_command`: a `git diff <base>...HEAD` string where `<base>` is the resolved default branch (`origin/main` via `git symbolic-ref --short refs/remotes/origin/HEAD`) or the `main` fallback when git/remote is unavailable. Added the net-new `git::suggested_diff_command` + `default_branch` following `repo_sha`'s non-fatal shell-out convention. Threaded `project_root` + `cwd` into `assemble_bundle`. Extended the text renderer with Sibling tasks / Paths / Suggested diff command sections.
- Undone: nothing in REQ-005 scope. T-006 (consistency section) and T-007 (size-invariant property test) remain separately scoped; the size invariant is honoured here in practice (the only spec-count-growing field is the one-line-per-task sibling index) but its executable proof is T-007's task, not this one.
- Hygiene checks: `cargo test --workspace` exit 0 (all suites green, incl. 13 context integration tests + 2 git unit tests); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0 (after `cargo +nightly fmt --all` applied one doc-comment rewrap in git.rs); `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-005.md`. Roll call for CHKs under REQ-005:
  - CHK-008 (six-task spec; bundle for T-003 has five siblings id/state/covers-only, no sibling body marker leaks): demonstrated → evidence Scenario 1 covers the red (no `siblings` field) → green (`bundle_sibling_index_carries_id_state_covers_only_no_bodies` asserts the exact five-entry index, the `{covers,id,state}`-only key set, sibling-marker absence, and selected-task-marker presence as the non-vacuity guard).
  - REQ-005 paths + diff-command behavior (not a numbered CHK but the second `<behavior>` bullet): demonstrated → evidence Scenario 2 covers it with two tests — `bundle_carries_repo_relative_paths_and_diff_command` (repo-relative paths resolve to real files; fallback `git diff main...HEAD` outside a repo) and `diff_command_uses_resolved_default_branch_and_runs_from_repo_root` (live repo: `git diff origin/main...HEAD` is emitted and runs as-is from the repo root, surfacing the feature change). The `git.rs` unit test `suggested_diff_command_outside_a_repo_falls_back_to_main_baseline` is hygiene-tier proof of the fallback.
- Discovered issues: none. The existing `to_repo_relative` helper, `TaskLocation` shape, and `repo_sha` convention were sufficient seams; no friction with the shipped skill or CLI surfaced.
- Procedural compliance: tests-first (extended `tests/context.rs` and added the `git.rs` unit test before wiring assembly). Edited only T-005's files (`git.rs`, `context.rs`, `context_output.rs`, `tests/context.rs`) plus the evidence file; no ejected pack files or unrelated code touched. State transitions via `speccy task transition` (pending→in-progress→in-review), never by hand-editing TASKS.md.
</implementer>
<review date="2026-06-10T22:38:19Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass. No security defects in the T-005 navigation-aid slice.
Reviewed the committed + working-tree diff for `git.rs`, `context.rs`,
`context_output.rs`, `tests/context.rs`.

- Git shell-outs (`git.rs:default_branch`/`suggested_diff_command`)
  use `Command::new("git")` with fixed argument arrays — no shell,
  no user-controlled args, `cwd` from the resolved project root, not
  from the selector. No command injection in speccy's own process.
- The resolved `origin/HEAD` ref is interpolated into the
  `diff_command` *string* only; speccy never executes it (it is
  consumer-facing text), and UTF-8/non-empty guards back the `main`
  fallback. Output is bounded by git's own ref-name grammar.
- Path fields (`build_paths` via `paths.rs:12 to_repo_relative`) are
  built from workspace-discovered `spec_dir` joined with fixed
  filenames and the parsed task id — no traversal vector, separators
  normalised, no raw selector reaches the filesystem.
- Sensitive-data minimisation is correct: the sibling index is
  projected to id/state/covers only, and
  `bundle_sibling_index_carries_id_state_covers_only_no_bodies`
  (`tests/context.rs`) asserts the exact `{covers,id,state}` key set
  plus sibling-body-marker absence, with the selected task's own
  marker present as a non-vacuity guard.
- Read-only command: no writes, no secrets, no crypto, no
  deserialization of untrusted input, no auth boundary touched.
</review>
<review date="2026-06-10T22:38:47Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Style pass — no violations found.

Naming: `SiblingEntry`, `BundlePaths`, `build_siblings`, `build_paths`, `journal_path` all match the project's PascalCase / snake_case conventions and the adjacent `TaskEntry` / `build_task_entry` / `build_journal` pattern.

Reuse survey: present and detailed. Verified `to_repo_relative` exists at `speccy-cli/src/paths.rs:12`, `TaskLocation.tasks_md` / `TaskLocation.spec_dir` reachable from `task_xml/mod.rs`, and `repo_sha`'s non-fatal shell-out convention in `git.rs` is genuinely mirrored. No reinvention found.

`#[must_use]` on `suggested_diff_command` carries a descriptive message, matching `repo_sha`'s pattern at `git.rs:30`. Consistent with project convention.

No `unwrap()`/`expect()` in production code; tests use `.expect("descriptive message")` throughout. No slice indexing with `[i]`. No `#[allow]` suppressions.

Dead code: none — all new symbols (`SiblingEntry`, `BundlePaths`, `suggested_diff_command`, `default_branch`, `DEFAULT_BRANCH_FALLBACK`, `build_siblings`, `build_paths`, `journal_path`) are consumed at call sites.

Doc coverage: all new `pub` and private items carry doc comments; module-level comment in `context.rs` and `context_output.rs` updated to reflect T-005 scope.

Tests are non-vacuous: the `bundle_sibling_index_carries_id_state_covers_only_no_bodies` absence check is guarded by confirming the selected task's own `SIBLING_BODY_MARKER_3` is present, so the assertion cannot trivially pass due to an empty/absent payload.

The `journal_path` private helper deduplication (shared by `build_journal` and `build_paths`) prevents the two bundle sections from diverging — clean, minimal factoring matching the surrounding style.
</review>
<review date="2026-06-10T22:38:54Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-005's four done-when clauses all map to the diff and are proven by tests. (1) N-1 sibling entries, id/state/covers only: `build_siblings` (`speccy-cli/src/context.rs`) filters the selected task; `bundle_sibling_index_carries_id_state_covers_only_no_bodies` asserts the exact five-entry index and the `{covers,id,state}`-only key set. (2) No sibling body leaks: `SiblingEntry` carries no body field, and the test guards non-vacuously (selected T-003's own marker present, the five siblings' markers absent). (3) Paths resolve repo-relative from the root via `to_repo_relative` against `project_root`; `bundle_carries_repo_relative_paths_and_diff_command` asserts the exact forward-slash paths and that SPEC.md/TASKS.md exist, with the journal path surfaced even when absent. (4) Diff command in merge-base triple-dot form (`git diff <base>...HEAD`) runnable as-is: `diff_command_uses_resolved_default_branch_and_runs_from_repo_root` stands up a real feature branch and executes `git diff origin/main...HEAD`, confirming it surfaces the change; the fallback `git diff main...HEAD` covers git-unavailable per the Assumption. The second behavior bullet (run the command on a feature branch, get the branch diff unmodified) is directly exercised. Stays inside non-goals: only a command string, never inlined diff content (DEC-003); no role flags or content toggles. Changelog's latest intent (net-new default-branch + merge-base machinery, since none existed) is exactly what landed. Serves the implementer reuse-survey user story. T-007's executable size-invariant proof is correctly left out of scope. All 13 context tests green.
</review>
<review date="2026-06-10T22:39:04Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defect found at the >=80 bar across the REQ-005 navigation aids.

Control flow / boundary: `build_siblings` (`speccy-cli/src/context.rs:227`) filters `location.tasks_md.tasks` by `&t.id != selected`, excluding exactly the one selected task and preserving declared parse order — N tasks yield N-1 siblings with no off-by-one; CHK-008's T-001/2/4/5/6 ordering holds.

Option/Result handling: `default_branch` (`git.rs:755`) handles every failure mode — spawn failure, non-zero exit, non-UTF-8 stdout, and empty stdout — via let-else/guards, each degrading to the `main` fallback rather than erroring; `build_journal` (`context.rs:267`) maps NotFound to the explicit `exists:false` empty marker while propagating other I/O and parse errors. No swallowed errors, no `?` that skips required cleanup.

Diff-command semantics: `suggested_diff_command` emits `git diff <base>...HEAD` (`git.rs:739`). The triple-dot form diffs against `merge-base(<base>, HEAD)`, which is exactly REQ-005's "merge-base form against the default branch" — the task body's suggested separate `git merge-base` call is redundant given triple-dot; the live test confirms `git diff origin/main...HEAD` runs as-is and surfaces the feature change.

No integer casts/overflow, no concurrency, and no resource leaks (Command output is collected with piped stdout and nulled stdin/stderr; no dangling handles on any path).
</review>
<review date="2026-06-10T22:39:15Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-005's three integration tests drive the real `context::run` entry point (no mocks) and the assertions are non-vacuous. CHK-008's `bundle_sibling_index_carries_id_state_covers_only_no_bodies` (`speccy-cli/tests/context.rs`) asserts the exact five-entry index `[T-001,T-002,T-004,T-005,T-006]` in declared order (excludes selected T-003), pins T-001's `state=="completed"`/`covers==["REQ-001"]` against a fixture that gives each task a distinct state, asserts every sibling's key set is exactly `{covers,id,state}`, and — the strong guard — asserts the five sibling body markers are absent while the selected task's own `SIBLING_BODY_MARKER_3` IS present, so the absence is sibling-scoped rather than a vacuous all-absent check. A mutation that projected sibling bodies, mis-filtered the selected task, or reordered would fail. The REQ-005 paths+diff-command behavior is covered by `bundle_carries_repo_relative_paths_and_diff_command` (asserts the three repo-relative forward-slash paths, that SPEC.md/TASKS.md resolve to real files, and the no-repo fallback `git diff main...HEAD`) and `diff_command_uses_resolved_default_branch_and_runs_from_repo_root`, which I confirmed does NOT self-skip in this environment: it stands up a real repo with `origin/HEAD -> origin/main`, asserts the bundle emits `git diff origin/main...HEAD` (exercising the live default-branch probe, distinct from the `main` fallback), then runs that exact command and asserts exit 0 with `feature change` in the output. Triple-dot vs two-dot and fallback-vs-resolved are both pinned. Red-phase claim verified against HEAD (T-004): `ContextBundle` carried no `siblings`/`paths`/`diff_command`, so the `.expect(...)` assertions were genuinely unreachable before this task. Evidence roll call covers every CHK under REQ-005 (CHK-008 demonstrated with a named test present in the diff; the unnumbered paths/diff behavior demonstrated + the `git.rs` fallback unit test labelled hygiene with a specific test name), and the roll-call command (`cargo test -p speccy-cli --test context`) is scoped, not the workspace-wide hygiene gate. All three tests pass locally.
</review>
