---
spec: SPEC-0056
task: T-001
generated_at: 2026-06-10T20:46:43Z
---

<implementer date="2026-06-10T20:46:43Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: bounded to REQ-003's area (check.rs walk, speccy-core parse types). Reuse-as-is: `Task`, `SpecDoc`, `Requirement`, `Scenario` types from `speccy_core::parse` (the new function takes/returns these unchanged); `ElementSpan`/`TaskState` re-exports for the direct-construction test. Write-fresh: `resolve_covering_requirements` in new `speccy-core/src/context.rs` — searched `speccy-core/src` for an existing covers→requirements resolver (`grep covers`), only `cross_ref::cross_ref` matched but it validates dangling covers and returns diagnostics, never resolves+returns covering requirements with scenarios; nothing returned covering `Requirement`s in covers order, so net-new.
- Completed: lifted the covers→requirements→scenarios walk out of `check::run_task` into one public `speccy_core::context::resolve_covering_requirements(&Task, &SpecDoc) -> Vec<&Requirement>`, registered `pub mod context` in `speccy-core/src/lib.rs`, and rewrote `check::run_task` to consume it (flatten each returned requirement's scenarios into `CollectedCheck`s). Semantics preserved verbatim: covers-list traversal order, dedup by requirement id at first occurrence, empty-covers → empty, unmatched covers token silently skipped (TSK-001 owns that). The CLI crate now holds no copy of the walk.
- Undone: nothing in scope for T-001. The `speccy context` command, the bundle payload, and CHK-004's payload-level assertion are T-002/T-003 (deliberately out of scope here).
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0 (advisories/bans/licenses/sources ok).
- Evidence: paper trail at `.speccy/specs/0056-task-context-bundle/evidence/T-001.md`. Roll call for CHKs under REQ-003:
  - CHK-004 (covered requirements appear with done-when/behavior/scenario content in the payload; others absent): judgment-only at T-001 → the `speccy context` payload does not exist until T-003, so the payload assertion is deferred; the underlying resolution semantics (exactly-covered, deduplicated, others absent) are proven by `context::tests::resolves_exactly_covered_requirements_in_covers_order` and `missing_requirement_id_is_silently_skipped` in `speccy-core/src/context.rs` under the hygiene gate.
  - CHK-005 (existing `check` tests pass unchanged): demonstrated → evidence Scenario 1 shows the 22 `speccy-cli/tests/check.rs` integration tests passing with no fixture or expectation edits after the refactor (`cargo test -p speccy-cli --test check`: 22 passed).
- Discovered issues: the TASKS.md parser rejects an empty `covers=""` attribute (`InvalidCoversFormat`), so the empty-covers branch in `check::run_task` is only reachable by constructing a `Task` directly. The `empty_covers_yields_empty_set` test does exactly that and documents why. No code change needed; noted for T-003 since the bundle inherits the same parser guarantee.
- Procedural compliance: followed `speccy-work` first-attempt branch (clean tree, no journal). Wrote tests in the new module, ran the project test command locally, self-reviewed the diff against all four reviewer lenses before flipping to in-review. No shipped-skill friction encountered, so no `resources/modules/` edits. New unit tests gate real invariants (resolution order, dedup, skip, empty) — not vacuous. No `unwrap`/`expect` in production code; test `.expect()` messages are descriptive.
</implementer>
<review date="2026-06-10T20:48:19Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. T-001 is a behavior-preserving internal refactor lifting the covers→requirements→scenarios walk into `speccy_core::context::resolve_covering_requirements` (`speccy-core/src/context.rs:35`), consumed by `check::run_task`. No new external input, auth/authz boundary, network, secret, crypto, or deserialization surface; the function operates on already-parsed `Task`/`SpecDoc` values and does only id-equality matching (no shell/path/SQL construction, no injection vector). Function is pure and infallible — no TOCTOU or auth-decision race. The silent skip of an unmatched `covers` token (`context.rs:42-44`) preserves prior semantics and is surfaced by TSK-001 at the lint layer, not a suppressed validation/authorization decision. No `unwrap`/`expect`/`panic` in production code; test `.expect()` messages are descriptive. No `Cargo.toml`/`Cargo.lock` changes, so no new dependency or CVE surface.
</review>
<review date="2026-06-10T20:48:46Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — the covers→requirements→scenarios walk extraction preserves control flow and ordering. Verified: covers-list traversal order unchanged; empty-covers handled before resolution in `check.rs:274`; unmatched token still silently skipped via `let ... else { continue }` in `context.rs:42-44`; no Option/Result mishandling, off-by-one, or resource concerns (infallible, borrowed-return with sound `'doc` lifetime). The dedup move from scenario-id to requirement-id (`context.rs:45-49`) is behavior-equivalent under the one-scenario-per-requirement invariant the doc comment asserts; the `task_selector_dedups_overlapping_checks_in_first_occurrence_order` integration test exercises the overlap path through the refactored code and passes. All 22 `speccy-cli/tests/check.rs` tests pass unchanged (CHK-005) and the four new resolver unit tests pass.
</review>
<review date="2026-06-10T20:48:52Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. T-001's diff matches REQ-003's in-scope done-when: the covers→requirements→scenarios walk is lifted into one shared `speccy_core::context::resolve_covering_requirements` (`speccy-core/src/context.rs:34-52`), `check::run_task` consumes it (`speccy-cli/src/check.rs:303-311`), and no duplicate walk remains in the CLI crate (grep for the inlined loop returns nothing). CHK-005's byte-stability contract is met: the 22 `speccy-cli/tests/check.rs` integration tests pass unmodified, including `task_selector_dedups_overlapping_checks_in_first_occurrence_order`. The old scenario-id dedup → new requirement-id dedup change is behaviorally equivalent for output (scenarios are element-owned by exactly one requirement, so the old scenario dedup was vacuous, as that test documents) and the duplicate-covers-token case is covered by a new unit test. CHK-004's payload-level assertion is correctly deferred to T-003 — the `speccy context` payload does not exist yet, and the T-001 task body + task-scenarios explicitly scope that assertion out of this task, so it is a sanctioned decomposition deferral, not a silent scope decision. Non-goals respected: no `speccy context` command, no bare-spec selector, no envelope, no diff/evidence/consistency content leaked in from later tasks. The implementer surfaced the `covers=""` parser quirk as a discovered issue and deferred it to T-003 rather than silently resolving it. The 2026-06-10 changelog grounding correction changed no requirement intent, and the diff reflects current intent.
</review>
<review date="2026-06-10T20:49:14Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Pass. No style violations found.

`speccy-core/src/context.rs` (new) and `speccy-cli/src/check.rs` (modified):

- `pub mod context;` inserted alphabetically in `speccy-core/src/lib.rs` — matches the existing module ordering.
- `#[must_use = "the resolved requirements are the bundle/check payload"]` follows the project's documented pattern (rule: always include a custom message; checked against `consistency.rs`, `next.rs`, `workspace.rs` usages — consistent).
- No `unwrap()`/`expect()` in production code. Test `.expect()` calls carry descriptive messages per convention.
- No `#[allow(...)]` suppressions introduced; no `#[expect(...)]` needed (clippy clean per hygiene evidence).
- Import `use speccy_core::context::resolve_covering_requirements;` in `check.rs` follows the file's existing sorted-by-crate import style.
- Dead-code check: no unused imports, no unused variables or parameters introduced.
- Naming: `resolve_covering_requirements`, `seen_ids`, `collected` are idiomatic and consistent with surrounding naming in `check.rs` pre-refactor.
- Dedup via `Vec<&str>::contains` rather than `HashSet` is a minor deviation from the codebase's broader pattern (tsk.rs, cross_ref.rs use `HashSet`), but it preserves the prior inlined behavior verbatim and the input domain is small enough that no lint fires. Not a blocker.
- Reuse survey present in the implementer block; named symbols (`cross_ref::cross_ref`) were verified to exist; survey's claim that no existing resolver returns covering `Requirement`s in covers order is correct per grep.
- Tests gate real invariants: covers-order, dedup at first occurrence, missing-id skip, empty-covers. No vacuous assertions.
- Module-level doc comment describes the purpose correctly and matches the code.
</review>
<review date="2026-06-10T20:49:16Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. T-001 covers REQ-003; both its CHKs are accounted for and backed by tests that drive the real code path.

CHK-005 (existing `check` integration tests pass unchanged) — demonstrated and independently reproduced: `git diff dff9f33 --stat -- speccy-cli/tests/` shows zero changes under the tests dir, and `cargo test -p speccy-cli --test check` runs 22 passed / 0 failed. The byte-stability claim is not merely "tests unchanged"; `task_selector_dedups_overlapping_checks_in_first_occurrence_order` (`speccy-cli/tests/check.rs:584`) plus the two `*_task_selector_renders_covered_scenarios` tests drive the refactored `check::run_task` with a two-requirement task and assert scenario ordering/presence across requirements in covers order, so a regression in the lifted walk would fail them.

CHK-004 (covered requirements appear in payload, others absent) — judgment-only label is defensible: the `speccy context` payload does not exist until T-003, where CHK-004 is its scenario. The resolution semantics CHK-004 depends on are proven here by the new `speccy-core/src/context.rs` unit tests, which I ran (4 passed). Adversarial mutation check: removing dedup fails `duplicate_covers_token_resolves_once_at_first_occurrence` (asserts `[REQ-002, REQ-001]`, not the duplicated triple); sorting instead of covers-order fails `resolves_exactly_covered_requirements_in_covers_order` (asserts `[REQ-003, REQ-001]` and the `[CHK-003, CHK-001]` scenario flattening order); treating a missing token as an error/inclusion fails `missing_requirement_id_is_silently_skipped`; returning all on empty covers fails `empty_covers_yields_empty_set`. None are vacuous.

Evidence at `.speccy/specs/0056-task-context-bundle/evidence/T-001.md` is honest for an extraction refactor: the red phase is the absent shared-function contract (compile failure on the not-yet-existing path), the green cites the scoped `cargo test -p speccy-cli --test check` (22 passed) which I reproduced, and the cited test names appear in the diff under review. Roll call covers both REQ-003 CHKs with valid labels and specific test cites.
</review>
