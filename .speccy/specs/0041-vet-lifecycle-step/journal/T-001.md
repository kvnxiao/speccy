---
spec: SPEC-0041
task: T-001
generated_at: 2026-05-23T02:43:09Z
---

<implementer date="2026-05-23T02:43:09Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- Added `NextAction::Vet` variant to `speccy_core::next::NextAction`,
  sitting between `Work` and `Ship`.
- Added private helpers `vet_gate_is_fresh_pass`, `sha256_hex`,
  `last_gate_block`, and `attribute_value` in `speccy-core/src/next.rs`.
  The `<gate>` parser is a small tolerant scanner (scans for the last
  `<gate ...>` opening tag, extracts `verdict` and `tasks_hash`
  attributes) — intentionally not coupled to the strict skill-side
  grammar, mirroring the SPEC's "parse failure → re-vet, not ship"
  posture.
- Updated `compute_for_spec` priority order: when all tasks are
  `state="completed"`, check the vet-gate freshness signal *before*
  REPORT.md. Stale or missing gate → `Vet`. Fresh-pass + REPORT.md
  absent → `Ship`. Fresh-pass + REPORT.md present → `None` (omit).
- Updated the module-level doc comment and `compute_for_spec`
  function-level doc to list the new six-step priority rule.
- Added `NextAction::Vet` match arms in `speccy-cli/src/next_output.rs`
  for both `to_json_action` (emits `"kind": "vet"` with no `task_id`)
  and `render_text_per_spec` (emits `SPEC-NNNN: vet\n`).
- Added the `sha2` workspace dep to `speccy-cli`'s dev-dependencies so
  integration tests can compute a `tasks_hash` for fresh-pass VET.md
  fixtures.
- Added unit tests inside `speccy-core/src/next.rs` for
  `sha256_hex` (known-vector check), `attribute_value`,
  `last_gate_block` (picks the final block; rejects `<gateway>`).
- Added six integration tests in `speccy-core/tests/next_priority.rs`
  covering: all-completed + no VET.md → Vet; VET.md ending in failed
  verdict → Vet; passed verdict + stale hash → Vet; fresh-pass + no
  REPORT.md → Ship; fresh-pass + REPORT.md → None; in-review task
  beats fresh-pass VET.md (priority ordering).
- Updated the existing `chk005_ship_when_all_done_no_report` to write
  fresh-pass VET.md fixtures so it still asserts Ship under the new
  lifecycle.
- Added two CLI JSON tests in `speccy-cli/tests/next_json.rs`
  (workspace JSON emits `kind="vet"` for completed/no-VET.md; emits
  `kind="ship"` when VET.md passes fresh).
- Added a `NextAction::Vet` text rendering test in
  `speccy-cli/src/next_output.rs::tests`.
- Updated `next_text.rs`'s `one_line_per_active_spec` (all-completed
  branch now asserts `vet` and adds a fresh-pass VET.md to the
  "completed → omitted" subcase).
- Updated `next_derived.rs::per_spec_json_ship_when_all_done_no_report`,
  `per_spec_json_null_when_all_done_and_report_present`, and
  `workspace_text_completed_spec_omitted` to write fresh-pass VET.md
  fixtures so they still pass under the new lifecycle. Added a
  `write_fresh_pass_vet_md` helper.

## Undone

Nothing in scope for T-001.

## Hygiene checks

- `cargo test --workspace`: all suites pass except the two
  pre-existing `docs_sweep` failures
  (`architecture_md_documents_xml_element_grammar` and
  `architecture_md_pins_no_public_speccy_fmt`), which look for
  `docs/ARCHITECTURE.md` at the workspace root but the test resolves
  the path to `.speccy/ARCHITECTURE.md`. Confirmed pre-existing by
  stashing my diff and re-running — same two failures with the same
  error message. Not introduced by T-001.
- `cargo clippy --workspace --all-targets --all-features -- -D
  warnings`: clean.
- `cargo +nightly fmt --all --check`: clean.
- `cargo deny check`: `advisories ok, bans ok, licenses ok, sources
  ok` (a few `license-not-encountered` warnings for unused allowance
  entries — pre-existing, harmless).

## Evidence

- `speccy-core/src/next.rs`: variant + resolver + parser + unit
  tests.
- `speccy-core/tests/next_priority.rs`: six new integration tests
  for the Vet priority transitions.
- `speccy-cli/src/next_output.rs`: JSON and text match arms +
  text-render test.
- `speccy-cli/tests/next_json.rs`: two new workspace JSON tests for
  vet/ship under the new lifecycle.
- `speccy-cli/tests/next_text.rs`: updated `vet` line assertion and
  fresh-pass omission fixture.
- `speccy-cli/tests/next_derived.rs`: updated three existing tests
  to seed fresh-pass VET.md so the assertions remain valid.

## Discovered issues

- `speccy-core/tests/docs_sweep.rs` has two failing tests at HEAD,
  unrelated to this task: they read `docs/ARCHITECTURE.md` via a
  path that resolves to `.speccy/ARCHITECTURE.md` (likely a relative
  vs. workspace-root resolution bug, or the file moved in commit
  `af84caf "Move ARCHITECTURE.md to docs folder"` without the test
  being updated). Not in scope for T-001; flagging for a separate
  task.

## Procedural compliance

- No production-code `expect()`/`unwrap()`/`panic!()` added. The
  hex-encoding loop in `sha256_hex` mirrors the existing
  `speccy-core/src/workspace.rs::hex_of_sha256` "infallible
  String-write" pattern (absorb the `Result` via `if .. is_err() {
  break }`).
- `<gate>` block parser is intentionally tolerant (extracts
  attributes off the last opening tag) rather than a full XML parse:
  the SPEC marks parse failure as "force re-vet" (safer than ship),
  and a strict parser would over-couple the resolver to the
  skill-side grammar that ships in T-004.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:47:24Z" round="1">
REQ-001 and REQ-002 done-when bullets are satisfied by the diff. `NextAction::Vet` is added between `Work` and `Ship` in the enum declaration (`speccy-core/src/next.rs:55`); JSON renders `"kind": "vet"` with no `task_id` field via `to_json_action` (`speccy-cli/src/next_output.rs:157`); plain-text renders `SPEC-NNNN: vet\n` via `render_text_per_spec` (`speccy-cli/src/next_output.rs:186`). The six-step priority list is updated in both the module doc-comment and the `compute_for_spec` function doc, with the new step 4 ("all completed + missing-or-stale gate-pass artifact → Vet") matching REQ-002 verbatim. CHK-001 through CHK-004 are covered by tests (unit + integration). Non-goals respected: no `speccy vet` CLI verb introduced, no change to the `orchestrator-verdict` return contract, no staleness check beyond TASKS.md SHA-256. The task scope (REQ-001 + REQ-002 only) leaves persona renames, skill body, and journal artifact rename for T-002–T-005, which is the right slicing.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]" date="2026-05-23T02:47:24Z" round="1">
Task-scenario coverage is concrete and exercises real behaviour, not mocks. Unit tests in `speccy-core/src/next.rs::tests` cover `sha256_hex` against a known empty-string vector, `attribute_value` extraction including the missing-attribute None branch, and `last_gate_block` picking the final block, returning None on no-gate, and ignoring `<gateway>`-style prefixes (negative case proving the scanner is bounded). Six integration tests in `speccy-core/tests/next_priority.rs` construct real workspaces, write real VET.md fixtures with computed SHA-256, and assert each transition: all-done+no-VET → Vet (CHK-001 unit), failed-verdict → Vet, stale-hash → Vet (CHK-004), fresh-pass+no-REPORT → Ship, fresh-pass+REPORT → None, in-review wins over fresh-pass (CHK-003). CLI JSON tests (`workspace_json_emits_vet_when_all_completed_and_no_vet_md`, `workspace_json_emits_ship_when_vet_passes_fresh`) drive the real `speccy next --json` binary via `Command::cargo_bin` and parse the JSON envelope (CHK-001 binary half, CHK-002). Text-render test in `next_text.rs` asserts the new "vet" verb and that fresh-pass+REPORT.md omits the spec from the workspace listing. Pre-existing integration tests (`per_spec_json_ship_when_all_done_no_report`, `chk005_ship_when_all_done_no_report`, `per_spec_json_null_when_all_done_and_report_present`, `workspace_text_completed_spec_omitted`) were updated to seed fresh-pass VET.md fixtures so they still assert their original invariants under the new lifecycle — keeping the regression surface intact rather than weakening it. `cargo test --workspace` passes 100% on the new tests; the two `docs_sweep.rs` failures are pre-existing path-resolution issues introduced by `af84caf` (ARCHITECTURE.md move) and reproduced on a clean stash, unrelated to this task. The implementer's `## Evidence` section enumerates the touched files matching the de-facto convention in recent SPECs (0038–0040); a separate `evidence/T-001.md` file would have been the strict-contract reading but is not the established pattern in this codebase.
</review>

<review persona="security" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-23T02:47:24Z" round="1">
No security-relevant surface area introduced. The new code reads VET.md and TASKS.md from `<spec-dir>` (workspace-internal paths constructed from `spec.dir`, not user input); `fs_err::read` failures fall through to a `false` result rather than panicking. The `<gate>` block scanner uses `.find()` + `.get(..)?` for all slicing — no `[i]` indexing, no panic-prone arithmetic — and `attribute_value` extracts double-quoted strings via the same bounded `.find()/.get()` chain. No new transitive dependency: `sha2` is already a workspace dep and is added only to `speccy-cli`'s `dev-dependencies` for the integration-test helper that computes a fresh `tasks_hash`. SHA-256 is used as an integrity signal between TASKS.md mutations and the gate artifact, not for cryptographic security; collision resistance is more than sufficient for the freshness-check use case. No secrets, no auth boundary, no input from untrusted callers, no logging changes.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-23T02:47:24Z" round="1">
Conventions consistent with `AGENTS.md` and the `.claude/rules/rust/` suite. No `unwrap()` / `expect()` / `panic!()` / `unreachable!()` / `todo!()` in production code. The `sha256_hex` infallible-`write!` pattern matches the existing `speccy-core/src/workspace.rs::hex_of_sha256` helper (`if write!(...).is_err() { break }`), as the implementer notes. No slice/Vec `[i]` indexing — every parser step uses `.get(..)` and propagates with `?`. The `#![allow(clippy::expect_used, reason = "...")]` inside `mod tests` (next.rs:278) matches the convention established at the top of every other tests file in the workspace (50+ call sites enumerated by grep). `#[must_use]` continues to apply to `compute_for_spec`. Naming is consistent with surrounding code: `sha256_hex`, `last_gate_block`, `attribute_value`, `vet_gate_is_fresh_pass`, `GateBlock`. Module-level doc comment updated to enumerate the new six-step priority rule, matching the prose form used since SPEC-0007. Test names follow the `chkNNN_descriptive_phrase` and `descriptive_phrase` patterns used in `next_priority.rs`. `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings` both clean.
</review>
