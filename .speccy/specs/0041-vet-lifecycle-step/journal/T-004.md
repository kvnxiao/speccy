---
spec: SPEC-0041
task: T-004
generated_at: 2026-05-22T00:00:00Z
---

<implementer date="2026-05-22T00:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

## Completed

- Added a "Phase 3 — write `<gate>` block" section to
  `resources/modules/skills/speccy-vet.md` immediately before the
  "Return contract" section. The new section instructs every exit
  path (Phase 0 integrity failures, Phase 1 round-budget
  exhaustion, Phase 1 `stuck` reverts, Phase 2 completion, success
  path) to append exactly one `<gate>` block to
  `<spec-dir>/journal/VET.md` under the current `## Invocation N`
  section before returning the `<orchestrator-verdict>` to the
  caller. The block carries `verdict="passed|failed"`,
  `tasks_hash="<lowercase-hex-sha256>"` of TASKS.md bytes read
  immediately before append, and `date="<ISO8601>"`. POSIX
  (`sha256sum | awk`) and PowerShell (`Get-FileHash`) hash recipes
  are both spelled out. The block body is a one-line summary
  matching the examples in the task description.
- Propagated the same Phase 3 section verbatim into the two ejected
  skill copies at `.claude/skills/speccy-vet/SKILL.md` and
  `.agents/skills/speccy-vet/SKILL.md` (the section contains no
  `{% if %}` templating, so the two ejected copies receive
  identical insertion text).
- Added a new "VET.md per-SPEC journal" section to
  `docs/ARCHITECTURE.md` (placed between "Lifecycle reading" and
  "Concurrent pickup" so it sits adjacent to the existing
  per-task-journal documentation). The section enumerates the
  four sub-agent verdict blocks that may appear in an invocation
  section (`<drift-review>`, `<holistic-fix>`, `<simplifier-scan>`,
  `<simplifier-apply>`) and then specifies the terminal `<gate>`
  block grammar + attribute rules + the `speccy next` resolver
  semantics that anchor on `verdict` and `tasks_hash`.

## Undone

Nothing. All four scenarios in the task body are addressed by the
skill body changes, and the ARCHITECTURE.md grammar update lands
alongside them.

## Hygiene checks

- `cargo test --workspace` — passes for all crates **except** the
  pre-existing `speccy-core --test docs_sweep` failures that
  hardcode `.speccy/ARCHITECTURE.md` instead of
  `docs/ARCHITECTURE.md`. Confirmed pre-existing by stashing my
  changes and re-running; the failure shape is identical on HEAD
  without my diff. Out of scope for T-004 (the move from
  `.speccy/ARCHITECTURE.md` to `docs/ARCHITECTURE.md` happened in
  commit af84caf "Move ARCHITECTURE.md to docs folder"; the test
  file was not updated to match).
- `cargo clippy`, `cargo fmt`, `cargo deny` — not relevant for
  this task; no Rust source changed.

## Evidence

- `resources/modules/skills/speccy-vet.md` — Phase 3 section added
  before "## Return contract".
- `.claude/skills/speccy-vet/SKILL.md` — same Phase 3 section
  added.
- `.agents/skills/speccy-vet/SKILL.md` — same Phase 3 section
  added.
- `docs/ARCHITECTURE.md` — new "## VET.md per-SPEC journal"
  section placed before "## Concurrent pickup".

## Discovered issues

- `speccy-core/tests/docs_sweep.rs` references
  `.speccy/ARCHITECTURE.md` at hardcoded paths. After the
  `docs/ARCHITECTURE.md` move (commit af84caf), those tests fail
  in isolation regardless of any T-004 change. This is a
  pre-existing repo hygiene gap, not drift introduced here. Worth
  filing a separate task to update the test paths.
- ARCHITECTURE.md at HEAD did not previously carry a journal-
  artifact section for VET.md (the existing
  `<drift-review>` / `<holistic-fix>` / `<simplifier-*>` family
  was undocumented). I added a new "## VET.md per-SPEC journal"
  section that documents the whole family rather than only the
  new `<gate>` block, since the task body said "alongside the
  existing element family". Reviewer should confirm the placement
  and scope is acceptable.

## Procedural compliance

- Flipped `state="pending"` → `state="in-progress"` before editing,
  then to `state="in-review"` at the end.
- All file paths in this journal are workspace-relative; the
  ejected skill copies were updated in lockstep with the
  `resources/modules/` source.
- No production Rust source was touched; this task is purely
  skill-body + docs.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-23T03:19:21Z" round="1">
REQ-003 satisfied end-to-end. The added Phase 3 section in
`resources/modules/skills/speccy-vet.md` (and the byte-identical
inserts at `.claude/skills/speccy-vet/SKILL.md` and
`.agents/skills/speccy-vet/SKILL.md`) instructs every exit path —
Phase 0 integrity failure, Phase 1 round-budget exhaustion, Phase 1
`stuck` revert, Phase 2 completion (pass or revert), and the success
path — to append exactly one `<gate>` block with the three required
attributes (`verdict`, `tasks_hash`, `date`). The block is placed
after any `<drift-review>` / `<holistic-fix>` / `<simplifier-scan>` /
`<simplifier-apply>` blocks already written for the invocation,
matching REQ-003's "appended *after* those existing blocks within the
same invocation section, not woven in." `<goals>`, `<non-goals>`, and
the three `<user-stories>` are all respected: the existing
`<drift-review>` / `<holistic-fix>` / `<simplifier-*>` grammar is
untouched, the `<orchestrator-verdict>` return contract is preserved,
no new CLI verb is introduced, and the gate-block grammar gives the
"reviewer reading a finished SPEC's journal/VET.md ... a closing
`<gate>` block I can grep for" user-story line its on-disk artifact.
The `docs/ARCHITECTURE.md` addition documents the full per-section
element family plus the gate-block grammar and the resolver's
last-block-anchor semantics, closing the loop with REQ-001/REQ-002's
freshness check. Phase 0 "spec unknown → cannot write to <spec-dir>"
remains an implicit boundary; the prose handles it via "if the
journal file does not exist yet, bootstrap the file and section per
Phase 0 step 3, then append the `<gate>` block" — fine for the
common Phase 0 failure shape (tasks incomplete with a resolvable
spec-dir).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-23T03:19:21Z" round="1">
T-004 is a pure skill-body + docs task with no behaviour-changing
Rust source and no per-task evidence/T-004.md is expected (consistent
with T-002 and T-003 in this SPEC, which also used `## Evidence`
section pointers to file changes rather than red-then-green captures).
All four `<task-scenarios>` Given/When/Then in TASKS.md are mapped to
concrete instructions in Phase 3:
(1) "passing verdict → one `<gate verdict="passed" tasks_hash="[0-9a-f]{64}"` match" — Phase 3
explicitly mandates lowercase-hex SHA-256 of TASKS.md bytes;
(2) "sha256sum byte-equal" — explicit `sha256sum | awk '{print $1}'`
recipe with PowerShell equivalent given for Windows;
(3) "Phase 0 fail still receives `<gate verdict="failed">`" — Phase
3 enumerates "Phase 0 integrity failures" as a covered exit path
with bootstrap-then-append fallback when the journal file does not
exist yet;
(4) "drift-round budget exhaustion ends with `<gate verdict="failed">`"
— enumerated as "Phase 1 round-budget exhaustion."
CHK-005 and CHK-006 will exercise against a future dogfooded `/speccy-vet`
run (the SPEC scenarios are themselves system-level integration checks,
not unit tests), and the skill-body instructions are sufficient for that
future invocation to satisfy both checks. `cargo test --workspace` exit
state in the implementer's hygiene-checks bullet correctly notes the
pre-existing `docs_sweep` failure unrelated to this task; spot-checked
the trailing newline state of the three modified skill-body files —
all end with `0x0a`, no formatter regressions introduced.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-23T03:19:21Z" round="1">
No security-relevant surface in this diff. The Phase 3 inserts and
the `docs/ARCHITECTURE.md` grammar are prose-only changes describing
SHA-256 hashing of public TASKS.md bytes for freshness anchoring; no
secret material, no auth boundary, no input parsing, no subprocess
execution introduced. The two recipe code blocks (`sha256sum | awk`
and PowerShell `Get-FileHash`) operate exclusively on a workspace-
local file whose contents are already public in git. ISO8601 `date`
attribute is locally generated metadata. The SHA-256 primitive itself
is project-appropriate for the integrity-anchor role; no cryptographic
authentication is implied or required (the rationale in DEC-003
confirms the hash is a freshness signal, not a tamper-resistance
boundary). No new dependencies; no log-exposure path; no CVE surface.
</review>

<review persona="style" verdict="pass" model="claude-opus-4-7[1m]/medium" date="2026-05-23T03:19:21Z" round="1">
Three skill-body files received byte-identical Phase 3 sections (the
section contains no `{% if %}` templating, so MiniJinja-render
parity is preserved by direct copy — verified by spot-checking the
`### Phase 3 — write \`<gate>\` block` line and the closing prose
match across `resources/modules/skills/speccy-vet.md` (line 453+),
`.claude/skills/speccy-vet/SKILL.md` (line 449+), and
`.agents/skills/speccy-vet/SKILL.md` (line 449+)). Markdown heading
levels and bullet style match the surrounding Phase 0/1/2 sections
(`### Phase N — <verb>`, fenced code blocks for shell recipes, em-dash
attribute summaries). The `docs/ARCHITECTURE.md` insertion follows
the doc's existing per-artifact section structure used for the
per-task `T-NNN.md` journal section that immediately precedes it;
the level-2 heading slot fits between "Lifecycle reading" and
"Concurrent pickup" with consistent prose density. No new Rust code,
no lint suppressions, no `unwrap()`/`expect()` introduced; AGENTS.md
conventions for skill-body authoring (no agent-behavior knobs in
CLI, no transitional aliases) are respected — the `<gate>` block is
written by the skill, not by a new CLI verb. No formatter drift
detected on the touched files.
</review>
