---
spec: SPEC-0049
task: T-003
generated_at: 2026-05-27T13:00:00Z
---

<implementer round="1" date="2026-05-27T13:00:00Z" model="claude-opus-4.7[1m]/low">
- Completed: Refactored the two remaining consuming source files to
  remove verbatim canonical rule bodies per DEC-002.
  (1) `resources/modules/skills/speccy-review.md` — replaced the
  `<!-- Shared partial: reconcile-policy. -->` /
  `{% include "modules/references/reconcile-policy.md" %}` /
  `<!-- End shared partial: reconcile-policy. -->` block in the
  "Entry precondition" section with the DEC-002 reconcile-policy
  invariant prose plus a pointer to
  `{{ speccy_references_path }}/reconcile-policy.md`. Also rewrote
  the preceding "dispatch the reconcile pass per the shared partial
  inlined below" sentence so it references the **Reconcile policy**
  summary instead of a now-absent shared partial (per the T-002
  retry pattern called out by the dispatcher).
  (2) `resources/modules/phases/speccy-work.md` — replaced the
  `<!-- Shared rule: retry-shape. -->` ... `<!-- End shared rule:
  retry-shape. -->` block (the `## Rule statement`, `## Read-only
  scope`, `## Worked example 1`, `## Worked example 2`, and
  `## Edge case — implementer awaiting review` sections) with the
  DEC-002 retry-shape invariant prose plus a pointer to
  `{{ speccy_references_path }}/retry-shape.md`. Also rewrote the
  step-2 lead-in ("apply the REQ-001 retry-shape rule inlined
  immediately below") to say "summarized immediately below", and
  rewrote the "Entry precondition" paragraph that referenced "the
  shared reconcile-policy partial inlined into the calling SKILL.md
  body" to use the DEC-002 **Reconcile policy** summary form (the
  consuming wrappers stop inlining it after T-004, so the
  structural-relationship phrasing no longer holds). Added a brief
  inline **Reconcile policy** invariant paragraph following the
  Entry precondition for consistency with `speccy-orchestrate.md`
  and `speccy-review.md`.
  Ran `just reeject`: overwrote 2 files (`.claude/skills/speccy-review/SKILL.md`,
  `.agents/skills/speccy-review/SKILL.md`,
  `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`
  — 4 files via the two phase consumers and two review consumers).
  Verified the verbatim retry-shape rule statement (`is in **retry
  shape** at`) does not appear in any of those four ejected files,
  and the verbatim reconcile-policy `commit_without_state` table
  row does not appear in any of them.
- Undone: None of this task's scope. T-004 (wrapper switch to
  pure-include) and T-005 (dogfood) remain pending — those are
  separate tasks per the decomposition.
- Hygiene checks:
  - `cargo test --workspace`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo deny check`: exit 0
- Evidence: no separate `evidence/T-003.md` paper trail file — this
  is a prose-template refactor with reviewer-judgment verification
  per the SPEC's Notes section (no automated prose substring
  matching per AGENTS.md no-vacuous-tests rule).
  Roll call for CHKs under REQ-001:
  - CHK-001 (retry-shape rule no longer verbatim at non-canonical
    sites): demonstrated by inspection → `resources/modules/phases/speccy-work.md`
    now carries the DEC-002 invariant + pointer, and ejected
    `.claude/agents/speccy-work.md` plus `.codex/agents/speccy-work.toml`
    contain zero occurrences of the `is in **retry shape** at` rule
    statement. (The two `.claude` / `.agents` speccy-work SKILL
    wrappers still inline retry-shape; converting those is T-004's
    explicit job per the SPEC.)
  - CHK-002 (reconcile-policy table no longer verbatim at
    non-canonical sites covered by this task): demonstrated by
    inspection → `resources/modules/skills/speccy-review.md` now
    carries the DEC-002 invariant + pointer, and ejected
    `.claude/skills/speccy-review/SKILL.md` plus
    `.agents/skills/speccy-review/SKILL.md` contain zero occurrences
    of `commit_without_state`. (The two `.claude` / `.agents`
    speccy-work SKILL wrappers still inline the reconcile-policy
    table; converting those is T-004's explicit job.)
  - CHK-003 (vet-phases canonical-owner exception preserved):
    judgment-only for this task — T-003 does not touch any
    vet-phases consumer; the canonical-owner exception in
    `modules/skills/speccy-vet.md` is unchanged and was already
    correct after T-002 landed.
- Discovered issues: None.
- Procedural compliance: Followed the T-002 retry lesson called out
  by the dispatcher — when removing the include + marker block,
  scanned the surrounding prose for sentences that described the
  structural relationship ("shared partial inlined below", "shared
  reconcile-policy partial inlined into the calling SKILL.md body")
  and rewrote them in the same edit so they describe what the file
  actually contains now. No edits to ejected files directly; all
  source edits live under `resources/`, ejected output produced by
  `just reeject`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-7[1m]/medium" round="1" date="2026-05-27T14:30:00Z">
T-003 satisfies its REQ-001 slice cleanly. Scope: the two named source files (`resources/modules/skills/speccy-review.md`, `resources/modules/phases/speccy-work.md`) have their verbatim canonical bodies replaced by the DEC-002 invariant prose + pointer wording verbatim from the SPEC. The four named ejected consumers no longer contain `commit_without_state` or the `is in **retry shape** at … iff` sentence; canonical references still carry the full bodies. CHK-001 / CHK-002 / CHK-003 (vet-phases untouched) all line up. No non-goal violation. The added inline **Reconcile policy** invariant paragraph in `resources/modules/phases/speccy-work.md` is justified — the file's "Entry precondition" prose previously referred to a "shared reconcile-policy partial inlined into the calling SKILL.md body" that T-004 will remove, and the implementer correctly applies the T-002 retry lesson. T-004's remaining wrapper-side inlines are explicitly out of T-003's scope per the SPEC's task split.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-7[1m]/high" round="1" date="2026-05-27T14:30:00Z">
T-003's two source-side edits and the four ejected files match each `<task-scenarios>` Given/When/Then exactly. Scenario 1: `resources/modules/skills/speccy-review.md` lost `{% include "modules/references/reconcile-policy.md" %}` and now carries the DEC-002 "**Reconcile policy.**" invariant paragraph plus a pointer. Scenario 2: `resources/modules/phases/speccy-work.md` lost the `## Rule statement` heading, the `is in **retry shape** at` paragraph, and the `## Worked example 1 — retry shape` heading, replaced by the DEC-002 "**Retry shape.**" invariant + pointer. Scenario 3: the four ejected files all show the verbatim canonical bodies removed. CHK-001 and CHK-002 are demonstrated by inspection; CHK-003 is correctly labeled judgment-only since T-003 does not touch any vet-phases consumer. The SPEC mandates reviewer judgment with no automated prose substring match per AGENTS.md's no-vacuous-tests rule, so the absence of a separate evidence file is consistent with the SPEC contract. Hygiene gates exit 0.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-7[1m]/low" round="1" date="2026-05-27T14:30:00Z">
Prose-template dedup only. T-003 swaps verbatim copies of the retry-shape rule and reconcile-policy table for DEC-002 invariant summaries + pointers to the canonical `modules/references/*.md` files in `resources/modules/phases/speccy-work.md` and `resources/modules/skills/speccy-review.md`, then re-ejects. No code, dependency, build, or workflow files touched; no auth, input-handling, secret, crypto, logging, or rate-limiting surface affected. Nothing in the diff exposes sensitive data, introduces injection vectors, or weakens an authorization boundary.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" round="1" date="2026-05-27T14:30:00Z">
All T-003 style criteria satisfied. Both source files have had the marker comment blocks and verbatim canonical rule bodies excised with no stale "inlined below / shared partial / shared rule" connective prose left behind. The one remaining "same partial below" sentence in `speccy-review.md` is accurate — the `{% include "modules/skills/partials/review-fanout.md" %}` directive immediately follows it and was correct to keep. All four ejected files contain zero occurrences of `commit_without_state` or `is in **retry shape** at`. The round-1 blocker pattern from T-002 (stale structural-relationship prose after include removal) does not recur here — the implementer rewrote the surrounding prose in the same edit per the dispatcher's note.
</review>
