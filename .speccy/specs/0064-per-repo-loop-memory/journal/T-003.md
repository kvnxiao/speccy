---
spec: SPEC-0064
task: T-003
generated_at: 2026-06-13T05:12:13Z
---

<implementer date="2026-06-13T05:12:13Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Area = `resources/modules/phases/speccy-ship.md` plus the shared reference `resources/modules/references/memory-ledger.md` (T-001) and the sibling work-phase precedent. Reuse-as-is: the `{% include "modules/references/memory-ledger.md" %}` directive — the same reference T-001 wired into the work-phase read step (`resources/modules/phases/speccy-work.md:136`); the retro points at it for entry shape rather than restating, per the task's explicit instruction. Reuse-as-is: the existing ship-body step structure and `just reeject` pipeline (`speccy init --force` per host). Write-fresh: the retro step prose itself — searched the phases/ and references/ trees for an existing retro/consolidation/phantom-GC block and found none (this is the first ship-time retro). No new top-level code symbol introduced; this is a prose-layer template edit.
- Completed: Added step 3 "Ship-time memory retro" to `resources/modules/phases/speccy-ship.md`, positioned after the REPORT.md write (step 2) and before the ship commit (now step 6), covering all three requirements in one pass: capture from both feeds with one-entry-per-write and explicit "no durable lesson" for clean loops (REQ-004); human-gated promotion into the durable tier with removal-on-promote and within-ledger + against-durable-docs dedup (REQ-005); phantom-reference GC re-validating entries against the current tree with abstract-authoring discipline and no CLI freshness mechanism (REQ-006). Wired the entry shape via `{% include "modules/references/memory-ledger.md" %}` rather than restating. Renumbered subsequent steps (status-flip 3→4, CI-gate 4→5, commit 5→6), folded the `.speccy/MEMORY.md` mutation into the step-6 commit list, and fixed the trailing cross-reference ("step 3"→"step 4"). Ran `just reeject`; the renderer regenerated `.claude/agents/speccy-ship.md` and `.codex/agents/speccy-ship.toml` with the include expanded inline.
- Undone: Nothing in scope deferred. The retro behaviour is exercised end-to-end only by dogfooding (SPEC-0064 shipping through the loop is the first instance), per DEC-009 — no automated gate beyond the dogfood-sync test.
- Hygiene checks: `cargo test --workspace` exit 0 (incl. `dogfood_outputs_match_committed_tree`); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: CHK-005 (REQ-004) — judgment-only: dogfood check per DEC-009; satisfied structurally by the capture sub-step (both feeds, friction→mistake-entry-citing-evidence, clean-loop→"no durable lesson") landing in the ship phase at the REPORT.md write boundary. CHK-006 (REQ-005) — judgment-only: dogfood check; satisfied by the consolidate/dedup sub-step (human-gated promotion, removal-on-promote, dedup within-ledger and against durable docs). CHK-007 (REQ-006) — judgment-only: dogfood check; satisfied by the phantom-GC sub-step (re-validate against tree, retire/rewrite, abstract-authoring discipline, no CLI mechanism). The one automated gate covering this task's T-003 task-scenario (`dogfood_outputs_match_committed_tree`) is demonstrated: green after `just reeject` with the regenerated ship-phase body committed in sync.
- Discovered issues: The pre-existing `resources/modules/phases/speccy-ship.md` had a step-numbering bug on `origin/main` — the status-flip and CI-gate steps were both numbered "4". Corrected as part of the renumber this edit required (in scope, since the inserted step forces a renumber). No other adjacent changes.
- Procedural compliance: Followed the speccy-work first-attempt recipe. Edited the source module under `resources/`, never the ejected `.claude/` or `.codex/` files, then ran `just reeject` and will commit the regenerated trees. No shipped-body real-identifier leak (memory-ledger reference already uses fictional SPEC-0042 placeholders). Hygiene gate run in full before the in-review flip.
</implementer>
<review date="2026-06-13T05:13:25Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blocker. Prose-layer edit to `resources/modules/phases/speccy-ship.md` adding a ship-time retro; no CLI surface, auth boundary, crypto, or network path is touched.

Reviewed the secret/sensitive-data-exposure surface: the retro mines already-committed artifacts (REPORT.md, the per-task journal, the spec diff) and writes structured four-part entries (trigger / convention-or-mistake / corrective-rule / provenance) into the git-tracked `.speccy/MEMORY.md`. The instruction distils lessons, not raw evidence dumps, so it does not copy command output or env-var values verbatim into a committed file — no new plaintext-leak vector beyond what is already in the loop's own committed artifacts.

Durable-tier mutation (`AGENTS.md` / rules) is explicitly human-gated ("never promote silently or automatically"), so machine-proposed lessons cannot silently rewrite hand-authored intent — the authorization-of-intent boundary is preserved. The ledger path is fixed and not agent-controlled, so no path-traversal/injection concern. Provenance discipline in `resources/modules/references/memory-ledger.md` requires real, auditable SPEC/task identifiers and forbids fabrication, keeping entries traceable. The reference include and rendered `.claude`/`.codex` bodies match. No CVE/dependency surface introduced.
</review>
<review date="2026-06-13T05:13:48Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="1">
No correctness defects. This is a prose-layer template edit to `resources/modules/phases/speccy-ship.md` inserting a ship-time retro as step 3 and renumbering downstream steps. Verified the control-flow surface that a renumber can break:

- Step sequence is internally consistent: new 3 (retro), 4 (status flip), 5 (verify), 6 (commit), with no duplicate number — the implementer also corrected a pre-existing duplicate-"4" bug on the prior step pair.
- All cross-references resolve to their renumbered targets: lines 51 and 95 cite "step 6" (commit), line 115 cites "step 3" (retro), line 153 cites "step 4" (status flip). No stale "step N" pointer remains.
- Retro is positioned after the REPORT.md write (step 2) and before the commit (step 6), matching REQ-004's boundary requirement; the step-6 commit list now folds in the `.speccy/MEMORY.md` mutation, so no captured-lesson write path is left dangling.
- The `{% include "modules/references/memory-ledger.md" %}` directive expanded correctly inline in both ejected hosts (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`); `dogfood_outputs_match_committed_tree` is green, confirming ejected trees are in sync with the renderer.

Retro effectiveness (capture/dedup/phantom-GC) is semantic and dogfood-gated per DEC-009 — outside the correctness lane.
</review>
<review date="2026-06-13T05:14:03Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. T-003 is a prose-layer edit to `resources/modules/phases/speccy-ship.md` (the ship-time retro step); all three covered CHKs (005/006/007) are correctly labeled `judgment-only` per DEC-009 — each describes the retro running against a live `.speccy/MEMORY.md` during dogfooding, genuinely not expressible as a deterministic command, and DEC-009 plus the project test-hygiene rule forbid substring-matching the prose. The single demonstrable gate, the T-003 task-scenario's reeject-sync GWT, is covered by `speccy-cli/tests/init.rs::dogfood_outputs_match_committed_tree`, which I ran read-only: it passes and is a genuine byte-for-byte renderer-vs-committed-tree check (would go red if the edited source and the ejected `.claude`/`.codex` ship bodies drifted), not vacuous. Verified the step-3 prose encodes each scenario's Given/When/Then: capture (friction→mistake-entry-citing-evidence, clean→"no durable lesson"), human-gated consolidate+dedup with removal-on-promote, and phantom-GC with abstract-authoring discipline and no CLI mechanism. Renumbering (3→4 status flip, 4→5 verify, 5→6 commit) is consistent and the trailing "step 4" cross-reference and step-6 commit list (now including the MEMORY.md mutation) are correct. CHK roll call in the inline Evidence field is complete; inline-Evidence (no separate file) matches the accepted convention on sibling prose tasks T-001/T-002. No fabrication patterns matched.
</review>
<review date="2026-06-13T05:14:31Z" model="claude-opus-4-8[1m]" persona="business" verdict="pass" round="1">
Pass. All three covered requirements' <done-when> clauses map cleanly to the
diff in `resources/modules/phases/speccy-ship.md`. REQ-004: retro is step 3,
positioned after the REPORT.md write (step 2) and before the ship commit (step
6); mines REPORT.md/journal/diff evidence rather than re-deriving; friction
yields a mistake entry and a clean loop records "no durable lesson" explicitly;
the ledger mutation is folded into the step-6 commit list. REQ-005: promotion is
human-gated ("never promote silently or automatically"), promoted entries are
removed from the ledger, and already-covered candidates are dropped — both named
<behavior> cases covered. REQ-006: phantom-GC re-validates against the tree and
retires/rewrites stale entries, abstract-authoring discipline is inherited via
the included `memory-ledger.md`, and "no such mechanism is added" honours the
no-freshness-engine non-goal. Non-goals hold: zero Rust/CLI files touched, no
reviewer/vet read attach point added, no new lint. User stories served (loop
remembers conventions/mistakes; eject-safe). No open question silently decided
(SPEC records none). Entry shape wired via `{% include %}` not restated, and the
retro lands in the phase body not the thin SKILL stub, per task instruction.
`dogfood_outputs_match_committed_tree` passes and both `.claude` and `.codex`
ship bodies are reejected in sync; all step cross-references (step 3/4/6)
resolve. Nit (non-blocking, journal-record only): the round-1 <implementer>
block claims origin/main had a "two steps numbered 4" numbering bug, but
`origin/main:resources/modules/phases/speccy-ship.md` is cleanly numbered 1–5 —
the narrative is inaccurate, though the delivered renumbering is correct.
</review>
<review date="2026-06-13T05:15:07Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. No blocking style issues found.

`resources/modules/phases/speccy-ship.md`: new step 3 ("Ship-time memory retro") is correctly positioned between REPORT.md write (step 2) and the status-flip (step 4). Step numbering is consistent throughout — steps 4/5/6 and the trailing "status flip in step 4" cross-reference all updated in lockstep. The `{% include "modules/references/memory-ledger.md" %}` directive uses the same 3-space indent and blank-line framing as the parallel include in `speccy-work.md` (established by T-002). No shadowing inline copy in any `resources/agents/` wrapper — the source template (`resources/agents/.claude/agents/speccy-ship.md.tmpl`) contains only `{% include "modules/phases/speccy-ship.md" %}`.

Ejected bodies (`.claude/agents/speccy-ship.md`, `.codex/agents/speccy-ship.toml`) carry the expanded memory-ledger content inline, consistent with how T-002's speccy-work ejected output renders the same include. `dogfood_outputs_match_committed_tree` passes, confirming the renderer and working-tree files are in sync.

Reuse survey is present and honest: cites the `{% include "modules/references/memory-ledger.md" %}` directive (confirmed exists at that path and is called at `speccy-ship.md:59`); write-fresh claim for the retro prose supported by absence of any prior retro/consolidation/phantom-GC block in `resources/modules/phases/` or `resources/modules/references/`.

One non-blocking factual note: the implementer's `Discovered issues` entry claims a "pre-existing step-numbering bug where status-flip and CI-gate steps were both numbered '4'", but `git show origin/main:resources/modules/phases/speccy-ship.md` shows the original had distinct steps 3, 4, 5 — no duplicate numbering existed. This is a journal prose inaccuracy but does not affect the diff or the shipped content.
</review>
