---
spec: SPEC-0064
task: T-001
generated_at: 2026-06-13T04:56:41Z
---

<implementer date="2026-06-13T04:56:41Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-002 / REQ-006; suggested file resources/modules/references/memory-ledger.md; neighbours: the existing resources/modules/references/ directory). Decisions: reuse-as-is — followed the existing reference-fragment convention of resources/modules/references/reuse-survey-implementer.md (self-contained, H2-start, {% include %}-able inline) so T-002/T-003 can include this file the same way speccy-work.md includes reuse-survey-implementer.md; no new mechanism. Write-fresh — the memory-ledger reference content itself; searched resources/ and docs/ARCHITECTURE.md for any prior memory-ledger entry-shape doc and found none. No new top-level code symbol introduced (this task is shipped reference prose only).
- Completed: Authored resources/modules/references/memory-ledger.md as a self-contained host-neutral reference (H2-start, include-able inline like reuse-survey-implementer.md). It documents the canonical .speccy/MEMORY.md path with its user-owned / git-tracked / never-overwritten-by-init / absence-is-silent status; the four-part entry shape (trigger, convention-or-mistake, corrective rule, provenance) shared by convention- and mistake-flavoured entries; the authoring discipline (prefer abstract convention-level wording over fragile code coordinates; provenance must resolve to a real identifier, with the dangling-provenance / DEC-007 note that this is the only structurally-checkable hygiene slice); and a worked example using fictional placeholders (SPEC-0042 / T-003 / 0042-example-slug) labelled illustrative. Ran `just reeject` — a no-op for rendered host trees (0 created, 0 overwritten, 36 unchanged) since no include site references this reference yet. Also fixed a pre-existing committed formatting defect in this SPEC's own SPEC.md: seven `</scenario>` close tags were directly followed by `</requirement>` with no intervening blank line, failing the in-tree `every_in_tree_spec_md_has_blank_line_after_each_close_tag` workspace test on HEAD (verified pre-existing via git stash). Inserted the missing blank line at each of the seven sites — formatting-only, no requirement text changed.
- Undone: T-002 (implementer read step) and T-003 (ship-time retro) wire this reference in via {% include %}; out of scope for this task. T-004 (verify softness + CHK-001/004/008 structural checks) also separate. Nothing from T-001's scope deferred.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, including dogfood_outputs_match_committed_tree and every_in_tree_spec_md_has_blank_line_after_each_close_tag). `cargo clippy --workspace --all-targets --all-features -- -D warnings` exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0 (advisories/bans/licenses/sources ok; no dependency changes).
- Evidence: no evidence/T-001.md paper trail — this task ships reference prose only, with no executable behaviour to red-then-green. CHK roll call for the task's covered REQs: CHK-002 (REQ-002, entry carries all four parts) → judgment-only: a dogfood check per DEC-009, satisfied when the ship-time retro (T-003) writes a real entry; this task only documents the shape, validated by manual inspection of the reference. CHK-007 (REQ-006, retro retires phantom-referencing entries) → judgment-only: the retro-time GC mechanism is T-003; this task covers REQ-006 only for the authoring-discipline and dangling-provenance wording, also a manual-inspection property. No CHK in T-001's scope is demonstrable by a red-then-green script — both are semantic properties DEC-009 forbids gating with prose-substring tests. The task's own acceptance scenario (dogfood_outputs_match_committed_tree stays green; render output unchanged since no include site added) is covered under hygiene.
- Discovered issues: The pre-existing blank-line-after-close-tag defect in .speccy/specs/0064-per-repo-loop-memory/SPEC.md was emitted by the spec authoring at decompose/create time and was already failing the workspace test on HEAD before this task started. Fixed in-place here because it blocked the hygiene gate; flagging that the SPEC-authoring path produced a SPEC.md that did not satisfy the in-tree blank-line convention.
- Procedural compliance: T-001 state flipped pending -> in-progress -> in-review via `speccy task transition` (never by hand-editing TASKS.md). This block lands in journal/T-001.md via `speccy journal append`; no <implementer-note> written into TASKS.md. No shipped skill body under resources/modules/skills/ or phases/ required edits — the friction (a bad SPEC.md formatting emission) is in a dogfood artifact, not in a shipped skill/template body, so no resources/ skill source change and no reeject-for-skill-fix was triggered.
</implementer>
<review date="2026-06-13T04:58:04Z" model="claude-opus-4-8[1m]" persona="security" verdict="pass" round="1">
No security defect in this slice. T-001 ships a documentation-only reference (`resources/modules/references/memory-ledger.md`) — no auth boundary, injection sink, crypto, secret handling, or network surface is touched. Checked: the worked example uses only fictional placeholders (`SPEC-0042` / `T-003` / `0042-example-slug`) per the shipped-template rule, so no real repo identifiers leak into users' trees; the file is `{% include %}`d verbatim, not a template interpolating untrusted input, so no SSTI vector; the SPEC.md working-tree delta is whitespace-only blank-line insertions after seven `</scenario>` close tags, not content. Advisory (non-blocking): the reference documents `.speccy/MEMORY.md` as git-tracked and reloaded into agent context but carries no caution against recording secrets/credentials/tokens in the convention-or-mistake or corrective-rule fields; a future captured "mistake" entry quoting a failing command or env var could commit a secret. Worth a one-line "never record secrets/credentials" note when T-003 wires the capture pipeline, but not a defect this authoring-only slice introduces.
</review>
<review date="2026-06-13T04:58:07Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
No correctness defect found. T-001 ships only host-neutral reference prose (`resources/modules/references/memory-ledger.md`), seven blank-line formatting insertions in this SPEC's own SPEC.md, and the T-001 state-attribute flip in TASKS.md. There is no executable code on the diff — no control flow, Option/Result handling, boundary conditions, concurrency, or resource lifecycle to assess in the correctness lane. The reference is not `{% include %}`d at any callsite yet (T-002/T-003 wire it in), so `just reeject` is a genuine no-op for the rendered host trees and the eject tree stays in sync, consistent with the dogfood_outputs_match_committed_tree acceptance scenario. Reference-content quality (whether the four parts and authoring discipline are well-stated) is a semantic/business+style property outside correctness and is covered by manual inspection per DEC-009.
</review>
<review date="2026-06-13T04:58:19Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Diff delivers the SPEC's promise for T-001's scope. The new
`resources/modules/references/memory-ledger.md` documents all four entry parts
named in REQ-002 (trigger / convention-or-mistake / corrective rule /
provenance) verbatim against SPEC.md:163-168, and captures the
convention-vs-mistake "same shape, differs only by feed source" behaviour
(lines 16-18) that REQ-002's second behavior clause requires. The
authoring-discipline half of REQ-006 is present and concrete (lines 32-44):
prefer abstract convention wording over fragile code coordinates, provenance
must resolve to a real identifier, and the DEC-007 note that dangling
SPEC/task provenance is the only structurally-checkable hygiene slice. The
worked example itself models the abstract-wording discipline (no pinned
function/line) and uses the required fictional placeholders (SPEC-0042 /
T-003 / 0042-example-slug, labelled illustrative, line 48/57). The
`.speccy/MEMORY.md` status prose (user-owned, git-tracked, BACKLOG.md sibling,
never enumerated/overwritten, absence-is-silent; lines 3-8) matches DEC-002.
Out-of-scope items the task deferred to T-003 (retro-time re-validation / GC)
and T-002/T-003 (include wiring) are correctly left undone — no silent
resolution of a T-003 question here. No non-goal breached: the diff adds no
`speccy memory` verb, no init-time detection, no freshness-hashing engine, no
lint family, no reviewer/vet read site — only prose plus the byte-for-byte
in-tree SPEC.md blank-line formatting fix and the T-001 state transition.
Requirement-to-diff mapping is clean for the slice this task owns.
</review>
<review date="2026-06-13T04:58:34Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Pass. T-001 ships prose only (`resources/modules/references/memory-ledger.md`); both covered CHKs are dogfood/judgment-only per DEC-009, which forbids substring-matching curated prose in tests, so the absence of a red-then-green evidence file is the correct state, not a fabrication smell. Roll call is complete: CHK-002 and CHK-007 (the only CHKs under covered REQ-002/REQ-006) both appear, labelled judgment-only with plausible justification — each requires the T-003 ship-time retro to write/GC a real entry and is a semantic property no deterministic command can gate. The single mechanically-checkable invariant — the eject tree staying in sync since this task adds no `{% include %}` site — is covered by `dogfood_outputs_match_committed_tree`, which I ran independently and confirmed green (1 passed). Grep confirms `memory-ledger.md` is not included anywhere yet (only referenced in journal/TASKS prose), so there is no shadowing inline copy and no render-output delta. Manual inspection of the reference confirms it documents the canonical `.speccy/MEMORY.md` never-overwritten/user-owned status, all four entry parts, the abstract-authoring discipline, and the DEC-007 dangling-provenance note, with fictional placeholders in the worked example.
</review>
<review date="2026-06-13T04:59:13Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. The new `resources/modules/references/memory-ledger.md` matches the project's reference-file convention: H2-start, self-contained, no Rust/host-specific content, fictional placeholders (`SPEC-0042`, `T-003`, `0042-example-slug`) labelled "illustrative". Kebab-case filename is consistent with siblings. Trailing newline confirmed (`0x0a`). The four required elements (canonical path/status, four-part entry shape, authoring discipline, worked example) are all present.

One non-blocking observation: `memory-ledger.md` lines 10-12 ("This file is the single source of truth…") are cross-file module-relationship notes that AGENTS.md classes as meta-annotation to cut from ejected content. However, AGENTS.md frames leanness as "Authoring judgment, not a test gate," and `journal-implementer.md` carries more extensive similar prose — the project is inconsistent here. The 2-sentence cost is minor and not blocking.

Reuse survey is present in the implementer block with honest accounting (reuse-as-is of the H2/include convention; write-fresh for the content; no prior memory-ledger shape doc found — confirmed by grep). Blank-line-after-close-tag fixes in SPEC.md are correct. No dead code, no unused imports (prose-only task). No `#[allow]`, no `unwrap`, no lint suppressions introduced.
</review>
