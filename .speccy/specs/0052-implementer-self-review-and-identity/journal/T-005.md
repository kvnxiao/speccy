---
spec: SPEC-0052
task: T-005
generated_at: 2026-05-28T20:30:00Z
---

<implementer date="2026-05-28T20:30:00Z" model="claude-opus-4-8[1m]" round="1">
- Completed: Replaced the misleading "sourced from the host harness's
  runtime model identifier (env var, runtime API, or host-specific
  equivalent)" trailer phrasing in two source modules with the
  single-source rule deferring to the T-004 identity-sourcing partial:
  `resources/modules/skills/partials/review-fanout.md` (the
  `Co-Authored-By` trailer bullet) and
  `resources/modules/phases/speccy-decompose.md` (the commit-message
  trailer bullet). The new text states `<model>` is the model segment
  sourced per the "Sourcing your recorded identity" rule — the host's
  in-context identifier transcribed verbatim in hyphen form (e.g.
  `claude-opus-4-8[1m]`), never a dotted form or a configured alias —
  and preserves the documented `Speccy Skill Pack` fallback string.
  The hyphen-form example in both edits replaces the env-var phrasing;
  neither touched file contained a literal `claude-opus-4.8` dot-form
  string to correct. Ran `just reeject` (exit 0); the corrected
  guidance propagated into the ejected `speccy-orchestrate` and
  `speccy-review` skills (which include review-fanout) and the
  ejected `speccy-decompose` agent across Claude Code and Codex.
- Undone: Nothing in scope. Note: pre-existing dot-form
  `claude-opus-4.8[1m]` strings remain in
  `resources/modules/references/journal-review.md` (ejected to
  `journal-review.md`), but that file is outside T-005's touched
  resources (review-fanout + decompose paths per REQ-008 / the task
  body) and is left alone per surgical-change discipline.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exit 0;
  `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail is the diff plus the post-reeject grep.
  Roll call for CHKs under REQ-008:
  - CHK-010 (env-var/runtime-API sourcing phrasing gone from the
    review fan-out and decompose paths, and no dot-form
    `claude-opus-4.8` example in the touched files; `just reeject`
    exits 0): judgment-only → no scriptable proof; confirmed on the
    diff and by ad-hoc grep over the touched source files and their
    ejected outputs (`grep "claude-opus-4\.8\|env var\|runtime API\|
    runtime model identifier"` returns no matches in
    review-fanout.md / speccy-decompose.md and in the ejected
    speccy-orchestrate / speccy-review / speccy-decompose packs), and
    `just reeject` exited 0. Reviewer-business / reviewer-style judge
    the wording on the diff.
- Discovered issues: None.
- Procedural compliance: Edited only source modules under
  `resources/`, never the ejected `.claude/` / `.agents/` / `.codex/`
  files, then ran `just reeject` to regenerate. No skill-friction
  edits were needed this turn.
</implementer>

<review date="2026-05-29T00:27:12Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-005 satisfies REQ-008 as scoped by the task body and CHK-010. The env-var/runtime-API sourcing phrasing is removed from both touched paths (`resources/modules/skills/partials/review-fanout.md`, `resources/modules/phases/speccy-decompose.md`) and replaced with a deferral to the single-source "Sourcing your recorded identity" rule, modelling the canonical hyphen-form example `claude-opus-4-8[1m]`; no dot-form `claude-opus-4.8` remains in either touched file; `just reeject` propagated the change to the ejected orchestrate/review/decompose packs across Claude Code and Codex. The cross-referenced rule heading is reachable in every ejected context that carries the new trailer text, so the user-story guidance is not a dangling pointer. Non-goals honored (prose-only, no CLI changes, no prose-asserting tests). The remaining corpus-wide dot-form strings the implementer left alone are intentional illustrative examples from commit 427334b and outside T-005's narrowed "touched files" scope — correctly excluded, not a gap.
</review>

<review date="2026-05-29T00:27:12Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-005 (REQ-008) is a deliberate no-test prose/template slice per DEC-005, so there is no executable test surface to evaluate — and the diff correctly contains zero test or Rust files. Evidence field is present with a complete CHK roll call: the sole covered scenario CHK-010 is labelled judgment-only, a plausible label the SPEC author itself assigned, deferred to reviewer-business/reviewer-style. No fabrication: every concrete evidence claim is independently verified — the negative-grep half of CHK-010 passes deterministically across all six touched files (source `review-fanout.md`/`speccy-decompose.md` and the ejected orchestrate/review/decompose packs show no `env var`, `runtime API`, `runtime model identifier`, or dot-form `claude-opus-4.8` strings), the ejected diffs are byte-consistent with the source edits, and the new "Sourcing your recorded identity" deferral target exists in the ejected packs so the cross-reference resolves.
</review>

<review date="2026-05-29T00:27:12Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security-relevant surface in this change. SPEC-0052/T-005 is a prose-only edit to commit-trailer model-identifier guidance in `resources/modules/skills/partials/review-fanout.md` and `resources/modules/phases/speccy-decompose.md` — no auth boundaries, input handling, secret/credential storage, logging of sensitive data, crypto, or dependency changes. Verified both touched source files now contain no env-var/runtime-API sourcing phrasing and no dot-form `claude-opus-4.8` example. The replaced text narrows guidance away from env-var sourcing toward an in-context identifier rule, mildly reducing any surface for pulling environment values into a commit message. Nothing to block.
</review>

<review date="2026-05-29T00:27:12Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Both touched source modules (`resources/modules/skills/partials/review-fanout.md` and `resources/modules/phases/speccy-decompose.md`) and their three ejected outputs receive identical replacement text, consistent wording, and no stray dot-form or env-var phrasing. Bullet indentation, markdown bold markers, and backtick quoting all match the surrounding file style. No dead code, no suppression annotations, and no parallel helper introduced.
</review>
