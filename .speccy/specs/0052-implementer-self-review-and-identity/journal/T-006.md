---
spec: SPEC-0052
task: T-006
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added a `## Changelog` section with a `<changelog>` table (`Date | Author | Summary` shape, one illustrative row with a fictional `acme-author`) inside the worked `SPEC.md` example in `resources/modules/references/spec.md`, and added a `<changelog>` bullet to the "Shape invariants the lint suite enforces" list naming it as parser-required (SPC-001). Ran `just reeject`; the ejected `speccy-plan` SPEC template reference was overwritten at both `.claude/skills/speccy-plan/references/spec.md` and `.agents/skills/speccy-plan/references/spec.md`.
- Undone: Nothing in scope is left undone.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: no separate evidence file; this is a prose-template change with no scriptable behavior. Roll call for CHKs under REQ-009:
  - CHK-011 (ejected speccy-plan SPEC template shows a `## Changelog` / `<changelog>` worked example and lists `<changelog>` among enforced shape invariants): judgment-only → SPEC marks it judgment-only; confirmed on the diff. The ejected `.claude/skills/speccy-plan/references/spec.md` carries the `## Changelog` heading, the `<changelog>` table in `Date | Author | Summary` shape, and the new shape-invariants bullet (verified via grep at lines 180-203). The deterministic guard is `just reeject` exit 0, which proves the source change propagated.
- Discovered issues: None.
- Procedural compliance: Edited the source module under `resources/modules/references/spec.md` and ran `just reeject` rather than touching the ejected `.claude/` / `.agents/` copies. Used a fictional placeholder author (`acme-author`) and `SPEC-NNNN` in the shipped template per the no-real-Speccy-identifiers rule.
</implementer>

<review date="2026-05-29T00:32:02Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-006 satisfies REQ-009 in full. The source `resources/modules/references/spec.md` gains a `## Changelog` section with a `<changelog>` table in `Date | Author | Summary` shape inside the worked example (placed last, matching archived specs), and the "Shape invariants the lint suite enforces" list now names `<changelog>` as parser-required with the `SPC-001` rationale. `just reeject` propagated identically to both ejected hosts (`.claude/skills/speccy-plan/references/spec.md` and `.agents/skills/speccy-plan/references/spec.md`); no `.codex/` copy exists, so cross-host coverage is complete. CHK-011 (judgment-only) confirmed on the diff. No scope creep into other requirements, no Rust changes, no prose-asserting tests; shipped template uses fictional placeholders (`acme-author`, `SPEC-NNNN`) per the no-real-identifiers rule. The behavior contract — an author copying the template verbatim now emits a `<changelog>` and avoids the missing-changelog `SPC-001` — is met.
</review>

<review date="2026-05-29T00:32:02Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-006 covers one CHK (CHK-011 under REQ-009), pre-marked judgment-only in the SPEC scenario itself and labelled judgment-only in the implementer's evidence roll call. The label is plausible: the scenario is a "reviewer reads the ejected template" doc-content assertion, and any executable test would be a forbidden prose-substring match over a human-curated template body (AGENTS.md vacuous-test rule). The genuinely testable surfaces are out of scope here — `just reeject` exit 0 (the second task-scenario, run by the implementer) and the SPC-001 parser invariant (already covered in `speccy-core/tests/lint_spc.rs`; this task only documents the template). No fabrication surface exists since the change is prose with no claimed red/green runner output. Confirmed on the uncommitted diff that the source and both ejected copies identically gained the `## Changelog`/`<changelog>` worked example (`Date | Author | Summary`, fictional `acme-author`) and the shape-invariants bullet naming `<changelog>` as parser-required. Evidence field present, roll call complete.
</review>

<review date="2026-05-29T00:32:02Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. T-006 is a documentation-only edit to the shipped `speccy-plan` SPEC template reference (`resources/modules/references/spec.md`, ejected verbatim to `.claude/` and `.agents/`). It adds a `## Changelog` worked example and a shape-invariants bullet. No secrets, tokens, env-var reads, injection vectors, auth boundaries, or crypto are introduced. The example uses fictional placeholders (`acme-author`, `2026-05-21`) and leaks no real Speccy identifiers into the downstream-shipped template, satisfying the no-real-identifiers shipping rule. The added content is inert example text inside a code-fenced block, not a live template-include path, so there is no MiniJinja-injection surface.
</review>

<review date="2026-05-29T00:32:02Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
The diff is clean and surgical. `resources/modules/references/spec.md` adds the `## Changelog` / `<changelog>` worked example and the shape-invariants bullet in the right positions; the two ejected copies (`.claude/...` and `.agents/...`) are identical propagations from `just reeject`. No dead code, no naming drift, no suppression annotations, no real Speccy identifiers in shipped template text — fictional `acme-author` and `SPEC-NNNN`-style placeholder values are used throughout. Prose matches the surrounding bullet style in the invariants list.
</review>
