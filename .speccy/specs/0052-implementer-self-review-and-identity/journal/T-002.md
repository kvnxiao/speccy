---
spec: SPEC-0052
task: T-002
generated_at: 2026-05-29T00:02:20Z
---

<implementer date="2026-05-29T00:02:20Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Added a self-review-before-handoff step to the implementer
  phase body (`resources/modules/phases/speccy-work.md`) as a new
  step 7, positioned after the implement step (6) and before the exit
  transition / hygiene gate (now step 8). The step instructs the agent
  to re-read its own diff through the reviewers' lens and fix findings
  in place, framed as the cheap place to catch drift versus a
  bounce-and-respawn review round. It carries a reviewer north-star map
  naming all four personas — business, tests, security, style — each
  with a one-line outcome to achieve, and the style entry defers to the
  shared convention-drift checklist via `{% include
  "modules/references/convention-checklist.md" %}`. Renumbered the
  trailing two steps (exit transition 7→8, exit 8→9) and updated the
  "(steps 4–8)"→"(steps 4–9)" first-attempt-branch reference, adding
  "self-review" to its inline summary. Wired the same convention-checklist
  include into the style reviewer persona
  (`resources/modules/personas/reviewer-style.md`), replacing the prior
  bespoke "what to look for that's easy to miss" enumeration whose
  unique items (reuse-over-reinvent, suppression justification,
  complexity-ceiling split, error-handling consistency) are now subsumed
  by the partial. Ran `just reeject` (exit 0): the new includes resolve
  at render time at both callsites across Claude Code and Codex
  (`speccy-work` and `reviewer-style` overwritten in both packs).
- Undone: Nothing in T-002 scope. REQ-001/002/003 are fully addressed;
  the identity-recording cluster (REQ-005–008) and REQ-004/REQ-009 are
  separate tasks.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
  - `just reeject`: exit 0 (2 overwritten, 31 unchanged).
- Evidence: no evidence file — DEC-005 forbids prose-asserting unit
  tests for this change, so the deterministic gate is the reeject +
  build run; the rest is judgment-only on the diff.
  Roll call for CHKs under REQ-001, REQ-002, REQ-003:
  - CHK-001 (self-review step after implement, before in-review flip,
    fixes drift in place): judgment-only → confirmed on the diff;
    new step 7 in `resources/modules/phases/speccy-work.md` sits
    between the implement step (6) and the exit transition (8) and
    instructs in-place fixing framed against a review round.
  - CHK-002 (four-persona north-star map, no tactics, style defers to
    checklist): judgment-only → confirmed on the diff; map names
    business/tests/security/style with one-line outcomes, discloses no
    detection tactic (no fabrication-detection or mutation method), and
    the style entry defers to the included checklist.
  - CHK-003 (same checklist text in both ejected callsites from one
    partial, old style enumeration gone): judgment-only → confirmed;
    `grep "Reuse over reinvent"` matches both ejected `speccy-work` and
    `reviewer-style` across Claude Code and Codex, and the
    "sub-agents often miss" bespoke text is absent from the ejected
    style persona.
  - CHK-004 (`just reeject` and `cargo test --workspace` both exit 0):
    hygiene → both ran and exited 0 this round.
- Discovered issues: none.
- Procedural compliance: edited only source under `resources/` then ran
  `just reeject`; did not hand-edit any file under `.claude/`,
  `.agents/`, or `.codex/`. Used the include-path form
  (`modules/references/...`) consistent with the existing
  `speccy-init.md` include. Model identity recorded per SPEC-0052's
  intended rule: model from host-stated in-context id
  (`claude-opus-4-8[1m]`, hyphen form) and effort `/low` from this
  `speccy-work` sub-agent's pinned definition, not the parent session.
</implementer>

<review date="2026-05-29T00:06:26Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-002 (covers REQ-001/REQ-002/REQ-003) satisfies all three requirements' observable contracts on the diff and the ejected artifacts. REQ-001: `speccy-work.md` adds step 7 (self-review) between implement (6) and the exit-transition/hygiene gate (8), instructing in-place fixing framed as the cheap place to catch drift vs a respawn round. REQ-002: the north-star map names Business/Tests/Security/Style with one-line outcomes, withholds tactics (the framing line states "not how the reviewers hunt for problems"), and the Style entry defers to the shared checklist. REQ-003: the T-001 partial is included at both callsites; the identical checklist text appears in ejected `speccy-work` and `reviewer-style` on both hosts, and the style reviewer's bespoke five-bullet enumeration is removed. `just reeject` re-ran clean; CHK-004 recorded exit 0. Non-goals respected (prose-only, no CLI logic, no tactics, no other-persona body edits, no prose-asserting tests). Open question (a) left unresolved as intended. No scope creep into T-003/T-004/T-005/T-006.
Non-blocking aside (style persona's lane): the ejected style persona now stacks an empty `## What to look for that's easy to miss` heading directly above the partial's own `## Convention-drift checklist` heading — a cosmetic doubled-heading artifact a follow-up could tidy.
</review>

<review date="2026-05-29T00:06:26Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-002 is a prose-only edit to skill-template source under `resources/`; DEC-005 and the project test-hygiene rule forbid prose-asserting unit tests, so the only deterministic gate is CHK-004 (hygiene), independently reproduced: `just reeject` exits 0 with the `{% include "modules/references/convention-checklist.md" %}` resolved into real checklist text at both callsites across both hosts, and `cargo test --workspace` exits 0. CHK-001/002/003 are SPEC-authored judgment-only and confirmed on the diff: the self-review step is step 7 between implement (6) and the in-review/hygiene-gate exit (8); the four-persona north-star map names business/tests/security/style with outcome-only wording and discloses no detection tactic; the old bespoke enumeration is gone from all ejected style-reviewer callsites while the shared checklist now appears in both implementer phase and style reviewer across both hosts. Mutation check holds: a wrong include path would fail reeject and a dropped include would drop the grep match — both mechanically catchable. The "no evidence file" declaration is coherent and SPEC-grounded, not a hidden red/green trail.
Non-blocking aside (style reviewer's lane): the ejected `reviewer-style.md` leaves an orphaned `## What to look for that's easy to miss` heading immediately above the included `## Convention-drift checklist` heading — an empty section header the include did not replace.
</review>

<review date="2026-05-29T00:06:26Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. T-002 is prompt/template prose under `resources/` with no executable surface — no auth boundary, input-validation path, secret handling, crypto primitive, or authorization race is introduced or changed. The one in-lane item, the security north-star line in `resources/modules/phases/speccy-work.md`, states an outcome rather than disclosing the reviewer's detection tactics, consistent with REQ-002/DEC-001. The new `resources/modules/references/convention-checklist.md` leaks no secrets and no Speccy-specific identifiers, so it ships safely downstream via `speccy init`.
</review>

<review date="2026-05-29T00:06:26Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All changes are precisely scoped. The self-review step in `resources/modules/phases/speccy-work.md` is correctly positioned at step 7 between implement (6) and exit transition (8), uses imperative voice matching the surrounding prose, and the north-star map names all four personas with outcome statements only — no detection tactics disclosed. The `reviewer-style.md` replacement of the bespoke enumeration with `{% include "modules/references/convention-checklist.md" %}` is clean; both ejected callsites carry the shared checklist text. The pre-existing dot-form `claude-opus-4.8` example strings at lines 209–211 of `speccy-work.md` are unchanged by this diff and are scoped to T-005. No dead code, orphaned text, or parallel helpers introduced.
</review>
