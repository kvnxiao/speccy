# Evidence: SPEC-0051 T-002

## CHK-002, CHK-003, CHK-007, CHK-008 — skill body upsert step + state matrix

**Classification:** judgment-only (reviewer audits the skill-body prose
on the diff and confirms the documented contract matches the SPEC).

**Artifact:** `resources/modules/phases/speccy-init.md`

**Shape confirmation at author time:**

- Step 3 enumerates the four-cell AGENTS.md state matrix (north-star
  present/absent × conventions present/absent) with the action per
  cell named, and explicitly states the two seeding decisions are
  made independently.
- Step 3 documents the missing-file path as "silently re-bootstraps"
  with no warning, refusal, or regression-detection ceremony.
- Step 5 (new) carries the conventions upsert step using the heading
  boundary as the sole delimiter: absent → append; present →
  replace heading-to-next-`##`-or-EOF.
- Step 5 explicitly states the replace path runs on every invocation
  (no "section already matches, skip" optimization) so upstream
  refreshes propagate after a Speccy upgrade.
- Step 5 expands the canonical body via
  `{% include "modules/references/agents-md-speccy-conventions.md" %}`
  so `just reeject` propagates the latest body into every host
  pack.
- Step 5 forbids HTML comment markers (no
  `<!-- speccy:conventions:start -->`) — the heading plus the
  in-body preamble line serve as the upsert contract.
- Steps 6 (Report) and 7 (Suggest next step) are renumbered from
  the prior 5/6 to make room for the conventions upsert.

**Verification via ejected pack:**

After `just reeject`, `.claude/skills/speccy-init/SKILL.md` and
`.agents/skills/speccy-init/SKILL.md` contain the expanded include —
"Speccy conventions" appears multiple times (heading + body
references + step-5 prose) and the canonical body's "When to use
which skill" subsection is present, confirming the MiniJinja include
directive expands at render time.

**Reviewer-audit framing per the SPEC:**

CHK-002, CHK-003, CHK-007, and CHK-008 are framed as
fresh-test-repo-runtime scenarios in the SPEC, but the in-tree
verification surface for T-002 is the skill body prose itself —
the runtime behavior follows from the documented contract. The
reviewer judges whether the prose, when executed by an agent,
yields the behavior the scenarios describe. No automated
prose-substring tests are added per the no-vacuous-tests rule.
