---
spec: SPEC-0051
task: T-003
generated_at: 2026-05-27T22:00:00Z
---

<implementer date="2026-05-27T22:00:00Z" model="claude-opus-4-7[1m]/low" round="1">
- Completed: Refactored step 4 of `resources/modules/phases/speccy-init.md`
  from the fixed seven-question script into a brainstorm-style adaptive
  flow. The new step instructs the agent to inspect the repo
  (README, manifest files, source layout, existing `AGENTS.md`) before
  asking anything, then walk the five subsections in template order
  (opening prose, `### Users`, `### V1.0 outcome`, `### Quality bar`,
  `### Known unknowns`) with a per-subsection draft-or-Socratic
  decision: draft-and-confirm when the subsection is legible from
  repo context, one-at-a-time Socratic Q&A (multi-choice when the
  answer space is enumerable) when it is not. Added a hard gate
  forbidding any write to `## Product north star` until every
  subsection is user-approved. Added an explicit asymmetry note
  contrasting north-star freeze-on-first-write (user-authored
  content) with the always-upsert conventions section (canonical
  boilerplate sourced from upstream). Inlined brainstorm-style
  patterns rather than dispatching to `/speccy-brainstorm`, per
  DEC-002. State C freeze-on-first-write branch (step 3) is
  untouched and continues to skip the Q&A path entirely when
  `## Product north star` is already present. Re-ejected host packs
  via `just reeject` (1 overwritten — the `speccy-init` skill body).
- Undone: Nothing in T-003 scope remains.
- Hygiene checks:
  - `cargo test --workspace`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo deny check`: exit 0
- Evidence: prose-only template edit; verification is by reading the
  refactored skill body. Roll call for CHKs under REQ-003 / REQ-004:
  - CHK-004 (`speccy-init.md` body describes five-subsection adaptive
    iteration with draft-or-Socratic decision and hard gate; fixed
    seven-question script absent; no cross-skill dispatch into
    `/speccy-brainstorm`): demonstrated → the refactored step 4 in
    `resources/modules/phases/speccy-init.md` names the five
    subsections in order, specifies the per-subsection
    draft-or-Socratic decision, states the hard gate explicitly, and
    inlines patterns with a "do not invoke `/speccy-brainstorm`"
    note. The prior seven-question list is gone.
  - CHK-005 (fresh repo run: agent inspects before asking, drafts at
    least one subsection from context, presents for confirmation;
    fixed script does not run): judgment-only → no scriptable proof
    available without spinning up an interactive `/speccy-init`
    session against a fresh test repo; reviewer-business and
    reviewer-tests judge the prose contract on the diff.
  - CHK-006 (re-run with existing distinctive `## Product north
    star` preserves the section byte-identical and no Q&A runs):
    judgment-only → the State C freeze-on-first-write path in step
    3 of `speccy-init.md` remains unchanged by this task; the
    behavioural guarantee follows from that untouched branch.
    Reviewer judgment on the diff confirms step 3 was not modified.
- Discovered issues: None.
- Procedural compliance: First-attempt shape (no prior T-003
  journal). Clean working tree at entry. Edited the source module
  under `resources/modules/phases/` and re-ejected via `just reeject`
  rather than patching `.claude/` or `.agents/` directly. All four
  hygiene gates run and exit 0 before the in-review flip.
</implementer>

<review date="2026-05-27T21:53:34Z" model="claude-opus-4-7[1m]/low" persona="business" verdict="pass" round="1">
T-003 satisfies REQ-003 and REQ-004 end-to-end. The fixed seven-question script is removed from `resources/modules/phases/speccy-init.md`; step 4 now describes the five-subsection adaptive flow in the documented template order (opening prose → Users → V1.0 outcome → Quality bar → Known unknowns) with an explicit "Inspect the repo first" precursor, a per-subsection draft-or-Socratic decision rule, a hard write-gate that forbids landing the section until every subsection is user-approved, and an explicit "Do not invoke `/speccy-brainstorm`" clause that satisfies DEC-002. The State C freeze-on-first-write branch in step 3 is untouched, preserving REQ-004. The added "Asymmetry vs. the conventions upsert" paragraph explains the principled asymmetry (user-authored content vs. canonical boilerplate) per REQ-004's third done-when. Non-goals are respected: no Rust CLI changes, no cross-skill dispatch, no backward-compat path for the seven-question script. Ejected pack at `.claude/skills/speccy-init/SKILL.md` carries the same content as the source module, consistent with `just reeject` having run. The user stories about brownfield-with-legible-context and empty-repo cases are both directly served by the draft-or-Socratic split. No open questions silently resolved.
</review>

<review date="2026-05-27T21:53:34Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-003 is a prose-only template refactor. The diff replaces the fixed seven-question script with a brainstorm-style adaptive flow that (a) names the five subsections in template order, (b) specifies the per-subsection draft-or-Socratic decision, (c) states the hard gate before write, (d) inlines patterns rather than dispatching to `/speccy-brainstorm` (the only reference is the explicit "Do not invoke" prohibition), and (e) carries the asymmetry note vs. the conventions upsert. The State C freeze-on-first-write branch in step 3 is unmodified, which is what CHK-006 relies on. A repo-wide grep for "What are we building, and why does it matter" returns zero hits, confirming the prior script is fully gone from both source and ejected packs. The evidence roll call covers all three CHKs under REQ-003/REQ-004; the two `judgment-only` labels (CHK-005, CHK-006) are defensible because both scenarios describe interactive runtime behaviour of an LLM-driven skill against a fresh test repo, which has no scoped deterministic-command analogue. All four hygiene gates exit 0.
</review>

<review date="2026-05-27T21:53:34Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
Prose-only edit to `resources/modules/phases/speccy-init.md` (and its two ejected mirrors under `.claude/` and `.agents/`) refactoring step 4 from a fixed Q&A script into a draft-or-Socratic adaptive flow. No code paths, dependencies, auth boundaries, input parsing, crypto, secret handling, or network surface are touched. Repo inspection reads only files already in the user's working tree (`README.md`, manifests, `AGENTS.md`); no path-traversal or untrusted-input vector. The new "hard gate before write" rule slightly strengthens user consent before persisting north-star content. Nothing in scope for this persona.
</review>

<review date="2026-05-27T21:53:34Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
Prose-only template change; all three files (`resources/modules/phases/speccy-init.md`, `.claude/skills/speccy-init/SKILL.md`, `.agents/skills/speccy-init/SKILL.md`) carry the T-003 additions consistently. The new step 4 body follows the existing bold-subheading prose convention already established throughout the file. No dead code, no lint suppressions, no parallel helpers, no real-identifier leakage into the shipped template. The ejected files were re-generated via `just reeject` rather than direct edits, matching the AGENTS.md editing rule.
</review>
