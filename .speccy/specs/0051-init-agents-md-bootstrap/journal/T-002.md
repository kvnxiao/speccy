---
spec: SPEC-0051
task: T-002
generated_at: 2026-05-27T22:30:00Z
---

<implementer date="2026-05-27T22:30:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Extended `resources/modules/phases/speccy-init.md` with the conventions upsert step and the state-matrix description per T-002's scope. Step 3 ("Inspect `AGENTS.md` at the repo root and decide per-section") was rewritten to enumerate the four cells of the AGENTS.md state matrix — north-star (present/absent) × conventions (present/absent) — with the action per cell named, an explicit statement that the two seeding decisions are made independently, and a "Missing-file path" paragraph documenting silent re-bootstrap with no warning, refusal, or regression-detection ceremony. A new step 5 ("Upsert the `## Speccy conventions` section") was inserted carrying the heading-boundary upsert rule (absent → append; present → replace heading-to-next-`##`-or-EOF), an explicit statement that the replace path runs on every invocation (no "already matches, skip" optimization), an explicit prohibition on HTML comment markers, and the `{% include "modules/references/agents-md-speccy-conventions.md" %}` directive expanding the T-001 canonical body verbatim into the rendered prompt. Prior steps 5 (Report) and 6 (Suggest the next step) renumbered to 6 and 7. Closing paragraph updated to note the conventions section is re-upserted on every invocation while the north star is freeze-on-first-write. Ran `just reeject` — `.agents/skills/speccy-init/SKILL.md` and `.claude/skills/speccy-init/SKILL.md` were both refreshed; grep confirms the canonical body's "When to use which skill" subsection now appears inline in the ejected packs (include expansion succeeded).
- Undone: T-003 (north-star adaptive flow refactor — the fixed seven-question script in step 4 remains untouched per T-002's scope boundary), T-004 (wrapper `description:` frontmatter updates + cumulative reeject), T-005 (cumulative hygiene gate after T-003/T-004 land). The skill body now seeds two sections, but the wrapper descriptions still mention only the north star — that's T-004's job.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0051-init-agents-md-bootstrap/evidence/T-002.md`. Roll call for CHKs under REQ-002 and REQ-005:
  - CHK-002 (fresh repo with north star but no conventions → conventions appended, north star untouched): judgment-only → no scriptable runtime proof in-tree; reviewer judges that the documented upsert contract in step 5 of the skill body, when executed by an agent, produces the scenario's outcome. Step 5 explicitly handles the "heading absent → append" branch and step 3's matrix names the (north-star present + conventions absent) cell with the append action.
  - CHK-003 (fresh repo with stale conventions + sibling `## My project notes` → conventions body replaced, sibling untouched): judgment-only → reviewer judges step 5's "heading present → replace heading-to-next-`##`-or-EOF" branch yields the scenario outcome. The boundary explicitly stops at the next top-level `##`, preserving sibling sections.
  - CHK-007 (twice-in-succession idempotent re-run): judgment-only → reviewer judges that step 3's matrix (skip north star on re-run, replace conventions verbatim from canonical body) plus step 5's "no skip optimization" together guarantee byte-identical output on the second run when the canonical body is unchanged.
  - CHK-008 (state matrix corner: conventions present, north star absent → both sections seeded independently): judgment-only → reviewer judges step 3's explicit statement that the two seeding decisions are made independently directly addresses this corner.
- Discovered issues: none. The T-001 canonical body's heading levels (`##` for the section, `###` for the five subsections) compose cleanly with the include directive — no rendering surprises in the ejected packs.
- Procedural compliance: followed `/speccy-work` recipe — first-attempt shape confirmed (no prior journal file for T-002), clean tree at entry, flipped state pending → in-progress, edited only the canonical source under `resources/modules/phases/` (never `.claude/`, `.agents/`, or `.codex/` directly), ran `just reeject` to refresh ejected packs, ran the four-gate hygiene suite before flipping to in-review. No Rust source touched, no new dependencies, no wrapper edits (deferred to T-004). T-002's scope deliberately leaves the north-star Q&A script in step 4 alone — T-003 owns that refactor.

</implementer>

<review date="2026-05-27T21:49:05Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-002 satisfies REQ-002 and REQ-005 cleanly within the documented scope boundary. `resources/modules/phases/speccy-init.md` step 3 enumerates all four state-matrix cells (north-star present/absent × conventions present/absent) with the action per cell named, explicitly states the two seeding decisions are independent, and documents the missing-file path as silent re-bootstrap with no warning/refusal/regression-detection. Step 5 carries the heading-boundary upsert rule (absent → append; present → replace heading-to-next-`##`-or-EOF), the explicit "no skip optimization" statement, the explicit prohibition on HTML comment markers, and the `{% include "modules/references/agents-md-speccy-conventions.md" %}` directive — verified expanded in `.claude/skills/speccy-init/SKILL.md`. Sibling-section preservation under REQ-002's CHK-003 behavior is addressed: "Content under sibling `##` headings (before or after the conventions section) is left byte-identical." Task-scope discipline is correct: step 4 still carries the legacy seven-question Q&A script — that refactor is T-003's job, explicitly out-of-scope for T-002, and the journal calls this out under "Undone". Wrapper `description:` updates correctly deferred to T-004. No Rust CLI files touched. Non-goals respected (no markers, no vendor CI, no split skill, no cross-skill dispatch).
</review>

<review date="2026-05-27T21:49:05Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-002 is a prose-only edit to `resources/modules/phases/speccy-init.md` whose four covered scenarios (CHK-002, CHK-003, CHK-007, CHK-008) describe agent-runtime behavior in fresh test repos. The evidence file at `.speccy/specs/0051-init-agents-md-bootstrap/evidence/T-002.md` exists, names all four CHKs in the roll call, and labels each `judgment-only`. The `judgment-only` label is plausible: the CHKs cannot be demonstrated by a deterministic in-tree command, and the SPEC's Notes section explicitly disclaims prose-substring matching as vacuous. No project tests were added, which is the correct call per AGENTS.md § "Conventions for AI agents specifically" (no-vacuous-tests rule) — adding a `.contains("## Speccy conventions")` test on the source file would gate editorial wording, not behavior. The evidence prose is honest (not fabricated runner output), and the four-gate hygiene claims are scope-appropriate since the diff is prose-only. The task-scenario assertions all hold on inspection of the source file. Nothing testable to block on from this persona.
</review>

<review date="2026-05-27T21:49:05Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
Template-prose-only change to `resources/modules/phases/speccy-init.md` adding an idempotent heading-boundary upsert step plus a state-matrix description; no code paths, no dependencies, no auth, no input handling, no secrets, no crypto, and no data exposure. The included canonical body at `resources/modules/references/agents-md-speccy-conventions.md` is documentation prose with no machine-actionable surface. The upsert logic — replacing from `## Speccy conventions` to the next top-level `##` — rewrites whatever a user pasted into that section verbatim every run, but that is the documented contract (REQ-002 + the preamble line "edits inside this section are overwritten on re-run") and not a security concern. The append-with-heading instruction contains no path-traversal, command-injection, or template-injection vector since the heading and body are hard-coded literals expanded at MiniJinja render time from in-repo source. Nothing security-relevant to block.
</review>

<review date="2026-05-27T21:49:05Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style criteria pass. The canonical edit is in `resources/modules/phases/speccy-init.md` (never the ejected files), following AGENTS.md's editing rule. Naming is consistent (state matrix terminology, `upsert` verb, `## Speccy conventions` heading). The `{% include "modules/references/agents-md-speccy-conventions.md" %}` directive reuses the project's existing template pipeline rather than inventing a parallel helper. No suppression annotations, no dead code, no orphaned references. The double blank line at the include boundary in the ejected `.claude/skills/speccy-init/SKILL.md` is a pre-existing pattern across six other ejected skill files and is not a new violation introduced by this diff.
</review>
