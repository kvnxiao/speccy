---
spec: SPEC-0051
task: T-004
generated_at: 2026-05-27T22:00:00Z
---

<implementer date="2026-05-27T22:00:00Z" model="claude-opus-4-7[1m]/low" round="1">

- Completed: Updated the `description:` frontmatter in both
  `resources/agents/.claude/skills/speccy-init/SKILL.md.tmpl` and
  `resources/agents/.agents/skills/speccy-init/SKILL.md.tmpl` to name
  both seeded `AGENTS.md` sections (`## Product north star` with the
  freeze-on-first-write adaptive flow, and `## Speccy conventions` as
  always-upsert canonical boilerplate covering when-to-use-which-skill,
  the dev loop, test hygiene, commit hygiene, and the CI-gate
  suggestion). Preserved the existing trigger phrases ("set up
  speccy", "init speccy", "add speccy to this repo", "start a
  spec-driven workflow somewhere that has no `.speccy/` yet"),
  precondition ("Requires: no preconditions"), and Do NOT clause.
  Ran `just reeject` — both `.claude/skills/speccy-init/SKILL.md`
  and `.agents/skills/speccy-init/SKILL.md` overwritten with the
  new frontmatter and the inlined canonical conventions body (from
  T-001 via the include directive added in T-002).
- Undone: Nothing in this task's scope. T-005 (hygiene-gate task)
  is the next pending task — its commands ran clean as part of this
  task's exit-transition gate, but T-005 remains pending state per
  the task-list division of labor.
- Hygiene checks:
  - `cargo test --workspace`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo deny check`: exit 0
- Evidence: paper trail at
  `.speccy/specs/0051-init-agents-md-bootstrap/journal/T-004.md`
  (this file).
  Roll call for CHKs under REQ-006:
  - CHK-009 (ejected packs carry adaptive flow, upsert step, canonical
    body after reeject): demonstrated → both
    `.claude/skills/speccy-init/SKILL.md` and
    `.agents/skills/speccy-init/SKILL.md` were overwritten by
    `just reeject`; `grep -c "Speccy conventions"` returns 9 in each
    file, confirming the canonical body expanded inline via the
    `{% include %}` directive.
  - CHK-010 (Rust CLI surface unchanged): demonstrated →
    `git diff --stat -- "speccy-cli/**/*.rs" "speccy-core/**/*.rs"
    "speccy-*/**/*.rs"` returns empty. Modified files are limited to
    the two wrapper templates, the two ejected skill bodies, and
    `TASKS.md`.
- Discovered issues: None.
- Procedural compliance: Followed `speccy-work` recipe. Clean-tree
  gate satisfied at entry; first-attempt shape (no prior journal for
  T-004); flipped state pending → in-progress → in-review; appended
  this `<implementer round="1">` block to the per-task journal.
  No edits to ejected files were made directly — wrapper templates
  edited under `resources/agents/`, then `just reeject` propagated.

</implementer>

<review date="2026-05-27T21:58:22Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-004 satisfies REQ-006 cleanly. Both wrapper templates now name both seeded sections (`## Product north star` freeze-on-first-write + `## Speccy conventions` always-upsert) while preserving the existing trigger phrases, precondition ("Requires: no preconditions"), and Do NOT clause. The two wrappers are byte-identical in their descriptions, consistent with the SPEC assumption that Claude Code and Codex packs seed identical conventions content. `just reeject` propagated the description update into both ejected skill bodies (CHK-009 scenario satisfied — each ejected body contains the adaptive north-star flow from T-003, the conventions upsert step from T-002, and the canonical body expanded via `{% include %}` from T-001, with 9 occurrences of "Speccy conventions" in each ejected file). CHK-010 holds — `git diff --stat` scoped to `speccy-cli/**/*.rs`, `speccy-core/**/*.rs`, `speccy-*/**/*.rs` returns empty. Diff is surgical: 2 wrappers, 2 ejected bodies, 1 TASKS.md state flip. No scope creep, no Rust CLI changes, no platform-specific CI wiring snuck in, no skill split, no marker comments. Open questions all remained resolved as the SPEC declared.
</review>

<review date="2026-05-27T21:58:22Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
T-004 is a wrapper-frontmatter + reeject task with zero behavioral code in scope. Both CHKs (CHK-009, CHK-010) are explicitly "Reviewer judgment confirms..." scenarios, so judgment-only would have been a defensible label; the implementer instead labelled both `demonstrated` and supplied reproducible verification (grep "Speccy conventions" = 9 in each ejected SKILL.md; `git diff --stat -- 'speccy-cli/**/*.rs' 'speccy-core/**/*.rs' 'speccy-*/**/*.rs'` empty). Both checks reproduce against the working tree. Adaptive/Socratic markers, upsert/heading-boundary/state-matrix markers, and the conventions subsections ("When to use which skill", "The dev loop", "Test hygiene", "Commit hygiene", "CI gate") all expand into the ejected bodies, confirming the `{% include %}` directive runs end-to-end. No project tests were added or modified — nothing in scope to be vacuous, mock-only, or snapshot-baked. Evidence roll call covers both CHKs under REQ-006 with proper labels and citations.
</review>

<review date="2026-05-27T21:58:22Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
Prose-only edit to two `description:` frontmatter lines (and their ejected copies) under `resources/agents/.{claude,agents}/skills/speccy-init/SKILL.md.tmpl`. No code, no CLI surface, no auth, input-handling, secret, crypto, or dependency changes. YAML single-quoted scalar is well-formed (no embedded single quotes); description contains only static prose with generic identifiers and code-fenced section names — no real repo IDs or sensitive data leak into the shipped packs. Nothing in security scope to flag.
</review>

<review date="2026-05-27T21:58:22Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
The diff touches exactly four files — two wrapper templates and two ejected skill bodies — changing only the `description:` frontmatter field in each. The new description text preserves all required trigger phrases, the precondition, and the Do NOT clause verbatim. No lint suppressions, dead code, naming drift, or parallel helpers introduced. The "No newline at end of file" condition on the `.tmpl` files pre-existed this diff and is not introduced by T-004.
</review>
