---
spec: SPEC-0060
task: T-001
generated_at: 2026-06-12T04:54:37Z
---

<implementer date="2026-06-12T04:54:37Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-001; suggested files `speccy-core/src/parse/journal_xml/mod.rs`, `speccy-cli/src/journal_show.rs`, `speccy-cli/src/context.rs`, `speccy-cli/src/context_output.rs`, `speccy-cli/tests/context.rs`; plus the immediate neighbours `speccy-core/src/parse/mod.rs` re-export hub and `speccy-cli/src/journal_show_output.rs`). Decisions — write-fresh: the `latest_round(&[JournalEntry]) -> Option<u32>` free function in `journal_xml/mod.rs`; searched the module for an existing free function computing the max round and found only the inline expression at `journal_show.rs:209` (an expression, not a reusable symbol), so this is DEC-001's single shared definition. It reuses `JournalEntry::round()` internally. Reuse-as-is: `to_json_journal_block` (already imported at `context.rs:46`) for the block projection, and the new `latest_round` at both call sites. Test fixtures — reuse-as-is `journal_two_rounds` for the CHK-001 rewrite; write-fresh `journal_single_round` for CHK-003 (the existing `journal_one_round` carries one block, too thin to prove every block of the latest round survives). Only one new top-level symbol introduced (`latest_round`).
- Completed: Sliced the context bundle's `journal.blocks` to the latest round only (REQ-001). Added `pub fn latest_round` to `speccy-core/src/parse/journal_xml/mod.rs` beside `JournalEntry::round()` and re-exported it from `parse/mod.rs`. Replaced the inline `doc.entries.iter().map(JournalEntry::round).max()` at `speccy-cli/src/journal_show.rs:209` with `latest_round(&doc.entries)` — byte-identical resolution, existing journal-show tests stay green. In `build_journal` (`context.rs`) resolve the highest round via the helper and project only entries whose `round` equals it through `to_json_journal_block`, preserving file order; a zero-entry journal yields `None` → empty `blocks` with `exists: true`, and the absent-journal early return (`exists: false`, exit 0) is unchanged. Updated the `BundleJournal` struct and `blocks` field doc-comments in `context_output.rs` (they claimed "every entry across all rounds"). The text renderer iterates `bundle.journal.blocks` unchanged, so it renders the latest-round-only content with no further edit.
- Undone: the `prior_rounds` index (T-002/REQ-002), prompt-module rewording plus re-eject (T-003/REQ-003), and the ARCHITECTURE.md envelope doc (T-004/REQ-004) are out of scope for this task and left to their owning tasks. Nothing from T-001's scope is deferred.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites green, including the new `bundle_inlines_only_latest_round_blocks`, `bundle_inlines_single_round_journal_in_full`, and `latest_round_resolves_highest_and_handles_empty`). `cargo clippy --workspace --all-targets --all-features -- -D warnings` exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0 (no dependency change).
- Evidence: no demonstrated red/green paper trail — the change is a deterministic projection slice fully gated by integration tests, so no `evidence/T-001.md` file. Roll call for the CHKs under REQ-001:
  - CHK-001 (two-round fixture inlines only round-2 blocks, full bodies, round-1 markers absent): hygiene → `bundle_inlines_only_latest_round_blocks` in `speccy-cli/tests/context.rs` covers it under `cargo test --workspace` (asserts 3 round-2 blocks, every block `round == 2` with non-empty body, round-1 body markers absent from the inlined block bodies). The 8→3 behaviour change is the rewrite of the superseded `bundle_inlines_full_journal_with_all_blocks_and_rounds` test, whose `blocks.len() == 8` / `rounds == [1,1,1,1,1,2,2,2]` assertions encoded the SPEC-0056 full-inline contract this SPEC reverses.
  - CHK-002 (absent journal → `exists: false`, empty `blocks`, exit 0): hygiene → `bundle_journal_absent_yields_explicit_empty_marker_and_success` and `binary_journal_absent_exits_zero` in `speccy-cli/tests/context.rs` (kept unchanged) cover it under `cargo test --workspace`.
  - CHK-003 (single-round journal inlines every block in full): hygiene → `bundle_inlines_single_round_journal_in_full` in `speccy-cli/tests/context.rs` covers it (3 round-1 blocks, all `round == 1`, all body markers present).
  - Done-when parity bullet (context's latest round equals `journal show --round latest`): proved structurally by the single shared `latest_round` symbol both call sites invoke (DEC-001), plus the `latest_round_resolves_highest_and_handles_empty` unit test in `speccy-core` and the unchanged journal-show suite. No CHK is judgment-only.
- Discovered issues: none.
- Procedural compliance: state flipped pending → in-progress → in-review via `speccy task transition` (never by editing the `state` attribute in TASKS.md). This handoff is appended to `journal/T-001.md` via `speccy journal append`; no implementer-note element was written into TASKS.md. No shipped skill bodies under `resources/` required edits during this task — no stale-instruction friction was hit.
</implementer>
<review date="2026-06-12T05:14:43Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns. This is an internal CLI/parser projection refactor with no auth, network, secret, or crypto surface. The change narrows the bundle's `journal.blocks` from all-rounds to latest-round-only (`speccy-cli/src/context.rs:323-334`), reducing the inlined data surface rather than widening it; prior-round bodies stay reachable only via the pre-existing `journal show --round N` disk-read path, no new disclosure path. The `bundle_inlines_only_latest_round_blocks` test affirmatively guards against over-disclosure by asserting round-1 body markers do not survive the slice (`speccy-cli/tests/context.rs`). `latest_round` (`speccy-core/src/parse/journal_xml/mod.rs:135`) is `Option`-returning with no panic/slice-index path; the zero-entry case yields empty `blocks` with no leak. No new dependencies, no error-message or logging changes.
</review>
<review date="2026-06-12T05:15:11Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-001 satisfied. All four done-when bullets map to the diff: (1) two-round fixture -> latest-round-only slice via `build_journal`'s `highest.is_some_and(|r| entry.round() == r)` filter (context.rs:330), gated by `bundle_inlines_only_latest_round_blocks` (3 round-2 blocks, every block round==2 with non-empty body, round-1 markers absent from the serialized blocks bodies); (2) single-round -> all blocks kept, gated by the new `bundle_inlines_single_round_journal_in_full`; (3) absent journal -> `exists: false`, empty blocks, exit 0 unchanged (existing `bundle_journal_absent_yields_explicit_empty_marker_and_success` and `binary_journal_absent_exits_zero` kept); (4) anti-drift parity -> both context.rs and journal_show.rs:210 now call the shared `latest_round(&doc.entries)` (DEC-001), unit-tested by `latest_round_resolves_highest_and_handles_empty`. Zero-entry-with-exists:true edge handled (`latest_round` -> None -> empty blocks). CHK-001/002/003 all covered. Non-goals respected: no `prior_rounds` index (T-002), no prompt reword (T-003), no ARCHITECTURE doc (T-004), `schema_version` stays 1, `blocks` field name kept, additive change only. The resolved open question (a.) is honored -- the CHK-001 count of 3 derives from the fixture's round-2 slice (1 implementer + 2 reviews) rather than being a contract value. Changelog "supersedes SPEC-0056 full-inline" intent reflected; the rewrite documents reversing the old `blocks.len()==8` / `rounds==[1,1,1,1,1,2,2,2]` assertions.
</review>
<review date="2026-06-12T05:15:27Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests faithfully drive all three REQ-001 scenarios against real code paths; no mocks of the system under test.

CHK-001 (`bundle_inlines_only_latest_round_blocks`, `speccy-cli/tests/context.rs:806`) drives `speccy context --json` over the real `journal_two_rounds` fixture and asserts `blocks.len() == 3`, every block `round == 2` with non-empty body, kind counts (1 impl + 2 review + 0 blockers), all three round-2 markers present, and — the negative path — all five round-1 markers (`IMPL_R1_MARKER`, both R1 review markers, `REVIEW_R1_SECURITY_MARKER`, `BLOCKERS_R1_MARKER`) absent from the concatenated block bodies. Markers live only in element inner-text → the `body` field, so asserting over concatenated bodies is a complete read; a regression to the SPEC-0056 full-inline behaviour fails on `len()` and the absent-marker assertions. CHK-003 (`bundle_inlines_single_round_journal_in_full:836`) uses a purpose-built three-block single-round fixture (the implementer correctly rejected the one-block `journal_one_round` as too thin to distinguish "all blocks" from "first block") and asserts every block survives with full bodies. CHK-002 (`bundle_journal_absent_yields_explicit_empty_marker_and_success:998` and `binary_journal_absent_exits_zero:1038`) kept intact, asserting `exists: false`, empty blocks, exit 0.

The DEC-001 shared-resolver done-when is gated, not merely asserted: `journal_show.rs:209` is rewired to call the new `latest_round` helper, and the existing `round_latest_verdict_blocking_returns_single_block` (`tests/journal_show.rs:94`) drives `--round latest` and asserts `latest_round == 2` over the same two-round shape, while `latest_round_resolves_highest_and_handles_empty` unit-tests `Some(2)`/`None`. A wrong helper (e.g. min-not-max) breaks both views.

Evidence roll call is complete: every CHK under REQ-001 appears labelled `hygiene` with a specific named test cite, no bare "suite passes" claim. The "no red/green paper trail" framing is plausible — a deterministic projection slice with no separable red phase beyond the test rewrite, which is itself captured. Fixtures use fixed timestamps and isolated per-test workspaces; no ordering/time/parallel-flake exposure.
</review>
<review date="2026-06-12T05:15:35Z" model="claude-opus-4-8[1m]" persona="correctness" verdict="pass" round="1">
No correctness defect found. The latest-round slice in `build_journal`
(`speccy-cli/src/context.rs:328-334`) is sound: `highest.is_some_and(|r| entry.round() == r)`
keeps exactly the highest-round entries; a zero-entry journal yields `highest == None` and
hence empty `blocks` with `exists: true`, and the absent-file early return
(`context.rs:305-313`) preserves `exists: false` and exit 0. No off-by-one or inverted
condition. The extracted `latest_round` helper
(`speccy-core/src/parse/journal_xml/mod.rs:137`) is a verbatim copy of the original
`.map(JournalEntry::round).max()` expression returning `Option<u32>` over `round(): u32`
— no truncation or overflow. In `journal_show.rs:210-211` the free-function call resolves
before the local `latest_round` shadow on the next line takes effect, so there is no
name-resolution hazard and resolution stays byte-identical. The context (`is_some_and`)
and journal-show (`is_none_or`) filters diverge only in the empty-journal case, where both
emit empty output, so the shared-round parity holds. `cargo test` for the context,
journal_show, and core suites all green.
</review>
<review date="2026-06-12T05:15:35Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
No style violations found.

Checked: naming (`latest_round` beside `JournalEntry::round()` as specified), `#[must_use]` annotation with message per `rust-code-quality.md`, re-export placement in `parse/mod.rs`, import ordering in both CLI files, `is_some_and` idiom in `context.rs` filter, doc-comment accuracy in `context_output.rs`, and test quality. All four hygiene checks pass (clippy, fmt, test, deny). The `latest_round` function name shadows a pre-existing local variable in `journal_show.rs:211` after import on line 38, but Clippy does not flag it and the function call on line 210 completes before the local binding — pre-existing structural issue, not introduced by this diff. Reuse survey present and accurate: `to_json_journal_block` and `journal_two_rounds` reused as-is; `latest_round` is the single new symbol, correctly identified. Tests are non-vacuous: round-2 body-marker absence, round-1 marker exclusion, and single-round all-blocks-survive all gate real invariants.
</review>
