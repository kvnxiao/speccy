---
spec: SPEC-0060
task: T-002
generated_at: 2026-06-12T05:31:21Z
---

<implementer date="2026-06-12T05:31:21Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: round-1 full area-map (covered REQ-002, suggested files, immediate module). Reuse-as-is: `latest_round` (`speccy_core::parse`, already imported in `context.rs`) for the highest-round resolution shared with `journal show --round latest` (DEC-001). Extend: `BundleJournal` (`context_output.rs`) gains a `prior_rounds` field; `build_journal` (`context.rs`) gains the partition; the text renderer's journal section. Write-fresh (per DEC-004, a separate struct keeps `JsonJournalBlock`'s `body` invariant intact for `journal show`): `JsonJournalBlockAttrs` and `to_json_journal_block_attrs`, placed beside `JsonJournalBlock` / `to_json_journal_block` in `journal_show_output.rs` and mirroring their attribute mapping. Per-symbol floor: both new top-level symbols are write-fresh, mandated by the task body and DEC-004; searched `journal_show_output.rs` for an existing attributes-only projection — none exists (the only sibling is the full-block `to_json_journal_block`).
- Completed: Added `JsonJournalBlockAttrs` (no `body` field; `block`/`date`/`round` plus `skip_serializing_if` optionals `model`/`persona`/`verdict`) and `to_json_journal_block_attrs` to `speccy-cli/src/journal_show_output.rs`. Added `prior_rounds: Vec<JsonJournalBlockAttrs>` to `BundleJournal` (`context_output.rs`) with a doc-comment stating the total/disjoint partition. In `build_journal` (`context.rs`) projected every entry whose round is strictly below the highest through `to_json_journal_block_attrs` into `prior_rounds` in file order; the absent-journal early return carries `prior_rounds: Vec::new()`. Extended the text journal section to render a `### Prior rounds (index)` block (one compact line per attrs entry: block type, round, persona/verdict where present) after the latest-round bodies. To stay under the 100-line clippy ceiling that the addition would have breached, extracted the whole journal section into a `render_journal` helper (behaviour-preserving; reduces `render_text`'s length rather than adding indirection). Added three tests (CHK-004/005/006) and an `invoke_text` helper to `speccy-cli/tests/context.rs`.
- Undone: Nothing in scope deferred. ARCHITECTURE.md was deliberately not touched — DEC-002's "documented in ARCHITECTURE.md" concerns the `blocks` content-semantics change (T-001's REQ-001 scope, which also did not edit it); T-002's suggested files are the four source/test files only, and the `prior_rounds` field is self-documented in `context_output.rs`.
- Hygiene checks: `cargo test --workspace` exit 0 (72 "test result: ok" lines, 0 failures; context suite 20 passed). `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0. `cargo +nightly fmt --all --check` exit 0. `cargo deny check` exit 0 (advisories/bans/licenses/sources ok).
- Evidence: paper trail at `.speccy/specs/0060-context-journal-slicing/evidence/T-002.md`. Roll call for CHKs under REQ-002:
  - CHK-004 (two-round fixture: prior_rounds has one attrs entry per round-1 block in file order, security review carries persona+verdict, no body key, no round-1 body marker): demonstrated -> evidence Scenario 1 covers the red (`.get("prior_rounds")` -> None, expect panics) / green (five entries in file order, no `body` key, security `verdict="blocking"`, no round-1 markers) pair via `prior_rounds_indexes_pre_latest_blocks_without_bodies`.
  - CHK-005 (single-round and absent journals yield prior_rounds: []): demonstrated -> evidence Scenario 2 covers the red/green pair via `prior_rounds_is_empty_for_single_round_and_absent_journals` (both single-round and absent sub-cases asserted in one test).
  - CHK-006 (text: round-2 bodies then a prior-rounds index naming each round-1 block's type/round/persona/verdict, no round-1 body content): demonstrated -> evidence Scenario 3 covers the red (no index header) / green (index after bodies, security line carries persona+verdict, no round-1 markers) pair via `text_journal_renders_latest_bodies_then_prior_rounds_index`.
  - Partition totality/disjointness (REQ-002 done-when "every entry appears in exactly one of blocks/prior_rounds"): demonstrated jointly -> the existing `bundle_inlines_only_latest_round_blocks` asserts the 3 round-2 blocks land in `blocks`; the new CHK-004 test asserts the 5 round-1 blocks land in `prior_rounds`; together they account for all 8 fixture entries with none dropped or duplicated.
- Discovered issues: none. The `bundle_size_scales_with_task_not_spec` test (REQ-007 invariant from SPEC-0056) still passes — `prior_rounds` grows with round count, not task count, so the per-task bound is unaffected.
- Procedural compliance: no shipped-skill or reference friction encountered; no `resources/` edits needed. Followed the first-attempt recipe (clean tree, journal absent). Captured a genuine red baseline by stashing only the three `src` files and running the new tests (runtime panics, not compile errors, since the tests probe JSON via `.get()`).
</implementer>
<review date="2026-06-12T05:32:46Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security-relevant surface in this slice. `speccy context` reads
repo-local journal files only — no auth/authz boundary, no caller-identity
decision, no network, no secrets, no crypto, no shared-state race.

Checked the one security-adjacent angle (sensitive-data exposure through the
new index): `JsonJournalBlockAttrs` (`speccy-cli/src/journal_show_output.rs:155`)
has no `body` field at all and the index projection
`to_json_journal_block_attrs` maps only `block`/`date`/`round`/`model`/
`persona`/`verdict` — so prior-round prose is dropped, not merely skip-empty.
The text renderer's prior-rounds loop (`speccy-cli/src/context.rs:535`) emits
the same attribute set with no body. Tests assert the absence structurally:
`prior_rounds_indexes_pre_latest_blocks_without_bodies` checks
`entry.get("body").is_none()` and that no round-1 body marker survives the
serialized subtree. No new dependencies. `block` is a `&'static str` literal,
not attacker-influenced — no injection sink.
</review>
<review date="2026-06-12T05:33:00Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-002 delivered: every <done-when> maps to observable behaviour in the diff. `build_journal` (context.rs) partitions entries by `entry.round() == highest` -> `blocks` (full) vs `round() < highest` -> `prior_rounds` (attrs-only), via the shared `latest_round` resolver (DEC-001), so the partition is total and disjoint. `JsonJournalBlockAttrs` (journal_show_output.rs) has no `body` field at all and `to_json_journal_block_attrs` maps the per-block-type optionals exactly as the contract names them — implementer -> model only, review -> model+persona+verdict, blockers -> none (DEC-004). Text `render_journal` emits the latest bodies then a `### Prior rounds (index)` section with no prior-round prose, honoring DEC-003. Non-goals respected: no `journal show` behaviour change (only a sibling projection added), no schema_version bump, no field rename, no orchestrate ladder, no VET handling. Goals served: the retry-implementer/orchestrator user still gets the latest round in one read plus a bounded history index. CHK-004/005/006 tests assert the two-round file-order index, single-round + absent `[]`, and the text index placement. Minor coverage note (a tests-persona matter, not a business blocker): the done-when's "zero-entry journal -> prior_rounds: []" case has no dedicated test, though `latest_round` returning None on an empty slice makes both arrays empty by construction; single-round and absent are tested.
</review>
<review date="2026-06-12T05:33:14Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Reviewed the T-002 prior-rounds-index slice (uncommitted working-tree diff over `journal_show_output.rs`, `context_output.rs`, `context.rs`, `tests/context.rs`).

Partition correctness: `build_journal` (`context.rs:340-356`) splits entries via two independent filters over the same `doc.entries.iter()` — `blocks` keeps `entry.round() == highest`, `prior_rounds` keeps `entry.round() < highest`. With `latest_round` returning the `max` over `u32` rounds, every entry has `round() <= highest`, so each lands in exactly one partition; the two are total and disjoint with no drop or duplication. When `highest` is `None` (zero entries) both `is_some_and` filters are false, so both partitions are empty — matching the documented zero-entry/single-round/absent behavior. The absent-journal early return (`context.rs:328`) also sets `prior_rounds: Vec::new()`.

File order: both filters walk `doc.entries.iter()`, preserving file order within each partition.

Attrs projection: `to_json_journal_block_attrs` (`journal_show_output.rs:307+`) mirrors `to_json_journal_block`'s optional mapping exactly — implementer carries `model` only, review carries `model`/`persona`/`verdict`, blockers carries none. `round: u32` matches `JournalEntry::round()`'s type. The separate struct omits `body` from serialization entirely (no key), as required.

Text renderer: `render_journal` extracts the prior latest-round loop verbatim (behavior-preserving), then gates the prior-rounds index on `!prior_rounds.is_empty()` and emits one line per attrs entry with persona/verdict where present. No prior-round body content rendered. No control-flow defect on the absent path (early return) or the empty-index path.

No off-by-one, no Option/Result mishandling, no resource concerns, no overflow on the `u32` round comparisons.
</review>
<review date="2026-06-12T05:33:24Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
All three scenarios (CHK-004/005/006) are backed by behavioral integration tests in `speccy-cli/tests/context.rs` that drive the real `to_json_journal_block_attrs` projection and `render_journal` text path through `build_journal`, not mocks. CHK-004 (`prior_rounds_indexes_pre_latest_blocks_without_bodies`) asserts the five round-1 attrs entries in file order, every `round==1`, `entry.get("body").is_none()` (the key-absent invariant, not empty-string), the security review's `persona`/`verdict`, and that none of the five round-1 body markers survive in the serialized subtree. CHK-005 covers single-round and absent journals both yielding `[]`. CHK-006 asserts round-2 bodies render in full, the index header follows them (ordering), the round-1 security index line carries `persona=security verdict=blocking`, and no round-1 markers leak. Falsification holds: a `<=` partition or a body-emitting attrs struct or a wrong-side classification each break a specific assertion. The disjoint/total partition done-when is jointly demonstrated by `bundle_inlines_only_latest_round_blocks` (3 round-2 → blocks, round-1 markers asserted absent) plus CHK-004 (5 round-1 → prior_rounds) = all 8 fixture entries, none dropped or duplicated. Evidence roll call names all CHKs under REQ-002 as `demonstrated` with specific test names; red-phase line citations (`context.rs:1073/1157/1221`) and expect-messages match the diff verbatim, and the evidence command (`cargo test -p speccy-cli --test context <name>`) is per-test scoped, distinct from the workspace hygiene run. Tests use per-test `Workspace::new()` with no time/order/global-state dependency, so no parallel-run flake.
</review>
<review date="2026-06-12T05:33:56Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
No style violations found.

Checked naming (`JsonJournalBlockAttrs` / `to_json_journal_block_attrs` placed beside `JsonJournalBlock` / `to_json_journal_block` in `journal_show_output.rs`), `#[must_use]` with message on both projection functions per `rust-code-quality.md`, import ordering in `context.rs` (crate-local alphabetical then external alphabetical, new `to_json_journal_block_attrs` follows `to_json_journal_block` correctly), `skip_serializing_if` on all three optionals mirroring the existing full-block struct, doc-comment accuracy on `BundleJournal` and `prior_rounds` fields, and test quality. No `#[allow(...)]` suppressions. No dead code introduced. The `render_journal` helper extraction is behaviour-preserving and reduces `render_text`'s length rather than adding unnecessary indirection. Reuse survey present and accurate: `latest_round` (T-001) and `to_json_journal_block` reused as-is; `JsonJournalBlockAttrs` / `to_json_journal_block_attrs` are write-fresh per DEC-004. Tests (`prior_rounds_indexes_pre_latest_blocks_without_bodies`, `prior_rounds_is_empty_for_single_round_and_absent_journals`, `text_journal_renders_latest_bodies_then_prior_rounds_index`) gate real invariants: block-kind ordering, no `body` key in attrs entries, round-1 body markers absent from serialised `prior_rounds`, index renders after latest-round bodies in text output. Hygiene checks pass (cargo test/clippy/fmt/deny all exit 0 per implementer).
</review>
