---
spec: SPEC-0060
task: T-003
generated_at: 2026-06-12T05:38:22Z
---

<implementer date="2026-06-12T05:38:22Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Full area-map (round 1). Reuse-as-is — the cli-stamps.md include partial and the existing `journal show <selector> --round N [--block <type>]` drill-down syntax (lifted verbatim from references/retry-shape.md, which the SPEC says needs no change) are reused as the canonical drill-down phrasing. Extend — the entry-precondition sentence in phases/speccy-work.md and the reviewer-spawn prompt in partials/review-fanout.md were extended in place to describe the new sliced shape rather than rewritten. Write-fresh — none; no new top-level symbol introduced (prose-only task). Searched resources/modules for any remaining "full per-task journal" / "the full journal" / "all rounds" claim and found none beyond the two named modules.
- Completed: Reworded the two source prompt modules for the sliced journal section. (1) resources/modules/phases/speccy-work.md: the entry-precondition bundle description now reads "the latest-round journal blocks inline with prior rounds as an attributes-only index" instead of "the full per-task journal"; the retry-branch reads now target the latest-round inline implementer / blockers blocks and add a prior-rounds drill-down bullet naming `speccy journal show SPEC-NNNN/T-NNN --round N [--block <type>]`. (2) resources/modules/skills/partials/review-fanout.md: the reviewer-spawn prompt now describes latest-round inline blocks plus an attributes-only prior-rounds index and names the same `journal show --round N` drill-down, dropping the "full per-task journal" claim. references/retry-shape.md left unchanged per SPEC. Ran `just reeject`; the four ejected outputs (.claude/agents/speccy-work.md, .codex/agents/speccy-work.toml, and the review SKILL.md in .claude + .agents that include review-fanout) regenerated to match source.
- Undone: Nothing in scope. T-004 (REQ-004) remains pending.
- Hygiene checks: cargo test --workspace exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings exit 0; cargo +nightly fmt --all --check exit 0; cargo deny check exit 0.
- Evidence: No evidence paper-trail file — both CHKs under REQ-003 are non-code (one mechanical, one review-verified). Roll call for CHKs under REQ-003:
  - CHK-007 (re-eject leaves no diff under the three pack roots): hygiene → demonstrated mechanically. `just reeject` ran twice; the second run reported "0 created, 0 overwritten" for both hosts, proving the ejected packs are in sync with the reworded source. `git status --porcelain` shows only the two source modules, their four ejected outputs, and TASKS.md — no stale/unsynced ejected file.
  - CHK-008 (both modules describe latest-round inline + prior-rounds index + journal show --round N drill-down, no full-journal claim): judgment-only → review-verified per AGENTS.md (prose is not substring-gated by tests). Self-review confirms: grep over .claude finds the new wording in both ejected files and zero "full per-task journal" occurrences; the source modules name the --round N drill-down and the attributes-only index in both the precondition/retry-branch (work) and the spawn prompt (review).
- Discovered issues: None.
- Procedural compliance: Edited only source modules under resources/; never hand-edited ejected .claude/ / .agents/ / .codex/ files (regenerated via `just reeject`). Followed the speccy-work recipe: resolved via `speccy context`, first-attempt shape (journal absent), clean-tree gate passed, state flipped through `speccy task transition` verbs only.
</implementer>
<review date="2026-06-12T05:39:55Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface in this slice. Prose-only reword of two source
prompt modules plus their mechanical re-eject; no code, no
auth/authz boundary, no parser, deserializer, or new trust boundary.
The drill-down phrasing `speccy journal show SPEC-NNNN/T-NNN --round N
[--block <type>]` uses fictional placeholders per AGENTS.md and the
underlying `journal show` CLI is out of scope/unchanged, so no
command-injection or argument-smuggling vector is introduced. The
change reduces (not widens) journal content inlined into agent
context — prior rounds drop to an attributes-only index (`round`,
`block`, `persona`, `verdict`), all non-sensitive metadata; no
secret or sensitive-data exposure. No dependency, crypto, or logging
changes. Verified the ejected packs mirror the source edits one-to-one
across `.claude`/`.agents`/`.codex` and no `full per-task journal` /
`all rounds` claim survives in any pack root.
</review>
<review date="2026-06-12T05:40:00Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
All prose changes match the stated intent and SPEC conventions. Source modules updated correctly; all four ejected outputs carry identical changes. No "full per-task journal" / "full journal" / "all rounds" claims remain in source or ejected pack roots. Placeholder identifiers (`SPEC-NNNN/T-NNN`) used consistently throughout. Reuse survey present and accurate (drill-down phrasing lifted verbatim from retry-shape.md). No new code — no lints, naming, or dead-code concerns apply. Ejected files regenerated via `just reeject`, not hand-edited.
</review>
<review date="2026-06-12T05:40:08Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass — no correctness defect. Prose-only rewording of two source prompt modules plus re-eject; no logic, control flow, error handling, concurrency, or resource paths in scope. Verified the in-scope mechanical invariant (CHK-007, ejected packs in sync with reworded source): the four ejected outputs — `.claude/agents/speccy-work.md`, `.codex/agents/speccy-work.toml`, and `.claude/`+`.agents/` `speccy-review/SKILL.md` — each carry verbatim the new wording from `resources/modules/phases/speccy-work.md` and `resources/modules/skills/partials/review-fanout.md`. No stale "full per-task journal" / "all rounds" claim survives in `resources/modules` or under the three ejected pack roots. The `review-fanout.md` partial has a single include callsite (`resources/modules/skills/speccy-review.md`), and both its ejects plus both `speccy-work` ejects are regenerated, so no source callsite was left un-ejected.
</review>
<review date="2026-06-12T05:40:23Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
PASS. REQ-003's four done-when bullets all map to the diff. (1) No "full per-task journal" / "all rounds" claim survives in resources/modules, .claude, .agents, or .codex (grep: zero matches). (2) speccy-work.md retry branch now reads the latest-round implementer and blockers blocks described as "inlined in the bundle's journal section" and adds a prior-rounds drill-down naming `speccy journal show SPEC-NNNN/T-NNN --round N [--block type]` (resources/modules/phases/speccy-work.md retry branch; ejected to .claude/agents/speccy-work.md:135 and .codex/agents/speccy-work.toml:134). (3) review-fanout.md describes prior rounds as an attributes-only index (round, block, persona, verdict) with the same `journal show --round N` drill-down (resources/modules/skills/partials/review-fanout.md; ejected to .claude + .agents speccy-review/SKILL.md:118). (4) Re-eject is in sync: the four ejected outputs already carry the new wording with correct host-path templating (Claude .claude/speccy-references, Codex .agents/speccy-references) and the bundle's consistency check reports status ok. retry-shape.md correctly left unchanged. Scope is exactly the two source modules plus their four ejected outputs — no grammar change, no escalation ladder, no schema bump (all non-goals respected). Serves the retry-round implementer / reviewer by stating what is inline vs. how to drill in. Placeholder IDs keep with shipped-template convention; no real-identifier leak.
</review>
<review date="2026-06-12T05:40:31Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
Pass. Prose-only reword slice with no project tests in scope, and none owed: CHK-008's SPEC scenario explicitly defers to review ("prose content is not substring-gated by tests per AGENTS.md"), and substring-gating curated prose is itself forbidden by AGENTS.md. Evidence roll call covers both CHKs under REQ-003 with valid labels — CHK-007 `hygiene` citing the re-runnable scoped command (`just reeject` + `git status --porcelain` over the three pack roots, not the workspace hygiene-table run), CHK-008 `judgment-only` with a label the SPEC scenario itself mandates. Verified directly against the dirty tree rather than trusting the field: `resources/modules/phases/speccy-work.md` and `resources/modules/skills/partials/review-fanout.md` carry the latest-round-inline + attributes-only prior-rounds index shape and the `speccy journal show SPEC-NNNN/T-NNN --round N [--block <type>]` drill-down; grep over `resources/modules` and over `.claude`/`.agents`/`.codex` finds zero surviving "full per-task journal"/"all rounds" claims; the four ejected outputs diff one-to-one with the source rewording, consistent with the implementer's "0 created, 0 overwritten" re-eject claim (CHK-007). No fabrication pattern applies — no test-runner output is claimed, and the mechanical evidence matches the diff I inspected.
</review>
