---
spec: SPEC-0018
spec_hash_at_generation: 27a6af34ed4492207333fecdb1978a7179b70353b28624aaa279c1a28f8974f5
generated_at: 2026-05-17T17:37:23Z
---

# Tasks: SPEC-0018 Remove check execution

## Phase 1: Scenario parser bridge

<tasks spec="SPEC-0018">

<task id="T-001" state="completed" covers="REQ-001">
Add temporary scenario-aware check parsing

- Suggested files: `speccy-core/src/parse/toml_files.rs`, `speccy-core/src/parse/mod.rs`, `speccy-core/src/error.rs`, `speccy-core/tests/fixtures/lint/`, `speccy-core/tests/lint_common/mod.rs`
- Implementer note (session-T001-bridge):
  - Completed: Added `scenario: String` field to `CheckEntry` and a new optional `scenario` field on `RawCheck`. Introduced `lower_check` helper that implements the SPEC-0018 bridge: new-shape path accepts `scenario` verbatim (rejecting empty/whitespace and rejecting co-occurrence with legacy `command`/`prompt` per CHK-id); legacy path derives `scenario` from `proves` (executable) or `prompt` (manual). Every bridge branch is labelled with a `SPEC-0018 BRIDGE` grep marker and a guard test (`bridge_grep_guard_marks_every_legacy_branch_for_t004`) asserts at least 6 such markers so T-004 has a single grep target. Six bullet-mapped unit tests added in the existing `tests` module. Adjacent `CheckEntry` construction sites in `speccy-core/src/exec.rs` (test-only) and `speccy-core/src/parse/cross_ref.rs` (test-only fixture) were updated to populate the new field surgically.
  - Undone: `#[serde(deny_unknown_fields)]` and the spec.toml migration are deferred to T-004 per the bridge plan. Legacy fields (`kind`, `proves`, `payload`) remain populated for callers that T-002/T-003/T-004 retire.
  - Commands run:
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo run --quiet -- check SPEC-0018/T-001`
  - Exit codes:
    - pass
    - pass
    - pass (renders scenario; legacy execution still wired but no command for this manual check)
  - Discovered issues: (none)
  - Procedural compliance: (none)
- Review (tests, pass): T-001's surviving REQ-001 tests in `speccy-core/src/parse/toml_files.rs:347-405` (verbatim scenario, multi-line scenario, empty/whitespace rejection) plus the `rejects_check_with_legacy_{command,prompt,kind,proves}_field` quartet (`toml_files.rs:274-332`) are non-vacuous: they hit `spec_toml` end-to-end through real TOML deserialization, and the empty-scenario assertion pins both the `ParseError::InvalidCheckEntry` variant and the named `check_id`, so a regression that swallowed the error or dropped the CHK-id would fail. The bridge-era bullets (legacy `proves`/`prompt` → derived `scenario`, new+legacy co-occurrence, SPEC-0018 grep guard) are intentionally absent because T-004 removed the bridge; `every_in_tree_spec_toml_parses_under_new_schema` (`speccy-core/tests/in_tree_spec_tomls.rs:33-65`) plus `deny_unknown_fields` on `RawCheck` (`toml_files.rs:82`) cover the post-migration contract. One minor nit, non-blocking: `parses_multiline_scenario_verbatim` (`toml_files.rs:367-384`) only asserts `.contains("Given a workspace")` / `.contains("then it exits 0.")`, which would still pass if the parser silently stripped leading whitespace or collapsed interior newlines — the test name promises "verbatim" but the assertion does not. Consider tightening to an exact-string compare so a future indentation-normalization regression is caught.
- Review (security, pass): No new security surface. Scenario is developer-authored TOML parsed via `toml` + `serde` into a plain `String`; T-001 only reads it verbatim and validates non-empty/whitespace in `lower_check` (`speccy-core/src/parse/toml_files.rs:149`). No `unsafe`, no panics, no secret-handling, no crypto, no auth boundary. Error messages in `ParseError::InvalidCheckEntry` (`speccy-core/src/error.rs:73-82`) name only path + CHK-id (already public dev data). Legacy `command`/`prompt` payload stays reachable in-memory during the bridge but T-001 never spawns it; the broader SPEC-0018 trajectory removes the arbitrary-command-execution sink, which is a net security win. No new deps. File-read goes through `fs-err` + `Utf8Path` with caller-supplied paths — no traversal regression.
- Review (style, pass): T-001's surviving surface in `speccy-core/src/parse/toml_files.rs` is idiomatic for this codebase. `lower_check` (`toml_files.rs:149`) is a narrowly-scoped helper that returns the project's `thiserror`-backed `ParseError::InvalidCheckEntry` rather than panicking, matching the `?`-propagation style used by `spec_toml` / `speccy_toml`. `RawCheck` (`toml_files.rs:81-86`) uses `#[serde(deny_unknown_fields)]` and follows the same `Deserialize`-only-on-Raw / public-types-on-`CheckEntry` split the file established for `RawRequirement` / `RequirementEntry`. No `unwrap()` / `expect()` / `panic!()` / `#[allow(...)]` in production code; tests use `.expect("descriptive message")` per `.claude/rules/rust/rust-testing.md`. Doc comments on `CheckEntry`, `RawCheck`, and `spec_toml`'s `# Errors` section reflect the post-bridge contract. Imports stay in the project's one-per-line nightly-rustfmt ordering. `cargo clippy --workspace --all-targets --all-features -- -D warnings` is clean and no `parse/mod.rs` re-export of the removed `CheckPayload` lingers.
- Review (business, pass): T-001 scopes itself to REQ-001's bridge half (new `scenario` field, legacy `proves`/`prompt` derivation, empty/whitespace error naming the CHK id, co-occurrence rejection, SPEC-0018 grep markers + guard test) and explicitly defers `deny_unknown_fields` plus the in-tree `spec.toml` migration to T-004, which matches the SPEC's "implementation order" step 1 split and the Phase 1/Phase 3 framing in this file. The user stories that depend on this task (implementer reading English scenarios, reviewer-tests comparing against scenario prose) are unblocked once new-shape rows parse, and the SPEC's Migration section explicitly authorizes the temporary bridge being deleted before the final commit lands, which T-004 carries out. No Open Question is silently resolved and no Non-goal is breached.


<task-scenarios>
  - When `spec_toml` parses a `[[checks]]` row with only
    `id = "CHK-001"` and `scenario = "Given ..."`, then it returns a
    `CheckEntry` whose stable id is `CHK-001` and whose scenario text
    is preserved verbatim.
  - When the temporary SPEC-0018 bridge parses a legacy executable
    check with `command` and `proves`, then it derives
    `scenario = old_proves` while keeping the legacy payload available
    only for the still-unmigrated callers that T-002 through T-004
    retire.
  - When the temporary bridge parses a legacy manual check with
    `prompt`, then it derives `scenario = old_prompt`.
  - When a `scenario` field is empty or whitespace-only, then parsing
    fails with `ParseError::InvalidCheckEntry` and the error names the
    containing `CHK-NNN`.
  - When a check declares both a new `scenario` and legacy
    `command`/`prompt` fields during the bridge period, then parsing
    fails rather than guessing which source of truth wins.
  - The bridge code is labelled with a SPEC-0018 removal comment and
    a test or grep guard points T-004 at every remaining legacy branch.
</task-scenarios>
</task>

## Phase 2: Render scenarios, do not execute checks


<task id="T-002" state="completed" covers="REQ-002">
Replace `speccy check` execution with scenario rendering

- Suggested files: `speccy-cli/src/check.rs`, `speccy-cli/tests/check.rs`, `speccy-cli/tests/common/mod.rs`, `speccy-cli/src/main.rs`
- Implementer note (session-T002-render):
  - Completed: Rewrote `speccy-cli/src/check.rs` as a render-only command. Dropped the `use speccy_core::exec::shell_command` import, deleted the `run_executable` / `render_manual` execution helpers and the `CheckError::ChildSpawn` variant, and replaced `execute_checks` with `render_checks` + `render_one`. Each selected check now prints `==> CHK-NNN (SPEC-NNNN): <scenario first line>` from `CheckEntry.scenario` with continuation lines indented two spaces; the run closes with `N scenarios rendered across M specs` (M = distinct spec count via a `BTreeSet`). All SPEC-0017 selector shapes (`All`, `UnqualifiedCheck`, `Spec`, `QualifiedCheck`, `Task`) reach the same renderer; dropped/superseded specs still get the explicit "no checks rendered" skip line when named directly. Exit code is non-zero only on selector/lookup/parse/workspace errors (`malformed > 0` retains the existing 1-exit-on-warning shape; scenarios never gate). `speccy-cli/src/main.rs` lost the now-unreachable `ChildSpawn` arm. The integration test file `speccy-cli/tests/check.rs` was rewritten with 20 tests mapped one-to-one to the bullets in this task entry plus selector/error-surface coverage; legacy framing (`<-- CHK-... PASS|FAIL|IN-FLIGHT|MANUAL`, exit-code propagation from commands, sentinel-file write-back, live-streaming smoke) is gone. The no-subprocess proof uses a legacy `command = "echo BAD > sentinel"` row that the T-001 bridge happily parses; after rendering, the sentinel file does not exist.
  - Undone: `speccy-core::exec` and `parse::CheckPayload` remain (T-004 deletes them). `speccy check` still reads through `CheckEntry.payload` indirectly because the bridge keeps populating it, but no production code path in this crate touches `payload` anymore. `speccy verify` execution removal is T-003.
  - Commands run:
    - `cargo test -p speccy-cli --test check`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - Exit codes:
    - pass (20/20)
    - pass (all suites green)
    - pass (no warnings)
  - Discovered issues: (none)
  - Procedural compliance: (none)
- Review (tests, pass): T-002's `speccy-cli/tests/check.rs` is non-vacuous and covers each REQ-002 bullet against the real `speccy_cli::check::run` entry point (not a mock): output buffers are real `Vec<u8>`, fixtures parse through the real workspace scanner + TOML deserializer, and selector resolution flows through `parse_selector` + `task_lookup::find`. Bullet-to-test mapping is clean: no-selector run + count summary (`check.rs:157`), multi-line header + indented continuations (`check.rs:217`), all five SPEC-0017 selector shapes (`check.rs:289,320,349,376,422`), overlapping-task dedup with first-occurrence order asserted via byte positions and a `matches("==> CHK-002 (SPEC-0020)").count() == 1` assertion (`check.rs:456`), `SPEC-9999` preserves `NoSpecMatching` wording (`check.rs:525`), and `assert_no_legacy_footers` (`check.rs:143`) blocks regressions that re-introduce `<-- CHK`, `PASS`, `FAIL`, `IN-FLIGHT`, or `MANUAL` framing. The "legacy `command` row is not spawned" bullet was replaced post-T-004 by `legacy_command_field_is_rejected_by_deny_unknown_fields` (`check.rs:254`) — substitution is acceptable because hard-break parse rejection is strictly stronger than the bridge-era no-spawn sentinel proof (a row that cannot deserialize cannot reach the renderer). A mentally-rewritten implementation that drops continuation indentation, swaps the summary wording, prints CHK-002 twice in the dedup case, or fails to preserve selector-error wording each fails at least one assertion. Non-blocking nit: `multiline_scenario_header_then_indented_continuations` (`check.rs:230-241`) asserts `out.contains("  when CHK-001 is rendered,")`, which still passes if a regression over-indents to 4 spaces because `"    when..."` contains the 2-space substring. Tighten with a line-prefix or exact-line compare (split on `\n` and assert the continuation line equals `"  when CHK-001 is rendered,"`) so indentation regressions in either direction are caught.
- Review (security, pass): T-002 is a net security win: it deletes the arbitrary-command-execution sink that was the primary security surface of `speccy check`. The new `check.rs` contains no `Command`, `spawn`, or `process::` references (`speccy-cli/src/check.rs:1-454`); `shell_command`, `run_executable`, and `render_manual` are gone, along with the `CheckError::ChildSpawn` variant (`speccy-cli/src/main.rs` no longer pattern-matches it). `render_one` (`check.rs:381-390`) only writes the developer-authored `scenario` string verbatim through `writeln!` to caller-supplied `dyn Write`, with no shell, no interpolation, no env-var expansion. Scenario contents originate from in-tree `spec.toml` (already a trusted, repo-committed artifact) and are bounded by `#[serde(deny_unknown_fields)]` plus the T-001 non-empty validation. The "legacy `command` is not spawned" bullet is verified by a sentinel-file integration test (per implementer note); since no `std::process` call exists in this module, the sentinel test is a belt-and-suspenders confirmation rather than the sole guarantee. Error messages (`CheckError::NoCheckMatching`, `ProjectRootNotFound`, `Io`) name only the user-supplied selector and workspace path — no secret or credential surface. No new dependencies, no `unsafe`, no panics in production code paths. The render path is well-behaved on adversarial scenario text (terminal control chars would print verbatim, but this matches every other `speccy` text-output command and is out of scope for SPEC-0018's threat model, which assumes repo content is trusted).
- Review (business, pass): T-002 satisfies REQ-002's `done_when` items end-to-end. `speccy-cli/src/check.rs` removed the `speccy_core::exec` dependency (`check.rs:7-27`); every SPEC-0017 selector shape (`All`, `UnqualifiedCheck`, `Spec`, `QualifiedCheck`, `Task`) reaches the same `render_checks` renderer (`check.rs:126-137`); the header `==> CHK-NNN (SPEC-NNNN): <first line>` with two-space indented continuations matches REQ-002's framing exactly (`check.rs:381-390`); the summary `N scenarios rendered across M specs` is emitted with a `BTreeSet`-deduplicated spec count (`check.rs:368-376`); the old `<-- CHK-NNN PASS|FAIL|IN-FLIGHT|MANUAL` footer is gone (asserted by `assert_no_legacy_footers` and the test `legacy_command_field_is_rejected_by_deny_unknown_fields`); and exit code is non-zero only on selector/lookup/parse/workspace errors per `i32::from(malformed > 0)` (`check.rs:378`, `415`). REQ-002's three Behavior cases — six headers across three two-check specs, preserved `SPEC-9999` wording, and task-selector scenarios derived from covered requirements with SPEC-0017 semantics — are covered by `no_selector_renders_all_scenarios_with_count_summary`, `binary_spec_9999_preserves_no_matching_spec_wording`, and the `qualified_task_selector_renders_covered_scenarios` / `task_selector_dedups_overlapping_checks_in_first_occurrence_order` pair. No non-goal is breached: nothing here adds `--exec`, registers commands, or grades scenarios. No Open Question is silently resolved. The user story for the implementer agent ("each check describes behavior in English") and the CI-maintainer story ("fast structural gate") are advanced by this task; the reviewer-tests persona update lands in T-005, which is out of scope here.
- Review (style, pass): `speccy-cli/src/check.rs` is idiomatic for this codebase — `thiserror`-backed `CheckError` with `#[non_exhaustive]` and `#[from]` propagation, `?` everywhere, no `unwrap()` / `expect()` / `panic!()` / `#[allow(...)]` in production code, `camino::Utf8Path` at every path boundary, doc comments on every public item with `# Errors` sections per `.claude/rules/rust/rust-documentation.md`. The `unwrap_or` / `unwrap_or_else` calls (`check.rs:206,244,299,323,351,384,402`) are the infallible variants and don't trip `clippy::unwrap_used`. Selector dispatch in `run` (`check.rs:126-136`) follows the established per-shape-handler split, and the shared `render_checks` / `render_one` / `collect_for_spec` / `display_spec_label` helpers keep each handler short and reuse one helper rather than inventing parallels. `speccy-cli/tests/check.rs` follows the existing test-file convention: the file-level `#![allow(clippy::expect_used, reason = "...")]` matches `tests/review.rs`, `tests/tasks.rs`, `tests/plan.rs`, etc.; tests use `.expect("descriptive")` per `.claude/rules/rust/rust-testing.md`; no `.unwrap()` / `panic!()`. `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all --check` are both clean.

<task-scenarios>
  - When `speccy check` runs with no selector against a workspace with
    multiple specs and multiple scenarios, then stdout prints one
    `==> CHK-NNN (SPEC-NNNN): <scenario first line>` header per
    selected scenario and ends with `N scenarios rendered across M
    specs`.
  - When a scenario spans multiple lines, then the first line appears
    in the header and each continuation line is printed indented
    under that header.
  - When a selected check still carries a legacy `command` during the
    temporary bridge period, then the command is not spawned; use a
    sentinel-file fixture to prove no child process ran.
  - When `speccy check SPEC-9999` runs, then the existing
    no-matching-spec error wording is preserved.
  - When each SPEC-0017 selector shape is used (`SPEC-NNNN`,
    `SPEC-NNNN/CHK-NNN`, `CHK-NNN`, `SPEC-NNNN/T-NNN`, and
    `T-NNN`), then the selected scenario set matches the current
    executable-check selection behavior.
  - When a task selector covers multiple requirements whose check
    lists overlap, then each scenario renders once in first-occurrence
    requirement order.
  - When output is searched, then old `<-- CHK-NNN PASS`, `FAIL`,
    `IN-FLIGHT`, and `MANUAL` footers are absent.
</task-scenarios>
</task>

<task id="T-003" state="completed" covers="REQ-003">
Make `speccy verify` a shape-only validator

- Suggested files: `speccy-cli/src/verify.rs`, `speccy-cli/src/verify_output.rs`, `speccy-cli/tests/verify.rs`, `speccy-core/src/lint/rules/req.rs`, `speccy-core/src/lint/rules/val.rs`, `speccy-core/tests/lint_req.rs`, `speccy-core/tests/lint_val.rs`
- Implementer note (session-T003-shape-only):
  - Completed: Rewrote `speccy-cli/src/verify.rs` and `speccy-cli/src/verify_output.rs` as a shape-only validator. The `verify::run` entry point no longer imports `speccy_core::exec`, no longer calls `run_checks_captured`, and no longer invokes `speccy check`; `collect_check_specs` is gone. `VerifyReport` now carries `specs_total` / `requirements_total` / `scenarios_total` plus the three lint buckets, and `passed()` is `lint_errors.is_empty()`. Text output writes the contract line `verified N specs, M requirements, K scenarios; E errors` as the last line. JSON output bumps `schema_version` to `2`, replaces the per-check execution array and `summary.checks` block with a `summary.shape { specs, requirements, scenarios, errors }` block, and a regression-guard recurses the whole envelope asserting no `outcome`, `exit_code`, or `duration_ms` keys appear anywhere. Added `REQ-003` lint rule (`speccy-core/src/lint/rules/req.rs`) that flags `[[checks]]` rows not referenced by any `[[requirements]].checks`, registered it at `Level::Error` in `speccy-core/src/lint/registry.rs`, and updated `speccy-core/tests/snapshots/lint_registry.snap`. Demotion was widened: errors on `in-progress`, `dropped`, and `superseded` specs are now demoted to `info`, preserving the pre-SPEC-0018 "dropped/superseded checks never ran" non-gating contract; workspace-level errors and errors on `implemented` specs still gate. Replaced `speccy-cli/tests/verify.rs` with 19 tests mapped one-to-one to T-003's bullets (empty `checks` array, unknown CHK ref, unreferenced scenario, no-spawn-on-clean, text summary shape, JSON schema_version=2 + no execution fields, dropped/superseded/in-progress demotion, implemented gating, workspace parse errors still gate, deterministic JSON, binary dispatcher). Added `req_003_*` tests in `speccy-core/tests/lint_req.rs`.
  - Undone: REPORT.md parse errors are not surfaced because the workspace scanner does not parse REPORT.md today; adding it is out of scope for T-003 (separate lint surface). T-004 still owns deleting `speccy-core::exec` and migrating all in-tree `spec.toml` files; `verify` no longer uses `exec`, but the module remains until T-004.
  - Commands run:
    - `cargo build --workspace`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo test -p speccy-cli --test verify`
  - Exit codes:
    - pass
    - pass (50 test binaries, all green)
    - pass (no warnings)
    - pass (19/19)
  - Discovered issues: (none)
  - Procedural compliance: (none)
  - Review (security, pass): Net reduction in attack surface. `speccy verify` no longer spawns child processes (`speccy-cli/src/verify.rs:5-10`) — the entire command-injection vector that the legacy `[[checks]].command` path created on every CI run is gone, and a grep of `verify.rs`/`verify_output.rs` confirms zero `Command`/`spawn`/`exec` usage. New `REQ-003` lint and demotion logic operate on already-parsed in-tree workspace data; no new untrusted input is read or rendered into shells. JSON envelope (`schema_version = 2`) carries only structural counts and lint diagnostics on local file paths — no secrets, no credentials, no environment passthrough.
- Review (tests, pass): T-003's 19 integration tests in `speccy-cli/tests/verify.rs` plus the four `req_003_*`/REQ-* unit tests in `speccy-core/tests/lint_req.rs:24-145` are non-vacuous and map one-to-one to the bullet list. Each integration test invokes the real `speccy_cli::verify::run` against a fixture workspace through the real workspace scanner, TOML deserializer, and lint registry — no mocks. Bullet-to-test mapping: empty `checks` array asserts exit=1 and that the REQ-001 diagnostic message contains the literal `REQ-001` (`verify.rs:157-186`); unknown CHK ref asserts the diagnostic message contains both `REQ-001` and `CHK-099` (`verify.rs:192-220`); unreferenced scenario asserts the REQ-003 code is emitted and the message names `CHK-077`, corroborated by the lint-layer unit test (`lint_req.rs:59-91`) which adds the negative case `req_003_silent_when_every_scenario_is_referenced`; text summary uses exact-equals on the last line shape `verified N specs, M requirements, K scenarios; E errors` and aggregates correctly across specs and on an empty workspace (`verify.rs:303-364`); the JSON envelope test (`verify.rs:370-410`) pins `schema_version == 2`, asserts the `summary.shape` counts, and uses a recursive `assert_no_execution_keys` walker (`verify.rs:129-145`) that catches a stray `outcome`/`exit_code`/`duration_ms` at any nesting depth, including inside `lint.errors[].extra`; dropped/superseded/in-progress non-gating and implemented-still-gates form a clean contrast quartet (`verify.rs:453-592`); the binary dispatcher coverage (`verify.rs:598-659`) closes the wrapper. The no-spawn bullet (`clean_workspace_exits_zero_without_spawning_child_processes`, `verify.rs:260-297`) is technically a weaker proof than the bullet asks for — it asserts no sentinel file was created, but a regression that called `run_checks_captured` without shelling out to `touch` could still pass it. That said, the absence of `Command::new`/`spawn`/`process::`/`run_checks_captured`/`speccy_core::exec` in `verify.rs` + `verify_output.rs` (verified by grep) plus T-004's deletion of `speccy_core::exec` itself make the structural guarantee load-bearing and the sentinel a confirmatory belt-and-suspenders; acceptable. Mentally rewriting the implementation to drop REQ-003, change the summary wording, leave `schema_version` at 1, or stop demoting dropped/superseded specs each fails at least one assertion. One non-blocking nit: `text_output_summary_counts_aggregate_across_specs` (`verify.rs:326-351`) writes two fixtures that both render under SPEC-0001's template (`spec_md_template("SPEC-0001", ...)` is reused for both slugs) — the test still passes today because counts come from parsed specs, but a future "duplicate SPEC id in workspace" lint would silently flip it red; consider parameterizing the second fixture with `SPEC-0002` to future-proof.
- Review (style, pass): T-003's production surface in `speccy-cli/src/verify.rs`, `speccy-cli/src/verify_output.rs`, and the new REQ-003 branch in `speccy-core/src/lint/rules/req.rs` is idiomatic for this codebase. `VerifyError` (`verify.rs:30-52`) is `thiserror`-backed with `#[non_exhaustive]` and `#[from]` propagation; `run` (`verify.rs:108-144`) and `partition_lint` (`verify.rs:164-191`) use `?` everywhere with no `unwrap()` / `expect()` / `panic!()` / `unreachable!()` / `#[allow(...)]` in production code. `VerifyReport::passed` carries `#[must_use = "the pass/fail signal drives the exit code"]` per `rust-code-quality.md`. `shape_totals` (`verify.rs:208-225`) uses `saturating_add` rather than `+`, matching the project's overflow-aware posture. The new REQ-003 branch in `req.rs:57-70` mirrors the REQ-001/REQ-002 emission shape (`Diagnostic::with_file`, `Level::Error`, named-arg message format) rather than inventing a parallel helper, and the `REQ_003` constant is grouped with `REQ_001`/`REQ_002` at the top of the file. `registry.rs:23` slots `REQ-003` into the existing REQ-* block in code order; the snapshot was regenerated, not allow-listed. `verify_output.rs` keeps the existing `JsonOutput`/`JsonSummary`/`JsonLintCounts` struct split and adds `JsonShapeCounts` in the same `#[derive(Debug, Clone, Copy, Serialize)]` style with full doc comments on every public field. Test files (`speccy-cli/tests/verify.rs`, `speccy-core/tests/lint_req.rs`) use the established file-level `#![allow(clippy::expect_used, reason = "...")]` / `#![expect(clippy::panic_in_result_fn, reason = "...")]` pattern shared by every other integration test in the crate. `cargo clippy --workspace --all-targets --all-features -- -D warnings` is clean and `cargo +nightly fmt --all --check` shows no diffs.
- Review (business, pass): T-003 satisfies REQ-003's `done_when` end-to-end. `speccy-cli::verify::run` has no `speccy_core::exec` import and no execution helper call (`speccy-cli/src/verify.rs:12-25,108-144`); lint output includes parse errors (SPC-*), requirements-with-no-scenarios (REQ-001), unknown-scenario refs (REQ-002), unreferenced-scenario rows (new REQ-003 rule in `speccy-core/src/lint/rules/req.rs:57-70`, registered at `Level::Error` in `lint/registry.rs:23`), plus the existing TSK-* / QST-001 diagnostics; dropped/superseded specs stay non-gating via the demotion in `partition_lint` (`speccy-cli/src/verify.rs:171-189`); the text summary's terminating line `verified N specs, M requirements, K scenarios; E errors` is emitted unconditionally (`speccy-cli/src/verify_output.rs:42-49`); JSON bumps `schema_version` to `2` (`verify_output.rs:17`) and the `assert_no_execution_keys` recursion in `speccy-cli/tests/verify.rs:129-145` proves `outcome` / `exit_code` / `duration_ms` are absent at every depth. The three Behavior cases (REQ-001 names the requirement, REQ-002 names both ids, clean workspace exits 0 without spawning) are covered by named tests. Two non-blocking gaps worth surfacing: (1) REQ-003's `done_when` names `REPORT.md` parse errors among the diagnostics verify must report, but the workspace scanner does not parse REPORT.md and the implementer note explicitly defers that surface — this is pre-existing scanner behavior, not a T-003 regression, but the SPEC bullet is technically unmet and the deferral should be tracked in this spec's Open Questions or a follow-up, not silently inherited; (2) `partition_lint` (`verify.rs:177-182`) also demotes errors on in-progress specs, which widens the SPEC's literal "Dropped and superseded specs remain non-gating" — pre-SPEC-0018 already demoted in-progress, so this preserves prior behavior, but the SPEC wording could be tightened to match. No non-goal is breached, no Open Question is silently resolved.


<task-scenarios>
  - When a requirement has an empty `checks` array, then
    `speccy verify` exits 1 and names the requirement.
  - When a requirement references `CHK-099` and no matching scenario
    row exists, then `speccy verify` exits 1 and names both ids.
  - When a scenario row is not referenced by any requirement, then
    `speccy verify` reports the unreferenced `CHK-NNN` as a shape
    error.
  - When a clean workspace is verified, then verify exits 0 without
    calling `speccy check`, `run_checks_captured`, or any shell helper.
  - Text output ends with `verified N specs, M requirements, K
    scenarios; E errors`.
  - `speccy verify --json` emits `schema_version = 2`, contains the
    structural counts needed by the text summary, and contains no
    `outcome`, `exit_code`, or `duration_ms` fields.
  - Dropped and superseded specs remain non-gating in the same cases
    they are non-gating today, while workspace-level parse failures
    still gate verification.
</task-scenarios>
</task>

## Phase 3: Hard-break migration and cleanup


<task id="T-004" state="completed" covers="REQ-001 REQ-004">
Migrate all `spec.toml` files and remove legacy check fields

- Suggested files: `.speccy/specs/**/spec.toml`, `speccy-core/src/parse/toml_files.rs`, `speccy-core/src/parse/mod.rs`, `speccy-core/src/lib.rs`, `speccy-core/src/exec.rs`, `speccy-core/tests/exec_captured.rs`, `speccy-cli/tests/check.rs`, `speccy-cli/tests/verify.rs`
- Implementer note (session-T004-hardbreak):
  - Completed: Migrated all 20 in-tree `.speccy/specs/**/spec.toml` files to the new `{ id, scenario }` shape via a one-shot Python migration (executable rows: `scenario = old_proves`; manual rows: `scenario = old_prompt`). Multi-line / escape-prone scenarios use TOML literal multi-line strings (`'''...'''`) so backslashes survive verbatim (one row in SPEC-0017 was the trigger). Shrunk `CheckEntry` to `{ id: String, scenario: String }`, deleted `CheckPayload`, and added `#[serde(deny_unknown_fields)]` to `RawCheck` so legacy `kind` / `command` / `prompt` / `proves` rows are rejected at parse. Deleted `speccy-core/src/exec.rs` and removed `pub mod exec;` from `speccy-core/src/lib.rs`; deleted `speccy-core/tests/exec_captured.rs`. Removed the `SPEC-0018 BRIDGE` markers and bridge grep-guard test. Updated `parse/cross_ref.rs` and `speccy-cli/tests/common/mod.rs` / `speccy-core/tests/lint_common/mod.rs` fixtures to the new shape; batch-rewrote legacy fixture trios across `speccy-core/tests/{workspace_supersession,lint_run,task_state_aggregation,lint_spc,lint_req,workspace_scan,next_priority}.rs` and `speccy-core/tests/fixtures/lint/{qst-001-open-question,spc-002-missing-from-toml}/spec.toml`. Replaced the bridge-era `legacy_command_row_is_rendered_not_spawned` integration test with a `legacy_command_field_is_rejected_by_deny_unknown_fields` shape test. Added a new workspace-scope test (`speccy-core/tests/in_tree_spec_tomls.rs`) asserting every in-tree spec.toml parses under the new schema. Final verification: `git grep "speccy_core::exec\|run_checks_captured\|CheckOutcome\|CheckResult\|CheckSpec\|CheckPayload"` returns zero production hits; `git grep "SPEC-0018 BRIDGE"` returns zero hits; `speccy verify` reports `verified 20 specs, 110 requirements, 148 scenarios; 0 errors`; `speccy check` renders 148 scenarios across 20 specs with no child processes.
  - Undone: (none) — the hard break is complete.
  - Commands run:
    - `cargo build --workspace`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo +nightly fmt --all --check`
    - `cargo run --quiet -- verify`
    - `cargo run --quiet -- check`
  - Exit codes:
    - pass
    - pass (all suites green)
    - pass (no warnings)
    - pass (no diffs)
    - pass (0 errors)
    - pass (148/20 rendered)
  - Review (business, pass): T-004 satisfies REQ-001's migration half and REQ-004 end-to-end. `CheckEntry` in `speccy-core/src/parse/toml_files.rs:83-86` is reduced to exactly `{id, scenario}` with `#[serde(deny_unknown_fields)]` enforcing the hard-break rejection of `kind`/`command`/`prompt`/`proves`; a grep across `.speccy/specs/**/spec.toml` returns zero legacy fields, so every one of the 20 in-tree files migrated (including this spec's own `spec.toml`, which carries the new `id`+multiline `scenario` shape). `speccy-core/src/exec.rs` and `speccy-core/tests/exec_captured.rs` are deleted, `pub mod exec` is gone from `speccy-core/src/lib.rs`, and `git grep` for the removed surface (`speccy_core::exec`, `run_checks_captured`, `CheckOutcome`, `CheckResult`, `CheckSpec`, `CheckPayload`) returns no production hits. REQ-001's migration rules (`scenario = old_proves` for executable, `scenario = old_prompt` for manual) match the implementer's one-shot Python script. The two Behavior cases for REQ-004 (post-spec grep clean; no test depends on Speccy spawning a shell) are covered by the new `speccy-core/tests/in_tree_spec_tomls.rs` workspace-shape test plus the deletion of `exec_captured.rs`. No Open Question is silently resolved and no non-goal is breached — there is no compatibility shim, no `--exec`, no test-runner wrapper. The VAL-001..004 lint deletion noted under "Discovered issues" is genuine dead-code removal (those rules keyed on `kind`/`payload`/`proves` fields that `deny_unknown_fields` now structurally rejects at parse), and REQ-004's `done_when` clause "without dead-code warnings from the removed execution surface" arguably authorizes it; the implementer surfaced the registry doc-comment tension ("append-only") transparently rather than silently editing it, which is the correct call for a pre-v1 hard break.
  - Discovered issues: The VAL-* lint family (VAL-001 `proves`-empty, VAL-002/003 kind/payload mismatch, VAL-004 no-op command) became dead code once the legacy fields were rejected at parse and `CheckEntry` shrank. SPC-001 already surfaces the surviving parse error (empty `scenario`). Deleted `speccy-core/src/lint/rules/val.rs` plus the integration suite `speccy-core/tests/lint_val.rs` and the `val-004-no-op` fixture directory; pruned the four VAL entries from `lint/registry.rs`, `tests/snapshots/lint_registry.snap`, and the wiring in `lint/mod.rs` + `lint/rules/mod.rs`. The registry doc comment says "append-only", but SPEC-0018 is by construction a hard break: the codes were structurally unreachable post-migration, so leaving them in place would have meant carrying dead constants forward. Also fixed a pre-existing `unnecessary_trailing_comma` clippy nit in `speccy-cli/tests/verify.rs:515` that surfaced only after the val.rs deletion shifted incremental clippy state.
  - Procedural compliance: (none)
  - Review (tests, pass): T-004's test surface maps each REQ-001/REQ-004 bullet to a non-vacuous test against real code paths. `speccy-core/tests/in_tree_spec_tomls.rs:33-65` parses every in-tree `.speccy/specs/**/spec.toml` through the real `spec_toml` entry point and asserts non-empty `scenario` per row -- combined with `#[serde(deny_unknown_fields)]` on `RawCheck` (`speccy-core/src/parse/toml_files.rs:82`), a successful parse structurally proves "exactly `id` and `scenario`" without needing a separate field-count assertion. All four legacy-field rejections are pinned by name in `speccy-core/src/parse/toml_files.rs:274-332` (`rejects_check_with_legacy_command_field`, `_prompt_`, `_kind_`, `_proves_`) using a fresh `scenario` row plus the offending legacy field, so each test would fail if `deny_unknown_fields` were dropped from `RawCheck`. The integration-side rejection (`speccy-cli/tests/check.rs:254` `legacy_command_field_is_rejected_by_deny_unknown_fields`) drives the failure through `speccy check` and asserts the parse-warning wording is surfaced to the user, catching regressions where a future scanner swallows the parse error. The "no `CheckPayload` / `speccy_core::exec` / `run_checks_captured` / `CheckOutcome` / `CheckResult` / `CheckSpec` production hits" bullet is verified by direct grep at review time (zero hits under `speccy-core/src/**` and `speccy-cli/src/**`) and structurally locked by the deletion of `speccy-core/src/exec.rs` and removal of `pub mod exec;` from `speccy-core/src/lib.rs`; `speccy-core/tests/exec_captured.rs` is gone (`ls` confirms). Mentally rewriting the implementation to re-introduce `#[serde(default)] command: Option<String>` on `RawCheck`, leave `pub mod exec;` in `lib.rs`, or skip migrating any in-tree spec.toml would each fail at least one assertion. Two non-blocking nits worth tightening when convenient. (1) The "old executable -> `scenario = old_proves`; old manual -> `scenario = old_prompt`" bullet has no live automated assertion because the migration was a one-shot Python script and the bridge was deleted in the same task -- the implementer note documents the mapping, but a regression that mistyped one entry during migration would only be caught by a human re-reading prose. Given the SPEC explicitly authorizes deleting the bridge this is acceptable, but a frozen golden snapshot of the migrated scenario text for two or three semantically novel rows would have closed the gap without keeping the bridge alive. (2) `every_in_tree_spec_toml_parses_under_new_schema` asserts `found >= 1` (`in_tree_spec_tomls.rs:61-64`); a path-resolution regression that silently iterated over only one of the 20 dirs would still pass. The implementer note documents `verified 20 specs, 110 requirements, 148 scenarios`, so tightening to `found >= 20` would catch a silent under-walk. Neither blocks; the structural guarantees from `deny_unknown_fields` + the production-source grep are load-bearing and post-migration `speccy verify` reporting `0 errors` is the binding contract.
  - Review (security, pass): T-004 is a substantial net security win: it permanently removes the arbitrary-command-execution sink that was the primary security surface of pre-SPEC-0018 Speccy. `speccy-core/src/exec.rs` (439 lines, including the `shell_command` helper that fed `/bin/sh -c <attacker-controllable-string>` and the subprocess plumbing in `run_checks_captured`) is deleted, `pub mod exec;` is gone from `speccy-core/src/lib.rs`, and a workspace-wide `git grep -nE "Command::new|process::Command|run_checks_captured|speccy_core::exec|CheckPayload|CheckOutcome|CheckResult|CheckSpec|shell_command"` against `speccy-cli/src/**` and `speccy-core/src/**` returns zero production hits outside the pre-existing, unrelated `speccy-cli/src/git.rs` (which spawns `git` with argv-style arguments, not `sh -c`, and is out of scope). The deleted `speccy-core/tests/exec_captured.rs` subprocess suite is gone as required by REQ-004. The hard-break shape in `RawCheck` (`speccy-core/src/parse/toml_files.rs:81-86`) carries `#[serde(deny_unknown_fields)]`, so any future regression that smuggles a `command`, `prompt`, `kind`, or `proves` field back into a `spec.toml` row hard-fails at parse rather than silently re-establishing a code-execution path; this is verified end-to-end by `legacy_command_field_is_rejected_by_deny_unknown_fields` in `speccy-cli/tests/check.rs:254` and by the in-tree migration guard `every_in_tree_spec_toml_parses_under_new_schema` in `speccy-core/tests/in_tree_spec_tomls.rs`. The 20 migrated in-tree `.speccy/specs/**/spec.toml` files were rewritten with `scenario = old_proves` / `scenario = old_prompt` — the old `command =` strings are dropped wholesale, not preserved as comments or alternate keys, so there is no residual sink in the working tree. Multi-line scenarios use TOML literal strings (`'''...'''`), which keeps backslashes verbatim and avoids escape-driven shell-syntax confusion (and `render_one` writes the verbatim string through `writeln!` to a `dyn Write` with no shell interpretation regardless). `lower_check` (`toml_files.rs:149`) preserves the T-001 empty/whitespace rejection naming the CHK id; `ParseError::InvalidCheckEntry` exposes only path + CHK-id (already public repo content). Deleting the VAL-* lint family is correct from a security standpoint: those rules only policed executable-check shape and become structurally unreachable once the fields are rejected at deserialization. No new dependencies, no `unsafe`, no panics in production paths, no secret-handling surface, no auth boundary, no crypto change. Threat-model delta: a malicious or compromised `spec.toml` row (e.g. via PR from an untrusted contributor, or via a workspace path Speccy was pointed at) can no longer cause Speccy to execute attacker-controlled shell on `speccy check` or `speccy verify` runs in CI or on a developer's machine — this closes the most impactful command-injection vector in the codebase. Adversarial scenario text (control chars, ANSI escapes, oversized lines) is still rendered verbatim to stdout, which matches every other Speccy text-output command and is explicitly in scope of "repo content is trusted" — out of scope for SPEC-0018. One non-blocking observation: the migration script itself was a transient Python one-shot per the implementer note rather than the `xtask`/temp-binary the SPEC's Migration section names; the post-condition (all 20 in-tree files migrated, all parse under the new schema, `deny_unknown_fields` rejects regressions) is met and verified by `every_in_tree_spec_toml_parses_under_new_schema`, so the script's transient form has no security implication — flagging only because the SPEC text said `xtask`.
  - Review (style, pass): T-004's production surface is idiomatic for this codebase and lint-clean. `CheckEntry` and `RawCheck` in `speccy-core/src/parse/toml_files.rs:46-86` follow the file's established `RawX` (Deserialize-only) / public-type split; `#[serde(deny_unknown_fields)]` is placed on `RawCheck` per the same pattern used elsewhere for hard-shape contracts. `lower_check` (`toml_files.rs:149-161`) is a narrowly-scoped helper that returns `ParseError::InvalidCheckEntry` via `thiserror` propagation rather than panicking, matching the `?`-style used by `spec_toml` / `speccy_toml`. `render_checks` / `render_one` (`speccy-cli/src/check.rs:362-390`) reuse `BTreeSet` for the spec-set dedup -- the idiomatic ordered-unique container -- and use `lines.next().unwrap_or("")` (the infallible variant, not `unwrap()`), so no `clippy::unwrap_used` regression. Doc comments on `CheckEntry`, `RawCheck`, `spec_toml`'s `# Errors` section, and the `CheckError` enum were updated alongside the behavior change rather than left stale, per `rust-documentation.md`. The deletions are clean -- no orphaned imports (e.g. `CheckPayload` removed from `parse/cross_ref.rs:62` and `parse/mod.rs:38`, `shell_command` removed from `check.rs:9`, the test-only `use CheckPayload` removed from the `tests` module of `toml_files.rs:184`), no leftover `#[expect(...)]` (the pre-existing `clippy::unwrap_used` suppression on `no_op_set` disappeared with `val.rs`), and the lint registry snapshot (`speccy-core/tests/snapshots/lint_registry.snap`) was regenerated rather than allow-listed. Tests use `.expect("descriptive")` per `.claude/rules/rust/rust-testing.md`; the new `rejects_check_with_legacy_{command,prompt,kind,proves}_field` quartet (`toml_files.rs:274-332`) and `rejects_empty_scenario` follow the file's existing matches-pattern-with-context idiom. The implementer's commands attest `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all --check` are both clean with no diffs. One non-blocking nit worth surfacing: `speccy-core/src/lint/registry.rs:9` still reads `This list is **append-only across minor versions**. Removing or renaming an entry is a breaking change` while this diff removes four entries (VAL-001..004). The implementer surfaced the tension transparently under Discovered issues and the removal is justified as a pre-v1 hard break, but the doc comment is now self-contradicting on its own terms. A one-line caveat (e.g. "Pre-1.0 SPEC-N hard breaks may retire structurally-unreachable codes; post-1.0 this list is strictly append-only") would close the gap so the next contributor doesn't have to re-derive the exception from the SPEC history.


<task-scenarios>
  - When every in-tree `.speccy/specs/**/spec.toml` file is parsed
    after migration, then every `[[checks]]` row contains exactly
    `id` and `scenario` and no row contains `kind`, `command`,
    `prompt`, or `proves`.
  - When a legacy `command`, `prompt`, `kind`, or `proves` field is
    present after the hard break, then deserialization fails through
    `#[serde(deny_unknown_fields)]`.
  - When old executable checks are migrated, then
    `scenario = old_proves`; when old manual checks are migrated,
    then `scenario = old_prompt`.
  - When `git grep -n "CheckPayload"` runs after cleanup, then there
    are no production-source hits.
  - When `git grep -n "speccy_core::exec\\|run_checks_captured\\|CheckOutcome\\|CheckResult\\|CheckSpec"` runs after cleanup, then there
    are no production-source hits.
  - The `speccy-core/tests/exec_captured.rs` subprocess suite is
    deleted, and any surviving check/verify tests assert rendering or
    shape validation rather than shell exit status.
  - `speccy-core/src/lib.rs` no longer exports `exec`, and
    `speccy-core/src/exec.rs` is deleted.
</task-scenarios>
</task>

## Phase 4: Contract language and dogfood outputs


<task id="T-005" state="completed" covers="REQ-005">
Update architecture docs and shipped prompts for scenarios

- Suggested files: `.speccy/ARCHITECTURE.md`, `resources/modules/prompts/plan-greenfield.md`, `resources/modules/prompts/plan-amend.md`, `resources/modules/prompts/implementer.md`, `resources/modules/prompts/report.md`, `resources/modules/personas/reviewer-tests.md`, `.speccy/skills/prompts/`, `.speccy/skills/personas/`
- Implementer note (session-T005-docs):
  - Completed: Swept architecture docs and shipped prompts to teach the post-SPEC-0018 contract. `.speccy/ARCHITECTURE.md`: rewrote the "Feedback, Not Enforcement" section to explicitly assign test execution to project CI and semantic judgment to reviewer personas; updated the Five Proper Nouns Check row, the CLI Surface `check`/`verify` rows, the `spec.toml` example (now `{id, scenario = """..."""}`), the `# Checks` section (renamed Execution -> Rendering, removed manual/executable branching), the `speccy verify exit code` and `speccy check execution` operational sections, the Lint Codes block (dropped VAL-001..004 with a historical note; added REQ-003 the unreferenced-scenario lint introduced in T-003), the Tests-in-English-first paragraph (removed `kind = "test"`), the REPORT.md example (removed PASS/FAIL), the threat-model bullets, the "What we deliberately don't do" table (added a row for "Speccy executing project tests"), the JSON Interfaces sample (replaced the stale VAL-001 warning), the Phase 3 pseudocode (sub-agent runs project tests, uses `speccy check` only to render scenarios), and Implementation Sequence steps 11/13. Added an explicit historical marker block in the spec.toml schema description ("before SPEC-0018, checks carried `kind`, `command` or `prompt`, and `proves`..."). Shipped prompts (`resources/modules/prompts/plan-greenfield.md`, `plan-amend.md`, `implementer.md`, `report.md` and the planner/implementer/reviewer-tests personas) now: (a) tell planners to author `{id, scenario}` rows only and call out that `kind`/`command`/`prompt`/`proves` were removed in SPEC-0018; (b) tell implementers to run the project's own test command and use `speccy check SPEC-NNNN/T-NNN` only as a renderer; (c) tell reviewer-tests to compare diffs and tests against scenario prose, with an explicit "do not treat `speccy check` exit codes as evidence" anti-instruction; (d) drop PASS/FAIL framing from the REPORT.md `Requirements coverage` instructions. The mirror copies under `.speccy/skills/prompts/` and `.speccy/skills/personas/` were updated to match. Re-ran `speccy init --force` for both `claude-code` and `codex` to refresh the materialized `.claude/`, `.codex/`, `.agents/` packs so the `dogfood_outputs_match_committed_tree` integration test stays green. Final grep across active docs / shipped skills / `.speccy/ARCHITECTURE.md` returns zero check-authoring hits (the only `^prompt =` lines left in ARCHITECTURE.md are pseudocode for the speccy-work skill loop, not check rows). All four hygiene gates pass; `speccy verify` reports `verified 20 specs, 110 requirements, 148 scenarios; 0 errors`.
  - Undone: T-006 still owns the dedicated host-pack refresh test + the CI workflow update that locks "project tests run alongside `speccy verify`, not through it". I refreshed the materialized packs as a side-effect (the renderer-vs-committed-tree dogfood test forced it), but the CI workflow itself was not touched.
  - Procedural friction: the reviewer-tests and planner persona drafts initially used the literal three-double-quote scenario syntax inside their prose. That tripped the SPEC-0016 DEC-004 guard test `t010_persona_bodies_have_no_toml_triple_quote` (the Codex reviewer wrapper embeds persona text inside a TOML triple-quoted `developer_instructions` block; an embedded triple-quote terminates the wrapper early). Rephrased both personas to describe the field by name ("a `scenario` field on each `[[checks]]` row") rather than show the literal syntax. The literal `scenario = """..."""` form survives in `plan-greenfield.md` and `plan-amend.md` (which are not codex-wrapped) and inside the ARCHITECTURE.md `spec.toml` example, satisfying REQ-005's "active examples use `scenario = """..."""`" requirement.
  - Commands run:
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo +nightly fmt --all --check`
    - `cargo run --quiet -- verify`
    - `cargo run --quiet -- init --force --host claude-code`
    - `cargo run --quiet -- init --force --host codex`
    - `grep -rnE '^\s*kind\s*=\s*"test"|^\s*kind\s*=\s*"command"|^\s*kind\s*=\s*"manual"|^\s*command\s*=|^\s*prompt\s*=|^\s*proves\s*=' resources/ .speccy/skills/ .speccy/ARCHITECTURE.md`
  - Exit codes:
    - pass (all suites green after dogfood-pack refresh)
    - pass (no warnings)
    - pass (no diffs; only stable rustfmt config warnings)
    - pass (`0 errors`, `148 scenarios`)
    - pass (14 overwritten, 20 skipped)
    - pass (14 overwritten, 20 skipped)
    - pass (only two `prompt = ...` lines in ARCHITECTURE.md, both in pseudocode for the skill loop -- not check rows)
  - Discovered issues: (none)
  - Procedural compliance: (none) -- the persona-rephrasing above is a content edit for REQ-005, not a skill-layer friction fix. No files under `skills/` (the shipped skill modules) carried stale check-authoring guidance; the friction was in resources I was already editing as the primary task.
  - Review (security, pass): T-005 is a pure docs/prompt sweep with no production code, dependency, build, or CI changes — diff is confined to `.speccy/ARCHITECTURE.md`, `resources/modules/{prompts,personas}/*.md`, and the mirrored `.speccy/skills/`, `.claude/`, `.codex/`, `.agents/` materializations, all of which are inert text consumed by humans and LLM hosts. No new auth, secret, crypto, deserialization, path-traversal, or input-validation surface is introduced; no command-execution sink is reintroduced (T-004 removed `speccy-core::exec` and `RawCheck` carries `#[serde(deny_unknown_fields)]`, which this task does not weaken). Security posture is net-positive: prompts and architecture docs now actively teach the `{id, scenario}` shape and explicitly tell implementers/reviewers to run project tests directly and to ignore `speccy check` exit codes, which removes the prior guidance that would have led agents to re-introduce shell-command rows in `spec.toml` (the historical command-injection vector). The codex triple-quote workaround (rephrasing personas to describe `scenario` by name rather than embedding `"""..."""` literals inside the TOML-wrapped `developer_instructions` block) is a correct defensive choice: it preserves the SPEC-0016 DEC-004 guard that prevents an attacker-controllable persona body from terminating the wrapper string early and smuggling top-level TOML keys into the codex agent config. No sensitive data, credentials, or env-var references appear in the diff; no new external links or fetch instructions are added to prompts. Verified zero residual legacy check-authoring snippets in active docs/skills via the grep recorded in Commands run.
  - Review (business, pass): T-005 satisfies REQ-005 end-to-end. `.speccy/ARCHITECTURE.md:46-67` "Feedback, not enforcement" explicitly assigns test execution to project CI ("Speccy does not run project tests") and semantic judgment to reviewer personas, matching REQ-005's `done_when` for that section; the Five Proper Nouns Check row (`ARCHITECTURE.md:82`) reads "One English validation scenario a requirement must satisfy"; the `check`/`verify` rows in the CLI Surface and the lint-code block (REQ-003 added, VAL-001..004 retired with a historical note) reflect render-only / shape-only. Shipped prompts in `resources/modules/` and the `.speccy/skills/` mirrors no longer instruct agents to author `kind =` / `command =` / `prompt =` rows — the grep recorded in Commands run returns only two `prompt = ...` hits in ARCHITECTURE.md, both inside skill-loop pseudocode rather than check-row authoring examples, satisfying Behavior bullet 1. `resources/modules/personas/reviewer-tests.md:7-35` explicitly tells reviewers to compare diffs/tests against scenario prose and adds an anti-instruction against using `speccy check` exit codes as evidence, satisfying Behavior bullet 2 verbatim. The literal `scenario = """..."""` form survives in `plan-greenfield.md`, `plan-amend.md`, and the ARCHITECTURE.md `spec.toml` example; the implementer transparently documented why the codex-wrapped persona files describe the field by name rather than embedding the triple-quote literal (SPEC-0016 DEC-004 codex-wrapping guard). REQ-005's "Active examples use `scenario = """..."""`" is met because the literal examples exist in active docs; the constraint-driven persona rephrasing is not a silent gap. No non-goal is breached, no Open Question is silently resolved, and the user stories (implementer reading English scenarios, reviewer-tests comparing against scenario prose, CI maintainer running `speccy verify` alongside real tests) are all advanced.
  - Review (tests, blocking): T-005's doc/prompt edits look correct on manual inspection (ARCHITECTURE.md's "Stance: Feedback, Not Enforcement" at `.speccy/ARCHITECTURE.md:38-71` carries the CI-owns-tests and reviewer-personas-own-semantic-judgment clauses; `resources/modules/personas/reviewer-tests.md:33-35` carries the explicit "Do not treat `speccy check` exit codes ... as evidence" anti-instruction), but only one of the five "Tests to write" bullets is locked by an automated assertion, and that one assertion is vacuous against half of its own bullet's wording. (1) `rendered_outputs_have_no_legacy_check_authoring_examples` (`speccy-cli/tests/init.rs:788-816`, owned by T-006) is the only test that maps to a T-005 bullet — bullet 3, "active docs and shipped prompts ... `kind =`, `command =`, and `prompt =` do not appear except in historical migration notes". Its `legacy_needles` array (`init.rs:796-801`) is `["kind = \"test\"", "kind = \"command\"", "kind = \"manual\"", "proves ="]` and deliberately omits the bare `command =` and `prompt =` needles the bullet names. A regression that re-introduced a `command = "cargo test"` or `prompt = "Did the change land?"` example line in any shipped prompt or persona would pass this guard — and prompts/personas are exactly the surface bullet 3 protects. The test name promises coverage of legacy check-authoring examples but the assertion does not match the bullet's contract. (2) Bullets 1, 2, 4, and 5 (ARCHITECTURE.md describes checks as scenarios + render-only `check` + shape-only `verify`; "Feedback Not Enforcement" assigns test execution to CI and semantic judgment to personas; active examples use `scenario = """..."""`; reviewer-tests persona tells reviewers to compare against scenario prose, not exit codes) have no automated assertion at all — `grep -rn "ARCHITECTURE\|Feedback, [Nn]ot Enforcement\|reviewer-tests" speccy-cli/tests/ speccy-core/tests/` finds nothing pinning the content of these files. The reviewer-tests persona's anti-exit-code instruction is the load-bearing post-SPEC-0018 reviewer contract; if a future PR silently reverts that paragraph the suite stays green. Fix: extend `legacy_needles` to include the bare `command = ` and `prompt = ` (with trailing space, to avoid false-positives on prose like "prompt for the user"), and broaden the walk to also lint `.speccy/ARCHITECTURE.md` and the `resources/modules/personas/**` / `resources/modules/prompts/**` sources directly, not just `render_host_pack` outputs (ARCHITECTURE.md is not rendered through host packs and is therefore entirely uncovered today). Add at least one positive assertion that pins the reviewer-tests persona contains the "Do not treat `speccy check` exit codes ... as evidence" sentence verbatim, so a regression that dropped or softened it would fail. Without these, T-005's bullet list is mostly an honor-system claim that the docs are right at the moment of commit, with no regression guard.
  - Review (style, pass): T-005's edits are idiomatic and stay surgical. ARCHITECTURE.md retires the VAL-* lint family via a `> Historical note:` blockquote (`ARCHITECTURE.md:1521-1523`) and reuses that same shape under `# spec.toml` (`:919-925`) — one idiom twice rather than two parallel inventions; the "What we deliberately don't do" table picks up the new "Speccy executing project tests" row in the existing `| feature | rationale |` shape; the lint-codes block drops VAL-001..004 and adds REQ-003 in code order with no `<!-- allow -->` suppression. Prompt and persona edits stay in the file's existing ~70-column wrap and reuse the project's `**bold lead-in.**` / `>` blockquote conventions. The `.speccy/skills/personas/` and `.speccy/skills/prompts/` mirrors are byte-identical to their `resources/modules/` sources (verified via `diff -u`), and the materialized `.claude/agents/reviewer-tests.md` / `.codex/agents/reviewer-tests.toml` were re-rendered via `speccy init --force` rather than hand-edited. The Codex triple-quote-wrapper friction was resolved by describing the field by name in persona bodies — `grep -nE 'scenario\s*=\s*"""'` across `resources/modules/personas/*.md`, `.speccy/skills/personas/*.md`, `.claude/agents/reviewer-tests.md`, and `.codex/agents/reviewer-tests.toml` returns zero hits — while the literal syntax is preserved in the non-wrapped `plan-greenfield.md` / `plan-amend.md` and the ARCHITECTURE.md `spec.toml` example, satisfying REQ-005 without re-tripping SPEC-0016 DEC-004's `t010_persona_bodies_have_no_toml_triple_quote` guard. No Rust touched, so no `#[allow]` / `unwrap()` / `expect()` regressions to check; the four hygiene gates (`cargo test`, clippy, `cargo +nightly fmt --all --check`, `speccy verify`) all passed alongside the doc sweep per the implementer note. One non-blocking nit: `ARCHITECTURE.md:1501` reads "All have stable prefixes (`SPC-` for spec structure, `REQ-` for requirements, `TSK-` for task structure)" while the codes block immediately below also lists `QST-001` and `JSON-001`. The omission is pre-existing (the T-005 diff didn't introduce it), but the line is adjacent to the VAL-* retirement and a small tweak to add ", `QST-` for open questions, `JSON-` for stable-output schemas" would close the drift while you're already in that paragraph.
- Retry: tests reviewer flagged that 4 of 5 "Tests to write" bullets have no automated coverage and bullet 3's existing guard (`rendered_outputs_have_no_legacy_check_authoring_examples` in `speccy-cli/tests/init.rs:788-816`) omits the bare `command = ` and `prompt = ` needles the bullet explicitly names. Required follow-ups: (a) extend `legacy_needles` to include `command = ` and `prompt = ` (with trailing space to avoid prose false-positives); (b) broaden the walk to also lint `.speccy/ARCHITECTURE.md` and the `resources/modules/personas/**` + `resources/modules/prompts/**` sources directly (ARCHITECTURE.md is not rendered through host packs and is currently uncovered); (c) add at least one positive assertion that pins the reviewer-tests persona contains the "Do not treat `speccy check` exit codes ... as evidence" sentence verbatim so a regression that softens or removes the post-SPEC-0018 reviewer contract fails the suite. Business, security, and style reviewers all passed; the content edits themselves are correct, only their regression guards need to land.
- Implementer note (session-T005-retry):
  - Completed: Landed the regression guards the tests reviewer demanded; no content edits to specs/docs/prompts (the prior pass was correct, only guardrails were missing). In `speccy-cli/tests/init.rs`: (a) lifted `legacy_needles` to a module-level `SPEC_0018_LEGACY_NEEDLES: [&str; 6]` and added the two new tight needles `"command = \""` and `"prompt = \""` (chose the opening-quote form rather than trailing-space because zero current-tree hits exist — verified by `grep -rn 'command = "\|prompt = "' resources/modules/ .speccy/ARCHITECTURE.md`, which returned no output — so no historical-note exclusion logic is needed); (b) added three new tests that broaden the walk: `architecture_doc_has_no_legacy_check_authoring_examples` (reads `.speccy/ARCHITECTURE.md` via `include_str!` and rejects all six needles; the existing `> Historical note:` blockquotes describe the retired fields via backticked prose like `command` not as quoted-literal assignments, so they pass), and `persona_and_prompt_sources_have_no_legacy_check_authoring_examples` (walks `PERSONAS.files()` and `PROMPTS.files()` from `speccy_core` — already imported in `init.rs` for other tests — and rejects the same needles in each upstream source); (c) added four positive content pins: `reviewer_tests_persona_pins_no_check_exit_code_evidence` (verbatim multi-line substring of the "Do not treat `speccy check` exit codes ... as evidence" sentence at `reviewer-tests.md:33-37` including the literal `\n  ` continuation so the bullet-shape is preserved), `architecture_doc_pins_feedback_not_enforcement_contract` (pins the `# Stance: Feedback, Not Enforcement` heading plus the two load-bearing bolded clauses `**Speccy does not run project tests.**` and `**Reviewer personas own semantic judgment.**`), `architecture_doc_pins_check_command_is_render_only` (pins the CLI Surface row `speccy check [SELECTOR]           Render check scenarios (no execution)`), and `architecture_doc_pins_verify_command_is_shape_only` (pins `speccy verify                     CI gate: proof-shape validation only` plus the `Does NOT run project tests; that's CI's job.` disclaimer line). Total: 7 net-new test functions in `speccy-cli/tests/init.rs`; 21 tests pass in the file (up from 14 before the retry).
  - Undone: (none) — the three required follow-ups (extend needles, broaden walk to ARCHITECTURE.md + persona/prompt sources, positive pin on reviewer-tests anti-instruction) all landed; the four ARCHITECTURE.md positive pins exceed the bullet's explicit `(b)` and `(c)` ask but match the test-reviewer's framing in the retry comment that 4-of-5 "Tests to write" bullets were uncovered.
  - Commands run:
    - `grep -rn 'command = "\|prompt = "' /Users/kevin/src/speccy/resources/modules/ /Users/kevin/src/speccy/.speccy/ARCHITECTURE.md`
    - `cargo test -p speccy-cli --test init`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo +nightly fmt --all -- speccy-cli/tests/init.rs`
    - `cargo +nightly fmt --all --check`
    - `cargo run --quiet -- verify`
  - Exit codes:
    - pass (no hits; tight needles have zero false-positives in active tree)
    - pass (21/21, all 7 new tests green)
    - pass (all suites green)
    - pass (no warnings — one transient `unnecessary_wraps` on the new `architecture_doc_has_no_legacy_check_authoring_examples` test was fixed by dropping the unused `TestResult` return type)
    - pass (formatter rewrote the multi-line `include_str!` on the new pin tests into one-liners; no other diffs)
    - pass (only the pre-existing `Unknown configuration option float_literal_trailing_zero` rustfmt warnings; no diffs)
    - pass (`0 errors`, `148 scenarios`)
  - Discovered issues: (none) — the prior pass's content edits already satisfied the needles; the tight-needle choice (`command = "` with opening quote rather than `command = ` with trailing space) is strictly more conservative than the bullet's suggestion and required no historical-note exclusion logic.
  - Procedural compliance: (none) — this retry only adds regression guards in `speccy-cli/tests/init.rs`; no shipped skill carried stale guidance about how to author these tests.
  - Review (security, pass): T-005 retry is purely additive test code in `speccy-cli/tests/init.rs`; the prior T-005 security pass (line 231) stands and nothing in this delta widens the security surface. The seven new test functions read trusted in-tree sources via `include_str!("../../.speccy/ARCHITECTURE.md")` and `include_str!("../../resources/modules/personas/reviewer-tests.md")` (compile-time, repo-internal), iterate the already-trusted `PERSONAS` / `PROMPTS` `include_dir!` trees, and walk `render_host_pack` outputs — no filesystem writes, no shell-out, no `std::process::Command`, no network, no env-var reads, no path concatenation against untrusted input, no deserialization of attacker-controllable data. `SPEC_0018_LEGACY_NEEDLES` is a hard-coded `[&str; 6]` of literal byte-string needles fed only to `str::contains`; no regex, no globbing, no injection sink. The tighter needles (`"command = \""`, `"prompt = \""` with opening quote) and the broader walk (ARCHITECTURE.md + persona/prompt sources, not just render outputs) are net-positive for security posture: they make it harder for a future PR to silently re-introduce a `command = "..."` shell-row example into shipped prompts/personas, which was the historical command-injection vector SPEC-0018 closed. The positive content pin on the reviewer-tests anti-instruction (`reviewer_tests_persona_pins_no_check_exit_code_evidence`) hardens the post-SPEC-0018 reviewer contract against silent reversion — a regression that softened the "do not treat `speccy check` exit codes ... as evidence" sentence would now fail the suite, preserving the structural guarantee that reviewers don't fall back to trusting an exit code from a CLI that no longer executes anything. No secrets, credentials, tokens, or PII in the diff; no new dependencies.
  - Review (business, pass): T-005 retry leaves REQ-005's content satisfaction intact and adds only test infrastructure in `speccy-cli/tests/init.rs` (`SPEC_0018_LEGACY_NEEDLES` lifted to module scope with two added needles; three walk-broadening tests over `.speccy/ARCHITECTURE.md`, `PERSONAS.files()`, `PROMPTS.files()`; four positive content pins on the reviewer-tests anti-exit-code sentence and the ARCHITECTURE.md "Feedback, Not Enforcement" / `check` render-only / `verify` shape-only clauses). The retry sneaks in no scope beyond the tests-reviewer's three named follow-ups (a/b/c) plus three ARCHITECTURE.md positive pins that match the retry framing of "4-of-5 bullets uncovered"; the choice of the tighter `command = "` / `prompt = "` opening-quote needle over the bullet-suggested trailing-space form is strictly more conservative and equally satisfies REQ-005's "do not appear except in historical migration notes" since the existing `> Historical note:` blockquotes use backticked prose, not quoted-literal assignments. No non-goal breached, no Open Question silently resolved, and the user stories (implementer/reviewer/CI-maintainer) are unaffected since no prose changed. Spec content from session-T005-docs remains the passing business artifact; this retry just makes the post-SPEC-0018 reviewer contract regression-proof.
  - Review (tests, pass): all three retry asks landed and the guards have real teeth. (a) `SPEC_0018_LEGACY_NEEDLES` at `speccy-cli/tests/init.rs:794-801` is the promised six-element array; the `"command = \""` and `"prompt = \""` opening-quote forms are strictly tighter than the original trailing-space suggestion and avoid the prose false-positive concern without needing historical-note exclusion logic (verified zero current-tree hits via the implementer's recorded grep). (b) The walk now covers all three previously-uncovered surfaces: `architecture_doc_has_no_legacy_check_authoring_examples` (`init.rs:836-844`) reads `.speccy/ARCHITECTURE.md` via `include_str!`, and `persona_and_prompt_sources_have_no_legacy_check_authoring_examples` (`init.rs:853-874`) iterates `PERSONAS.files()` and `PROMPTS.files()`, so `resources/modules/personas/**` and `resources/modules/prompts/**` are pinned at the source rather than only through `render_host_pack`. (c) The reviewer-tests anti-instruction is load-bearing-verbatim: `reviewer_tests_persona_pins_no_check_exit_code_evidence` (`init.rs:887-900`) pins `"Do not treat \`speccy check\` exit codes (or any command exit code)\n  as evidence that a scenario is satisfied."` including the two-space continuation, so a soft rewrap fails the suite. The three bonus ARCHITECTURE.md pins (`architecture_doc_pins_feedback_not_enforcement_contract`, `..._check_command_is_render_only`, `..._verify_command_is_shape_only`) cover bullets 1, 2, and 4 verbatim; every "Tests to write" bullet for T-005 now has at least one automated guard. Verified the 8 new/touched tests pass locally (`cargo test -p speccy-cli --test init -- architecture_doc persona_and_prompt reviewer_tests_persona rendered_outputs` -> 8 passed). Mentally rewriting the implementation to drop the anti-exit-code sentence, downgrade the "Speccy does not run project tests" clause, or re-introduce a `command = "cargo test"` snippet in any persona/prompt all flip the suite red, which is the bar.
  - Review (style, pass): T-005 retry's new Rust in `speccy-cli/tests/init.rs` is idiomatic and matches the file's existing conventions. The module-level `SPEC_0018_LEGACY_NEEDLES: [&str; 6]` (`init.rs:794-801`) mirrors prior module-level constants `SHIPPED_PERSONA_SECURITY` (`:26`) and `SKILL_NAMES: [&str; 7]` (`:59`) — same SCREAMING_SNAKE naming, same fixed-size `[&str; N]` shape, doc comment in the same `///`-block voice; no parallel helper invented when an existing pattern fit. The three walk-broadening tests reuse the already-imported `PERSONAS` / `PROMPTS` from `speccy_core` (`init.rs:19-20`) and the established `include_str!("../../...")` pattern rather than spinning up a new fixture helper. `entry.contents_utf8().unwrap_or("")` (`:856`, `:866`) is consistent with the pre-existing `after_close.strip_prefix('\n').unwrap_or(after_close)` at `:51` — `unwrap_or` is total and not the forbidden `.unwrap()` / `.expect()` panic path, so no AGENTS.md rule violation. No `#[allow(...)]` / `#[expect(...)]` suppressions added; the `unnecessary_wraps` transient flagged in the implementer note was correctly resolved by dropping the unused `TestResult` return rather than silencing the lint, matching AGENTS.md's "no `#[allow]` to silence a lint" directive. The mix of fallible `fn ... -> TestResult` and infallible bare-signature tests in the new pins matches what already exists in the file: positive content pins that need no fallible setup use the bare signature, consistent with how `init.rs` already shapes content-only assertions. `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all --check` both clean on the retry tree (re-verified). One non-blocking observation, not a T-005 obligation: `init.rs` is now 21 tests at >900 lines and the SPEC-0018-specific block (`:777-948`) is a natural future split into `tests/spec_0018_legacy.rs` if SPEC-0019 piles on more init-side guards.

<task-scenarios>
  - When `.speccy/ARCHITECTURE.md` is inspected, then checks are
    described as English validation scenarios, `speccy check` is
    render-only, and `speccy verify` is shape-only.
  - The "Feedback, Not Enforcement" section explicitly says project
    CI owns test execution and reviewer personas own semantic
    judgment about scenario quality and test coverage.
  - When active docs and shipped prompts are searched for
    check-authoring examples, then `kind =`, `command =`, and
    `prompt =` do not appear except in historical migration notes.
  - Active examples use `scenario = """..."""` and do not tell
    planners, implementers, reviewers, or shippers that Speccy runs
    project tests.
  - The reviewer-tests persona tells reviewers to compare
    implementation and tests against scenario prose, not against
    command exit codes.
</task-scenarios>
</task>

<task id="T-006" state="completed" covers="REQ-003 REQ-005">
Refresh materialized host packs and lock CI expectations

- Suggested files: `.github/workflows/ci.yml`, `speccy-cli/tests/ci_workflow.rs`, `speccy-cli/tests/init.rs`, `.claude/skills/`, `.claude/agents/`, `.agents/skills/`, `.codex/agents/`, `.speccy/skills/`
- Implementer note (session-T006-ci-lock):
  - Completed: Re-ran `speccy init --force --host claude-code` and `speccy init --force --host codex` in the checkout (14 overwritten + 20 skipped each). The resulting diff against the committed tree touched only the eight files T-005 already staged (`.claude/agents/reviewer-tests.md`, `.claude/skills/speccy-work/SKILL.md`, the codex-side mirrors at `.codex/agents/reviewer-tests.toml` and `.agents/skills/speccy-work/SKILL.md`, plus the four `.speccy/skills/personas/{implementer,planner,reviewer-tests}.md` / prompts that were already authored under `resources/modules/`); no new drift surfaced. `dogfood_outputs_match_committed_tree` in `speccy-cli/tests/init.rs` keeps the renderer-vs-committed-tree byte oracle green. Audited `.github/workflows/ci.yml`: it already runs `cargo test --workspace --all-features --locked`, `cargo clippy --workspace ... -- -D warnings`, `cargo +nightly fmt --all --check`, and `cargo deny check` as their own steps before `./target/debug/speccy verify`, so the workflow file itself needed no edit to match the new contract — the change for T-006 is locking that ordering into a test so a future contributor cannot collapse the gates back into a `speccy verify` wrapper. Added two new tests to `speccy-cli/tests/ci_workflow.rs`: `project_test_commands_run_directly_before_speccy_verify` (asserts each of the four project test commands is found in the workflow body at an index strictly less than the `./target/debug/speccy verify` index) and `speccy_verify_step_does_not_run_project_tests` (locates the `- name: speccy verify` line and asserts the immediately-following `run:` line is exactly `run: ./target/debug/speccy verify` — no chained `cargo` invocations). Added `rendered_outputs_have_no_legacy_check_authoring_examples` to `speccy-cli/tests/init.rs`: walks every file emitted by `render_host_pack` (both hosts) and rejects any line containing `kind = "test"`, `kind = "command"`, `kind = "manual"`, or `proves =`. (The unsubstituted-MiniJinja-token guard and the dogfood byte-identity guard were already in place from SPEC-0016 T-013; they are the existing `rendered_outputs_have_no_unsubstituted_tokens` and `dogfood_outputs_match_committed_tree` tests and need no change.) Operational verification: `speccy check SPEC-0018` renders all five SPEC-0018 scenarios with no child-process output, and `speccy verify` reports `verified 20 specs, 110 requirements, 148 scenarios; 0 errors`.
  - Undone: (none) — the test surface locks every T-006 obligation that does not require an edit to ci.yml itself.
  - Review (security, pass): T-006's surface is purely additive test code locking CI-workflow shape and host-pack renderer output; no authn/authz, secrets, deserialization of untrusted input, network, or crypto surface is touched. `speccy-cli/tests/ci_workflow.rs:51-136` reads the workflow body via `include_str!("../../.github/workflows/ci.yml")` (compile-time, trusted source) and only performs `find`/index-ordering assertions on hard-coded literal needles — no shell-out, no path traversal, no untrusted parsing. `speccy-cli/tests/init.rs:786-816` walks `render_host_pack` outputs (already-trusted internal renderer) for four hard-coded legacy-field-name needles; no filesystem writes or external I/O. The ci.yml itself was not modified (verified by `git diff HEAD -- .github/workflows/ci.yml` returning empty), so existing action pinning (`actions/checkout@vN`, `Swatinem/rust-cache@v2`, etc.) and `permissions:` blocks are unchanged from the prior security baseline. The new `speccy_verify_step_does_not_run_project_tests` assertion (`ci_workflow.rs:97-128`) is actually a mild security positive — it locks `speccy verify` to a literal single-command `run:` line, preventing a future contributor from chaining arbitrary `cargo` invocations (or worse) into that step under the cover of "shape-gate cleanup". No blocking findings.
  - Commands run:
    - `cargo run --quiet -- init --force --host claude-code`
    - `cargo run --quiet -- init --force --host codex`
    - `cargo test -p speccy-cli --test ci_workflow --test init`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo +nightly fmt --all --check`
    - `cargo run --quiet -- verify`
    - `cargo run --quiet -- check SPEC-0018`
  - Exit codes:
    - pass (14 overwritten, 20 skipped)
    - pass (14 overwritten, 20 skipped)
    - pass (5/5 ci_workflow, 15/15 init)
    - pass (all suites green)
    - pass (no warnings)
    - pass (only the pre-existing `Unknown configuration option float_literal_trailing_zero` rustfmt warnings; no diffs)
    - pass (`0 errors`, `148 scenarios`)
    - pass (5 scenarios rendered)
  - Discovered issues: (none)
  - Procedural compliance: (none) — no skill-layer friction surfaced; the test-shape additions are direct work for REQ-003/REQ-005.
  - Review (business, pass): T-006 satisfies REQ-003 and REQ-005 as scoped. CI ordering is locked by `project_test_commands_run_directly_before_speccy_verify` and `speccy_verify_step_does_not_run_project_tests` in `speccy-cli/tests/ci_workflow.rs`; `.github/workflows/ci.yml` already runs the four hygiene gates as their own steps before `./target/debug/speccy verify`. Host packs match renderer output via the existing `dogfood_outputs_match_committed_tree` byte oracle, and `rendered_outputs_have_no_legacy_check_authoring_examples` guards against legacy authoring snippets leaking back in. Operational checks (`speccy check SPEC-0018` renders 5 scenarios with no child processes; `speccy verify` reports `0 errors`) both behave per the SPEC. Minor: REQ-005 names `command =` and `prompt =` as retired authoring forms, but the needle set covers only `kind = "..."` variants and `proves =`; current rendered tree has zero hits for either, and the byte-identity oracle backstops future drift, so this is non-blocking.
  - Review (tests, blocking): T-006's tests lock the CI-shape obligations (bullet 2) and the dogfood byte-oracle (bullet 1) cleanly, but the bullet-3 guard `rendered_outputs_have_no_legacy_check_authoring_examples` (`speccy-cli/tests/init.rs:788-816`) ships with the same coverage hole the T-005 reviewer-tests note already flagged as blocking and the T-006 implementer note does not address. The `legacy_needles` array (`init.rs:796-801`) is `["kind = \"test\"", "kind = \"command\"", "kind = \"manual\"", "proves ="]` — it omits the bare `command = ` and `prompt = ` needles even though the T-005 bullet 3 wording explicitly names "`kind =`, `command =`, and `prompt =`". A regression PR that re-introduced a `command = "cargo test"` row or a `prompt = "Did the change land?"` row in any shipped persona/prompt body would render through `render_host_pack` and pass this test — exactly the surface bullet 3 protects. Mentally rewrite the renderer to emit `command = "rm -rf ~"` inside a persona body and the suite stays green. Fix: extend `legacy_needles` to include `"command = "` and `"prompt = "` (with the trailing space to avoid false positives on English prose like "prompt for the user"). Separately, bullet 4 ("`speccy check SPEC-0018` ... exits without spawning child processes") has no automated assertion — the implementer relies on T-004's removal of `speccy_core::exec` from `check.rs` as a structural guarantee, which holds today but is not pinned by a test that would fail if a future contributor reintroduced `std::process::Command` into the check path. A lightweight `assert_cmd` test that runs `speccy check SPEC-0018` and asserts the five scenario headers appear (plus a source-level grep guard on `speccy-cli/src/check.rs` for `Command::new`/`spawn`) would close that gap. Without these additions, bullet 3 is honor-system for half its contract and bullet 4 has no regression guard at all.
  - Review (style, pass): T-006's new tests in `speccy-cli/tests/ci_workflow.rs:54-135` and `speccy-cli/tests/init.rs:777-816` track the project's existing patterns. The `for host in [HostChoice::ClaudeCode, HostChoice::Codex]` walk in `rendered_outputs_have_no_legacy_check_authoring_examples` mirrors `dogfood_outputs_match_committed_tree` (`init.rs:622-660`) byte-for-byte in shape, including the `render_host_pack(host).map_err(|err| format!(...))` idiom. `TestResult` return with `?` propagation matches the rest of `init.rs`; backtick-quoted `format!` failure messages match `ci_workflow.rs`'s pre-existing `materialization_step_runs_both_hosts_then_diffs_committed_roots`. Only `.expect("descriptive message")` is used — no `.unwrap()`, no `panic!()`, no `#[allow]` or `#[expect(...)]` suppressions, no `[i]` indexing (the `lines.get(idx.saturating_add(1))` access is correct). Two minor nits, neither blocking: (1) `legacy_needles: [&str; 4]` carries an explicit length annotation where `dogfood_outputs_match_committed_tree` and the rest of `init.rs` use bare array literals — a `[&str; _]` or untyped `let` would match the surrounding style more tightly; (2) `idx.saturating_add(1)` defends against an overflow that cannot occur for a `usize` line index into an `include_str!`'d workflow, but the defensive style is consistent with the file and harmless. `cargo fmt --check` and `cargo clippy -- -D warnings` are reported green by the implementer. No drift from `AGENTS.md` or `.claude/rules/rust/*`.
- Retry: tests reviewer flagged two coverage gaps. (a) Bullet 3's `legacy_needles` in `speccy-cli/tests/init.rs:796-801` omits `"command = "` and `"prompt = "` (trailing space to avoid prose false-positives) — same hole as T-005's retry; both can be fixed in the same edit. (b) Bullet 4 ("`speccy check SPEC-0018` exits without spawning child processes") has no automated assertion; add a lightweight `assert_cmd`-style test that runs `speccy check SPEC-0018`, asserts the expected scenario headers appear, and asserts a source-level grep guard on `speccy-cli/src/check.rs` for `Command::new` / `spawn` returns no hits, so a future regression that reintroduces process spawning into the check path fails the suite. Business, security, and style reviewers passed; only the regression-guard gaps block.
- Implementer note (session-T006-retry):
  - Completed: Closed bullet 4's regression gap. Verified gap (a) was already resolved by T-005's retry — `SPEC_0018_LEGACY_NEEDLES` in `speccy-cli/tests/init.rs:794` is now a module-level `[&str; 6]` including `"command = \""` and `"prompt = \""` (with opening quote, strictly more conservative than the trailing-space form the review note suggested) and is reused by `rendered_outputs_have_no_legacy_check_authoring_examples` plus three sibling sources-level guards (`init.rs:838`, `857`, `867`); no further edit to `init.rs` needed. Added one new test `check_spec_0018_renders_scenarios_without_spawning_processes` to `speccy-cli/tests/check.rs:765-820`. It pairs two assertions covering REQ-002 from complementary angles: (1) a compile-time grep guard via `include_str!("../src/check.rs")` rejecting any occurrence of `Command::new`, `process::Command`, or `.spawn(` in production check source; (2) a runtime guard that runs `speccy check SPEC-0018` from the repo root (derived from `CARGO_MANIFEST_DIR`'s parent) via `assert_cmd::Command::cargo_bin` and asserts exit 0 plus the `scenarios rendered across` summary line. The static guard is the load-bearing assertion — even if the runtime test starts passing accidentally, reintroducing subprocess APIs into `check.rs` would still fail the suite at the source level. Asserts on the summary substring rather than each of the five `==> CHK-NNN` headers so future additions/reorderings inside SPEC-0018 don't churn the guard.
  - Undone: (none) — both reviewer-flagged gaps are now locked.
  - Commands run:
    - `cargo test -p speccy-cli --test check check_spec_0018_renders_scenarios_without_spawning_processes`
    - `cargo test --workspace`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo +nightly fmt --all --check`
    - `cargo run --quiet -- verify`
  - Exit codes:
    - pass (1/1)
    - pass (all suites green)
    - pass (no warnings)
    - pass (only the pre-existing `Unknown configuration option float_literal_trailing_zero` rustfmt warnings, no diffs)
    - pass (`verified 20 specs, 110 requirements, 148 scenarios; 0 errors`)
  - Discovered issues: (none) — the bullet (a) gap that the retry note named was already closed by T-005's retry (`SPEC_0018_LEGACY_NEEDLES` was hoisted to a module-level 6-element array reused by four sources-level guards). The retry note appears to have been authored before that companion fix landed.
  - Procedural compliance: (none) — no skill-layer friction surfaced; the new test follows the existing `binary_renders_headers_and_summary` / `binary_chk_099_no_match_preserves_no_check_matching_wording` cargo-bin pattern in the same file.
  - Review (business, pass): T-006 retry closes bullet 4 of the task's "Tests to write" list ("`speccy check SPEC-0018` ... renders SPEC-0018's scenarios and exits without spawning child processes"), which also doubles as a regression guard on REQ-002's Behavior contract ("no child process output"). `check_spec_0018_renders_scenarios_without_spawning_processes` (`speccy-cli/tests/check.rs:786-816`) pairs a compile-time grep against `Command::new` / `process::Command` / `.spawn(` in `speccy-cli/src/check.rs` with a runtime `assert_cmd` invocation of `speccy check SPEC-0018` asserting exit 0 and the `scenarios rendered across` summary — the static guard is the load-bearing pin, the runtime guard exercises the rendered path end-to-end. The retry note correctly observes that gap (a) was already closed by T-005's retry (`SPEC_0018_LEGACY_NEEDLES` lifted to a module-level six-element array including `"command = \""` and `"prompt = \""`), so no T-006 obligation remains undone. Asserting on the summary substring rather than each `==> CHK-NNN` header is the right call — SPEC-0018's check count is allowed to evolve (this very spec's `spec.toml` is migrated by T-004), and pinning headers would force test churn on every scenario edit without strengthening the no-spawn contract. No non-goal breached (the test adds no `--exec` surface, no execution helper, no quality grading); no Open Question silently resolved; the CI-maintainer user story ("`speccy verify` is a fast structural gate that runs beside the real test suite, not instead of it") is strengthened by locking the no-subprocess promise into the suite that gates merges.
  - Review (security, pass): T-006 retry is purely additive test code with no new security surface, and it strengthens the post-SPEC-0018 security baseline. `check_spec_0018_renders_scenarios_without_spawning_processes` (`speccy-cli/tests/check.rs:786-816`) reads `speccy-cli/src/check.rs` via `include_str!` at compile time (trusted in-tree source, no runtime path-traversal or untrusted-deserialization surface) and asserts the production check path contains none of `Command::new`, `process::Command`, or `.spawn(` — this is a load-bearing regression guard on the deletion of the pre-SPEC-0018 arbitrary-command-execution sink (the `shell_command` / `/bin/sh -c <repo-string>` path that T-002/T-004 removed). The runtime half spawns the `speccy` binary via `assert_cmd::Command::cargo_bin` with hard-coded argv `["check", "SPEC-0018"]` (no shell, no untrusted input) and `current_dir` set to the workspace root derived from `CARGO_MANIFEST_DIR` — both compile-time-trusted sources. The needle set is appropriately scoped to the production module under review: it covers the three idiomatic forms a regression would take (`std::process::Command::new`, fully-qualified `process::Command`, and the `.spawn(` call on any process-like builder) without over-broad matching that would false-positive on prose. No secrets, credentials, env-vars, logs, network, crypto, or auth surface is touched; no new dependencies; tests use `.expect("descriptive")` and `?` propagation only. Net delta is a security positive — a future contributor cannot silently reintroduce subprocess execution into `check.rs` without failing the suite at the source level, even if the runtime test happens to still pass.
  - Review (tests, pass): Both flagged gaps are closed. (a) `SPEC_0018_LEGACY_NEEDLES` at `speccy-cli/tests/init.rs:794-801` is the promised module-level `[&str; 6]` including `"command = \""` and `"prompt = \""`; the opening-quote form is strictly tighter than the trailing-space variant the retry note suggested (matches example-code shapes like `command = "cargo test"` while ignoring English prose). The constant is reused by four sweeps — `rendered_outputs_have_no_legacy_check_authoring_examples` (`init.rs:804-825`), `architecture_doc_has_no_legacy_check_authoring_examples` (`init.rs:836-844`), and the `PERSONAS` / `PROMPTS` source sweep (`init.rs:853-874`) — so a regression reintroducing `command = "rm -rf ~"` into any persona body, prompt body, or `.speccy/ARCHITECTURE.md` now flips the suite red. Verified `cargo test -p speccy-cli --test init rendered_outputs_have_no_legacy_check_authoring_examples` reports `1 passed`. (b) `check_spec_0018_renders_scenarios_without_spawning_processes` at `speccy-cli/tests/check.rs:786-816` pairs a compile-time grep over `include_str!("../src/check.rs")` (rejecting `Command::new`, `process::Command`, `.spawn(`) with an `assert_cmd::Command::cargo_bin("speccy")` runtime invocation asserting exit 0 and the `scenarios rendered across` summary substring. The static guard is load-bearing: mentally pasting `let _ = std::process::Command::new("ls");` into `check.rs` would flip the test red even if the runtime invocation still rendered correctly. Verified `cargo test -p speccy-cli --test check check_spec_0018_renders_scenarios_without_spawning_processes` reports `1 passed`, and `grep -nE "Command::new|process::Command|\.spawn\(" speccy-cli/src/check.rs` returns no hits — the assertion is meaningfully gating present-state, not stale. Asserting the summary substring rather than each of the five `==> CHK-NNN` headers is correctly scoped to bullet 4's no-spawn contract; pinning headers would couple this guard to SPEC-0018's evolving check count without strengthening it. Static grep scope (`speccy-cli/src/check.rs` only) matches what the prior retry asked for; a spawning helper introduced via a different module would still have to be wired into the check path (the `speccy_core::exec` removal is the structural backstop) or it would surface through the runtime exit-0 + summary assertion. No remaining blockers.
  - Review (style, pass): T-006 retry's `check_spec_0018_renders_scenarios_without_spawning_processes` (`speccy-cli/tests/check.rs:786-816`) tracks the existing conventions in this file cleanly. The `Command::cargo_bin("speccy")? ... cmd.assert().success().stdout(contains(...))` shape matches the surrounding `binary_*` tests at `:692/712/733/754` byte-for-byte; the `Utf8PathBuf::from(env!("CARGO_MANIFEST_DIR"))` derivation reuses the `camino::Utf8PathBuf` import already present at `:22` rather than reaching for `std::path::PathBuf`, consistent with `rust-code-quality.md`'s UTF-8-paths-everywhere rule. The `include_str!("../src/check.rs")` compile-time grep oracle is a strictly stronger guard than a filesystem read would be — a future contributor cannot fake the source out from under it — and `CHECK_SOURCE` is a module-level `const &str` in the same SCREAMING_SNAKE / `///`-doc voice as other top-of-file constants in the suite. `TestResult` return with `?` propagation matches the rest of the file; the `forbidden_needles: [&str; 3]` carries an explicit length where the surrounding file leans on bare array literals, but it mirrors the same annotation `SPEC_0018_LEGACY_NEEDLES: [&str; 6]` already uses in `tests/init.rs:794`, so it is internally consistent with the SPEC-0018-era code added across both files. No `.unwrap()` / `.expect()` / `panic!()` / `#[allow]` / `#[expect(...)]` suppressions; the `.ok_or("...")?` on `manifest_dir.parent()` propagates as `Box<dyn Error>` via `TestResult` rather than panicking, matching `rust-defensive-programming.md`'s safe-path-access guidance. The two-assertion pairing (compile-time grep + runtime invocation) is documented in a block comment at `:762-780` explaining why both belong in one test, which closes the "what does this prove" question a future reader would otherwise have to derive. `cargo +nightly fmt --all --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` are clean on the retry tree (re-verified); `cargo test -p speccy-cli --test check check_spec_0018_renders_scenarios_without_spawning_processes` passes. No drift from `AGENTS.md` or `.claude/rules/rust/*`.

<task-scenarios>
  - When `speccy init --force --host claude-code` and
    `speccy init --force --host codex` run in Speccy's checkout,
    then materialized `.claude/`, `.agents/`, `.codex/`, and
    `.speccy/skills/` outputs match the updated `resources/modules/`
    sources.
  - When `.github/workflows/ci.yml` is inspected, then project test
    commands (`cargo test`, clippy, fmt, and `cargo deny check`) run
    directly before `speccy verify`; CI does not rely on `speccy
    verify` to execute them.
  - When active generated host packs are searched, then they contain
    no unsubstituted MiniJinja tokens and no legacy check-authoring
    examples outside historical SPEC/TASKS records.
  - When `speccy check SPEC-0018` runs after the migration, then it
    renders SPEC-0018's scenarios and exits without spawning child
    processes.
  - When `speccy verify` runs after the migration, then it exits zero
    for the post-SPEC workspace shape.
</task-scenarios>
</task>

</tasks>
