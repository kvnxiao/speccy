---
spec: SPEC-0059
task: T-005
generated_at: 2026-06-11T07:58:53Z
---

<implementer date="2026-06-11T07:58:53Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the task area (`resources/modules/skills/speccy-plan.md` + `speccy-cli/tests/authoring_commit.rs`). Reuse-as-is — `{% include "modules/references/branch-guard.md" %}` and `{% include "modules/references/commit-recipe.md" %}` (the shared modules from T-001/T-002), pulled exactly as the T-004 decompose phase does; the test helpers `panic_with_message`, `RESOURCES.get_file`, and `render_host_pack(HostChoice::ClaudeCode)` already in `authoring_commit.rs`. Write-fresh — new `plan_body()` reader plus the four `plan_*`/`rendered_plan_*` tests (searched `authoring_commit.rs`: it carried T-001..T-004 only, no plan reader or plan-scoped tests). New top-level symbols this round: `plan_body` (mirrors the existing `decompose_body`/`review_fanout_body` readers — extend-by-analogy of that pattern) and four `#[test]` fns (no existing plan-scoped test to reuse).
- Completed: Added a new step 5 to `resources/modules/skills/speccy-plan.md` — after the step-3 self-review pass and before the next-step suggestion (renumbered old 5→6) — running the branch-guard prelude then the shared commit recipe, parameterised to narrow `git add <spec-dir>/SPEC.md` staging and the title `[SPEC-NNNN]: create spec`. Left the `speccy vacancy --json` ID-allocation step (step 1) untouched. Ran `just reeject` to regenerate `.claude/skills/speccy-plan/SKILL.md` and `.agents/skills/speccy-plan/SKILL.md`. Added four tests + a `plan_body()` reader to `speccy-cli/tests/authoring_commit.rs` and extended the crate `//!` header to name T-005.
- Undone: Nothing in scope deferred. T-006 (amend) and T-007 remain pending per the sibling index; out of scope for this task.
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 (caught one `doc_nested_refdefs` on a doc-link bullet, fixed with the `[]` form matching the existing T-003 bullet); `cargo +nightly fmt --all --check` exit 0 (applied `cargo +nightly fmt --all` to reflow the new doc-comment lines); `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-005.md` with a red→green trail (red captured against the pre-edit HEAD plan skill, green after edit + reeject). Roll call for CHKs under REQ-003:
  - CHK-004 (the speccy-plan commit step invokes the shared recipe with the `[SPEC-NNNN]: create spec` title and SPEC.md-only narrow staging, runs after the self-review pass; plus the task-scenarios' render-expansion and vacancy-preservation properties): demonstrated → evidence Scenarios 1–2 (`plan_includes_shared_commit_recipe_and_branch_guard`, `plan_stages_spec_md_alone_with_create_spec_title_after_self_review`), Scenario 4 (`rendered_plan_skill_fully_expands_includes`) for the render side, and Scenario 3 (`plan_retains_vacancy_id_allocation`) for the CHK-015 vacancy-not-status preservation property the task body pins.
- Discovered issues: None. The branch-guard self-reference recursion that bit T-004 (its line-6 live include) was already fixed in that task; speccy-plan's two live includes expand cleanly.
- Procedural compliance: Edited only under `resources/` then ran `just reeject`; did not hand-edit `.claude`/`.agents`/`.codex`. Followed speccy-work first-attempt branch (clean tree, no journal at entry). Pre-empted the two sibling blockers flagged by the caller: wrote a real `evidence/T-005.md` red→green file and updated the crate-level `//!` header to cover T-005. No friction in the shipped skill prose to report.
</implementer>
<review date="2026-06-11T08:00:39Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No injection surface introduced by the plan commit step or its two included modules.
Branch-name derivation (`git switch -c spec-NNNN-slug`, `branch-guard.md:88`) takes the Speccy-allocated spec-dir basename (`NNNN-slug`), not free-form input; staging is a fixed literal path `git add <spec-dir>/SPEC.md` (`speccy-plan.md` step 5) under a Speccy-controlled directory — no whole-tree `git add -A`/`git add .`, so unrelated dirty paths stay untouched. The shared commit recipe uses a quoted heredoc `git commit -m "$(cat <<'EOF' ...)"` (`commit-recipe.md:63`), which is the correct hardening: `$(...)`, backtick, and `$VAR` are not expanded, so the one file-sourced value (the SPEC.md frontmatter `title:`) cannot drive command substitution. No auth boundary, secret handling, network call, or untrusted external input is added; git runs in the developer's trust domain. The no-git short-circuit (`git rev-parse --is-inside-work-tree`) fails closed by skipping silently. Rendered-output tests assert no residual MiniJinja markup, ruling out unexpanded-template leakage.
</review>
<review date="2026-06-11T08:00:45Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defect found. The new commit step (renumbered 5) in `resources/modules/skills/speccy-plan.md:127` runs the branch-guard prelude then the shared commit recipe, sitting after the step-3 self-review pass and step-4 open-questions surfacing, before the renumbered step-6 next-step suggestion — so the committed SPEC.md is complete and CHK-004's after-self-review ordering holds. Step numbering 1–6 is contiguous (old next-step 5→6, no gap or duplicate). Idempotency is sound: `git add <spec-dir>/SPEC.md` is a no-op on unchanged content, `git diff --cached --quiet` skips when nothing is staged, and a re-run hits the branch-guard reuse path (HEAD already on the feature branch) — no spurious commit, no resource left dangling. Branch-guard control flow is exhaustive: no-git short-circuit precedes any mutation, the three-tier default-branch detection has explicit fall-through, and create-vs-reuse covers default/detached vs all other branches. No self-recursive include — the line-5 path reference in `branch-guard.md` is quoted prose, not a live directive — and the rendered `.claude`/`.agents` SKILL.md expand with no residual MiniJinja markup. The `speccy vacancy --json` ID allocation (step 1) is untouched. All 20 tests in `speccy-cli/tests/authoring_commit.rs` pass.
</review>
<review date="2026-06-11T08:01:01Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS — the diff delivers REQ-003 as specified. The new step 5 in
`resources/modules/skills/speccy-plan.md:127` runs the branch-guard
prelude then the shared commit recipe parameterised to narrow
`git add <spec-dir>/SPEC.md` staging (explicitly forbidding `git add
-A`/`git add .`) and the title `[SPEC-NNNN]: create spec`, positioned
after the step-3 self-review pass and before the renumbered step-6
next-step suggestion. All three `<done-when>` items map cleanly:
create-spec title + SPEC.md-only changed paths (narrow staging),
the `Co-Authored-By` trailer via the included commit-recipe's
identity-sourcing rule, and idempotency via the recipe's unified
`git diff --cached --quiet` skip-if-empty (the skill prose also
states re-running on an unchanged SPEC.md produces no new commit).
The `<behavior>` happy and unchanged-tree paths and CHK-004's three
properties are all present. The vacancy-not-status invariant (task
constraint) is preserved — step 1 still calls `speccy vacancy --json`
and no `speccy status --json` appears. Rendered `.claude` and
`.agents` plan skills are fully expanded with no residual MiniJinja
markup (verified both). The four `plan_*` tests pass and are
non-vacuous (they gate staging path, title, ordering, vacancy
preservation, and render-expansion). No non-goal breached (no CLI
change, no mode toggle, no confirmation prompt, no dirty-tree
refusal), no open question silently resolved, and no scope creep into
sibling-task source files — the 698-line `authoring_commit.rs` diff is
the full file's branch lineage (T-001..T-005), with T-005 adding only
the `plan_body()` reader and four plan-scoped tests.
</review>
<review date="2026-06-11T08:01:13Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests genuinely gate CHK-004 over the embedded RESOURCES bundle and the live render path; all four pass against the tree and would catch a wrong implementation.
`plan_includes_shared_commit_recipe_and_branch_guard` (authoring_commit.rs:584) asserts the literal `commit-recipe.md` and branch-guard include directives — a hand-rolled inline recipe fails. `plan_stages_spec_md_alone_with_create_spec_title_after_self_review` (authoring_commit.rs:606) pins the `[SPEC-NNNN]: create spec` title, the narrow `git add <spec-dir>/SPEC.md` staging, the absence of TASKS.md staging (DEC-005), and the document-order self-review-before-recipe positional check — wrong title, broad/TASKS.md staging, or a misplaced commit step all fail. `plan_retains_vacancy_id_allocation` (authoring_commit.rs:650) gates both presence of `speccy vacancy --json` and absence of `speccy status --json`, catching a regression of the CHK-015 invariant. `rendered_plan_skill_fully_expands_includes` (authoring_commit.rs:670) renders fresh via `render_host_pack(ClaudeCode)` (not the possibly-stale ejected file) and gates zero residual MiniJinja markup plus presence of the recipe's `git diff --cached --quiet` text — proving the includes expand. Verified both included modules (`commit-recipe.md`, `branch-guard.md`) actually carry the asserted text, and the rendered `.claude/skills/speccy-plan/SKILL.md` expands branch-guard + commit-recipe + the nested identity-sourcing include with the step renumbered 5 to 6.
Evidence file `evidence/T-005.md` exists, is named in the implementer Evidence field, and carries a non-fabricated red-to-green trail: red output uses scoped per-test `cargo test --test authoring_commit <name>` commands (not the workspace hygiene run), shows materially distinct red (`FAILED. 0 passed; 1 failed`, custom assertion text) versus green (`... ok`), all test names appear in the diff, and the pre-edit HEAD plan skill genuinely had zero `commit-recipe.md`/`branch-guard.md`/`create spec` matches (confirmed via `git show HEAD`), so the red claims are plausible. Scenario 3 transparently labels itself a preservation check with no meaningful red — honest, not fabricated. CHK roll call covers CHK-004 (the only CHK under the sole covered REQ-003) labelled demonstrated with specific test cites.
</review>
<review date="2026-06-11T08:01:16Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
`speccy-cli/tests/authoring_commit.rs:1` uses `#![allow(clippy::expect_used, reason = "...")]` while every sibling test file in `speccy-cli/tests/` uses `#![expect(clippy::expect_used, reason = "...")]`. The project convention in AGENTS.md is explicit: "Don't add `#[allow(...)]` to silence a lint. Use `#[expect(..., reason = "...")]` so the suppression is auto-removed when the underlying issue resolves." The other five sibling test files (`skill_packs.rs`, `skill_body_discovery.rs`, `pin_shape.rs`, `persona_snippets.rs`, `skill_stub_shape.rs`) all use `#![expect(...)]` at the crate level. This single file breaks the pattern.

No other style issues found. The source `resources/modules/skills/speccy-plan.md` correctly uses `{% include %}` directives for both `branch-guard.md` and `commit-recipe.md` rather than verbatim copies. The ejected packs (`.claude/skills/speccy-plan/SKILL.md` and `.agents/skills/speccy-plan/SKILL.md`) are fully expanded with no residual MiniJinja markup. The `//!` crate-level doc header in `authoring_commit.rs` names T-001 through T-005. The four new tests (`plan_includes_shared_commit_recipe_and_branch_guard`, `plan_stages_spec_md_alone_with_create_spec_title_after_self_review`, `plan_retains_vacancy_id_allocation`, `rendered_plan_skill_fully_expands_includes`) gate real structural properties. The `plan_body()` reader follows the same pattern as the existing `decompose_body()`, `review_fanout_body()`, etc. helpers. No dead imports, unused variables, or parallel-helper reinventions found.

Fix required: change `#![allow(` to `#![expect(` on line 1 of `speccy-cli/tests/authoring_commit.rs`.
</review>
