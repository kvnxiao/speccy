---
spec: SPEC-0059
task: T-006
generated_at: 2026-06-11T08:37:15Z
---

<implementer date="2026-06-11T08:37:15Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Area-mapped the three sibling commit steps (speccy-plan.md step 5, speccy-decompose.md step 4) and the authoring_commit.rs test file. Reuse-as-is: the `branch-guard.md` and `commit-recipe.md` include modules (existing, unchanged); the `panic_with_message`/body-reader test-helper pattern; `render_host_pack` + `RESOURCES`. Extend: the crate `//!` module-doc header (added a T-006 paragraph covering the new tests, per the caller's note that it named only T-001..T-005). Write-fresh: step 7 in speccy-amend.md (no amend commit step existed — modelled on the plan/decompose step shape; searched speccy-amend.md and found no `git add`/`commit-recipe`/`branch-guard` at HEAD); the four `amend_*`/`rendered_amend_*` test functions (new top-level symbols — patterned on the existing `plan_*` functions, no reusable existing helper covered them); evidence file `evidence/T-006.md` (none existed).
- Completed: Added step 7 to resources/modules/skills/speccy-amend.md — branch-guard prelude include + shared commit-recipe include, narrow `git add <paths>` staging (SPEC.md always, reconciled TASKS.md only when it exists, journal blocker files `<spec-dir>/journal/T-NNN.md` appended this run), title `[SPEC-NNNN]: amend — <why>` with `<why>` sourced from the newest `## Changelog` row, body = that row, runs after the step-6 `TSK-003`-clear check; absent TASKS.md tolerated. Added four tests to speccy-cli/tests/authoring_commit.rs and a T-006 paragraph to the crate `//!` header. Ran `just reeject` (1 overwritten: .claude/skills/speccy-amend/SKILL.md, plus the .agents/.codex parity copies). Wrote evidence/T-006.md.
- Undone: Nothing in scope deferred. T-007 (REQ-008/REQ-009) remains pending — out of scope here.
- Hygiene checks: cargo test --workspace -> exit 0; cargo clippy --workspace --all-targets --all-features -- -D warnings -> exit 0; cargo +nightly fmt --all --check -> exit 0 (after applying `cargo +nightly fmt --all` to reflow the doc comment); cargo deny check -> exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-006.md`. Roll call for CHKs under REQ-005:
  - CHK-006 (amend commit step invokes shared recipe with `[SPEC-NNNN]: amend — <why>` title, staging set = SPEC.md + TASKS.md-when-present + appended journal files, runs after TSK-003 clear, `<why>` from newest Changelog row): demonstrated -> evidence Scenarios 1-3 + 4 cover the red(no commit step at HEAD)->green(includes expand, title present, staging paths named, runs after TSK-003 check) pairs via `amend_includes_shared_commit_recipe_and_branch_guard`, `amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear`, `amend_stages_spec_md_and_tolerates_absent_tasks_md`, `rendered_amend_skill_fully_expands_includes`.
  - CHK-007 (absent TASKS.md tolerated — commits SPEC.md and journal files without requiring the tasks file): demonstrated -> evidence Scenario 3, `amend_stages_spec_md_and_tolerates_absent_tasks_md` asserts the TASKS.md-when-present qualification and the "without failing on the absent tasks file" prose.
- Discovered issues: My first draft of the narrow-staging negative assertion bare-substringed `git add -A`/`git add .`, which false-matched the step's own prose forbidding those forms (the same trap the sibling plan/decompose staging tests already note in comments). Relaxed it to anchor on the positive `git add <paths>` form plus the explicit path commands — documented in the evidence Scenario 3 note. Also collapsed interior whitespace in the CHK-007 prose assertion so it does not hinge on where the doc line-wraps.
- Procedural compliance: Edited only under resources/ then ran `just reeject` (never touched .claude/.agents/.codex by hand). File-level `#![allow(clippy::expect_used, ...)]` left as `allow` per the established convention and the caller's note. No skill/reference friction encountered; no resources/ instruction edits needed beyond the task's own target file.
</implementer>
<review date="2026-06-11T08:39:00Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pass — no exploitable injection surface in the new amend commit step. The three flagged vectors are clean:

- Commit-body source: `<why>` and body are sourced from the newest `## Changelog` row (`resources/modules/skills/speccy-amend.md:250-256`) and flow into the shared recipe's `git commit -m "$(cat <<'EOF' ... EOF)"` (`resources/modules/references/commit-recipe.md:62-71`). The heredoc delimiter is single-quoted (`<<'EOF'`), so the shell performs no expansion — changelog content containing `$(...)`, backticks, or `$VAR` lands verbatim in the message rather than executing. This is the injection-resistant form and matches the already-shipped plan/decompose/review callsites; no new exposure.
- Branch-name derivation: `git switch -c spec-NNNN-slug` (`resources/modules/references/branch-guard.md:87-95`) uses the literal `spec-` prefix plus the spec-dir basename as a fixed branch name with no shell interpolation or command substitution; the spec-dir path is a Speccy-convention path component, not an untrusted external input.
- Staging-path interpolation: narrow explicit `git add <spec-dir>/SPEC.md` / `<spec-dir>/journal/T-NNN.md`, forbidding `git add -A`/`git add .` (`speccy-amend.md:163-186`). Narrow staging is the safer choice — it cannot sweep unrelated dirty or secret-bearing files into the spec commit. Absent `TASKS.md` is dropped from the list rather than erroring.

The amend skill carries no independent `git commit` line and no broad `git add` — it delegates commit mechanics entirely to the shared quoted-heredoc recipe. No authn/authz, secret-handling, sensitive-data-exposure, or cryptographic surface, and no new dependency in this skill-prose change.
</review>
<review date="2026-06-11T08:39:02Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
PASS. REQ-005's three <done-when> clauses each map to the diff. (1) Commit title `[SPEC-NNNN]: amend — <why>` with the staging set SPEC.md + reconciled-TASKS.md-when-present + appended journal blockers — `resources/modules/skills/speccy-amend.md:155-204`. (2) Absent TASKS.md tolerated: lines 161-164 and 184-188 omit the missing tasks path from the stage "without failing on the absent tasks file" (CHK-007). (3) `<why>` sourced from the newest `## Changelog` row, not separately prompted (lines 190-196). Step 7 runs after the step-6 TSK-003-clear check (CHK-006 ordering), confirmed by source order and the passing `amend_titles_commit_and_sources_why_from_changelog_after_tsk003_clear` test. Goals honored: shared recipe pulled via `{% include %}` for both branch-guard and commit-recipe with no verbatim copy; narrow `git add <paths>` staging with `git add -A`/`git add .` forbidden. Non-goals respected: no CLI change, no mode toggle, no dirty-tree refusal/stash. SPEC Changelog (SPEC.md:629) carries a single initial row with no intent shift, so the diff reflects current intent. Rendered `.claude/skills/speccy-amend/SKILL.md` fully expands both includes with zero residual MiniJinja markup. All 24 `authoring_commit` tests pass.
</review>
<review date="2026-06-11T08:39:25Z" model="claude-opus-4-8[1m]" persona="tests" verdict="pass" round="1">
The four `amend_*`/`rendered_amend_*` tests in `speccy-cli/tests/authoring_commit.rs` genuinely gate CHK-006/CHK-007 over the real RESOURCES bundle and rendered pack, not vacuous prohibition-prose matches. Verified: (1) `amend_includes_shared_commit_recipe_and_branch_guard` asserts both `{% include %}` directives present — pre-edit source had neither (evidence red phase: 0 matches), genuinely red→green. (2) `amend_titles_commit_..._after_tsk003_clear` carries its red on the net-new `[SPEC-NNNN]: amend — <why>` title plus a meaningful `tsk003_pos < recipe_pos` document-ordering check anchored on the pre-existing `confirm \`TSK-003\` cleared` line (no spurious .expect() panic). (3) `amend_stages_spec_md_and_tolerates_absent_tasks_md` gates the SPEC.md/TASKS.md/journal staging paths and the CHK-007 distinguisher "without failing on the absent tasks file" — a wrong impl that required TASKS.md to exist would not carry that prose, so it is caught; correctly anchors the narrow-staging negative on the positive `git add <paths>` form rather than bare-substringing the forbidden `git add -A`/`git add .` (which appear legitimately in both the recipe's option list and the step's own prohibition). (4) `rendered_amend_skill_fully_expands_includes` exercises the real `render_host_pack` path, asserting no residual MiniJinja markup and the recipe's `git diff --cached --quiet` text in the rendered `.claude/skills/speccy-amend/SKILL.md` — confirmed expanded in the regenerated harness diff (`git switch -c`, recipe body, identity-sourcing all present). Since the behaviour is skill-pack prose (CLI never runs git, DEC-001), content-property assertions over the bundle/render are the correct and only test surface. Evidence file exists with a non-fabricated red→green trail: red/green halves differ materially (specific assertion message + `FAILED. 0 passed; 1 failed` → `... ok`), scoped per-test invocations not the hygiene full-suite, and every cited test name appears in the diff. Roll call lists CHK-006 and CHK-007, both `demonstrated` with specific test cites. Minor non-blocking note: the `body.contains("## Changelog")` assertion in test 2 is satisfied by the pre-edit source's pre-existing Changelog references, so it does not itself gate the "why sourced from newest row" behaviour — but it is not that test's load-bearing assertion (the title and ordering checks carry it).
</review>
<review date="2026-06-11T08:39:42Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. Reviewed the new step 7 in resources/modules/skills/speccy-amend.md (and its expanded .claude/.agents/.codex renders) for control-flow/logic defects in my lane. Findings:
- Ordering is correct: the reconcile-delta commit (step 7) runs after the TASKS.md reconcile (step 4), after the spec-hash re-record (`speccy lock`, step 5), and after the TSK-003-clear check (step 6), matching REQ-005/CHK-006. Because `speccy lock` writes the re-recorded hash into TASKS.md frontmatter (no separate lock-file artifact), staging the reconciled TASKS.md captures the lock result with no dangling unstaged hash file.
- Branch-guard prelude precedes the commit-recipe staging on every path, so the commit cannot land on the default branch; create-vs-reuse and the no-git short-circuit are inherited verbatim from branch-guard.md.
- Absent-TASKS.md handling is the correct control-flow: the prose omits the missing path from the narrow `git add <paths>` list rather than passing it, avoiding the `git add <missing>` non-zero exit that would abort the stage (CHK-007). SPEC.md plus journal files still commit.
- Idempotency is delegated to the shared recipe's single stage-then-`git diff --cached --quiet` skip; no double-check, no inverted exit-code handling (exit 0 = nothing staged = skip).
- Includes fully expand with no residual MiniJinja `{%`/`{{` markup; host renders differ only by the expected cmd_prefix / references-path substitutions, leaving step-7 logic byte-identical across hosts.
</review>
<review date="2026-06-11T08:40:57Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
Pass. All style invariants satisfied.

Checked:
- `resources/modules/skills/speccy-amend.md`: step 7 uses `{% include %}` directives for both `branch-guard.md` and `commit-recipe.md`; no verbatim recipe copy; narrow `git add <paths>` staging; correct `[SPEC-NNNN]: amend — <why>` title format; TASKS.md conditional staging with absent-file tolerance prose present.
- Ejected packs (`.agents/skills/speccy-amend/SKILL.md`, `.claude/skills/speccy-amend/SKILL.md`): fully expanded — no residual `{{`, `{%`, or `{#` markup in changed sections.
- `speccy-cli/tests/authoring_commit.rs`: crate-level `//!` doc names T-001..T-006 (line 5). Four T-006 test functions are non-vacuous: each asserts a real structural invariant (include presence via exact string match, ordering via `find` + positional compare, staging path names, absent-TASKS.md phrasing), none re-asserts a production constant, none checks only existence/non-emptiness.
- `#![allow(clippy::expect_used, reason = "...")]` matches the majority sibling pattern (check.rs, context.rs, journal_append.rs, etc.). `#[expect(clippy::panic, reason = "...")]` on `panic_with_message` is the correct suppression form per AGENTS.md.
- No unused imports; all three (`RESOURCES`, `HostChoice`, `render_host_pack`) are called in the test bodies.
- No reinvented helpers; `panic_with_message` / `amend_body()` follow the established pattern from prior T-001..T-005 body-reader helpers.
- Reuse survey present in the implementer block and consistent with what the diff calls.
</review>
