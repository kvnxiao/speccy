---
spec: SPEC-0059
task: T-002
generated_at: 2026-06-11T07:04:22Z
---

<implementer date="2026-06-11T07:04:22Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Area = the reference-module set under `resources/modules/references/` plus its embedded-bundle test harness `speccy-cli/tests/authoring_commit.rs` (T-001's sibling). Three things added: (1) `branch-guard.md` module body — write-fresh; searched `resources/modules/references/` for existing default-branch-detection / `git switch -c` / branch-creation prose, none present (only `commit-recipe.md` carries a no-git probe, and that is commit-specific; REQ-010/CHK-013 explicitly wants a branch-guard side too). Mirrored `commit-recipe.md`'s structure and its "not a git repository" / "without erroring" no-git phrasing for consistency. (2) `branch_guard_body()` test fixture — extends the T-001 pattern (`commit_recipe_body()`); reuse-as-is of the existing `panic_with_message` helper at `authoring_commit.rs:61`. (3) Three `branch_guard_*` tests — extend the existing test file rather than a new file (same embedded-RESOURCES assertion style as the three `commit_recipe_*` siblings).
- Completed: Created `resources/modules/references/branch-guard.md` defining the branch-guard prelude: a `git rev-parse --is-inside-work-tree` no-git short-circuit (REQ-010/CHK-013); an ordered three-tier default-branch detection chain (`origin/HEAD` via `git symbolic-ref`, then `git config init.defaultBranch`, then a `main`/`master` name match), each gated on the prior not resolving (REQ-002/CHK-003); a branch-creation condition that derives `spec-` + spec-dir basename (focus segment dropped for mission-foldered specs), `git switch -c`es on the default-branch-or-detached condition, reuses any other branch, and emits the one-line creation notice only on the create path (REQ-001/CHK-001/CHK-002). All illustrative values fictional (`spec-NNNN-slug`, `0042-example-slug`, `acme/`). Added three tests + a `branch_guard_body()` fixture to `speccy-cli/tests/authoring_commit.rs` (one per task scenario). Confirmed `just reeject` renders clean (0 created, 0 overwritten, 36 unchanged).
- Undone: Nothing in scope. No callsite is wired — the three authoring consumers (plan/decompose/amend) land in T-004/T-005/T-006 per the task body, so this module produces no ejected output yet (expected).
- Hygiene checks: `cargo test --workspace` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo +nightly fmt --all --check` exit 0 (after applying `cargo +nightly fmt --all` to rewrap one doc-comment line); `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-002.md`. Roll call for CHKs under REQ-001, REQ-002, REQ-010:
  - CHK-003 (three-tier detection chain named in order, each gated on prior not resolving): demonstrated → evidence Scenario 1 covers the red/green pair via `branch_guard_names_three_detection_tiers_in_order` (byte-offset ordering `origin/HEAD` < `init.defaultBranch` < `` `main` or `master` `` + gating-phrase assertion) in `speccy-cli/tests/authoring_commit.rs`.
  - CHK-002 (create-on-default-or-detached, reuse otherwise, notice only on create path): demonstrated → evidence Scenario 2 covers `branch_guard_states_creation_condition_and_notice` (asserts `spec-`+`basename` derivation, `git switch -c`, `detached`, reuse path, `notice`, `only on the create path`). Subsumes CHK-001's derivation property.
  - CHK-013 (branch-guard side no-git short-circuit): demonstrated → evidence Scenario 3 covers `branch_guard_specifies_no_git_short_circuit` (`git rev-parse --is-inside-work-tree` probe + `not a git repository`/`without erroring` contract). Commit-recipe side was demonstrated in T-001.
- Discovered issues: None. The brittle-substring trap I hit during authoring (a bare `body.find("main")` matched the illustrative `origin/main` in tier 1, not the tier-3 fallback set) was caught at red→green and fixed before handoff by anchoring tier 3 on the distinctive `` `main` or `master` `` phrasing.
- Procedural compliance: Followed speccy-work first-attempt branch (clean tree confirmed, journal absent → first-attempt shape). Edited only under `resources/` and `speccy-cli/tests/`; ran `just reeject` (clean). Note: `include_dir!` did not pick up the brand-new `branch-guard.md` on the first incremental build — a `touch speccy-cli/src/embedded.rs` forced the recompile that re-snapshotted the resources tree. This is a known incremental-build wrinkle of `include_dir!` adding a new file, not a defect in this change; flagging for any later contributor who sees a stale "RESOURCES bundle must contain ..." panic after adding a resource file.
</implementer>
<review date="2026-06-11T07:05:53Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blockers. The module defines branch-guard prose that drives
only read-only / local-branch git ops (`rev-parse --is-inside-work-tree`,
`symbolic-ref`, `config init.defaultBranch`, `switch -c`): no secrets,
auth boundaries, network calls, or sensitive-data exposure. The no-git
probe is the correct non-destructive `git rev-parse --is-inside-work-tree`
with a graceful skip and no error surfaced
(`resources/modules/references/branch-guard.md:21-34`). Command-injection
surface (branch name = `spec-` + spec-dir basename fed to `git switch -c`,
lines 79-89) is constrained by the `NNNN-slug` slug convention and uses a
literal placeholder, not shell interpolation; actual interpolation lands at
the T-004/T-005/T-006 callsites, so any quoting / `--` discipline belongs
there, not in this definition-only module. The test file adds no production
code. All illustrative values are fictional placeholders, satisfying the
no-real-identifiers rule for downstream-shipped modules.
</review>
<review date="2026-06-11T07:06:09Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests pass. The three `branch_guard_*` tests in `speccy-cli/tests/authoring_commit.rs` (uncommitted WIP, lines 122-241) drive every covered scenario over the real embedded `RESOURCES` bundle, not a mock. CHK-003: `branch_guard_names_three_detection_tiers_in_order` asserts byte-offset ordering `origin/HEAD` < `init.defaultBranch` < `` `main` or `master` `` plus the gating phrase — a reordered chain in `branch-guard.md:42/53/63` would fail it. CHK-002/CHK-001 derivation: `branch_guard_states_creation_condition_and_notice` asserts `spec-`+`basename`, `git switch -c`, `detached`, the reuse path, and the `only on the create path` notice constraint, each present at `branch-guard.md:81-101`; dropping the create-only-notice clause fails it. CHK-013: `branch_guard_specifies_no_git_short_circuit` asserts the `git rev-parse --is-inside-work-tree` probe + `not a git repository`/`without erroring` contract at `branch-guard.md:27-31`. Ran `cargo test --test authoring_commit` read-only: 6 passed, 0 failed — matches the evidence green half. Evidence at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-002.md` is a genuine red-then-green trail: the red fixture-lookup panic text matches the real `panic_with_message` string, named tests all appear in the diff, red (3 failed) and green (6 passed) outputs differ materially, and the commands are scoped per-test rather than the full hygiene run — no fabrication signals. Roll call is complete for the covered REQs: CHK-003/CHK-002/CHK-013 demonstrated, CHK-001's in-scope derivation property correctly folded into CHK-002 (its callsite-inclusion half is out of scope per the task body, landing in T-004/T-005/T-006). These are content-property assertions over a shipped skill-pack module — the module text is the behavioral contract, so this is the endorsed structural-content-check form, not vacuous prose substring-matching.
</review>
<review date="2026-06-11T07:06:09Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pass. The branch-guard prelude at resources/modules/references/branch-guard.md describes a procedure whose control flow is correct on every reachable path. No-git short-circuit (lines 21-34) gates the whole prelude on `git rev-parse --is-inside-work-tree` exiting non-zero, so non-git projects skip without erroring. The three-tier detection chain (lines 36-66) is ordered origin/HEAD -> init.defaultBranch -> {main,master} with each tier explicitly gated on the prior not resolving; the tier-1 prose correctly treats both "no remote" and "origin/HEAD unset" as non-resolution (the `git symbolic-ref` probe exits non-zero in both), so the fall-through is sound. The create-condition (lines 68-102) reads current HEAD via `git symbolic-ref --quiet --short HEAD`, treats its non-zero exit as detached, and takes the create path on default-branch-OR-detached, reuse otherwise — independent of detection, so a detached HEAD with no remote and no init.defaultBranch still creates a branch (no inverted condition or missed edge). No off-by-one, no swallowed-error arm doing the wrong thing, no fall-through that skips a required step. Test-quality and prose-content concerns are out of my lane (tests/style personas).
</review>
<review date="2026-06-11T07:06:23Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
Pass. The diff delivers what the three covered requirements promise. REQ-001 done-when maps cleanly to `resources/modules/references/branch-guard.md`: branch derivation as `spec-` + spec-dir basename with the focus segment dropped for mission-foldered specs (lines 79-85, 104-107), create-on-default-or-detached then `git switch -c` (lines 79-89), reuse-on-other-branch (97-99), notice only on the create path (91-95, 101-102), and no confirmation prompt (12-13). REQ-002's three-tier detection chain is named in order with each tier gated on the prior not resolving — `origin/HEAD` (42-51), `init.defaultBranch` (53-61), `{main, master}` name match (63-66). REQ-010's branch-guard-side no-git short-circuit via `git rev-parse --is-inside-work-tree`, skip without erroring (21-34). Non-goals respected: no CLI change, no callsite wired (T-004/5/6 own that per the task body), no mode toggle, no confirmation prompt; working-tree changes are limited to the module, the three `branch_guard_*` tests, the evidence file, the journal, and the TASKS.md state flip. No open question silently resolved — all five brainstorm questions are recorded as resolved in SPEC Open Questions (615-622), and the "mission-foldered → flat" resolution is exactly what the module implements. Changelog (624-630) shows a single initial entry, so the diff reflects current intent. The SPEC's concrete `0059` mission-foldered done-when example is covered by the module's general basename rule; using the real spec id in the shipped module would violate the placeholder rule, so its absence is correct.
</review>
<review date="2026-06-11T07:06:40Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
One vacuous assertion arm in `speccy-cli/tests/authoring_commit.rs`.

`branch_guard_names_three_detection_tiers_in_order` (line 171-175 in the on-disk file) contains:

```rust
assert!(
    lower.contains("does not resolve") || lower.contains("otherwise"),
    "branch-guard.md must gate each detection tier on the prior one not \
     resolving (CHK-003 'each tier used only when the prior does not resolve')",
);
```

The `|| lower.contains("otherwise")` arm is vacuous. The word "otherwise" appears in virtually every reference-prose document (it appears at least four times in `branch-guard.md` itself). The alternative can never drive a test failure: as long as "otherwise" is anywhere in the file, the assertion passes regardless of whether any gating language is present. Per AGENTS.md "Don't write vacuous tests" — a test must gate a real invariant, not include a fallback that is always true. Remove the `|| lower.contains("otherwise")` arm and assert only on the distinctive gating phrase `"does not resolve"`, which is already present in `branch-guard.md` at the module and is the phrase the error message quotes as the expected invariant.

No other style findings: `branch-guard.md` follows `commit-recipe.md`'s structure and naming conventions; fictional placeholders are used throughout; the three new test functions mirror the T-001 `commit_recipe_*` pattern; the `branch_guard_body()` fixture correctly reuses the existing `panic_with_message` helper.
</review>
<blockers date="2026-06-11T07:07:14Z" round="1">
Remove the vacuous assertion arm in the branch-guard detection-order test before the next pass.

- **style**: In `speccy-cli/tests/authoring_commit.rs`, the test `branch_guard_names_three_detection_tiers_in_order` asserts `lower.contains("does not resolve") || lower.contains("otherwise")`. The `|| lower.contains("otherwise")` arm is vacuous — "otherwise" appears throughout `branch-guard.md` (≥4 times) and any reference-prose file, so the disjunction can never fail regardless of whether the gating language is present. Per AGENTS.md "Don't write vacuous tests," drop the `|| lower.contains("otherwise")` arm and assert only on the distinctive gating phrase `"does not resolve"` (already present in the module and quoted by the error message as the expected CHK-003 invariant).
</blockers>
<implementer date="2026-06-11T07:10:24Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Retry round addressing a single non-reuse style blocker; no new top-level symbol introduced, so the full area-map is not re-run (per round semantics — re-survey only on a reuse-related blocker). The amend touches one existing assertion arm in `branch_guard_names_three_detection_tiers_in_order`.
- Completed: Removed the vacuous `|| lower.contains("otherwise")` disjunct from the detection-order gating assertion in `speccy-cli/tests/authoring_commit.rs` (the style blocker). The test now asserts only on the distinctive gating phrase `body.contains("does not resolve")`, which the module carries three times (lines 40/51/61). Dropped the now-unused `let lower = body.to_lowercase();` that fed only the removed arm; the other two branch_guard tests keep their own `lower` bindings. No production code or module prose changed — the blocker was test-quality only.
- Undone: Nothing in scope. The branch-guard module body is unchanged and correct from round 1; no callsite is wired (T-004/T-005/T-006 own that).
- Hygiene checks: `cargo test --workspace` exit 0 (authoring_commit: 6 passed, 0 failed; branch_guard_names_three_detection_tiers_in_order green); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 (no unused-variable warning from the dropped `lower`); `cargo +nightly fmt --all --check` exit 0; `cargo deny check` exit 0.
- Evidence: paper trail at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-002.md`. CHK roll call unchanged from round 1 — this amend hardened the CHK-003 assertion (it can now fail if the module drops the `does not resolve` gating language) without altering what each CHK demonstrates:
  - CHK-003 (three-tier detection chain named in order, each gated on prior not resolving): demonstrated → `branch_guard_names_three_detection_tiers_in_order` now asserts byte-offset ordering plus the non-vacuous `does not resolve` gating phrase.
  - CHK-002 (create-on-default-or-detached, reuse otherwise, notice only on create path): demonstrated → `branch_guard_states_creation_condition_and_notice`. Subsumes CHK-001's derivation property.
  - CHK-013 (branch-guard side no-git short-circuit): demonstrated → `branch_guard_specifies_no_git_short_circuit`.
- Discovered issues: None.
- Procedural compliance: Followed speccy-work retry branch — read the latest round-1 blocker directive, amended the prior pass's uncommitted WIP in place (no restore/clean/reset), flipped state via `speccy task transition` (in-progress to in-review), edited only under `speccy-cli/tests/`. No `resources/` change this round, so no `just reeject` needed.
</implementer>
<review date="2026-06-11T07:11:54Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security blockers. Round 2 changed only the test file
(`speccy-cli/tests/authoring_commit.rs`): the vacuous
`|| lower.contains("otherwise")` arm and its now-unused `lower` binding
were dropped from `branch_guard_names_three_detection_tiers_in_order` —
test-quality only, no production/shipped behaviour change, no new
dependency, no new attack surface. The branch-guard module body
(`resources/modules/references/branch-guard.md`) is unchanged from round
1 and still drives only read-only / local-branch git ops:
`rev-parse --is-inside-work-tree` (line 27), `symbolic-ref` (46/73),
`config init.defaultBranch` (57), `switch -c` (88) — no secrets, auth
boundaries, network calls, or sensitive-data exposure. The no-git probe
is the correct non-destructive `git rev-parse --is-inside-work-tree`
with a graceful skip and no error surfaced (lines 21-34). Command-
injection surface (branch name = `spec-` + spec-dir basename fed to
`git switch -c`, lines 79-89) is a literal placeholder here, not shell
interpolation; actual interpolation/quoting discipline lands at the
T-004/T-005/T-006 callsites, out of scope for this definition-only
module. All illustrative values are fictional placeholders, satisfying
the no-real-identifiers rule for downstream-shipped modules.
</review>
<review date="2026-06-11T07:11:56Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pass (round 2). The round-2 amend touches only the test file; the branch-guard module prose at resources/modules/references/branch-guard.md is byte-unchanged from round 1 and its control flow remains correct on every reachable path — no-git short-circuit gating the whole prelude on `git rev-parse --is-inside-work-tree` (lines 21-34), ordered three-tier detection with sound fall-through where each tier treats non-resolution correctly (lines 36-66), and a create-on-default-or-detached vs reuse condition with no inverted branch (lines 68-102). The amend to `branch_guard_names_three_detection_tiers_in_order` (speccy-cli/tests/authoring_commit.rs:141-175) drops the always-true `|| lower.contains("otherwise")` disjunct and the now-unused `lower` binding; the remaining assertions (`find`-based `tier1 < tier2 < tier3` ordering with each `.find` guarded by `.expect`, plus `body.contains("does not resolve")`) carry no off-by-one or swallowed-error logic. No correctness defect in scope. Test-quality and prose-content judgments belong to the tests/style personas.
</review>
<review date="2026-06-11T07:12:00Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Pass (round 2). The round-2 amend addressed only the style blocker (dropped the vacuous `|| lower.contains("otherwise")` arm in `branch_guard_names_three_detection_tiers_in_order`, now asserting solely `does not resolve`) without touching the branch-guard module body, so the round-1 business coverage holds and no requirement regressed.

The diff delivers every <done-when> for the three covered requirements via `resources/modules/references/branch-guard.md`:
- REQ-001: branch derived as `spec-` + spec-dir basename with the focus segment dropped for mission-foldered specs (lines 79-85, 104-107); create-on-default-or-detached then `git switch -c` (79-89); reuse-on-other-branch with no notice (97-102); no confirmation prompt (12-13). The SPEC's concrete `0059` mission-foldered done-when example is correctly expressed as the general basename rule — embedding the real spec id in a shipped module would violate the no-real-identifiers rule, so its absence is correct.
- REQ-002: three-tier chain named in order with each tier gated on the prior not resolving — `origin/HEAD` (42-51), `init.defaultBranch` (53-61), `{main, master}` name match (63-66).
- REQ-010: branch-guard-side no-git short-circuit via `git rev-parse --is-inside-work-tree`, skip without erroring (21-34).

Non-goals honoured: no CLI change (prose-only module), no callsite wired (T-004/5/6 own that per the task body), no mode toggle, no confirmation prompt, no stash/refuse-on-dirty. Open Questions are "None" (SPEC.md:615-618) and the mission-foldered-flat resolution is exactly what the module implements; the single Changelog row reflects current intent. No scope sneaked in.
</review>
<review date="2026-06-11T07:12:08Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Tests pass; round-1 style blocker fixed. The vacuous
`|| lower.contains("otherwise")` arm is gone from
`branch_guard_names_three_detection_tiers_in_order`
(`speccy-cli/tests/authoring_commit.rs:170-174`); it now asserts only
the distinctive gating phrase `body.contains("does not resolve")`, which
the module carries three times (`branch-guard.md:40/51/61`) — dropping
the gating language now fails the test, so CHK-003 gates a real
invariant. The now-unused `let lower` binding was correctly removed from
that function; the other two tests keep their own bindings.

All three covered scenarios are exercised over the real embedded
RESOURCES bundle, not a mock. CHK-003:
`branch_guard_names_three_detection_tiers_in_order` asserts byte-offset
ordering `origin/HEAD` < `init.defaultBranch` < `` `main` or `master` ``
(tier 3 anchored on the distinctive phrasing, correctly dodging the
illustrative `origin/main` in tier 1) plus the gating phrase — a
reordered chain fails it. CHK-002/CHK-001 derivation:
`branch_guard_states_creation_condition_and_notice` asserts
`spec-`+`basename` derivation, `git switch -c`, `detached`, the
reuse path, and the `only on the create path` notice constraint
(`branch-guard.md:79-102`); dropping the create-only-notice clause fails
it. CHK-013: `branch_guard_specifies_no_git_short_circuit` asserts the
`git rev-parse --is-inside-work-tree` probe plus the `not a git
repository`/`without erroring` contract (`branch-guard.md:27-31`).

These are content-property assertions over a shipped skill-pack template
module whose text is the behavioral contract — the endorsed
structural-content-check form per AGENTS.md, not vacuous human-prose
substring matching; the task body itself names `authoring_commit.rs` and
the scenarios are read-the-module-body assertions.

Evidence at `.speccy/specs/0059-authoring-branch-and-commit/evidence/T-002.md`
is a genuine red-then-green trail: the red fixture-lookup panic text
matches the real `panic_with_message` string at `authoring_commit.rs:131-132`,
all named tests appear in the file, red (3 failed) and green (6 passed)
differ materially, and commands are scoped per-test rather than the full
hygiene run — no fabrication signals. Roll call is complete for the
covered REQs: CHK-003/CHK-002/CHK-013 demonstrated with named tests;
CHK-001's derivation half folded into CHK-002, its callsite-inclusion
half correctly deferred to T-004/T-005/T-006 per the task body. Minor
non-blocking nit: the evidence Scenario-1 green prose still mentions the
old `does not resolve`/`otherwise` pair from round 1 and was not
re-touched after the amend narrowed the assertion to `does not resolve`
only; the executable assertion is correct and non-vacuous.
</review>
<review date="2026-06-11T07:12:11Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Prior blocker resolved: the vacuous `|| lower.contains("otherwise")` arm has been removed from `branch_guard_names_three_detection_tiers_in_order` (speccy-cli/tests/authoring_commit.rs:168-174). The test now asserts only on the distinctive gating phrase `body.contains("does not resolve")`, which is non-vacuous — dropping the three "does not resolve" occurrences from `branch-guard.md` (lines 40, 51, 61) would fail it. The now-unused `let lower = body.to_lowercase();` binding was also removed, so no dead code is left. No other style changes in the round-2 diff. The remaining disjunctions (`lower.contains("reuse") || lower.contains("reuses")` and `lower.contains("only on the create path") || lower.contains("not on the reuse path")`) are legitimate — each arm is a non-trivially-true alternative phrasing of the same invariant, not a fallback that always passes. `branch-guard.md` continues to follow `commit-recipe.md`'s structure, uses fictional placeholders throughout, and introduces no naming drift. No new `#[allow]` suppressions introduced by T-002.
</review>
