# Evidence for SPEC-0059 T-005

Red-then-green paper trail for T-005: the new commit step added to
`resources/modules/skills/speccy-plan.md` (REQ-003) that, after the step-3
self-review pass, runs the branch-guard prelude via
`{% include "modules/references/branch-guard.md" %}` and then commits
`SPEC.md` alone via `{% include "modules/references/commit-recipe.md" %}`
with narrow `git add <spec-dir>/SPEC.md` staging and the title
`[SPEC-NNNN]: create spec`. The covered CHK (CHK-004) is a content-property
set of assertions over the embedded `RESOURCES` bundle and the rendered Claude
Code pack, proven by the four new `plan_*` / `rendered_plan_*` tests in
`speccy-cli/tests/authoring_commit.rs`.

The red phase is captured against the pre-edit tree: at HEAD,
`speccy-plan.md` had no commit step at all — step 4 went straight from the
self-review pass (step 3) to the `## Open Questions` surface, with no
`{% include %}` directives, no `[SPEC-NNNN]: create spec` title, and no
`git add`. The new tests were authored first and run against that pre-edit
source (which is the live source the `include_dir`-embedded `RESOURCES` bundle
compiled against before the `speccy-plan.md` edit landed), then re-run green
after the edit + `just reeject`.

## Session 2026-06-11-T005

### Pre-edit source inspection (HEAD skill, before the commit step)

```
$ git show HEAD:resources/modules/skills/speccy-plan.md \
    | grep -cE 'commit-recipe.md|branch-guard.md'
0
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -n 'create spec'
(no output)
$ git show HEAD:resources/modules/skills/speccy-plan.md | grep -n 'git add'
(no output)
$ git show HEAD:resources/modules/skills/speccy-plan.md \
    | grep -nE 'Self-review|next step'
61:3. **Self-review pass.** Run this pass exactly once after writing
75:   `- [ ] {ordinal}. **Self-review caught:** {issue}`
127:5. Suggest the next step: `{{ cmd_prefix }}speccy-decompose SPEC-NNNN` to
```

The pre-edit skill carries the self-review pass (step 3) and jumps from the
`## Open Questions` step (step 4) to the next-step suggestion (step 5) with no
commit step, no includes, no `create spec` title, and no `git add`.

### Scenario 1 — Shared recipe + branch-guard pulled via `{% include %}` (CHK-004 recipe property, REQ-003)

<red>
Pre-edit: `speccy-plan.md` at HEAD has no commit step and neither include, so
the include-present assertion fails.

```
$ cargo test --test authoring_commit plan_includes_shared_commit_recipe_and_branch_guard
---- plan_includes_shared_commit_recipe_and_branch_guard ----
speccy-plan.md must pull the shared commit recipe via
`{% include "modules/references/commit-recipe.md" %}` (CHK-004); the SPEC.md
commit step must delegate to the shared recipe
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the new step 5 pulls both the branch-guard prelude and the shared
commit recipe via `{% include %}`.

```
$ cargo test --test authoring_commit plan_includes_shared_commit_recipe_and_branch_guard
test plan_includes_shared_commit_recipe_and_branch_guard ... ok
```
</green>

### Scenario 2 — `[SPEC-NNNN]: create spec` title, narrow SPEC.md-only staging, after the self-review pass (CHK-004)

<red>
Pre-edit: the skill has neither the `[SPEC-NNNN]: create spec` title nor a
narrow `git add <spec-dir>/SPEC.md` staging command, so the title assertion
fails first.

```
$ cargo test --test authoring_commit plan_stages_spec_md_alone_with_create_spec_title_after_self_review
---- plan_stages_spec_md_alone_with_create_spec_title_after_self_review ----
speccy-plan.md must title the commit `[SPEC-NNNN]: create spec` (CHK-004)
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit: the commit titles `[SPEC-NNNN]: create spec`, stages
`git add <spec-dir>/SPEC.md` narrowly (no `git add -A`/`git add .`, TASKS.md
absent from the staging set per DEC-005), and the `Self-review pass` heading
still precedes the recipe include in document order.

```
$ cargo test --test authoring_commit plan_stages_spec_md_alone_with_create_spec_title_after_self_review
test plan_stages_spec_md_alone_with_create_spec_title_after_self_review ... ok
```
</green>

### Scenario 3 — `speccy vacancy --json` ID allocation preserved (CHK-015 invariant)

<red>
This scenario is a *preservation* check: the prior CHK-015
`speccy-plan`-must-use-`vacancy`-not-`status` invariant must stay green across
this edit. At HEAD the invariant already holds, so this test passes both before
and after — there is no meaningful red against HEAD. The interesting failure it
guards against is a *regression* introduced by the commit-step edit (e.g. an
edit that replaced the `speccy vacancy --json` line, or added a stray
`speccy status --json`). Verified by running it against the post-edit source:
had the edit displaced the vacancy call or introduced a `status` call, this
test would have flipped red.

```
$ cargo test --test authoring_commit plan_retains_vacancy_id_allocation
test plan_retains_vacancy_id_allocation ... ok   (both pre- and post-edit)
```
</red>

<green>
Post-edit: step 1 still invokes `speccy vacancy --json` and no
`speccy status --json` appears; the commit step (step 5) sits below the
ID-allocation step and does not displace it.

```
$ cargo test --test authoring_commit plan_retains_vacancy_id_allocation
test plan_retains_vacancy_id_allocation ... ok
```
</green>

### Scenario 4 — Rendered plan skill fully expands both includes (CHK-004 render side)

<red>
Pre-edit: the rendered `.claude/skills/speccy-plan/SKILL.md` carries no
commit recipe, so the assertion that the rendered body contains the recipe's
`git diff --cached --quiet` text fails. (The pre-edit rendered body is also
MiniJinja-clean, since the pre-edit source had no includes — so the meaningful
red here is the missing recipe text, proving the include had not yet been added
and expanded.)

```
$ cargo test --test authoring_commit rendered_plan_skill_fully_expands_includes
---- rendered_plan_skill_fully_expands_includes ----
rendered `.claude/skills/speccy-plan/SKILL.md` must carry the recipe's
`git diff --cached --quiet` idempotency check, proving the commit-recipe
include expanded into the plan skill body
test result: FAILED. 0 passed; 1 failed
```
</red>

<green>
Post-edit + `just reeject`: the rendered `.claude/skills/speccy-plan/SKILL.md`
expands branch-guard, commit-recipe, and the nested identity-sourcing include
exactly once each, with zero residual `{{` / `{%` / `{#` markup, and carries
the recipe's `git diff --cached --quiet` text.

```
$ just reeject
$ cargo test --test authoring_commit rendered_plan_skill_fully_expands_includes
test rendered_plan_skill_fully_expands_includes ... ok
```
</green>

### Full file — all twenty tests green (T-001 + T-002 + T-003 + T-004 + T-005)

```
$ cargo test --test authoring_commit
test result: ok. 20 passed; 0 failed; 0 ignored
```

## Hygiene-gate evidence

```
$ cargo test --workspace
test result: ok. (all passed)
$ echo $?
0

$ cargo clippy --workspace --all-targets --all-features -- -D warnings
(no warnings)
$ echo $?
0

$ cargo +nightly fmt --all --check
$ echo $?
0

$ cargo deny check
$ echo $?
0
```

All four standard hygiene gates exited 0 after the T-005 diff landed.

## CHK roll call

- **CHK-004** (the `speccy-plan` commit step invokes the shared commit recipe
  with the title `[SPEC-NNNN]: create spec` and a staging set limited to the
  spec's `SPEC.md`, runs after the self-review pass, and — per the task's
  scenarios block — the rendered skill fully expands the includes and the
  `speccy vacancy --json` ID-allocation step is preserved):
  demonstrated → Scenarios 1–2,
  `plan_includes_shared_commit_recipe_and_branch_guard`,
  `plan_stages_spec_md_alone_with_create_spec_title_after_self_review`, plus
  Scenario 4's `rendered_plan_skill_fully_expands_includes` proving the recipe
  (which carries the `create spec` title and the single-parent HEREDOC commit)
  expands into the rendered plan skill body. The CHK-015 preservation property
  named by the task's scenarios block is demonstrated by Scenario 3
  (`plan_retains_vacancy_id_allocation`).
