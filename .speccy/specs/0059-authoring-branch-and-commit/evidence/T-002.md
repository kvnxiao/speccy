# Evidence for SPEC-0059 T-002

Red-then-green paper trail for T-002: creation of the shared branch-guard
prelude module at `resources/modules/references/branch-guard.md`. The covered
CHKs (CHK-003, CHK-002, CHK-013 branch-guard side) are content-property
assertions over the embedded `RESOURCES` bundle, proven by the three new
`branch_guard_*` tests in `speccy-cli/tests/authoring_commit.rs`.

## Session 2026-06-11-T002-attempt-1

### Scenario 1 — Three-tier default-branch detection chain, in order (CHK-003)

<red>
Pre-edit: the module file does not exist. The three `branch_guard_*` tests
fail at the `branch_guard_body()` fixture lookup because
`RESOURCES.get_file("modules/references/branch-guard.md")` returns `None`.

```
$ cargo test --test authoring_commit   # against tree without branch-guard.md
branch_guard_names_three_detection_tiers_in_order ... RESOURCES bundle must
contain `modules/references/branch-guard.md`; SPEC-0059 T-002 requires this
shared module to be created
test result: FAILED. 3 passed; 3 failed
```
</red>

<green>
Post-edit: the module exists. `branch_guard_names_three_detection_tiers_in_order`
locates the byte offsets of `origin/HEAD`, `init.defaultBranch`, and the
distinctive `` `main` or `master` `` tier-3 phrasing and asserts
`tier1 < tier2 < tier3`, then asserts the gating phrase (`does not resolve` /
`otherwise`) is present.

```
$ cargo test --test authoring_commit branch_guard_names_three_detection_tiers_in_order
test branch_guard_names_three_detection_tiers_in_order ... ok
```
</green>

### Scenario 2 — Branch-creation condition, derivation, and create-only notice (CHK-002, CHK-001)

<red>
Same fixture-lookup red as Scenario 1: with the module absent,
`branch_guard_states_creation_condition_and_notice` panics at
`branch_guard_body()`.
</red>

<green>
Post-edit: the test asserts the `spec-` prefix + `basename` derivation prose,
the `git switch -c` create command, the `detached` create condition, the
`reuse`/`reuses` reuse path, the `notice`, and the
`only on the create path` constraint all appear in the module body.

```
$ cargo test --test authoring_commit branch_guard_states_creation_condition_and_notice
test branch_guard_states_creation_condition_and_notice ... ok
```
</green>

### Scenario 3 — No-git-repository short-circuit (CHK-013 branch-guard side)

<red>
Same fixture-lookup red: with the module absent,
`branch_guard_specifies_no_git_short_circuit` panics at `branch_guard_body()`.
</red>

<green>
Post-edit: the test asserts the `git rev-parse --is-inside-work-tree` probe
token plus the lowercased `not a git repository` + `without erroring`
no-error contract phrasing are present.

```
$ cargo test --test authoring_commit branch_guard_specifies_no_git_short_circuit
test branch_guard_specifies_no_git_short_circuit ... ok
```
</green>

### Full file — all six tests green (T-001 + T-002)

```
$ cargo test --test authoring_commit
test result: ok. 6 passed; 0 failed; 0 ignored
```

## CHK roll call

- **CHK-003** (three-tier detection chain in order): demonstrated → Scenario 1,
  `branch_guard_names_three_detection_tiers_in_order` (byte-offset ordering +
  gating phrase).
- **CHK-002** (creation condition, derivation, create-only notice):
  demonstrated → Scenario 2,
  `branch_guard_states_creation_condition_and_notice`. Subsumes CHK-001's
  derivation property (`spec-` + spec-dir basename).
- **CHK-013** (branch-guard side, no-git short-circuit): demonstrated →
  Scenario 3, `branch_guard_specifies_no_git_short_circuit`. The
  commit-recipe side of CHK-013 was demonstrated in T-001.
