---
spec: SPEC-0038
task: T-003
generated_at: 2026-05-22T01:30:00Z
---

<implementer date="2026-05-22T01:30:00Z" model="claude-opus-4.7[1m]/low" round="1">
- Completed: Wired the seven canonical references from
  `resources/modules/references/` into both host packs via
  ejection-path option 1 (wrapper `.tmpl` stubs, per the SPEC's
  preferred path). Created 14 wrapper files (7 per host) under
  `resources/agents/.claude/skills/<skill>/references/<file>.md.tmpl`,
  `resources/agents/.claude/speccy-references/<file>.md.tmpl`, and
  their `.agents/` siblings. Each wrapper carries a single
  `{% include "modules/references/<file>.md" %}` directive. The
  existing `render_host_pack` walk under `agents/.<install_root>/`
  discovers them mechanically, so no changes were needed in
  `embedded.rs` (compile-time `include_dir!` picks up the new
  files) or in `init.rs` (the planning loop already iterates every
  `RenderedFile` returned by `render_host_pack`). Added
  `speccy_references_path` to `TemplateContext` in `host.rs`
  resolving to `.claude/speccy-references` for Claude Code and
  `.agents/speccy-references` for Codex (DEC-002 — Codex sub-agents
  under `.codex/agents/` still resolve to `.agents/...`). Extended
  the probe template and both `template_context_*_renders_expected_keys`
  tests to assert the new key. Refreshed the in-tree dogfood pack
  with `speccy init --force --host claude-code` and
  `speccy init --force --host codex`: 7 files created per host, 18
  unchanged.
- Undone: T-004 (consuming-body pointer lines), T-005 (reviewer
  audit), T-006 (orphan-references test), T-007 (ARCHITECTURE.md
  doc update) remain pending — out of scope for T-003.
- Hygiene checks: `cargo test --workspace` → all suites pass
  (initial run flagged `pin_shape.rs` panicking on missing
  frontmatter for reference `.md` files; fixed by filtering
  `/references/` and `/speccy-references/` paths in
  `collect_pin_records`, consistent with the SPEC's non-goal "No
  YAML frontmatter on reference files"). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` → clean.
  `cargo +nightly fmt --all --check` → clean (fmt rewrapped a
  doc-comment line in `host.rs`, applied with
  `cargo +nightly fmt --all`). `cargo deny check` → advisories ok,
  bans ok, licenses ok, sources ok.
- Evidence: T-003's task scenarios are observed by CHK-001 / CHK-002 /
  CHK-004 / CHK-005 / CHK-006 (`speccy check SPEC-0038/T-003`) plus
  the two probe-render tests in `host.rs`. Manual init runs against
  the repo root show 7 `created` entries per host for the new
  paths, and the parallel paths under `.claude/` vs `.agents/`
  share a single canonical source so byte-identity is guaranteed by
  construction (every wrapper is `{% include "modules/references/<file>.md" %}`).
- Discovered issues: `pin_shape.rs::scan_finds_expected_minimum_files`
  and the model/effort scans walk every `.md` under
  `.claude/skills` and `.agents/skills`. They previously assumed
  every such `.md` carries `---` frontmatter; the new reference
  files do not (per SPEC non-goal). Surgical fix: skip paths
  containing `/references/` or `/speccy-references/` in
  `collect_pin_records`. Comment cites SPEC-0038 so future
  contributors see the link.
- Procedural compliance: Followed the speccy-work primitive — one
  task, no iteration into T-004+. Used preferred ejection path
  (option 1) since the SPEC marks it as such and the byte-identical
  invariant in REQ-007 will catch any wrapper-vs-source drift. No
  shipped skill body edits in this turn (those are T-004's). No
  changes to `.speccy/ARCHITECTURE.md` (that is T-007).
</implementer>

<review date="2026-05-22T02:09:14Z" model="claude-opus-4-7[1m]" persona="business" verdict="pass" round="1">
T-003 satisfies REQ-001 + REQ-002 within scope. Seven reference files ship to both host packs via the SPEC-preferred option-1 wrapper path; `speccy_references_path` resolves to `.claude/speccy-references` and `.agents/speccy-references` per DEC-002; ARCHITECTURE.md doc update correctly deferred to T-007.
</review>

<review date="2026-05-22T02:09:14Z" model="claude-opus-4-7[1m]" persona="tests" verdict="pass" round="1">
Template-context expected-keys tests in `host.rs` extended for both Claude Code and Codex assert the new key renders correctly; `dogfood_outputs_match_committed_tree` walks `render_host_pack` end-to-end and covers the 14 new wrapper-rendered files; fresh-tempdir `speccy init --host claude-code` and `--host codex` produce all seven paths byte-identical across hosts and against the canonical source. The `chkNNN_no_orphan_references` test is explicitly T-006 scope.
</review>

<review date="2026-05-22T02:09:14Z" model="claude-sonnet-4-7[1m]/high" persona="security" verdict="pass" round="1">
No auth, input-validation, secret-handling, or crypto surface introduced. The `pin_shape.rs` skip filter for `/references/` and `/speccy-references/` substrings is the correct semantic exclusion given SPEC-0038's non-goal "No YAML frontmatter on reference files"; pin_shape's scan domain is unchanged.
</review>

<review date="2026-05-22T02:09:14Z" model="claude-sonnet-4-7[1m]/medium" persona="style" verdict="pass" round="1">
Four hygiene checks pass cleanly (`cargo test --workspace`, `clippy -D warnings`, `+nightly fmt --check`, `cargo deny check`). New `speccy_references_path` field documented in `template_context`'s docstring matching the existing pattern. Dead-code removal (`render_speccy_examples_pack`, `collect_all_files`, and the three sibling tests) is consistent with T-002's prior deletion of `resources/modules/examples/`. Path-normalization (`replace('\\', '/')`) in the new pin_shape filter is idiomatic for cross-platform substring matching. SPEC-0038 citation comment present on the filter per project conventions.
</review>
